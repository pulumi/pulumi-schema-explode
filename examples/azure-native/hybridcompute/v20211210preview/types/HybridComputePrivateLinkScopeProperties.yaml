$token: azure-native:hybridcompute/v20211210preview:HybridComputePrivateLinkScopeProperties
description: Properties that define a Azure Arc PrivateLinkScope resource.
properties:
  publicNetworkAccess:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:hybridcompute/v20211210preview:PublicNetworkAccessType"
    description: Indicates whether machines associated with the private link scope can also use public Azure Arc service endpoints.
type: object
