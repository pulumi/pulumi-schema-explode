$token: azure-native:hybridcompute/v20220310:OSProfileLinuxConfiguration
description: Specifies the linux configuration for update management.
properties:
  assessmentMode:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:hybridcompute/v20220310:AssessmentModeTypes"
    description: Specifies the assessment mode.
  patchMode:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:hybridcompute/v20220310:PatchModeTypes"
    description: Specifies the patch mode.
type: object
