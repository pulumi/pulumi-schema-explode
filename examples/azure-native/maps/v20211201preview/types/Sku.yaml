$token: azure-native:maps/v20211201preview:Sku
description: The SKU of the Maps Account.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:maps/v20211201preview:Name"
    description: The name of the SKU, in standard format (such as S0).
type: object
required:
  - name
