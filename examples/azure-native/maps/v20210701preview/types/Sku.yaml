$token: azure-native:maps/v20210701preview:Sku
description: The SKU of the Maps Account.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:maps/v20210701preview:Name"
    description: The name of the SKU, in standard format (such as S0).
type: object
required:
  - name
