$token: azure-native:storagesync/v20190201:SyncActivityStatusResponse
description: Sync Session status object.
properties:
  appliedBytes:
    type: number
    description: Applied bytes
  appliedItemCount:
    type: number
    description: Applied item count.
  perItemErrorCount:
    type: number
    description: Per item error count
  timestamp:
    type: string
    description: Timestamp when properties were updated
  totalBytes:
    type: number
    description: Total bytes (if available)
  totalItemCount:
    type: number
    description: Total item count (if available)
type: object
required:
  - appliedBytes
  - appliedItemCount
  - perItemErrorCount
  - timestamp
  - totalBytes
  - totalItemCount
