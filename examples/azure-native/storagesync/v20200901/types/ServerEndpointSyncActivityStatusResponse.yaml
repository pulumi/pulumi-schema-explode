$token: azure-native:storagesync/v20200901:ServerEndpointSyncActivityStatusResponse
description: Sync Session status object.
properties:
  appliedBytes:
    type: number
    description: Applied bytes
  appliedItemCount:
    type: number
    description: Applied item count.
  perItemErrorCount:
    type: number
    description: Per item error count
  sessionMinutesRemaining:
    type: integer
    description: Session minutes remaining (if available)
  syncMode:
    type: string
    description: Sync mode
  timestamp:
    type: string
    description: Timestamp when properties were updated
  totalBytes:
    type: number
    description: Total bytes (if available)
  totalItemCount:
    type: number
    description: Total item count (if available)
type: object
required:
  - appliedBytes
  - appliedItemCount
  - perItemErrorCount
  - sessionMinutesRemaining
  - syncMode
  - timestamp
  - totalBytes
  - totalItemCount
