$token: azure-native:devices/v20190701preview:getIotHubResource
description: The description of the IoT hub.
inputs:
  properties:
    resourceGroupName:
      type: string
      description: The name of the resource group that contains the IoT hub.
    resourceName:
      type: string
      description: The name of the IoT hub.
  type: object
  required:
    - resourceGroupName
    - resourceName
outputs:
  description: The description of the IoT hub.
  properties:
    etag:
      type: string
      description: The Etag field is *not* required. If it is provided in the response body, it must also be provided as a header per the normal ETag convention.
    id:
      type: string
      description: The resource identifier.
    location:
      type: string
      description: The resource location.
    name:
      type: string
      description: The resource name.
    properties:
      type: object
      $ref: "#/types/azure-native:devices/v20190701preview:IotHubPropertiesResponse"
      description: IotHub properties
    sku:
      type: object
      $ref: "#/types/azure-native:devices/v20190701preview:IotHubSkuInfoResponse"
      description: IotHub SKU info
    tags:
      type: object
      additionalProperties:
        type: string
      description: The resource tags.
    type:
      type: string
      description: The resource type.
  type: object
  required:
    - id
    - location
    - name
    - properties
    - sku
    - type
deprecationMessage: Version 2019-07-01-preview will be removed in v2 of the provider.
