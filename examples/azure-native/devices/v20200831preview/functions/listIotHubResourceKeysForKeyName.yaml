$token: azure-native:devices/v20200831preview:listIotHubResourceKeysForKeyName
description: The properties of an IoT hub shared access policy.
inputs:
  properties:
    keyName:
      type: string
      description: The name of the shared access policy.
    resourceGroupName:
      type: string
      description: The name of the resource group that contains the IoT hub.
    resourceName:
      type: string
      description: The name of the IoT hub.
  type: object
  required:
    - keyName
    - resourceGroupName
    - resourceName
outputs:
  description: The properties of an IoT hub shared access policy.
  properties:
    keyName:
      type: string
      description: The name of the shared access policy.
    primaryKey:
      type: string
      description: The primary key.
    rights:
      type: string
      description: The permissions assigned to the shared access policy.
    secondaryKey:
      type: string
      description: The secondary key.
  type: object
  required:
    - keyName
    - rights
