$token: azure-native:devices/v20200831preview:listIotHubResourceKeys
description: The list of shared access policies with a next link.
inputs:
  properties:
    resourceGroupName:
      type: string
      description: The name of the resource group that contains the IoT hub.
    resourceName:
      type: string
      description: The name of the IoT hub.
  type: object
  required:
    - resourceGroupName
    - resourceName
outputs:
  description: The list of shared access policies with a next link.
  properties:
    nextLink:
      type: string
      description: The next link.
    value:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:devices/v20200831preview:SharedAccessSignatureAuthorizationRuleResponse"
      description: The list of shared access policies.
  type: object
  required:
    - nextLink
