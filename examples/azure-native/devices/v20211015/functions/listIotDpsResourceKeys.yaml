$token: azure-native:devices/v20211015:listIotDpsResourceKeys
description: List of shared access keys.
inputs:
  properties:
    provisioningServiceName:
      type: string
      description: The provisioning service name to get the shared access keys for.
    resourceGroupName:
      type: string
      description: resource group name
  type: object
  required:
    - provisioningServiceName
    - resourceGroupName
outputs:
  description: List of shared access keys.
  properties:
    nextLink:
      type: string
      description: The next link.
    value:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:devices/v20211015:SharedAccessSignatureAuthorizationRuleAccessRightsDescriptionResponse"
      description: The list of shared access policies.
  type: object
  required:
    - nextLink
