$token: azure-native:devices/v20170821preview:IotDpsSkuInfo
description: List of possible provisioning service SKUs.
properties:
  capacity:
    type: number
    description: The number of services of the selected tier allowed in the subscription.
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:devices/v20170821preview:IotDpsSku"
type: object
