$token: azure-native:devices/v20170821preview:IotDpsPropertiesDescription
properties:
  allocationPolicy:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:devices/v20170821preview:AllocationPolicy"
    description: Allocation policy to be used by this provisioning service.
  authorizationPolicies:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:devices/v20170821preview:SharedAccessSignatureAuthorizationRuleAccessRightsDescription"
  iotHubs:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:devices/v20170821preview:IotHubDefinitionDescription"
    description: List of IoT hubs associated with this provisioning service.
  provisioningState:
    type: string
    description: The ARM provisioning state of the provisioning service.
  state:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:devices/v20170821preview:State"
    description: Current state of the provisioning service.
type: object
