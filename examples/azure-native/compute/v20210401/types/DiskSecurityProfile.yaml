$token: azure-native:compute/v20210401:DiskSecurityProfile
description: Contains the security related information for the resource.
properties:
  securityType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:compute/v20210401:DiskSecurityTypes"
    description: Specifies the SecurityType of the VM. Applicable for OS disks only.
type: object
