$token: azure-native:compute:CloudServiceOsProfile
description: Describes the OS profile for the cloud service.
properties:
  secrets:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:compute:CloudServiceVaultSecretGroup"
    description: Specifies set of certificates that should be installed onto the role instances.
type: object
