$token: azure-native:compute:CloudServiceRoleProfileResponse
description: Describes the role profile for the cloud service.
properties:
  roles:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:compute:CloudServiceRoleProfilePropertiesResponse"
    description: List of roles for the cloud service.
type: object
