$token: azure-native:compute:VirtualMachineNetworkInterfaceDnsSettingsConfigurationResponse
description: Describes a virtual machines network configuration's DNS settings.
properties:
  dnsServers:
    type: array
    items:
      type: string
    description: List of DNS servers IP addresses
type: object
