$token: azure-native:compute:CloudServiceExtensionProfile
description: Describes a cloud service extension profile.
properties:
  extensions:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:compute:Extension"
    description: List of extensions for the cloud service.
type: object
