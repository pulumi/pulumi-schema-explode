$token: azure-native:compute/v20211001:SharingStatusResponse
description: Sharing status of current gallery.
properties:
  aggregatedState:
    type: string
    description: Aggregated sharing state of current gallery.
  summary:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:compute/v20211001:RegionalSharingStatusResponse"
    description: Summary of all regional sharing status.
type: object
required:
  - aggregatedState
