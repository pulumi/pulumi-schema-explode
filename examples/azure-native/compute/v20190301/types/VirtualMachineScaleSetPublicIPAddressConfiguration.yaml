$token: azure-native:compute/v20190301:VirtualMachineScaleSetPublicIPAddressConfiguration
description: Describes a virtual machines scale set IP Configuration's PublicIPAddress configuration
properties:
  dnsSettings:
    type: object
    $ref: "#/types/azure-native:compute/v20190301:VirtualMachineScaleSetPublicIPAddressConfigurationDnsSettings"
    description: The dns settings to be applied on the publicIP addresses .
  idleTimeoutInMinutes:
    type: integer
    description: The idle timeout of the public IP address.
  ipTags:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:compute/v20190301:VirtualMachineScaleSetIpTag"
    description: The list of IP tags associated with the public IP address.
  name:
    type: string
    description: The publicIP address configuration name.
  publicIPPrefix:
    type: object
    $ref: "#/types/azure-native:compute/v20190301:SubResource"
    description: The PublicIPPrefix from which to allocate publicIP addresses.
type: object
required:
  - name
