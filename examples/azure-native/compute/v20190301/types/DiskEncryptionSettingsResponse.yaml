$token: azure-native:compute/v20190301:DiskEncryptionSettingsResponse
description: Describes a Encryption Settings for a Disk
properties:
  diskEncryptionKey:
    type: object
    $ref: "#/types/azure-native:compute/v20190301:KeyVaultSecretReferenceResponse"
    description: Specifies the location of the disk encryption key, which is a Key Vault Secret.
  enabled:
    type: boolean
    description: Specifies whether disk encryption should be enabled on the virtual machine.
  keyEncryptionKey:
    type: object
    $ref: "#/types/azure-native:compute/v20190301:KeyVaultKeyReferenceResponse"
    description: Specifies the location of the key encryption key in Key Vault.
type: object
