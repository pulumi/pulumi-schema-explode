$token: azure-native:compute/v20211101:KeyVaultKeyReferenceResponse
description: Describes a reference to Key Vault Key
properties:
  keyUrl:
    type: string
    description: The URL referencing a key encryption key in Key Vault.
  sourceVault:
    type: object
    $ref: "#/types/azure-native:compute/v20211101:SubResourceResponse"
    description: The relative URL of the Key Vault containing the key.
type: object
required:
  - keyUrl
  - sourceVault
