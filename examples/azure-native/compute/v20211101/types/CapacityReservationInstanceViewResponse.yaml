$token: azure-native:compute/v20211101:CapacityReservationInstanceViewResponse
description: The instance view of a capacity reservation that provides as snapshot of the runtime properties of the capacity reservation that is managed by the platform and can change outside of control plane operations.
properties:
  statuses:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:compute/v20211101:InstanceViewStatusResponse"
    description: The resource status information.
  utilizationInfo:
    type: object
    $ref: "#/types/azure-native:compute/v20211101:CapacityReservationUtilizationResponse"
    description: Unutilized capacity of the capacity reservation.
type: object
