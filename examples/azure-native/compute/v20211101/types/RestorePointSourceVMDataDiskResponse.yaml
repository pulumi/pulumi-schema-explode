$token: azure-native:compute/v20211101:RestorePointSourceVMDataDiskResponse
description: Describes a data disk.
properties:
  caching:
    type: string
    description: Gets the caching type.
  diskRestorePoint:
    type: object
    $ref: "#/types/azure-native:compute/v20211101:ApiEntityReferenceResponse"
    description: Gets the disk restore point Id.
  diskSizeGB:
    type: integer
    description: Gets the initial disk size in GB for blank data disks, and the new desired size for existing OS and Data disks.
  lun:
    type: integer
    description: Gets the logical unit number.
  managedDisk:
    type: object
    $ref: "#/types/azure-native:compute/v20211101:ManagedDiskParametersResponse"
    description: Gets the managed disk details
  name:
    type: string
    description: Gets the disk name.
type: object
