$token: azure-native:compute/v20201201:DiskSecurityProfile
description: Contains the security related information for the resource.
properties:
  securityType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:compute/v20201201:DiskSecurityTypes"
    description: Specifies the SecurityType of the VM. Applicable for OS disks only.
type: object
