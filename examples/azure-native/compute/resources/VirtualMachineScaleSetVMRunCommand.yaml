$token: azure-native:compute:VirtualMachineScaleSetVMRunCommand
description: |-
  Describes a Virtual Machine run command.
  API Version: 2021-03-01.
properties:
  asyncExecution:
    type: boolean
    description: Optional. If set to true, provisioning will complete as soon as the script starts and will not wait for script to complete.
    default: false
  errorBlobUri:
    type: string
    description: Specifies the Azure storage blob where script error stream will be uploaded.
  instanceView:
    type: object
    $ref: "#/types/azure-native:compute:VirtualMachineRunCommandInstanceViewResponse"
    description: The virtual machine run command instance view.
  location:
    type: string
    description: Resource location
  name:
    type: string
    description: Resource name
  outputBlobUri:
    type: string
    description: Specifies the Azure storage blob where script output stream will be uploaded.
  parameters:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:compute:RunCommandInputParameterResponse"
    description: The parameters used by the script.
  protectedParameters:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:compute:RunCommandInputParameterResponse"
    description: The parameters used by the script.
  provisioningState:
    type: string
    description: The provisioning state, which only appears in the response.
  runAsPassword:
    type: string
    description: Specifies the user account password on the VM when executing the run command.
  runAsUser:
    type: string
    description: Specifies the user account on the VM when executing the run command.
  source:
    type: object
    $ref: "#/types/azure-native:compute:VirtualMachineRunCommandScriptSourceResponse"
    description: The source of the run command script.
  tags:
    type: object
    additionalProperties:
      type: string
    description: Resource tags
  timeoutInSeconds:
    type: integer
    description: The timeout in seconds to execute the run command.
  type:
    type: string
    description: Resource type
type: object
required:
  - instanceView
  - location
  - name
  - provisioningState
  - type
inputProperties:
  asyncExecution:
    type: boolean
    description: Optional. If set to true, provisioning will complete as soon as the script starts and will not wait for script to complete.
    default: false
  errorBlobUri:
    type: string
    description: Specifies the Azure storage blob where script error stream will be uploaded.
  instanceId:
    type: string
    description: The instance ID of the virtual machine.
  location:
    type: string
    description: Resource location
  outputBlobUri:
    type: string
    description: Specifies the Azure storage blob where script output stream will be uploaded.
  parameters:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:compute:RunCommandInputParameter"
    description: The parameters used by the script.
  protectedParameters:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:compute:RunCommandInputParameter"
    description: The parameters used by the script.
  resourceGroupName:
    type: string
    description: The name of the resource group.
  runAsPassword:
    type: string
    description: Specifies the user account password on the VM when executing the run command.
  runAsUser:
    type: string
    description: Specifies the user account on the VM when executing the run command.
  runCommandName:
    type: string
    description: The name of the virtual machine run command.
  source:
    type: object
    $ref: "#/types/azure-native:compute:VirtualMachineRunCommandScriptSource"
    description: The source of the run command script.
  tags:
    type: object
    additionalProperties:
      type: string
    description: Resource tags
  timeoutInSeconds:
    type: integer
    description: The timeout in seconds to execute the run command.
  vmScaleSetName:
    type: string
    description: The name of the VM scale set.
requiredInputs:
  - instanceId
  - resourceGroupName
  - vmScaleSetName
aliases:
  - type: azure-native:compute/v20200601:VirtualMachineScaleSetVMRunCommand
  - type: azure-native:compute/v20201201:VirtualMachineScaleSetVMRunCommand
  - type: azure-native:compute/v20210301:VirtualMachineScaleSetVMRunCommand
  - type: azure-native:compute/v20210401:VirtualMachineScaleSetVMRunCommand
  - type: azure-native:compute/v20210701:VirtualMachineScaleSetVMRunCommand
  - type: azure-native:compute/v20211101:VirtualMachineScaleSetVMRunCommand
  - type: azure-native:compute/v20220301:VirtualMachineScaleSetVMRunCommand
