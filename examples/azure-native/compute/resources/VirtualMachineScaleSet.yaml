$token: azure-native:compute:VirtualMachineScaleSet
description: |-
  Describes a Virtual Machine Scale Set.
  API Version: 2021-03-01.
properties:
  additionalCapabilities:
    type: object
    $ref: "#/types/azure-native:compute:AdditionalCapabilitiesResponse"
    description: "Specifies additional capabilities enabled or disabled on the Virtual Machines in the Virtual Machine Scale Set. For instance: whether the Virtual Machines have the capability to support attaching managed data disks with UltraSSD_LRS storage account type."
  automaticRepairsPolicy:
    type: object
    $ref: "#/types/azure-native:compute:AutomaticRepairsPolicyResponse"
    description: Policy for automatic repairs.
  doNotRunExtensionsOnOverprovisionedVMs:
    type: boolean
    description: When Overprovision is enabled, extensions are launched only on the requested number of VMs which are finally kept. This property will hence ensure that the extensions do not run on the extra overprovisioned VMs.
  extendedLocation:
    type: object
    $ref: "#/types/azure-native:compute:ExtendedLocationResponse"
    description: The extended location of the Virtual Machine Scale Set.
  hostGroup:
    type: object
    $ref: "#/types/azure-native:compute:SubResourceResponse"
    description: "Specifies information about the dedicated host group that the virtual machine scale set resides in. <br><br>Minimum api-version: 2020-06-01."
  identity:
    type: object
    $ref: "#/types/azure-native:compute:VirtualMachineScaleSetIdentityResponse"
    description: The identity of the virtual machine scale set, if configured.
  location:
    type: string
    description: Resource location
  name:
    type: string
    description: Resource name
  orchestrationMode:
    type: string
    description: Specifies the orchestration mode for the virtual machine scale set.
  overprovision:
    type: boolean
    description: Specifies whether the Virtual Machine Scale Set should be overprovisioned.
  plan:
    type: object
    $ref: "#/types/azure-native:compute:PlanResponse"
    description: Specifies information about the marketplace image used to create the virtual machine. This element is only used for marketplace images. Before you can use a marketplace image from an API, you must enable the image for programmatic use.  In the Azure portal, find the marketplace image that you want to use and then click **Want to deploy programmatically, Get Started ->**. Enter any required information and then click **Save**.
  platformFaultDomainCount:
    type: integer
    description: Fault Domain count for each placement group.
  provisioningState:
    type: string
    description: The provisioning state, which only appears in the response.
  proximityPlacementGroup:
    type: object
    $ref: "#/types/azure-native:compute:SubResourceResponse"
    description: "Specifies information about the proximity placement group that the virtual machine scale set should be assigned to. <br><br>Minimum api-version: 2018-04-01."
  scaleInPolicy:
    type: object
    $ref: "#/types/azure-native:compute:ScaleInPolicyResponse"
    description: Specifies the scale-in policy that decides which virtual machines are chosen for removal when a Virtual Machine Scale Set is scaled-in.
  singlePlacementGroup:
    type: boolean
    description: "When true this limits the scale set to a single placement group, of max size 100 virtual machines. NOTE: If singlePlacementGroup is true, it may be modified to false. However, if singlePlacementGroup is false, it may not be modified to true."
  sku:
    type: object
    $ref: "#/types/azure-native:compute:SkuResponse"
    description: The virtual machine scale set sku.
  tags:
    type: object
    additionalProperties:
      type: string
    description: Resource tags
  type:
    type: string
    description: Resource type
  uniqueId:
    type: string
    description: Specifies the ID which uniquely identifies a Virtual Machine Scale Set.
  upgradePolicy:
    type: object
    $ref: "#/types/azure-native:compute:UpgradePolicyResponse"
    description: The upgrade policy.
  virtualMachineProfile:
    type: object
    $ref: "#/types/azure-native:compute:VirtualMachineScaleSetVMProfileResponse"
    description: The virtual machine profile.
  zoneBalance:
    type: boolean
    description: Whether to force strictly even Virtual Machine distribution cross x-zones in case there is zone outage.
  zones:
    type: array
    items:
      type: string
    description: "The virtual machine scale set zones. NOTE: Availability zones can only be set when you create the scale set"
type: object
required:
  - location
  - name
  - provisioningState
  - type
  - uniqueId
inputProperties:
  additionalCapabilities:
    type: object
    $ref: "#/types/azure-native:compute:AdditionalCapabilities"
    description: "Specifies additional capabilities enabled or disabled on the Virtual Machines in the Virtual Machine Scale Set. For instance: whether the Virtual Machines have the capability to support attaching managed data disks with UltraSSD_LRS storage account type."
  automaticRepairsPolicy:
    type: object
    $ref: "#/types/azure-native:compute:AutomaticRepairsPolicy"
    description: Policy for automatic repairs.
  doNotRunExtensionsOnOverprovisionedVMs:
    type: boolean
    description: When Overprovision is enabled, extensions are launched only on the requested number of VMs which are finally kept. This property will hence ensure that the extensions do not run on the extra overprovisioned VMs.
  extendedLocation:
    type: object
    $ref: "#/types/azure-native:compute:ExtendedLocation"
    description: The extended location of the Virtual Machine Scale Set.
  hostGroup:
    type: object
    $ref: "#/types/azure-native:compute:SubResource"
    description: "Specifies information about the dedicated host group that the virtual machine scale set resides in. <br><br>Minimum api-version: 2020-06-01."
  identity:
    type: object
    $ref: "#/types/azure-native:compute:VirtualMachineScaleSetIdentity"
    description: The identity of the virtual machine scale set, if configured.
  location:
    type: string
    description: Resource location
  orchestrationMode:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:compute:OrchestrationMode"
    description: Specifies the orchestration mode for the virtual machine scale set.
  overprovision:
    type: boolean
    description: Specifies whether the Virtual Machine Scale Set should be overprovisioned.
  plan:
    type: object
    $ref: "#/types/azure-native:compute:Plan"
    description: Specifies information about the marketplace image used to create the virtual machine. This element is only used for marketplace images. Before you can use a marketplace image from an API, you must enable the image for programmatic use.  In the Azure portal, find the marketplace image that you want to use and then click **Want to deploy programmatically, Get Started ->**. Enter any required information and then click **Save**.
  platformFaultDomainCount:
    type: integer
    description: Fault Domain count for each placement group.
  proximityPlacementGroup:
    type: object
    $ref: "#/types/azure-native:compute:SubResource"
    description: "Specifies information about the proximity placement group that the virtual machine scale set should be assigned to. <br><br>Minimum api-version: 2018-04-01."
  resourceGroupName:
    type: string
    description: The name of the resource group.
  scaleInPolicy:
    type: object
    $ref: "#/types/azure-native:compute:ScaleInPolicy"
    description: Specifies the scale-in policy that decides which virtual machines are chosen for removal when a Virtual Machine Scale Set is scaled-in.
  singlePlacementGroup:
    type: boolean
    description: "When true this limits the scale set to a single placement group, of max size 100 virtual machines. NOTE: If singlePlacementGroup is true, it may be modified to false. However, if singlePlacementGroup is false, it may not be modified to true."
  sku:
    type: object
    $ref: "#/types/azure-native:compute:Sku"
    description: The virtual machine scale set sku.
  tags:
    type: object
    additionalProperties:
      type: string
    description: Resource tags
  upgradePolicy:
    type: object
    $ref: "#/types/azure-native:compute:UpgradePolicy"
    description: The upgrade policy.
  virtualMachineProfile:
    type: object
    $ref: "#/types/azure-native:compute:VirtualMachineScaleSetVMProfile"
    description: The virtual machine profile.
  vmScaleSetName:
    type: string
    description: The name of the VM scale set to create or update.
  zoneBalance:
    type: boolean
    description: Whether to force strictly even Virtual Machine distribution cross x-zones in case there is zone outage.
  zones:
    type: array
    items:
      type: string
    description: "The virtual machine scale set zones. NOTE: Availability zones can only be set when you create the scale set"
requiredInputs:
  - resourceGroupName
aliases:
  - type: azure-native:compute/v20150615:VirtualMachineScaleSet
  - type: azure-native:compute/v20160330:VirtualMachineScaleSet
  - type: azure-native:compute/v20160430preview:VirtualMachineScaleSet
  - type: azure-native:compute/v20170330:VirtualMachineScaleSet
  - type: azure-native:compute/v20171201:VirtualMachineScaleSet
  - type: azure-native:compute/v20180401:VirtualMachineScaleSet
  - type: azure-native:compute/v20180601:VirtualMachineScaleSet
  - type: azure-native:compute/v20181001:VirtualMachineScaleSet
  - type: azure-native:compute/v20190301:VirtualMachineScaleSet
  - type: azure-native:compute/v20190701:VirtualMachineScaleSet
  - type: azure-native:compute/v20191201:VirtualMachineScaleSet
  - type: azure-native:compute/v20200601:VirtualMachineScaleSet
  - type: azure-native:compute/v20201201:VirtualMachineScaleSet
  - type: azure-native:compute/v20210301:VirtualMachineScaleSet
  - type: azure-native:compute/v20210401:VirtualMachineScaleSet
  - type: azure-native:compute/v20210701:VirtualMachineScaleSet
  - type: azure-native:compute/v20211101:VirtualMachineScaleSet
  - type: azure-native:compute/v20220301:VirtualMachineScaleSet
