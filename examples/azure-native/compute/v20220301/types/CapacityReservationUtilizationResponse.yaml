$token: azure-native:compute/v20220301:CapacityReservationUtilizationResponse
description: Represents the capacity reservation utilization in terms of resources allocated.
properties:
  virtualMachinesAllocated:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:compute/v20220301:SubResourceReadOnlyResponse"
    description: A list of all virtual machines resource ids allocated against the capacity reservation.
type: object
required:
  - virtualMachinesAllocated
