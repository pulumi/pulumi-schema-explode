$token: azure-native:compute/v20220301:PublicIPAddressSku
description: Describes the public IP Sku. It can only be set with OrchestrationMode as Flexible.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:compute/v20220301:PublicIPAddressSkuName"
    description: Specify public IP sku name
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:compute/v20220301:PublicIPAddressSkuTier"
    description: Specify public IP sku tier
type: object
