$token: azure-native:compute/v20180401:DiskSku
description: The disks sku name. Can be Standard_LRS, Premium_LRS, or StandardSSD_LRS.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:compute/v20180401:StorageAccountTypes"
    description: The sku name.
type: object
