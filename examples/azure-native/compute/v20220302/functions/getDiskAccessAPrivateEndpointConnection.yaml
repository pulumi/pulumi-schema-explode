$token: azure-native:compute/v20220302:getDiskAccessAPrivateEndpointConnection
description: The Private Endpoint Connection resource.
inputs:
  properties:
    diskAccessName:
      type: string
      description: The name of the disk access resource that is being created. The name can't be changed after the disk encryption set is created. Supported characters for the name are a-z, A-Z, 0-9, _ and -. The maximum name length is 80 characters.
    privateEndpointConnectionName:
      type: string
      description: The name of the private endpoint connection.
    resourceGroupName:
      type: string
      description: The name of the resource group.
  type: object
  required:
    - diskAccessName
    - privateEndpointConnectionName
    - resourceGroupName
outputs:
  description: The Private Endpoint Connection resource.
  properties:
    id:
      type: string
      description: private endpoint connection Id
    name:
      type: string
      description: private endpoint connection name
    privateEndpoint:
      type: object
      $ref: "#/types/azure-native:compute/v20220302:PrivateEndpointResponse"
      description: The resource of private end point.
    privateLinkServiceConnectionState:
      type: object
      $ref: "#/types/azure-native:compute/v20220302:PrivateLinkServiceConnectionStateResponse"
      description: A collection of information about the state of the connection between DiskAccess and Virtual Network.
    provisioningState:
      type: string
      description: The provisioning state of the private endpoint connection resource.
    type:
      type: string
      description: private endpoint connection type
  type: object
  required:
    - id
    - name
    - privateEndpoint
    - privateLinkServiceConnectionState
    - provisioningState
    - type
