$token: azure-native:compute/v20171201:VirtualMachineScaleSetPublicIPAddressConfigurationResponse
description: Describes a virtual machines scale set IP Configuration's PublicIPAddress configuration
properties:
  dnsSettings:
    type: object
    $ref: "#/types/azure-native:compute/v20171201:VirtualMachineScaleSetPublicIPAddressConfigurationDnsSettingsResponse"
    description: The dns settings to be applied on the publicIP addresses .
  idleTimeoutInMinutes:
    type: integer
    description: The idle timeout of the public IP address.
  name:
    type: string
    description: The publicIP address configuration name.
type: object
required:
  - name
