$token: azure-native:compute/v20170330:VirtualMachineScaleSetNetworkConfigurationResponse
description: Describes a virtual machine scale set network profile's network configurations.
properties:
  dnsSettings:
    type: object
    $ref: "#/types/azure-native:compute/v20170330:VirtualMachineScaleSetNetworkConfigurationDnsSettingsResponse"
    description: The dns settings to be applied on the network interfaces.
  enableAcceleratedNetworking:
    type: boolean
    description: Specifies whether the network interface is accelerated networking-enabled.
  id:
    type: string
    description: Resource Id
  ipConfigurations:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:compute/v20170330:VirtualMachineScaleSetIPConfigurationResponse"
    description: Specifies the IP configurations of the network interface.
  name:
    type: string
    description: The network configuration name.
  networkSecurityGroup:
    type: object
    $ref: "#/types/azure-native:compute/v20170330:SubResourceResponse"
    description: The network security group.
  primary:
    type: boolean
    description: Specifies the primary network interface in case the virtual machine has more than 1 network interface.
type: object
required:
  - ipConfigurations
  - name
