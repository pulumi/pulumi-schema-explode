$token: azure-native:compute/v20170330:DiskSku
description: The disks and snapshots sku name. Can be Standard_LRS or Premium_LRS.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:compute/v20170330:StorageAccountTypes"
    description: The sku name.
type: object
