$token: azure-native:compute/v20160430preview:getImage
description: The source user image virtual hard disk. The virtual hard disk will be copied before being attached to the virtual machine. If SourceImage is provided, the destination virtual hard drive must not exist.
inputs:
  properties:
    expand:
      type: string
      description: The expand expression to apply on the operation.
    imageName:
      type: string
      description: The name of the image.
    resourceGroupName:
      type: string
      description: The name of the resource group.
  type: object
  required:
    - imageName
    - resourceGroupName
outputs:
  description: The source user image virtual hard disk. The virtual hard disk will be copied before being attached to the virtual machine. If SourceImage is provided, the destination virtual hard drive must not exist.
  properties:
    id:
      type: string
      description: Resource Id
    location:
      type: string
      description: Resource location
    name:
      type: string
      description: Resource name
    provisioningState:
      type: string
      description: The provisioning state.
    sourceVirtualMachine:
      type: object
      $ref: "#/types/azure-native:compute/v20160430preview:SubResourceResponse"
      description: The source virtual machine from which Image is created.
    storageProfile:
      type: object
      $ref: "#/types/azure-native:compute/v20160430preview:ImageStorageProfileResponse"
      description: Specifies the storage settings for the virtual machine disks.
    tags:
      type: object
      additionalProperties:
        type: string
      description: Resource tags
    type:
      type: string
      description: Resource type
  type: object
  required:
    - id
    - location
    - name
    - provisioningState
    - type
deprecationMessage: Version 2016-04-30-preview will be removed in v2 of the provider.
