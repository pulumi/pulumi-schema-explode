$token: azure-native:compute/v20210701:PublicIPAddressSku
description: Describes the public IP Sku
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:compute/v20210701:PublicIPAddressSkuName"
    description: Specify public IP sku name
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:compute/v20210701:PublicIPAddressSkuTier"
    description: Specify public IP sku tier
type: object
