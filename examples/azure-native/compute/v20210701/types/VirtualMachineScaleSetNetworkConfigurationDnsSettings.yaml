$token: azure-native:compute/v20210701:VirtualMachineScaleSetNetworkConfigurationDnsSettings
description: Describes a virtual machines scale sets network configuration's DNS settings.
properties:
  dnsServers:
    type: array
    items:
      type: string
    description: List of DNS servers IP addresses
type: object
