$token: azure-native:compute/v20180930:EncryptionSettingsCollectionResponse
description: Encryption settings for disk or snapshot
properties:
  enabled:
    type: boolean
    description: Set this flag to true and provide DiskEncryptionKey and optional KeyEncryptionKey to enable encryption. Set this flag to false and remove DiskEncryptionKey and KeyEncryptionKey to disable encryption. If EncryptionSettings is null in the request object, the existing settings remain unchanged.
  encryptionSettings:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:compute/v20180930:EncryptionSettingsElementResponse"
    description: A collection of encryption settings, one for each disk volume.
type: object
required:
  - enabled
