$token: azure-native:eventhub:NWRuleSetIpRules
description: Description of NetWorkRuleSet - IpRules resource.
properties:
  action:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:eventhub:NetworkRuleIPAction"
    description: The IP Filter Action
    default: Allow
  ipMask:
    type: string
    description: IP Mask
type: object
