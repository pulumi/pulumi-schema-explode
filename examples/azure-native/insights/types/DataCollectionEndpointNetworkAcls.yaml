$token: azure-native:insights:DataCollectionEndpointNetworkAcls
description: Network access control rules for the endpoints.
properties:
  publicNetworkAccess:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:insights:KnownPublicNetworkAccessOptions"
    description: The configuration to set whether network access from public internet to the endpoints are allowed.
type: object
