$token: azure-native:insights:DataSourceConfiguration
properties:
  eventLogs:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:insights:EventLogConfiguration"
    description: Windows event logs configuration.
  perfCounters:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:insights:PerformanceCounterConfiguration"
    description: Performance counter configuration
  providers:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:insights:EtwProviderConfiguration"
    description: ETW providers configuration
type: object
