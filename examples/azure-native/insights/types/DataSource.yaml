$token: azure-native:insights:DataSource
description: Data source object contains configuration to collect telemetry and one or more sinks to send that telemetry data to
properties:
  configuration:
    type: object
    $ref: "#/types/azure-native:insights:DataSourceConfiguration"
  kind:
    type: string
    description: Datasource kind
  sinks:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:insights:SinkConfiguration"
type: object
required:
  - configuration
  - kind
  - sinks
