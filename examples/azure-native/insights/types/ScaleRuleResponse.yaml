$token: azure-native:insights:ScaleRuleResponse
description: A rule that provide the triggers and parameters for the scaling action.
properties:
  metricTrigger:
    type: object
    $ref: "#/types/azure-native:insights:MetricTriggerResponse"
    description: the trigger that results in a scaling action.
  scaleAction:
    type: object
    $ref: "#/types/azure-native:insights:ScaleActionResponse"
    description: the parameters for the scaling action.
type: object
required:
  - metricTrigger
  - scaleAction
