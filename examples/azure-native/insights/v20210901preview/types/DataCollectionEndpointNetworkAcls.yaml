$token: azure-native:insights/v20210901preview:DataCollectionEndpointNetworkAcls
description: Network access control rules for the endpoints.
properties:
  publicNetworkAccess:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:insights/v20210901preview:KnownPublicNetworkAccessOptions"
    description: The configuration to set whether network access from public internet to the endpoints are allowed.
type: object
