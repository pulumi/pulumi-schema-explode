$token: azure-native:insights/v20210901preview:IisLogsDataSource
description: Enables IIS logs to be collected by this data collection rule.
properties:
  logDirectories:
    type: array
    items:
      type: string
    description: Absolute paths file location
  name:
    type: string
    description: "A friendly name for the data source. \r

      This name should be unique across all data sources (regardless of type) within the data collection rule."
  streams:
    type: array
    items:
      type: string
    description: IIS streams
type: object
required:
  - streams
