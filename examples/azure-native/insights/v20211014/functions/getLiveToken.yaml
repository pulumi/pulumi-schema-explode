$token: azure-native:insights/v20211014:getLiveToken
description: The response to a live token query.
inputs:
  properties:
    resourceUri:
      type: string
      description: The identifier of the resource.
  type: object
  required:
    - resourceUri
outputs:
  description: The response to a live token query.
  properties:
    liveToken:
      type: string
      description: JWT token for accessing live metrics stream data.
  type: object
  required:
    - liveToken
