$token: azure-native:insights/v20210401:DataFlow
description: Definition of which streams are sent to which destinations.
properties:
  destinations:
    type: array
    items:
      type: string
    description: List of destinations for this data flow.
  streams:
    type: array
    items:
      oneOf:
        - type: string
        - $ref: "#/types/azure-native:insights/v20210401:KnownDataFlowStreams"
    description: List of streams for this data flow.
type: object
