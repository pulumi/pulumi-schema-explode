$token: azure-native:insights/v20140401:ScaleRuleResponse
description: A rule that provide the triggers and parameters for the scaling action.
properties:
  metricTrigger:
    type: object
    $ref: "#/types/azure-native:insights/v20140401:MetricTriggerResponse"
    description: the trigger that results in a scaling action.
  scaleAction:
    type: object
    $ref: "#/types/azure-native:insights/v20140401:ScaleActionResponse"
    description: the parameters for the scaling action.
type: object
required:
  - metricTrigger
  - scaleAction
