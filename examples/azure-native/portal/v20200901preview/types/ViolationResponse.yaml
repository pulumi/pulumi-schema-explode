$token: azure-native:portal/v20200901preview:ViolationResponse
description: Violation information.
properties:
  errorMessage:
    type: string
    description: Error message.
  id:
    type: string
    description: Id of the item that violates tenant configuration.
  userId:
    type: string
    description: Id of the user who owns violated item.
type: object
required:
  - errorMessage
  - id
  - userId
