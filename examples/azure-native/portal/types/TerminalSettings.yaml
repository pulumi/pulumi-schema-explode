$token: azure-native:portal:TerminalSettings
description: Settings for terminal appearance.
properties:
  fontSize:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:portal:FontSize"
    description: Size of terminal font.
  fontStyle:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:portal:FontStyle"
    description: Style of terminal font.
type: object
