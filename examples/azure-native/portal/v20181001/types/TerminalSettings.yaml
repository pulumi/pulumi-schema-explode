$token: azure-native:portal/v20181001:TerminalSettings
description: Settings for terminal appearance.
properties:
  fontSize:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:portal/v20181001:FontSize"
    description: Size of terminal font.
  fontStyle:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:portal/v20181001:FontStyle"
    description: Style of terminal font.
type: object
