$token: azure-native:portal/v20181001:getUserSettingsWithLocation
description: Response to get user settings
inputs:
  properties:
    location:
      type: string
      description: The provider location
    userSettingsName:
      type: string
      description: The name of the user settings
  type: object
  required:
    - location
    - userSettingsName
outputs:
  description: Response to get user settings
  properties:
    properties:
      type: object
      $ref: "#/types/azure-native:portal/v20181001:UserPropertiesResponse"
      description: The cloud shell user settings properties.
  type: object
  required:
    - properties
