$token: azure-native:security:SuppressionAlertsScopeResponse
properties:
  allOf:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:security:ScopeElementResponse"
    description: All the conditions inside need to be true in order to suppress the alert
type: object
required:
  - allOf
