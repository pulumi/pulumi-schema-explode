$token: azure-native:security/v20150601preview:getJitNetworkAccessPolicy
inputs:
  properties:
    ascLocation:
      type: string
      description: The location where ASC stores the data of the subscription. can be retrieved from Get locations
    jitNetworkAccessPolicyName:
      type: string
      description: Name of a Just-in-Time access configuration policy.
    resourceGroupName:
      type: string
      description: The name of the resource group within the user's subscription. The name is case insensitive.
  type: object
  required:
    - ascLocation
    - jitNetworkAccessPolicyName
    - resourceGroupName
outputs:
  properties:
    id:
      type: string
      description: Resource Id
    kind:
      type: string
      description: Kind of the resource
    location:
      type: string
      description: Location where the resource is stored
    name:
      type: string
      description: Resource name
    provisioningState:
      type: string
      description: Gets the provisioning state of the Just-in-Time policy.
    requests:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:security/v20150601preview:JitNetworkAccessRequestResponse"
    type:
      type: string
      description: Resource type
    virtualMachines:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:security/v20150601preview:JitNetworkAccessPolicyVirtualMachineResponse"
      description: Configurations for Microsoft.Compute/virtualMachines resource type.
  type: object
  required:
    - id
    - location
    - name
    - provisioningState
    - type
    - virtualMachines
deprecationMessage: Version 2015-06-01-preview will be removed in v2 of the provider.
