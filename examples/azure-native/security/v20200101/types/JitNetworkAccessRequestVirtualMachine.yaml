$token: azure-native:security/v20200101:JitNetworkAccessRequestVirtualMachine
properties:
  id:
    type: string
    description: Resource ID of the virtual machine that is linked to this policy
  ports:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:security/v20200101:JitNetworkAccessRequestPort"
    description: The ports that were opened for the virtual machine
type: object
required:
  - id
  - ports
