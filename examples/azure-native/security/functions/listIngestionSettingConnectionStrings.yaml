$token: azure-native:security:listIngestionSettingConnectionStrings
description: |-
  Connection string for ingesting security data and logs
  API Version: 2021-01-15-preview.
inputs:
  properties:
    ingestionSettingName:
      type: string
      description: Name of the ingestion setting
  type: object
  required:
    - ingestionSettingName
outputs:
  description: Connection string for ingesting security data and logs
  properties:
    value:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:security:IngestionConnectionStringResponse"
      description: Connection strings
  type: object
  required:
    - value
