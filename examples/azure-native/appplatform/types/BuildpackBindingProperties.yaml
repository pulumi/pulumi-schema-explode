$token: azure-native:appplatform:BuildpackBindingProperties
description: Properties of a buildpack binding
properties:
  bindingType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:appplatform:BindingType"
    description: Buildpack Binding Type
  launchProperties:
    type: object
    $ref: "#/types/azure-native:appplatform:BuildpackBindingLaunchProperties"
    description: The object describes the buildpack binding launch properties
type: object
