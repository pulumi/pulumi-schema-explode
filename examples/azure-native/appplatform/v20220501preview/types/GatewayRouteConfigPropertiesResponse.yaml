$token: azure-native:appplatform/v20220501preview:GatewayRouteConfigPropertiesResponse
description: API route config of the Spring Cloud Gateway
properties:
  appResourceId:
    type: string
    description: The resource Id of the Azure Spring Apps app, required unless route defines `uri`.
  openApi:
    type: object
    $ref: "#/types/azure-native:appplatform/v20220501preview:GatewayRouteConfigOpenApiPropertiesResponse"
    description: OpenAPI properties of Spring Cloud Gateway route config.
  provisioningState:
    type: string
    description: State of the Spring Cloud Gateway route config.
  routes:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:appplatform/v20220501preview:GatewayApiRouteResponse"
    description: Array of API routes, each route contains properties such as `title`, `uri`, `ssoEnabled`, `predicates`, `filters`.
type: object
required:
  - provisioningState
