$token: azure-native:appplatform/v20210601preview:UserSourceInfoResponse
description: Source information for a deployment
properties:
  artifactSelector:
    type: string
    description: "Selector for the artifact to be used for the deployment for multi-module projects. This should be\r

      the relative path to the target module/project."
  customContainer:
    type: object
    $ref: "#/types/azure-native:appplatform/v20210601preview:CustomContainerResponse"
    description: Custom container payload
  relativePath:
    type: string
    description: Relative path of the storage which stores the source
  type:
    type: string
    description: Type of the source uploaded
  version:
    type: string
    description: Version of the source
type: object
