$token: azure-native:appplatform/v20210601preview:getDeployment
description: Deployment resource payload
inputs:
  properties:
    appName:
      type: string
      description: The name of the App resource.
    deploymentName:
      type: string
      description: The name of the Deployment resource.
    resourceGroupName:
      type: string
      description: The name of the resource group that contains the resource. You can obtain this value from the Azure Resource Manager API or the portal.
    serviceName:
      type: string
      description: The name of the Service resource.
  type: object
  required:
    - appName
    - deploymentName
    - resourceGroupName
    - serviceName
outputs:
  description: Deployment resource payload
  properties:
    id:
      type: string
      description: Fully qualified resource Id for the resource.
    name:
      type: string
      description: The name of the resource.
    properties:
      type: object
      $ref: "#/types/azure-native:appplatform/v20210601preview:DeploymentResourcePropertiesResponse"
      description: Properties of the Deployment resource
    sku:
      type: object
      $ref: "#/types/azure-native:appplatform/v20210601preview:SkuResponse"
      description: Sku of the Deployment resource
    type:
      type: string
      description: The type of the resource.
  type: object
  required:
    - id
    - name
    - properties
    - type
