$token: azure-native:elastic/v20211001preview:MonitoringTagRulesProperties
description: Definition of the properties for a TagRules resource.
properties:
  logRules:
    type: object
    $ref: "#/types/azure-native:elastic/v20211001preview:LogRules"
    description: Rules for sending logs.
  provisioningState:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:elastic/v20211001preview:ProvisioningState"
    description: Provisioning state of the monitoring tag rules.
type: object
