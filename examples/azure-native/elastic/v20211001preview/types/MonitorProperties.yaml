$token: azure-native:elastic/v20211001preview:MonitorProperties
description: Properties specific to the monitor resource.
properties:
  monitoringStatus:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:elastic/v20211001preview:MonitoringStatus"
    description: Flag specifying if the resource monitoring is enabled or disabled.
  provisioningState:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:elastic/v20211001preview:ProvisioningState"
    description: Provisioning state of the monitor resource.
  userInfo:
    type: object
    $ref: "#/types/azure-native:elastic/v20211001preview:UserInfo"
    description: User information.
  version:
    type: string
    description: Version of elastic of the monitor resource
type: object
