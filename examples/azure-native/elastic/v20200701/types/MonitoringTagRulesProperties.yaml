$token: azure-native:elastic/v20200701:MonitoringTagRulesProperties
description: Definition of the properties for a TagRules resource.
properties:
  logRules:
    type: object
    $ref: "#/types/azure-native:elastic/v20200701:LogRules"
    description: Rules for sending logs.
  provisioningState:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:elastic/v20200701:ProvisioningState"
    description: Provisioning state of the monitoring tag rules.
type: object
