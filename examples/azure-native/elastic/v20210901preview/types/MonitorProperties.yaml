$token: azure-native:elastic/v20210901preview:MonitorProperties
description: Properties specific to the monitor resource.
properties:
  monitoringStatus:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:elastic/v20210901preview:MonitoringStatus"
    description: Flag specifying if the resource monitoring is enabled or disabled.
  provisioningState:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:elastic/v20210901preview:ProvisioningState"
    description: Provisioning state of the monitor resource.
  userInfo:
    type: object
    $ref: "#/types/azure-native:elastic/v20210901preview:UserInfo"
    description: User information.
type: object
