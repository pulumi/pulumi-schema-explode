$token: azure-native:elastic:MonitorProperties
description: Properties specific to the monitor resource.
properties:
  monitoringStatus:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:elastic:MonitoringStatus"
    description: Flag specifying if the resource monitoring is enabled or disabled.
  provisioningState:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:elastic:ProvisioningState"
    description: Provisioning state of the monitor resource.
  userInfo:
    type: object
    $ref: "#/types/azure-native:elastic:UserInfo"
    description: User information.
type: object
