$token: azure-native:elastic:MonitoringTagRulesProperties
description: Definition of the properties for a TagRules resource.
properties:
  logRules:
    type: object
    $ref: "#/types/azure-native:elastic:LogRules"
    description: Rules for sending logs.
  provisioningState:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:elastic:ProvisioningState"
    description: Provisioning state of the monitoring tag rules.
type: object
