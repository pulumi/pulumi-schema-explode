$token: azure-native:elastic:IdentityProperties
description: Identity properties.
properties:
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:elastic:ManagedIdentityTypes"
    description: Managed identity type.
type: object
