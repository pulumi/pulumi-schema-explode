$token: azure-native:powerplatform/v20201030preview:PropertiesEncryption
description: The encryption settings for a configuration store.
properties:
  keyVault:
    type: object
    $ref: "#/types/azure-native:powerplatform/v20201030preview:KeyVaultProperties"
    description: Key vault properties.
  state:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:powerplatform/v20201030preview:State"
    description: The state of onboarding, which only appears in the response.
type: object
