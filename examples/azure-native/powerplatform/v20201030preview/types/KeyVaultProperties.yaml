$token: azure-native:powerplatform/v20201030preview:KeyVaultProperties
description: Settings concerning key vault encryption for a configuration store.
properties:
  id:
    type: string
    description: Uri of KeyVault
  key:
    type: object
    $ref: "#/types/azure-native:powerplatform/v20201030preview:KeyProperties"
    description: Identity of the secret that includes name and version.
type: object
