$token: azure-native:powerplatform:VirtualNetworkProperties
description: Settings concerning the virtual network.
properties:
  id:
    type: string
    description: Uri of the virtual network.
  subnet:
    type: object
    $ref: "#/types/azure-native:powerplatform:SubnetProperties"
    description: Properties of a subnet.
type: object
