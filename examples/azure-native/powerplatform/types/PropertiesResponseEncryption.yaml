$token: azure-native:powerplatform:PropertiesResponseEncryption
description: The encryption settings for a configuration store.
properties:
  keyVault:
    type: object
    $ref: "#/types/azure-native:powerplatform:KeyVaultPropertiesResponse"
    description: Key vault properties.
  state:
    type: string
    description: The state of onboarding, which only appears in the response.
type: object
