$token: azure-native:powerplatform:PropertiesEncryption
description: The encryption settings for a configuration store.
properties:
  keyVault:
    type: object
    $ref: "#/types/azure-native:powerplatform:KeyVaultProperties"
    description: Key vault properties.
  state:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:powerplatform:State"
    description: The state of onboarding, which only appears in the response.
type: object
