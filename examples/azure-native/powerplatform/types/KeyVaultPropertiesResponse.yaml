$token: azure-native:powerplatform:KeyVaultPropertiesResponse
description: Settings concerning key vault encryption for a configuration store.
properties:
  id:
    type: string
    description: Uri of KeyVault
  key:
    type: object
    $ref: "#/types/azure-native:powerplatform:KeyPropertiesResponse"
    description: Identity of the secret that includes name and version.
type: object
