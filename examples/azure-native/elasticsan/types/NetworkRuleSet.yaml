$token: azure-native:elasticsan:NetworkRuleSet
description: A set of rules governing the network accessibility.
properties:
  virtualNetworkRules:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:elasticsan:VirtualNetworkRule"
    description: The list of virtual network rules.
type: object
