$token: azure-native:elasticsan:Sku
description: The SKU name. Required for account creation; optional for update.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:elasticsan:SkuName"
    description: The sku name.
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:elasticsan:SkuTier"
    description: The sku tier.
type: object
required:
  - name
