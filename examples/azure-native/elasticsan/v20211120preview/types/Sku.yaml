$token: azure-native:elasticsan/v20211120preview:Sku
description: The SKU name. Required for account creation; optional for update.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:elasticsan/v20211120preview:SkuName"
    description: The sku name.
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:elasticsan/v20211120preview:SkuTier"
    description: The sku tier.
type: object
required:
  - name
