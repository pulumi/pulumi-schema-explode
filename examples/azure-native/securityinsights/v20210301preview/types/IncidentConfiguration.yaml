$token: azure-native:securityinsights/v20210301preview:IncidentConfiguration
description: Incident Configuration property bag.
properties:
  createIncident:
    type: boolean
    description: Create incidents from alerts triggered by this analytics rule
  groupingConfiguration:
    type: object
    $ref: "#/types/azure-native:securityinsights/v20210301preview:GroupingConfiguration"
    description: Set how the alerts that are triggered by this analytics rule, are grouped into incidents
type: object
required:
  - createIncident
