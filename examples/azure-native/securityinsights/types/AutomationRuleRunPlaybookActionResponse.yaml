$token: azure-native:securityinsights:AutomationRuleRunPlaybookActionResponse
description: Describes an automation rule action to run a playbook
properties:
  actionConfiguration:
    type: object
    $ref: "#/types/azure-native:securityinsights:AutomationRuleRunPlaybookActionResponseActionConfiguration"
    description: The configuration of the run playbook automation rule action
  actionType:
    type: string
    description: |-
      The type of the automation rule action
      Expected value is 'RunPlaybook'.
    const: RunPlaybook
  order:
    type: integer
    description: The order of execution of the automation rule action
type: object
required:
  - actionConfiguration
  - actionType
  - order
