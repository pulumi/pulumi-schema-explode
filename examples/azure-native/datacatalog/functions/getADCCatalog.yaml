$token: azure-native:datacatalog:getADCCatalog
description: |-
  Azure Data Catalog.
  API Version: 2016-03-30.
inputs:
  properties:
    catalogName:
      type: string
      description: The name of the data catalog in the specified subscription and resource group.
    resourceGroupName:
      type: string
      description: The name of the resource group within the user's subscription. The name is case insensitive.
  type: object
  required:
    - catalogName
    - resourceGroupName
outputs:
  description: Azure Data Catalog.
  properties:
    admins:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:datacatalog:PrincipalsResponse"
      description: Azure data catalog admin list.
    enableAutomaticUnitAdjustment:
      type: boolean
      description: Automatic unit adjustment enabled or not.
    etag:
      type: string
      description: Resource etag
    id:
      type: string
      description: Resource Id
    location:
      type: string
      description: Resource location
    name:
      type: string
      description: Resource name
    sku:
      type: string
      description: Azure data catalog SKU.
    successfullyProvisioned:
      type: boolean
      description: Azure data catalog provision status.
    tags:
      type: object
      additionalProperties:
        type: string
      description: Resource tags
    type:
      type: string
      description: Resource type
    units:
      type: integer
      description: Azure data catalog units.
    users:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:datacatalog:PrincipalsResponse"
      description: Azure data catalog user list.
  type: object
  required:
    - id
    - name
    - type
