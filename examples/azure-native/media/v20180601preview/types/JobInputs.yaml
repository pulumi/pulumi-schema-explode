$token: azure-native:media/v20180601preview:JobInputs
description: Describes a list of inputs to a Job.
properties:
  inputs:
    type: array
    items:
      oneOf:
        - type: object
          $ref: "#/types/azure-native:media/v20180601preview:JobInputAsset"
        - type: object
          $ref: "#/types/azure-native:media/v20180601preview:JobInputClip"
        - type: object
          $ref: "#/types/azure-native:media/v20180601preview:JobInputHttp"
        - type: object
          $ref: "#/types/azure-native:media/v20180601preview:JobInputs"
      discriminator:
        propertyName: odataType
        mapping:
          "#Microsoft.Media.JobInputAsset": "#/types/azure-native:media/v20180601preview:JobInputAsset"
          "#Microsoft.Media.JobInputClip": "#/types/azure-native:media/v20180601preview:JobInputClip"
          "#Microsoft.Media.JobInputHttp": "#/types/azure-native:media/v20180601preview:JobInputHttp"
          "#Microsoft.Media.JobInputs": "#/types/azure-native:media/v20180601preview:JobInputs"
    description: List of inputs to a Job.
  label:
    type: string
    description: A label that is assigned to a JobInput, that is used to satisfy a reference used in the Transform. For example, a Transform can be authored so as to take an image file with the label 'xyz' and apply it as an overlay onto the input video before it is encoded. When submitting a Job, exactly one of the JobInputs should be the image file, and it should have the label 'xyz'.
  odataType:
    type: string
    description: |-
      The discriminator for derived types.
      Expected value is '#Microsoft.Media.JobInputs'.
    const: "#Microsoft.Media.JobInputs"
type: object
required:
  - odataType
