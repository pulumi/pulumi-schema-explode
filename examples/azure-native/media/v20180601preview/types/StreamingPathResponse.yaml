$token: azure-native:media/v20180601preview:StreamingPathResponse
description: Class of paths for streaming
properties:
  encryptionScheme:
    type: string
    description: Encryption scheme
  paths:
    type: array
    items:
      type: string
    description: Streaming paths for each protocol and encryptionScheme pair
  streamingProtocol:
    type: string
    description: Streaming protocol
type: object
required:
  - encryptionScheme
  - streamingProtocol
