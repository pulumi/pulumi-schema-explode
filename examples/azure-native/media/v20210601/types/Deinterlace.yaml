$token: azure-native:media/v20210601:Deinterlace
description: Describes the de-interlacing settings.
properties:
  mode:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:media/v20210601:DeinterlaceMode"
    description: The deinterlacing mode. Defaults to AutoPixelAdaptive.
  parity:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:media/v20210601:DeinterlaceParity"
    description: The field parity for de-interlacing, defaults to Auto.
type: object
