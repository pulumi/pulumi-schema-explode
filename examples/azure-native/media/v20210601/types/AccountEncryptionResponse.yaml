$token: azure-native:media/v20210601:AccountEncryptionResponse
properties:
  identity:
    type: object
    $ref: "#/types/azure-native:media/v20210601:ResourceIdentityResponse"
    description: The Key Vault identity.
  keyVaultProperties:
    type: object
    $ref: "#/types/azure-native:media/v20210601:KeyVaultPropertiesResponse"
    description: The properties of the key used to encrypt the account.
  status:
    type: string
    description: The current status of the Key Vault mapping.
  type:
    type: string
    description: The type of key used to encrypt the Account Key.
type: object
required:
  - status
  - type
