$token: azure-native:media/v20210601:StreamOptionsFlag
type: string
enum:
  - description: Live streaming with no special latency optimizations.
    value: Default
  - description: The live event provides lower end to end latency by reducing its internal buffers. This could result in more client buffering during playback if network bandwidth is low.
    value: LowLatency
