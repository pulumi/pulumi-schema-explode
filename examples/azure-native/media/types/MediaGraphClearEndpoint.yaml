$token: azure-native:media:MediaGraphClearEndpoint
description: An endpoint to connect to with no encryption in transit.
properties:
  credentials:
    type: object
    $ref: "#/types/azure-native:media:MediaGraphUsernamePasswordCredentials"
    description: Polymorphic credentials to present to the endpoint.
  odataType:
    type: string
    description: |-
      The discriminator for derived types.
      Expected value is '#Microsoft.Media.MediaGraphClearEndpoint'.
    const: "#Microsoft.Media.MediaGraphClearEndpoint"
  url:
    type: string
    description: Url for the endpoint.
type: object
required:
  - odataType
  - url
