$token: azure-native:media:MediaGraphAssetSinkResponse
description: Asset sink.
properties:
  assetName:
    type: string
    description: Asset name.
  inputs:
    type: array
    items:
      type: string
    description: Sink inputs.
  name:
    type: string
    description: Sink name.
  odataType:
    type: string
    description: |-
      The discriminator for derived types.
      Expected value is '#Microsoft.Media.MediaGraphAssetSink'.
    const: "#Microsoft.Media.MediaGraphAssetSink"
type: object
required:
  - assetName
  - inputs
  - name
  - odataType
