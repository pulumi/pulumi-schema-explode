$token: azure-native:media:FilterTrackSelectionResponse
description: Representing a list of FilterTrackPropertyConditions to select a track.  The filters are combined using a logical AND operation.
properties:
  trackSelections:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:media:FilterTrackPropertyConditionResponse"
    description: The track selections.
type: object
required:
  - trackSelections
