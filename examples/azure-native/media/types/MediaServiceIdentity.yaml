$token: azure-native:media:MediaServiceIdentity
properties:
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:media:ManagedIdentityType"
    description: The identity type.
type: object
required:
  - type
