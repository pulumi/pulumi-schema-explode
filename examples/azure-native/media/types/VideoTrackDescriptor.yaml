$token: azure-native:media:VideoTrackDescriptor
description: A TrackSelection to select video tracks.
properties:
  odataType:
    type: string
    description: |-
      The discriminator for derived types.
      Expected value is '#Microsoft.Media.VideoTrackDescriptor'.
    const: "#Microsoft.Media.VideoTrackDescriptor"
type: object
required:
  - odataType
