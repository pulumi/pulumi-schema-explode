$token: azure-native:media:VideoTrackResponse
description: Represents a video track in the asset.
properties:
  odataType:
    type: string
    description: |-
      The discriminator for derived types.
      Expected value is '#Microsoft.Media.VideoTrack'.
    const: "#Microsoft.Media.VideoTrack"
type: object
required:
  - odataType
