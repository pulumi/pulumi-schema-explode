$token: azure-native:media:FilterTrackSelection
description: Representing a list of FilterTrackPropertyConditions to select a track.  The filters are combined using a logical AND operation.
properties:
  trackSelections:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:media:FilterTrackPropertyCondition"
    description: The track selections.
type: object
required:
  - trackSelections
