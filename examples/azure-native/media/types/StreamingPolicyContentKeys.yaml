$token: azure-native:media:StreamingPolicyContentKeys
description: Class to specify properties of all content keys in Streaming Policy
properties:
  defaultKey:
    type: object
    $ref: "#/types/azure-native:media:DefaultKey"
    description: Default content key for an encryption scheme
  keyToTrackMappings:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:media:StreamingPolicyContentKey"
    description: Representing tracks needs separate content key
type: object
