$token: azure-native:media/v20200501:AccountEncryptionResponse
properties:
  keyVaultProperties:
    type: object
    $ref: "#/types/azure-native:media/v20200501:KeyVaultPropertiesResponse"
    description: The properties of the key used to encrypt the account.
  type:
    type: string
    description: The type of key used to encrypt the Account Key.
type: object
required:
  - type
