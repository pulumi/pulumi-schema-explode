$token: azure-native:media/v20180330preview:CopyVideo
description: A codec flag, which tells the encoder to copy the input video bitstream without re-encoding.
properties:
  label:
    type: string
    description: An optional label for the codec. The label can be used to control muxing behavior.
  odataType:
    type: string
    description: |-
      The discriminator for derived types.
      Expected value is '#Microsoft.Media.CopyVideo'.
    const: "#Microsoft.Media.CopyVideo"
type: object
required:
  - odataType
