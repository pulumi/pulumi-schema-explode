$token: azure-native:media/v20180330preview:listAssetContainerSas
description: The Asset Storage container SAS URLs.
inputs:
  properties:
    accountName:
      type: string
      description: The Media Services account name.
    assetName:
      type: string
      description: The Asset name.
    expiryTime:
      type: string
      description: The SAS URL expiration time.  This must be less than 24 hours from the current time.
    permissions:
      oneOf:
        - type: string
        - $ref: "#/types/azure-native:media/v20180330preview:AssetContainerPermission"
      description: The permissions to set on the SAS URL.
    resourceGroupName:
      type: string
      description: The name of the resource group within the Azure subscription.
  type: object
  required:
    - accountName
    - assetName
    - resourceGroupName
outputs:
  description: The Asset Storage container SAS URLs.
  properties:
    assetContainerSasUrls:
      type: array
      items:
        type: string
      description: The list of Asset container SAS URLs.
  type: object
