$token: azure-native:media/v20211101:listMediaServiceEdgePolicies
inputs:
  properties:
    accountName:
      type: string
      description: The Media Services account name.
    deviceId:
      type: string
      description: Unique identifier of the edge device.
    resourceGroupName:
      type: string
      description: The name of the resource group within the Azure subscription.
  type: object
  required:
    - accountName
    - resourceGroupName
outputs:
  properties:
    usageDataCollectionPolicy:
      type: object
      $ref: "#/types/azure-native:media/v20211101:EdgeUsageDataCollectionPolicyResponse"
  type: object
