$token: azure-native:media/v20211101:CopyAudio
description: A codec flag, which tells the encoder to copy the input audio bitstream.
properties:
  label:
    type: string
    description: An optional label for the codec. The label can be used to control muxing behavior.
  odataType:
    type: string
    description: |-
      The discriminator for derived types.
      Expected value is '#Microsoft.Media.CopyAudio'.
    const: "#Microsoft.Media.CopyAudio"
type: object
required:
  - odataType
