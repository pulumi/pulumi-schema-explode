$token: azure-native:hdinsight/v20150301preview:AutoscaleRecurrenceResponse
description: Schedule-based autoscale request parameters
properties:
  schedule:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:hdinsight/v20150301preview:AutoscaleScheduleResponse"
    description: Array of schedule-based autoscale rules
  timeZone:
    type: string
    description: The time zone for the autoscale schedule times
type: object
