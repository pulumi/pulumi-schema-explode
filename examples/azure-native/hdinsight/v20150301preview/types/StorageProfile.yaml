$token: azure-native:hdinsight/v20150301preview:StorageProfile
description: The storage profile.
properties:
  storageaccounts:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:hdinsight/v20150301preview:StorageAccount"
    description: The list of storage accounts in the cluster.
type: object
