$token: azure-native:hdinsight:AutoscaleRecurrence
description: Schedule-based autoscale request parameters
properties:
  schedule:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:hdinsight:AutoscaleSchedule"
    description: Array of schedule-based autoscale rules
  timeZone:
    type: string
    description: The time zone for the autoscale schedule times
type: object
