$token: azure-native:hdinsight:ComputeProfile
description: Describes the compute profile.
properties:
  roles:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:hdinsight:Role"
    description: The list of roles in the cluster.
type: object
