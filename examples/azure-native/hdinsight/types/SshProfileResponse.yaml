$token: azure-native:hdinsight:SshProfileResponse
description: The list of SSH public keys.
properties:
  publicKeys:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:hdinsight:SshPublicKeyResponse"
    description: The list of SSH public keys.
type: object
