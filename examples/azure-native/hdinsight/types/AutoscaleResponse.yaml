$token: azure-native:hdinsight:AutoscaleResponse
description: The autoscale request parameters
properties:
  capacity:
    type: object
    $ref: "#/types/azure-native:hdinsight:AutoscaleCapacityResponse"
    description: Parameters for load-based autoscale
  recurrence:
    type: object
    $ref: "#/types/azure-native:hdinsight:AutoscaleRecurrenceResponse"
    description: Parameters for schedule-based autoscale
type: object
