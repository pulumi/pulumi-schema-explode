$token: azure-native:hdinsight:StorageProfile
description: The storage profile.
properties:
  storageaccounts:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:hdinsight:StorageAccount"
    description: The list of storage accounts in the cluster.
type: object
