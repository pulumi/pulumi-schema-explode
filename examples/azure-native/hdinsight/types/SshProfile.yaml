$token: azure-native:hdinsight:SshProfile
description: The list of SSH public keys.
properties:
  publicKeys:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:hdinsight:SshPublicKey"
    description: The list of SSH public keys.
type: object
