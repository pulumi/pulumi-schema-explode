$token: azure-native:hdinsight:NetworkProperties
description: The network properties.
properties:
  privateLink:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:hdinsight:PrivateLink"
    description: Indicates whether or not private link is enabled.
  resourceProviderConnection:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:hdinsight:ResourceProviderConnection"
    description: The direction for the resource provider connection.
type: object
