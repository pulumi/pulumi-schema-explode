$token: azure-native:hdinsight:StorageProfileResponse
description: The storage profile.
properties:
  storageaccounts:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:hdinsight:StorageAccountResponse"
    description: The list of storage accounts in the cluster.
type: object
