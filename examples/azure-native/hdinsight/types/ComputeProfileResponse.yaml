$token: azure-native:hdinsight:ComputeProfileResponse
description: Describes the compute profile.
properties:
  roles:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:hdinsight:RoleResponse"
    description: The list of roles in the cluster.
type: object
