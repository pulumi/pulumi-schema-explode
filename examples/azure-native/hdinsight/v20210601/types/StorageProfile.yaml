$token: azure-native:hdinsight/v20210601:StorageProfile
description: The storage profile.
properties:
  storageaccounts:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:hdinsight/v20210601:StorageAccount"
    description: The list of storage accounts in the cluster.
type: object
