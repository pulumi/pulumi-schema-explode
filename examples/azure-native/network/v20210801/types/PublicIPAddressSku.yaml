$token: azure-native:network/v20210801:PublicIPAddressSku
description: SKU of a public IP address.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20210801:PublicIPAddressSkuName"
    description: Name of a public IP address SKU.
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20210801:PublicIPAddressSkuTier"
    description: Tier of a public IP address SKU.
type: object
