$token: azure-native:network/v20180801:ApplicationGatewaySku
description: SKU of an application gateway
properties:
  capacity:
    type: integer
    description: Capacity (instance count) of an application gateway.
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20180801:ApplicationGatewaySkuName"
    description: Name of an application gateway SKU.
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20180801:ApplicationGatewayTier"
    description: Tier of an application gateway.
type: object
