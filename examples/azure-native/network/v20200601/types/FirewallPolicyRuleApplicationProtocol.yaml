$token: azure-native:network/v20200601:FirewallPolicyRuleApplicationProtocol
description: Properties of the application rule protocol.
properties:
  port:
    type: integer
    description: Port number for the protocol, cannot be greater than 64000.
  protocolType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20200601:FirewallPolicyRuleApplicationProtocolType"
    description: Protocol type.
type: object
