$token: azure-native:network/v20200601:ConnectionMonitorEndpointFilterItem
description: Describes the connection monitor endpoint filter item.
properties:
  address:
    type: string
    description: The address of the filter item.
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20200601:ConnectionMonitorEndpointFilterItemType"
    description: The type of item included in the filter. Currently only 'AgentAddress' is supported.
type: object
