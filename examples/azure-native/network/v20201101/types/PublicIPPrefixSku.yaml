$token: azure-native:network/v20201101:PublicIPPrefixSku
description: SKU of a public IP prefix.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20201101:PublicIPPrefixSkuName"
    description: Name of a public IP prefix SKU.
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20201101:PublicIPPrefixSkuTier"
    description: Tier of a public IP prefix SKU.
type: object
