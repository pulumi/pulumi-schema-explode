$token: azure-native:network/v20201101:ConnectionMonitorEndpointScopeResponse
description: Describes the connection monitor endpoint scope.
properties:
  exclude:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:network/v20201101:ConnectionMonitorEndpointScopeItemResponse"
    description: List of items which needs to be excluded from the endpoint scope.
  include:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:network/v20201101:ConnectionMonitorEndpointScopeItemResponse"
    description: List of items which needs to be included to the endpoint scope.
type: object
