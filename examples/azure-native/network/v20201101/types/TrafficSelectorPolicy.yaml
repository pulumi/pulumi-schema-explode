$token: azure-native:network/v20201101:TrafficSelectorPolicy
description: An traffic selector policy for a virtual network gateway connection.
properties:
  localAddressRanges:
    type: array
    items:
      type: string
    description: A collection of local address spaces in CIDR format.
  remoteAddressRanges:
    type: array
    items:
      type: string
    description: A collection of remote address spaces in CIDR format.
type: object
required:
  - localAddressRanges
  - remoteAddressRanges
