$token: azure-native:network/v20201101:VnetRoute
description: List of routes that control routing from VirtualHub into a virtual network connection.
properties:
  staticRoutes:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:network/v20201101:StaticRoute"
    description: List of all Static Routes.
type: object
