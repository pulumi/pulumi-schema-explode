$token: azure-native:network/v20200801:PublicIPAddressSku
description: SKU of a public IP address.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20200801:PublicIPAddressSkuName"
    description: Name of a public IP address SKU.
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20200801:PublicIPAddressSkuTier"
    description: Tier of a public IP address SKU.
type: object
