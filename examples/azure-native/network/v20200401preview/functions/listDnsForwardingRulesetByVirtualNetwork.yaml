$token: azure-native:network/v20200401preview:listDnsForwardingRulesetByVirtualNetwork
description: The response to an enumeration operation on Virtual Network DNS Forwarding Ruleset.
inputs:
  properties:
    resourceGroupName:
      type: string
      description: The name of the resource group. The name is case insensitive.
    top:
      type: integer
      description: The maximum number of results to return. If not specified, returns up to 100 results.
    virtualNetworkName:
      type: string
      description: The name of the virtual network.
  type: object
  required:
    - resourceGroupName
    - virtualNetworkName
outputs:
  description: The response to an enumeration operation on Virtual Network DNS Forwarding Ruleset.
  properties:
    nextLink:
      type: string
      description: The continuation token for the next page of results.
    value:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:network/v20200401preview:VirtualNetworkDnsForwardingRulesetResponse"
      description: Enumeration of the Virtual Network DNS Forwarding Ruleset.
  type: object
  required:
    - nextLink
