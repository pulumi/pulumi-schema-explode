$token: azure-native:network/v20200701:ConnectionMonitorTcpConfiguration
description: Describes the TCP configuration.
properties:
  destinationPortBehavior:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20200701:DestinationPortBehavior"
    description: Destination port behavior.
  disableTraceRoute:
    type: boolean
    description: Value indicating whether path evaluation with trace route should be disabled.
  port:
    type: integer
    description: The port to connect to.
type: object
