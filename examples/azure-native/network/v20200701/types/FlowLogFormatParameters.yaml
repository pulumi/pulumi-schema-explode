$token: azure-native:network/v20200701:FlowLogFormatParameters
description: Parameters that define the flow log format.
properties:
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20200701:FlowLogFormatType"
    description: The file type of flow log.
  version:
    type: integer
    description: The version (revision) of the flow log.
    default: 0
type: object
