$token: azure-native:network/v20200701:ConnectionMonitorEndpointFilterType
description: The behavior of the endpoint filter. Currently only 'Include' is supported.
type: string
enum:
  - value: Include
