$token: azure-native:network/v20200701:ConnectionMonitorEndpointFilter
description: Describes the connection monitor endpoint filter.
properties:
  items:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:network/v20200701:ConnectionMonitorEndpointFilterItem"
    description: List of items in the filter.
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20200701:ConnectionMonitorEndpointFilterType"
    description: The behavior of the endpoint filter. Currently only 'Include' is supported.
type: object
