$token: azure-native:network/v20190201:OutboundRule
description: Outbound rule of the load balancer.
properties:
  allocatedOutboundPorts:
    type: integer
    description: The number of outbound ports to be used for NAT.
  backendAddressPool:
    type: object
    $ref: "#/types/azure-native:network/v20190201:SubResource"
    description: A reference to a pool of DIPs. Outbound traffic is randomly load balanced across IPs in the backend IPs.
  enableTcpReset:
    type: boolean
    description: Receive bidirectional TCP Reset on TCP flow idle timeout or unexpected connection termination. This element is only used when the protocol is set to TCP.
  etag:
    type: string
    description: A unique read-only string that changes whenever the resource is updated.
  frontendIPConfigurations:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:network/v20190201:SubResource"
    description: The Frontend IP addresses of the load balancer.
  id:
    type: string
    description: Resource ID.
  idleTimeoutInMinutes:
    type: integer
    description: The timeout for the TCP idle connection
  name:
    type: string
    description: The name of the resource that is unique within a resource group. This name can be used to access the resource.
  protocol:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20190201:LoadBalancerOutboundRuleProtocol"
    description: "The protocol for the outbound rule in load balancer. Possible values are: 'Tcp', 'Udp', and 'All'."
  provisioningState:
    type: string
    description: "Gets the provisioning state of the PublicIP resource. Possible values are: 'Updating', 'Deleting', and 'Failed'."
type: object
required:
  - backendAddressPool
  - frontendIPConfigurations
  - protocol
