$token: azure-native:network/v20190401:BackendPoolsSettings
description: Settings that apply to all backend pools.
properties:
  enforceCertificateNameCheck:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20190401:EnforceCertificateNameCheckEnabledState"
    description: Whether to enforce certificate name check on HTTPS requests to all backend pools. No effect on non-HTTPS requests.
    default: Enabled
type: object
