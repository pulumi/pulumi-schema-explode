$token: azure-native:network/v20210201:ConnectionMonitorEndpointFilterType
description: The behavior of the endpoint filter. Currently only 'Include' is supported.
type: string
enum:
  - value: Include
