$token: azure-native:network/v20210201:VpnSiteLinkConnection
description: VpnSiteLinkConnection Resource.
properties:
  connectionBandwidth:
    type: integer
    description: Expected bandwidth in MBPS.
  egressNatRules:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:network/v20210201:SubResource"
    description: List of egress NatRules.
  enableBgp:
    type: boolean
    description: EnableBgp flag.
  enableRateLimiting:
    type: boolean
    description: EnableBgp flag.
  id:
    type: string
    description: Resource ID.
  ingressNatRules:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:network/v20210201:SubResource"
    description: List of ingress NatRules.
  ipsecPolicies:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:network/v20210201:IpsecPolicy"
    description: The IPSec Policies to be considered by this connection.
  name:
    type: string
    description: The name of the resource that is unique within a resource group. This name can be used to access the resource.
  routingWeight:
    type: integer
    description: Routing weight for vpn connection.
  sharedKey:
    type: string
    description: SharedKey for the vpn connection.
  useLocalAzureIpAddress:
    type: boolean
    description: Use local azure ip to initiate connection.
  usePolicyBasedTrafficSelectors:
    type: boolean
    description: Enable policy-based traffic selectors.
  vpnConnectionProtocolType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20210201:VirtualNetworkGatewayConnectionProtocol"
    description: Connection protocol used for this connection.
  vpnLinkConnectionMode:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20210201:VpnLinkConnectionMode"
    description: Vpn link connection mode.
  vpnSiteLink:
    type: object
    $ref: "#/types/azure-native:network/v20210201:SubResource"
    description: Id of the connected vpn site link.
type: object
