$token: azure-native:network/v20210201:getNatRule
description: VpnGatewayNatRule Resource.
inputs:
  properties:
    gatewayName:
      type: string
      description: The name of the gateway.
    natRuleName:
      type: string
      description: The name of the nat rule.
    resourceGroupName:
      type: string
      description: The resource group name of the VpnGateway.
  type: object
  required:
    - gatewayName
    - natRuleName
    - resourceGroupName
outputs:
  description: VpnGatewayNatRule Resource.
  properties:
    egressVpnSiteLinkConnections:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:network/v20210201:SubResourceResponse"
      description: List of egress VpnSiteLinkConnections.
    etag:
      type: string
      description: A unique read-only string that changes whenever the resource is updated.
    externalMappings:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:network/v20210201:VpnNatRuleMappingResponse"
      description: The private IP address external mapping for NAT.
    id:
      type: string
      description: Resource ID.
    ingressVpnSiteLinkConnections:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:network/v20210201:SubResourceResponse"
      description: List of ingress VpnSiteLinkConnections.
    internalMappings:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:network/v20210201:VpnNatRuleMappingResponse"
      description: The private IP address internal mapping for NAT.
    ipConfigurationId:
      type: string
      description: The IP Configuration ID this NAT rule applies to.
    mode:
      type: string
      description: The Source NAT direction of a VPN NAT.
    name:
      type: string
      description: The name of the resource that is unique within a resource group. This name can be used to access the resource.
    provisioningState:
      type: string
      description: The provisioning state of the NAT Rule resource.
    type:
      type: string
      description: Resource type.
  type: object
  required:
    - egressVpnSiteLinkConnections
    - etag
    - ingressVpnSiteLinkConnections
    - provisioningState
    - type
