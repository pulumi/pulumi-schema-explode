$token: azure-native:network/v20220101:AzureFirewallApplicationRuleProtocol
description: Properties of the application rule protocol.
properties:
  port:
    type: integer
    description: Port number for the protocol, cannot be greater than 64000. This field is optional.
  protocolType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20220101:AzureFirewallApplicationRuleProtocolType"
    description: Protocol type.
type: object
