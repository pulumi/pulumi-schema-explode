$token: azure-native:network/v20220101:ConnectionMonitorEndpointResponse
description: Describes the connection monitor endpoint.
properties:
  address:
    type: string
    description: Address of the connection monitor endpoint (IP or domain name).
  coverageLevel:
    type: string
    description: Test coverage for the endpoint.
  filter:
    type: object
    $ref: "#/types/azure-native:network/v20220101:ConnectionMonitorEndpointFilterResponse"
    description: Filter for sub-items within the endpoint.
  name:
    type: string
    description: The name of the connection monitor endpoint.
  resourceId:
    type: string
    description: Resource ID of the connection monitor endpoint.
  scope:
    type: object
    $ref: "#/types/azure-native:network/v20220101:ConnectionMonitorEndpointScopeResponse"
    description: Endpoint scope.
  type:
    type: string
    description: The endpoint type.
type: object
required:
  - name
