$token: azure-native:network/v20220101:ConnectivityGroupItemResponse
description: Connectivity group item.
properties:
  groupConnectivity:
    type: string
    description: Group connectivity type.
  isGlobal:
    type: string
    description: Flag if global is supported.
  networkGroupId:
    type: string
    description: Network group Id.
  useHubGateway:
    type: string
    description: Flag if need to use hub gateway.
type: object
required:
  - groupConnectivity
  - networkGroupId
