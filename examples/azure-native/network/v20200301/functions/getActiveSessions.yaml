$token: azure-native:network/v20200301:getActiveSessions
description: Response for GetActiveSessions.
inputs:
  properties:
    bastionHostName:
      type: string
      description: The name of the Bastion Host.
    resourceGroupName:
      type: string
      description: The name of the resource group.
  type: object
  required:
    - bastionHostName
    - resourceGroupName
outputs:
  description: Response for GetActiveSessions.
  properties:
    nextLink:
      type: string
      description: The URL to get the next set of results.
    value:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:network/v20200301:BastionActiveSessionResponse"
      description: List of active sessions on the bastion.
  type: object
