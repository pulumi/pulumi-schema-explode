$token: azure-native:network/v20210501:PublicIPPrefixSku
description: SKU of a public IP prefix.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20210501:PublicIPPrefixSkuName"
    description: Name of a public IP prefix SKU.
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20210501:PublicIPPrefixSkuTier"
    description: Tier of a public IP prefix SKU.
type: object
