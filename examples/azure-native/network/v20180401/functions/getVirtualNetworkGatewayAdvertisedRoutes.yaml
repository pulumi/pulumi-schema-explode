$token: azure-native:network/v20180401:getVirtualNetworkGatewayAdvertisedRoutes
description: List of virtual network gateway routes
inputs:
  properties:
    peer:
      type: string
      description: The IP address of the peer
    resourceGroupName:
      type: string
      description: The name of the resource group.
    virtualNetworkGatewayName:
      type: string
      description: The name of the virtual network gateway.
  type: object
  required:
    - peer
    - resourceGroupName
    - virtualNetworkGatewayName
outputs:
  description: List of virtual network gateway routes
  properties:
    value:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:network/v20180401:GatewayRouteResponse"
      description: List of gateway routes
  type: object
