$token: azure-native:network/v20200501:ConnectionMonitorEndpointFilterResponse
description: Describes the connection monitor endpoint filter.
properties:
  items:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:network/v20200501:ConnectionMonitorEndpointFilterItemResponse"
    description: List of items in the filter.
  type:
    type: string
    description: The behavior of the endpoint filter. Currently only 'Include' is supported.
type: object
