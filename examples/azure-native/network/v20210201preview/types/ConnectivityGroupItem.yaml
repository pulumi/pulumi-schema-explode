$token: azure-native:network/v20210201preview:ConnectivityGroupItem
properties:
  groupConnectivity:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20210201preview:GroupConnectivity"
    description: Group connectivity type.
  isGlobal:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20210201preview:IsGlobal"
    description: Flag if global is supported.
  networkGroupId:
    type: string
    description: Network group Id.
  useHubGateway:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20210201preview:UseHubGateway"
    description: Flag if need to use hub gateway.
type: object
