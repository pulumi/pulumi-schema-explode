$token: azure-native:network/v20150615:Subnet
description: Subnet in a virtual network resource.
properties:
  addressPrefix:
    type: string
    description: The address prefix for the subnet.
  etag:
    type: string
    description: A unique read-only string that changes whenever the resource is updated.
  id:
    type: string
    description: Resource Identifier.
  ipConfigurations:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:network/v20150615:IPConfiguration"
    description: Gets an array of references to the network interface IP configurations using subnet.
  name:
    type: string
    description: The name of the resource that is unique within a resource group. This name can be used to access the resource.
  networkSecurityGroup:
    type: object
    $ref: "#/types/azure-native:network/v20150615:NetworkSecurityGroup"
    description: The reference of the NetworkSecurityGroup resource.
  provisioningState:
    type: string
    description: The provisioning state of the resource.
  routeTable:
    type: object
    $ref: "#/types/azure-native:network/v20150615:RouteTable"
    description: The reference of the RouteTable resource.
type: object
