$token: azure-native:network/v20150501preview:ApplicationGatewaySku
description: SKU of application gateway
properties:
  capacity:
    type: integer
    description: Gets or sets capacity (instance count) of application gateway
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20150501preview:ApplicationGatewaySkuName"
    description: Gets or sets name of application gateway SKU
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20150501preview:ApplicationGatewayTier"
    description: Gets or sets tier of application gateway
type: object
