$token: azure-native:network:FirewallPolicySku
description: SKU of Firewall policy.
properties:
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network:FirewallPolicySkuTier"
    description: Tier of Firewall Policy.
type: object
