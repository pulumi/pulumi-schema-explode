$token: azure-native:network:FirewallPolicyIntrusionDetectionResponse
description: Configuration for intrusion detection mode and rules.
properties:
  configuration:
    type: object
    $ref: "#/types/azure-native:network:FirewallPolicyIntrusionDetectionConfigurationResponse"
    description: Intrusion detection configuration properties.
  mode:
    type: string
    description: Intrusion detection general state.
type: object
