$token: azure-native:network:FirewallPolicyTransportSecurityResponse
description: Configuration needed to perform TLS termination & initiation.
properties:
  certificateAuthority:
    type: object
    $ref: "#/types/azure-native:network:FirewallPolicyCertificateAuthorityResponse"
    description: The CA used for intermediate CA generation.
type: object
