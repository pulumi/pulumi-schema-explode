$token: azure-native:network:NatGatewaySku
description: SKU of nat gateway.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network:NatGatewaySkuName"
    description: Name of Nat Gateway SKU.
type: object
