$token: azure-native:network:ManagedRuleSetListResponse
description: Defines the list of managed rule sets for the policy.
properties:
  managedRuleSets:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:network:FrontDoorManagedRuleSetResponse"
    description: List of rule sets.
type: object
