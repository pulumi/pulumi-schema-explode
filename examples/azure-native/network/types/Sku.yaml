$token: azure-native:network:Sku
description: The pricing tier of the web application firewall policy.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network:SkuName"
    description: Name of the pricing tier.
type: object
