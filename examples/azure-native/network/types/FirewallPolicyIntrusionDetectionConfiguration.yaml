$token: azure-native:network:FirewallPolicyIntrusionDetectionConfiguration
description: The operation for configuring intrusion detection.
properties:
  bypassTrafficSettings:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:network:FirewallPolicyIntrusionDetectionBypassTrafficSpecifications"
    description: List of rules for traffic to bypass.
  signatureOverrides:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:network:FirewallPolicyIntrusionDetectionSignatureSpecification"
    description: List of specific signatures states.
type: object
