$token: azure-native:network:FirewallPolicyRuleConditionApplicationProtocol
description: Properties of the application rule protocol.
properties:
  port:
    type: integer
    description: Port number for the protocol, cannot be greater than 64000.
  protocolType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network:FirewallPolicyRuleConditionApplicationProtocolType"
    description: Protocol type.
type: object
