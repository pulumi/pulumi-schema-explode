$token: azure-native:network:BastionShareableLink
description: Bastion Shareable Link.
properties:
  vm:
    type: object
    $ref: "#/types/azure-native:network:VM"
    description: Reference of the virtual machine resource.
type: object
required:
  - vm
