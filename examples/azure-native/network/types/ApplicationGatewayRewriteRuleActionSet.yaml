$token: azure-native:network:ApplicationGatewayRewriteRuleActionSet
description: Set of actions in the Rewrite Rule in Application Gateway.
properties:
  requestHeaderConfigurations:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:network:ApplicationGatewayHeaderConfiguration"
    description: Request Header Actions in the Action Set.
  responseHeaderConfigurations:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:network:ApplicationGatewayHeaderConfiguration"
    description: Response Header Actions in the Action Set.
  urlConfiguration:
    type: object
    $ref: "#/types/azure-native:network:ApplicationGatewayUrlConfiguration"
    description: Url Configuration Action in the Action Set.
type: object
