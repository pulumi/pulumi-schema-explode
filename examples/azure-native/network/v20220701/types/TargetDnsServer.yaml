$token: azure-native:network/v20220701:TargetDnsServer
description: Describes a server to forward the DNS queries to.
properties:
  ipAddress:
    type: string
    description: DNS server IP address.
  port:
    type: integer
    description: DNS server port.
    default: 53
type: object
required:
  - ipAddress
