$token: azure-native:network/v20191101:getVirtualNetworkGatewayLearnedRoutes
description: List of virtual network gateway routes.
inputs:
  properties:
    resourceGroupName:
      type: string
      description: The name of the resource group.
    virtualNetworkGatewayName:
      type: string
      description: The name of the virtual network gateway.
  type: object
  required:
    - resourceGroupName
    - virtualNetworkGatewayName
outputs:
  description: List of virtual network gateway routes.
  properties:
    value:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:network/v20191101:GatewayRouteResponse"
      description: List of gateway routes.
  type: object
