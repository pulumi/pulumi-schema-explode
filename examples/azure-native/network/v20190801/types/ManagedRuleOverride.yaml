$token: azure-native:network/v20190801:ManagedRuleOverride
description: Defines a managed rule group override setting.
properties:
  ruleId:
    type: string
    description: Identifier for the managed rule.
  state:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20190801:ManagedRuleEnabledState"
    description: Describes the state of the managed rule. Defaults to Disabled if not specified.
type: object
required:
  - ruleId
