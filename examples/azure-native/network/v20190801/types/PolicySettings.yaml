$token: azure-native:network/v20190801:PolicySettings
description: Defines contents of a web application firewall global configuration.
properties:
  fileUploadLimitInMb:
    type: integer
    description: Maximum file upload size in Mb for WAF.
  maxRequestBodySizeInKb:
    type: integer
    description: Maximum request body size in Kb for WAF.
  mode:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20190801:WebApplicationFirewallMode"
    description: Describes if it is in detection mode or prevention mode at policy level.
  requestBodyCheck:
    type: boolean
    description: Whether to allow WAF to check request Body.
  state:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20190801:WebApplicationFirewallEnabledState"
    description: Describes if the policy is in enabled state or disabled state.
type: object
