$token: azure-native:network/v20190901:FirewallPolicyRuleConditionApplicationProtocol
description: Properties of the application rule protocol.
properties:
  port:
    type: integer
    description: Port number for the protocol, cannot be greater than 64000.
  protocolType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20190901:FirewallPolicyRuleConditionApplicationProtocolType"
    description: Protocol type.
type: object
