$token: azure-native:network/v20160601:ApplicationGatewaySku
description: SKU of application gateway
properties:
  capacity:
    type: integer
    description: Capacity (instance count) of application gateway
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20160601:ApplicationGatewaySkuName"
    description: Name of application gateway SKU
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20160601:ApplicationGatewayTier"
    description: Tier of application gateway
type: object
