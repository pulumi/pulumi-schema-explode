$token: azure-native:network/v20190601:PolicySettings
description: Defines contents of a web application firewall global configuration.
properties:
  enabledState:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20190601:WebApplicationFirewallEnabledState"
    description: Describes if the policy is in enabled state or disabled state.
  mode:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20190601:WebApplicationFirewallMode"
    description: Describes if it is in detection mode or prevention mode at policy level.
type: object
