$token: azure-native:costmanagement/v20191101:PivotProperties
description: Each pivot must contain a 'type' and 'name'.
properties:
  name:
    type: string
    description: Data field to show in view.
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:costmanagement/v20191101:PivotTypeType"
    description: Data type to show in view.
type: object
