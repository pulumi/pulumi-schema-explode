$token: azure-native:costmanagement/v20190901:QueryGrouping
description: The group by expression to be used in the query.
properties:
  name:
    type: string
    description: The name of the column to group.
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:costmanagement/v20190901:QueryColumnType"
    description: Has type of the column to group.
type: object
required:
  - name
  - type
