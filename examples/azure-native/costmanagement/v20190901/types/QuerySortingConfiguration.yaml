$token: azure-native:costmanagement/v20190901:QuerySortingConfiguration
description: The configuration for sorting in the query.
properties:
  name:
    type: string
    description: The name of the column to use in sorting.
  querySortingDirection:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:costmanagement/v20190901:SortDirection"
    description: The sorting direction
type: object
