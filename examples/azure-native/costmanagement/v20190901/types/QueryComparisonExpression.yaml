$token: azure-native:costmanagement/v20190901:QueryComparisonExpression
description: The comparison expression to be used in the query.
properties:
  name:
    type: string
    description: The name of the column to use in comparison.
  operator:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:costmanagement/v20190901:OperatorType"
    description: The operator to use for comparison.
  values:
    type: array
    items:
      type: string
    description: Array of values to use for comparison
type: object
required:
  - name
  - operator
  - values
