$token: azure-native:costmanagement/v20220601preview:FileDestination
description: Destination of the view data. This is optional. Currently only csv format is supported.
properties:
  fileFormats:
    type: array
    items:
      oneOf:
        - type: string
        - $ref: "#/types/azure-native:costmanagement/v20220601preview:FileFormat"
    description: Destination of the view data. Currently only csv format is supported.
type: object
