$token: azure-native:costmanagement/v20220801preview:ReportConfigSorting
description: The order by expression to be used in the report.
properties:
  direction:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:costmanagement/v20220801preview:ReportConfigSortingType"
    description: Direction of sort.
  name:
    type: string
    description: The name of the column to sort.
type: object
required:
  - name
