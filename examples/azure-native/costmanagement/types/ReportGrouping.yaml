$token: azure-native:costmanagement:ReportGrouping
description: The group by expression to be used in the report.
properties:
  name:
    type: string
    description: The name of the column to group.
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:costmanagement:ReportColumnType"
    description: Has type of the column to group.
type: object
required:
  - name
  - type
