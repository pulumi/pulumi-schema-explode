$token: azure-native:costmanagement:CostAllocationRuleProperties
description: The properties of a cost allocation rule
properties:
  description:
    type: string
    description: Description of a cost allocation rule.
  details:
    type: object
    $ref: "#/types/azure-native:costmanagement:CostAllocationRuleDetails"
    description: Resource information for the cost allocation rule
  status:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:costmanagement:RuleStatus"
    description: Status of the rule
type: object
required:
  - details
  - status
