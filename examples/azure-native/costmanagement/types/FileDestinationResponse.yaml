$token: azure-native:costmanagement:FileDestinationResponse
description: Destination of the view data. Currently only csv format is supported.
properties:
  fileFormats:
    type: array
    items:
      type: string
    description: Destination of the view data. Currently only csv format is supported.
type: object
