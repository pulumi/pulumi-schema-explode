$token: azure-native:costmanagement:FileDestination
description: Destination of the view data. Currently only csv format is supported.
properties:
  fileFormats:
    type: array
    items:
      oneOf:
        - type: string
        - $ref: "#/types/azure-native:costmanagement:FileFormat"
    description: Destination of the view data. Currently only csv format is supported.
type: object
