$token: azure-native:costmanagement:ReportDefinition
description: The definition of a report.
properties:
  dataset:
    type: object
    $ref: "#/types/azure-native:costmanagement:ReportDataset"
    description: Has definition for data in this report.
  timePeriod:
    type: object
    $ref: "#/types/azure-native:costmanagement:ReportTimePeriod"
    description: Has time period for pulling data for the report.
  timeframe:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:costmanagement:TimeframeType"
    description: The time frame for pulling data for the report. If custom, then a specific time period must be provided.
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:costmanagement:ReportType"
    description: The type of the report.
type: object
required:
  - timeframe
  - type
