$token: azure-native:costmanagement:CostAllocationRuleDetailsResponse
description: Resource details of the cost allocation rule
properties:
  sourceResources:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:costmanagement:SourceCostAllocationResourceResponse"
    description: Source resources for cost allocation. At this time, this list can contain no more than one element.
  targetResources:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:costmanagement:TargetCostAllocationResourceResponse"
    description: Target resources for cost allocation. At this time, this list can contain no more than one element.
type: object
