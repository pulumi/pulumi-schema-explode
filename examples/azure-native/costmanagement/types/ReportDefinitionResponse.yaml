$token: azure-native:costmanagement:ReportDefinitionResponse
description: The definition of a report.
properties:
  dataset:
    type: object
    $ref: "#/types/azure-native:costmanagement:ReportDatasetResponse"
    description: Has definition for data in this report.
  timePeriod:
    type: object
    $ref: "#/types/azure-native:costmanagement:ReportTimePeriodResponse"
    description: Has time period for pulling data for the report.
  timeframe:
    type: string
    description: The time frame for pulling data for the report. If custom, then a specific time period must be provided.
  type:
    type: string
    description: The type of the report.
type: object
required:
  - timeframe
  - type
