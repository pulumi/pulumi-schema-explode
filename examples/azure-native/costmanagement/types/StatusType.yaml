$token: azure-native:costmanagement:StatusType
description: The status of the schedule. Whether active or not. If inactive, the report's scheduled execution is paused.
type: string
enum:
  - value: Active
  - value: Inactive
