$token: azure-native:costmanagement:ReportConfigSortingResponse
description: The order by expression to be used in the report.
properties:
  direction:
    type: string
    description: Direction of sort.
  name:
    type: string
    description: The name of the column to sort.
type: object
required:
  - name
