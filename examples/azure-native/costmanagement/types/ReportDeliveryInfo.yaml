$token: azure-native:costmanagement:ReportDeliveryInfo
description: The delivery information associated with a report.
properties:
  destination:
    type: object
    $ref: "#/types/azure-native:costmanagement:ReportDeliveryDestination"
    description: Has destination for the report being delivered.
type: object
required:
  - destination
