$token: azure-native:costmanagement:ExportDeliveryInfo
description: The delivery information associated with a export.
properties:
  destination:
    type: object
    $ref: "#/types/azure-native:costmanagement:ExportDeliveryDestination"
    description: Has destination for the export being delivered.
type: object
required:
  - destination
