$token: azure-native:costmanagement:getReport
description: |-
  A report resource.
  API Version: 2018-08-01-preview.
inputs:
  properties:
    reportName:
      type: string
      description: Report Name.
  type: object
  required:
    - reportName
outputs:
  description: A report resource.
  properties:
    definition:
      type: object
      $ref: "#/types/azure-native:costmanagement:ReportDefinitionResponse"
      description: Has definition for the report.
    deliveryInfo:
      type: object
      $ref: "#/types/azure-native:costmanagement:ReportDeliveryInfoResponse"
      description: Has delivery information for the report.
    format:
      type: string
      description: The format of the report being delivered.
    id:
      type: string
      description: Resource Id.
    name:
      type: string
      description: Resource name.
    schedule:
      type: object
      $ref: "#/types/azure-native:costmanagement:ReportScheduleResponse"
      description: Has schedule information for the report.
    tags:
      type: object
      additionalProperties:
        type: string
      description: Resource tags.
    type:
      type: string
      description: Resource type.
  type: object
  required:
    - definition
    - deliveryInfo
    - id
    - name
    - tags
    - type
