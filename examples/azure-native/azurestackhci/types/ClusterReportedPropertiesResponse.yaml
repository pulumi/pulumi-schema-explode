$token: azure-native:azurestackhci:ClusterReportedPropertiesResponse
description: Properties reported by cluster agent.
properties:
  clusterId:
    type: string
    description: Unique id generated by the on-prem cluster.
  clusterName:
    type: string
    description: Name of the on-prem cluster connected to this resource.
  clusterVersion:
    type: string
    description: Version of the cluster software.
  lastUpdated:
    type: string
    description: Last time the cluster reported the data.
  nodes:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:azurestackhci:ClusterNodeResponse"
    description: List of nodes reported by the cluster.
type: object
required:
  - clusterId
  - clusterName
  - clusterVersion
  - lastUpdated
  - nodes
