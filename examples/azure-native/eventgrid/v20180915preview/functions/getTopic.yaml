$token: azure-native:eventgrid/v20180915preview:getTopic
description: EventGrid Topic
inputs:
  properties:
    resourceGroupName:
      type: string
      description: The name of the resource group within the user's subscription.
    topicName:
      type: string
      description: Name of the topic
  type: object
  required:
    - resourceGroupName
    - topicName
outputs:
  description: EventGrid Topic
  properties:
    endpoint:
      type: string
      description: Endpoint for the topic.
    id:
      type: string
      description: Fully qualified identifier of the resource
    inputSchema:
      type: string
      description: This determines the format that Event Grid should expect for incoming events published to the topic.
      default: EventGridSchema
    inputSchemaMapping:
      type: object
      $ref: "#/types/azure-native:eventgrid/v20180915preview:JsonInputSchemaMappingResponse"
      description: This enables publishing using custom event schemas. An InputSchemaMapping can be specified to map various properties of a source schema to various required properties of the EventGridEvent schema.
    location:
      type: string
      description: Location of the resource
    name:
      type: string
      description: Name of the resource
    provisioningState:
      type: string
      description: Provisioning state of the topic.
    tags:
      type: object
      additionalProperties:
        type: string
      description: Tags of the resource
    type:
      type: string
      description: Type of the resource
  type: object
  required:
    - endpoint
    - id
    - location
    - name
    - provisioningState
    - type
deprecationMessage: Version 2018-09-15-preview will be removed in v2 of the provider.
