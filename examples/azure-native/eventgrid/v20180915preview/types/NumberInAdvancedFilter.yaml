$token: azure-native:eventgrid/v20180915preview:NumberInAdvancedFilter
description: NumberIn filter
properties:
  key:
    type: string
    description: The filter key. Represents an event property with up to two levels of nesting.
  operatorType:
    type: string
    description: |-
      Represents the filter operator
      Expected value is 'NumberIn'.
    const: NumberIn
  values:
    type: array
    items:
      type: number
    description: The set of filter values
type: object
required:
  - operatorType
