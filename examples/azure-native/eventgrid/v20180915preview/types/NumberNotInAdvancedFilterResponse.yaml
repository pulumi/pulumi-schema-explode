$token: azure-native:eventgrid/v20180915preview:NumberNotInAdvancedFilterResponse
description: NumberNotIn Filter
properties:
  key:
    type: string
    description: The filter key. Represents an event property with up to two levels of nesting.
  operatorType:
    type: string
    description: |-
      Represents the filter operator
      Expected value is 'NumberNotIn'.
    const: NumberNotIn
  values:
    type: array
    items:
      type: number
    description: The set of filter values
type: object
required:
  - operatorType
