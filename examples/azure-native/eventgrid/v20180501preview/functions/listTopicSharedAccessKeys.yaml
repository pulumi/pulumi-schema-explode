$token: azure-native:eventgrid/v20180501preview:listTopicSharedAccessKeys
description: Shared access keys of the Topic
inputs:
  properties:
    resourceGroupName:
      type: string
      description: The name of the resource group within the user's subscription.
    topicName:
      type: string
      description: Name of the topic
  type: object
  required:
    - resourceGroupName
    - topicName
outputs:
  description: Shared access keys of the Topic
  properties:
    key1:
      type: string
      description: Shared access key1 for the topic.
    key2:
      type: string
      description: Shared access key2 for the topic.
  type: object
