$token: azure-native:eventgrid/v20220615:getDomainTopicEventSubscriptionFullUrl
description: Full endpoint url of an event subscription
inputs:
  properties:
    domainName:
      type: string
      description: Name of the top level domain.
    eventSubscriptionName:
      type: string
      description: Name of the event subscription.
    resourceGroupName:
      type: string
      description: The name of the resource group within the user's subscription.
    topicName:
      type: string
      description: Name of the domain topic.
  type: object
  required:
    - domainName
    - eventSubscriptionName
    - resourceGroupName
    - topicName
outputs:
  description: Full endpoint url of an event subscription
  properties:
    endpointUrl:
      type: string
      description: The URL that represents the endpoint of the destination of an event subscription.
  type: object
