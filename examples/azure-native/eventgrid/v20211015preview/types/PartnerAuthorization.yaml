$token: azure-native:eventgrid/v20211015preview:PartnerAuthorization
description: The partner authorization details.
properties:
  authorizedPartnersList:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:eventgrid/v20211015preview:Partner"
    description: The list of authorized partners.
  defaultMaximumExpirationTimeInDays:
    type: integer
    description: "Time used to validate the authorization expiration time for each authorized partner. If DefaultMaximumExpirationTimeInDays is\r

      not specified, the default is 7 days. Otherwise, allowed values are between 1 and 365 days."
type: object
