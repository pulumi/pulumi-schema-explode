$token: azure-native:eventgrid/v20211015preview:PartnerTopicRoutingMode
description: "This determines if events published to this partner namespace should use the source attribute in the event payload\r

  or use the channel name in the header when matching to the partner topic. If none is specified, source attribute routing will be used to match the partner topic."
type: string
enum:
  - value: SourceEventAttribute
  - value: ChannelNameHeader
