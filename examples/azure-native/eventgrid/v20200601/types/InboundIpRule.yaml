$token: azure-native:eventgrid/v20200601:InboundIpRule
properties:
  action:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:eventgrid/v20200601:IpActionType"
    description: Action to perform based on the match or no match of the IpMask.
  ipMask:
    type: string
    description: IP Address in CIDR notation e.g., 10.0.0.0/8.
type: object
