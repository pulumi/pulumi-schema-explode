$token: azure-native:eventgrid/v20190201preview:getDomainTopic
description: Domain Topic
inputs:
  properties:
    domainName:
      type: string
      description: Name of the domain
    domainTopicName:
      type: string
      description: Name of the topic
    resourceGroupName:
      type: string
      description: The name of the resource group within the user's subscription.
  type: object
  required:
    - domainName
    - domainTopicName
    - resourceGroupName
outputs:
  description: Domain Topic
  properties:
    id:
      type: string
      description: Fully qualified identifier of the resource
    name:
      type: string
      description: Name of the resource
    provisioningState:
      type: string
      description: Provisioning state of the domain topic.
    type:
      type: string
      description: Type of the resource
  type: object
  required:
    - id
    - name
    - type
deprecationMessage: Version 2019-02-01-preview will be removed in v2 of the provider.
