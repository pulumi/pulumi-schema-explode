$token: azure-native:resources/v20180501:getDeploymentAtSubscriptionScope
description: Deployment information.
inputs:
  properties:
    deploymentName:
      type: string
      description: The name of the deployment to get.
  type: object
  required:
    - deploymentName
outputs:
  description: Deployment information.
  properties:
    id:
      type: string
      description: The ID of the deployment.
    location:
      type: string
      description: the location of the deployment.
    name:
      type: string
      description: The name of the deployment.
    properties:
      type: object
      $ref: "#/types/azure-native:resources/v20180501:DeploymentPropertiesExtendedResponse"
      description: Deployment properties.
    type:
      type: string
      description: The type of the deployment.
  type: object
  required:
    - id
    - name
    - properties
    - type
deprecationMessage: Version 2018-05-01 will be removed in v2 of the provider.
