$token: azure-native:resources/v20180201:getDeployment
description: Deployment information.
inputs:
  properties:
    deploymentName:
      type: string
      description: The name of the deployment to get.
    resourceGroupName:
      type: string
      description: The name of the resource group. The name is case insensitive.
  type: object
  required:
    - deploymentName
    - resourceGroupName
outputs:
  description: Deployment information.
  properties:
    id:
      type: string
      description: The ID of the deployment.
    name:
      type: string
      description: The name of the deployment.
    properties:
      type: object
      $ref: "#/types/azure-native:resources/v20180201:DeploymentPropertiesExtendedResponse"
      description: Deployment properties.
  type: object
  required:
    - id
    - name
    - properties
deprecationMessage: Version 2018-02-01 will be removed in v2 of the provider.
