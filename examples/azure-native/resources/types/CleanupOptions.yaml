$token: azure-native:resources:CleanupOptions
description: The clean up preference when the script execution gets in a terminal state. Default setting is 'Always'.
type: string
enum:
  - value: Always
  - value: OnSuccess
  - value: OnExpiration
