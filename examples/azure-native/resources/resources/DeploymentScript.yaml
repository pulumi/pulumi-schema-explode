$token: azure-native:resources:DeploymentScript
description: |-
  Deployment script object.
  API Version: 2020-10-01.
properties:
  identity:
    type: object
    $ref: "#/types/azure-native:resources:ManagedServiceIdentityResponse"
    description: Optional property. Managed identity to be used for this deployment script. Currently, only user-assigned MSI is supported.
  kind:
    type: string
    description: Type of the script.
  location:
    type: string
    description: The location of the ACI and the storage account for the deployment script.
  name:
    type: string
    description: Name of this resource.
  systemData:
    type: object
    $ref: "#/types/azure-native:resources:SystemDataResponse"
    description: The system metadata related to this resource.
  tags:
    type: object
    additionalProperties:
      type: string
    description: Resource tags.
  type:
    type: string
    description: Type of this resource.
type: object
required:
  - kind
  - location
  - name
  - systemData
  - type
inputProperties:
  identity:
    type: object
    $ref: "#/types/azure-native:resources:ManagedServiceIdentity"
    description: Optional property. Managed identity to be used for this deployment script. Currently, only user-assigned MSI is supported.
  kind:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:resources:ScriptType"
    description: Type of the script.
  location:
    type: string
    description: The location of the ACI and the storage account for the deployment script.
  resourceGroupName:
    type: string
    description: The name of the resource group. The name is case insensitive.
  scriptName:
    type: string
    description: Name of the deployment script.
  tags:
    type: object
    additionalProperties:
      type: string
    description: Resource tags.
requiredInputs:
  - kind
  - resourceGroupName
aliases:
  - type: azure-native:resources/v20191001preview:DeploymentScript
  - type: azure-native:resources/v20201001:DeploymentScript
deprecationMessage: "Please use one of the variants: AzureCliScript, AzurePowerShellScript."
