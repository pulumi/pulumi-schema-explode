$token: azure-native:resources/v20151101:getDeployment
description: Deployment information.
inputs:
  properties:
    deploymentName:
      type: string
      description: The name of the deployment.
    resourceGroupName:
      type: string
      description: The name of the resource group to get. The name is case insensitive.
  type: object
  required:
    - deploymentName
    - resourceGroupName
outputs:
  description: Deployment information.
  properties:
    id:
      type: string
      description: Gets or sets the ID of the deployment.
    name:
      type: string
      description: Gets or sets the name of the deployment.
    properties:
      type: object
      $ref: "#/types/azure-native:resources/v20151101:DeploymentPropertiesExtendedResponse"
      description: Gets or sets deployment properties.
  type: object
  required:
    - id
    - name
    - properties
deprecationMessage: Version 2015-11-01 will be removed in v2 of the provider.
