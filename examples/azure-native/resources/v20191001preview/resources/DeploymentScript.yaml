$token: azure-native:resources/v20191001preview:DeploymentScript
description: Deployment script object.
properties:
  identity:
    type: object
    $ref: "#/types/azure-native:resources/v20191001preview:ManagedServiceIdentityResponse"
    description: Managed identity to be used for this deployment script. Currently, only user-assigned MSI is supported.
  kind:
    type: string
    description: Type of the script.
  location:
    type: string
    description: The location of the ACI and the storage account for the deployment script.
  name:
    type: string
    description: Name of this resource.
  systemData:
    type: object
    $ref: "#/types/azure-native:resources/v20191001preview:SystemDataResponse"
    description: The system metadata related to this resource.
  tags:
    type: object
    additionalProperties:
      type: string
    description: Resource tags.
  type:
    type: string
    description: Type of this resource.
type: object
required:
  - identity
  - kind
  - location
  - name
  - systemData
  - type
inputProperties:
  identity:
    type: object
    $ref: "#/types/azure-native:resources/v20191001preview:ManagedServiceIdentity"
    description: Managed identity to be used for this deployment script. Currently, only user-assigned MSI is supported.
  kind:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:resources/v20191001preview:ScriptType"
    description: Type of the script.
  location:
    type: string
    description: The location of the ACI and the storage account for the deployment script.
  resourceGroupName:
    type: string
    description: The name of the resource group. The name is case insensitive.
  scriptName:
    type: string
    description: Name of the deployment script.
  tags:
    type: object
    additionalProperties:
      type: string
    description: Resource tags.
requiredInputs:
  - identity
  - kind
  - resourceGroupName
aliases:
  - type: azure-native:resources:DeploymentScript
  - type: azure-native:resources/v20201001:DeploymentScript
deprecationMessage: "Please use one of the variants: AzureCliScript, AzurePowerShellScript."
