$token: azure-native:resources/v20190701:getDeployment
description: Deployment information.
inputs:
  properties:
    deploymentName:
      type: string
      description: The name of the deployment.
    resourceGroupName:
      type: string
      description: The name of the resource group. The name is case insensitive.
  type: object
  required:
    - deploymentName
    - resourceGroupName
outputs:
  description: Deployment information.
  properties:
    id:
      type: string
      description: The ID of the deployment.
    location:
      type: string
      description: the location of the deployment.
    name:
      type: string
      description: The name of the deployment.
    properties:
      type: object
      $ref: "#/types/azure-native:resources/v20190701:DeploymentPropertiesExtendedResponse"
      description: Deployment properties.
    type:
      type: string
      description: The type of the deployment.
  type: object
  required:
    - id
    - name
    - properties
    - type
