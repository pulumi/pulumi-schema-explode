$token: azure-native:resources/v20190701:getDeploymentAtScope
description: Deployment information.
inputs:
  properties:
    deploymentName:
      type: string
      description: The name of the deployment.
    scope:
      type: string
      description: The scope of a deployment.
  type: object
  required:
    - deploymentName
    - scope
outputs:
  description: Deployment information.
  properties:
    id:
      type: string
      description: The ID of the deployment.
    location:
      type: string
      description: the location of the deployment.
    name:
      type: string
      description: The name of the deployment.
    properties:
      type: object
      $ref: "#/types/azure-native:resources/v20190701:DeploymentPropertiesExtendedResponse"
      description: Deployment properties.
    type:
      type: string
      description: The type of the deployment.
  type: object
  required:
    - id
    - name
    - properties
    - type
