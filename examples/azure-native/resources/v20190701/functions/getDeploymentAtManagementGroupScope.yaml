$token: azure-native:resources/v20190701:getDeploymentAtManagementGroupScope
description: Deployment information.
inputs:
  properties:
    deploymentName:
      type: string
      description: The name of the deployment.
    groupId:
      type: string
      description: The management group ID.
  type: object
  required:
    - deploymentName
    - groupId
outputs:
  description: Deployment information.
  properties:
    id:
      type: string
      description: The ID of the deployment.
    location:
      type: string
      description: the location of the deployment.
    name:
      type: string
      description: The name of the deployment.
    properties:
      type: object
      $ref: "#/types/azure-native:resources/v20190701:DeploymentPropertiesExtendedResponse"
      description: Deployment properties.
    type:
      type: string
      description: The type of the deployment.
  type: object
  required:
    - id
    - name
    - properties
    - type
