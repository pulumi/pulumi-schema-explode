$token: azure-native:edgeorder/v20211201:AddressDetailsResponse
description: Address details for an order item.
properties:
  forwardAddress:
    type: object
    $ref: "#/types/azure-native:edgeorder/v20211201:AddressPropertiesResponse"
    description: Customer address and contact details. It should be address resource
  returnAddress:
    type: object
    $ref: "#/types/azure-native:edgeorder/v20211201:AddressPropertiesResponse"
    description: Return shipping address
type: object
required:
  - forwardAddress
  - returnAddress
