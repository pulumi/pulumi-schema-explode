$token: azure-native:edgeorder/v20201201preview:AddressDetails
description: Address details for an order item.
properties:
  forwardAddress:
    type: object
    $ref: "#/types/azure-native:edgeorder/v20201201preview:AddressProperties"
    description: Customer address and contact details. It should be address resource
type: object
required:
  - forwardAddress
