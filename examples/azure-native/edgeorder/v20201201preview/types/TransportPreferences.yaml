$token: azure-native:edgeorder/v20201201preview:TransportPreferences
description: Preferences related to the shipment logistics of the sku
properties:
  preferredShipmentType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:edgeorder/v20201201preview:TransportShipmentTypes"
    description: Indicates Shipment Logistics type that the customer preferred.
type: object
required:
  - preferredShipmentType
