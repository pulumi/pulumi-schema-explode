$token: azure-native:edgeorder/v20201201preview:AddressDetailsResponse
description: Address details for an order item.
properties:
  forwardAddress:
    type: object
    $ref: "#/types/azure-native:edgeorder/v20201201preview:AddressPropertiesResponse"
    description: Customer address and contact details. It should be address resource
  returnAddress:
    type: object
    $ref: "#/types/azure-native:edgeorder/v20201201preview:AddressPropertiesResponse"
    description: Return shipping address
type: object
required:
  - forwardAddress
  - returnAddress
