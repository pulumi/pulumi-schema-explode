$token: azure-native:edgeorder:AddressProperties
description: Address Properties
properties:
  contactDetails:
    type: object
    $ref: "#/types/azure-native:edgeorder:ContactDetails"
    description: Contact details for the address
  shippingAddress:
    type: object
    $ref: "#/types/azure-native:edgeorder:ShippingAddress"
    description: Shipping details for the address
type: object
required:
  - contactDetails
