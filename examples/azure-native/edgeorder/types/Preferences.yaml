$token: azure-native:edgeorder:Preferences
description: Preferences related to the order
properties:
  encryptionPreferences:
    type: object
    $ref: "#/types/azure-native:edgeorder:EncryptionPreferences"
    description: Preferences related to the Encryption.
  managementResourcePreferences:
    type: object
    $ref: "#/types/azure-native:edgeorder:ManagementResourcePreferences"
    description: Preferences related to the Management resource.
  notificationPreferences:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:edgeorder:NotificationPreference"
    description: Notification preferences.
  transportPreferences:
    type: object
    $ref: "#/types/azure-native:edgeorder:TransportPreferences"
    description: Preferences related to the shipment logistics of the order.
type: object
