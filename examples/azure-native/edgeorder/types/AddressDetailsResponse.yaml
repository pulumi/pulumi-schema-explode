$token: azure-native:edgeorder:AddressDetailsResponse
description: Address details for an order item.
properties:
  forwardAddress:
    type: object
    $ref: "#/types/azure-native:edgeorder:AddressPropertiesResponse"
    description: Customer address and contact details. It should be address resource
  returnAddress:
    type: object
    $ref: "#/types/azure-native:edgeorder:AddressPropertiesResponse"
    description: Return shipping address
type: object
required:
  - forwardAddress
  - returnAddress
