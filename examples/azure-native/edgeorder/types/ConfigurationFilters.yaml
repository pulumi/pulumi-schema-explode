$token: azure-native:edgeorder:ConfigurationFilters
description: Configuration filters
properties:
  filterableProperty:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:edgeorder:FilterableProperty"
    description: Filters specific to product
  hierarchyInformation:
    type: object
    $ref: "#/types/azure-native:edgeorder:HierarchyInformation"
    description: Product hierarchy information
type: object
required:
  - hierarchyInformation
