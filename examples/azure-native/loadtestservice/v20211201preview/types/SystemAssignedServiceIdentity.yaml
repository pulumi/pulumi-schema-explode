$token: azure-native:loadtestservice/v20211201preview:SystemAssignedServiceIdentity
description: Managed service identity (either system assigned, or none)
properties:
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:loadtestservice/v20211201preview:SystemAssignedServiceIdentityType"
    description: Type of managed service identity (either system assigned, or none).
type: object
required:
  - type
