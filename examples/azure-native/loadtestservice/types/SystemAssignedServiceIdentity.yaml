$token: azure-native:loadtestservice:SystemAssignedServiceIdentity
description: Managed service identity (either system assigned, or none)
properties:
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:loadtestservice:SystemAssignedServiceIdentityType"
    description: Type of managed service identity (either system assigned, or none).
type: object
required:
  - type
