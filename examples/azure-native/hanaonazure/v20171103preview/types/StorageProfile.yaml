$token: azure-native:hanaonazure/v20171103preview:StorageProfile
description: Specifies the storage settings for the HANA instance disks.
properties:
  hanaSids:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:hanaonazure/v20171103preview:SAPSystemID"
    description: Specifies information related to SAP system IDs for the hana instance.
  osDisks:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:hanaonazure/v20171103preview:Disk"
    description: Specifies information about the operating system disk used by the hana instance.
type: object
