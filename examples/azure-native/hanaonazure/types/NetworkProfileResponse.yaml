$token: azure-native:hanaonazure:NetworkProfileResponse
description: Specifies the network settings for the HANA instance disks.
properties:
  circuitId:
    type: string
    description: Specifies the circuit id for connecting to express route.
  networkInterfaces:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:hanaonazure:IpAddressResponse"
    description: Specifies the network interfaces for the HANA instance.
type: object
required:
  - circuitId
