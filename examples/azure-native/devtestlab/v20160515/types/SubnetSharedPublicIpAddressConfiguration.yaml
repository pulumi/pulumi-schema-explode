$token: azure-native:devtestlab/v20160515:SubnetSharedPublicIpAddressConfiguration
description: Configuration for public IP address sharing.
properties:
  allowedPorts:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:devtestlab/v20160515:Port"
    description: Backend ports that virtual machines on this subnet are allowed to expose
type: object
