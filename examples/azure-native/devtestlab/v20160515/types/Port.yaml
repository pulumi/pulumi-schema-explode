$token: azure-native:devtestlab/v20160515:Port
description: Properties of a network port.
properties:
  backendPort:
    type: integer
    description: Backend port of the target virtual machine.
  transportProtocol:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:devtestlab/v20160515:TransportProtocol"
    description: Protocol type of the port.
type: object
