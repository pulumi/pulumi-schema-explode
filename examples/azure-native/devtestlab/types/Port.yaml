$token: azure-native:devtestlab:Port
description: Properties of a network port.
properties:
  backendPort:
    type: integer
    description: Backend port of the target virtual machine.
  transportProtocol:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:devtestlab:TransportProtocol"
    description: Protocol type of the port.
type: object
