$token: azure-native:devtestlab:SharedPublicIpAddressConfigurationResponse
description: Properties of a virtual machine that determine how it is connected to a load balancer.
properties:
  inboundNatRules:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:devtestlab:InboundNatRuleResponse"
    description: The incoming NAT rules
type: object
