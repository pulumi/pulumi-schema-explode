$token: azure-native:devtestlab:WindowsOsInfo
description: Information about a Windows OS.
properties:
  windowsOsState:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:devtestlab:WindowsOsState"
    description: The state of the Windows OS (i.e. NonSysprepped, SysprepRequested, SysprepApplied).
type: object
