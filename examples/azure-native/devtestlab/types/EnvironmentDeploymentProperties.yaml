$token: azure-native:devtestlab:EnvironmentDeploymentProperties
description: Properties of an environment deployment.
properties:
  armTemplateId:
    type: string
    description: The Azure Resource Manager template's identifier.
  parameters:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:devtestlab:ArmTemplateParameterProperties"
    description: The parameters of the Azure Resource Manager template.
type: object
