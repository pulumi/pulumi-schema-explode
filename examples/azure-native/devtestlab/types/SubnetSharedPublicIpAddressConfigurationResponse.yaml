$token: azure-native:devtestlab:SubnetSharedPublicIpAddressConfigurationResponse
description: Configuration for public IP address sharing.
properties:
  allowedPorts:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:devtestlab:PortResponse"
    description: Backend ports that virtual machines on this subnet are allowed to expose
type: object
