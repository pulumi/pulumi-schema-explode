$token: azure-native:devtestlab:Event
description: An event to be notified for.
properties:
  eventName:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:devtestlab:NotificationChannelEventType"
    description: The event type for which this notification is enabled (i.e. AutoShutdown, Cost)
type: object
