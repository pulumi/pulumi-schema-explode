$token: azure-native:authorization/v20170601preview:getPolicyAssignment
description: The policy assignment.
inputs:
  properties:
    policyAssignmentName:
      type: string
      description: The name of the policy assignment to get.
    scope:
      type: string
      description: The scope of the policy assignment.
  type: object
  required:
    - policyAssignmentName
    - scope
outputs:
  description: The policy assignment.
  properties:
    description:
      type: string
      description: This message will be part of response in case of policy violation.
    displayName:
      type: string
      description: The display name of the policy assignment.
    id:
      type: string
      description: The ID of the policy assignment.
    metadata:
      $ref: pulumi.json#/Any
      description: The policy assignment metadata.
    name:
      type: string
      description: The name of the policy assignment.
    notScopes:
      type: array
      items:
        type: string
      description: The policy's excluded scopes.
    parameters:
      $ref: pulumi.json#/Any
      description: Required if a parameter is used in policy rule.
    policyDefinitionId:
      type: string
      description: The ID of the policy definition.
    scope:
      type: string
      description: The scope for the policy assignment.
    sku:
      type: object
      $ref: "#/types/azure-native:authorization/v20170601preview:PolicySkuResponse"
      description: The policy sku.
    type:
      type: string
      description: The type of the policy assignment.
  type: object
  required:
    - id
    - name
    - type
