$token: azure-native:databricks:IdentityData
description: Identity for the resource.
properties:
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:databricks:IdentityType"
    description: The identity type.
type: object
required:
  - type
