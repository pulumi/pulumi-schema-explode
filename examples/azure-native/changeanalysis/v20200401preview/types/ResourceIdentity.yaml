$token: azure-native:changeanalysis/v20200401preview:ResourceIdentity
description: The identity block returned by ARM resource that supports managed identity.
properties:
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:changeanalysis/v20200401preview:ManagedIdentityTypes"
    description: The type of managed identity used. The type 'SystemAssigned, UserAssigned' includes both an implicitly created identity and a set of user-assigned identities. The type 'None' will remove any identities.
type: object
