$token: azure-native:changeanalysis:ConfigurationProfileResourceProperties
description: The properties of a configuration profile.
properties:
  notifications:
    type: object
    $ref: "#/types/azure-native:changeanalysis:NotificationSettings"
    description: Settings of change notification configuration for a subscription.
type: object
