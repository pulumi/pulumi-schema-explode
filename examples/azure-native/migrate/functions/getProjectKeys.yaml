$token: azure-native:migrate:getProjectKeys
description: |-
  ID and Key for Migration Project.
  API Version: 2018-02-02.
inputs:
  properties:
    projectName:
      type: string
      description: Name of the Azure Migrate project.
    resourceGroupName:
      type: string
      description: Name of the Azure Resource Group that project is part of.
  type: object
  required:
    - projectName
    - resourceGroupName
outputs:
  description: ID and Key for Migration Project.
  properties:
    workspaceId:
      type: string
      description: ID of Migration Project.
    workspaceKey:
      type: string
      description: Key of Migration Project.
  type: object
  required:
    - workspaceId
    - workspaceKey
