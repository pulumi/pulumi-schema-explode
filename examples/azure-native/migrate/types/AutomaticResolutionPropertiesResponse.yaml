$token: azure-native:migrate:AutomaticResolutionPropertiesResponse
description: Defines the properties for automatic resolution.
properties:
  moveResourceId:
    type: string
    description: "Gets the MoveResource ARM ID of\r

      the dependent resource if the resolution type is Automatic."
type: object
