$token: azure-native:migrate:MigrateProjectProperties
description: Class for migrate project properties.
properties:
  provisioningState:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:migrate:ProvisioningState"
    description: Provisioning state of the migrate project.
  registeredTools:
    type: array
    items:
      type: string
    description: Gets or sets the list of tools registered with the migrate project.
type: object
