$token: azure-native:migrate:SqlDatabaseResourceSettings
description: Defines the Sql Database resource settings.
properties:
  resourceType:
    type: string
    description: |-
      The resource type. For example, the value can be Microsoft.Compute/virtualMachines.
      Expected value is 'Microsoft.Sql/servers/databases'.
    const: Microsoft.Sql/servers/databases
  targetResourceName:
    type: string
    description: Gets or sets the target Resource name.
  zoneRedundant:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:migrate:ZoneRedundant"
    description: Defines the zone redundant resource setting.
type: object
required:
  - resourceType
  - targetResourceName
