$token: azure-native:migrate:PrivateEndpointConnectionProperties
description: Private endpoint connection properties.
properties:
  privateLinkServiceConnectionState:
    type: object
    $ref: "#/types/azure-native:migrate:PrivateLinkServiceConnectionState"
    description: State of the private endpoint connection.
type: object
