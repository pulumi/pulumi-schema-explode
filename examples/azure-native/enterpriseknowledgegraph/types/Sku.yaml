$token: azure-native:enterpriseknowledgegraph:Sku
description: The SKU of the EnterpriseKnowledgeGraph service account.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:enterpriseknowledgegraph:SkuName"
    description: The sku name
type: object
required:
  - name
