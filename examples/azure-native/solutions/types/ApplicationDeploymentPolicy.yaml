$token: azure-native:solutions:ApplicationDeploymentPolicy
description: Managed application deployment policy.
properties:
  deploymentMode:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:solutions:DeploymentMode"
    description: The managed application deployment mode.
type: object
required:
  - deploymentMode
