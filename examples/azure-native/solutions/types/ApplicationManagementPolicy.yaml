$token: azure-native:solutions:ApplicationManagementPolicy
description: Managed application management policy.
properties:
  mode:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:solutions:ApplicationManagementMode"
    description: The managed application management mode.
type: object
