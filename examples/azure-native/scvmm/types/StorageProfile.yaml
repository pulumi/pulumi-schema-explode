$token: azure-native:scvmm:StorageProfile
description: Defines the resource properties.
properties:
  disks:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:scvmm:VirtualDisk"
    description: Gets or sets the list of virtual disks associated with the virtual machine.
type: object
