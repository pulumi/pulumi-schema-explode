$token: azure-native:chaos/v20220701preview:StepResponse
description: Model that represents a step in the Experiment resource.
properties:
  branches:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:chaos/v20220701preview:BranchResponse"
    description: List of branches.
  name:
    type: string
    description: String of the step name.
type: object
required:
  - branches
  - name
