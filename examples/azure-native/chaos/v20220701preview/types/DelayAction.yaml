$token: azure-native:chaos/v20220701preview:DelayAction
description: Model that represents a delay action.
properties:
  duration:
    type: string
    description: ISO8601 formatted string that represents a duration.
  name:
    type: string
    description: String that represents a Capability URN.
  type:
    type: string
    description: |-
      Enum that discriminates between action models.
      Expected value is 'delay'.
    const: delay
type: object
required:
  - duration
  - name
  - type
