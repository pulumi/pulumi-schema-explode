$token: azure-native:webpubsub/v20211001:UpstreamAuthSettingsResponse
description: Upstream auth settings. If not set, no auth is used for upstream messages.
properties:
  managedIdentity:
    type: object
    $ref: "#/types/azure-native:webpubsub/v20211001:ManagedIdentitySettingsResponse"
    description: Managed identity settings for upstream.
  type:
    type: string
    description: Upstream auth type enum.
type: object
