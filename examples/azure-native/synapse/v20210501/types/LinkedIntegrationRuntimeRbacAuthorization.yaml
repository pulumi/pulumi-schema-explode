$token: azure-native:synapse/v20210501:LinkedIntegrationRuntimeRbacAuthorization
description: The role based access control (RBAC) authorization type integration runtime.
properties:
  authorizationType:
    type: string
    description: |-
      The authorization type for integration runtime sharing.
      Expected value is 'RBAC'.
    const: RBAC
  resourceId:
    type: string
    description: The resource identifier of the integration runtime to be shared.
type: object
required:
  - authorizationType
  - resourceId
