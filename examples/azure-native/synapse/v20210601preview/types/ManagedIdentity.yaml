$token: azure-native:synapse/v20210601preview:ManagedIdentity
description: The workspace managed identity
properties:
  type:
    $ref: "#/types/azure-native:synapse/v20210601preview:ResourceIdentityType"
    description: The type of managed identity for the workspace
  userAssignedIdentities:
    type: object
    additionalProperties:
      $ref: pulumi.json#/Any
    description: The user assigned managed identities.
type: object
