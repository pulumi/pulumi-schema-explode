$token: azure-native:synapse:ManagedIdentity
description: The workspace managed identity
properties:
  type:
    $ref: "#/types/azure-native:synapse:ResourceIdentityType"
    description: The type of managed identity for the workspace
type: object
