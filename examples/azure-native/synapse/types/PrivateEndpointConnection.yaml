$token: azure-native:synapse:PrivateEndpointConnection
description: A private endpoint connection
properties:
  privateLinkServiceConnectionState:
    type: object
    $ref: "#/types/azure-native:synapse:PrivateLinkServiceConnectionState"
    description: Connection state of the private endpoint connection.
type: object
