$token: azure-native:synapse:PrivateEndpointConnectionForPrivateLinkHubBasicResponse
description: Private Endpoint Connection For Private Link Hub - Basic
properties:
  id:
    type: string
    description: identifier
  privateEndpoint:
    type: object
    $ref: "#/types/azure-native:synapse:PrivateEndpointResponse"
    description: The private endpoint which the connection belongs to.
  privateLinkServiceConnectionState:
    type: object
    $ref: "#/types/azure-native:synapse:PrivateLinkServiceConnectionStateResponse"
    description: Connection state of the private endpoint connection.
  provisioningState:
    type: string
    description: Provisioning state of the private endpoint connection.
type: object
required:
  - id
  - provisioningState
