$token: azure-native:synapse/v20201201:getIntegrationRuntimeStatus
description: Integration runtime status response.
inputs:
  properties:
    integrationRuntimeName:
      type: string
      description: Integration runtime name
    resourceGroupName:
      type: string
      description: The name of the resource group. The name is case insensitive.
    workspaceName:
      type: string
      description: The name of the workspace.
  type: object
  required:
    - integrationRuntimeName
    - resourceGroupName
    - workspaceName
outputs:
  description: Integration runtime status response.
  properties:
    name:
      type: string
      description: The integration runtime name.
    properties:
      oneOf:
        - type: object
          $ref: "#/types/azure-native:synapse/v20201201:ManagedIntegrationRuntimeStatusResponse"
        - type: object
          $ref: "#/types/azure-native:synapse/v20201201:SelfHostedIntegrationRuntimeStatusResponse"
      discriminator:
        propertyName: type
        mapping:
          Managed: "#/types/azure-native:synapse/v20201201:ManagedIntegrationRuntimeStatusResponse"
          SelfHosted: "#/types/azure-native:synapse/v20201201:SelfHostedIntegrationRuntimeStatusResponse"
      description: Integration runtime properties.
  type: object
  required:
    - name
    - properties
