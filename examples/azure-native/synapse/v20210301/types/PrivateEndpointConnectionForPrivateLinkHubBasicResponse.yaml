$token: azure-native:synapse/v20210301:PrivateEndpointConnectionForPrivateLinkHubBasicResponse
description: Private Endpoint Connection For Private Link Hub - Basic
properties:
  id:
    type: string
    description: identifier
  privateEndpoint:
    type: object
    $ref: "#/types/azure-native:synapse/v20210301:PrivateEndpointResponse"
    description: The private endpoint which the connection belongs to.
  privateLinkServiceConnectionState:
    type: object
    $ref: "#/types/azure-native:synapse/v20210301:PrivateLinkServiceConnectionStateResponse"
    description: Connection state of the private endpoint connection.
  provisioningState:
    type: string
    description: Provisioning state of the private endpoint connection.
type: object
required:
  - id
  - provisioningState
