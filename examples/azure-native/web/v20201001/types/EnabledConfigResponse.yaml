$token: azure-native:web/v20201001:EnabledConfigResponse
description: Enabled configuration.
properties:
  enabled:
    type: boolean
    description: True if configuration is enabled, false if it is disabled and null if configuration is not set.
type: object
