$token: azure-native:web/v20201001:listWebAppFunctionSecrets
description: Function secrets.
inputs:
  properties:
    functionName:
      type: string
      description: Function name.
    name:
      type: string
      description: Site name.
    resourceGroupName:
      type: string
      description: Name of the resource group to which the resource belongs.
  type: object
  required:
    - functionName
    - name
    - resourceGroupName
outputs:
  description: Function secrets.
  properties:
    key:
      type: string
      description: Secret key.
    triggerUrl:
      type: string
      description: Trigger URL.
  type: object
