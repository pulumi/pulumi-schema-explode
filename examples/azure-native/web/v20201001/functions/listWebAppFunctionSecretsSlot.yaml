$token: azure-native:web/v20201001:listWebAppFunctionSecretsSlot
description: Function secrets.
inputs:
  properties:
    functionName:
      type: string
      description: Function name.
    name:
      type: string
      description: Site name.
    resourceGroupName:
      type: string
      description: Name of the resource group to which the resource belongs.
    slot:
      type: string
      description: Name of the deployment slot.
  type: object
  required:
    - functionName
    - name
    - resourceGroupName
    - slot
outputs:
  description: Function secrets.
  properties:
    key:
      type: string
      description: Secret key.
    triggerUrl:
      type: string
      description: Trigger URL.
  type: object
