$token: azure-native:web/v20201001:getStaticSite
description: Static Site ARM resource.
inputs:
  properties:
    name:
      type: string
      description: Name of the static site.
    resourceGroupName:
      type: string
      description: Name of the resource group to which the resource belongs.
  type: object
  required:
    - name
    - resourceGroupName
outputs:
  description: Static Site ARM resource.
  properties:
    branch:
      type: string
      description: The target branch in the repository.
    buildProperties:
      type: object
      $ref: "#/types/azure-native:web/v20201001:StaticSiteBuildPropertiesResponse"
      description: Build properties to configure on the repository.
    customDomains:
      type: array
      items:
        type: string
      description: The custom domains associated with this static site.
    defaultHostname:
      type: string
      description: The default autogenerated hostname for the static site.
    id:
      type: string
      description: Resource Id.
    kind:
      type: string
      description: Kind of resource.
    location:
      type: string
      description: Resource Location.
    name:
      type: string
      description: Resource Name.
    repositoryToken:
      type: string
      description: A user's github repository token. This is used to setup the Github Actions workflow file and API secrets.
    repositoryUrl:
      type: string
      description: URL for the repository of the static site.
    sku:
      type: object
      $ref: "#/types/azure-native:web/v20201001:SkuDescriptionResponse"
      description: Description of a SKU for a scalable resource.
    systemData:
      type: object
      $ref: "#/types/azure-native:web/v20201001:SystemDataResponse"
      description: The system metadata relating to this resource.
    tags:
      type: object
      additionalProperties:
        type: string
      description: Resource tags.
    type:
      type: string
      description: Resource type.
  type: object
  required:
    - customDomains
    - defaultHostname
    - id
    - location
    - name
    - systemData
    - type
