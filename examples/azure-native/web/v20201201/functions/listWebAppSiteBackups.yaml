$token: azure-native:web/v20201201:listWebAppSiteBackups
description: Collection of backup items.
inputs:
  properties:
    name:
      type: string
      description: Name of the app.
    resourceGroupName:
      type: string
      description: Name of the resource group to which the resource belongs.
  type: object
  required:
    - name
    - resourceGroupName
outputs:
  description: Collection of backup items.
  properties:
    nextLink:
      type: string
      description: Link to next page of resources.
    value:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:web/v20201201:BackupItemResponse"
      description: Collection of resources.
  type: object
  required:
    - nextLink
    - value
