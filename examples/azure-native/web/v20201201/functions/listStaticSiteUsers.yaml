$token: azure-native:web/v20201201:listStaticSiteUsers
description: Collection of static site custom users.
inputs:
  properties:
    authprovider:
      type: string
      description: The auth provider for the users.
    name:
      type: string
      description: Name of the static site.
    resourceGroupName:
      type: string
      description: Name of the resource group to which the resource belongs.
  type: object
  required:
    - authprovider
    - name
    - resourceGroupName
outputs:
  description: Collection of static site custom users.
  properties:
    nextLink:
      type: string
      description: Link to next page of resources.
    value:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:web/v20201201:StaticSiteUserARMResourceResponse"
      description: Collection of resources.
  type: object
  required:
    - nextLink
    - value
