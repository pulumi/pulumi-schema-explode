$token: azure-native:web/v20160801:getWebAppVnetConnectionSlot
description: Virtual Network information contract.
inputs:
  properties:
    name:
      type: string
      description: Name of the app.
    resourceGroupName:
      type: string
      description: Name of the resource group to which the resource belongs.
    slot:
      type: string
      description: Name of the deployment slot. If a slot is not specified, the API will get the named virtual network for the production slot.
    vnetName:
      type: string
      description: Name of the virtual network.
  type: object
  required:
    - name
    - resourceGroupName
    - slot
    - vnetName
outputs:
  description: Virtual Network information contract.
  properties:
    certBlob:
      type: string
      description: |-
        A certificate file (.cer) blob containing the public key of the private key used to authenticate a 
        Point-To-Site VPN connection.
    certThumbprint:
      type: string
      description: The client certificate thumbprint.
    dnsServers:
      type: string
      description: DNS servers to be used by this Virtual Network. This should be a comma-separated list of IP addresses.
    id:
      type: string
      description: Resource Id.
    kind:
      type: string
      description: Kind of resource.
    name:
      type: string
      description: Resource Name.
    resyncRequired:
      type: boolean
      description: <code>true</code> if a resync is required; otherwise, <code>false</code>.
    routes:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:web/v20160801:VnetRouteResponse"
      description: The routes that this Virtual Network connection uses.
    type:
      type: string
      description: Resource type.
    vnetResourceId:
      type: string
      description: The Virtual Network's resource ID.
  type: object
  required:
    - certThumbprint
    - id
    - name
    - resyncRequired
    - routes
    - type
