$token: azure-native:web/v20150801:CorsSettingsResponse
description: Cross-Origin Resource Sharing (CORS) settings for the web app.
properties:
  allowedOrigins:
    type: array
    items:
      type: string
    description: "Gets or sets the list of origins that should be allowed to make cross-origin\r

      \            calls (for example: http://example.com:12345). Use \"*\" to allow all."
type: object
