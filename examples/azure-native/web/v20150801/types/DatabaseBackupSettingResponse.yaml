$token: azure-native:web/v20150801:DatabaseBackupSettingResponse
description: "Note: properties are serialized in JSON format and stored in DB. \r

  \            if new properties are added they might not be in the previous data rows \r

  \            so please handle nulls"
properties:
  connectionString:
    type: string
    description: Contains a connection string to a database which is being backed up/restored. If the restore should happen to a new database, the database name inside is the new one.
  connectionStringName:
    type: string
    description: "Contains a connection string name that is linked to the SiteConfig.ConnectionStrings.\r

      \            This is used during restore with overwrite connection strings options."
  databaseType:
    type: string
    description: SqlAzure / MySql
  name:
    type: string
type: object
