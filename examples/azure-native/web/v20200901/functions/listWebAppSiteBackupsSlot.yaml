$token: azure-native:web/v20200901:listWebAppSiteBackupsSlot
description: Collection of backup items.
inputs:
  properties:
    name:
      type: string
      description: Name of the app.
    resourceGroupName:
      type: string
      description: Name of the resource group to which the resource belongs.
    slot:
      type: string
      description: Name of the deployment slot. If a slot is not specified, the API will get backups of the production slot.
  type: object
  required:
    - name
    - resourceGroupName
    - slot
outputs:
  description: Collection of backup items.
  properties:
    nextLink:
      type: string
      description: Link to next page of resources.
    value:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:web/v20200901:BackupItemResponse"
      description: Collection of resources.
  type: object
  required:
    - nextLink
    - value
