$token: azure-native:web/v20220301:ContainerResources
description: Container App container resource requirements.
properties:
  cpu:
    type: number
    description: Required CPU in cores, e.g. 0.5
  memory:
    type: string
    description: Required memory, e.g. "250Mb"
type: object
