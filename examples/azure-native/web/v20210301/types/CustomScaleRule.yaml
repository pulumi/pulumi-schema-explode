$token: azure-native:web/v20210301:CustomScaleRule
description: Container App container Custom scaling rule.
properties:
  auth:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:web/v20210301:ScaleRuleAuth"
    description: Authentication secrets for the custom scale rule.
  metadata:
    type: object
    additionalProperties:
      type: string
    description: Metadata properties to describe custom scale rule.
  type:
    type: string
    description: |-
      Type of the custom scale rule
      eg: azure-servicebus, redis etc.
type: object
