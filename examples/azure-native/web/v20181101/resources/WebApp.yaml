$token: azure-native:web/v20181101:WebApp
description: A web app, a mobile app backend, or an API app.
properties:
  availabilityState:
    type: string
    description: Management information availability state for the app.
  clientAffinityEnabled:
    type: boolean
    description: <code>true</code> to enable client affinity; <code>false</code> to stop sending session affinity cookies, which route client requests in the same session to the same instance. Default is <code>true</code>.
  clientCertEnabled:
    type: boolean
    description: <code>true</code> to enable client certificate authentication (TLS mutual authentication); otherwise, <code>false</code>. Default is <code>false</code>.
  clientCertExclusionPaths:
    type: string
    description: client certificate authentication comma-separated exclusion paths
  containerSize:
    type: integer
    description: Size of the function container.
  dailyMemoryTimeQuota:
    type: integer
    description: Maximum allowed daily memory-time quota (applicable on dynamic apps only).
  defaultHostName:
    type: string
    description: Default hostname of the app. Read-only.
  enabled:
    type: boolean
    description: <code>true</code> if the app is enabled; otherwise, <code>false</code>. Setting this value to false disables the app (takes the app offline).
  enabledHostNames:
    type: array
    items:
      type: string
    description: |-
      Enabled hostnames for the app.Hostnames need to be assigned (see HostNames) AND enabled. Otherwise,
      the app is not served on those hostnames.
  geoDistributions:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:web/v20181101:GeoDistributionResponse"
    description: GeoDistributions for this site
  hostNameSslStates:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:web/v20181101:HostNameSslStateResponse"
    description: Hostname SSL states are used to manage the SSL bindings for app's hostnames.
  hostNames:
    type: array
    items:
      type: string
    description: Hostnames associated with the app.
  hostNamesDisabled:
    type: boolean
    description: |-
      <code>true</code> to disable the public hostnames of the app; otherwise, <code>false</code>.
       If <code>true</code>, the app is only accessible via API management process.
  hostingEnvironmentProfile:
    type: object
    $ref: "#/types/azure-native:web/v20181101:HostingEnvironmentProfileResponse"
    description: App Service Environment to use for the app.
  httpsOnly:
    type: boolean
    description: |-
      HttpsOnly: configures a web site to accept only https requests. Issues redirect for
      http requests
  hyperV:
    type: boolean
    description: Hyper-V sandbox.
    default: false
  identity:
    type: object
    $ref: "#/types/azure-native:web/v20181101:ManagedServiceIdentityResponse"
    description: Managed service identity.
  inProgressOperationId:
    type: string
    description: Specifies an operation id if this site has a pending operation.
  isDefaultContainer:
    type: boolean
    description: <code>true</code> if the app is a default container; otherwise, <code>false</code>.
  isXenon:
    type: boolean
    description: "Obsolete: Hyper-V sandbox."
    default: false
  kind:
    type: string
    description: Kind of resource.
  lastModifiedTimeUtc:
    type: string
    description: Last time the app was modified, in UTC. Read-only.
  location:
    type: string
    description: Resource Location.
  maxNumberOfWorkers:
    type: integer
    description: |-
      Maximum number of workers.
      This only applies to Functions container.
  name:
    type: string
    description: Resource Name.
  outboundIpAddresses:
    type: string
    description: List of IP addresses that the app uses for outbound connections (e.g. database access). Includes VIPs from tenants that site can be hosted with current settings. Read-only.
  possibleOutboundIpAddresses:
    type: string
    description: List of IP addresses that the app uses for outbound connections (e.g. database access). Includes VIPs from all tenants. Read-only.
  redundancyMode:
    type: string
    description: Site redundancy mode
  repositorySiteName:
    type: string
    description: Name of the repository site.
  reserved:
    type: boolean
    description: <code>true</code> if reserved; otherwise, <code>false</code>.
    default: false
  resourceGroup:
    type: string
    description: Name of the resource group the app belongs to. Read-only.
  scmSiteAlsoStopped:
    type: boolean
    description: <code>true</code> to stop SCM (KUDU) site when the app is stopped; otherwise, <code>false</code>. The default is <code>false</code>.
    default: false
  serverFarmId:
    type: string
    description: 'Resource ID of the associated App Service plan, formatted as: "/subscriptions/{subscriptionID}/resourceGroups/{groupName}/providers/Microsoft.Web/serverfarms/{appServicePlanName}".'
  siteConfig:
    type: object
    $ref: "#/types/azure-native:web/v20181101:SiteConfigResponse"
    description: Configuration of the app.
  slotSwapStatus:
    type: object
    $ref: "#/types/azure-native:web/v20181101:SlotSwapStatusResponse"
    description: Status of the last deployment slot swap operation.
  state:
    type: string
    description: Current state of the app.
  suspendedTill:
    type: string
    description: App suspended till in case memory-time quota is exceeded.
  tags:
    type: object
    additionalProperties:
      type: string
    description: Resource tags.
  targetSwapSlot:
    type: string
    description: Specifies which deployment slot this app will swap into. Read-only.
  trafficManagerHostNames:
    type: array
    items:
      type: string
    description: Azure Traffic Manager hostnames associated with the app. Read-only.
  type:
    type: string
    description: Resource type.
  usageState:
    type: string
    description: State indicating whether the app has exceeded its quota usage. Read-only.
type: object
required:
  - availabilityState
  - defaultHostName
  - enabledHostNames
  - hostNames
  - inProgressOperationId
  - isDefaultContainer
  - lastModifiedTimeUtc
  - location
  - maxNumberOfWorkers
  - name
  - outboundIpAddresses
  - possibleOutboundIpAddresses
  - repositorySiteName
  - resourceGroup
  - slotSwapStatus
  - state
  - suspendedTill
  - targetSwapSlot
  - trafficManagerHostNames
  - type
  - usageState
inputProperties:
  clientAffinityEnabled:
    type: boolean
    description: <code>true</code> to enable client affinity; <code>false</code> to stop sending session affinity cookies, which route client requests in the same session to the same instance. Default is <code>true</code>.
  clientCertEnabled:
    type: boolean
    description: <code>true</code> to enable client certificate authentication (TLS mutual authentication); otherwise, <code>false</code>. Default is <code>false</code>.
  clientCertExclusionPaths:
    type: string
    description: client certificate authentication comma-separated exclusion paths
  cloningInfo:
    type: object
    $ref: "#/types/azure-native:web/v20181101:CloningInfo"
    description: If specified during app creation, the app is cloned from a source app.
  containerSize:
    type: integer
    description: Size of the function container.
  dailyMemoryTimeQuota:
    type: integer
    description: Maximum allowed daily memory-time quota (applicable on dynamic apps only).
  enabled:
    type: boolean
    description: <code>true</code> if the app is enabled; otherwise, <code>false</code>. Setting this value to false disables the app (takes the app offline).
  geoDistributions:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:web/v20181101:GeoDistribution"
    description: GeoDistributions for this site
  hostNameSslStates:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:web/v20181101:HostNameSslState"
    description: Hostname SSL states are used to manage the SSL bindings for app's hostnames.
  hostNamesDisabled:
    type: boolean
    description: |-
      <code>true</code> to disable the public hostnames of the app; otherwise, <code>false</code>.
       If <code>true</code>, the app is only accessible via API management process.
  hostingEnvironmentProfile:
    type: object
    $ref: "#/types/azure-native:web/v20181101:HostingEnvironmentProfile"
    description: App Service Environment to use for the app.
  httpsOnly:
    type: boolean
    description: |-
      HttpsOnly: configures a web site to accept only https requests. Issues redirect for
      http requests
  hyperV:
    type: boolean
    description: Hyper-V sandbox.
    default: false
  identity:
    type: object
    $ref: "#/types/azure-native:web/v20181101:ManagedServiceIdentity"
    description: Managed service identity.
  isXenon:
    type: boolean
    description: "Obsolete: Hyper-V sandbox."
    default: false
  kind:
    type: string
    description: Kind of resource.
  location:
    type: string
    description: Resource Location.
  name:
    type: string
    description: Unique name of the app to create or update. To create or update a deployment slot, use the {slot} parameter.
  redundancyMode:
    $ref: "#/types/azure-native:web/v20181101:RedundancyMode"
    description: Site redundancy mode
  reserved:
    type: boolean
    description: <code>true</code> if reserved; otherwise, <code>false</code>.
    default: false
  resourceGroupName:
    type: string
    description: Name of the resource group to which the resource belongs.
  scmSiteAlsoStopped:
    type: boolean
    description: <code>true</code> to stop SCM (KUDU) site when the app is stopped; otherwise, <code>false</code>. The default is <code>false</code>.
    default: false
  serverFarmId:
    type: string
    description: 'Resource ID of the associated App Service plan, formatted as: "/subscriptions/{subscriptionID}/resourceGroups/{groupName}/providers/Microsoft.Web/serverfarms/{appServicePlanName}".'
  siteConfig:
    type: object
    $ref: "#/types/azure-native:web/v20181101:SiteConfig"
    description: Configuration of the app.
  tags:
    type: object
    additionalProperties:
      type: string
    description: Resource tags.
requiredInputs:
  - resourceGroupName
aliases:
  - type: azure-native:web:WebApp
  - type: azure-native:web/v20150801:WebApp
  - type: azure-native:web/v20160801:WebApp
  - type: azure-native:web/v20180201:WebApp
  - type: azure-native:web/v20190801:WebApp
  - type: azure-native:web/v20200601:WebApp
  - type: azure-native:web/v20200901:WebApp
  - type: azure-native:web/v20201001:WebApp
  - type: azure-native:web/v20201201:WebApp
  - type: azure-native:web/v20210101:WebApp
  - type: azure-native:web/v20210115:WebApp
  - type: azure-native:web/v20210201:WebApp
  - type: azure-native:web/v20210301:WebApp
  - type: azure-native:web/v20220301:WebApp
