$token: azure-native:web/v20210201:AzureBlobStorageApplicationLogsConfig
description: Application logs azure blob storage configuration.
properties:
  level:
    $ref: "#/types/azure-native:web/v20210201:LogLevel"
    description: Log level.
  retentionInDays:
    type: integer
    description: |-
      Retention in days.
      Remove blobs older than X days.
      0 or lower means no retention.
  sasUrl:
    type: string
    description: SAS url to a azure blob container with read/write/list/delete permissions.
type: object
