$token: azure-native:web/v20150801preview:HostingEnvironmentServiceDescriptions
description: Back end service per ASE
properties:
  hostId:
    type: string
    description: Host Id
  hostingEnvironmentId:
    type: string
    description: Hosting environment Id
  serviceUrl:
    type: string
    description: service url to use
  useInternalRouting:
    type: boolean
    description: "When the backend url is in same ASE, for performance reason this flag can be set to true\r

      \            If WebApp.DisableHostNames is also set it improves the security by making the back end accessible only \r

      \            via API calls\r

      \            Note: calls will fail if this option is used but back end is not on the same ASE"
type: object
