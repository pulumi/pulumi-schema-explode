$token: azure-native:web:AutoHealActions
description: Actions which to take by the auto-heal module when a rule is triggered.
properties:
  actionType:
    $ref: "#/types/azure-native:web:AutoHealActionType"
    description: Predefined action to be taken.
  customAction:
    type: object
    $ref: "#/types/azure-native:web:AutoHealCustomAction"
    description: Custom action to be taken.
  minProcessExecutionTime:
    type: string
    description: |-
      Minimum time the process must execute
      before taking the action
type: object
