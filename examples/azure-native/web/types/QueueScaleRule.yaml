$token: azure-native:web:QueueScaleRule
description: Container App container Azure Queue based scaling rule.
properties:
  auth:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:web:ScaleRuleAuth"
    description: Authentication secrets for the queue scale rule.
  queueLength:
    type: integer
    description: Queue length.
  queueName:
    type: string
    description: Queue name.
type: object
