$token: azure-native:web:Experiments
description: Routing rules in production experiments.
properties:
  rampUpRules:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:web:RampUpRule"
    description: List of ramp-up rules.
type: object
