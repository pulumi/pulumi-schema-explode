$token: azure-native:web:ScaleRule
description: Container App container scaling rule.
properties:
  azureQueue:
    type: object
    $ref: "#/types/azure-native:web:QueueScaleRule"
    description: Azure Queue based scaling.
  custom:
    type: object
    $ref: "#/types/azure-native:web:CustomScaleRule"
    description: Custom scale rule.
  http:
    type: object
    $ref: "#/types/azure-native:web:HttpScaleRule"
    description: HTTP requests based scaling.
  name:
    type: string
    description: Scale Rule Name
type: object
