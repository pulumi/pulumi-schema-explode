$token: azure-native:web:IdentityProvidersResponse
description: The configuration settings of each of the identity providers used to configure App Service Authentication/Authorization.
properties:
  apple:
    type: object
    $ref: "#/types/azure-native:web:AppleResponse"
    description: The configuration settings of the Apple provider.
  azureActiveDirectory:
    type: object
    $ref: "#/types/azure-native:web:AzureActiveDirectoryResponse"
    description: The configuration settings of the Azure Active directory provider.
  azureStaticWebApps:
    type: object
    $ref: "#/types/azure-native:web:AzureStaticWebAppsResponse"
    description: The configuration settings of the Azure Static Web Apps provider.
  customOpenIdConnectProviders:
    type: object
    additionalProperties:
      type: object
      $ref: "#/types/azure-native:web:CustomOpenIdConnectProviderResponse"
    description: |-
      The map of the name of the alias of each custom Open ID Connect provider to the
      configuration settings of the custom Open ID Connect provider.
  facebook:
    type: object
    $ref: "#/types/azure-native:web:FacebookResponse"
    description: The configuration settings of the Facebook provider.
  gitHub:
    type: object
    $ref: "#/types/azure-native:web:GitHubResponse"
    description: The configuration settings of the GitHub provider.
  google:
    type: object
    $ref: "#/types/azure-native:web:GoogleResponse"
    description: The configuration settings of the Google provider.
  legacyMicrosoftAccount:
    type: object
    $ref: "#/types/azure-native:web:LegacyMicrosoftAccountResponse"
    description: The configuration settings of the legacy Microsoft Account provider.
  twitter:
    type: object
    $ref: "#/types/azure-native:web:TwitterResponse"
    description: The configuration settings of the Twitter provider.
type: object
