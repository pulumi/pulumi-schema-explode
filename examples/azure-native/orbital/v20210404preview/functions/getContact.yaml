$token: azure-native:orbital/v20210404preview:getContact
description: Customer creates a contact resource for a spacecraft resource.
inputs:
  properties:
    contactName:
      type: string
      description: Contact Name
    resourceGroupName:
      type: string
      description: The name of the resource group. The name is case insensitive.
    spacecraftName:
      type: string
      description: Spacecraft ID
  type: object
  required:
    - contactName
    - resourceGroupName
    - spacecraftName
outputs:
  description: Customer creates a contact resource for a spacecraft resource.
  properties:
    contactProfile:
      type: object
      $ref: "#/types/azure-native:orbital/v20210404preview:ResourceReferenceResponse"
      description: The reference to the contact profile resource.
    endAzimuthDegrees:
      type: number
      description: Azimuth of the antenna at the end of the contact in decimal degrees.
    endElevationDegrees:
      type: number
      description: Spacecraft elevation above the horizon at contact end.
    errorMessage:
      type: string
      description: Any error message while scheduling a contact.
    etag:
      type: string
      description: A unique read-only string that changes whenever the resource is updated.
    groundStationName:
      type: string
      description: Azure Ground Station name.
    id:
      type: string
      description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
    maximumElevationDegrees:
      type: number
      description: Maximum elevation of the antenna during the contact in decimal degrees.
    name:
      type: string
      description: The name of the resource
    reservationEndTime:
      type: string
      description: Reservation end time of a contact.
    reservationStartTime:
      type: string
      description: Reservation start time of a contact.
    rxEndTime:
      type: string
      description: Receive end time of a contact.
    rxStartTime:
      type: string
      description: Receive start time of a contact.
    startAzimuthDegrees:
      type: number
      description: Azimuth of the antenna at the start of the contact in decimal degrees.
    startElevationDegrees:
      type: number
      description: Spacecraft elevation above the horizon at contact start.
    status:
      type: string
      description: Status of a contact.
    systemData:
      type: object
      $ref: "#/types/azure-native:orbital/v20210404preview:SystemDataResponse"
      description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
    txEndTime:
      type: string
      description: Transmit end time of a contact.
    txStartTime:
      type: string
      description: Transmit start time of a contact.
    type:
      type: string
      description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
  type: object
  required:
    - contactProfile
    - endAzimuthDegrees
    - endElevationDegrees
    - errorMessage
    - etag
    - groundStationName
    - id
    - maximumElevationDegrees
    - name
    - reservationEndTime
    - reservationStartTime
    - rxEndTime
    - rxStartTime
    - startAzimuthDegrees
    - startElevationDegrees
    - status
    - systemData
    - txEndTime
    - txStartTime
    - type
