$token: azure-native:orbital:ContactProfileLink
description: Contact Profile link
properties:
  channels:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:orbital:ContactProfileLinkChannel"
    description: Contact Profile Link Channel
  direction:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:orbital:Direction"
    description: Direction (uplink or downlink)
  eirpdBW:
    type: number
    description: Effective Isotropic Radiated Power (EIRP) in dBW.
  gainOverTemperature:
    type: number
    description: Gain To Noise Temperature in db/K.
  polarization:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:orbital:Polarization"
    description: polarization. eg (RHCP, LHCP)
type: object
required:
  - channels
  - direction
  - polarization
