$token: azure-native:orbital:getContactProfile
description: |-
  Customer creates a Contact Profile Resource, which will contain all of the configurations required for scheduling a contact.
  API Version: 2021-04-04-preview.
inputs:
  properties:
    contactProfileName:
      type: string
      description: Contact Profile Name
    resourceGroupName:
      type: string
      description: The name of the resource group. The name is case insensitive.
  type: object
  required:
    - contactProfileName
    - resourceGroupName
outputs:
  description: Customer creates a Contact Profile Resource, which will contain all of the configurations required for scheduling a contact.
  properties:
    autoTrackingConfiguration:
      type: string
      description: Auto track configuration.
    etag:
      type: string
      description: A unique read-only string that changes whenever the resource is updated.
    eventHubUri:
      type: string
      description: The URI of the Event Hub used for telemetry
    id:
      type: string
      description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
    links:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:orbital:ContactProfileLinkResponse"
      description: Links of the Contact Profile
    location:
      type: string
      description: The geo-location where the resource lives
    minimumElevationDegrees:
      type: number
      description: Minimum viable elevation for the contact in decimal degrees.
    minimumViableContactDuration:
      type: string
      description: Minimum viable contact duration in ISO 8601 format.
    name:
      type: string
      description: The name of the resource
    systemData:
      type: object
      $ref: "#/types/azure-native:orbital:SystemDataResponse"
      description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
    tags:
      type: object
      additionalProperties:
        type: string
      description: Resource tags.
    type:
      type: string
      description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
  type: object
  required:
    - etag
    - id
    - links
    - location
    - name
    - systemData
    - type
