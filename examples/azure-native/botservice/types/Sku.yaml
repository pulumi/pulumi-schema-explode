$token: azure-native:botservice:Sku
description: The SKU of the cognitive services account.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:botservice:SkuName"
    description: The sku name
type: object
required:
  - name
