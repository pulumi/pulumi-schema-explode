$token: azure-native:botservice:EnterpriseChannelPropertiesResponse
description: The parameters to provide for the Enterprise Channel.
properties:
  nodes:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:botservice:EnterpriseChannelNodeResponse"
    description: The nodes associated with the Enterprise Channel.
  state:
    type: string
    description: The current state of the Enterprise Channel.
type: object
required:
  - nodes
