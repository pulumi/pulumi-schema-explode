$token: azure-native:botservice:EnterpriseChannelProperties
description: The parameters to provide for the Enterprise Channel.
properties:
  nodes:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:botservice:EnterpriseChannelNode"
    description: The nodes associated with the Enterprise Channel.
  state:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:botservice:EnterpriseChannelState"
    description: The current state of the Enterprise Channel.
type: object
required:
  - nodes
