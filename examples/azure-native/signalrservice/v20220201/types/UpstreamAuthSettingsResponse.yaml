$token: azure-native:signalrservice/v20220201:UpstreamAuthSettingsResponse
description: Upstream auth settings. If not set, no auth is used for upstream messages.
properties:
  managedIdentity:
    type: object
    $ref: "#/types/azure-native:signalrservice/v20220201:ManagedIdentitySettingsResponse"
    description: Managed identity settings for upstream.
  type:
    type: string
    description: Upstream auth type enum.
type: object
