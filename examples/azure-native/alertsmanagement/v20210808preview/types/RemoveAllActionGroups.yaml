$token: azure-native:alertsmanagement/v20210808preview:RemoveAllActionGroups
description: Indicates if all action groups should be removed.
properties:
  actionType:
    type: string
    description: |-
      Action that should be applied.
      Expected value is 'RemoveAllActionGroups'.
    const: RemoveAllActionGroups
type: object
required:
  - actionType
