$token: azure-native:alertsmanagement/v20210808:AddActionGroups
description: Add action groups to alert processing rule.
properties:
  actionGroupIds:
    type: array
    items:
      type: string
    description: List of action group Ids to add to alert processing rule.
  actionType:
    type: string
    description: |-
      Action that should be applied.
      Expected value is 'AddActionGroups'.
    const: AddActionGroups
type: object
required:
  - actionGroupIds
  - actionType
