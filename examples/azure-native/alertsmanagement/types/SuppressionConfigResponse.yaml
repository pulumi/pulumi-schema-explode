$token: azure-native:alertsmanagement:SuppressionConfigResponse
description: Suppression logic for a given action rule
properties:
  recurrenceType:
    type: string
    description: Specifies when the suppression should be applied
  schedule:
    type: object
    $ref: "#/types/azure-native:alertsmanagement:SuppressionScheduleResponse"
    description: suppression schedule configuration
type: object
required:
  - recurrenceType
