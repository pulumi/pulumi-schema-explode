$token: azure-native:alertsmanagement:SuppressionConfig
description: Suppression logic for a given action rule
properties:
  recurrenceType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:alertsmanagement:SuppressionType"
    description: Specifies when the suppression should be applied
  schedule:
    type: object
    $ref: "#/types/azure-native:alertsmanagement:SuppressionSchedule"
    description: suppression schedule configuration
type: object
required:
  - recurrenceType
