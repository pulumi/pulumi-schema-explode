$token: azure-native:alertsmanagement:Suppression
description: Action rule with suppression configuration
properties:
  conditions:
    type: object
    $ref: "#/types/azure-native:alertsmanagement:Conditions"
    description: conditions on which alerts will be filtered
  description:
    type: string
    description: Description of action rule
  scope:
    type: object
    $ref: "#/types/azure-native:alertsmanagement:Scope"
    description: scope on which action rule will apply
  status:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:alertsmanagement:ActionRuleStatus"
    description: Indicates if the given action rule is enabled or disabled
  suppressionConfig:
    type: object
    $ref: "#/types/azure-native:alertsmanagement:SuppressionConfig"
    description: suppression configuration for the action rule
  type:
    type: string
    description: |-
      Indicates type of action rule
      Expected value is 'Suppression'.
    const: Suppression
type: object
required:
  - suppressionConfig
  - type
