$token: azure-native:marketplace/v20211201:listPrivateStoreSubscriptionsContext
description: List of subscription Ids in the private store
inputs:
  properties:
    privateStoreId:
      type: string
      description: The store ID - must use the tenant ID
  type: object
  required:
    - privateStoreId
outputs:
  description: List of subscription Ids in the private store
  properties:
    subscriptionsIds:
      type: array
      items:
        type: string
  type: object
