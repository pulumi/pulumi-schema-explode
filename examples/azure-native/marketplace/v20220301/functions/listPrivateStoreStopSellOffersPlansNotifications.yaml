$token: azure-native:marketplace/v20220301:listPrivateStoreStopSellOffersPlansNotifications
description: List of stop sell offers and plans notifications.
inputs:
  properties:
    privateStoreId:
      type: string
      description: The store ID - must use the tenant ID
    subscriptions:
      type: array
      items:
        type: string
  type: object
  required:
    - privateStoreId
outputs:
  description: List of stop sell offers and plans notifications.
  properties:
    stopSellNotifications:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:marketplace/v20220301:StopSellOffersPlansNotificationsListPropertiesResponse"
  type: object
