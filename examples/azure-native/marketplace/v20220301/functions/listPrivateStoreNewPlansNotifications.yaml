$token: azure-native:marketplace/v20220301:listPrivateStoreNewPlansNotifications
description: List of all new plans notifications for public offers
inputs:
  properties:
    privateStoreId:
      type: string
      description: The store ID - must use the tenant ID
  type: object
  required:
    - privateStoreId
outputs:
  description: List of all new plans notifications for public offers
  properties:
    newPlansNotifications:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:marketplace/v20220301:NewNotificationsResponse"
  type: object
