$token: azure-native:guestconfiguration/v20180630preview:getGuestConfigurationAssignment
description: Guest configuration assignment is an association between a VM and guest configuration.
inputs:
  properties:
    guestConfigurationAssignmentName:
      type: string
      description: The guest configuration assignment name.
    resourceGroupName:
      type: string
      description: The resource group name.
    vmName:
      type: string
      description: The name of the virtual machine.
  type: object
  required:
    - guestConfigurationAssignmentName
    - resourceGroupName
    - vmName
outputs:
  description: Guest configuration assignment is an association between a VM and guest configuration.
  properties:
    id:
      type: string
      description: ARM resource id of the guest configuration assignment.
    location:
      type: string
      description: Region where the VM is located.
    name:
      type: string
      description: Name of the guest configuration assignment.
    properties:
      type: object
      $ref: "#/types/azure-native:guestconfiguration/v20180630preview:GuestConfigurationAssignmentPropertiesResponse"
      description: Properties of the Guest configuration assignment.
    type:
      type: string
      description: The type of the resource.
  type: object
  required:
    - id
    - properties
    - type
deprecationMessage: Version 2018-06-30-preview will be removed in v2 of the provider.
