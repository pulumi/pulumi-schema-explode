$token: azure-native:providerhub/v20201120:NotificationRegistrationProperties
properties:
  includedEvents:
    type: array
    items:
      type: string
  messageScope:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:providerhub/v20201120:MessageScope"
  notificationEndpoints:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:providerhub/v20201120:NotificationEndpoint"
  notificationMode:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:providerhub/v20201120:NotificationMode"
  provisioningState:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:providerhub/v20201120:ProvisioningState"
type: object
