$token: azure-native:providerhub:ResourceProviderCapabilities
properties:
  effect:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:providerhub:ResourceProviderCapabilitiesEffect"
  quotaId:
    type: string
  requiredFeatures:
    type: array
    items:
      type: string
type: object
required:
  - effect
  - quotaId
