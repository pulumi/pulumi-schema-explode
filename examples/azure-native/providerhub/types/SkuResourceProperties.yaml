$token: azure-native:providerhub:SkuResourceProperties
properties:
  provisioningState:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:providerhub:ProvisioningState"
  skuSettings:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:providerhub:SkuSetting"
type: object
required:
  - skuSettings
