$token: azure-native:providerhub:SubscriptionStateRule
properties:
  allowedActions:
    type: array
    items:
      type: string
  state:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:providerhub:SubscriptionState"
type: object
