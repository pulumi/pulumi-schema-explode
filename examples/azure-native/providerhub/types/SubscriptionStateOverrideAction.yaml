$token: azure-native:providerhub:SubscriptionStateOverrideAction
properties:
  action:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:providerhub:SubscriptionNotificationOperation"
  state:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:providerhub:SubscriptionTransitioningState"
type: object
required:
  - action
  - state
