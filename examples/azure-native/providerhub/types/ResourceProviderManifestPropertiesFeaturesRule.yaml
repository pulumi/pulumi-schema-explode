$token: azure-native:providerhub:ResourceProviderManifestPropertiesFeaturesRule
properties:
  requiredFeaturesPolicy:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:providerhub:FeaturesPolicy"
type: object
required:
  - requiredFeaturesPolicy
