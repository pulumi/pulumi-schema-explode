$token: azure-native:providerhub:SkuSettingCapacity
properties:
  default:
    type: integer
  maximum:
    type: integer
  minimum:
    type: integer
  scaleType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:providerhub:SkuScaleType"
type: object
required:
  - minimum
