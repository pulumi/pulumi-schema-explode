$token: azure-native:providerhub:ResourceTypeEndpointFeaturesRule
properties:
  requiredFeaturesPolicy:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:providerhub:FeaturesPolicy"
type: object
required:
  - requiredFeaturesPolicy
