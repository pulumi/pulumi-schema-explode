$token: azure-native:providerhub:ThrottlingMetric
properties:
  interval:
    type: string
  limit:
    type: number
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:providerhub:ThrottlingMetricType"
type: object
required:
  - limit
  - type
