$token: azure-native:providerhub:NotificationRegistrationProperties
properties:
  includedEvents:
    type: array
    items:
      type: string
  messageScope:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:providerhub:MessageScope"
  notificationEndpoints:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:providerhub:NotificationEndpoint"
  notificationMode:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:providerhub:NotificationMode"
  provisioningState:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:providerhub:ProvisioningState"
type: object
