$token: azure-native:providerhub/v20210601preview:SkuResourceProperties
properties:
  provisioningState:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:providerhub/v20210601preview:ProvisioningState"
    description: The provisioned state of the resource.
  skuSettings:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:providerhub/v20210601preview:SkuSetting"
type: object
required:
  - skuSettings
