$token: azure-native:providerhub/v20210601preview:NotificationRegistrationProperties
properties:
  includedEvents:
    type: array
    items:
      type: string
  messageScope:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:providerhub/v20210601preview:MessageScope"
  notificationEndpoints:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:providerhub/v20210601preview:NotificationEndpoint"
  notificationMode:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:providerhub/v20210601preview:NotificationMode"
  provisioningState:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:providerhub/v20210601preview:ProvisioningState"
    description: The provisioned state of the resource.
type: object
