$token: azure-native:cdn:UrlSigningParamIdentifier
description: Defines how to identify a parameter for a specific purpose e.g. expires
properties:
  paramIndicator:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:cdn:ParamIndicator"
    description: Indicates the purpose of the parameter
  paramName:
    type: string
    description: Parameter name
type: object
required:
  - paramIndicator
  - paramName
