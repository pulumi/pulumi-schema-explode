$token: azure-native:cdn:DeliveryRuleHttpVersionCondition
description: Defines the HttpVersion condition for the delivery rule.
properties:
  name:
    type: string
    description: |-
      The name of the condition for the delivery rule.
      Expected value is 'HttpVersion'.
    const: HttpVersion
  parameters:
    type: object
    $ref: "#/types/azure-native:cdn:HttpVersionMatchConditionParameters"
    description: Defines the parameters for the condition.
type: object
required:
  - name
  - parameters
