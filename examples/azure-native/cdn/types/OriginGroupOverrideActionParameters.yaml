$token: azure-native:cdn:OriginGroupOverrideActionParameters
description: Defines the parameters for the origin group override action.
properties:
  odataType:
    type: string
  originGroup:
    type: object
    $ref: "#/types/azure-native:cdn:ResourceReference"
    description: defines the OriginGroup that would override the DefaultOriginGroup.
type: object
required:
  - odataType
  - originGroup
