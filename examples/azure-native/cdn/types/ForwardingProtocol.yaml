$token: azure-native:cdn:ForwardingProtocol
description: Protocol this rule will use when forwarding traffic to backends.
type: string
enum:
  - value: HttpOnly
  - value: HttpsOnly
  - value: MatchRequest
