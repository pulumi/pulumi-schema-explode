$token: azure-native:cdn:SecurityPolicyWebApplicationFirewallAssociationResponse
description: settings for security policy patterns to match
properties:
  domains:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:cdn:ResourceReferenceResponse"
    description: List of domains.
  patternsToMatch:
    type: array
    items:
      type: string
    description: List of paths
type: object
