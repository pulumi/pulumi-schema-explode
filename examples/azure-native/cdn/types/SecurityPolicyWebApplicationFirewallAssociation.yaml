$token: azure-native:cdn:SecurityPolicyWebApplicationFirewallAssociation
description: settings for security policy patterns to match
properties:
  domains:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:cdn:ResourceReference"
    description: List of domains.
  patternsToMatch:
    type: array
    items:
      type: string
    description: List of paths
type: object
