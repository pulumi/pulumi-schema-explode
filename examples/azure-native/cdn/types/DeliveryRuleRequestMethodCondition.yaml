$token: azure-native:cdn:DeliveryRuleRequestMethodCondition
description: Defines the RequestMethod condition for the delivery rule.
properties:
  name:
    type: string
    description: |-
      The name of the condition for the delivery rule.
      Expected value is 'RequestMethod'.
    const: RequestMethod
  parameters:
    type: object
    $ref: "#/types/azure-native:cdn:RequestMethodMatchConditionParameters"
    description: Defines the parameters for the condition.
type: object
required:
  - name
  - parameters
