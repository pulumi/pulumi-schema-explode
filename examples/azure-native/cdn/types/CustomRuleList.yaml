$token: azure-native:cdn:CustomRuleList
description: Defines contents of custom rules
properties:
  rules:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:cdn:CustomRule"
    description: List of rules
type: object
