$token: azure-native:cdn:DeliveryRuleCacheKeyQueryStringAction
description: Defines the cache-key query string action for the delivery rule.
properties:
  name:
    type: string
    description: |-
      The name of the action for the delivery rule.
      Expected value is 'CacheKeyQueryString'.
    const: CacheKeyQueryString
  parameters:
    type: object
    $ref: "#/types/azure-native:cdn:CacheKeyQueryStringActionParameters"
    description: Defines the parameters for the action.
type: object
required:
  - name
  - parameters
