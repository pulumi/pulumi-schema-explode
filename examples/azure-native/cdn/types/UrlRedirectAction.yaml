$token: azure-native:cdn:UrlRedirectAction
description: Defines the url redirect action for the delivery rule.
properties:
  name:
    type: string
    description: |-
      The name of the action for the delivery rule.
      Expected value is 'UrlRedirect'.
    const: UrlRedirect
  parameters:
    type: object
    $ref: "#/types/azure-native:cdn:UrlRedirectActionParameters"
    description: Defines the parameters for the action.
type: object
required:
  - name
  - parameters
