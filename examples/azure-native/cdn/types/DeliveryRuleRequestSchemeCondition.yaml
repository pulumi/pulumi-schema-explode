$token: azure-native:cdn:DeliveryRuleRequestSchemeCondition
description: Defines the RequestScheme condition for the delivery rule.
properties:
  name:
    type: string
    description: |-
      The name of the condition for the delivery rule.
      Expected value is 'RequestScheme'.
    const: RequestScheme
  parameters:
    type: object
    $ref: "#/types/azure-native:cdn:RequestSchemeMatchConditionParameters"
    description: Defines the parameters for the condition.
type: object
required:
  - name
  - parameters
