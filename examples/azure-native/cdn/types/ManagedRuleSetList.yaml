$token: azure-native:cdn:ManagedRuleSetList
description: Defines the list of managed rule sets for the policy.
properties:
  managedRuleSets:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:cdn:ManagedRuleSet"
    description: List of rule sets.
type: object
