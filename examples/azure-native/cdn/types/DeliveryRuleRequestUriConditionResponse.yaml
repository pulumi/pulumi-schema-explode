$token: azure-native:cdn:DeliveryRuleRequestUriConditionResponse
description: Defines the RequestUri condition for the delivery rule.
properties:
  name:
    type: string
    description: |-
      The name of the condition for the delivery rule.
      Expected value is 'RequestUri'.
    const: RequestUri
  parameters:
    type: object
    $ref: "#/types/azure-native:cdn:RequestUriMatchConditionParametersResponse"
    description: Defines the parameters for the condition.
type: object
required:
  - name
  - parameters
