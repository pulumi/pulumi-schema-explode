$token: azure-native:cdn:RateLimitRuleList
description: Defines contents of rate limit rules
properties:
  rules:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:cdn:RateLimitRule"
    description: List of rules
type: object
