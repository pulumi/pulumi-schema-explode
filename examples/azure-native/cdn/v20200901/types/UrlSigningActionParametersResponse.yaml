$token: azure-native:cdn/v20200901:UrlSigningActionParametersResponse
description: Defines the parameters for the Url Signing action.
properties:
  algorithm:
    type: string
    description: Algorithm to use for URL signing
  odataType:
    type: string
  parameterNameOverride:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:cdn/v20200901:UrlSigningParamIdentifierResponse"
    description: "Defines which query string parameters in the url to be considered for expires, key id etc. "
type: object
required:
  - odataType
