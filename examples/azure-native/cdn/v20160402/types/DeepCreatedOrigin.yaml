$token: azure-native:cdn/v20160402:DeepCreatedOrigin
description: Deep created origins within a CDN endpoint.
properties:
  hostName:
    type: string
    description: The address of the origin. Domain names, IPv4 addresses, and IPv6 addresses are supported.
  httpPort:
    type: integer
    description: The value of the HTTP port. Must be between 1 and 65535
  httpsPort:
    type: integer
    description: The value of the HTTPS port. Must be between 1 and 65535
  name:
    type: string
    description: Origin name
type: object
required:
  - hostName
  - name
