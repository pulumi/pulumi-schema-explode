$token: azure-native:cdn/v20160402:getCustomDomain
description: CDN CustomDomain represents a mapping between a user specified domain name and a CDN endpoint. This is to use custom domain names to represent the URLs for branding purposes.
inputs:
  properties:
    customDomainName:
      type: string
      description: Name of the custom domain within an endpoint.
    endpointName:
      type: string
      description: Name of the endpoint within the CDN profile.
    profileName:
      type: string
      description: Name of the CDN profile within the resource group.
    resourceGroupName:
      type: string
      description: Name of the resource group within the Azure subscription.
  type: object
  required:
    - customDomainName
    - endpointName
    - profileName
    - resourceGroupName
outputs:
  description: CDN CustomDomain represents a mapping between a user specified domain name and a CDN endpoint. This is to use custom domain names to represent the URLs for branding purposes.
  properties:
    hostName:
      type: string
      description: The host name of the custom domain. Must be a domain name.
    id:
      type: string
      description: Resource ID
    name:
      type: string
      description: Resource name
    provisioningState:
      type: string
      description: Provisioning status of the custom domain.
    resourceState:
      type: string
      description: Resource status of the custom domain.
    type:
      type: string
      description: Resource type
  type: object
  required:
    - hostName
    - id
    - name
    - provisioningState
    - resourceState
    - type
deprecationMessage: Version 2016-04-02 will be removed in v2 of the provider.
