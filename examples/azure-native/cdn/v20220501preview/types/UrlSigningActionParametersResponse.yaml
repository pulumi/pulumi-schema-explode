$token: azure-native:cdn/v20220501preview:UrlSigningActionParametersResponse
description: Defines the parameters for the Url Signing action.
properties:
  algorithm:
    type: string
    description: Algorithm to use for URL signing
  parameterNameOverride:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:cdn/v20220501preview:UrlSigningParamIdentifierResponse"
    description: "Defines which query string parameters in the url to be considered for expires, key id etc. "
  typeName:
    type: string
type: object
required:
  - typeName
