$token: azure-native:cdn/v20220501preview:DeliveryRuleSocketAddrCondition
description: Defines the SocketAddress condition for the delivery rule.
properties:
  name:
    type: string
    description: |-
      The name of the condition for the delivery rule.
      Expected value is 'SocketAddr'.
    const: SocketAddr
  parameters:
    type: object
    $ref: "#/types/azure-native:cdn/v20220501preview:SocketAddrMatchConditionParameters"
    description: Defines the parameters for the condition.
type: object
required:
  - name
  - parameters
