$token: azure-native:cdn/v20210601:OriginGroupOverrideResponse
description: Defines the parameters for the origin group override configuration.
properties:
  forwardingProtocol:
    type: string
    description: Protocol this rule will use when forwarding traffic to backends.
  originGroup:
    type: object
    $ref: "#/types/azure-native:cdn/v20210601:ResourceReferenceResponse"
    description: defines the OriginGroup that would override the DefaultOriginGroup on route.
type: object
