$token: azure-native:cdn/v20210601:HostNameMatchConditionParameters
description: Defines the parameters for HostName match conditions
properties:
  matchValues:
    type: array
    items:
      type: string
    description: The match value for the condition of the delivery rule
  negateCondition:
    type: boolean
    description: Describes if this is negate condition or not
  operator:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:cdn/v20210601:HostNameOperator"
    description: Describes operator to be matched
  transforms:
    type: array
    items:
      oneOf:
        - type: string
        - $ref: "#/types/azure-native:cdn/v20210601:Transform"
    description: List of transforms
  typeName:
    type: string
type: object
required:
  - operator
  - typeName
