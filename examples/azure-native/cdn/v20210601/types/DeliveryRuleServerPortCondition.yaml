$token: azure-native:cdn/v20210601:DeliveryRuleServerPortCondition
description: Defines the ServerPort condition for the delivery rule.
properties:
  name:
    type: string
    description: |-
      The name of the condition for the delivery rule.
      Expected value is 'ServerPort'.
    const: ServerPort
  parameters:
    type: object
    $ref: "#/types/azure-native:cdn/v20210601:ServerPortMatchConditionParameters"
    description: Defines the parameters for the condition.
type: object
required:
  - name
  - parameters
