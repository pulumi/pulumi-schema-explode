$token: azure-native:cdn/v20190615preview:PostArgsMatchConditionParameters
description: Defines the parameters for PostArgs match conditions
properties:
  matchValues:
    type: array
    items:
      type: string
    description: The match value for the condition of the delivery rule
  negateCondition:
    type: boolean
    description: Describes if this is negate condition or not
  odataType:
    type: string
  operator:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:cdn/v20190615preview:PostArgsOperator"
    description: Describes operator to be matched
  selector:
    type: string
    description: Name of PostArg to be matched
  transforms:
    type: array
    items:
      oneOf:
        - type: string
        - $ref: "#/types/azure-native:cdn/v20190615preview:Transform"
    description: List of transforms
type: object
required:
  - matchValues
  - odataType
  - operator
  - selector
