$token: azure-native:cdn/v20190615preview:DeliveryRuleCacheExpirationAction
description: Defines the cache expiration action for the delivery rule.
properties:
  name:
    type: string
    description: |-
      The name of the action for the delivery rule.
      Expected value is 'CacheExpiration'.
    const: CacheExpiration
  parameters:
    type: object
    $ref: "#/types/azure-native:cdn/v20190615preview:CacheExpirationActionParameters"
    description: Defines the parameters for the action.
type: object
required:
  - name
  - parameters
