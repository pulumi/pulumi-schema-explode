$token: azure-native:cdn/v20200415:CookiesMatchConditionParameters
description: Defines the parameters for Cookies match conditions
properties:
  matchValues:
    type: array
    items:
      type: string
    description: The match value for the condition of the delivery rule
  negateCondition:
    type: boolean
    description: Describes if this is negate condition or not
  odataType:
    type: string
  operator:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:cdn/v20200415:CookiesOperator"
    description: Describes operator to be matched
  selector:
    type: string
    description: Name of Cookies to be matched
  transforms:
    type: array
    items:
      oneOf:
        - type: string
        - $ref: "#/types/azure-native:cdn/v20200415:Transform"
    description: List of transforms
type: object
required:
  - odataType
  - operator
