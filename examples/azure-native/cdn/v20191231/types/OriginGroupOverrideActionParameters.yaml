$token: azure-native:cdn/v20191231:OriginGroupOverrideActionParameters
description: Defines the parameters for the Origin Group override action.
properties:
  odataType:
    type: string
  originGroup:
    type: object
    $ref: "#/types/azure-native:cdn/v20191231:ResourceReference"
    description: A reference to the origin group from where the content will be fetched from when CDN does not have it
type: object
required:
  - odataType
  - originGroup
