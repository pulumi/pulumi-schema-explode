$token: azure-native:logic/v20160601:listIntegrationAccountKeyVaultKeys
description: Collection of key vault keys.
inputs:
  properties:
    integrationAccountName:
      type: string
      description: The integration account name.
    keyVault:
      type: object
      $ref: "#/types/azure-native:logic/v20160601:KeyVaultReference"
      description: The key vault reference.
    resourceGroupName:
      type: string
      description: The resource group name.
    skipToken:
      type: string
      description: The skip token.
  type: object
  required:
    - integrationAccountName
    - keyVault
    - resourceGroupName
outputs:
  description: Collection of key vault keys.
  properties:
    skipToken:
      type: string
      description: The skip token.
    value:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:logic/v20160601:KeyVaultKeyResponse"
      description: The key vault keys.
  type: object
