$token: azure-native:logic:FlowEndpoints
description: The flow endpoints configuration.
properties:
  accessEndpointIpAddresses:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:logic:IpAddress"
    description: The access endpoint ip address.
  outgoingIpAddresses:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:logic:IpAddress"
    description: The outgoing ip address.
type: object
