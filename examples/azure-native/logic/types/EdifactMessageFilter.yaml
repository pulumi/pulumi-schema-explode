$token: azure-native:logic:EdifactMessageFilter
description: The Edifact message filter for odata query.
properties:
  messageFilterType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:logic:MessageFilterType"
    description: The message filter type.
type: object
required:
  - messageFilterType
