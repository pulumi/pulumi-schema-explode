$token: azure-native:logic:OpenAuthenticationAccessPolicyResponse
description: Open authentication access policy defined by user.
properties:
  claims:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:logic:OpenAuthenticationPolicyClaimResponse"
    description: The access policy claims.
  type:
    type: string
    description: Type of provider for OAuth.
type: object
