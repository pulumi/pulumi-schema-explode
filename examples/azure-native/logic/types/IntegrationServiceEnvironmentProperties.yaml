$token: azure-native:logic:IntegrationServiceEnvironmentProperties
description: The integration service environment properties.
properties:
  encryptionConfiguration:
    type: object
    $ref: "#/types/azure-native:logic:IntegrationServiceEnvironmenEncryptionConfiguration"
    description: The encryption configuration.
  endpointsConfiguration:
    type: object
    $ref: "#/types/azure-native:logic:FlowEndpointsConfiguration"
    description: The endpoints configuration.
  integrationServiceEnvironmentId:
    type: string
    description: Gets the tracking id.
  networkConfiguration:
    type: object
    $ref: "#/types/azure-native:logic:NetworkConfiguration"
    description: The network configuration.
  provisioningState:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:logic:WorkflowProvisioningState"
    description: The provisioning state.
  state:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:logic:WorkflowState"
    description: The integration service environment state.
type: object
