$token: azure-native:logic:FlowAccessControlConfigurationPolicy
description: The access control configuration policy.
properties:
  allowedCallerIpAddresses:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:logic:IpAddressRange"
    description: The allowed caller IP address ranges.
  openAuthenticationPolicies:
    type: object
    $ref: "#/types/azure-native:logic:OpenAuthenticationAccessPolicies"
    description: The authentication policies for workflow.
type: object
