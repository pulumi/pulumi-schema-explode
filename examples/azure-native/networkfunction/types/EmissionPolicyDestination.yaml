$token: azure-native:networkfunction:EmissionPolicyDestination
description: Emission policy destination properties.
properties:
  destinationType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:networkfunction:DestinationType"
    description: Emission destination type.
type: object
