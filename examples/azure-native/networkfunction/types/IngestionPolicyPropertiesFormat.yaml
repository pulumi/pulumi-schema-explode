$token: azure-native:networkfunction:IngestionPolicyPropertiesFormat
description: Ingestion Policy properties.
properties:
  ingestionSources:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:networkfunction:IngestionSourcesPropertiesFormat"
    description: Ingestion Sources.
  ingestionType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:networkfunction:IngestionType"
    description: The ingestion type.
type: object
