$token: azure-native:networkfunction:EmissionPoliciesPropertiesFormat
description: Emission policy properties.
properties:
  emissionDestinations:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:networkfunction:EmissionPolicyDestination"
    description: Emission policy destinations.
  emissionType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:networkfunction:EmissionType"
    description: Emission format type.
type: object
