$token: azure-native:machinelearning:InputPort
description: Asset input port
properties:
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:machinelearning:InputPortType"
    description: Port data type.
    default: Dataset
type: object
