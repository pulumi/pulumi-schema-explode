$token: azure-native:iotcentral/v20210601:SystemAssignedServiceIdentity
description: Managed service identity (either system assigned, or none)
properties:
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:iotcentral/v20210601:SystemAssignedServiceIdentityType"
    description: Type of managed service identity (either system assigned, or none).
type: object
required:
  - type
