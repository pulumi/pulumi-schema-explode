$token: azure-native:iotcentral/v20211101preview:NetworkRuleSetIpRuleResponse
description: An object for an IP range that will be allowed access.
properties:
  action:
    type: string
    description: The network action for the IP mask.
  filterName:
    type: string
    description: The readable name of the IP rule.
  ipMask:
    type: string
    description: The CIDR block defining the IP range.
type: object
required:
  - action
