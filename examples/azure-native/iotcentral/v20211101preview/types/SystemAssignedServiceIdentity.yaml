$token: azure-native:iotcentral/v20211101preview:SystemAssignedServiceIdentity
description: Managed service identity (either system assigned, or none)
properties:
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:iotcentral/v20211101preview:SystemAssignedServiceIdentityType"
    description: Type of managed service identity (either system assigned, or none).
type: object
required:
  - type
