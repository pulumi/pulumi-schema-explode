$token: azure-native:iotcentral:AppSkuInfo
description: Information about the SKU of the IoT Central application.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:iotcentral:AppSku"
    description: The name of the SKU.
type: object
required:
  - name
