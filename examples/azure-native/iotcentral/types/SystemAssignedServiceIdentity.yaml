$token: azure-native:iotcentral:SystemAssignedServiceIdentity
description: Managed service identity (either system assigned, or none)
properties:
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:iotcentral:SystemAssignedServiceIdentityType"
    description: Type of managed service identity (either system assigned, or none).
type: object
required:
  - type
