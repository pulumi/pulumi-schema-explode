$token: azure-native:sqlvirtualmachine/v20220201:AgReplica
description: Availability group replica configuration.
properties:
  commit:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:sqlvirtualmachine/v20220201:Commit"
    description: Replica commit mode in availability group.
  failover:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:sqlvirtualmachine/v20220201:Failover"
    description: Replica failover mode in availability group.
  readableSecondary:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:sqlvirtualmachine/v20220201:ReadableSecondary"
    description: Replica readable secondary mode in availability group.
  role:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:sqlvirtualmachine/v20220201:Role"
    description: Replica Role in availability group.
  sqlVirtualMachineInstanceId:
    type: string
    description: Sql VirtualMachine Instance Id.
type: object
