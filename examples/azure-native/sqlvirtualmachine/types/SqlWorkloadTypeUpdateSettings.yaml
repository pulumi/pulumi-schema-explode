$token: azure-native:sqlvirtualmachine:SqlWorkloadTypeUpdateSettings
description: Set workload type to optimize storage for SQL Server.
properties:
  sqlWorkloadType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:sqlvirtualmachine:SqlWorkloadType"
    description: SQL Server workload type.
type: object
