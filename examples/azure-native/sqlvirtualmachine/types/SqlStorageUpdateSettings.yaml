$token: azure-native:sqlvirtualmachine:SqlStorageUpdateSettings
description: Set disk storage settings for SQL Server.
properties:
  diskConfigurationType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:sqlvirtualmachine:DiskConfigurationType"
    description: Disk configuration to apply to SQL Server.
  diskCount:
    type: integer
    description: Virtual machine disk count.
  startingDeviceId:
    type: integer
    description: Device id of the first disk to be updated.
type: object
