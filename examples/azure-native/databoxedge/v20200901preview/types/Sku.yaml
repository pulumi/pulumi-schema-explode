$token: azure-native:databoxedge/v20200901preview:Sku
description: The SKU type.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:databoxedge/v20200901preview:SkuName"
    description: SKU name.
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:databoxedge/v20200901preview:SkuTier"
    description: The SKU tier. This is based on the SKU name.
type: object
