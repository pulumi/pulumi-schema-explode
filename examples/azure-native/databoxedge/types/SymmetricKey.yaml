$token: azure-native:databoxedge:SymmetricKey
description: Symmetric key for authentication.
properties:
  connectionString:
    type: object
    $ref: "#/types/azure-native:databoxedge:AsymmetricEncryptedSecret"
    description: Connection string based on the symmetric key.
type: object
