$token: azure-native:databoxedge:ResourceIdentity
description: Msi identity details of the resource
properties:
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:databoxedge:MsiIdentityType"
    description: Identity type
type: object
