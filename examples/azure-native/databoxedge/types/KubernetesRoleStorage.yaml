$token: azure-native:databoxedge:KubernetesRoleStorage
description: Kubernetes role storage resource
properties:
  endpoints:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:databoxedge:MountPointMap"
    description: Mount points of shares in role(s).
type: object
