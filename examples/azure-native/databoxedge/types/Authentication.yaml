$token: azure-native:databoxedge:Authentication
description: Authentication mechanism for IoT devices.
properties:
  symmetricKey:
    type: object
    $ref: "#/types/azure-native:databoxedge:SymmetricKey"
    description: Symmetric key for authentication.
type: object
