$token: azure-native:databoxedge:MetricCounterSetResponse
description: The metric counter set
properties:
  counters:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:databoxedge:MetricCounterResponse"
    description: The counters that should be collected in this set.
type: object
required:
  - counters
