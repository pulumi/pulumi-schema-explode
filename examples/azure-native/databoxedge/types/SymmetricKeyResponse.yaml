$token: azure-native:databoxedge:SymmetricKeyResponse
description: Symmetric key for authentication.
properties:
  connectionString:
    type: object
    $ref: "#/types/azure-native:databoxedge:AsymmetricEncryptedSecretResponse"
    description: Connection string based on the symmetric key.
type: object
