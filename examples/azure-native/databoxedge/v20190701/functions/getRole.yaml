$token: azure-native:databoxedge/v20190701:getRole
description: Compute role.
inputs:
  properties:
    deviceName:
      type: string
      description: The device name.
    name:
      type: string
      description: The role name.
    resourceGroupName:
      type: string
      description: The resource group name.
  type: object
  required:
    - deviceName
    - name
    - resourceGroupName
outputs:
  description: Compute role.
  properties:
    id:
      type: string
      description: The path ID that uniquely identifies the object.
    kind:
      type: string
      description: Role type.
    name:
      type: string
      description: The object name.
    type:
      type: string
      description: The hierarchical type of the object.
  type: object
  required:
    - id
    - kind
    - name
    - type
deprecationMessage: Version 2019-07-01 will be removed in v2 of the provider.
