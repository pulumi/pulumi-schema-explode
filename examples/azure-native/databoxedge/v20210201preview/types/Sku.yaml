$token: azure-native:databoxedge/v20210201preview:Sku
description: The SKU type.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:databoxedge/v20210201preview:SkuName"
    description: SKU name.
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:databoxedge/v20210201preview:SkuTier"
    description: The SKU tier. This is based on the SKU name.
type: object
