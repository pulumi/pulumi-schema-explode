$token: azure-native:hybridconnectivity:listEndpointManagedProxyDetails
description: |-
  Managed Proxy
  API Version: 2022-05-01-preview.
inputs:
  properties:
    endpointName:
      type: string
      description: The endpoint name.
    hostname:
      type: string
      description: The target host name.
    resourceUri:
      type: string
      description: The fully qualified Azure Resource manager identifier of the resource to be connected.
    service:
      type: string
      description: The name of the service.
  type: object
  required:
    - endpointName
    - resourceUri
    - service
outputs:
  description: Managed Proxy
  properties:
    expiresOn:
      type: number
      description: The expiration time of short lived proxy name in unix epoch.
    proxy:
      type: string
      description: The short lived proxy name.
  type: object
  required:
    - expiresOn
    - proxy
