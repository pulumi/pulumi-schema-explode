$token: azure-native:appconfiguration:PrivateLinkServiceConnectionState
description: The state of a private link service connection.
properties:
  description:
    type: string
    description: The private link service connection description.
  status:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:appconfiguration:ConnectionStatus"
    description: The private link service connection status.
type: object
