$token: azure-native:powerbi:AzureSku
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:powerbi:AzureSkuName"
    description: SKU name
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:powerbi:AzureSkuTier"
    description: SKU tier
type: object
required:
  - name
  - tier
