$token: azure-native:powerbi/v20160129:AzureSku
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:powerbi/v20160129:AzureSkuName"
    description: SKU name
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:powerbi/v20160129:AzureSkuTier"
    description: SKU tier
type: object
required:
  - name
  - tier
