$token: azure-native:customerinsights/v20170426:RelationshipTypeMapping
description: Maps fields in Profile to their corresponding StrongIds in Related Profile.
properties:
  fieldMappings:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:customerinsights/v20170426:RelationshipTypeFieldMapping"
    description: Maps a profile property with the StrongId of related profile. This is an array to support StrongIds that are composite key as well.
type: object
required:
  - fieldMappings
