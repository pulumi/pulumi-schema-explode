$token: azure-native:customerinsights:PredictionDistributionDefinitionResponse
description: The definition of the prediction distribution.
properties:
  distributions:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:customerinsights:PredictionDistributionDefinitionResponseDistributions"
    description: Distributions of the prediction.
  totalNegatives:
    type: number
    description: Total negatives in the distribution.
  totalPositives:
    type: number
    description: Total positive in the distribution.
type: object
