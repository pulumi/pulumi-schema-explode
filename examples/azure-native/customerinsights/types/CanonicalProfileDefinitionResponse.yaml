$token: azure-native:customerinsights:CanonicalProfileDefinitionResponse
description: Definition of canonical profile.
properties:
  canonicalProfileId:
    type: integer
    description: Canonical profile ID.
  properties:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:customerinsights:CanonicalProfileDefinitionResponseProperties"
    description: Properties of the canonical profile.
type: object
