$token: azure-native:customerinsights:KpiThresholds
description: Defines the KPI Threshold limits.
properties:
  increasingKpi:
    type: boolean
    description: Whether or not the KPI is an increasing KPI.
  lowerLimit:
    type: number
    description: The lower threshold limit.
  upperLimit:
    type: number
    description: The upper threshold limit.
type: object
required:
  - increasingKpi
  - lowerLimit
  - upperLimit
