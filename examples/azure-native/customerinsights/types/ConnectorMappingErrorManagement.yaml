$token: azure-native:customerinsights:ConnectorMappingErrorManagement
description: The error management.
properties:
  errorLimit:
    type: integer
    description: The error limit allowed while importing data.
  errorManagementType:
    $ref: "#/types/azure-native:customerinsights:ErrorManagementTypes"
    description: The type of error management to use for the mapping.
type: object
required:
  - errorManagementType
