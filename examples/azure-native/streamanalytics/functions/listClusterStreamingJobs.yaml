$token: azure-native:streamanalytics:listClusterStreamingJobs
description: |-
  A list of streaming jobs. Populated by a List operation.
  API Version: 2020-03-01-preview.
inputs:
  properties:
    clusterName:
      type: string
      description: The name of the cluster.
    resourceGroupName:
      type: string
      description: The name of the resource group. The name is case insensitive.
  type: object
  required:
    - clusterName
    - resourceGroupName
outputs:
  description: A list of streaming jobs. Populated by a List operation.
  properties:
    nextLink:
      type: string
      description: The URL to fetch the next set of streaming jobs.
    value:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:streamanalytics:ClusterJobResponse"
      description: A list of streaming jobs.
  type: object
  required:
    - nextLink
    - value
