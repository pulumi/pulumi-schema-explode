$token: azure-native:streamanalytics/v20170401preview:ParquetSerializationResponse
description: Describes how data from an input is serialized or how data is serialized when written to an output in Parquet format.
properties:
  type:
    type: string
    description: |-
      Indicates the type of serialization that the input or output uses. Required on PUT (CreateOrReplace) requests.
      Expected value is 'Parquet'.
    const: Parquet
type: object
required:
  - type
