$token: azure-native:streamanalytics/v20211001preview:GatewayMessageBusOutputDataSource
description: Describes a Gateway Message Bus output data source.
properties:
  topic:
    type: string
    description: The name of the Service Bus topic.
  type:
    type: string
    description: |-
      Indicates the type of data source output will be written to. Required on PUT (CreateOrReplace) requests.
      Expected value is 'GatewayMessageBus'.
    const: GatewayMessageBus
type: object
required:
  - type
