$token: azure-native:streamanalytics/v20211001preview:Compression
description: Describes how input data is compressed
properties:
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:streamanalytics/v20211001preview:CompressionType"
    description: Indicates the type of compression that the input uses. Required on PUT (CreateOrReplace) requests.
type: object
required:
  - type
