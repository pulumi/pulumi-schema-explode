$token: azure-native:cache/v20201201:getRedis
description: A single Redis item in List or Get Operation.
inputs:
  properties:
    name:
      type: string
      description: The name of the Redis cache.
    resourceGroupName:
      type: string
      description: The name of the resource group.
  type: object
  required:
    - name
    - resourceGroupName
outputs:
  description: A single Redis item in List or Get Operation.
  properties:
    accessKeys:
      type: object
      $ref: "#/types/azure-native:cache/v20201201:RedisAccessKeysResponse"
      description: The keys of the Redis cache - not set if this object is not the response to Create or Update redis cache
    enableNonSslPort:
      type: boolean
      description: Specifies whether the non-ssl Redis server port (6379) is enabled.
      default: false
    hostName:
      type: string
      description: Redis host name.
    id:
      type: string
      description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
    instances:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:cache/v20201201:RedisInstanceDetailsResponse"
      description: List of the Redis instances associated with the cache
    linkedServers:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:cache/v20201201:RedisLinkedServerResponse"
      description: List of the linked servers associated with the cache
    location:
      type: string
      description: The geo-location where the resource lives
    minimumTlsVersion:
      type: string
      description: "Optional: requires clients to use a specified TLS version (or higher) to connect (e,g, '1.0', '1.1', '1.2')"
    name:
      type: string
      description: The name of the resource
    port:
      type: integer
      description: Redis non-SSL port.
    privateEndpointConnections:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:cache/v20201201:PrivateEndpointConnectionResponse"
      description: List of private endpoint connection associated with the specified redis cache
    provisioningState:
      type: string
      description: Redis instance provisioning status.
    publicNetworkAccess:
      type: string
      description: Whether or not public endpoint access is allowed for this cache.  Value is optional but if passed in, must be 'Enabled' or 'Disabled'. If 'Disabled', private endpoints are the exclusive access method. Default value is 'Enabled'
      default: Enabled
    redisConfiguration:
      type: object
      $ref: "#/types/azure-native:cache/v20201201:RedisCommonPropertiesResponseRedisConfiguration"
      description: "All Redis Settings. Few possible keys: rdb-backup-enabled,rdb-storage-connection-string,rdb-backup-frequency,maxmemory-delta,maxmemory-policy,notify-keyspace-events,maxmemory-samples,slowlog-log-slower-than,slowlog-max-len,list-max-ziplist-entries,list-max-ziplist-value,hash-max-ziplist-entries,hash-max-ziplist-value,set-max-intset-entries,zset-max-ziplist-entries,zset-max-ziplist-value etc."
    redisVersion:
      type: string
      description: "Redis version. This should be in the form 'major[.minor[.build]]' (only 'major' is required) or the value 'latest' which refers to the latest stable Redis version that is available. Only the major and minor version are used in a PUT/PATCH request. Supported versions: 4.0, 6.0."
    replicasPerMaster:
      type: integer
      description: The number of replicas to be created per primary.
    replicasPerPrimary:
      type: integer
      description: The number of replicas to be created per primary.
    shardCount:
      type: integer
      description: The number of shards to be created on a Premium Cluster Cache.
    sku:
      type: object
      $ref: "#/types/azure-native:cache/v20201201:SkuResponse"
      description: The SKU of the Redis cache to deploy.
    sslPort:
      type: integer
      description: Redis SSL port.
    staticIP:
      type: string
      description: Static IP address. Optionally, may be specified when deploying a Redis cache inside an existing Azure Virtual Network; auto assigned by default.
    subnetId:
      type: string
      description: "The full resource ID of a subnet in a virtual network to deploy the Redis cache in. Example format: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/Microsoft.{Network|ClassicNetwork}/VirtualNetworks/vnet1/subnets/subnet1"
    tags:
      type: object
      additionalProperties:
        type: string
      description: Resource tags.
    tenantSettings:
      type: object
      additionalProperties:
        type: string
      description: A dictionary of tenant settings
    type:
      type: string
      description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
    zones:
      type: array
      items:
        type: string
      description: A list of availability zones denoting where the resource needs to come from.
  type: object
  required:
    - accessKeys
    - hostName
    - id
    - instances
    - linkedServers
    - location
    - name
    - port
    - privateEndpointConnections
    - provisioningState
    - sku
    - sslPort
    - type
