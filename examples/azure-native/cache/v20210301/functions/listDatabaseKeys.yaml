$token: azure-native:cache/v20210301:listDatabaseKeys
description: The secret access keys used for authenticating connections to redis
inputs:
  properties:
    clusterName:
      type: string
      description: The name of the RedisEnterprise cluster.
    databaseName:
      type: string
      description: The name of the database.
    resourceGroupName:
      type: string
      description: The name of the resource group. The name is case insensitive.
  type: object
  required:
    - clusterName
    - databaseName
    - resourceGroupName
outputs:
  description: The secret access keys used for authenticating connections to redis
  properties:
    primaryKey:
      type: string
      description: The current primary key that clients can use to authenticate
    secondaryKey:
      type: string
      description: The current secondary key that clients can use to authenticate
  type: object
  required:
    - primaryKey
    - secondaryKey
