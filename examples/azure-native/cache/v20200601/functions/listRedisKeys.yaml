$token: azure-native:cache/v20200601:listRedisKeys
description: Redis cache access keys.
inputs:
  properties:
    name:
      type: string
      description: The name of the Redis cache.
    resourceGroupName:
      type: string
      description: The name of the resource group.
  type: object
  required:
    - name
    - resourceGroupName
outputs:
  description: Redis cache access keys.
  properties:
    primaryKey:
      type: string
      description: The current primary key that clients can use to authenticate with Redis cache.
    secondaryKey:
      type: string
      description: The current secondary key that clients can use to authenticate with Redis cache.
  type: object
  required:
    - primaryKey
    - secondaryKey
