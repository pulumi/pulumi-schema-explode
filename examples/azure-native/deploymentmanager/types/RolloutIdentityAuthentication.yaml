$token: azure-native:deploymentmanager:RolloutIdentityAuthentication
description: RolloutIdentity uses the user-assigned managed identity authentication context specified in the Identity property during rollout creation.
properties:
  type:
    type: string
    description: |-
      The authentication type.
      Expected value is 'RolloutIdentity'.
    const: RolloutIdentity
type: object
required:
  - type
