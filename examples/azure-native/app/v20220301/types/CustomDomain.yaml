$token: azure-native:app/v20220301:CustomDomain
description: Custom Domain of a Container App
properties:
  bindingType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:app/v20220301:BindingType"
    description: Custom Domain binding type.
  certificateId:
    type: string
    description: Resource Id of the Certificate to be bound to this hostname. Must exist in the Managed Environment.
  name:
    type: string
    description: Hostname.
type: object
required:
  - certificateId
  - name
