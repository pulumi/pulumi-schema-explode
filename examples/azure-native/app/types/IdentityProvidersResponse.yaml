$token: azure-native:app:IdentityProvidersResponse
description: The configuration settings of each of the identity providers used to configure ContainerApp Service Authentication/Authorization.
properties:
  apple:
    type: object
    $ref: "#/types/azure-native:app:AppleResponse"
    description: The configuration settings of the Apple provider.
  azureActiveDirectory:
    type: object
    $ref: "#/types/azure-native:app:AzureActiveDirectoryResponse"
    description: The configuration settings of the Azure Active directory provider.
  azureStaticWebApps:
    type: object
    $ref: "#/types/azure-native:app:AzureStaticWebAppsResponse"
    description: The configuration settings of the Azure Static Web Apps provider.
  customOpenIdConnectProviders:
    type: object
    additionalProperties:
      type: object
      $ref: "#/types/azure-native:app:CustomOpenIdConnectProviderResponse"
    description: |-
      The map of the name of the alias of each custom Open ID Connect provider to the
      configuration settings of the custom Open ID Connect provider.
  facebook:
    type: object
    $ref: "#/types/azure-native:app:FacebookResponse"
    description: The configuration settings of the Facebook provider.
  gitHub:
    type: object
    $ref: "#/types/azure-native:app:GitHubResponse"
    description: The configuration settings of the GitHub provider.
  google:
    type: object
    $ref: "#/types/azure-native:app:GoogleResponse"
    description: The configuration settings of the Google provider.
  twitter:
    type: object
    $ref: "#/types/azure-native:app:TwitterResponse"
    description: The configuration settings of the Twitter provider.
type: object
