$token: azure-native:app:GithubActionConfiguration
description: Configuration properties that define the mutable settings of a Container App SourceControl
properties:
  azureCredentials:
    type: object
    $ref: "#/types/azure-native:app:AzureCredentials"
    description: AzureCredentials configurations.
  contextPath:
    type: string
    description: Context path
  image:
    type: string
    description: Image name
  os:
    type: string
    description: Operation system
  publishType:
    type: string
    description: Code or Image
  registryInfo:
    type: object
    $ref: "#/types/azure-native:app:RegistryInfo"
    description: Registry configurations.
  runtimeStack:
    type: string
    description: Runtime stack
  runtimeVersion:
    type: string
    description: Runtime version
type: object
