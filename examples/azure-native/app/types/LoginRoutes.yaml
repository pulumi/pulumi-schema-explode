$token: azure-native:app:LoginRoutes
description: The routes that specify the endpoints used for login and logout requests.
properties:
  logoutEndpoint:
    type: string
    description: The endpoint at which a logout request should be made.
type: object
