$token: azure-native:app:ScaleRuleResponse
description: Container App container scaling rule.
properties:
  azureQueue:
    type: object
    $ref: "#/types/azure-native:app:QueueScaleRuleResponse"
    description: Azure Queue based scaling.
  custom:
    type: object
    $ref: "#/types/azure-native:app:CustomScaleRuleResponse"
    description: Custom scale rule.
  http:
    type: object
    $ref: "#/types/azure-native:app:HttpScaleRuleResponse"
    description: HTTP requests based scaling.
  name:
    type: string
    description: Scale Rule Name
type: object
