$token: azure-native:app/v20220101preview:CustomScaleRule
description: Container App container Custom scaling rule.
properties:
  auth:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:app/v20220101preview:ScaleRuleAuth"
    description: Authentication secrets for the custom scale rule.
  metadata:
    type: object
    additionalProperties:
      type: string
    description: Metadata properties to describe custom scale rule.
  type:
    type: string
    description: |-
      Type of the custom scale rule
      eg: azure-servicebus, redis etc.
type: object
