$token: azure-native:app/v20220101preview:Scale
description: Container App scaling configurations.
properties:
  maxReplicas:
    type: integer
    description: Optional. Maximum number of container replicas. Defaults to 10 if not set.
  minReplicas:
    type: integer
    description: Optional. Minimum number of container replicas.
  rules:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:app/v20220101preview:ScaleRule"
    description: Scaling rules.
type: object
