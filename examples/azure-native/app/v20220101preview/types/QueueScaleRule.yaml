$token: azure-native:app/v20220101preview:QueueScaleRule
description: Container App container Azure Queue based scaling rule.
properties:
  auth:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:app/v20220101preview:ScaleRuleAuth"
    description: Authentication secrets for the queue scale rule.
  queueLength:
    type: integer
    description: Queue length.
  queueName:
    type: string
    description: Queue name.
type: object
