$token: azure-native:recommendationsservice:EndpointAuthentication
description: Service endpoints authentication details.
properties:
  aadTenantID:
    type: string
    description: AAD tenant ID.
  principalID:
    type: string
    description: AAD principal ID.
  principalType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:recommendationsservice:PrincipalType"
    description: AAD principal type.
type: object
