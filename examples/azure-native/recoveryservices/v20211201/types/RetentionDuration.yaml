$token: azure-native:recoveryservices/v20211201:RetentionDuration
description: Retention duration.
properties:
  count:
    type: integer
    description: "Count of duration types. Retention duration is obtained by the counting the duration type Count times.\r

      For example, when Count = 3 and DurationType = Weeks, retention duration will be three weeks."
  durationType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:recoveryservices/v20211201:RetentionDurationType"
    description: Retention duration type of retention policy.
type: object
