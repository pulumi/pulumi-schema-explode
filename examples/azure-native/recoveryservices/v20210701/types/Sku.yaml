$token: azure-native:recoveryservices/v20210701:Sku
description: Identifies the unique system identifier for each Azure resource.
properties:
  capacity:
    type: string
    description: The sku capacity
  family:
    type: string
    description: The sku family
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:recoveryservices/v20210701:SkuName"
    description: The Sku name.
  size:
    type: string
    description: The sku size
  tier:
    type: string
    description: The Sku tier.
type: object
required:
  - name
