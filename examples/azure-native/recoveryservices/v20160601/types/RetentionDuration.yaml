$token: azure-native:recoveryservices/v20160601:RetentionDuration
description: Retention duration.
properties:
  count:
    type: integer
    description: "Count of the duration types. Retention duration is determined by the combining the Count times and durationType. \r

      \   For example, if Count = 3 and durationType = Weeks, then the retention duration is three weeks."
  durationType:
    $ref: "#/types/azure-native:recoveryservices/v20160601:RetentionDurationType"
    description: The retention duration type of the retention policy.
type: object
