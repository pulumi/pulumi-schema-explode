$token: azure-native:recoveryservices/v20160601:Sku
description: Identifies the unique system identifier for each Azure resource.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:recoveryservices/v20160601:SkuName"
    description: The Sku name.
type: object
required:
  - name
