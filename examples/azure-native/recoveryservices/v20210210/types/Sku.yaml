$token: azure-native:recoveryservices/v20210210:Sku
description: Identifies the unique system identifier for each Azure resource.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:recoveryservices/v20210210:SkuName"
    description: The Sku name.
  tier:
    type: string
    description: The Sku tier.
type: object
required:
  - name
