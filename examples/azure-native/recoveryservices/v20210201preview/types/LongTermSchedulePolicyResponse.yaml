$token: azure-native:recoveryservices/v20210201preview:LongTermSchedulePolicyResponse
description: Long term policy schedule.
properties:
  schedulePolicyType:
    type: string
    description: |-
      This property will be used as the discriminator for deciding the specific types in the polymorphic chain of types.
      Expected value is 'LongTermSchedulePolicy'.
    const: LongTermSchedulePolicy
type: object
required:
  - schedulePolicyType
