$token: azure-native:recoveryservices/v20201201:Settings
description: Common settings field for backup management
properties:
  isCompression:
    type: boolean
    description: "Workload compression flag. This has been added so that 'isSqlCompression'\r

      will be deprecated once clients upgrade to consider this flag."
  issqlcompression:
    type: boolean
    description: SQL compression flag
  timeZone:
    type: string
    description: 'TimeZone optional input as string. For example: TimeZone = "Pacific Standard Time".'
type: object
