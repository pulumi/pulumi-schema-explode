$token: azure-native:recoveryservices:Sku
description: Identifies the unique system identifier for each Azure resource.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:recoveryservices:SkuName"
    description: The Sku name.
  tier:
    type: string
    description: The Sku tier.
type: object
required:
  - name
