$token: azure-native:iotsecurity:DefenderSettingsPropertiesMdeIntegration
description: MDE integration configuration
properties:
  status:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:iotsecurity:MdeIntegration"
    description: Integration status
type: object
required:
  - status
