$token: azure-native:connectedvmwarevsphere/v20201001preview:NicIPSettings
description: Defines the network interface ip settings.
properties:
  allocationMethod:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:connectedvmwarevsphere/v20201001preview:IPAddressAllocationMethod"
    description: Gets or sets the nic allocation method.
  dnsServers:
    type: array
    items:
      type: string
    description: Gets or sets the dns servers.
  gateway:
    type: array
    items:
      type: string
    description: Gets or sets the gateway.
  ipAddress:
    type: string
    description: Gets or sets the ip address for the nic.
  subnetMask:
    type: string
    description: Gets or sets the mask.
type: object
