$token: azure-native:connectedvmwarevsphere:Identity
description: Managed service identity.
properties:
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:connectedvmwarevsphere:IdentityType"
    description: The type of managed service identity.
type: object
required:
  - type
