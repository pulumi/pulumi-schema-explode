$token: azure-native:documentdb:CassandraSchema
description: Cosmos DB Cassandra table schema
properties:
  clusterKeys:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:documentdb:ClusterKey"
    description: List of cluster key.
  columns:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:documentdb:Column"
    description: List of Cassandra table columns.
  partitionKeys:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:documentdb:CassandraPartitionKey"
    description: List of partition key.
type: object
