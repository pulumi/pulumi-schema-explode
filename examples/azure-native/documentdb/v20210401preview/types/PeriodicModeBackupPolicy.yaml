$token: azure-native:documentdb/v20210401preview:PeriodicModeBackupPolicy
description: The object representing periodic mode backup policy.
properties:
  periodicModeProperties:
    type: object
    $ref: "#/types/azure-native:documentdb/v20210401preview:PeriodicModeProperties"
    description: Configuration values for periodic mode backup
  type:
    type: string
    description: |-
      Describes the mode of backups.
      Expected value is 'Periodic'.
    const: Periodic
type: object
required:
  - type
