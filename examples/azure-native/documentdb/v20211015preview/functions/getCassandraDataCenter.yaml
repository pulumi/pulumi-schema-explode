$token: azure-native:documentdb/v20211015preview:getCassandraDataCenter
description: A managed Cassandra data center.
inputs:
  properties:
    clusterName:
      type: string
      description: Managed Cassandra cluster name.
    dataCenterName:
      type: string
      description: Data center name in a managed Cassandra cluster.
    resourceGroupName:
      type: string
      description: The name of the resource group. The name is case insensitive.
  type: object
  required:
    - clusterName
    - dataCenterName
    - resourceGroupName
outputs:
  description: A managed Cassandra data center.
  properties:
    id:
      type: string
      description: The unique resource identifier of the database account.
    name:
      type: string
      description: The name of the database account.
    properties:
      type: object
      $ref: "#/types/azure-native:documentdb/v20211015preview:DataCenterResourceResponseProperties"
      description: Properties of a managed Cassandra data center.
    type:
      type: string
      description: The type of Azure resource.
  type: object
  required:
    - id
    - name
    - properties
    - type
