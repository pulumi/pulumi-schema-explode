$token: azure-native:documentdb/v20150408:listDatabaseAccountConnectionStrings
description: The connection strings for the given database account.
inputs:
  properties:
    accountName:
      type: string
      description: Cosmos DB database account name.
    resourceGroupName:
      type: string
      description: Name of an Azure resource group.
  type: object
  required:
    - accountName
    - resourceGroupName
outputs:
  description: The connection strings for the given database account.
  properties:
    connectionStrings:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:documentdb/v20150408:DatabaseAccountConnectionStringResponse"
      description: An array that contains the connection strings for the Cosmos DB account.
  type: object
