$token: azure-native:documentdb/v20220515preview:ContinuousModeProperties
description: Configuration values for periodic mode backup
properties:
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:documentdb/v20220515preview:ContinuousTier"
    description: Enum to indicate type of Continuos backup mode
type: object
