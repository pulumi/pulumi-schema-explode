$token: azure-native:documentdb/v20210301preview:PeriodicModeBackupPolicyResponse
description: The object representing periodic mode backup policy.
properties:
  periodicModeProperties:
    type: object
    $ref: "#/types/azure-native:documentdb/v20210301preview:PeriodicModePropertiesResponse"
    description: Configuration values for periodic mode backup
  type:
    type: string
    description: |-
      Describes the mode of backups.
      Expected value is 'Periodic'.
    const: Periodic
type: object
required:
  - type
