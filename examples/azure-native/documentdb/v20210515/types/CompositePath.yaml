$token: azure-native:documentdb/v20210515:CompositePath
properties:
  order:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:documentdb/v20210515:CompositePathSortOrder"
    description: Sort order for composite paths.
  path:
    type: string
    description: The path for which the indexing behavior applies to. Index paths typically start with root and end with wildcard (/path/*)
type: object
