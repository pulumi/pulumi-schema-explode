$token: azure-native:operationalinsights/v20190801preview:getLinkedStorageAccount
description: Linked storage accounts top level resource container.
inputs:
  properties:
    dataSourceType:
      type: string
      description: Linked storage accounts type.
    resourceGroupName:
      type: string
      description: The name of the resource group to get. The name is case insensitive.
    workspaceName:
      type: string
      description: Name of the Log Analytics Workspace that will contain the resource.
  type: object
  required:
    - dataSourceType
    - resourceGroupName
    - workspaceName
outputs:
  description: Linked storage accounts top level resource container.
  properties:
    dataSourceType:
      type: string
      description: Linked storage accounts type.
    id:
      type: string
      description: Resource ID.
    name:
      type: string
      description: Resource name.
    storageAccountIds:
      type: array
      items:
        type: string
      description: Linked storage accounts resources ids.
    type:
      type: string
      description: Resource type.
  type: object
  required:
    - dataSourceType
    - id
    - name
    - type
