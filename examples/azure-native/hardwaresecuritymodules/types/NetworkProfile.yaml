$token: azure-native:hardwaresecuritymodules:NetworkProfile
properties:
  networkInterfaces:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:hardwaresecuritymodules:NetworkInterface"
    description: Specifies the list of resource Ids for the network interfaces associated with the dedicated HSM.
  subnet:
    type: object
    $ref: "#/types/azure-native:hardwaresecuritymodules:ApiEntityReference"
    description: Specifies the identifier of the subnet.
type: object
