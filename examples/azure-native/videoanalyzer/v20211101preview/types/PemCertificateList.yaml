$token: azure-native:videoanalyzer/v20211101preview:PemCertificateList
description: A list of PEM formatted certificates.
properties:
  certificates:
    type: array
    items:
      type: string
    description: PEM formatted public certificates. One certificate per entry.
  type:
    type: string
    description: |-
      The discriminator for derived types.
      Expected value is '#Microsoft.VideoAnalyzer.PemCertificateList'.
    const: "#Microsoft.VideoAnalyzer.PemCertificateList"
type: object
required:
  - certificates
  - type
