$token: azure-native:sql/v20150501preview:getSyncGroup
description: An Azure SQL Database sync group.
inputs:
  properties:
    databaseName:
      type: string
      description: The name of the database on which the sync group is hosted.
    resourceGroupName:
      type: string
      description: The name of the resource group that contains the resource. You can obtain this value from the Azure Resource Manager API or the portal.
    serverName:
      type: string
      description: The name of the server.
    syncGroupName:
      type: string
      description: The name of the sync group.
  type: object
  required:
    - databaseName
    - resourceGroupName
    - serverName
    - syncGroupName
outputs:
  description: An Azure SQL Database sync group.
  properties:
    conflictResolutionPolicy:
      type: string
      description: Conflict resolution policy of the sync group.
    hubDatabaseUserName:
      type: string
      description: User name for the sync group hub database credential.
    id:
      type: string
      description: Resource ID.
    interval:
      type: integer
      description: Sync interval of the sync group.
    lastSyncTime:
      type: string
      description: Last sync time of the sync group.
    name:
      type: string
      description: Resource name.
    schema:
      type: object
      $ref: "#/types/azure-native:sql/v20150501preview:SyncGroupSchemaResponse"
      description: Sync schema of the sync group.
    syncDatabaseId:
      type: string
      description: ARM resource id of the sync database in the sync group.
    syncState:
      type: string
      description: Sync state of the sync group.
    type:
      type: string
      description: Resource type.
  type: object
  required:
    - id
    - lastSyncTime
    - name
    - syncState
    - type
