$token: azure-native:sql/v20170301preview:getServerDnsAlias
description: A server DNS alias.
inputs:
  properties:
    dnsAliasName:
      type: string
      description: The name of the server DNS alias.
    resourceGroupName:
      type: string
      description: The name of the resource group that contains the resource. You can obtain this value from the Azure Resource Manager API or the portal.
    serverName:
      type: string
      description: The name of the server that the alias is pointing to.
  type: object
  required:
    - dnsAliasName
    - resourceGroupName
    - serverName
outputs:
  description: A server DNS alias.
  properties:
    azureDnsRecord:
      type: string
      description: The fully qualified DNS record for alias
    id:
      type: string
      description: Resource ID.
    name:
      type: string
      description: Resource name.
    type:
      type: string
      description: Resource type.
  type: object
  required:
    - azureDnsRecord
    - id
    - name
    - type
