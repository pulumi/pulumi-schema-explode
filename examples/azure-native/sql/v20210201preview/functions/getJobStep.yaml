$token: azure-native:sql/v20210201preview:getJobStep
description: A job step.
inputs:
  properties:
    jobAgentName:
      type: string
      description: The name of the job agent.
    jobName:
      type: string
      description: The name of the job.
    resourceGroupName:
      type: string
      description: The name of the resource group that contains the resource. You can obtain this value from the Azure Resource Manager API or the portal.
    serverName:
      type: string
      description: The name of the server.
    stepName:
      type: string
      description: The name of the job step.
  type: object
  required:
    - jobAgentName
    - jobName
    - resourceGroupName
    - serverName
    - stepName
outputs:
  description: A job step.
  properties:
    action:
      type: object
      $ref: "#/types/azure-native:sql/v20210201preview:JobStepActionResponse"
      description: The action payload of the job step.
    credential:
      type: string
      description: The resource ID of the job credential that will be used to connect to the targets.
    executionOptions:
      type: object
      $ref: "#/types/azure-native:sql/v20210201preview:JobStepExecutionOptionsResponse"
      description: Execution options for the job step.
    id:
      type: string
      description: Resource ID.
    name:
      type: string
      description: Resource name.
    output:
      type: object
      $ref: "#/types/azure-native:sql/v20210201preview:JobStepOutputResponse"
      description: Output destination properties of the job step.
    stepId:
      type: integer
      description: The job step's index within the job. If not specified when creating the job step, it will be created as the last step. If not specified when updating the job step, the step id is not modified.
    targetGroup:
      type: string
      description: The resource ID of the target group that the job step will be executed on.
    type:
      type: string
      description: Resource type.
  type: object
  required:
    - action
    - credential
    - id
    - name
    - targetGroup
    - type
