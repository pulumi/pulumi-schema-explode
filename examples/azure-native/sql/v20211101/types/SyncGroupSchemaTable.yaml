$token: azure-native:sql/v20211101:SyncGroupSchemaTable
description: Properties of table in sync group schema.
properties:
  columns:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:sql/v20211101:SyncGroupSchemaTableColumn"
    description: List of columns in sync group schema.
  quotedName:
    type: string
    description: Quoted name of sync group schema table.
type: object
