$token: azure-native:sql/v20211101:FailoverGroupReadOnlyEndpoint
description: Read-only endpoint of the failover group instance.
properties:
  failoverPolicy:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:sql/v20211101:ReadOnlyEndpointFailoverPolicy"
    description: Failover policy of the read-only endpoint for the failover group.
type: object
