$token: azure-native:sql:PrivateLinkServiceConnectionStateProperty
properties:
  description:
    type: string
    description: The private link service connection description.
  status:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:sql:PrivateLinkServiceConnectionStateStatus"
    description: The private link service connection status.
type: object
required:
  - description
  - status
