$token: azure-native:sql:SyncGroupSchemaTableResponse
description: Properties of table in sync group schema.
properties:
  columns:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:sql:SyncGroupSchemaTableColumnResponse"
    description: List of columns in sync group schema.
  quotedName:
    type: string
    description: Quoted name of sync group schema table.
type: object
