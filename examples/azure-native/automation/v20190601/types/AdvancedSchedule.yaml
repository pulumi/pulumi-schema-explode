$token: azure-native:automation/v20190601:AdvancedSchedule
description: The properties of the create Advanced Schedule.
properties:
  monthDays:
    type: array
    items:
      type: integer
    description: Days of the month that the job should execute on. Must be between 1 and 31.
  monthlyOccurrences:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:automation/v20190601:AdvancedScheduleMonthlyOccurrence"
    description: Occurrences of days within a month.
  weekDays:
    type: array
    items:
      type: string
    description: Days of the week that the job should execute on.
type: object
