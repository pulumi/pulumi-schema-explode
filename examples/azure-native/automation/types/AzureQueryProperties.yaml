$token: azure-native:automation:AzureQueryProperties
description: Azure query for the update configuration.
properties:
  locations:
    type: array
    items:
      type: string
    description: List of locations to scope the query to.
  scope:
    type: array
    items:
      type: string
    description: List of Subscription or Resource Group ARM Ids.
  tagSettings:
    type: object
    $ref: "#/types/azure-native:automation:TagSettingsProperties"
    description: Tag settings for the VM.
type: object
