$token: azure-native:automation:Sku
description: The account SKU.
properties:
  capacity:
    type: integer
    description: Gets or sets the SKU capacity.
  family:
    type: string
    description: Gets or sets the SKU family.
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:automation:SkuNameEnum"
    description: Gets or sets the SKU name of the account.
type: object
required:
  - name
