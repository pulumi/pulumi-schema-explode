$token: azure-native:automation:EncryptionProperties
description: The encryption settings for automation account
properties:
  identity:
    type: object
    $ref: "#/types/azure-native:automation:EncryptionPropertiesIdentity"
    description: User identity used for CMK.
  keySource:
    $ref: "#/types/azure-native:automation:EncryptionKeySourceType"
    description: Encryption Key Source
  keyVaultProperties:
    type: object
    $ref: "#/types/azure-native:automation:KeyVaultProperties"
    description: Key vault properties.
type: object
