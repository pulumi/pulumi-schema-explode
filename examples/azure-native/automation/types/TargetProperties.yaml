$token: azure-native:automation:TargetProperties
description: Group specific to the update configuration.
properties:
  azureQueries:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:automation:AzureQueryProperties"
    description: List of Azure queries in the software update configuration.
  nonAzureQueries:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:automation:NonAzureQueryProperties"
    description: List of non Azure queries in the software update configuration.
type: object
