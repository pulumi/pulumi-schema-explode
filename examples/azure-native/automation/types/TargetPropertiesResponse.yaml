$token: azure-native:automation:TargetPropertiesResponse
description: Group specific to the update configuration.
properties:
  azureQueries:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:automation:AzureQueryPropertiesResponse"
    description: List of Azure queries in the software update configuration.
  nonAzureQueries:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:automation:NonAzureQueryPropertiesResponse"
    description: List of non Azure queries in the software update configuration.
type: object
