$token: azure-native:labservices:SizeConfigurationPropertiesResponse
description: Represents the size configuration under the lab account
properties:
  environmentSizes:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:labservices:EnvironmentSizeResponse"
    description: Represents a list of size categories supported by this Lab Account (Small, Medium, Large)
type: object
