$token: azure-native:containerregistry/v20210601preview:KeyVaultProperties
properties:
  identity:
    type: string
    description: The client id of the identity which will be used to access key vault.
  keyIdentifier:
    type: string
    description: Key vault uri to access the encryption key.
type: object
