$token: azure-native:containerregistry/v20190601preview:SourceRegistryCredentialsResponse
description: Describes the credential parameters for accessing the source registry.
properties:
  loginMode:
    type: string
    description: "The authentication mode which determines the source registry login scope. The credentials for the source registry\r

      will be generated using the given scope. These credentials will be used to login to\r

      the source registry during the run."
type: object
