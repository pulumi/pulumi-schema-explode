$token: azure-native:containerregistry:listBuildStepBuildArguments
description: |-
  The list of build arguments for a build step.
  API Version: 2018-02-01-preview.
inputs:
  properties:
    buildTaskName:
      type: string
      description: The name of the container registry build task.
    registryName:
      type: string
      description: The name of the container registry.
    resourceGroupName:
      type: string
      description: The name of the resource group to which the container registry belongs.
    stepName:
      type: string
      description: The name of a build step for a container registry build task.
  type: object
  required:
    - buildTaskName
    - registryName
    - resourceGroupName
    - stepName
outputs:
  description: The list of build arguments for a build step.
  properties:
    nextLink:
      type: string
      description: The URI that can be used to request the next set of paged results.
    value:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:containerregistry:BuildArgumentResponse"
      description: The collection value.
  type: object
