$token: azure-native:containerregistry:getRegistryCredentials
description: |-
  The result of a request to get the administrator login credentials for a container registry.
  API Version: 2016-06-27-preview.
inputs:
  properties:
    registryName:
      type: string
      description: The name of the container registry.
    resourceGroupName:
      type: string
      description: The name of the resource group to which the container registry belongs.
  type: object
  required:
    - registryName
    - resourceGroupName
outputs:
  description: The result of a request to get the administrator login credentials for a container registry.
  properties:
    password:
      type: string
      description: The administrator password.
    username:
      type: string
      description: The administrator username.
  type: object
