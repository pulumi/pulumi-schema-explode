$token: azure-native:containerregistry/v20211201preview:ExportPolicy
description: The export policy for a container registry.
properties:
  status:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:containerregistry/v20211201preview:ExportPolicyStatus"
    description: The value that indicates whether the policy is enabled or not.
    default: enabled
type: object
