$token: azure-native:containerregistry/v20211201preview:Policies
description: The policies for a container registry.
properties:
  exportPolicy:
    type: object
    $ref: "#/types/azure-native:containerregistry/v20211201preview:ExportPolicy"
    description: The export policy for a container registry.
  quarantinePolicy:
    type: object
    $ref: "#/types/azure-native:containerregistry/v20211201preview:QuarantinePolicy"
    description: The quarantine policy for a container registry.
  retentionPolicy:
    type: object
    $ref: "#/types/azure-native:containerregistry/v20211201preview:RetentionPolicy"
    description: The retention policy for a container registry.
  trustPolicy:
    type: object
    $ref: "#/types/azure-native:containerregistry/v20211201preview:TrustPolicy"
    description: The content trust policy for a container registry.
type: object
