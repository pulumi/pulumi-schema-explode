$token: azure-native:containerregistry/v20210901:QuarantinePolicy
description: The quarantine policy for a container registry.
properties:
  status:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:containerregistry/v20210901:PolicyStatus"
    description: The value that indicates whether the policy is enabled or not.
    default: disabled
type: object
