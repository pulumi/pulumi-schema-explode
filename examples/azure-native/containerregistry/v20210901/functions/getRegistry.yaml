$token: azure-native:containerregistry/v20210901:getRegistry
description: An object that represents a container registry.
inputs:
  properties:
    registryName:
      type: string
      description: The name of the container registry.
    resourceGroupName:
      type: string
      description: The name of the resource group to which the container registry belongs.
  type: object
  required:
    - registryName
    - resourceGroupName
outputs:
  description: An object that represents a container registry.
  properties:
    adminUserEnabled:
      type: boolean
      description: The value that indicates whether the admin user is enabled.
      default: false
    creationDate:
      type: string
      description: The creation date of the container registry in ISO8601 format.
    dataEndpointEnabled:
      type: boolean
      description: Enable a single data endpoint per region for serving data.
    dataEndpointHostNames:
      type: array
      items:
        type: string
      description: List of host names that will serve data when dataEndpointEnabled is true.
    encryption:
      type: object
      $ref: "#/types/azure-native:containerregistry/v20210901:EncryptionPropertyResponse"
      description: The encryption settings of container registry.
    id:
      type: string
      description: The resource ID.
    identity:
      type: object
      $ref: "#/types/azure-native:containerregistry/v20210901:IdentityPropertiesResponse"
      description: The identity of the container registry.
    location:
      type: string
      description: The location of the resource. This cannot be changed after the resource is created.
    loginServer:
      type: string
      description: The URL that can be used to log into the container registry.
    name:
      type: string
      description: The name of the resource.
    networkRuleBypassOptions:
      type: string
      description: Whether to allow trusted Azure services to access a network restricted registry.
      default: AzureServices
    networkRuleSet:
      type: object
      $ref: "#/types/azure-native:containerregistry/v20210901:NetworkRuleSetResponse"
      description: The network rule set for a container registry.
    policies:
      type: object
      $ref: "#/types/azure-native:containerregistry/v20210901:PoliciesResponse"
      description: The policies for a container registry.
    privateEndpointConnections:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:containerregistry/v20210901:PrivateEndpointConnectionResponse"
      description: List of private endpoint connections for a container registry.
    provisioningState:
      type: string
      description: The provisioning state of the container registry at the time the operation was called.
    publicNetworkAccess:
      type: string
      description: Whether or not public network access is allowed for the container registry.
      default: Enabled
    sku:
      type: object
      $ref: "#/types/azure-native:containerregistry/v20210901:SkuResponse"
      description: The SKU of the container registry.
    status:
      type: object
      $ref: "#/types/azure-native:containerregistry/v20210901:StatusResponse"
      description: The status of the container registry at the time the operation was called.
    systemData:
      type: object
      $ref: "#/types/azure-native:containerregistry/v20210901:SystemDataResponse"
      description: Metadata pertaining to creation and last modification of the resource.
    tags:
      type: object
      additionalProperties:
        type: string
      description: The tags of the resource.
    type:
      type: string
      description: The type of the resource.
    zoneRedundancy:
      type: string
      description: Whether or not zone redundancy is enabled for this container registry
      default: Disabled
  type: object
  required:
    - creationDate
    - dataEndpointHostNames
    - id
    - location
    - loginServer
    - name
    - privateEndpointConnections
    - provisioningState
    - sku
    - status
    - systemData
    - type
