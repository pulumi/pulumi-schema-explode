$token: azure-native:containerregistry/v20210901:listRegistryCredentials
description: The response from the ListCredentials operation.
inputs:
  properties:
    registryName:
      type: string
      description: The name of the container registry.
    resourceGroupName:
      type: string
      description: The name of the resource group to which the container registry belongs.
  type: object
  required:
    - registryName
    - resourceGroupName
outputs:
  description: The response from the ListCredentials operation.
  properties:
    passwords:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:containerregistry/v20210901:RegistryPasswordResponse"
      description: The list of passwords for a container registry.
    username:
      type: string
      description: The username for a container registry.
  type: object
