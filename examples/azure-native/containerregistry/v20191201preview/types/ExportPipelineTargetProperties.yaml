$token: azure-native:containerregistry/v20191201preview:ExportPipelineTargetProperties
description: The properties of the export pipeline target.
properties:
  keyVaultUri:
    type: string
    description: They key vault secret uri to obtain the target storage SAS token.
  type:
    type: string
    description: The type of target for the export pipeline.
  uri:
    type: string
    description: "The target uri of the export pipeline.\r

      When 'AzureStorageBlob': \"https://accountName.blob.core.windows.net/containerName/blobName\"\r

      When 'AzureStorageBlobContainer':  \"https://accountName.blob.core.windows.net/containerName\""
type: object
required:
  - keyVaultUri
