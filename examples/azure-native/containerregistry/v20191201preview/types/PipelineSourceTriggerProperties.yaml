$token: azure-native:containerregistry/v20191201preview:PipelineSourceTriggerProperties
properties:
  status:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:containerregistry/v20191201preview:TriggerStatus"
    description: The current status of the source trigger.
    default: Enabled
type: object
required:
  - status
