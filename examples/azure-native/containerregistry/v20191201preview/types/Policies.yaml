$token: azure-native:containerregistry/v20191201preview:Policies
description: The policies for a container registry.
properties:
  quarantinePolicy:
    type: object
    $ref: "#/types/azure-native:containerregistry/v20191201preview:QuarantinePolicy"
    description: The quarantine policy for a container registry.
  retentionPolicy:
    type: object
    $ref: "#/types/azure-native:containerregistry/v20191201preview:RetentionPolicy"
    description: The retention policy for a container registry.
  trustPolicy:
    type: object
    $ref: "#/types/azure-native:containerregistry/v20191201preview:TrustPolicy"
    description: The content trust policy for a container registry.
type: object
