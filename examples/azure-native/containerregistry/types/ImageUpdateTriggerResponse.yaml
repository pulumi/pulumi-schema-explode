$token: azure-native:containerregistry:ImageUpdateTriggerResponse
description: The image update trigger that caused a build.
properties:
  id:
    type: string
    description: The unique ID of the trigger.
  images:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:containerregistry:ImageDescriptorResponse"
    description: The list of image updates that caused the build.
  timestamp:
    type: string
    description: The timestamp when the image update happened.
type: object
