$token: azure-native:containerregistry:OverrideTaskStepPropertiesResponse
properties:
  arguments:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:containerregistry:ArgumentResponse"
    description: "Gets or sets the collection of override arguments to be used when\r

      executing a build step."
  contextPath:
    type: string
    description: The source context against which run has to be queued.
  file:
    type: string
    description: The file against which run has to be queued.
  target:
    type: string
    description: The name of the target build stage for the docker build.
  updateTriggerToken:
    type: string
    description: Base64 encoded update trigger token that will be attached with the base image trigger webhook.
  values:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:containerregistry:SetValueResponse"
    description: The collection of overridable values that can be passed when running a Task.
type: object
