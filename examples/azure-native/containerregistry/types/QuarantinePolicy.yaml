$token: azure-native:containerregistry:QuarantinePolicy
description: The quarantine policy for a container registry.
properties:
  status:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:containerregistry:PolicyStatus"
    description: The value that indicates whether the policy is enabled or not.
    default: disabled
type: object
