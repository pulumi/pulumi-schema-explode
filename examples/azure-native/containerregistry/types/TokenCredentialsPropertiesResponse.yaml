$token: azure-native:containerregistry:TokenCredentialsPropertiesResponse
description: The properties of the credentials that can be used for authenticating the token.
properties:
  certificates:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:containerregistry:TokenCertificateResponse"
  passwords:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:containerregistry:TokenPasswordResponse"
type: object
