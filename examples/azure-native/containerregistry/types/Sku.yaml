$token: azure-native:containerregistry:Sku
description: The SKU of a container registry.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:containerregistry:SkuName"
    description: The SKU name of the container registry. Required for registry creation.
type: object
required:
  - name
