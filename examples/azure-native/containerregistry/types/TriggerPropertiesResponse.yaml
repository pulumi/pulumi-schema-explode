$token: azure-native:containerregistry:TriggerPropertiesResponse
description: The properties of a trigger.
properties:
  baseImageTrigger:
    type: object
    $ref: "#/types/azure-native:containerregistry:BaseImageTriggerResponse"
    description: The trigger based on base image dependencies.
  sourceTriggers:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:containerregistry:SourceTriggerResponse"
    description: The collection of triggers based on source code repository.
  timerTriggers:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:containerregistry:TimerTriggerResponse"
    description: The collection of timer triggers.
type: object
