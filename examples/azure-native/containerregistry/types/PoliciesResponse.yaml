$token: azure-native:containerregistry:PoliciesResponse
description: The policies for a container registry.
properties:
  quarantinePolicy:
    type: object
    $ref: "#/types/azure-native:containerregistry:QuarantinePolicyResponse"
    description: The quarantine policy for a container registry.
  retentionPolicy:
    type: object
    $ref: "#/types/azure-native:containerregistry:RetentionPolicyResponse"
    description: The retention policy for a container registry.
  trustPolicy:
    type: object
    $ref: "#/types/azure-native:containerregistry:TrustPolicyResponse"
    description: The content trust policy for a container registry.
type: object
