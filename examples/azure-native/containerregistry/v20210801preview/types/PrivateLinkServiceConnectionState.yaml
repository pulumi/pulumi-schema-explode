$token: azure-native:containerregistry/v20210801preview:PrivateLinkServiceConnectionState
description: The state of a private link service connection.
properties:
  actionsRequired:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:containerregistry/v20210801preview:ActionsRequired"
    description: A message indicating if changes on the service provider require any updates on the consumer.
  description:
    type: string
    description: The description for connection status. For example if connection is rejected it can indicate reason for rejection.
  status:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:containerregistry/v20210801preview:ConnectionStatus"
    description: The private link service connection status.
type: object
