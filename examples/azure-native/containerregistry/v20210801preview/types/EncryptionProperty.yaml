$token: azure-native:containerregistry/v20210801preview:EncryptionProperty
properties:
  keyVaultProperties:
    type: object
    $ref: "#/types/azure-native:containerregistry/v20210801preview:KeyVaultProperties"
    description: Key vault properties.
  status:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:containerregistry/v20210801preview:EncryptionStatus"
    description: Indicates whether or not the encryption is enabled for container registry.
type: object
