$token: azure-native:containerregistry/v20210801preview:Sku
description: The SKU of a container registry.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:containerregistry/v20210801preview:SkuName"
    description: The SKU name of the container registry. Required for registry creation.
type: object
required:
  - name
