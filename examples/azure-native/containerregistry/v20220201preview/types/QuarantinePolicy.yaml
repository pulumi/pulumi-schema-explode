$token: azure-native:containerregistry/v20220201preview:QuarantinePolicy
description: The quarantine policy for a container registry.
properties:
  status:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:containerregistry/v20220201preview:PolicyStatus"
    description: The value that indicates whether the policy is enabled or not.
    default: disabled
type: object
