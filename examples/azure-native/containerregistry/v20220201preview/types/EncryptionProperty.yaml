$token: azure-native:containerregistry/v20220201preview:EncryptionProperty
properties:
  keyVaultProperties:
    type: object
    $ref: "#/types/azure-native:containerregistry/v20220201preview:KeyVaultProperties"
    description: Key vault properties.
  status:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:containerregistry/v20220201preview:EncryptionStatus"
    description: Indicates whether or not the encryption is enabled for container registry.
type: object
