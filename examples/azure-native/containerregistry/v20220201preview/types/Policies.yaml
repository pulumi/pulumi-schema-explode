$token: azure-native:containerregistry/v20220201preview:Policies
description: The policies for a container registry.
properties:
  azureADAuthenticationAsArmPolicy:
    type: object
    $ref: "#/types/azure-native:containerregistry/v20220201preview:AzureADAuthenticationAsArmPolicy"
    description: The policy for using ARM audience token for a container registry.
  exportPolicy:
    type: object
    $ref: "#/types/azure-native:containerregistry/v20220201preview:ExportPolicy"
    description: The export policy for a container registry.
  quarantinePolicy:
    type: object
    $ref: "#/types/azure-native:containerregistry/v20220201preview:QuarantinePolicy"
    description: The quarantine policy for a container registry.
  retentionPolicy:
    type: object
    $ref: "#/types/azure-native:containerregistry/v20220201preview:RetentionPolicy"
    description: The retention policy for a container registry.
  softDeletePolicy:
    type: object
    $ref: "#/types/azure-native:containerregistry/v20220201preview:SoftDeletePolicy"
    description: The soft delete policy for a container registry.
  trustPolicy:
    type: object
    $ref: "#/types/azure-native:containerregistry/v20220201preview:TrustPolicy"
    description: The content trust policy for a container registry.
type: object
