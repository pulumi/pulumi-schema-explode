$token: azure-native:mobilenetwork/v20220401preview:PlatformConfiguration
description: The platform where the packet core is deployed.
properties:
  azureStackEdgeDevice:
    type: object
    $ref: "#/types/azure-native:mobilenetwork/v20220401preview:AzureStackEdgeDeviceResourceId"
    description: The Azure Stack Edge device where where the packet core is deployed. If the device is part of a fault tolerant pair, either device in the pair can be specified.
  connectedCluster:
    type: object
    $ref: "#/types/azure-native:mobilenetwork/v20220401preview:ConnectedClusterResourceId"
    description: Azure Arc connected cluster where the packet core is deployed.
  customLocation:
    type: object
    $ref: "#/types/azure-native:mobilenetwork/v20220401preview:CustomLocationResourceId"
    description: Azure Arc custom location where the packet core is deployed.
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:mobilenetwork/v20220401preview:PlatformType"
    description: The platform type where packet core is deployed.
type: object
required:
  - type
