$token: azure-native:mobilenetwork/v20220401preview:PlatformConfigurationResponse
description: The platform where the packet core is deployed.
properties:
  azureStackEdgeDevice:
    type: object
    $ref: "#/types/azure-native:mobilenetwork/v20220401preview:AzureStackEdgeDeviceResourceIdResponse"
    description: The Azure Stack Edge device where where the packet core is deployed. If the device is part of a fault tolerant pair, either device in the pair can be specified.
  connectedCluster:
    type: object
    $ref: "#/types/azure-native:mobilenetwork/v20220401preview:ConnectedClusterResourceIdResponse"
    description: Azure Arc connected cluster where the packet core is deployed.
  customLocation:
    type: object
    $ref: "#/types/azure-native:mobilenetwork/v20220401preview:CustomLocationResourceIdResponse"
    description: Azure Arc custom location where the packet core is deployed.
  type:
    type: string
    description: The platform type where packet core is deployed.
type: object
required:
  - type
