$token: azure-native:mobilenetwork:listMobileNetworkSimIds
description: |-
  Response for list sim ids API service call.
  API Version: 2022-01-01-preview.
inputs:
  properties:
    mobileNetworkName:
      type: string
      description: The name of the mobile network.
    resourceGroupName:
      type: string
      description: The name of the resource group. The name is case insensitive.
  type: object
  required:
    - mobileNetworkName
    - resourceGroupName
outputs:
  description: Response for list sim ids API service call.
  properties:
    nextLink:
      type: string
      description: The URL to get the next set of results.
    value:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:mobilenetwork:SubResourceResponse"
      description: A list of sim profile ids in a resource group.
  type: object
  required:
    - nextLink
