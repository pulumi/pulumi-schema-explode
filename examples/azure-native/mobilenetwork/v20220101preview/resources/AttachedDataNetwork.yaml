$token: azure-native:mobilenetwork/v20220101preview:AttachedDataNetwork
description: Attached data network resource.
properties:
  createdAt:
    type: string
    description: The timestamp of resource creation (UTC).
  createdBy:
    type: string
    description: The identity that created the resource.
  createdByType:
    type: string
    description: The type of identity that created the resource.
  lastModifiedAt:
    type: string
    description: The timestamp of resource last modification (UTC)
  lastModifiedBy:
    type: string
    description: The identity that last modified the resource.
  lastModifiedByType:
    type: string
    description: The type of identity that last modified the resource.
  location:
    type: string
    description: The geo-location where the resource lives
  name:
    type: string
    description: The name of the resource
  naptConfiguration:
    type: object
    $ref: "#/types/azure-native:mobilenetwork/v20220101preview:NaptConfigurationResponse"
    description: |-
      The Network Address and Port Translation configuration.
      If not specified the attached data network uses a default NAPT configuration with NAPT enabled.
  provisioningState:
    type: string
    description: The provisioning state of the attached data network resource.
  tags:
    type: object
    additionalProperties:
      type: string
    description: Resource tags.
  type:
    type: string
    description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
  userEquipmentAddressPoolPrefix:
    type: array
    items:
      type: string
    description: |-
      The user equipment address pool prefixes for the attached data network that are dynamically assigned by the core to UEs when they set up a PDU session.
      At least one of userEquipmentAddressPoolPrefix and userEquipmentStaticAddressPoolPrefix must be defined. If both are defined then they must be the same size.
  userEquipmentStaticAddressPoolPrefix:
    type: array
    items:
      type: string
    description: |-
      The user equipment address pool prefixes for the attached data network that are statically assigned by the core to UEs when they set up a PDU session.
      The mapping of static IP to sim is configured in staticIpConfiguration on the sim resource.
      At least one of userEquipmentAddressPoolPrefix and userEquipmentStaticAddressPoolPrefix must be defined. If both are defined then they must be the same size.
  userPlaneDataInterface:
    type: object
    $ref: "#/types/azure-native:mobilenetwork/v20220101preview:InterfacePropertiesResponse"
    description: The user plane interface on the data network. In 5G networks this is called as N6 interface whereas in 4G networks this is called as SGi interface.
type: object
required:
  - location
  - name
  - provisioningState
  - type
  - userPlaneDataInterface
inputProperties:
  attachedDataNetworkName:
    type: string
    description: The name of the attached data network.
  createdAt:
    type: string
    description: The timestamp of resource creation (UTC).
  createdBy:
    type: string
    description: The identity that created the resource.
  createdByType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:mobilenetwork/v20220101preview:CreatedByType"
    description: The type of identity that created the resource.
  lastModifiedAt:
    type: string
    description: The timestamp of resource last modification (UTC)
  lastModifiedBy:
    type: string
    description: The identity that last modified the resource.
  lastModifiedByType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:mobilenetwork/v20220101preview:CreatedByType"
    description: The type of identity that last modified the resource.
  location:
    type: string
    description: The geo-location where the resource lives
  naptConfiguration:
    type: object
    $ref: "#/types/azure-native:mobilenetwork/v20220101preview:NaptConfiguration"
    description: |-
      The Network Address and Port Translation configuration.
      If not specified the attached data network uses a default NAPT configuration with NAPT enabled.
  packetCoreControlPlaneName:
    type: string
    description: The name of the packet core control plane.
  packetCoreDataPlaneName:
    type: string
    description: The name of the packet core data plane.
  resourceGroupName:
    type: string
    description: The name of the resource group. The name is case insensitive.
  tags:
    type: object
    additionalProperties:
      type: string
    description: Resource tags.
  userEquipmentAddressPoolPrefix:
    type: array
    items:
      type: string
    description: |-
      The user equipment address pool prefixes for the attached data network that are dynamically assigned by the core to UEs when they set up a PDU session.
      At least one of userEquipmentAddressPoolPrefix and userEquipmentStaticAddressPoolPrefix must be defined. If both are defined then they must be the same size.
  userEquipmentStaticAddressPoolPrefix:
    type: array
    items:
      type: string
    description: |-
      The user equipment address pool prefixes for the attached data network that are statically assigned by the core to UEs when they set up a PDU session.
      The mapping of static IP to sim is configured in staticIpConfiguration on the sim resource.
      At least one of userEquipmentAddressPoolPrefix and userEquipmentStaticAddressPoolPrefix must be defined. If both are defined then they must be the same size.
  userPlaneDataInterface:
    type: object
    $ref: "#/types/azure-native:mobilenetwork/v20220101preview:InterfaceProperties"
    description: The user plane interface on the data network. In 5G networks this is called as N6 interface whereas in 4G networks this is called as SGi interface.
requiredInputs:
  - packetCoreControlPlaneName
  - packetCoreDataPlaneName
  - resourceGroupName
  - userPlaneDataInterface
aliases:
  - type: azure-native:mobilenetwork:AttachedDataNetwork
  - type: azure-native:mobilenetwork/v20220301preview:AttachedDataNetwork
  - type: azure-native:mobilenetwork/v20220401preview:AttachedDataNetwork
