$token: azure-native:mobilenetwork:PacketCoreControlPlane
description: |-
  Packet core control plane resource.
  API Version: 2022-01-01-preview.
properties:
  controlPlaneAccessInterface:
    type: object
    $ref: "#/types/azure-native:mobilenetwork:InterfacePropertiesResponse"
    description: The control plane interface on the access network. In 5G networks this is called as N2 interface whereas in 4G networks this is called as S1-MME interface.
  coreNetworkTechnology:
    type: string
    description: The core network technology generation.
  createdAt:
    type: string
    description: The timestamp of resource creation (UTC).
  createdBy:
    type: string
    description: The identity that created the resource.
  createdByType:
    type: string
    description: The type of identity that created the resource.
  customLocation:
    type: object
    $ref: "#/types/azure-native:mobilenetwork:CustomLocationResourceIdResponse"
    description: Azure ARC custom location where the packet core is deployed.
  lastModifiedAt:
    type: string
    description: The timestamp of resource last modification (UTC)
  lastModifiedBy:
    type: string
    description: The identity that last modified the resource.
  lastModifiedByType:
    type: string
    description: The type of identity that last modified the resource.
  location:
    type: string
    description: The geo-location where the resource lives
  mobileNetwork:
    type: object
    $ref: "#/types/azure-native:mobilenetwork:MobileNetworkResourceIdResponse"
    description: Mobile network that this packet core control plane belongs to
  name:
    type: string
    description: The name of the resource
  provisioningState:
    type: string
    description: The provisioning state of the packet core control plane resource.
  tags:
    type: object
    additionalProperties:
      type: string
    description: Resource tags.
  type:
    type: string
    description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
  version:
    type: string
    description: The version of the packet core software that is deployed.
type: object
required:
  - controlPlaneAccessInterface
  - location
  - mobileNetwork
  - name
  - provisioningState
  - type
inputProperties:
  controlPlaneAccessInterface:
    type: object
    $ref: "#/types/azure-native:mobilenetwork:InterfaceProperties"
    description: The control plane interface on the access network. In 5G networks this is called as N2 interface whereas in 4G networks this is called as S1-MME interface.
  coreNetworkTechnology:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:mobilenetwork:CoreNetworkType"
    description: The core network technology generation.
  createdAt:
    type: string
    description: The timestamp of resource creation (UTC).
  createdBy:
    type: string
    description: The identity that created the resource.
  createdByType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:mobilenetwork:CreatedByType"
    description: The type of identity that created the resource.
  customLocation:
    type: object
    $ref: "#/types/azure-native:mobilenetwork:CustomLocationResourceId"
    description: Azure ARC custom location where the packet core is deployed.
  lastModifiedAt:
    type: string
    description: The timestamp of resource last modification (UTC)
  lastModifiedBy:
    type: string
    description: The identity that last modified the resource.
  lastModifiedByType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:mobilenetwork:CreatedByType"
    description: The type of identity that last modified the resource.
  location:
    type: string
    description: The geo-location where the resource lives
  mobileNetwork:
    type: object
    $ref: "#/types/azure-native:mobilenetwork:MobileNetworkResourceId"
    description: Mobile network that this packet core control plane belongs to
  packetCoreControlPlaneName:
    type: string
    description: The name of the packet core control plane.
  resourceGroupName:
    type: string
    description: The name of the resource group. The name is case insensitive.
  tags:
    type: object
    additionalProperties:
      type: string
    description: Resource tags.
  version:
    type: string
    description: The version of the packet core software that is deployed.
requiredInputs:
  - controlPlaneAccessInterface
  - mobileNetwork
  - resourceGroupName
aliases:
  - type: azure-native:mobilenetwork/v20220101preview:PacketCoreControlPlane
  - type: azure-native:mobilenetwork/v20220301preview:PacketCoreControlPlane
  - type: azure-native:mobilenetwork/v20220401preview:PacketCoreControlPlane
