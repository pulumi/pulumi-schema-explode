$token: azure-native:hybridcontainerservice:agentPool
description: |-
  The agentPool resource definition
  API Version: 2022-05-01-preview.
properties:
  availabilityZones:
    type: array
    items:
      type: string
    description: AvailabilityZones - The list of Availability zones to use for nodes. Datacenter racks modelled as zones
  cloudProviderProfile:
    type: object
    $ref: "#/types/azure-native:hybridcontainerservice:CloudProviderProfileResponse"
    description: The underlying cloud infra provider properties.
  count:
    type: integer
    description: Count - Number of agents to host docker containers. Allowed values must be in the range of 1 to 100 (inclusive). The default value is 1.
    default: 1
  extendedLocation:
    type: object
    $ref: "#/types/azure-native:hybridcontainerservice:AgentPoolResponseExtendedLocation"
  location:
    type: string
    description: The resource location
  maxCount:
    type: integer
    description: The maximum number of nodes for auto-scaling
  maxPods:
    type: integer
    description: The maximum number of pods that can run on a node.
  minCount:
    type: integer
    description: The minimum number of nodes for auto-scaling
  mode:
    type: string
    description: "Mode - AgentPoolMode represents mode of an agent pool. Possible values include: 'System', 'LB', 'User'. Default is 'User'"
    default: User
  name:
    type: string
    description: Resource Name
  nodeImageVersion:
    type: string
    description: The version of node image
  nodeLabels:
    type: object
    additionalProperties:
      type: string
    description: NodeLabels - Agent pool node labels to be persisted across all nodes in agent pool.
  nodeTaints:
    type: array
    items:
      type: string
    description: NodeTaints - Taints added to new nodes during node pool create and scale. For example, key=value:NoSchedule.
  osType:
    type: string
    description: "OsType - OsType to be used to specify os type. Choose from Linux and Windows. Default to Linux. Possible values include: 'Linux', 'Windows'"
    default: Linux
  provisioningState:
    type: string
  status:
    type: object
    $ref: "#/types/azure-native:hybridcontainerservice:AgentPoolProvisioningStatusResponseStatus"
    description: HybridAKSNodePoolStatus defines the observed state of HybridAKSNodePool
  systemData:
    type: object
    $ref: "#/types/azure-native:hybridcontainerservice:SystemDataResponse"
    description: Metadata pertaining to creation and last modification of the resource.
  tags:
    type: object
    additionalProperties:
      type: string
    description: Resource tags
  type:
    type: string
    description: Resource Type
  vmSize:
    type: string
    description: VmSize - The size of the agent pool VMs.
type: object
required:
  - name
  - provisioningState
  - systemData
  - type
inputProperties:
  agentPoolName:
    type: string
    description: Parameter for the name of the agent pool in the provisioned cluster
  availabilityZones:
    type: array
    items:
      type: string
    description: AvailabilityZones - The list of Availability zones to use for nodes. Datacenter racks modelled as zones
  cloudProviderProfile:
    type: object
    $ref: "#/types/azure-native:hybridcontainerservice:CloudProviderProfile"
    description: The underlying cloud infra provider properties.
  count:
    type: integer
    description: Count - Number of agents to host docker containers. Allowed values must be in the range of 1 to 100 (inclusive). The default value is 1.
    default: 1
  extendedLocation:
    type: object
    $ref: "#/types/azure-native:hybridcontainerservice:AgentPoolExtendedLocation"
  location:
    type: string
    description: The resource location
  maxCount:
    type: integer
    description: The maximum number of nodes for auto-scaling
  maxPods:
    type: integer
    description: The maximum number of pods that can run on a node.
  minCount:
    type: integer
    description: The minimum number of nodes for auto-scaling
  mode:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:hybridcontainerservice:Mode"
    description: "Mode - AgentPoolMode represents mode of an agent pool. Possible values include: 'System', 'LB', 'User'. Default is 'User'"
    default: User
  nodeImageVersion:
    type: string
    description: The version of node image
  nodeLabels:
    type: object
    additionalProperties:
      type: string
    description: NodeLabels - Agent pool node labels to be persisted across all nodes in agent pool.
  nodeTaints:
    type: array
    items:
      type: string
    description: NodeTaints - Taints added to new nodes during node pool create and scale. For example, key=value:NoSchedule.
  osType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:hybridcontainerservice:OsType"
    description: "OsType - OsType to be used to specify os type. Choose from Linux and Windows. Default to Linux. Possible values include: 'Linux', 'Windows'"
    default: Linux
  provisionedClustersName:
    type: string
    description: Parameter for the name of the provisioned cluster
  resourceGroupName:
    type: string
    description: The name of the resource group. The name is case insensitive.
  status:
    type: object
    $ref: "#/types/azure-native:hybridcontainerservice:AgentPoolProvisioningStatusStatus"
    description: HybridAKSNodePoolStatus defines the observed state of HybridAKSNodePool
  tags:
    type: object
    additionalProperties:
      type: string
    description: Resource tags
  vmSize:
    type: string
    description: VmSize - The size of the agent pool VMs.
requiredInputs:
  - provisionedClustersName
  - resourceGroupName
aliases:
  - type: azure-native:hybridcontainerservice/v20220501preview:agentPool
