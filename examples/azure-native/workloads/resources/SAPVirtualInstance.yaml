$token: azure-native:workloads:SAPVirtualInstance
description: |-
  Define the Virtual Instance for SAP solutions resource.
  API Version: 2021-12-01-preview.
properties:
  configuration:
    oneOf:
      - type: object
        $ref: "#/types/azure-native:workloads:DeploymentConfigurationResponse"
      - type: object
        $ref: "#/types/azure-native:workloads:DeploymentWithOSConfigurationResponse"
      - type: object
        $ref: "#/types/azure-native:workloads:DiscoveryConfigurationResponse"
    discriminator:
      propertyName: configurationType
      mapping:
        Deployment: "#/types/azure-native:workloads:DeploymentConfigurationResponse"
        DeploymentWithOSConfig: "#/types/azure-native:workloads:DeploymentWithOSConfigurationResponse"
        Discovery: "#/types/azure-native:workloads:DiscoveryConfigurationResponse"
    description: Defines if the SAP system is being created using Azure Center for SAP solutions (ACSS) or if an existing SAP system is being registered with ACSS
  environment:
    type: string
    description: Defines the environment type - Production/Non Production.
  errors:
    type: object
    $ref: "#/types/azure-native:workloads:SAPVirtualInstanceErrorResponse"
    description: Indicates any errors on the Virtual Instance for SAP solutions resource.
  health:
    type: string
    description: Defines the health of SAP Instances.
  identity:
    type: object
    $ref: "#/types/azure-native:workloads:UserAssignedServiceIdentityResponse"
    description: Managed service identity (user assigned identities)
  location:
    type: string
    description: The geo-location where the resource lives
  managedResourceGroupConfiguration:
    type: object
    $ref: "#/types/azure-native:workloads:ManagedRGConfigurationResponse"
    description: Managed resource group configuration
  name:
    type: string
    description: The name of the resource
  provisioningState:
    type: string
    description: Defines the provisioning states.
  sapProduct:
    type: string
    description: Defines the SAP Product type.
  state:
    type: string
    description: Defines the Virtual Instance for SAP state.
  status:
    type: string
    description: Defines the SAP Instance status.
  systemData:
    type: object
    $ref: "#/types/azure-native:workloads:SystemDataResponse"
    description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
  tags:
    type: object
    additionalProperties:
      type: string
    description: Resource tags.
  type:
    type: string
    description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
type: object
required:
  - configuration
  - environment
  - errors
  - health
  - location
  - name
  - provisioningState
  - sapProduct
  - state
  - status
  - systemData
  - type
inputProperties:
  configuration:
    oneOf:
      - type: object
        $ref: "#/types/azure-native:workloads:DeploymentConfiguration"
      - type: object
        $ref: "#/types/azure-native:workloads:DeploymentWithOSConfiguration"
      - type: object
        $ref: "#/types/azure-native:workloads:DiscoveryConfiguration"
    discriminator:
      propertyName: configurationType
      mapping:
        Deployment: "#/types/azure-native:workloads:DeploymentConfiguration"
        DeploymentWithOSConfig: "#/types/azure-native:workloads:DeploymentWithOSConfiguration"
        Discovery: "#/types/azure-native:workloads:DiscoveryConfiguration"
    description: Defines if the SAP system is being created using Azure Center for SAP solutions (ACSS) or if an existing SAP system is being registered with ACSS
  environment:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:workloads:SAPEnvironmentType"
    description: Defines the environment type - Production/Non Production.
  identity:
    type: object
    $ref: "#/types/azure-native:workloads:UserAssignedServiceIdentity"
    description: Managed service identity (user assigned identities)
  location:
    type: string
    description: The geo-location where the resource lives
  managedResourceGroupConfiguration:
    type: object
    $ref: "#/types/azure-native:workloads:ManagedRGConfiguration"
    description: Managed resource group configuration
  resourceGroupName:
    type: string
    description: The name of the resource group. The name is case insensitive.
  sapProduct:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:workloads:SAPProductType"
    description: Defines the SAP Product type.
  sapVirtualInstanceName:
    type: string
    description: The name of the Virtual Instances for SAP solutions resource
  tags:
    type: object
    additionalProperties:
      type: string
    description: Resource tags.
requiredInputs:
  - configuration
  - environment
  - resourceGroupName
  - sapProduct
aliases:
  - type: azure-native:workloads/v20211201preview:SAPVirtualInstance
