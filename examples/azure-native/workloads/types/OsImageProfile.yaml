$token: azure-native:workloads:OsImageProfile
description: OS image profile
properties:
  offer:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:workloads:OSImageOffer"
    description: OS image offer
  publisher:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:workloads:OSImagePublisher"
    description: OS image publisher
  sku:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:workloads:OSImageSku"
    description: OS image sku
  version:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:workloads:OSImageVersion"
    description: OS image version
type: object
