$token: azure-native:workloads:PhpProfile
description: PHP profile
properties:
  version:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:workloads:PHPVersion"
    description: PHP version
type: object
required:
  - version
