$token: azure-native:workloads:UserProfile
description: User profile to configure on a compute resources such as VM, VMSS
properties:
  sshPublicKey:
    type: string
    description: SSH public key data
  userName:
    type: string
    description: User name
type: object
required:
  - sshPublicKey
  - userName
