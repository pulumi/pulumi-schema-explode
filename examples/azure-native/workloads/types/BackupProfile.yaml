$token: azure-native:workloads:BackupProfile
description: Backup profile
properties:
  backupEnabled:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:workloads:EnableBackup"
    description: Whether to enable Azure backup for the workload
type: object
required:
  - backupEnabled
