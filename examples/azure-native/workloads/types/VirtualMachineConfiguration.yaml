$token: azure-native:workloads:VirtualMachineConfiguration
description: Defines the virtual machine configuration.
properties:
  imageReference:
    type: object
    $ref: "#/types/azure-native:workloads:ImageReference"
    description: The image reference.
  osProfile:
    type: object
    $ref: "#/types/azure-native:workloads:OSProfile"
    description: The OS profile.
  vmSize:
    type: string
    description: The virtual machine size.
type: object
required:
  - imageReference
  - osProfile
  - vmSize
