$token: azure-native:workloads:UserAssignedServiceIdentity
description: Managed service identity (user assigned identities)
properties:
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:workloads:ManagedServiceIdentityType"
    description: Type of manage identity
  userAssignedIdentities:
    type: object
    additionalProperties:
      $ref: pulumi.json#/Any
    description: User assigned identities dictionary
type: object
required:
  - type
