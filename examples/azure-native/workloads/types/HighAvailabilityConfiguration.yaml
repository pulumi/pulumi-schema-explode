$token: azure-native:workloads:HighAvailabilityConfiguration
description: Gets or sets the high availability configuration.
properties:
  highAvailabilityType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:workloads:SAPHighAvailabilityType"
    description: The high availability type.
type: object
required:
  - highAvailabilityType
