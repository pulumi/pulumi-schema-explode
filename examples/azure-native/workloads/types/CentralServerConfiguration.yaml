$token: azure-native:workloads:CentralServerConfiguration
description: Gets or sets the central server configuration.
properties:
  instanceCount:
    type: number
    description: The number of central server VMs.
  subnetId:
    type: string
    description: The subnet id.
  virtualMachineConfiguration:
    type: object
    $ref: "#/types/azure-native:workloads:VirtualMachineConfiguration"
    description: Gets or sets the virtual machine configuration.
type: object
required:
  - instanceCount
  - subnetId
  - virtualMachineConfiguration
