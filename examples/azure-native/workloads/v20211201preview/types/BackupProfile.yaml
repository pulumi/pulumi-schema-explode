$token: azure-native:workloads/v20211201preview:BackupProfile
description: Backup profile
properties:
  backupEnabled:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:workloads/v20211201preview:EnableBackup"
    description: Whether to enable Azure backup for the workload
type: object
required:
  - backupEnabled
