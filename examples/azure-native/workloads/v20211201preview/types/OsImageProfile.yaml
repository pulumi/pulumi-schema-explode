$token: azure-native:workloads/v20211201preview:OsImageProfile
description: OS image profile
properties:
  offer:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:workloads/v20211201preview:OSImageOffer"
    description: OS image offer
  publisher:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:workloads/v20211201preview:OSImagePublisher"
    description: OS image publisher
  sku:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:workloads/v20211201preview:OSImageSku"
    description: OS image sku
  version:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:workloads/v20211201preview:OSImageVersion"
    description: OS image version
type: object
