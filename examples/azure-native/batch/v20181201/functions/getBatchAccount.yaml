$token: azure-native:batch/v20181201:getBatchAccount
description: Contains information about an Azure Batch account.
inputs:
  properties:
    accountName:
      type: string
      description: The name of the Batch account.
    resourceGroupName:
      type: string
      description: The name of the resource group that contains the Batch account.
  type: object
  required:
    - accountName
    - resourceGroupName
outputs:
  description: Contains information about an Azure Batch account.
  properties:
    accountEndpoint:
      type: string
      description: The account endpoint used to interact with the Batch service.
    activeJobAndJobScheduleQuota:
      type: integer
    autoStorage:
      type: object
      $ref: "#/types/azure-native:batch/v20181201:AutoStoragePropertiesResponse"
      description: Contains information about the auto-storage account associated with a Batch account.
    dedicatedCoreQuota:
      type: integer
    id:
      type: string
      description: The ID of the resource.
    keyVaultReference:
      type: object
      $ref: "#/types/azure-native:batch/v20181201:KeyVaultReferenceResponse"
      description: Identifies the Azure key vault associated with a Batch account.
    location:
      type: string
      description: The location of the resource.
    lowPriorityCoreQuota:
      type: integer
    name:
      type: string
      description: The name of the resource.
    poolAllocationMode:
      type: string
      description: The allocation mode for creating pools in the Batch account.
    poolQuota:
      type: integer
    provisioningState:
      type: string
      description: The provisioned state of the resource
    tags:
      type: object
      additionalProperties:
        type: string
      description: The tags of the resource.
    type:
      type: string
      description: The type of the resource.
  type: object
  required:
    - accountEndpoint
    - activeJobAndJobScheduleQuota
    - autoStorage
    - dedicatedCoreQuota
    - id
    - keyVaultReference
    - location
    - lowPriorityCoreQuota
    - name
    - poolAllocationMode
    - poolQuota
    - provisioningState
    - tags
    - type
deprecationMessage: Version 2018-12-01 will be removed in v2 of the provider.
