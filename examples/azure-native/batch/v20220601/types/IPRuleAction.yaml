$token: azure-native:batch/v20220601:IPRuleAction
description: Action when client IP address is matched.
type: string
enum:
  - description: Allow access for the matched client IP address.
    value: Allow
