$token: azure-native:batch/v20170101:getApplication
description: Contains information about an application in a Batch account.
inputs:
  properties:
    accountName:
      type: string
      description: The name of the Batch account.
    applicationId:
      type: string
      description: The ID of the application.
    resourceGroupName:
      type: string
      description: The name of the resource group that contains the Batch account.
  type: object
  required:
    - accountName
    - applicationId
    - resourceGroupName
outputs:
  description: Contains information about an application in a Batch account.
  properties:
    allowUpdates:
      type: boolean
      description: A value indicating whether packages within the application may be overwritten using the same version string.
    defaultVersion:
      type: string
      description: The package to use if a client requests the application but does not specify a version.
    displayName:
      type: string
      description: The display name for the application.
    id:
      type: string
      description: A string that uniquely identifies the application within the account.
    packages:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:batch/v20170101:ApplicationPackageResponse"
      description: The list of packages under this application.
  type: object
deprecationMessage: Version 2017-01-01 will be removed in v2 of the provider.
