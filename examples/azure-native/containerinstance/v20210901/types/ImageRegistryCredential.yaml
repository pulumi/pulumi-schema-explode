$token: azure-native:containerinstance/v20210901:ImageRegistryCredential
description: Image registry credential.
properties:
  identity:
    type: string
    description: The identity for the private registry.
  identityUrl:
    type: string
    description: The identity URL for the private registry.
  password:
    type: string
    description: The password for the private registry.
  server:
    type: string
    description: The Docker image registry server without a protocol such as "http" and "https".
  username:
    type: string
    description: The username for the private registry.
type: object
required:
  - server
