$token: azure-native:containerinstance/v20211001:ContainerPort
description: The port exposed on the container instance.
properties:
  port:
    type: integer
    description: The port number exposed within the container group.
  protocol:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:containerinstance/v20211001:ContainerNetworkProtocol"
    description: The protocol associated with the port.
type: object
required:
  - port
