$token: azure-native:containerinstance/v20191201:ContainerHttpGet
description: The container Http Get settings, for liveness or readiness probe
properties:
  path:
    type: string
    description: The path to probe.
  port:
    type: integer
    description: The port number to probe.
  scheme:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:containerinstance/v20191201:Scheme"
    description: The scheme.
type: object
required:
  - port
