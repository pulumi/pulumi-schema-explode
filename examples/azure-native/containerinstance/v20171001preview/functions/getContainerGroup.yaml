$token: azure-native:containerinstance/v20171001preview:getContainerGroup
description: A container group.
inputs:
  properties:
    containerGroupName:
      type: string
      description: The name of the container group.
    resourceGroupName:
      type: string
      description: The name of the resource group.
  type: object
  required:
    - containerGroupName
    - resourceGroupName
outputs:
  description: A container group.
  properties:
    containers:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:containerinstance/v20171001preview:ContainerResponse"
      description: The containers within the container group.
    id:
      type: string
      description: The resource id.
    imageRegistryCredentials:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:containerinstance/v20171001preview:ImageRegistryCredentialResponse"
      description: The image registry credentials by which the container group is created from.
    instanceView:
      type: object
      $ref: "#/types/azure-native:containerinstance/v20171001preview:ContainerGroupResponseInstanceView"
      description: The instance view of the container group. Only valid in response.
    ipAddress:
      type: object
      $ref: "#/types/azure-native:containerinstance/v20171001preview:IpAddressResponse"
      description: The IP address type of the container group.
    location:
      type: string
      description: The resource location.
    name:
      type: string
      description: The resource name.
    osType:
      type: string
      description: The operating system type required by the containers in the container group.
    provisioningState:
      type: string
      description: The provisioning state of the container group. This only appears in the response.
    restartPolicy:
      type: string
      description: |
        Restart policy for all containers within the container group. 
        - `Always` Always restart
        - `OnFailure` Restart on failure
        - `Never` Never restart
    tags:
      type: object
      additionalProperties:
        type: string
      description: The resource tags.
    type:
      type: string
      description: The resource type.
    volumes:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:containerinstance/v20171001preview:VolumeResponse"
      description: The list of volumes that can be mounted by containers in this container group.
  type: object
  required:
    - containers
    - id
    - instanceView
    - location
    - name
    - osType
    - provisioningState
    - type
deprecationMessage: Version 2017-10-01-preview will be removed in v2 of the provider.
