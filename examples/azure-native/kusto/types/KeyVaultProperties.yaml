$token: azure-native:kusto:KeyVaultProperties
description: Properties of the key vault.
properties:
  keyName:
    type: string
    description: The name of the key vault key.
  keyVaultUri:
    type: string
    description: The Uri of the key vault.
  keyVersion:
    type: string
    description: The version of the key vault key.
  userIdentity:
    type: string
    description: The user assigned identity (ARM resource id) that has access to the key.
type: object
required:
  - keyName
  - keyVaultUri
