$token: azure-native:datafactory/v20180601:listIntegrationRuntimeAuthKeys
description: The integration runtime authentication keys.
inputs:
  properties:
    factoryName:
      type: string
      description: The factory name.
    integrationRuntimeName:
      type: string
      description: The integration runtime name.
    resourceGroupName:
      type: string
      description: The resource group name.
  type: object
  required:
    - factoryName
    - integrationRuntimeName
    - resourceGroupName
outputs:
  description: The integration runtime authentication keys.
  properties:
    authKey1:
      type: string
      description: The primary integration runtime authentication key.
    authKey2:
      type: string
      description: The secondary integration runtime authentication key.
  type: object
