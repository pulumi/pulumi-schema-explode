$token: azure-native:datafactory/v20180601:ParquetFormat
description: The data stored in Parquet format.
properties:
  deserializer:
    $ref: pulumi.json#/Any
    description: "Deserializer. Type: string (or Expression with resultType string)."
  serializer:
    $ref: pulumi.json#/Any
    description: "Serializer. Type: string (or Expression with resultType string)."
  type:
    type: string
    description: |-
      Type of dataset storage format.
      Expected value is 'ParquetFormat'.
    const: ParquetFormat
type: object
required:
  - type
