$token: azure-native:datafactory:TriggerPipelineReference
description: Pipeline that needs to be triggered with the given parameters.
properties:
  parameters:
    type: object
    additionalProperties:
      $ref: pulumi.json#/Any
    description: Pipeline parameters.
  pipelineReference:
    type: object
    $ref: "#/types/azure-native:datafactory:PipelineReference"
    description: Pipeline reference.
type: object
