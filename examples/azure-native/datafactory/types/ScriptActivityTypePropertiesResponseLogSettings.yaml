$token: azure-native:datafactory:ScriptActivityTypePropertiesResponseLogSettings
description: Log settings of script activity.
properties:
  logDestination:
    type: string
    description: "The destination of logs. Type: string."
  logLocationSettings:
    type: object
    $ref: "#/types/azure-native:datafactory:LogLocationSettingsResponse"
    description: Log location settings customer needs to provide when enabling log.
type: object
required:
  - logDestination
