$token: azure-native:datafactory:ODataAadServicePrincipalCredentialType
description: Specify the credential type (key or cert) is used for service principal.
type: string
enum:
  - value: ServicePrincipalKey
  - value: ServicePrincipalCert
