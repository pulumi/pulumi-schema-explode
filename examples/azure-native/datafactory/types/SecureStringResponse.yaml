$token: azure-native:datafactory:SecureStringResponse
description: Azure Data Factory secure string definition. The string value will be masked with asterisks '*' during Get or List API calls.
properties:
  type:
    type: string
    description: |-
      Type of the secret.
      Expected value is 'SecureString'.
    const: SecureString
  value:
    type: string
    description: Value of secure string.
type: object
required:
  - type
  - value
