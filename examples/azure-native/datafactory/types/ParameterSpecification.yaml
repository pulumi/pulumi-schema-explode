$token: azure-native:datafactory:ParameterSpecification
description: Definition of a single parameter for an entity.
properties:
  defaultValue:
    $ref: pulumi.json#/Any
    description: Default value of parameter.
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:datafactory:ParameterType"
    description: Parameter type.
type: object
required:
  - type
