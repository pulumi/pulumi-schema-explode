$token: azure-native:datafactory:PowerQuerySinkMapping
description: Map Power Query mashup query to sink dataset(s).
properties:
  dataflowSinks:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:datafactory:PowerQuerySink"
    description: List of sinks mapped to Power Query mashup query.
  queryName:
    type: string
    description: Name of the query in Power Query mashup document.
type: object
