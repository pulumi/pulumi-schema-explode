$token: azure-native:datafactory:AzureDataLakeStoreLocation
description: The location of azure data lake store dataset.
properties:
  fileName:
    $ref: pulumi.json#/Any
    description: "Specify the file name of dataset. Type: string (or Expression with resultType string)."
  folderPath:
    $ref: pulumi.json#/Any
    description: "Specify the folder path of dataset. Type: string (or Expression with resultType string)"
  type:
    type: string
    description: |-
      Type of dataset storage location.
      Expected value is 'AzureDataLakeStoreLocation'.
    const: AzureDataLakeStoreLocation
type: object
required:
  - type
