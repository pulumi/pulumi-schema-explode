$token: azure-native:datafactory:PrivateLinkConnectionApprovalRequest
description: A request to approve or reject a private endpoint connection
properties:
  privateEndpoint:
    type: object
    $ref: "#/types/azure-native:datafactory:PrivateEndpoint"
    description: The resource of private endpoint.
  privateLinkServiceConnectionState:
    type: object
    $ref: "#/types/azure-native:datafactory:PrivateLinkConnectionState"
    description: The state of a private link connection
type: object
