$token: azure-native:datafactory:StoredProcedureParameter
description: SQL stored procedure parameter.
properties:
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:datafactory:StoredProcedureParameterType"
    description: Stored procedure parameter type.
  value:
    $ref: pulumi.json#/Any
    description: "Stored procedure parameter value. Type: string (or Expression with resultType string)."
type: object
