$token: azure-native:datafactory:TriggerReference
description: Trigger reference type.
properties:
  referenceName:
    type: string
    description: Reference trigger name.
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:datafactory:TriggerReferenceType"
    description: Trigger reference type.
type: object
required:
  - referenceName
  - type
