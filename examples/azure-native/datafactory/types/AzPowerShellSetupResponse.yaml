$token: azure-native:datafactory:AzPowerShellSetupResponse
description: The express custom setup of installing Azure PowerShell.
properties:
  type:
    type: string
    description: |-
      The type of custom setup.
      Expected value is 'AzPowerShellSetup'.
    const: AzPowerShellSetup
  version:
    type: string
    description: The required version of Azure PowerShell to install.
type: object
required:
  - type
  - version
