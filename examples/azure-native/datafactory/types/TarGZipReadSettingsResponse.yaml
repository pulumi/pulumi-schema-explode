$token: azure-native:datafactory:TarGZipReadSettingsResponse
description: The TarGZip compression read settings.
properties:
  preserveCompressionFileNameAsFolder:
    $ref: pulumi.json#/Any
    description: "Preserve the compression file name as folder path. Type: boolean (or Expression with resultType boolean)."
  type:
    type: string
    description: |-
      The Compression setting type.
      Expected value is 'TarGZipReadSettings'.
    const: TarGZipReadSettings
type: object
required:
  - type
