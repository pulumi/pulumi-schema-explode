$token: azure-native:datafactory:CredentialReference
description: Credential reference type.
properties:
  referenceName:
    type: string
    description: Reference credential name.
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:datafactory:CredentialReferenceType"
    description: Credential reference type.
type: object
required:
  - referenceName
  - type
