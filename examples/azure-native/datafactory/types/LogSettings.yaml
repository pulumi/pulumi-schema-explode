$token: azure-native:datafactory:LogSettings
description: Log settings.
properties:
  copyActivityLogSettings:
    type: object
    $ref: "#/types/azure-native:datafactory:CopyActivityLogSettings"
    description: Specifies settings for copy activity log.
  enableCopyActivityLog:
    $ref: pulumi.json#/Any
    description: "Specifies whether to enable copy activity log. Type: boolean (or Expression with resultType boolean)."
  logLocationSettings:
    type: object
    $ref: "#/types/azure-native:datafactory:LogLocationSettings"
    description: Log location settings customer needs to provide when enabling log.
type: object
required:
  - logLocationSettings
