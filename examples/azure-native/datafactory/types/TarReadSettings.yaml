$token: azure-native:datafactory:TarReadSettings
description: The Tar compression read settings.
properties:
  preserveCompressionFileNameAsFolder:
    $ref: pulumi.json#/Any
    description: "Preserve the compression file name as folder path. Type: boolean (or Expression with resultType boolean)."
  type:
    type: string
    description: |-
      The Compression setting type.
      Expected value is 'TarReadSettings'.
    const: TarReadSettings
type: object
required:
  - type
