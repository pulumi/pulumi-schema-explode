$token: azure-native:datafactory:ScriptActivityScriptBlockResponse
description: Script block of scripts.
properties:
  parameters:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:datafactory:ScriptActivityParameterResponse"
    description: "Array of script parameters. Type: array."
  text:
    $ref: pulumi.json#/Any
    description: "The query text. Type: string (or Expression with resultType string)."
  type:
    type: string
    description: "The type of the query. Type: string."
type: object
required:
  - text
  - type
