$token: azure-native:datafactory:AvroFormat
description: The data stored in Avro format.
properties:
  deserializer:
    $ref: pulumi.json#/Any
    description: "Deserializer. Type: string (or Expression with resultType string)."
  serializer:
    $ref: pulumi.json#/Any
    description: "Serializer. Type: string (or Expression with resultType string)."
  type:
    type: string
    description: |-
      Type of dataset storage format.
      Expected value is 'AvroFormat'.
    const: AvroFormat
type: object
required:
  - type
