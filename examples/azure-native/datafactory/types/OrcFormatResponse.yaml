$token: azure-native:datafactory:OrcFormatResponse
description: The data stored in Optimized Row Columnar (ORC) format.
properties:
  deserializer:
    $ref: pulumi.json#/Any
    description: "Deserializer. Type: string (or Expression with resultType string)."
  serializer:
    $ref: pulumi.json#/Any
    description: "Serializer. Type: string (or Expression with resultType string)."
  type:
    type: string
    description: |-
      Type of dataset storage format.
      Expected value is 'OrcFormat'.
    const: OrcFormat
type: object
required:
  - type
