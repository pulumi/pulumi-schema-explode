$token: azure-native:datafactory/v20170901preview:ExecutePipelineActivity
description: Execute pipeline activity.
properties:
  dependsOn:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:datafactory/v20170901preview:ActivityDependency"
    description: Activity depends on condition.
  description:
    type: string
    description: Activity description.
  name:
    type: string
    description: Activity name.
  parameters:
    type: object
    additionalProperties:
      $ref: pulumi.json#/Any
    description: Pipeline parameters.
  pipeline:
    type: object
    $ref: "#/types/azure-native:datafactory/v20170901preview:PipelineReference"
    description: Pipeline reference.
  type:
    type: string
    description: |-
      Type of activity.
      Expected value is 'ExecutePipeline'.
    const: ExecutePipeline
  waitOnCompletion:
    type: boolean
    description: Defines whether activity execution will wait for the dependent pipeline execution to finish. Default is false.
type: object
required:
  - name
  - pipeline
  - type
