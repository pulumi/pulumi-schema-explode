$token: azure-native:datafactory/v20170901preview:ScheduleTriggerResponse
description: Trigger that creates pipeline runs periodically, on schedule.
properties:
  description:
    type: string
    description: Trigger description.
  pipelines:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:datafactory/v20170901preview:TriggerPipelineReferenceResponse"
    description: Pipelines that need to be started.
  recurrence:
    type: object
    $ref: "#/types/azure-native:datafactory/v20170901preview:ScheduleTriggerRecurrenceResponse"
    description: Recurrence schedule configuration.
  runtimeState:
    type: string
    description: Indicates if trigger is running or not. Updated when Start/Stop APIs are called on the Trigger.
  type:
    type: string
    description: |-
      Trigger type.
      Expected value is 'ScheduleTrigger'.
    const: ScheduleTrigger
type: object
required:
  - recurrence
  - runtimeState
  - type
