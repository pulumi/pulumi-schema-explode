$token: azure-native:datafactory/v20170901preview:MultiplePipelineTriggerResponse
description: Base class for all triggers that support one to many model for trigger to pipeline.
properties:
  description:
    type: string
    description: Trigger description.
  pipelines:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:datafactory/v20170901preview:TriggerPipelineReferenceResponse"
    description: Pipelines that need to be started.
  runtimeState:
    type: string
    description: Indicates if trigger is running or not. Updated when Start/Stop APIs are called on the Trigger.
  type:
    type: string
    description: |-
      Trigger type.
      Expected value is 'MultiplePipelineTrigger'.
    const: MultiplePipelineTrigger
type: object
required:
  - runtimeState
  - type
