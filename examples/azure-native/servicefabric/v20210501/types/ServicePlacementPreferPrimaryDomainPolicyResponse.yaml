$token: azure-native:servicefabric/v20210501:ServicePlacementPreferPrimaryDomainPolicyResponse
properties:
  domainName:
    type: string
    description: The name of the domain that should used for placement as per this policy.
  type:
    type: string
    description: |-
      The type of placement policy for a service fabric service. Following are the possible values.
      Expected value is 'PreferredPrimaryDomain'.
    const: PreferredPrimaryDomain
type: object
required:
  - domainName
  - type
