$token: azure-native:servicefabric:ApplicationTypeVersionsCleanupPolicy
properties:
  maxUnusedVersionsToKeep:
    type: number
    description: Number of unused versions per application type to keep.
type: object
required:
  - maxUnusedVersionsToKeep
