$token: azure-native:servicefabric/v20210701preview:ServicePlacementNonPartiallyPlaceServicePolicyResponse
description: The name of the domain that should used for placement as per this policy.
properties:
  type:
    type: string
    description: |-
      The type of placement policy for a service fabric service. Following are the possible values.
      Expected value is 'NonPartiallyPlaceService'.
    const: NonPartiallyPlaceService
type: object
required:
  - type
