$token: azure-native:servicefabric:getmanagedAzResiliencyStatus
description: |-
  Describes the result of the request to list Managed VM Sizes for Service Fabric Managed Clusters.
  API Version: 2022-02-01-preview.
inputs:
  properties:
    clusterName:
      type: string
      description: The name of the cluster resource.
    resourceGroupName:
      type: string
      description: The name of the resource group.
  type: object
  required:
    - clusterName
    - resourceGroupName
outputs:
  description: Describes the result of the request to list Managed VM Sizes for Service Fabric Managed Clusters.
  properties:
    baseResourceStatus:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:servicefabric:ResourceAzStatusResponse"
      description: List of Managed VM Sizes for Service Fabric Managed Clusters.
    isClusterZoneResilient:
      type: boolean
      description: URL to get the next set of Managed VM Sizes if there are any.
  type: object
  required:
    - isClusterZoneResilient
