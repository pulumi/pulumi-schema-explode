$token: azure-native:servicefabric/v20220101:NamedPartitionScheme
description: Describes the named partition scheme of the service.
properties:
  names:
    type: array
    items:
      type: string
    description: Array for the names of the partitions.
  partitionScheme:
    type: string
    description: |-
      Enumerates the ways that a service can be partitioned.
      Expected value is 'Named'.
    const: Named
type: object
required:
  - names
  - partitionScheme
