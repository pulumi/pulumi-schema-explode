$token: azure-native:servicefabric/v20210601:listClusterUpgradableVersions
description: The list of intermediate cluster code versions for an upgrade or downgrade. Or minimum and maximum upgradable version if no target was given
inputs:
  properties:
    clusterName:
      type: string
      description: The name of the cluster resource.
    resourceGroupName:
      type: string
      description: The name of the resource group.
    targetVersion:
      type: string
      description: The target code version.
  type: object
  required:
    - clusterName
    - resourceGroupName
    - targetVersion
outputs:
  description: The list of intermediate cluster code versions for an upgrade or downgrade. Or minimum and maximum upgradable version if no target was given
  properties:
    supportedPath:
      type: array
      items:
        type: string
  type: object
