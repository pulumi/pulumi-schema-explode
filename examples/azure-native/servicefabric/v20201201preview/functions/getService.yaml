$token: azure-native:servicefabric/v20201201preview:getService
description: The service resource.
inputs:
  properties:
    applicationName:
      type: string
      description: The name of the application resource.
    clusterName:
      type: string
      description: The name of the cluster resource.
    resourceGroupName:
      type: string
      description: The name of the resource group.
    serviceName:
      type: string
      description: The name of the service resource in the format of {applicationName}~{serviceName}.
  type: object
  required:
    - applicationName
    - clusterName
    - resourceGroupName
    - serviceName
outputs:
  description: The service resource.
  properties:
    correlationScheme:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:servicefabric/v20201201preview:ServiceCorrelationDescriptionResponse"
      description: A list that describes the correlation of the service with other services.
    defaultMoveCost:
      type: string
      description: Specifies the move cost for the service.
    etag:
      type: string
      description: Azure resource etag.
    id:
      type: string
      description: Azure resource identifier.
    location:
      type: string
      description: It will be deprecated in New API, resource location depends on the parent resource.
    name:
      type: string
      description: Azure resource name.
    partitionDescription:
      oneOf:
        - type: object
          $ref: "#/types/azure-native:servicefabric/v20201201preview:NamedPartitionSchemeDescriptionResponse"
        - type: object
          $ref: "#/types/azure-native:servicefabric/v20201201preview:SingletonPartitionSchemeDescriptionResponse"
        - type: object
          $ref: "#/types/azure-native:servicefabric/v20201201preview:UniformInt64RangePartitionSchemeDescriptionResponse"
      discriminator:
        propertyName: partitionScheme
        mapping:
          Named: "#/types/azure-native:servicefabric/v20201201preview:NamedPartitionSchemeDescriptionResponse"
          Singleton: "#/types/azure-native:servicefabric/v20201201preview:SingletonPartitionSchemeDescriptionResponse"
          UniformInt64Range: "#/types/azure-native:servicefabric/v20201201preview:UniformInt64RangePartitionSchemeDescriptionResponse"
      description: Describes how the service is partitioned.
    placementConstraints:
      type: string
      description: 'The placement constraints as a string. Placement constraints are boolean expressions on node properties and allow for restricting a service to particular nodes based on the service requirements. For example, to place a service on nodes where NodeType is blue specify the following: "NodeColor == blue)".'
    provisioningState:
      type: string
      description: The current deployment or provisioning state, which only appears in the response
    serviceDnsName:
      type: string
      description: Dns name used for the service. If this is specified, then the service can be accessed via its DNS name instead of service name.
    serviceKind:
      type: string
      description: The kind of service (Stateless or Stateful).
    serviceLoadMetrics:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:servicefabric/v20201201preview:ServiceLoadMetricDescriptionResponse"
      description: The service load metrics is given as an array of ServiceLoadMetricDescription objects.
    servicePackageActivationMode:
      type: string
      description: The activation Mode of the service package
    servicePlacementPolicies:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:servicefabric/v20201201preview:ServicePlacementPolicyDescriptionResponse"
      description: A list that describes the correlation of the service with other services.
    serviceTypeName:
      type: string
      description: The name of the service type
    systemData:
      type: object
      $ref: "#/types/azure-native:servicefabric/v20201201preview:SystemDataResponse"
      description: Metadata pertaining to creation and last modification of the resource.
    tags:
      type: object
      additionalProperties:
        type: string
      description: Azure resource tags.
    type:
      type: string
      description: Azure resource type.
  type: object
  required:
    - etag
    - id
    - name
    - provisioningState
    - serviceKind
    - systemData
    - type
