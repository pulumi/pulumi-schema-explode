$token: azure-native:devops:BootstrapConfigurationResponse
description: Configuration used to bootstrap a Pipeline.
properties:
  sourceRepository:
    type: object
    $ref: "#/types/azure-native:devops:CodeRepositoryResponse"
    description: Repository containing the source code for the pipeline. Currently only 'azurePipeline' pipeline type supports this.
  template:
    type: object
    $ref: "#/types/azure-native:devops:PipelineTemplateResponse"
    description: Template used to bootstrap the pipeline.
type: object
required:
  - template
