$token: azure-native:devops/v20200713preview:BootstrapConfiguration
description: Configuration used to bootstrap a Pipeline.
properties:
  sourceRepository:
    type: object
    $ref: "#/types/azure-native:devops/v20200713preview:CodeRepository"
    description: Repository containing the source code for the pipeline. Currently only 'azurePipeline' pipeline type supports this.
  template:
    type: object
    $ref: "#/types/azure-native:devops/v20200713preview:PipelineTemplate"
    description: Template used to bootstrap the pipeline.
type: object
required:
  - template
