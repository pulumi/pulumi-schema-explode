$token: azure-native:devops/v20200713preview:BootstrapConfigurationResponse
description: Configuration used to bootstrap a Pipeline.
properties:
  sourceRepository:
    type: object
    $ref: "#/types/azure-native:devops/v20200713preview:CodeRepositoryResponse"
    description: Repository containing the source code for the pipeline. Currently only 'azurePipeline' pipeline type supports this.
  template:
    type: object
    $ref: "#/types/azure-native:devops/v20200713preview:PipelineTemplateResponse"
    description: Template used to bootstrap the pipeline.
type: object
required:
  - template
