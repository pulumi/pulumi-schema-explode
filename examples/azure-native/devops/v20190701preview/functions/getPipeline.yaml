$token: azure-native:devops/v20190701preview:getPipeline
description: Azure DevOps Pipeline used to configure Continuous Integration (CI) & Continuous Delivery (CD) for Azure resources.
inputs:
  properties:
    pipelineName:
      type: string
      description: The name of the Azure Pipeline resource in ARM.
    resourceGroupName:
      type: string
      description: Name of the resource group within the Azure subscription.
  type: object
  required:
    - pipelineName
    - resourceGroupName
outputs:
  description: Azure DevOps Pipeline used to configure Continuous Integration (CI) & Continuous Delivery (CD) for Azure resources.
  properties:
    bootstrapConfiguration:
      type: object
      $ref: "#/types/azure-native:devops/v20190701preview:BootstrapConfigurationResponse"
      description: Configuration used to bootstrap the Pipeline.
    id:
      type: string
      description: Resource Id
    location:
      type: string
      description: Resource Location
    name:
      type: string
      description: Resource Name
    organization:
      type: object
      $ref: "#/types/azure-native:devops/v20190701preview:OrganizationReferenceResponse"
      description: Reference to the Azure DevOps Organization containing the Pipeline.
    pipelineId:
      type: integer
      description: Unique identifier of the Azure Pipeline within the Azure DevOps Project.
    project:
      type: object
      $ref: "#/types/azure-native:devops/v20190701preview:ProjectReferenceResponse"
      description: Reference to the Azure DevOps Project containing the Pipeline.
    tags:
      type: object
      additionalProperties:
        type: string
      description: Resource Tags
    type:
      type: string
      description: Resource Type
  type: object
  required:
    - bootstrapConfiguration
    - id
    - name
    - organization
    - pipelineId
    - project
    - type
deprecationMessage: Version 2019-07-01-preview will be removed in v2 of the provider.
