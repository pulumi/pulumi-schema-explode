$token: azure-native:devops/v20190701preview:AuthorizationResponse
description: Authorization info used to access a resource (like code repository).
properties:
  authorizationType:
    type: string
    description: Type of authorization.
  parameters:
    type: object
    additionalProperties:
      type: string
    description: Authorization parameters corresponding to the authorization type.
type: object
required:
  - authorizationType
