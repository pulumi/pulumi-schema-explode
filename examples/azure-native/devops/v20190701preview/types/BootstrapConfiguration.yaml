$token: azure-native:devops/v20190701preview:BootstrapConfiguration
description: Configuration used to bootstrap a Pipeline.
properties:
  repository:
    type: object
    $ref: "#/types/azure-native:devops/v20190701preview:CodeRepository"
    description: Repository containing the source code for the pipeline.
  template:
    type: object
    $ref: "#/types/azure-native:devops/v20190701preview:PipelineTemplate"
    description: Template used to bootstrap the pipeline.
type: object
required:
  - template
