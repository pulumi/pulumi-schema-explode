$token: azure-native:scheduler:JobProperties
properties:
  action:
    type: object
    $ref: "#/types/azure-native:scheduler:JobAction"
    description: Gets or sets the job action.
  recurrence:
    type: object
    $ref: "#/types/azure-native:scheduler:JobRecurrence"
    description: Gets or sets the job recurrence.
  startTime:
    type: string
    description: Gets or sets the job start time.
  state:
    $ref: "#/types/azure-native:scheduler:JobState"
    description: Gets or set the job state.
type: object
