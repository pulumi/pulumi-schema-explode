$token: azure-native:scheduler:JobCollectionProperties
properties:
  quota:
    type: object
    $ref: "#/types/azure-native:scheduler:JobCollectionQuota"
    description: Gets or sets the job collection quota.
  sku:
    type: object
    $ref: "#/types/azure-native:scheduler:Sku"
    description: Gets or sets the SKU.
  state:
    $ref: "#/types/azure-native:scheduler:JobCollectionState"
    description: Gets or sets the state.
type: object
