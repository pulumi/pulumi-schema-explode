$token: azure-native:scheduler:JobCollectionPropertiesResponse
properties:
  quota:
    type: object
    $ref: "#/types/azure-native:scheduler:JobCollectionQuotaResponse"
    description: Gets or sets the job collection quota.
  sku:
    type: object
    $ref: "#/types/azure-native:scheduler:SkuResponse"
    description: Gets or sets the SKU.
  state:
    type: string
    description: Gets or sets the state.
type: object
