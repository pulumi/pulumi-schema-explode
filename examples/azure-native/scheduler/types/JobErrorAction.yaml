$token: azure-native:scheduler:JobErrorAction
properties:
  queueMessage:
    type: object
    $ref: "#/types/azure-native:scheduler:StorageQueueMessage"
    description: Gets or sets the storage queue message.
  request:
    type: object
    $ref: "#/types/azure-native:scheduler:HttpRequest"
    description: Gets or sets the http requests.
  retryPolicy:
    type: object
    $ref: "#/types/azure-native:scheduler:RetryPolicy"
    description: Gets or sets the retry policy.
  serviceBusQueueMessage:
    type: object
    $ref: "#/types/azure-native:scheduler:ServiceBusQueueMessage"
    description: Gets or sets the service bus queue message.
  serviceBusTopicMessage:
    type: object
    $ref: "#/types/azure-native:scheduler:ServiceBusTopicMessage"
    description: Gets or sets the service bus topic message.
  type:
    $ref: "#/types/azure-native:scheduler:JobActionType"
    description: Gets or sets the job error action type.
type: object
