$token: azure-native:scheduler:JobErrorActionResponse
properties:
  queueMessage:
    type: object
    $ref: "#/types/azure-native:scheduler:StorageQueueMessageResponse"
    description: Gets or sets the storage queue message.
  request:
    type: object
    $ref: "#/types/azure-native:scheduler:HttpRequestResponse"
    description: Gets or sets the http requests.
  retryPolicy:
    type: object
    $ref: "#/types/azure-native:scheduler:RetryPolicyResponse"
    description: Gets or sets the retry policy.
  serviceBusQueueMessage:
    type: object
    $ref: "#/types/azure-native:scheduler:ServiceBusQueueMessageResponse"
    description: Gets or sets the service bus queue message.
  serviceBusTopicMessage:
    type: object
    $ref: "#/types/azure-native:scheduler:ServiceBusTopicMessageResponse"
    description: Gets or sets the service bus topic message.
  type:
    type: string
    description: Gets or sets the job error action type.
type: object
