$token: azure-native:scheduler/v20160301:JobProperties
properties:
  action:
    type: object
    $ref: "#/types/azure-native:scheduler/v20160301:JobAction"
    description: Gets or sets the job action.
  recurrence:
    type: object
    $ref: "#/types/azure-native:scheduler/v20160301:JobRecurrence"
    description: Gets or sets the job recurrence.
  startTime:
    type: string
    description: Gets or sets the job start time.
  state:
    $ref: "#/types/azure-native:scheduler/v20160301:JobState"
    description: Gets or set the job state.
type: object
