$token: azure-native:scheduler/v20140801preview:getJobCollection
inputs:
  properties:
    jobCollectionName:
      type: string
      description: The job collection name.
    resourceGroupName:
      type: string
      description: The resource group name.
  type: object
  required:
    - jobCollectionName
    - resourceGroupName
outputs:
  properties:
    id:
      type: string
      description: Gets the job collection resource identifier.
    location:
      type: string
      description: Gets or sets the storage account location.
    name:
      type: string
      description: Gets or sets the job collection resource name.
    properties:
      type: object
      $ref: "#/types/azure-native:scheduler/v20140801preview:JobCollectionPropertiesResponse"
      description: Gets or sets the job collection properties.
    tags:
      type: object
      additionalProperties:
        type: string
      description: Gets or sets the tags.
    type:
      type: string
      description: Gets the job collection resource type.
  type: object
  required:
    - id
    - properties
    - type
deprecationMessage: Version 2014-08-01-preview will be removed in v2 of the provider.
