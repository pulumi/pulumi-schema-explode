$token: azure-native:scheduler/v20140801preview:RetryPolicy
properties:
  retryCount:
    type: integer
    description: Gets or sets the number of times a retry should be attempted.
  retryInterval:
    type: string
    description: Gets or sets the retry interval between retries.
  retryType:
    $ref: "#/types/azure-native:scheduler/v20140801preview:RetryType"
    description: Gets or sets the retry strategy to be used.
type: object
