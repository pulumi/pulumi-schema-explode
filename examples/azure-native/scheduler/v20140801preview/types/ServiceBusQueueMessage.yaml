$token: azure-native:scheduler/v20140801preview:ServiceBusQueueMessage
properties:
  authentication:
    type: object
    $ref: "#/types/azure-native:scheduler/v20140801preview:ServiceBusAuthentication"
    description: Gets or sets the authentication.
  brokeredMessageProperties:
    type: object
    $ref: "#/types/azure-native:scheduler/v20140801preview:ServiceBusBrokeredMessageProperties"
    description: Gets or sets the brokered message properties.
  customMessageProperties:
    type: object
    additionalProperties:
      type: string
    description: Gets or sets the custom message properties.
  message:
    type: string
    description: Gets or sets the message.
  namespace:
    type: string
    description: Gets or sets the namespace.
  queueName:
    type: string
    description: Gets or sets the queue name.
  transportType:
    $ref: "#/types/azure-native:scheduler/v20140801preview:ServiceBusTransportType"
    description: Gets or sets the transport type.
type: object
