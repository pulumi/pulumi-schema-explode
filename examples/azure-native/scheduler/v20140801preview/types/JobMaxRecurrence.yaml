$token: azure-native:scheduler/v20140801preview:JobMaxRecurrence
properties:
  frequency:
    $ref: "#/types/azure-native:scheduler/v20140801preview:RecurrenceFrequency"
    description: Gets or sets the frequency of recurrence (second, minute, hour, day, week, month).
  interval:
    type: integer
    description: Gets or sets the interval between retries.
type: object
