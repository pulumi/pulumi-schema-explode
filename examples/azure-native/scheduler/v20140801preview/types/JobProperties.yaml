$token: azure-native:scheduler/v20140801preview:JobProperties
properties:
  action:
    type: object
    $ref: "#/types/azure-native:scheduler/v20140801preview:JobAction"
    description: Gets or sets the job action.
  recurrence:
    type: object
    $ref: "#/types/azure-native:scheduler/v20140801preview:JobRecurrence"
    description: Gets or sets the job recurrence.
  startTime:
    type: string
    description: Gets or sets the job start time.
  state:
    $ref: "#/types/azure-native:scheduler/v20140801preview:JobState"
    description: Gets or set the job state.
type: object
