$token: azure-native:scheduler/v20140801preview:JobErrorAction
properties:
  queueMessage:
    type: object
    $ref: "#/types/azure-native:scheduler/v20140801preview:StorageQueueMessage"
    description: Gets or sets the storage queue message.
  request:
    type: object
    $ref: "#/types/azure-native:scheduler/v20140801preview:HttpRequest"
    description: Gets or sets the http requests.
  retryPolicy:
    type: object
    $ref: "#/types/azure-native:scheduler/v20140801preview:RetryPolicy"
    description: Gets or sets the retry policy.
  serviceBusQueueMessage:
    type: object
    $ref: "#/types/azure-native:scheduler/v20140801preview:ServiceBusQueueMessage"
    description: Gets or sets the service bus queue message.
  serviceBusTopicMessage:
    type: object
    $ref: "#/types/azure-native:scheduler/v20140801preview:ServiceBusTopicMessage"
    description: Gets or sets the service bus topic message.
  type:
    $ref: "#/types/azure-native:scheduler/v20140801preview:JobActionType"
    description: Gets or sets the job error action type.
type: object
