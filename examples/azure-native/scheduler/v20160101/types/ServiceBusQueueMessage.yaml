$token: azure-native:scheduler/v20160101:ServiceBusQueueMessage
properties:
  authentication:
    type: object
    $ref: "#/types/azure-native:scheduler/v20160101:ServiceBusAuthentication"
    description: Gets or sets the authentication.
  brokeredMessageProperties:
    type: object
    $ref: "#/types/azure-native:scheduler/v20160101:ServiceBusBrokeredMessageProperties"
    description: Gets or sets the brokered message properties.
  customMessageProperties:
    type: object
    additionalProperties:
      type: string
    description: Gets or sets the custom message properties.
  message:
    type: string
    description: Gets or sets the message.
  namespace:
    type: string
    description: Gets or sets the namespace.
  queueName:
    type: string
    description: Gets or sets the queue name.
  transportType:
    $ref: "#/types/azure-native:scheduler/v20160101:ServiceBusTransportType"
    description: Gets or sets the transport type.
type: object
