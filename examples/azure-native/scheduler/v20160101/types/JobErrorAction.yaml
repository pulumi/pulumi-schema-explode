$token: azure-native:scheduler/v20160101:JobErrorAction
properties:
  queueMessage:
    type: object
    $ref: "#/types/azure-native:scheduler/v20160101:StorageQueueMessage"
    description: Gets or sets the storage queue message.
  request:
    type: object
    $ref: "#/types/azure-native:scheduler/v20160101:HttpRequest"
    description: Gets or sets the http requests.
  retryPolicy:
    type: object
    $ref: "#/types/azure-native:scheduler/v20160101:RetryPolicy"
    description: Gets or sets the retry policy.
  serviceBusQueueMessage:
    type: object
    $ref: "#/types/azure-native:scheduler/v20160101:ServiceBusQueueMessage"
    description: Gets or sets the service bus queue message.
  serviceBusTopicMessage:
    type: object
    $ref: "#/types/azure-native:scheduler/v20160101:ServiceBusTopicMessage"
    description: Gets or sets the service bus topic message.
  type:
    $ref: "#/types/azure-native:scheduler/v20160101:JobActionType"
    description: Gets or sets the job error action type.
type: object
