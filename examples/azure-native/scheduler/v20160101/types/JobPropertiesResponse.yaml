$token: azure-native:scheduler/v20160101:JobPropertiesResponse
properties:
  action:
    type: object
    $ref: "#/types/azure-native:scheduler/v20160101:JobActionResponse"
    description: Gets or sets the job action.
  recurrence:
    type: object
    $ref: "#/types/azure-native:scheduler/v20160101:JobRecurrenceResponse"
    description: Gets or sets the job recurrence.
  startTime:
    type: string
    description: Gets or sets the job start time.
  state:
    type: string
    description: Gets or set the job state.
  status:
    type: object
    $ref: "#/types/azure-native:scheduler/v20160101:JobStatusResponse"
    description: Gets the job status.
type: object
required:
  - status
