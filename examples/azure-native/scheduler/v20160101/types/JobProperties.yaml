$token: azure-native:scheduler/v20160101:JobProperties
properties:
  action:
    type: object
    $ref: "#/types/azure-native:scheduler/v20160101:JobAction"
    description: Gets or sets the job action.
  recurrence:
    type: object
    $ref: "#/types/azure-native:scheduler/v20160101:JobRecurrence"
    description: Gets or sets the job recurrence.
  startTime:
    type: string
    description: Gets or sets the job start time.
  state:
    $ref: "#/types/azure-native:scheduler/v20160101:JobState"
    description: Gets or set the job state.
type: object
