$token: azure-native:scheduler/v20160101:getJob
inputs:
  properties:
    jobCollectionName:
      type: string
      description: The job collection name.
    jobName:
      type: string
      description: The job name.
    resourceGroupName:
      type: string
      description: The resource group name.
  type: object
  required:
    - jobCollectionName
    - jobName
    - resourceGroupName
outputs:
  properties:
    id:
      type: string
      description: Gets the job resource identifier.
    name:
      type: string
      description: Gets the job resource name.
    properties:
      type: object
      $ref: "#/types/azure-native:scheduler/v20160101:JobPropertiesResponse"
      description: Gets or sets the job properties.
    type:
      type: string
      description: Gets the job resource type.
  type: object
  required:
    - id
    - name
    - properties
    - type
deprecationMessage: Version 2016-01-01 will be removed in v2 of the provider.
