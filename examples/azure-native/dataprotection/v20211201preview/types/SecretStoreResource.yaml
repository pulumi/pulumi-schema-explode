$token: azure-native:dataprotection/v20211201preview:SecretStoreResource
description: Class representing a secret store resource.
properties:
  secretStoreType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:dataprotection/v20211201preview:SecretStoreType"
    description: Gets or sets the type of secret store
  uri:
    type: string
    description: Uri to get to the resource
type: object
required:
  - secretStoreType
