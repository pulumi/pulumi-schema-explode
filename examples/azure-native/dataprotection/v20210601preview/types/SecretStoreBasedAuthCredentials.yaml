$token: azure-native:dataprotection/v20210601preview:SecretStoreBasedAuthCredentials
description: Secret store based authentication credentials.
properties:
  objectType:
    type: string
    description: |-
      Type of the specific object - used for deserializing
      Expected value is 'SecretStoreBasedAuthCredentials'.
    const: SecretStoreBasedAuthCredentials
  secretStoreResource:
    type: object
    $ref: "#/types/azure-native:dataprotection/v20210601preview:SecretStoreResource"
    description: Secret store resource
type: object
required:
  - objectType
