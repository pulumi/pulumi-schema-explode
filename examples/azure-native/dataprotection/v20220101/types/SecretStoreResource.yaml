$token: azure-native:dataprotection/v20220101:SecretStoreResource
description: Class representing a secret store resource.
properties:
  secretStoreType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:dataprotection/v20220101:SecretStoreType"
    description: Gets or sets the type of secret store
  uri:
    type: string
    description: Uri to get to the resource
  value:
    type: string
    description: Gets or sets value stored in secret store resource
type: object
required:
  - secretStoreType
