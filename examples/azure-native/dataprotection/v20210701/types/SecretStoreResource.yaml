$token: azure-native:dataprotection/v20210701:SecretStoreResource
description: Class representing a secret store resource.
properties:
  secretStoreType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:dataprotection/v20210701:SecretStoreType"
    description: Gets or sets the type of secret store
  uri:
    type: string
    description: Uri to get to the resource
type: object
required:
  - secretStoreType
