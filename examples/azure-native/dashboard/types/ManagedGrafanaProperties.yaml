$token: azure-native:dashboard:ManagedGrafanaProperties
description: Properties specific to the grafana resource.
properties:
  apiKey:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:dashboard:ApiKey"
    description: The api key setting of the Grafana instance.
  autoGeneratedDomainNameLabelScope:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:dashboard:AutoGeneratedDomainNameLabelScope"
    description: Scope for dns deterministic name hash calculation.
  deterministicOutboundIP:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:dashboard:DeterministicOutboundIP"
    description: Whether a Grafana instance uses deterministic outbound IPs.
  publicNetworkAccess:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:dashboard:PublicNetworkAccess"
    description: Indicate the state for enable or disable traffic over the public interface.
  zoneRedundancy:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:dashboard:ZoneRedundancy"
    description: The zone redundancy setting of the Grafana instance.
type: object
