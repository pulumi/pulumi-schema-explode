$token: azure-native:dashboard/v20210901preview:ManagedGrafanaProperties
description: Properties specific to the grafana resource.
properties:
  autoGeneratedDomainNameLabelScope:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:dashboard/v20210901preview:AutoGeneratedDomainNameLabelScope"
  provisioningState:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:dashboard/v20210901preview:ProvisioningState"
    description: Provisioning state of the resource.
  zoneRedundancy:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:dashboard/v20210901preview:ZoneRedundancy"
type: object
