$token: azure-native:dashboard/v20220801:ManagedGrafanaProperties
description: Properties specific to the grafana resource.
properties:
  apiKey:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:dashboard/v20220801:ApiKey"
    description: The api key setting of the Grafana instance.
  autoGeneratedDomainNameLabelScope:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:dashboard/v20220801:AutoGeneratedDomainNameLabelScope"
    description: Scope for dns deterministic name hash calculation.
  deterministicOutboundIP:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:dashboard/v20220801:DeterministicOutboundIP"
    description: Whether a Grafana instance uses deterministic outbound IPs.
  grafanaIntegrations:
    type: object
    $ref: "#/types/azure-native:dashboard/v20220801:GrafanaIntegrations"
    description: GrafanaIntegrations is a bundled observability experience (e.g. pre-configured data source, tailored Grafana dashboards, alerting defaults) for common monitoring scenarios.
  publicNetworkAccess:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:dashboard/v20220801:PublicNetworkAccess"
    description: Indicate the state for enable or disable traffic over the public interface.
  zoneRedundancy:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:dashboard/v20220801:ZoneRedundancy"
    description: The zone redundancy setting of the Grafana instance.
type: object
