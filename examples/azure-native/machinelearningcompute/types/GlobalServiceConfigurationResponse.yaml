$token: azure-native:machinelearningcompute:GlobalServiceConfigurationResponse
description: Global configuration for services in the cluster.
properties:
  autoScale:
    type: object
    $ref: "#/types/azure-native:machinelearningcompute:AutoScaleConfigurationResponse"
    description: The auto-scale configuration
  etag:
    type: string
    description: The configuration ETag for updates.
  serviceAuth:
    type: object
    $ref: "#/types/azure-native:machinelearningcompute:ServiceAuthConfigurationResponse"
    description: Optional global authorization keys for all user services deployed in cluster. These are used if the service does not have auth keys.
  ssl:
    type: object
    $ref: "#/types/azure-native:machinelearningcompute:SslConfigurationResponse"
    description: The SSL configuration properties
type: object
