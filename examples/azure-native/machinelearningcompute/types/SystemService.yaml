$token: azure-native:machinelearningcompute:SystemService
description: Information about a system service deployed in the cluster
properties:
  systemServiceType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:machinelearningcompute:SystemServiceType"
    description: The system service type
type: object
required:
  - systemServiceType
