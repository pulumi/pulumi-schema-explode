$token: azure-native:machinelearningservices/v20220201preview:ColumnTransformerResponse
description: Column transformer parameters.
properties:
  fields:
    type: array
    items:
      type: string
    description: Fields to apply transformer logic on.
  parameters:
    $ref: pulumi.json#/Any
    description: "Different properties to be passed to transformer.\r

      Input expected is dictionary of key,value pairs in JSON format."
type: object
