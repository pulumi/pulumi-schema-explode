$token: azure-native:machinelearningservices/v20220201preview:CustomTargetLags
properties:
  mode:
    type: string
    description: |-
      Target lags selection modes.
      Expected value is 'Custom'.
    const: Custom
  values:
    type: array
    items:
      type: integer
    description: "[Required] Set target lags values."
type: object
required:
  - mode
  - values
