$token: azure-native:machinelearningservices/v20220201preview:OnlineRequestSettingsResponse
description: Online deployment scoring requests configuration.
properties:
  maxConcurrentRequestsPerInstance:
    type: integer
    description: The number of maximum concurrent requests per node allowed per deployment. Defaults to 1.
    default: 1
  maxQueueWait:
    type: string
    description: "The maximum amount of time a request will stay in the queue in ISO 8601 format.\r

      Defaults to 500ms."
    default: PT0.5S
  requestTimeout:
    type: string
    description: "The scoring timeout in ISO 8601 format.\r

      Defaults to 5000ms."
    default: PT5S
type: object
