$token: azure-native:machinelearningservices:ResourceIdentity
description: Service identity associated with a resource.
properties:
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:machinelearningservices:ResourceIdentityAssignment"
    description: Defines values for a ResourceIdentity's type.
  userAssignedIdentities:
    type: object
    additionalProperties:
      type: object
      $ref: "#/types/azure-native:machinelearningservices:UserAssignedIdentityMeta"
    description: Dictionary of the user assigned identities, key is ARM resource ID of the UAI.
type: object
