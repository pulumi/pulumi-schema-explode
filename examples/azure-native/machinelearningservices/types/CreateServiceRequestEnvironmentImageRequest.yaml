$token: azure-native:machinelearningservices:CreateServiceRequestEnvironmentImageRequest
description: The Environment, models and assets needed for inferencing.
properties:
  assets:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:machinelearningservices:ImageAsset"
    description: The list of assets.
  driverProgram:
    type: string
    description: The name of the driver file.
  environment:
    type: object
    $ref: "#/types/azure-native:machinelearningservices:EnvironmentImageRequestEnvironment"
    description: The details of the AZURE ML environment.
  environmentReference:
    type: object
    $ref: "#/types/azure-native:machinelearningservices:EnvironmentImageRequestEnvironmentReference"
    description: The unique identifying details of the AZURE ML environment.
  modelIds:
    type: array
    items:
      type: string
    description: The list of model Ids.
  models:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:machinelearningservices:Model"
    description: The list of models.
type: object
