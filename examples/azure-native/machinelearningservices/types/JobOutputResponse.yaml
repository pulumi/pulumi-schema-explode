$token: azure-native:machinelearningservices:JobOutputResponse
description: Job output definition container information on where to find job output/logs.
properties:
  datastoreId:
    type: string
    description: ARM ID of the datastore where the job logs and artifacts are stored, or null for the default container ("azureml") in the workspace's storage account.
  path:
    type: string
    description: Path within the datastore to the job logs and artifacts.
type: object
required:
  - datastoreId
  - path
