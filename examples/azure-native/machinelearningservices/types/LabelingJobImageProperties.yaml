$token: azure-native:machinelearningservices:LabelingJobImageProperties
properties:
  annotationType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:machinelearningservices:ImageAnnotationType"
    description: Annotation type of image labeling tasks.
  mediaType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:machinelearningservices:MediaType"
    description: Media type of data asset.
type: object
required:
  - mediaType
