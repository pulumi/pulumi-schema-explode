$token: azure-native:machinelearningservices:BatchOutputConfiguration
description: Batch inference output configuration.
properties:
  appendRowFileName:
    type: string
    description: Customized output file name for append_row output action.
  outputAction:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:machinelearningservices:BatchOutputAction"
    description: Indicates how the output will be organized.
type: object
