$token: azure-native:machinelearningservices:DataPathAssetReference
description: Reference to an asset via its path in a datastore.
properties:
  datastoreId:
    type: string
    description: ARM resource ID of the datastore where the asset is located.
  path:
    type: string
    description: The path of the file/directory in the datastore.
  referenceType:
    type: string
    description: |-
      Enum to determine which reference method to use for an asset.
      Expected value is 'DataPath'.
    const: DataPath
type: object
required:
  - referenceType
