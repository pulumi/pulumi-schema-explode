$token: azure-native:machinelearningservices:DockerImagePlatform
properties:
  operatingSystemType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:machinelearningservices:OperatingSystemType"
    description: The OS type the Environment.
type: object
