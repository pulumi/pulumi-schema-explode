$token: azure-native:machinelearningservices/v20210301preview:LabelingJobTextProperties
description: Properties of a labeling job for text data
properties:
  annotationType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:machinelearningservices/v20210301preview:TextAnnotationType"
    description: Annotation type of text labeling job.
  mediaType:
    type: string
    description: |-
      Media type of data asset.
      Expected value is 'Text'.
    const: Text
type: object
required:
  - mediaType
