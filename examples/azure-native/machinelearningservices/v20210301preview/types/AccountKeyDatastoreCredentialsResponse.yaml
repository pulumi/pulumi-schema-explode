$token: azure-native:machinelearningservices/v20210301preview:AccountKeyDatastoreCredentialsResponse
description: Account key datastore credentials configuration.
properties:
  credentialsType:
    type: string
    description: |-
      Enum to determine the datastore credentials type.
      Expected value is 'AccountKey'.
    const: AccountKey
type: object
required:
  - credentialsType
