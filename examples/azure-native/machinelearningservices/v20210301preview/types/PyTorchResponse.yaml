$token: azure-native:machinelearningservices/v20210301preview:PyTorchResponse
description: PyTorch distribution configuration.
properties:
  distributionType:
    type: string
    description: |-
      Enum to determine the job distribution type.
      Expected value is 'PyTorch'.
    const: PyTorch
  processCount:
    type: integer
    description: Total process count for the distributed job.
type: object
required:
  - distributionType
