$token: azure-native:machinelearningservices/v20210301preview:OutputPathAssetReferenceResponse
description: Reference to an asset via its path in a job output.
properties:
  jobId:
    type: string
    description: ARM resource ID of the job.
  path:
    type: string
    description: The path of the file/directory in the job output.
  referenceType:
    type: string
    description: |-
      Enum to determine which reference method to use for an asset.
      Expected value is 'OutputPath'.
    const: OutputPath
type: object
required:
  - referenceType
