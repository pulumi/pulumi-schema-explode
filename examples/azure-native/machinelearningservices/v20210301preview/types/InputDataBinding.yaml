$token: azure-native:machinelearningservices/v20210301preview:InputDataBinding
properties:
  dataId:
    type: string
    description: ARM resource ID of the registered dataVersion.
  mode:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:machinelearningservices/v20210301preview:DataBindingMode"
    description: Mechanism for accessing the data artifact.
  pathOnCompute:
    type: string
    description: Location of data inside the container process.
type: object
