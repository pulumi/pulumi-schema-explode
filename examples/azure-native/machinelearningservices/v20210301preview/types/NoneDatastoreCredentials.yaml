$token: azure-native:machinelearningservices/v20210301preview:NoneDatastoreCredentials
description: Empty/none datastore credentials.
properties:
  credentialsType:
    type: string
    description: |-
      Enum to determine the datastore credentials type.
      Expected value is 'None'.
    const: None
  secrets:
    type: object
    $ref: "#/types/azure-native:machinelearningservices/v20210301preview:NoneDatastoreSecrets"
    description: Empty/none datastore secret.
type: object
required:
  - credentialsType
