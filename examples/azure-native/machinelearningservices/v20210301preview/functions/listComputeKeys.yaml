$token: azure-native:machinelearningservices/v20210301preview:listComputeKeys
description: Secrets related to a Machine Learning compute. Might differ for every type of compute.
inputs:
  properties:
    computeName:
      type: string
      description: Name of the Azure Machine Learning compute.
    resourceGroupName:
      type: string
      description: The name of the resource group. The name is case insensitive.
    workspaceName:
      type: string
      description: Name of Azure Machine Learning workspace.
  type: object
  required:
    - computeName
    - resourceGroupName
    - workspaceName
outputs:
  description: Secrets related to a Machine Learning compute. Might differ for every type of compute.
  properties:
    computeType:
      type: string
      description: The type of compute
  type: object
  required:
    - computeType
