$token: azure-native:machinelearningservices/v20200218preview:KeyVaultPropertiesResponse
properties:
  identityClientId:
    type: string
    description: For future use - The client id of the identity which will be used to access key vault.
  keyIdentifier:
    type: string
    description: Key vault uri to access the encryption key.
  keyVaultArmId:
    type: string
    description: The ArmId of the keyVault where the customer owned encryption key is present.
type: object
required:
  - keyIdentifier
  - keyVaultArmId
