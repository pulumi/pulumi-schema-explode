$token: azure-native:machinelearningservices/v20200218preview:ComputeInstanceProperties
description: Compute Instance properties
properties:
  applicationSharingPolicy:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:machinelearningservices/v20200218preview:ApplicationSharingPolicy"
    description: Policy for sharing applications on this compute instance among users of parent workspace. If Personal, only the creator can access applications on this compute instance. When Shared, any workspace user can access applications on this instance depending on his/her assigned role.
    default: Shared
  computeInstanceAuthorizationType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:machinelearningservices/v20200218preview:ComputeInstanceAuthorizationType"
    description: The Compute Instance Authorization type. Available values are personal (default).
    default: personal
  personalComputeInstanceSettings:
    type: object
    $ref: "#/types/azure-native:machinelearningservices/v20200218preview:PersonalComputeInstanceSettings"
    description: Settings for a personal compute instance.
  sshSettings:
    type: object
    $ref: "#/types/azure-native:machinelearningservices/v20200218preview:ComputeInstanceSshSettings"
    description: Specifies policy and settings for SSH access.
  subnet:
    type: object
    $ref: "#/types/azure-native:machinelearningservices/v20200218preview:ResourceId"
    description: Virtual network subnet resource ID the compute nodes belong to.
  vmSize:
    type: string
    description: Virtual Machine Size
type: object
