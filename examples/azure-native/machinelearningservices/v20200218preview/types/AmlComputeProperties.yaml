$token: azure-native:machinelearningservices/v20200218preview:AmlComputeProperties
description: AML Compute properties
properties:
  osType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:machinelearningservices/v20200218preview:OsType"
    description: OS Type.
    default: Linux
  remoteLoginPortPublicAccess:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:machinelearningservices/v20200218preview:RemoteLoginPortPublicAccess"
    description: "State of the public SSH port. Possible values are: Disabled - Indicates that the public ssh port is closed on all nodes of the cluster. Enabled - Indicates that the public ssh port is open on all nodes of the cluster. NotSpecified - Indicates that the public ssh port is closed on all nodes of the cluster if VNet is defined, else is open all public nodes. It can be default only during cluster creation time, after creation it will be either enabled or disabled."
    default: NotSpecified
  scaleSettings:
    type: object
    $ref: "#/types/azure-native:machinelearningservices/v20200218preview:ScaleSettings"
    description: Scale settings for AML Compute
  subnet:
    type: object
    $ref: "#/types/azure-native:machinelearningservices/v20200218preview:ResourceId"
    description: Virtual network subnet resource ID the compute nodes belong to.
  userAccountCredentials:
    type: object
    $ref: "#/types/azure-native:machinelearningservices/v20200218preview:UserAccountCredentials"
    description: Credentials for an administrator user account that will be created on each compute node.
  virtualMachineImage:
    type: object
    $ref: "#/types/azure-native:machinelearningservices/v20200218preview:ResourceId"
    description: The ARM resource identifier of the virtual machine image for the compute nodes. This is of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroup}/providers/Microsoft.Compute/galleries/{gallery name}/images/{image definition name}/versions/{version id}. The virtual machine image must be in the same region and subscription as the cluster.
  vmPriority:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:machinelearningservices/v20200218preview:VmPriority"
    description: Virtual Machine priority
  vmSize:
    type: string
    description: Virtual Machine Size
type: object
