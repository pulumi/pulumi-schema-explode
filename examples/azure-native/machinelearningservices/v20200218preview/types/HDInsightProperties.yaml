$token: azure-native:machinelearningservices/v20200218preview:HDInsightProperties
properties:
  address:
    type: string
    description: Public IP address of the master node of the cluster.
  administratorAccount:
    type: object
    $ref: "#/types/azure-native:machinelearningservices/v20200218preview:VirtualMachineSshCredentials"
    description: Admin credentials for master node of the cluster
  sshPort:
    type: integer
    description: Port open for ssh connections on the master node of the cluster.
type: object
