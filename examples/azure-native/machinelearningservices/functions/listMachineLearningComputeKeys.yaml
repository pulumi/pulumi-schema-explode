$token: azure-native:machinelearningservices:listMachineLearningComputeKeys
description: |-
  Secrets related to a Machine Learning compute. Might differ for every type of compute.
  API Version: 2021-01-01.
inputs:
  properties:
    computeName:
      type: string
      description: Name of the Azure Machine Learning compute.
    resourceGroupName:
      type: string
      description: Name of the resource group in which workspace is located.
    workspaceName:
      type: string
      description: Name of Azure Machine Learning workspace.
  type: object
  required:
    - computeName
    - resourceGroupName
    - workspaceName
outputs:
  description: Secrets related to a Machine Learning compute. Might differ for every type of compute.
  properties:
    computeType:
      type: string
      description: The type of compute
  type: object
  required:
    - computeType
