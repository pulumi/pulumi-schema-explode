$token: azure-native:machinelearningservices/v20220601preview:DataFactory
description: A DataFactory compute.
properties:
  computeType:
    type: string
    description: |-
      The type of compute
      Expected value is 'DataFactory'.
    const: DataFactory
  description:
    type: string
    description: The description of the Machine Learning compute.
  disableLocalAuth:
    type: boolean
    description: Opt-out of local authentication and ensure customers can use only MSI and AAD exclusively for authentication.
  resourceId:
    type: string
    description: ARM resource id of the underlying compute
type: object
required:
  - computeType
