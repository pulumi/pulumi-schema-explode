$token: azure-native:machinelearningservices/v20220601preview:RecurrenceSchedule
properties:
  hours:
    type: array
    items:
      type: integer
    description: "[Required] List of hours for the schedule."
  minutes:
    type: array
    items:
      type: integer
    description: "[Required] List of minutes for the schedule."
  weekDays:
    type: array
    items:
      oneOf:
        - type: string
        - $ref: "#/types/azure-native:machinelearningservices/v20220601preview:WeekDay"
    description: List of days for the schedule.
type: object
required:
  - hours
  - minutes
