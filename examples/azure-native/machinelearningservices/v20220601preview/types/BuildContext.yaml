$token: azure-native:machinelearningservices/v20220601preview:BuildContext
description: Configuration settings for Docker build context
properties:
  contextUri:
    type: string
    description: "[Required] URI of the Docker build context used to build the image. Supports blob URIs on environment creation and may return blob or Git URIs.\r

      <seealso href=\"https://docs.docker.com/engine/reference/commandline/build/#extended-description\" />"
  dockerfilePath:
    type: string
    description: "Path to the Dockerfile in the build context.\r

      <seealso href=\"https://docs.docker.com/engine/reference/builder/\" />"
    default: Dockerfile
type: object
required:
  - contextUri
