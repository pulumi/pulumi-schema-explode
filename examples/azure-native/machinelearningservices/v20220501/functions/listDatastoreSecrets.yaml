$token: azure-native:machinelearningservices/v20220501:listDatastoreSecrets
description: Base definition for datastore secrets.
inputs:
  properties:
    name:
      type: string
      description: Datastore name.
    resourceGroupName:
      type: string
      description: The name of the resource group. The name is case insensitive.
    workspaceName:
      type: string
      description: Name of Azure Machine Learning workspace.
  type: object
  required:
    - name
    - resourceGroupName
    - workspaceName
outputs:
  description: Base definition for datastore secrets.
  properties:
    secretsType:
      type: string
      description: "[Required] Credential type used to authentication with storage."
  type: object
  required:
    - secretsType
