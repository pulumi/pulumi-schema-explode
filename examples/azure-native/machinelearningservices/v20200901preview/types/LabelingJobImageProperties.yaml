$token: azure-native:machinelearningservices/v20200901preview:LabelingJobImageProperties
properties:
  annotationType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:machinelearningservices/v20200901preview:ImageAnnotationType"
    description: Annotation type of image labeling tasks.
  mediaType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:machinelearningservices/v20200901preview:MediaType"
    description: Media type of data asset.
type: object
required:
  - mediaType
