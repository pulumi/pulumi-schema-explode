$token: azure-native:machinelearningservices/v20210701:EncryptionPropertyResponse
properties:
  identity:
    type: object
    $ref: "#/types/azure-native:machinelearningservices/v20210701:IdentityForCmkResponse"
    description: The identity that will be used to access the key vault for encryption at rest.
  keyVaultProperties:
    type: object
    $ref: "#/types/azure-native:machinelearningservices/v20210701:KeyVaultPropertiesResponse"
    description: Customer Key vault properties.
  status:
    type: string
    description: Indicates whether or not the encryption is enabled for the workspace.
type: object
required:
  - keyVaultProperties
  - status
