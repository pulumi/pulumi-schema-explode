$token: azure-native:logz:listSubAccountMonitoredResources
description: |-
  Response of a list operation.
  API Version: 2020-10-01.
inputs:
  properties:
    monitorName:
      type: string
      description: Monitor resource name
    resourceGroupName:
      type: string
      description: The name of the resource group. The name is case insensitive.
    subAccountName:
      type: string
      description: Sub Account resource name
  type: object
  required:
    - monitorName
    - resourceGroupName
    - subAccountName
outputs:
  description: Response of a list operation.
  properties:
    nextLink:
      type: string
      description: Link to the next set of results, if any.
    value:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:logz:MonitoredResourceResponse"
      description: Results of a list operation.
  type: object
