$token: azure-native:logz:MonitoringTagRulesPropertiesResponse
description: Definition of the properties for a TagRules resource.
properties:
  logRules:
    type: object
    $ref: "#/types/azure-native:logz:LogRulesResponse"
    description: Set of rules for sending logs for the Monitor resource.
  provisioningState:
    type: string
    description: Flag specifying if the resource provisioning state as tracked by ARM.
  systemData:
    type: object
    $ref: "#/types/azure-native:logz:SystemDataResponse"
    description: Metadata pertaining to creation and last modification of the resource.
type: object
required:
  - provisioningState
  - systemData
