$token: azure-native:keyvault:ManagedHsmSku
description: SKU details
properties:
  family:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:keyvault:ManagedHsmSkuFamily"
    description: SKU Family of the managed HSM Pool
  name:
    $ref: "#/types/azure-native:keyvault:ManagedHsmSkuName"
    description: SKU of the managed HSM Pool
type: object
required:
  - family
  - name
