$token: azure-native:keyvault/v20150601:PermissionsResponse
description: Permissions the identity has for keys, secrets and certificates.
properties:
  certificates:
    type: array
    items:
      type: string
    description: Permissions to certificates
  keys:
    type: array
    items:
      type: string
    description: Permissions to keys
  secrets:
    type: array
    items:
      type: string
    description: Permissions to secrets
type: object
