$token: azure-native:containerservice/v20210501:listManagedClusterMonitoringUserCredentials
description: The list credential result response.
inputs:
  properties:
    resourceGroupName:
      type: string
      description: The name of the resource group.
    resourceName:
      type: string
      description: The name of the managed cluster resource.
    serverFqdn:
      type: string
      description: server fqdn type for credentials to be returned
  type: object
  required:
    - resourceGroupName
    - resourceName
outputs:
  description: The list credential result response.
  properties:
    kubeconfigs:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:containerservice/v20210501:CredentialResultResponse"
      description: Base64-encoded Kubernetes configuration file.
  type: object
  required:
    - kubeconfigs
