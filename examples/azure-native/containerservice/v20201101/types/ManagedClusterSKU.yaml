$token: azure-native:containerservice/v20201101:ManagedClusterSKU
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:containerservice/v20201101:ManagedClusterSKUName"
    description: Name of a managed cluster SKU.
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:containerservice/v20201101:ManagedClusterSKUTier"
    description: Tier of a managed cluster SKU.
type: object
