$token: azure-native:containerservice/v20201101:ManagedClusterPodIdentityProfile
properties:
  enabled:
    type: boolean
    description: Whether the pod identity addon is enabled.
  userAssignedIdentities:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:containerservice/v20201101:ManagedClusterPodIdentity"
    description: User assigned pod identity settings.
  userAssignedIdentityExceptions:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:containerservice/v20201101:ManagedClusterPodIdentityException"
    description: User assigned pod identity exception settings.
type: object
