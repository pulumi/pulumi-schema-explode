$token: azure-native:containerservice/v20191101:ManagedClusterAPIServerAccessProfileResponse
description: Access profile for managed cluster API server.
properties:
  authorizedIPRanges:
    type: array
    items:
      type: string
    description: Authorized IP Ranges to kubernetes API server.
  enablePrivateCluster:
    type: boolean
    description: Whether to create the cluster as a private cluster or not.
type: object
