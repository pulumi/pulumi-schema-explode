$token: azure-native:containerservice/v20180331:ManagedClusterAgentPoolProfile
description: Profile for the container service agent pool.
properties:
  count:
    type: integer
    description: "Number of agents (VMs) to host docker containers. Allowed values must be in the range of 1 to 100 (inclusive). The default value is 1. "
    default: 1
  maxPods:
    type: integer
    description: Maximum number of pods that can run on a node.
  name:
    type: string
    description: Unique name of the agent pool profile in the context of the subscription and resource group.
  osDiskSizeGB:
    type: integer
    description: OS Disk Size in GB to be used to specify the disk size for every machine in this master/agent pool. If you specify 0, it will apply the default osDisk size according to the vmSize specified.
  osType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:containerservice/v20180331:OSType"
    description: OsType to be used to specify os type. Choose from Linux and Windows. Default to Linux.
  vmSize:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:containerservice/v20180331:ContainerServiceVMSizeTypes"
    description: Size of agent VMs.
  vnetSubnetID:
    type: string
    description: VNet SubnetID specifies the VNet's subnet identifier.
type: object
required:
  - name
  - vmSize
