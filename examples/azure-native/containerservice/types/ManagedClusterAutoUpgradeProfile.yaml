$token: azure-native:containerservice:ManagedClusterAutoUpgradeProfile
description: Auto upgrade profile for a managed cluster.
properties:
  upgradeChannel:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:containerservice:UpgradeChannel"
    description: upgrade channel for auto upgrade.
type: object
