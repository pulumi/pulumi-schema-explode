$token: azure-native:containerservice:TimeInWeek
description: Time in a week.
properties:
  day:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:containerservice:WeekDay"
    description: A day in a week.
  hourSlots:
    type: array
    items:
      type: integer
    description: hour slots in a day.
type: object
