$token: azure-native:servicefabricmesh:NetworkRef
description: Describes a network reference in a service.
properties:
  endpointRefs:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:servicefabricmesh:EndpointRef"
    description: A list of endpoints that are exposed on this network.
  name:
    type: string
    description: Name of the network
type: object
