$token: azure-native:servicefabricmesh:NetworkResourcePropertiesResponse
description: Describes properties of a network resource.
properties:
  description:
    type: string
    description: User readable description of the network.
  kind:
    type: string
    description: |-
      The type of a Service Fabric container network.
      Expected value is 'NetworkResourceProperties'.
    const: NetworkResourceProperties
  provisioningState:
    type: string
    description: State of the resource.
  status:
    type: string
    description: Status of the network.
  statusDetails:
    type: string
    description: Gives additional information about the current status of the network.
type: object
required:
  - kind
  - provisioningState
  - status
  - statusDetails
