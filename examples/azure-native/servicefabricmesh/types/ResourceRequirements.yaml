$token: azure-native:servicefabricmesh:ResourceRequirements
description: This type describes the resource requirements for a container or a service.
properties:
  limits:
    type: object
    $ref: "#/types/azure-native:servicefabricmesh:ResourceLimits"
    description: Describes the maximum limits on the resources for a given container.
  requests:
    type: object
    $ref: "#/types/azure-native:servicefabricmesh:ResourceRequests"
    description: Describes the requested resources for a given container.
type: object
required:
  - requests
