$token: azure-native:servicefabricmesh:HttpRouteMatchRule
description: Describes a rule for http route matching.
properties:
  headers:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:servicefabricmesh:HttpRouteMatchHeader"
    description: headers and their values to match in request.
  path:
    type: object
    $ref: "#/types/azure-native:servicefabricmesh:HttpRouteMatchPath"
    description: Path to match for routing.
type: object
required:
  - path
