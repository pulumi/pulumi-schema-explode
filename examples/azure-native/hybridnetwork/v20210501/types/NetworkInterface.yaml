$token: azure-native:hybridnetwork/v20210501:NetworkInterface
description: Network interface properties.
properties:
  ipConfigurations:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:hybridnetwork/v20210501:NetworkInterfaceIPConfiguration"
    description: A list of IP configurations of the network interface.
  macAddress:
    type: string
    description: The MAC address of the network interface.
  networkInterfaceName:
    type: string
    description: The name of the network interface.
  vmSwitchType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:hybridnetwork/v20210501:VMSwitchType"
    description: The type of the VM switch.
type: object
