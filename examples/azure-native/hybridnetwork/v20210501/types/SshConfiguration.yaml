$token: azure-native:hybridnetwork/v20210501:SshConfiguration
description: SSH configuration for Linux based VMs running on Azure
properties:
  publicKeys:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:hybridnetwork/v20210501:SshPublicKey"
    description: The list of SSH public keys used to authenticate with linux based VMs.
type: object
