$token: azure-native:hybridnetwork/v20210501:getVendorSkuPreview
description: Customer subscription which can use a sku.
inputs:
  properties:
    previewSubscription:
      type: string
      description: Preview subscription ID.
    skuName:
      type: string
      description: The name of the vendor sku.
    vendorName:
      type: string
      description: The name of the vendor.
  type: object
  required:
    - previewSubscription
    - skuName
    - vendorName
outputs:
  description: Customer subscription which can use a sku.
  properties:
    id:
      type: string
      description: The ARM ID of the resource.
    name:
      type: string
      description: The preview subscription ID.
    provisioningState:
      type: string
      description: The provisioning state of the PreviewSubscription resource.
    systemData:
      type: object
      $ref: "#/types/azure-native:hybridnetwork/v20210501:SystemDataResponse"
      description: The system meta data relating to this resource.
    type:
      type: string
      description: The type of the resource.
  type: object
  required:
    - id
    - name
    - provisioningState
    - systemData
    - type
