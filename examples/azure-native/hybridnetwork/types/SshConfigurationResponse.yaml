$token: azure-native:hybridnetwork:SshConfigurationResponse
description: SSH configuration for Linux based VMs running on Azure
properties:
  publicKeys:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:hybridnetwork:SshPublicKeyResponse"
    description: The list of SSH public keys used to authenticate with linux based VMs.
type: object
