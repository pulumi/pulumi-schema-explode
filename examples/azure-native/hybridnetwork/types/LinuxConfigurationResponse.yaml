$token: azure-native:hybridnetwork:LinuxConfigurationResponse
description: Specifies the Linux operating system settings on the virtual machine.
properties:
  ssh:
    type: object
    $ref: "#/types/azure-native:hybridnetwork:SshConfigurationResponse"
    description: Specifies the ssh key configuration for a Linux OS.
type: object
