$token: azure-native:storagecache/v20200301:CacheNetworkSettingsResponse
description: Cache network settings.
properties:
  mtu:
    type: integer
    description: The IPv4 maximum transmission unit configured for the subnet.
    default: 1500
  utilityAddresses:
    type: array
    items:
      type: string
    description: Array of additional IP addresses used by this Cache.
type: object
required:
  - utilityAddresses
