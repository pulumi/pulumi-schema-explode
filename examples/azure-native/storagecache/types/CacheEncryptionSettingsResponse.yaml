$token: azure-native:storagecache:CacheEncryptionSettingsResponse
description: Cache encryption settings.
properties:
  keyEncryptionKey:
    type: object
    $ref: "#/types/azure-native:storagecache:KeyVaultKeyReferenceResponse"
    description: Specifies the location of the key encryption key in Key Vault.
type: object
