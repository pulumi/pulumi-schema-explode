$token: azure-native:storagecache:CacheEncryptionSettings
description: Cache encryption settings.
properties:
  keyEncryptionKey:
    type: object
    $ref: "#/types/azure-native:storagecache:KeyVaultKeyReference"
    description: Specifies the location of the key encryption key in Key Vault.
type: object
