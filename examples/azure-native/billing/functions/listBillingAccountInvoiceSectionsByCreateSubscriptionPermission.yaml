$token: azure-native:billing:listBillingAccountInvoiceSectionsByCreateSubscriptionPermission
description: |-
  The list of invoice section properties with create subscription permission.
  API Version: 2020-05-01.
inputs:
  properties:
    billingAccountName:
      type: string
      description: The ID that uniquely identifies a billing account.
  type: object
  required:
    - billingAccountName
outputs:
  description: The list of invoice section properties with create subscription permission.
  properties:
    nextLink:
      type: string
      description: The link (url) to the next page of results.
    value:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:billing:InvoiceSectionWithCreateSubPermissionResponse"
      description: The list of invoice section properties with create subscription permission.
  type: object
  required:
    - nextLink
