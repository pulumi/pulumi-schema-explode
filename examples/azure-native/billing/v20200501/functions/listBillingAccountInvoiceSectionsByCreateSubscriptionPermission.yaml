$token: azure-native:billing/v20200501:listBillingAccountInvoiceSectionsByCreateSubscriptionPermission
description: The list of invoice section properties with create subscription permission.
inputs:
  properties:
    billingAccountName:
      type: string
      description: The ID that uniquely identifies a billing account.
  type: object
  required:
    - billingAccountName
outputs:
  description: The list of invoice section properties with create subscription permission.
  properties:
    nextLink:
      type: string
      description: The link (url) to the next page of results.
    value:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:billing/v20200501:InvoiceSectionWithCreateSubPermissionResponse"
      description: The list of invoice section properties with create subscription permission.
  type: object
  required:
    - nextLink
