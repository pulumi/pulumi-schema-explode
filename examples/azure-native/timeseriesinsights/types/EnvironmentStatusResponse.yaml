$token: azure-native:timeseriesinsights:EnvironmentStatusResponse
description: An object that represents the status of the environment, and its internal state in the Time Series Insights service.
properties:
  ingress:
    type: object
    $ref: "#/types/azure-native:timeseriesinsights:IngressEnvironmentStatusResponse"
    description: An object that represents the status of ingress on an environment.
  warmStorage:
    type: object
    $ref: "#/types/azure-native:timeseriesinsights:WarmStorageEnvironmentStatusResponse"
    description: An object that represents the status of warm storage on an environment.
type: object
required:
  - ingress
  - warmStorage
