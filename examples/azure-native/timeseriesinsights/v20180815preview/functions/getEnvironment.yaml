$token: azure-native:timeseriesinsights/v20180815preview:getEnvironment
description: An environment is a set of time-series data available for query, and is the top level Azure Time Series Insights resource.
inputs:
  properties:
    environmentName:
      type: string
      description: The name of the Time Series Insights environment associated with the specified resource group.
    expand:
      type: string
      description: Setting $expand=status will include the status of the internal services of the environment in the Time Series Insights service.
    resourceGroupName:
      type: string
      description: Name of an Azure Resource group.
  type: object
  required:
    - environmentName
    - resourceGroupName
outputs:
  description: An environment is a set of time-series data available for query, and is the top level Azure Time Series Insights resource.
  properties:
    id:
      type: string
      description: Resource Id
    kind:
      type: string
      description: The kind of the environment.
    location:
      type: string
      description: Resource location
    name:
      type: string
      description: Resource name
    sku:
      type: object
      $ref: "#/types/azure-native:timeseriesinsights/v20180815preview:SkuResponse"
      description: The sku determines the type of environment, either standard (S1 or S2) or long-term (L1). For standard environments the sku determines the capacity of the environment, the ingress rate, and the billing rate.
    tags:
      type: object
      additionalProperties:
        type: string
      description: Resource tags
    type:
      type: string
      description: Resource type
  type: object
  required:
    - id
    - kind
    - location
    - name
    - sku
    - type
deprecationMessage: Version 2018-08-15-preview will be removed in v2 of the provider.
