$token: azure-native:apimanagement/v20180601preview:CertificateConfiguration
description: Certificate configuration which consist of non-trusted intermediates and root certificates.
properties:
  certificate:
    type: object
    $ref: "#/types/azure-native:apimanagement/v20180601preview:CertificateInformation"
    description: Certificate information.
  certificatePassword:
    type: string
    description: Certificate Password.
  encodedCertificate:
    type: string
    description: Base64 Encoded certificate.
  storeName:
    type: string
    description: The System.Security.Cryptography.x509certificates.StoreName certificate store location. Only Root and CertificateAuthority are valid locations.
type: object
required:
  - storeName
