$token: azure-native:apimanagement/v20191201:listIdentityProviderSecrets
description: Client or app secret used in IdentityProviders, Aad, OpenID or OAuth.
inputs:
  properties:
    identityProviderName:
      type: string
      description: Identity Provider Type identifier.
    resourceGroupName:
      type: string
      description: The name of the resource group.
    serviceName:
      type: string
      description: The name of the API Management service.
  type: object
  required:
    - identityProviderName
    - resourceGroupName
    - serviceName
outputs:
  description: Client or app secret used in IdentityProviders, Aad, OpenID or OAuth.
  properties:
    clientSecret:
      type: string
      description: Client or app secret used in IdentityProviders, Aad, OpenID or OAuth.
  type: object
