$token: azure-native:apimanagement:PipelineDiagnosticSettings
description: Diagnostic settings for incoming/outgoing HTTP messages to the Gateway.
properties:
  request:
    type: object
    $ref: "#/types/azure-native:apimanagement:HttpMessageDiagnostic"
    description: Diagnostic settings for request.
  response:
    type: object
    $ref: "#/types/azure-native:apimanagement:HttpMessageDiagnostic"
    description: Diagnostic settings for response.
type: object
