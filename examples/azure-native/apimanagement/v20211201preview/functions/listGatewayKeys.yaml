$token: azure-native:apimanagement/v20211201preview:listGatewayKeys
description: Gateway authentication keys.
inputs:
  properties:
    gatewayId:
      type: string
      description: Gateway entity identifier. Must be unique in the current API Management service instance. Must not have value 'managed'
    resourceGroupName:
      type: string
      description: The name of the resource group.
    serviceName:
      type: string
      description: The name of the API Management service.
  type: object
  required:
    - gatewayId
    - resourceGroupName
    - serviceName
outputs:
  description: Gateway authentication keys.
  properties:
    primary:
      type: string
      description: Primary gateway key.
    secondary:
      type: string
      description: Secondary gateway key.
  type: object
