$token: azure-native:apimanagement/v20191201preview:listOpenIdConnectProviderSecrets
description: Client or app secret used in IdentityProviders, Aad, OpenID or OAuth.
inputs:
  properties:
    opid:
      type: string
      description: Identifier of the OpenID Connect Provider.
    resourceGroupName:
      type: string
      description: The name of the resource group.
    serviceName:
      type: string
      description: The name of the API Management service.
  type: object
  required:
    - opid
    - resourceGroupName
    - serviceName
outputs:
  description: Client or app secret used in IdentityProviders, Aad, OpenID or OAuth.
  properties:
    clientSecret:
      type: string
      description: Client or app secret used in IdentityProviders, Aad, OpenID or OAuth.
  type: object
