$token: azure-native:apimanagement/v20210401preview:listSubscriptionSecrets
description: Subscription keys.
inputs:
  properties:
    resourceGroupName:
      type: string
      description: The name of the resource group.
    serviceName:
      type: string
      description: The name of the API Management service.
    sid:
      type: string
      description: Subscription entity Identifier. The entity represents the association between a user and a product in API Management.
  type: object
  required:
    - resourceGroupName
    - serviceName
    - sid
outputs:
  description: Subscription keys.
  properties:
    primaryKey:
      type: string
      description: Subscription primary key.
    secondaryKey:
      type: string
      description: Subscription secondary key.
  type: object
