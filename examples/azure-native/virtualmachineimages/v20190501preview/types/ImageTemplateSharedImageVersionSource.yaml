$token: azure-native:virtualmachineimages/v20190501preview:ImageTemplateSharedImageVersionSource
description: Describes an image source that is an image version in a shared image gallery.
properties:
  imageVersionId:
    type: string
    description: ARM resource id of the image version in the shared image gallery
  type:
    type: string
    description: |-
      Specifies the type of source image you want to start with.
      Expected value is 'SharedImageVersion'.
    const: SharedImageVersion
type: object
required:
  - imageVersionId
  - type
