$token: azure-native:datashare/v20181101preview:ScheduledSourceSynchronizationSettingResponse
description: A type of synchronization setting based on schedule
properties:
  kind:
    type: string
    description: |-
      Kind of synchronization setting on share.
      Expected value is 'ScheduleBased'.
    const: ScheduleBased
  recurrenceInterval:
    type: string
    description: Recurrence Interval
  synchronizationTime:
    type: string
    description: Synchronization time
type: object
required:
  - kind
