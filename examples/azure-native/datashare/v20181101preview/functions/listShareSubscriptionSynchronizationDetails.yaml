$token: azure-native:datashare/v20181101preview:listShareSubscriptionSynchronizationDetails
description: details of synchronization
inputs:
  properties:
    accountName:
      type: string
      description: The name of the share account.
    filter:
      type: string
      description: Filters the results using OData syntax.
    orderby:
      type: string
      description: Sorts the results using OData syntax.
    resourceGroupName:
      type: string
      description: The resource group name.
    shareSubscriptionName:
      type: string
      description: The name of the share subscription.
    skipToken:
      type: string
      description: Continuation token
    synchronizationId:
      type: string
      description: Synchronization id
  type: object
  required:
    - accountName
    - resourceGroupName
    - shareSubscriptionName
    - synchronizationId
outputs:
  description: details of synchronization
  properties:
    nextLink:
      type: string
      description: The Url of next result page.
    value:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:datashare/v20181101preview:SynchronizationDetailsResponse"
      description: Collection of items of type DataTransferObjects.
  type: object
  required:
    - value
