$token: azure-native:datashare:Identity
description: Identity of resource
properties:
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:datashare:Type"
    description: Identity Type
type: object
