$token: azure-native:resourceconnector:listApplianceClusterCustomerUserCredential
description: |-
  The List Cluster Customer User Credential Results appliance.
  API Version: 2022-04-15-preview.
inputs:
  properties:
    resourceGroupName:
      type: string
      description: The name of the resource group. The name is case insensitive.
    resourceName:
      type: string
      description: Appliances name.
  type: object
  required:
    - resourceGroupName
    - resourceName
outputs:
  description: The List Cluster Customer User Credential Results appliance.
  properties:
    kubeconfigs:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:resourceconnector:ApplianceCredentialKubeconfigResponse"
      description: The list of appliance kubeconfigs.
    sshKeys:
      type: object
      additionalProperties:
        type: object
        $ref: "#/types/azure-native:resourceconnector:SSHKeyResponse"
      description: Map of Customer User Public and Private SSH Keys
  type: object
  required:
    - kubeconfigs
    - sshKeys
