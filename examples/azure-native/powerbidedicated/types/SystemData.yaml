$token: azure-native:powerbidedicated:SystemData
description: Metadata pertaining to creation and last modification of the resource.
properties:
  createdAt:
    type: string
    description: The timestamp of resource creation (UTC)
  createdBy:
    type: string
    description: An identifier for the identity that created the resource
  createdByType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:powerbidedicated:IdentityType"
    description: The type of identity that created the resource
  lastModifiedAt:
    type: string
    description: The timestamp of resource last modification (UTC)
  lastModifiedBy:
    type: string
    description: An identifier for the identity that last modified the resource
  lastModifiedByType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:powerbidedicated:IdentityType"
    description: The type of identity that last modified the resource
type: object
