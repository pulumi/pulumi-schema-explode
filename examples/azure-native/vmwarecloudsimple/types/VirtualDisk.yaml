$token: azure-native:vmwarecloudsimple:VirtualDisk
description: Virtual disk model
properties:
  controllerId:
    type: string
    description: Disk's Controller id
  independenceMode:
    $ref: "#/types/azure-native:vmwarecloudsimple:DiskIndependenceMode"
    description: Disk's independence mode type
  totalSize:
    type: integer
    description: Disk's total size
  virtualDiskId:
    type: string
    description: Disk's id
type: object
required:
  - controllerId
  - independenceMode
  - totalSize
