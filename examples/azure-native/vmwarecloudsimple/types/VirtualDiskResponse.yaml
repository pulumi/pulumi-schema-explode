$token: azure-native:vmwarecloudsimple:VirtualDiskResponse
description: Virtual disk model
properties:
  controllerId:
    type: string
    description: Disk's Controller id
  independenceMode:
    type: string
    description: Disk's independence mode type
  totalSize:
    type: integer
    description: Disk's total size
  virtualDiskId:
    type: string
    description: Disk's id
  virtualDiskName:
    type: string
    description: Disk's display name
type: object
required:
  - controllerId
  - independenceMode
  - totalSize
  - virtualDiskName
