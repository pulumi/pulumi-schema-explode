$token: azure-native:vmwarecloudsimple:GuestOSNICCustomizationResponse
description: Guest OS nic customization
properties:
  allocation:
    type: string
    description: IP address allocation method
  dnsServers:
    type: array
    items:
      type: string
    description: List of dns servers to use
  gateway:
    type: array
    items:
      type: string
    description: Gateway addresses assigned to nic
  ipAddress:
    type: string
    description: Static ip address for nic
  mask:
    type: string
    description: Network mask for nic
  primaryWinsServer:
    type: string
    description: primary WINS server for Windows
  secondaryWinsServer:
    type: string
    description: secondary WINS server for Windows
type: object
