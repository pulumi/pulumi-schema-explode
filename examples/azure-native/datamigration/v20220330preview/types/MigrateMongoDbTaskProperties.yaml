$token: azure-native:datamigration/v20220330preview:MigrateMongoDbTaskProperties
description: Properties for the task that migrates data between MongoDB data sources
properties:
  clientData:
    type: object
    additionalProperties:
      type: string
    description: Key value pairs of client data to attach meta data information to task
  input:
    type: object
    $ref: "#/types/azure-native:datamigration/v20220330preview:MongoDbMigrationSettings"
    description: Describes how a MongoDB data migration should be performed
  taskType:
    type: string
    description: |-
      Task type.
      Expected value is 'Migrate.MongoDb'.
    const: Migrate.MongoDb
type: object
required:
  - taskType
