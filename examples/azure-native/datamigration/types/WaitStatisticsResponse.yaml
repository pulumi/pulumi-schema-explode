$token: azure-native:datamigration:WaitStatisticsResponse
description: Wait statistics gathered during query batch execution
properties:
  waitCount:
    type: number
    description: Total no. of waits
  waitTimeMs:
    type: number
    description: Total wait time in millisecond(s)
    default: 0
  waitType:
    type: string
    description: Type of the Wait
type: object
required:
  - waitCount
  - waitTimeMs
  - waitType
