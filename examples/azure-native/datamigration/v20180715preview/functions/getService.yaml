$token: azure-native:datamigration/v20180715preview:getService
description: A Database Migration Service resource
inputs:
  properties:
    groupName:
      type: string
      description: Name of the resource group
    serviceName:
      type: string
      description: Name of the service
  type: object
  required:
    - groupName
    - serviceName
outputs:
  description: A Database Migration Service resource
  properties:
    etag:
      type: string
      description: HTTP strong entity tag value. Ignored if submitted
    id:
      type: string
      description: Resource ID.
    kind:
      type: string
      description: The resource kind. Only 'vm' (the default) is supported.
    location:
      type: string
      description: Resource location.
    name:
      type: string
      description: Resource name.
    provisioningState:
      type: string
      description: The resource's provisioning state
    publicKey:
      type: string
      description: The public key of the service, used to encrypt secrets sent to the service
    sku:
      type: object
      $ref: "#/types/azure-native:datamigration/v20180715preview:ServiceSkuResponse"
      description: Service SKU
    tags:
      type: object
      additionalProperties:
        type: string
      description: Resource tags.
    type:
      type: string
      description: Resource type.
    virtualNicId:
      type: string
      description: The ID of the Microsoft.Network/networkInterfaces resource which the service have
    virtualSubnetId:
      type: string
      description: The ID of the Microsoft.Network/virtualNetworks/subnets resource to which the service should be joined
  type: object
  required:
    - id
    - location
    - name
    - provisioningState
    - type
    - virtualSubnetId
