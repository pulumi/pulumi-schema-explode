$token: azure-native:cognitiveservices/v20211001:DeploymentScaleSettings
description: Properties of Cognitive Services account deployment model.
properties:
  capacity:
    type: integer
    description: Deployment capacity.
  scaleType:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:cognitiveservices/v20211001:DeploymentScaleType"
    description: Deployment scale type.
type: object
