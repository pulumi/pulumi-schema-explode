$token: azure-native:cognitiveservices/v20211001:AccountPropertiesResponse
description: Properties of Cognitive Services account.
properties:
  allowedFqdnList:
    type: array
    items:
      type: string
  apiProperties:
    type: object
    $ref: "#/types/azure-native:cognitiveservices/v20211001:ApiPropertiesResponse"
    description: The api properties for special APIs.
  callRateLimit:
    type: object
    $ref: "#/types/azure-native:cognitiveservices/v20211001:CallRateLimitResponse"
    description: The call rate limit Cognitive Services account.
  capabilities:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:cognitiveservices/v20211001:SkuCapabilityResponse"
    description: Gets the capabilities of the cognitive services account. Each item indicates the capability of a specific feature. The values are read-only and for reference only.
  customSubDomainName:
    type: string
    description: Optional subdomain name used for token-based authentication.
  dateCreated:
    type: string
    description: Gets the date of cognitive services account creation.
  disableLocalAuth:
    type: boolean
  encryption:
    type: object
    $ref: "#/types/azure-native:cognitiveservices/v20211001:EncryptionResponse"
    description: The encryption properties for this resource.
  endpoint:
    type: string
    description: Endpoint of the created account.
  endpoints:
    type: object
    additionalProperties:
      type: string
  internalId:
    type: string
    description: The internal identifier (deprecated, do not use this property).
  isMigrated:
    type: boolean
    description: If the resource is migrated from an existing key.
  migrationToken:
    type: string
    description: Resource migration token.
  networkAcls:
    type: object
    $ref: "#/types/azure-native:cognitiveservices/v20211001:NetworkRuleSetResponse"
    description: A collection of rules governing the accessibility from specific network locations.
  privateEndpointConnections:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:cognitiveservices/v20211001:PrivateEndpointConnectionResponse"
    description: The private endpoint connection associated with the Cognitive Services account.
  provisioningState:
    type: string
    description: Gets the status of the cognitive services account at the time the operation was called.
  publicNetworkAccess:
    type: string
    description: Whether or not public endpoint access is allowed for this account. Value is optional but if passed in, must be 'Enabled' or 'Disabled'
  quotaLimit:
    type: object
    $ref: "#/types/azure-native:cognitiveservices/v20211001:QuotaLimitResponse"
  restore:
    type: boolean
  restrictOutboundNetworkAccess:
    type: boolean
  skuChangeInfo:
    type: object
    $ref: "#/types/azure-native:cognitiveservices/v20211001:SkuChangeInfoResponse"
    description: Sku change info of account.
  userOwnedStorage:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:cognitiveservices/v20211001:UserOwnedStorageResponse"
    description: The storage accounts for this resource.
type: object
required:
  - callRateLimit
  - capabilities
  - dateCreated
  - endpoint
  - endpoints
  - internalId
  - isMigrated
  - privateEndpointConnections
  - provisioningState
  - quotaLimit
  - skuChangeInfo
