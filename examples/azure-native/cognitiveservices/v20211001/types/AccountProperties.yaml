$token: azure-native:cognitiveservices/v20211001:AccountProperties
description: Properties of Cognitive Services account.
properties:
  allowedFqdnList:
    type: array
    items:
      type: string
  apiProperties:
    type: object
    $ref: "#/types/azure-native:cognitiveservices/v20211001:ApiProperties"
    description: The api properties for special APIs.
  customSubDomainName:
    type: string
    description: Optional subdomain name used for token-based authentication.
  disableLocalAuth:
    type: boolean
  encryption:
    type: object
    $ref: "#/types/azure-native:cognitiveservices/v20211001:Encryption"
    description: The encryption properties for this resource.
  migrationToken:
    type: string
    description: Resource migration token.
  networkAcls:
    type: object
    $ref: "#/types/azure-native:cognitiveservices/v20211001:NetworkRuleSet"
    description: A collection of rules governing the accessibility from specific network locations.
  publicNetworkAccess:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:cognitiveservices/v20211001:PublicNetworkAccess"
    description: Whether or not public endpoint access is allowed for this account. Value is optional but if passed in, must be 'Enabled' or 'Disabled'
  restore:
    type: boolean
  restrictOutboundNetworkAccess:
    type: boolean
  userOwnedStorage:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:cognitiveservices/v20211001:UserOwnedStorage"
    description: The storage accounts for this resource.
type: object
