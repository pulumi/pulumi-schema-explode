$token: azure-native:cognitiveservices:CognitiveServicesAccountProperties
description: Properties of Cognitive Services account.
properties:
  apiProperties:
    type: object
    $ref: "#/types/azure-native:cognitiveservices:CognitiveServicesAccountApiProperties"
    description: The api properties for special APIs.
  customSubDomainName:
    type: string
    description: Optional subdomain name used for token-based authentication.
  encryption:
    type: object
    $ref: "#/types/azure-native:cognitiveservices:Encryption"
    description: The encryption properties for this resource.
  networkAcls:
    type: object
    $ref: "#/types/azure-native:cognitiveservices:NetworkRuleSet"
    description: A collection of rules governing the accessibility from specific network locations.
  privateEndpointConnections:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:cognitiveservices:PrivateEndpointConnection"
    description: The private endpoint connection associated with the Cognitive Services account.
  publicNetworkAccess:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:cognitiveservices:PublicNetworkAccess"
    description: Whether or not public endpoint access is allowed for this account. Value is optional but if passed in, must be 'Enabled' or 'Disabled'
  userOwnedStorage:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:cognitiveservices:UserOwnedStorage"
    description: The storage accounts for this resource.
type: object
