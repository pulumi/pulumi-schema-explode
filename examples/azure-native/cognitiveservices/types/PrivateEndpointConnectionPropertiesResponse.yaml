$token: azure-native:cognitiveservices:PrivateEndpointConnectionPropertiesResponse
description: Properties of the PrivateEndpointConnectProperties.
properties:
  groupIds:
    type: array
    items:
      type: string
    description: The private link resource group ids.
  privateEndpoint:
    type: object
    $ref: "#/types/azure-native:cognitiveservices:PrivateEndpointResponse"
    description: The resource of private end point.
  privateLinkServiceConnectionState:
    type: object
    $ref: "#/types/azure-native:cognitiveservices:PrivateLinkServiceConnectionStateResponse"
    description: A collection of information about the state of the connection between service consumer and provider.
type: object
required:
  - privateLinkServiceConnectionState
