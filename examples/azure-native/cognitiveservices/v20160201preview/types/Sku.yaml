$token: azure-native:cognitiveservices/v20160201preview:Sku
description: The SKU of the cognitive services account.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:cognitiveservices/v20160201preview:SkuName"
    description: Gets or sets the sku name. Required for account creation, optional for update.
type: object
required:
  - name
