$token: azure-native:testbase:PackageValidationResultResponse
description: The validation results. There's validation on package when it's created or updated.
properties:
  errors:
    type: array
    items:
      type: string
    description: Error information.
  isValid:
    type: boolean
    description: Indicates whether the package passed the validation.
  validationName:
    type: string
    description: Validation name.
type: object
required:
  - errors
  - isValid
  - validationName
