$token: azure-native:storage/v20210601:AzureFilesIdentityBasedAuthentication
description: Settings for Azure Files identity based authentication.
properties:
  activeDirectoryProperties:
    type: object
    $ref: "#/types/azure-native:storage/v20210601:ActiveDirectoryProperties"
    description: Required if choose AD.
  defaultSharePermission:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:storage/v20210601:DefaultSharePermission"
    description: Default share permission for users using Kerberos authentication if RBAC role is not assigned.
  directoryServiceOptions:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:storage/v20210601:DirectoryServiceOptions"
    description: Indicates the directory service used.
type: object
required:
  - directoryServiceOptions
