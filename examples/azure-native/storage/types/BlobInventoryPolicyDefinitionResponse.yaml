$token: azure-native:storage:BlobInventoryPolicyDefinitionResponse
description: An object that defines the blob inventory rule. Each definition consists of a set of filters.
properties:
  filters:
    type: object
    $ref: "#/types/azure-native:storage:BlobInventoryPolicyFilterResponse"
    description: An object that defines the filter set.
type: object
required:
  - filters
