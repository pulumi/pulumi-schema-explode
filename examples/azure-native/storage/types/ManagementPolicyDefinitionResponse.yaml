$token: azure-native:storage:ManagementPolicyDefinitionResponse
description: An object that defines the Lifecycle rule. Each definition is made up with a filters set and an actions set.
properties:
  actions:
    type: object
    $ref: "#/types/azure-native:storage:ManagementPolicyActionResponse"
    description: An object that defines the action set.
  filters:
    type: object
    $ref: "#/types/azure-native:storage:ManagementPolicyFilterResponse"
    description: An object that defines the filter set.
type: object
required:
  - actions
