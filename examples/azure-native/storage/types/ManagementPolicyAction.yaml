$token: azure-native:storage:ManagementPolicyAction
description: Actions are applied to the filtered blobs when the execution condition is met.
properties:
  baseBlob:
    type: object
    $ref: "#/types/azure-native:storage:ManagementPolicyBaseBlob"
    description: The management policy action for base blob
  snapshot:
    type: object
    $ref: "#/types/azure-native:storage:ManagementPolicySnapShot"
    description: The management policy action for snapshot
  version:
    type: object
    $ref: "#/types/azure-native:storage:ManagementPolicyVersion"
    description: The management policy action for version
type: object
