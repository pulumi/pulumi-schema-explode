$token: azure-native:storage/v20210401:AzureFilesIdentityBasedAuthentication
description: Settings for Azure Files identity based authentication.
properties:
  activeDirectoryProperties:
    type: object
    $ref: "#/types/azure-native:storage/v20210401:ActiveDirectoryProperties"
    description: Required if choose AD.
  defaultSharePermission:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:storage/v20210401:DefaultSharePermission"
    description: Default share permission for users using Kerberos authentication if RBAC role is not assigned.
  directoryServiceOptions:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:storage/v20210401:DirectoryServiceOptions"
    description: Indicates the directory service used.
type: object
required:
  - directoryServiceOptions
