$token: azure-native:storage/v20210901:LocalUser
description: The local user associated with the storage accounts.
properties:
  hasSharedKey:
    type: boolean
    description: Indicates whether shared key exists. Set it to false to remove existing shared key.
  hasSshKey:
    type: boolean
    description: Indicates whether ssh key exists. Set it to false to remove existing SSH key.
  hasSshPassword:
    type: boolean
    description: Indicates whether ssh password exists. Set it to false to remove existing SSH password.
  homeDirectory:
    type: string
    description: Optional, local user home directory.
  name:
    type: string
    description: The name of the resource
  permissionScopes:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:storage/v20210901:PermissionScopeResponse"
    description: The permission scopes of the local user.
  sid:
    type: string
    description: A unique Security Identifier that is generated by the server.
  sshAuthorizedKeys:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:storage/v20210901:SshPublicKeyResponse"
    description: Optional, local user ssh authorized keys for SFTP.
  systemData:
    type: object
    $ref: "#/types/azure-native:storage/v20210901:SystemDataResponse"
    description: Metadata pertaining to creation and last modification of the resource.
  type:
    type: string
    description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
type: object
required:
  - name
  - sid
  - systemData
  - type
inputProperties:
  accountName:
    type: string
    description: The name of the storage account within the specified resource group. Storage account names must be between 3 and 24 characters in length and use numbers and lower-case letters only.
  hasSharedKey:
    type: boolean
    description: Indicates whether shared key exists. Set it to false to remove existing shared key.
  hasSshKey:
    type: boolean
    description: Indicates whether ssh key exists. Set it to false to remove existing SSH key.
  hasSshPassword:
    type: boolean
    description: Indicates whether ssh password exists. Set it to false to remove existing SSH password.
  homeDirectory:
    type: string
    description: Optional, local user home directory.
  permissionScopes:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:storage/v20210901:PermissionScope"
    description: The permission scopes of the local user.
  resourceGroupName:
    type: string
    description: The name of the resource group within the user's subscription. The name is case insensitive.
  sshAuthorizedKeys:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:storage/v20210901:SshPublicKey"
    description: Optional, local user ssh authorized keys for SFTP.
  username:
    type: string
    description: The name of local user. The username must contain lowercase letters and numbers only. It must be unique only within the storage account.
requiredInputs:
  - accountName
  - resourceGroupName
aliases:
  - type: azure-native:storage:LocalUser
  - type: azure-native:storage/v20210801:LocalUser
  - type: azure-native:storage/v20220501:LocalUser
