$token: azure-native:storage/v20190401:Sku
description: The SKU of the storage account.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:storage/v20190401:SkuName"
    description: Gets or sets the SKU name. Required for account creation; optional for update. Note that in older versions, SKU name was called accountType.
  restrictions:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:storage/v20190401:Restriction"
    description: The restrictions because of which SKU cannot be used. This is empty if there are no restrictions.
type: object
required:
  - name
