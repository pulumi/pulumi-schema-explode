$token: azure-native:storage/v20220501:listLocalUserKeys
description: The Storage Account Local User keys.
inputs:
  properties:
    accountName:
      type: string
      description: The name of the storage account within the specified resource group. Storage account names must be between 3 and 24 characters in length and use numbers and lower-case letters only.
    resourceGroupName:
      type: string
      description: The name of the resource group within the user's subscription. The name is case insensitive.
    username:
      type: string
      description: The name of local user. The username must contain lowercase letters and numbers only. It must be unique only within the storage account.
  type: object
  required:
    - accountName
    - resourceGroupName
    - username
outputs:
  description: The Storage Account Local User keys.
  properties:
    sharedKey:
      type: string
      description: Auto generated by the server for SMB authentication.
    sshAuthorizedKeys:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:storage/v20220501:SshPublicKeyResponse"
      description: Optional, local user ssh authorized keys for SFTP.
  type: object
  required:
    - sharedKey
