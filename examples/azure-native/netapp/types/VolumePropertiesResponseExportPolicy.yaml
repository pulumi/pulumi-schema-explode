$token: azure-native:netapp:VolumePropertiesResponseExportPolicy
description: Set of export policy rules
properties:
  rules:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:netapp:ExportPolicyRuleResponse"
    description: Export policy rule
type: object
