$token: azure-native:netapp:VolumePropertiesDataProtection
description: DataProtection type volumes include an object containing details of the replication
properties:
  backup:
    type: object
    $ref: "#/types/azure-native:netapp:VolumeBackupProperties"
    description: Backup Properties
  replication:
    type: object
    $ref: "#/types/azure-native:netapp:ReplicationObject"
    description: Replication properties
  snapshot:
    type: object
    $ref: "#/types/azure-native:netapp:VolumeSnapshotProperties"
    description: Snapshot properties.
type: object
