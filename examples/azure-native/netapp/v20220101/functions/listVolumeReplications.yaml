$token: azure-native:netapp/v20220101:listVolumeReplications
description: List Replications
inputs:
  properties:
    accountName:
      type: string
      description: The name of the NetApp account
    poolName:
      type: string
      description: The name of the capacity pool
    resourceGroupName:
      type: string
      description: The name of the resource group.
    volumeName:
      type: string
      description: The name of the volume
  type: object
  required:
    - accountName
    - poolName
    - resourceGroupName
    - volumeName
outputs:
  description: List Replications
  properties:
    value:
      type: array
      items:
        type: object
        $ref: "#/types/azure-native:netapp/v20220101:ReplicationResponse"
      description: A list of replications
  type: object
