$token: azure-native:netapp/v20200501:MountTargetPropertiesResponse
description: Mount target properties
properties:
  fileSystemId:
    type: string
    description: UUID v4 used to identify the MountTarget
  ipAddress:
    type: string
    description: The mount target's IPv4 address
  mountTargetId:
    type: string
    description: UUID v4 used to identify the MountTarget
  smbServerFqdn:
    type: string
    description: The SMB server's Fully Qualified Domain Name, FQDN
type: object
required:
  - fileSystemId
  - ipAddress
  - mountTargetId
