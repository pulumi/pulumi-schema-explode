$token: azure-native:peering/v20190801preview:PeeringSku
description: The SKU that defines the tier and kind of the peering.
properties:
  family:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:peering/v20190801preview:Family"
    description: The family of the peering SKU.
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:peering/v20190801preview:Name"
    description: The name of the peering SKU.
  size:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:peering/v20190801preview:Size"
    description: The size of the peering SKU.
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:peering/v20190801preview:Tier"
    description: The tier of the peering SKU.
type: object
