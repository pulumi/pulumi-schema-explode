$token: azure-native:peering:PeeringPropertiesExchange
description: The properties that define an exchange peering.
properties:
  connections:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:peering:ExchangeConnection"
    description: The set of connections that constitute an exchange peering.
  peerAsn:
    type: object
    $ref: "#/types/azure-native:peering:SubResource"
    description: The reference of the peer ASN.
type: object
