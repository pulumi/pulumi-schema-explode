$token: azure-native:peering:PeeringSku
description: The SKU that defines the tier and kind of the peering.
properties:
  family:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:peering:Family"
    description: The family of the peering SKU.
  name:
    type: string
    description: The name of the peering SKU.
  size:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:peering:Size"
    description: The size of the peering SKU.
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:peering:Tier"
    description: The tier of the peering SKU.
type: object
