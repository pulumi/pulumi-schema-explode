$token: azure-native:peering:PeeringPropertiesExchangeResponse
description: The properties that define an exchange peering.
properties:
  connections:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:peering:ExchangeConnectionResponse"
    description: The set of connections that constitute an exchange peering.
  peerAsn:
    type: object
    $ref: "#/types/azure-native:peering:SubResourceResponse"
    description: The reference of the peer ASN.
type: object
