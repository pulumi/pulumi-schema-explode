$token: azure-native:peering/v20201001:PeeringSku
description: The SKU that defines the tier and kind of the peering.
properties:
  family:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:peering/v20201001:Family"
    description: The family of the peering SKU.
  name:
    type: string
    description: The name of the peering SKU.
  size:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:peering/v20201001:Size"
    description: The size of the peering SKU.
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:peering/v20201001:Tier"
    description: The tier of the peering SKU.
type: object
