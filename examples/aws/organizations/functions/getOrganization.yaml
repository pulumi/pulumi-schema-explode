$token: aws:organizations/getOrganization:getOrganization
outputs:
  description: |
    A collection of values returned by getOrganization.
  properties:
    accounts:
      type: array
      items:
        $ref: "#/types/aws:organizations/getOrganizationAccount:getOrganizationAccount"
      description: |
        List of organization accounts including the master account. For a list excluding the master account, see the `non_master_accounts` attribute. All elements have these attributes:
    arn:
      type: string
      description: |
        ARN of the root
    awsServiceAccessPrincipals:
      type: array
      items:
        type: string
      description: |
        A list of AWS service principal names that have integration enabled with your organization. Organization must have `feature_set` set to `ALL`. For additional information, see the [AWS Organizations User Guide](https://docs.aws.amazon.com/organizations/latest/userguide/orgs_integrate_services.html).
    enabledPolicyTypes:
      type: array
      items:
        type: string
      description: |
        A list of Organizations policy types that are enabled in the Organization Root. Organization must have `feature_set` set to `ALL`. For additional information about valid policy types (e.g., `SERVICE_CONTROL_POLICY`), see the [AWS Organizations API Reference](https://docs.aws.amazon.com/organizations/latest/APIReference/API_EnablePolicyType.html).
    featureSet:
      type: string
      description: |
        FeatureSet of the organization.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    masterAccountArn:
      type: string
      description: |
        ARN of the account that is designated as the master account for the organization.
    masterAccountEmail:
      type: string
      description: |
        The email address that is associated with the AWS account that is designated as the master account for the organization.
    masterAccountId:
      type: string
      description: |
        Unique identifier (ID) of the master account of an organization.
    nonMasterAccounts:
      type: array
      items:
        $ref: "#/types/aws:organizations/getOrganizationNonMasterAccount:getOrganizationNonMasterAccount"
      description: |
        List of organization accounts excluding the master account. For a list including the master account, see the `accounts` attribute. All elements have these attributes:
    roots:
      type: array
      items:
        $ref: "#/types/aws:organizations/getOrganizationRoot:getOrganizationRoot"
      description: |
        List of organization roots. All elements have these attributes:
  type: object
  required:
    - accounts
    - arn
    - awsServiceAccessPrincipals
    - enabledPolicyTypes
    - featureSet
    - masterAccountArn
    - masterAccountEmail
    - masterAccountId
    - nonMasterAccounts
    - roots
    - id
