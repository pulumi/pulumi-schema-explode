$token: aws:organizations/getDelegatedServices:getDelegatedServices
inputs:
  description: |
    A collection of arguments for invoking getDelegatedServices.
  properties:
    accountId:
      type: string
      description: |
        Account ID number of a delegated administrator account in the organization.
  type: object
  required:
    - accountId
outputs:
  description: |
    A collection of values returned by getDelegatedServices.
  properties:
    accountId:
      type: string
    delegatedServices:
      type: array
      items:
        $ref: "#/types/aws:organizations/getDelegatedServicesDelegatedService:getDelegatedServicesDelegatedService"
      description: |
        Services for which the account is a delegated administrator, which have the following attributes:
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
  type: object
  required:
    - accountId
    - delegatedServices
    - id
