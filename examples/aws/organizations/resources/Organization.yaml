$token: aws:organizations/organization:Organization
properties:
  accounts:
    type: array
    items:
      $ref: "#/types/aws:organizations/OrganizationAccount:OrganizationAccount"
    description: |
      List of organization accounts including the master account. For a list excluding the master account, see the `non_master_accounts` attribute. All elements have these attributes:
  arn:
    type: string
    description: |
      ARN of the root
  awsServiceAccessPrincipals:
    type: array
    items:
      type: string
    description: |
      List of AWS service principal names for which you want to enable integration with your organization. This is typically in the form of a URL, such as service-abbreviation.amazonaws.com. Organization must have `feature_set` set to `ALL`. For additional information, see the [AWS Organizations User Guide](https://docs.aws.amazon.com/organizations/latest/userguide/orgs_integrate_services.html).
  enabledPolicyTypes:
    type: array
    items:
      type: string
    description: |
      List of Organizations policy types to enable in the Organization Root. Organization must have `feature_set` set to `ALL`. For additional information about valid policy types (e.g., `AISERVICES_OPT_OUT_POLICY`, `BACKUP_POLICY`, `SERVICE_CONTROL_POLICY`, and `TAG_POLICY`), see the [AWS Organizations API Reference](https://docs.aws.amazon.com/organizations/latest/APIReference/API_EnablePolicyType.html).
  featureSet:
    type: string
    description: |
      Specify "ALL" (default) or "CONSOLIDATED_BILLING".
  masterAccountArn:
    type: string
    description: |
      ARN of the master account
  masterAccountEmail:
    type: string
    description: |
      Email address of the master account
  masterAccountId:
    type: string
    description: |
      Identifier of the master account
  nonMasterAccounts:
    type: array
    items:
      $ref: "#/types/aws:organizations/OrganizationNonMasterAccount:OrganizationNonMasterAccount"
    description: |
      List of organization accounts excluding the master account. For a list including the master account, see the `accounts` attribute. All elements have these attributes:
  roots:
    type: array
    items:
      $ref: "#/types/aws:organizations/OrganizationRoot:OrganizationRoot"
    description: |
      List of organization roots. All elements have these attributes:
required:
  - accounts
  - arn
  - masterAccountArn
  - masterAccountEmail
  - masterAccountId
  - nonMasterAccounts
  - roots
inputProperties:
  awsServiceAccessPrincipals:
    type: array
    items:
      type: string
    description: |
      List of AWS service principal names for which you want to enable integration with your organization. This is typically in the form of a URL, such as service-abbreviation.amazonaws.com. Organization must have `feature_set` set to `ALL`. For additional information, see the [AWS Organizations User Guide](https://docs.aws.amazon.com/organizations/latest/userguide/orgs_integrate_services.html).
  enabledPolicyTypes:
    type: array
    items:
      type: string
    description: |
      List of Organizations policy types to enable in the Organization Root. Organization must have `feature_set` set to `ALL`. For additional information about valid policy types (e.g., `AISERVICES_OPT_OUT_POLICY`, `BACKUP_POLICY`, `SERVICE_CONTROL_POLICY`, and `TAG_POLICY`), see the [AWS Organizations API Reference](https://docs.aws.amazon.com/organizations/latest/APIReference/API_EnablePolicyType.html).
  featureSet:
    type: string
    description: |
      Specify "ALL" (default) or "CONSOLIDATED_BILLING".
stateInputs:
  description: |
    Input properties used for looking up and filtering Organization resources.
  properties:
    accounts:
      type: array
      items:
        $ref: "#/types/aws:organizations/OrganizationAccount:OrganizationAccount"
      description: |
        List of organization accounts including the master account. For a list excluding the master account, see the `non_master_accounts` attribute. All elements have these attributes:
    arn:
      type: string
      description: |
        ARN of the root
    awsServiceAccessPrincipals:
      type: array
      items:
        type: string
      description: |
        List of AWS service principal names for which you want to enable integration with your organization. This is typically in the form of a URL, such as service-abbreviation.amazonaws.com. Organization must have `feature_set` set to `ALL`. For additional information, see the [AWS Organizations User Guide](https://docs.aws.amazon.com/organizations/latest/userguide/orgs_integrate_services.html).
    enabledPolicyTypes:
      type: array
      items:
        type: string
      description: |
        List of Organizations policy types to enable in the Organization Root. Organization must have `feature_set` set to `ALL`. For additional information about valid policy types (e.g., `AISERVICES_OPT_OUT_POLICY`, `BACKUP_POLICY`, `SERVICE_CONTROL_POLICY`, and `TAG_POLICY`), see the [AWS Organizations API Reference](https://docs.aws.amazon.com/organizations/latest/APIReference/API_EnablePolicyType.html).
    featureSet:
      type: string
      description: |
        Specify "ALL" (default) or "CONSOLIDATED_BILLING".
    masterAccountArn:
      type: string
      description: |
        ARN of the master account
    masterAccountEmail:
      type: string
      description: |
        Email address of the master account
    masterAccountId:
      type: string
      description: |
        Identifier of the master account
    nonMasterAccounts:
      type: array
      items:
        $ref: "#/types/aws:organizations/OrganizationNonMasterAccount:OrganizationNonMasterAccount"
      description: |
        List of organization accounts excluding the master account. For a list including the master account, see the `accounts` attribute. All elements have these attributes:
    roots:
      type: array
      items:
        $ref: "#/types/aws:organizations/OrganizationRoot:OrganizationRoot"
      description: |
        List of organization roots. All elements have these attributes:
  type: object
