$token: aws:organizations/organizationalUnit:OrganizationalUnit
properties:
  accounts:
    type: array
    items:
      $ref: "#/types/aws:organizations/OrganizationalUnitAccount:OrganizationalUnitAccount"
    description: |
      List of child accounts for this Organizational Unit. Does not return account information for child Organizational Units. All elements have these attributes:
  arn:
    type: string
    description: |
      ARN of the organizational unit
  name:
    type: string
    description: |
      The name for the organizational unit
  parentId:
    type: string
    description: |
      ID of the parent organizational unit, which may be the root
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - accounts
  - arn
  - name
  - parentId
  - tagsAll
inputProperties:
  name:
    type: string
    description: |
      The name for the organizational unit
  parentId:
    type: string
    description: |
      ID of the parent organizational unit, which may be the root
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - parentId
stateInputs:
  description: |
    Input properties used for looking up and filtering OrganizationalUnit resources.
  properties:
    accounts:
      type: array
      items:
        $ref: "#/types/aws:organizations/OrganizationalUnitAccount:OrganizationalUnitAccount"
      description: |
        List of child accounts for this Organizational Unit. Does not return account information for child Organizational Units. All elements have these attributes:
    arn:
      type: string
      description: |
        ARN of the organizational unit
    name:
      type: string
      description: |
        The name for the organizational unit
    parentId:
      type: string
      description: |
        ID of the parent organizational unit, which may be the root
      willReplaceOnChanges: true
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
