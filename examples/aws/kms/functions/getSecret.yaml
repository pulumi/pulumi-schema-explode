$token: aws:kms/getSecret:getSecret
inputs:
  description: |
    A collection of arguments for invoking getSecret.
  properties:
    secrets:
      type: array
      items:
        $ref: "#/types/aws:kms/getSecretSecret:getSecretSecret"
  type: object
  required:
    - secrets
outputs:
  description: |
    A collection of values returned by getSecret.
  properties:
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    secrets:
      type: array
      items:
        $ref: "#/types/aws:kms/getSecretSecret:getSecretSecret"
  type: object
  required:
    - secrets
    - id
