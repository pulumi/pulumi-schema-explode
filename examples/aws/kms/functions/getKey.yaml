$token: aws:kms/getKey:getKey
inputs:
  description: |
    A collection of arguments for invoking getKey.
  properties:
    grantTokens:
      type: array
      items:
        type: string
      description: |
        List of grant tokens
    keyId:
      type: string
      description: |
        Key identifier which can be one of the following format:
        * Key ID. E.g: `1234abcd-12ab-34cd-56ef-1234567890ab`
        * Key ARN. E.g.: `arn:aws:kms:us-east-1:111122223333:key/1234abcd-12ab-34cd-56ef-1234567890ab`
        * Alias name. E.g.: `alias/my-key`
        * Alias ARN: E.g.: `arn:aws:kms:us-east-1:111122223333:alias/my-key`
  type: object
  required:
    - keyId
outputs:
  description: |
    A collection of values returned by getKey.
  properties:
    arn:
      type: string
    awsAccountId:
      type: string
    creationDate:
      type: string
    customerMasterKeySpec:
      type: string
    deletionDate:
      type: string
    description:
      type: string
    enabled:
      type: boolean
    expirationModel:
      type: string
    grantTokens:
      type: array
      items:
        type: string
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    keyId:
      type: string
    keyManager:
      type: string
    keyState:
      type: string
    keyUsage:
      type: string
    multiRegion:
      type: boolean
    multiRegionConfigurations:
      type: array
      items:
        $ref: "#/types/aws:kms/getKeyMultiRegionConfiguration:getKeyMultiRegionConfiguration"
    origin:
      type: string
    validTo:
      type: string
  type: object
  required:
    - arn
    - awsAccountId
    - creationDate
    - customerMasterKeySpec
    - deletionDate
    - description
    - enabled
    - expirationModel
    - keyId
    - keyManager
    - keyState
    - keyUsage
    - multiRegion
    - multiRegionConfigurations
    - origin
    - validTo
    - id
