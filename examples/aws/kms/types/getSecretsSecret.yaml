$token: aws:kms/getSecretsSecret:getSecretsSecret
properties:
  context:
    type: object
    additionalProperties:
      type: string
    description: |
      An optional mapping that makes up the Encryption Context for the secret.
    language:
      python:
        mapCase: false
  grantTokens:
    type: array
    items:
      type: string
    description: |
      An optional list of Grant Tokens for the secret.
    language:
      python:
        mapCase: false
  name:
    type: string
    description: |
      Name to export this secret under in the attributes.
    language:
      python:
        mapCase: false
  payload:
    type: string
    description: |
      Base64 encoded payload, as returned from a KMS encrypt operation.
    language:
      python:
        mapCase: false
type: object
required:
  - name
  - payload
