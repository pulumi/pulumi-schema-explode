$token: aws:dms/EndpointRedshiftSettings:EndpointRedshiftSettings
properties:
  bucketFolder:
    type: string
    description: |
      S3 object prefix.
    language:
      python:
        mapCase: false
  bucketName:
    type: string
    description: |
      S3 bucket name.
    language:
      python:
        mapCase: false
  encryptionMode:
    type: string
    description: |
      Server-side encryption mode that you want to encrypt your .csv or .parquet object files copied to S3. Valid values are `SSE_S3` and `SSE_KMS`. Default is `SSE_S3`.
    language:
      python:
        mapCase: false
  serverSideEncryptionKmsKeyId:
    type: string
    description: |
      If you set encryptionMode to `SSE_KMS`, set this parameter to the ARN for the AWS KMS key.
    language:
      python:
        mapCase: false
  serviceAccessRoleArn:
    type: string
    description: |
      ARN of the IAM Role with permissions to read from or write to the S3 Bucket.
    language:
      python:
        mapCase: false
type: object
