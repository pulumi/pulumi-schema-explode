$token: aws:acmpca/CertificateAuthorityRevocationConfigurationOcspConfiguration:CertificateAuthorityRevocationConfigurationOcspConfiguration
properties:
  enabled:
    type: boolean
    description: |
      Boolean value that specifies whether a custom OCSP responder is enabled.
    language:
      python:
        mapCase: false
  ocspCustomCname:
    type: string
    description: |
      CNAME specifying a customized OCSP domain. Note: The value of the CNAME must not include a protocol prefix such as "http://" or "https://".
    language:
      python:
        mapCase: false
type: object
required:
  - enabled
