$token: aws:acmpca/certificate:Certificate
properties:
  arn:
    type: string
    description: |
      ARN of the certificate.
  certificate:
    type: string
    description: |
      PEM-encoded certificate value.
    language:
      csharp:
        name: CertificateDetails
  certificateAuthorityArn:
    type: string
    description: |
      ARN of the certificate authority.
  certificateChain:
    type: string
    description: |
      PEM-encoded certificate chain that includes any intermediate certificates and chains up to root CA.
  certificateSigningRequest:
    type: string
    description: |
      Certificate Signing Request in PEM format.
  signingAlgorithm:
    type: string
    description: |
      Algorithm to use to sign certificate requests. Valid values: `SHA256WITHRSA`, `SHA256WITHECDSA`, `SHA384WITHRSA`, `SHA384WITHECDSA`, `SHA512WITHRSA`, `SHA512WITHECDSA`
  templateArn:
    type: string
    description: |
      Template to use when issuing a certificate. See [ACM PCA Documentation](https://docs.aws.amazon.com/acm-pca/latest/userguide/UsingTemplates.html) for more information.
  validity:
    $ref: "#/types/aws:acmpca/CertificateValidity:CertificateValidity"
    description: |
      Configures end of the validity period for the certificate. See validity block below.
required:
  - arn
  - certificate
  - certificateAuthorityArn
  - certificateChain
  - certificateSigningRequest
  - signingAlgorithm
  - validity
inputProperties:
  certificateAuthorityArn:
    type: string
    description: |
      ARN of the certificate authority.
    willReplaceOnChanges: true
  certificateSigningRequest:
    type: string
    description: |
      Certificate Signing Request in PEM format.
    willReplaceOnChanges: true
  signingAlgorithm:
    type: string
    description: |
      Algorithm to use to sign certificate requests. Valid values: `SHA256WITHRSA`, `SHA256WITHECDSA`, `SHA384WITHRSA`, `SHA384WITHECDSA`, `SHA512WITHRSA`, `SHA512WITHECDSA`
    willReplaceOnChanges: true
  templateArn:
    type: string
    description: |
      Template to use when issuing a certificate. See [ACM PCA Documentation](https://docs.aws.amazon.com/acm-pca/latest/userguide/UsingTemplates.html) for more information.
    willReplaceOnChanges: true
  validity:
    $ref: "#/types/aws:acmpca/CertificateValidity:CertificateValidity"
    description: |
      Configures end of the validity period for the certificate. See validity block below.
    willReplaceOnChanges: true
requiredInputs:
  - certificateAuthorityArn
  - certificateSigningRequest
  - signingAlgorithm
  - validity
stateInputs:
  description: |
    Input properties used for looking up and filtering Certificate resources.
  properties:
    arn:
      type: string
      description: |
        ARN of the certificate.
    certificate:
      type: string
      description: |
        PEM-encoded certificate value.
      language:
        csharp:
          name: CertificateDetails
    certificateAuthorityArn:
      type: string
      description: |
        ARN of the certificate authority.
      willReplaceOnChanges: true
    certificateChain:
      type: string
      description: |
        PEM-encoded certificate chain that includes any intermediate certificates and chains up to root CA.
    certificateSigningRequest:
      type: string
      description: |
        Certificate Signing Request in PEM format.
      willReplaceOnChanges: true
    signingAlgorithm:
      type: string
      description: |
        Algorithm to use to sign certificate requests. Valid values: `SHA256WITHRSA`, `SHA256WITHECDSA`, `SHA384WITHRSA`, `SHA384WITHECDSA`, `SHA512WITHRSA`, `SHA512WITHECDSA`
      willReplaceOnChanges: true
    templateArn:
      type: string
      description: |
        Template to use when issuing a certificate. See [ACM PCA Documentation](https://docs.aws.amazon.com/acm-pca/latest/userguide/UsingTemplates.html) for more information.
      willReplaceOnChanges: true
    validity:
      $ref: "#/types/aws:acmpca/CertificateValidity:CertificateValidity"
      description: |
        Configures end of the validity period for the certificate. See validity block below.
      willReplaceOnChanges: true
  type: object
