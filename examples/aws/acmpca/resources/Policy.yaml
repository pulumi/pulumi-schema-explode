$token: aws:acmpca/policy:Policy
properties:
  policy:
    type: string
    description: |
      JSON-formatted IAM policy to attach to the specified private CA resource.
    language:
      csharp:
        name: PolicyDetails
  resourceArn:
    type: string
    description: |
      ARN of the private CA to associate with the policy.
required:
  - policy
  - resourceArn
inputProperties:
  policy:
    type: string
    description: |
      JSON-formatted IAM policy to attach to the specified private CA resource.
    language:
      csharp:
        name: PolicyDetails
  resourceArn:
    type: string
    description: |
      ARN of the private CA to associate with the policy.
    willReplaceOnChanges: true
requiredInputs:
  - policy
  - resourceArn
stateInputs:
  description: |
    Input properties used for looking up and filtering Policy resources.
  properties:
    policy:
      type: string
      description: |
        JSON-formatted IAM policy to attach to the specified private CA resource.
      language:
        csharp:
          name: PolicyDetails
    resourceArn:
      type: string
      description: |
        ARN of the private CA to associate with the policy.
      willReplaceOnChanges: true
  type: object
