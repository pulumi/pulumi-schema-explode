$token: aws:acmpca/certificateAuthorityCertificate:CertificateAuthorityCertificate
properties:
  certificate:
    type: string
    description: |
      PEM-encoded certificate for the Certificate Authority.
  certificateAuthorityArn:
    type: string
    description: |
      ARN of the Certificate Authority.
  certificateChain:
    type: string
    description: |
      PEM-encoded certificate chain that includes any intermediate certificates and chains up to root CA. Required for subordinate Certificate Authorities. Not allowed for root Certificate Authorities.
required:
  - certificate
  - certificateAuthorityArn
inputProperties:
  certificate:
    type: string
    description: |
      PEM-encoded certificate for the Certificate Authority.
    willReplaceOnChanges: true
  certificateAuthorityArn:
    type: string
    description: |
      ARN of the Certificate Authority.
    willReplaceOnChanges: true
  certificateChain:
    type: string
    description: |
      PEM-encoded certificate chain that includes any intermediate certificates and chains up to root CA. Required for subordinate Certificate Authorities. Not allowed for root Certificate Authorities.
    willReplaceOnChanges: true
requiredInputs:
  - certificate
  - certificateAuthorityArn
stateInputs:
  description: |
    Input properties used for looking up and filtering CertificateAuthorityCertificate resources.
  properties:
    certificate:
      type: string
      description: |
        PEM-encoded certificate for the Certificate Authority.
      willReplaceOnChanges: true
    certificateAuthorityArn:
      type: string
      description: |
        ARN of the Certificate Authority.
      willReplaceOnChanges: true
    certificateChain:
      type: string
      description: |
        PEM-encoded certificate chain that includes any intermediate certificates and chains up to root CA. Required for subordinate Certificate Authorities. Not allowed for root Certificate Authorities.
      willReplaceOnChanges: true
  type: object
