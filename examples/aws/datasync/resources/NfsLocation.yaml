$token: aws:datasync/nfsLocation:NfsLocation
properties:
  arn:
    type: string
    description: |
      Amazon Resource Name (ARN) of the DataSync Location.
  mountOptions:
    $ref: "#/types/aws:datasync/NfsLocationMountOptions:NfsLocationMountOptions"
    description: |
      Configuration block containing mount options used by DataSync to access the NFS Server.
  onPremConfig:
    $ref: "#/types/aws:datasync/NfsLocationOnPremConfig:NfsLocationOnPremConfig"
    description: |
      Configuration block containing information for connecting to the NFS File System.
  serverHostname:
    type: string
    description: |
      Specifies the IP address or DNS name of the NFS server. The DataSync Agent(s) use this to mount the NFS server.
  subdirectory:
    type: string
    description: |
      Subdirectory to perform actions as source or destination. Should be exported by the NFS server.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value pairs of resource tags to assign to the DataSync Location. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  uri:
    type: string
required:
  - arn
  - onPremConfig
  - serverHostname
  - subdirectory
  - tagsAll
  - uri
inputProperties:
  mountOptions:
    $ref: "#/types/aws:datasync/NfsLocationMountOptions:NfsLocationMountOptions"
    description: |
      Configuration block containing mount options used by DataSync to access the NFS Server.
  onPremConfig:
    $ref: "#/types/aws:datasync/NfsLocationOnPremConfig:NfsLocationOnPremConfig"
    description: |
      Configuration block containing information for connecting to the NFS File System.
  serverHostname:
    type: string
    description: |
      Specifies the IP address or DNS name of the NFS server. The DataSync Agent(s) use this to mount the NFS server.
    willReplaceOnChanges: true
  subdirectory:
    type: string
    description: |
      Subdirectory to perform actions as source or destination. Should be exported by the NFS server.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value pairs of resource tags to assign to the DataSync Location. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - onPremConfig
  - serverHostname
  - subdirectory
stateInputs:
  description: |
    Input properties used for looking up and filtering NfsLocation resources.
  properties:
    arn:
      type: string
      description: |
        Amazon Resource Name (ARN) of the DataSync Location.
    mountOptions:
      $ref: "#/types/aws:datasync/NfsLocationMountOptions:NfsLocationMountOptions"
      description: |
        Configuration block containing mount options used by DataSync to access the NFS Server.
    onPremConfig:
      $ref: "#/types/aws:datasync/NfsLocationOnPremConfig:NfsLocationOnPremConfig"
      description: |
        Configuration block containing information for connecting to the NFS File System.
    serverHostname:
      type: string
      description: |
        Specifies the IP address or DNS name of the NFS server. The DataSync Agent(s) use this to mount the NFS server.
      willReplaceOnChanges: true
    subdirectory:
      type: string
      description: |
        Subdirectory to perform actions as source or destination. Should be exported by the NFS server.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value pairs of resource tags to assign to the DataSync Location. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    uri:
      type: string
  type: object
