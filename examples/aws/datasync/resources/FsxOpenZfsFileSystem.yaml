$token: aws:datasync/fsxOpenZfsFileSystem:FsxOpenZfsFileSystem
properties:
  arn:
    type: string
    description: |
      Amazon Resource Name (ARN) of the DataSync Location.
  creationTime:
    type: string
    description: |
      The time that the FSx for openzfs location was created.
  fsxFilesystemArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) for the FSx for OpenZfs file system.
  protocol:
    $ref: "#/types/aws:datasync/FsxOpenZfsFileSystemProtocol:FsxOpenZfsFileSystemProtocol"
    description: |
      The type of protocol that DataSync uses to access your file system. See below.
  securityGroupArns:
    type: array
    items:
      type: string
    description: |
      The Amazon Resource Names (ARNs) of the security groups that are to use to configure the FSx for openzfs file system.
  subdirectory:
    type: string
    description: |
      Subdirectory to perform actions as source or destination. Must start with `/fsx`.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value pairs of resource tags to assign to the DataSync Location. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  uri:
    type: string
    description: |
      The URL of the FSx for openzfs location that was described.
required:
  - arn
  - creationTime
  - fsxFilesystemArn
  - protocol
  - securityGroupArns
  - subdirectory
  - tagsAll
  - uri
inputProperties:
  fsxFilesystemArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) for the FSx for OpenZfs file system.
    willReplaceOnChanges: true
  protocol:
    $ref: "#/types/aws:datasync/FsxOpenZfsFileSystemProtocol:FsxOpenZfsFileSystemProtocol"
    description: |
      The type of protocol that DataSync uses to access your file system. See below.
    willReplaceOnChanges: true
  securityGroupArns:
    type: array
    items:
      type: string
    description: |
      The Amazon Resource Names (ARNs) of the security groups that are to use to configure the FSx for openzfs file system.
    willReplaceOnChanges: true
  subdirectory:
    type: string
    description: |
      Subdirectory to perform actions as source or destination. Must start with `/fsx`.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value pairs of resource tags to assign to the DataSync Location. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - fsxFilesystemArn
  - protocol
  - securityGroupArns
stateInputs:
  description: |
    Input properties used for looking up and filtering FsxOpenZfsFileSystem resources.
  properties:
    arn:
      type: string
      description: |
        Amazon Resource Name (ARN) of the DataSync Location.
    creationTime:
      type: string
      description: |
        The time that the FSx for openzfs location was created.
    fsxFilesystemArn:
      type: string
      description: |
        The Amazon Resource Name (ARN) for the FSx for OpenZfs file system.
      willReplaceOnChanges: true
    protocol:
      $ref: "#/types/aws:datasync/FsxOpenZfsFileSystemProtocol:FsxOpenZfsFileSystemProtocol"
      description: |
        The type of protocol that DataSync uses to access your file system. See below.
      willReplaceOnChanges: true
    securityGroupArns:
      type: array
      items:
        type: string
      description: |
        The Amazon Resource Names (ARNs) of the security groups that are to use to configure the FSx for openzfs file system.
      willReplaceOnChanges: true
    subdirectory:
      type: string
      description: |
        Subdirectory to perform actions as source or destination. Must start with `/fsx`.
      willReplaceOnChanges: true
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value pairs of resource tags to assign to the DataSync Location. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    uri:
      type: string
      description: |
        The URL of the FSx for openzfs location that was described.
  type: object
