$token: aws:cognito/getUserPoolClients:getUserPoolClients
inputs:
  description: |
    A collection of arguments for invoking getUserPoolClients.
  properties:
    userPoolId:
      type: string
      description: |
        Cognito user pool ID.
  type: object
  required:
    - userPoolId
outputs:
  description: |
    A collection of values returned by getUserPoolClients.
  properties:
    clientIds:
      type: array
      items:
        type: string
      description: |
        List of Cognito user pool client IDs.
    clientNames:
      type: array
      items:
        type: string
      description: |
        List of Cognito user pool client names.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    userPoolId:
      type: string
  type: object
  required:
    - clientIds
    - clientNames
    - userPoolId
    - id
