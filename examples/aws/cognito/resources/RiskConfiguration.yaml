$token: aws:cognito/riskConfiguration:RiskConfiguration
properties:
  accountTakeoverRiskConfiguration:
    $ref: "#/types/aws:cognito/RiskConfigurationAccountTakeoverRiskConfiguration:RiskConfigurationAccountTakeoverRiskConfiguration"
    description: |
      The account takeover risk configuration. See details below.
  clientId:
    type: string
    description: |
      The app client ID. When the client ID is not provided, the same risk configuration is applied to all the clients in the User Pool.
  compromisedCredentialsRiskConfiguration:
    $ref: "#/types/aws:cognito/RiskConfigurationCompromisedCredentialsRiskConfiguration:RiskConfigurationCompromisedCredentialsRiskConfiguration"
    description: |
      The compromised credentials risk configuration. See details below.
  riskExceptionConfiguration:
    $ref: "#/types/aws:cognito/RiskConfigurationRiskExceptionConfiguration:RiskConfigurationRiskExceptionConfiguration"
    description: |
      The configuration to override the risk decision. See details below.
  userPoolId:
    type: string
    description: |
      The user pool ID.
required:
  - userPoolId
inputProperties:
  accountTakeoverRiskConfiguration:
    $ref: "#/types/aws:cognito/RiskConfigurationAccountTakeoverRiskConfiguration:RiskConfigurationAccountTakeoverRiskConfiguration"
    description: |
      The account takeover risk configuration. See details below.
  clientId:
    type: string
    description: |
      The app client ID. When the client ID is not provided, the same risk configuration is applied to all the clients in the User Pool.
    willReplaceOnChanges: true
  compromisedCredentialsRiskConfiguration:
    $ref: "#/types/aws:cognito/RiskConfigurationCompromisedCredentialsRiskConfiguration:RiskConfigurationCompromisedCredentialsRiskConfiguration"
    description: |
      The compromised credentials risk configuration. See details below.
  riskExceptionConfiguration:
    $ref: "#/types/aws:cognito/RiskConfigurationRiskExceptionConfiguration:RiskConfigurationRiskExceptionConfiguration"
    description: |
      The configuration to override the risk decision. See details below.
  userPoolId:
    type: string
    description: |
      The user pool ID.
    willReplaceOnChanges: true
requiredInputs:
  - userPoolId
stateInputs:
  description: |
    Input properties used for looking up and filtering RiskConfiguration resources.
  properties:
    accountTakeoverRiskConfiguration:
      $ref: "#/types/aws:cognito/RiskConfigurationAccountTakeoverRiskConfiguration:RiskConfigurationAccountTakeoverRiskConfiguration"
      description: |
        The account takeover risk configuration. See details below.
    clientId:
      type: string
      description: |
        The app client ID. When the client ID is not provided, the same risk configuration is applied to all the clients in the User Pool.
      willReplaceOnChanges: true
    compromisedCredentialsRiskConfiguration:
      $ref: "#/types/aws:cognito/RiskConfigurationCompromisedCredentialsRiskConfiguration:RiskConfigurationCompromisedCredentialsRiskConfiguration"
      description: |
        The compromised credentials risk configuration. See details below.
    riskExceptionConfiguration:
      $ref: "#/types/aws:cognito/RiskConfigurationRiskExceptionConfiguration:RiskConfigurationRiskExceptionConfiguration"
      description: |
        The configuration to override the risk decision. See details below.
    userPoolId:
      type: string
      description: |
        The user pool ID.
      willReplaceOnChanges: true
  type: object
