$token: aws:cognito/getUserPoolClientAnalyticsConfiguration:getUserPoolClientAnalyticsConfiguration
properties:
  applicationArn:
    type: string
    description: |
      (Optional) Application ARN for an Amazon Pinpoint application. Conflicts with `external_id` and `role_arn`.
    language:
      python:
        mapCase: false
  applicationId:
    type: string
    description: |
      (Optional) Application ID for an Amazon Pinpoint application.
    language:
      python:
        mapCase: false
  externalId:
    type: string
    description: |
      (Optional) ID for the Analytics Configuration. Conflicts with `application_arn`.
    language:
      python:
        mapCase: false
  roleArn:
    type: string
    description: |
      (Optional) ARN of an IAM role that authorizes Amazon Cognito to publish events to Amazon Pinpoint analytics. Conflicts with `application_arn`.
      * `user_data_shared` (Optional) If set to `true`, Amazon Cognito will include user data in the events it publishes to Amazon Pinpoint analytics.
    language:
      python:
        mapCase: false
  userDataShared:
    type: boolean
    language:
      python:
        mapCase: false
type: object
required:
  - applicationArn
  - applicationId
  - externalId
  - roleArn
  - userDataShared
language:
  nodejs:
    requiredInputs: []
