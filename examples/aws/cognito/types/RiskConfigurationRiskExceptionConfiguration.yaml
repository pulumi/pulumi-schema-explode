$token: aws:cognito/RiskConfigurationRiskExceptionConfiguration:RiskConfigurationRiskExceptionConfiguration
properties:
  blockedIpRangeLists:
    type: array
    items:
      type: string
    description: |
      Overrides the risk decision to always block the pre-authentication requests. The IP range is in CIDR notation, a compact representation of an IP address and its routing prefix.
    language:
      python:
        mapCase: false
  skippedIpRangeLists:
    type: array
    items:
      type: string
    description: |
      Risk detection isn't performed on the IP addresses in this range list. The IP range is in CIDR notation.
    language:
      python:
        mapCase: false
type: object
