$token: aws:cognito/UserPoolAdminCreateUserConfig:UserPoolAdminCreateUserConfig
properties:
  allowAdminCreateUserOnly:
    type: boolean
    description: |
      Set to True if only the administrator is allowed to create user profiles. Set to False if users can sign themselves up via an app.
    language:
      python:
        mapCase: false
  inviteMessageTemplate:
    $ref: "#/types/aws:cognito/UserPoolAdminCreateUserConfigInviteMessageTemplate:UserPoolAdminCreateUserConfigInviteMessageTemplate"
    description: |
      Invite message template structure. Detailed below.
    language:
      python:
        mapCase: false
type: object
