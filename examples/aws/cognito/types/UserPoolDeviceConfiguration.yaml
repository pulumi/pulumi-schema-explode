$token: aws:cognito/UserPoolDeviceConfiguration:UserPoolDeviceConfiguration
properties:
  challengeRequiredOnNewDevice:
    type: boolean
    description: |
      Whether a challenge is required on a new device. Only applicable to a new device.
    language:
      python:
        mapCase: false
  deviceOnlyRememberedOnUserPrompt:
    type: boolean
    description: |
      Whether a device is only remembered on user prompt. `false` equates to "Always" remember, `true` is "User Opt In," and not using a `device_configuration` block is "No."
    language:
      python:
        mapCase: false
type: object
