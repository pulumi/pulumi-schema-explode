$token: aws:cognito/IdentityPoolCognitoIdentityProvider:IdentityPoolCognitoIdentityProvider
properties:
  clientId:
    type: string
    description: |
      The client ID for the Amazon Cognito Identity User Pool.
    language:
      python:
        mapCase: false
  providerName:
    type: string
    description: |
      The provider name for an Amazon Cognito Identity User Pool.
    language:
      python:
        mapCase: false
  serverSideTokenCheck:
    type: boolean
    description: |
      Whether server-side token validation is enabled for the identity provider’s token or not.
    language:
      python:
        mapCase: false
type: object
