$token: aws:cognito/UserPoolEmailConfiguration:UserPoolEmailConfiguration
properties:
  configurationSet:
    type: string
    description: |
      Email configuration set name from SES.
    language:
      python:
        mapCase: false
  emailSendingAccount:
    type: string
    description: |
      Email delivery method to use. `COGNITO_DEFAULT` for the default email functionality built into Cognito or `DEVELOPER` to use your Amazon SES configuration.
    language:
      python:
        mapCase: false
  fromEmailAddress:
    type: string
    description: |
      Sender’s email address or sender’s display name with their email address (e.g., `john@example.com`, `John Smith <john@example.com>` or `\"John Smith Ph.D.\" <john@example.com>`). Escaped double quotes are required around display names that contain certain characters as specified in [RFC 5322](https://tools.ietf.org/html/rfc5322).
    language:
      python:
        mapCase: false
  replyToEmailAddress:
    type: string
    description: |
      REPLY-TO email address.
    language:
      python:
        mapCase: false
  sourceArn:
    type: string
    description: |
      ARN of the SES verified email identity to to use. Required if `email_sending_account` is set to `DEVELOPER`.
    language:
      python:
        mapCase: false
type: object
