$token: aws:cognito/RiskConfigurationCompromisedCredentialsRiskConfiguration:RiskConfigurationCompromisedCredentialsRiskConfiguration
properties:
  actions:
    $ref: "#/types/aws:cognito/RiskConfigurationCompromisedCredentialsRiskConfigurationActions:RiskConfigurationCompromisedCredentialsRiskConfigurationActions"
    description: |
      The compromised credentials risk configuration actions. See details below.
    language:
      python:
        mapCase: false
  eventFilters:
    type: array
    items:
      type: string
    description: |
      Perform the action for these events. The default is to perform all events if no event filter is specified. Valid values are `SIGN_IN`, `PASSWORD_CHANGE`, and `SIGN_UP`.
    language:
      python:
        mapCase: false
type: object
required:
  - actions
language:
  nodejs:
    requiredOutputs:
      - actions
      - eventFilters
