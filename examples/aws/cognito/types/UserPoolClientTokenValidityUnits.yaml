$token: aws:cognito/UserPoolClientTokenValidityUnits:UserPoolClientTokenValidityUnits
properties:
  accessToken:
    type: string
    description: |
      Time unit in for the value in `access_token_validity`, defaults to `hours`.
    language:
      python:
        mapCase: false
  idToken:
    type: string
    description: |
      Time unit in for the value in `id_token_validity`, defaults to `hours`.
    language:
      python:
        mapCase: false
  refreshToken:
    type: string
    description: |
      Time unit in for the value in `refresh_token_validity`, defaults to `days`.
    language:
      python:
        mapCase: false
type: object
