$token: aws:cognito/IdentityPoolRoleAttachmentRoleMappingMappingRule:IdentityPoolRoleAttachmentRoleMappingMappingRule
properties:
  claim:
    type: string
    description: |
      The claim name that must be present in the token, for example, "isAdmin" or "paid".
    language:
      python:
        mapCase: false
  matchType:
    type: string
    description: |
      The match condition that specifies how closely the claim value in the IdP token must match Value.
    language:
      python:
        mapCase: false
  roleArn:
    type: string
    description: |
      The role ARN.
    language:
      python:
        mapCase: false
  value:
    type: string
    description: |
      A brief string that the claim must match, for example, "paid" or "yes".
    language:
      python:
        mapCase: false
type: object
required:
  - claim
  - matchType
  - roleArn
  - value
