$token: aws:cognito/IdentityPoolRoleAttachmentRoleMapping:IdentityPoolRoleAttachmentRoleMapping
properties:
  ambiguousRoleResolution:
    type: string
    description: |
      Specifies the action to be taken if either no rules match the claim value for the Rules type, or there is no cognito:preferred_role claim and there are multiple cognito:roles matches for the Token type. `Required` if you specify Token or Rules as the Type.
    language:
      python:
        mapCase: false
  identityProvider:
    type: string
    description: |
      A string identifying the identity provider, for example, "graph.facebook.com" or "cognito-idp.us-east-1.amazonaws.com/us-east-1_abcdefghi:app_client_id". Depends on `cognito_identity_providers` set on `aws.cognito.IdentityPool` resource or a `aws.cognito.IdentityProvider` resource.
    language:
      python:
        mapCase: false
  mappingRules:
    type: array
    items:
      $ref: "#/types/aws:cognito/IdentityPoolRoleAttachmentRoleMappingMappingRule:IdentityPoolRoleAttachmentRoleMappingMappingRule"
    description: |
      The Rules Configuration to be used for mapping users to roles. You can specify up to 25 rules per identity provider. Rules are evaluated in order. The first one to match specifies the role.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      The role mapping type.
    language:
      python:
        mapCase: false
type: object
required:
  - identityProvider
  - type
