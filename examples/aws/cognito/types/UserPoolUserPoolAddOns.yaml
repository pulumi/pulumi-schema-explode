$token: aws:cognito/UserPoolUserPoolAddOns:UserPoolUserPoolAddOns
properties:
  advancedSecurityMode:
    type: string
    description: |
      Mode for advanced security, must be one of `OFF`, `AUDIT` or `ENFORCED`.
    language:
      python:
        mapCase: false
type: object
required:
  - advancedSecurityMode
