$token: aws:cognito/UserPoolAdminCreateUserConfigInviteMessageTemplate:UserPoolAdminCreateUserConfigInviteMessageTemplate
properties:
  emailMessage:
    type: string
    description: |
      Email message template. Must contain the `{####}` placeholder. Conflicts with `email_verification_message` argument.
    language:
      python:
        mapCase: false
  emailSubject:
    type: string
    description: |
      Subject line for the email message template. Conflicts with `email_verification_subject` argument.
    language:
      python:
        mapCase: false
  smsMessage:
    type: string
    description: |
      SMS message template. Must contain the `{####}` placeholder. Conflicts with `sms_verification_message` argument.
    language:
      python:
        mapCase: false
type: object
