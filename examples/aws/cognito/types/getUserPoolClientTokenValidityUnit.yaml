$token: aws:cognito/getUserPoolClientTokenValidityUnit:getUserPoolClientTokenValidityUnit
properties:
  accessToken:
    type: string
    description: |
      (Optional) Time unit in for the value in `access_token_validity`, defaults to `hours`.
    language:
      python:
        mapCase: false
  idToken:
    type: string
    description: |
      (Optional) Time unit in for the value in `id_token_validity`, defaults to `hours`.
    language:
      python:
        mapCase: false
  refreshToken:
    type: string
    description: |
      (Optional) Time unit in for the value in `refresh_token_validity`, defaults to `days`.
    language:
      python:
        mapCase: false
type: object
required:
  - accessToken
  - idToken
  - refreshToken
language:
  nodejs:
    requiredInputs: []
