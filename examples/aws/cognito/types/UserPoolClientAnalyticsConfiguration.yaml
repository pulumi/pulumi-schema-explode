$token: aws:cognito/UserPoolClientAnalyticsConfiguration:UserPoolClientAnalyticsConfiguration
properties:
  applicationArn:
    type: string
    description: |
      Application ARN for an Amazon Pinpoint application. Conflicts with `external_id` and `role_arn`.
    language:
      python:
        mapCase: false
  applicationId:
    type: string
    description: |
      Application ID for an Amazon Pinpoint application.
    language:
      python:
        mapCase: false
  externalId:
    type: string
    description: |
      ID for the Analytics Configuration. Conflicts with `application_arn`.
    language:
      python:
        mapCase: false
  roleArn:
    type: string
    description: |
      ARN of an IAM role that authorizes Amazon Cognito to publish events to Amazon Pinpoint analytics. Conflicts with `application_arn`.
    language:
      python:
        mapCase: false
  userDataShared:
    type: boolean
    description: |
      If set to `true`, Amazon Cognito will include user data in the events it publishes to Amazon Pinpoint analytics.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - roleArn
