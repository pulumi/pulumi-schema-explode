$token: aws:cognito/UserPoolLambdaConfig:UserPoolLambdaConfig
properties:
  createAuthChallenge:
    type: string
    description: |
      ARN of the lambda creating an authentication challenge.
    language:
      python:
        mapCase: false
  customEmailSender:
    $ref: "#/types/aws:cognito/UserPoolLambdaConfigCustomEmailSender:UserPoolLambdaConfigCustomEmailSender"
    description: |
      A custom email sender AWS Lambda trigger. See custom_email_sender Below.
    language:
      python:
        mapCase: false
  customMessage:
    type: string
    description: |
      Custom Message AWS Lambda trigger.
    language:
      python:
        mapCase: false
  customSmsSender:
    $ref: "#/types/aws:cognito/UserPoolLambdaConfigCustomSmsSender:UserPoolLambdaConfigCustomSmsSender"
    description: |
      A custom SMS sender AWS Lambda trigger. See custom_sms_sender Below.
    language:
      python:
        mapCase: false
  defineAuthChallenge:
    type: string
    description: |
      Defines the authentication challenge.
    language:
      python:
        mapCase: false
  kmsKeyId:
    type: string
    description: |
      The Amazon Resource Name of Key Management Service Customer master keys. Amazon Cognito uses the key to encrypt codes and temporary passwords sent to CustomEmailSender and CustomSMSSender.
    language:
      python:
        mapCase: false
  postAuthentication:
    type: string
    description: |
      Post-authentication AWS Lambda trigger.
    language:
      python:
        mapCase: false
  postConfirmation:
    type: string
    description: |
      Post-confirmation AWS Lambda trigger.
    language:
      python:
        mapCase: false
  preAuthentication:
    type: string
    description: |
      Pre-authentication AWS Lambda trigger.
    language:
      python:
        mapCase: false
  preSignUp:
    type: string
    description: |
      Pre-registration AWS Lambda trigger.
    language:
      python:
        mapCase: false
  preTokenGeneration:
    type: string
    description: |
      Allow to customize identity token claims before token generation.
    language:
      python:
        mapCase: false
  userMigration:
    type: string
    description: |
      User migration Lambda config type.
    language:
      python:
        mapCase: false
  verifyAuthChallengeResponse:
    type: string
    description: |
      Verifies the authentication challenge response.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - customEmailSender
      - customSmsSender
