$token: aws:servicecatalog/getLaunchPathsSummary:getLaunchPathsSummary
properties:
  constraintSummaries:
    type: array
    items:
      $ref: "#/types/aws:servicecatalog/getLaunchPathsSummaryConstraintSummary:getLaunchPathsSummaryConstraintSummary"
    description: |
      Block for constraints on the portfolio-product relationship. See details below.
    language:
      python:
        mapCase: false
  name:
    type: string
    description: |
      Name of the portfolio to which the path was assigned.
    language:
      python:
        mapCase: false
  pathId:
    type: string
    description: |
      Identifier of the product path.
    language:
      python:
        mapCase: false
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Tags associated with this product path.
    language:
      python:
        mapCase: false
type: object
required:
  - constraintSummaries
  - name
  - pathId
  - tags
language:
  nodejs:
    requiredInputs: []
