$token: aws:servicecatalog/ProvisionedProductProvisioningParameter:ProvisionedProductProvisioningParameter
properties:
  key:
    type: string
    description: |
      Parameter key.
    language:
      python:
        mapCase: false
  usePreviousValue:
    type: boolean
    description: |
      Whether to ignore `value` and keep the previous parameter value. Ignored when initially provisioning a product.
    language:
      python:
        mapCase: false
  value:
    type: string
    description: |
      Parameter value.
    language:
      python:
        mapCase: false
type: object
required:
  - key
