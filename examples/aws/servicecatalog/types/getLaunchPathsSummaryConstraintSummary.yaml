$token: aws:servicecatalog/getLaunchPathsSummaryConstraintSummary:getLaunchPathsSummaryConstraintSummary
properties:
  description:
    type: string
    description: |
      Description of the constraint.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      Type of constraint. Valid values are `LAUNCH`, `NOTIFICATION`, `STACKSET`, and `TEMPLATE`.
    language:
      python:
        mapCase: false
type: object
required:
  - description
  - type
language:
  nodejs:
    requiredInputs: []
