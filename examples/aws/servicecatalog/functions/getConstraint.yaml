$token: aws:servicecatalog/getConstraint:getConstraint
inputs:
  description: |
    A collection of arguments for invoking getConstraint.
  properties:
    acceptLanguage:
      type: string
      description: |
        Language code. Valid values: `en` (English), `jp` (Japanese), `zh` (Chinese). Default value is `en`.
    description:
      type: string
      description: |
        Description of the constraint.
    id:
      type: string
      description: |
        Constraint identifier.
  type: object
  required:
    - id
outputs:
  description: |
    A collection of values returned by getConstraint.
  properties:
    acceptLanguage:
      type: string
    description:
      type: string
      description: |
        Description of the constraint.
    id:
      type: string
    owner:
      type: string
      description: |
        Owner of the constraint.
    parameters:
      type: string
      description: |
        Constraint parameters in JSON format.
    portfolioId:
      type: string
      description: |
        Portfolio identifier.
    productId:
      type: string
      description: |
        Product identifier.
    status:
      type: string
      description: |
        Constraint status.
    type:
      type: string
      description: |
        Type of constraint. Valid values are `LAUNCH`, `NOTIFICATION`, `RESOURCE_UPDATE`, `STACKSET`, and `TEMPLATE`.
  type: object
  required:
    - description
    - id
    - owner
    - parameters
    - portfolioId
    - productId
    - status
    - type
