$token: aws:servicecatalog/budgetResourceAssociation:BudgetResourceAssociation
properties:
  budgetName:
    type: string
    description: |
      Budget name.
  resourceId:
    type: string
    description: |
      Resource identifier.
required:
  - budgetName
  - resourceId
inputProperties:
  budgetName:
    type: string
    description: |
      Budget name.
    willReplaceOnChanges: true
  resourceId:
    type: string
    description: |
      Resource identifier.
    willReplaceOnChanges: true
requiredInputs:
  - budgetName
  - resourceId
stateInputs:
  description: |
    Input properties used for looking up and filtering BudgetResourceAssociation resources.
  properties:
    budgetName:
      type: string
      description: |
        Budget name.
      willReplaceOnChanges: true
    resourceId:
      type: string
      description: |
        Resource identifier.
      willReplaceOnChanges: true
  type: object
