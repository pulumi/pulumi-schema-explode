$token: aws:servicecatalog/principalPortfolioAssociation:PrincipalPortfolioAssociation
properties:
  acceptLanguage:
    type: string
    description: |
      Language code. Valid values: `en` (English), `jp` (Japanese), `zh` (Chinese). Default value is `en`.
  portfolioId:
    type: string
    description: |
      Portfolio identifier.
  principalArn:
    type: string
    description: |
      Principal ARN.
  principalType:
    type: string
    description: |
      Principal type. Setting this argument empty (e.g., `principal_type = ""`) will result in an error. Valid value is `IAM`. Default is `IAM`.
required:
  - portfolioId
  - principalArn
inputProperties:
  acceptLanguage:
    type: string
    description: |
      Language code. Valid values: `en` (English), `jp` (Japanese), `zh` (Chinese). Default value is `en`.
    willReplaceOnChanges: true
  portfolioId:
    type: string
    description: |
      Portfolio identifier.
    willReplaceOnChanges: true
  principalArn:
    type: string
    description: |
      Principal ARN.
    willReplaceOnChanges: true
  principalType:
    type: string
    description: |
      Principal type. Setting this argument empty (e.g., `principal_type = ""`) will result in an error. Valid value is `IAM`. Default is `IAM`.
    willReplaceOnChanges: true
requiredInputs:
  - portfolioId
  - principalArn
stateInputs:
  description: |
    Input properties used for looking up and filtering PrincipalPortfolioAssociation resources.
  properties:
    acceptLanguage:
      type: string
      description: |
        Language code. Valid values: `en` (English), `jp` (Japanese), `zh` (Chinese). Default value is `en`.
      willReplaceOnChanges: true
    portfolioId:
      type: string
      description: |
        Portfolio identifier.
      willReplaceOnChanges: true
    principalArn:
      type: string
      description: |
        Principal ARN.
      willReplaceOnChanges: true
    principalType:
      type: string
      description: |
        Principal type. Setting this argument empty (e.g., `principal_type = ""`) will result in an error. Valid value is `IAM`. Default is `IAM`.
      willReplaceOnChanges: true
  type: object
