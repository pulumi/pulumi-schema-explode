$token: aws:servicecatalog/productPortfolioAssociation:ProductPortfolioAssociation
properties:
  acceptLanguage:
    type: string
    description: |
      Language code. Valid values: `en` (English), `jp` (Japanese), `zh` (Chinese). Default value is `en`.
  portfolioId:
    type: string
    description: |
      Portfolio identifier.
  productId:
    type: string
    description: |
      Product identifier.
  sourcePortfolioId:
    type: string
    description: |
      Identifier of the source portfolio.
required:
  - portfolioId
  - productId
inputProperties:
  acceptLanguage:
    type: string
    description: |
      Language code. Valid values: `en` (English), `jp` (Japanese), `zh` (Chinese). Default value is `en`.
    willReplaceOnChanges: true
  portfolioId:
    type: string
    description: |
      Portfolio identifier.
    willReplaceOnChanges: true
  productId:
    type: string
    description: |
      Product identifier.
    willReplaceOnChanges: true
  sourcePortfolioId:
    type: string
    description: |
      Identifier of the source portfolio.
    willReplaceOnChanges: true
requiredInputs:
  - portfolioId
  - productId
stateInputs:
  description: |
    Input properties used for looking up and filtering ProductPortfolioAssociation resources.
  properties:
    acceptLanguage:
      type: string
      description: |
        Language code. Valid values: `en` (English), `jp` (Japanese), `zh` (Chinese). Default value is `en`.
      willReplaceOnChanges: true
    portfolioId:
      type: string
      description: |
        Portfolio identifier.
      willReplaceOnChanges: true
    productId:
      type: string
      description: |
        Product identifier.
      willReplaceOnChanges: true
    sourcePortfolioId:
      type: string
      description: |
        Identifier of the source portfolio.
      willReplaceOnChanges: true
  type: object
