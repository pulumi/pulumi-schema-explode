$token: aws:servicecatalog/serviceAction:ServiceAction
properties:
  acceptLanguage:
    type: string
    description: |
      Language code. Valid values are `en` (English), `jp` (Japanese), and `zh` (Chinese). Default is `en`.
  definition:
    $ref: "#/types/aws:servicecatalog/ServiceActionDefinition:ServiceActionDefinition"
    description: |
      Self-service action definition configuration block. Detailed below.
  description:
    type: string
    description: |
      Self-service action description.
  name:
    type: string
    description: |
      Self-service action name.
required:
  - definition
  - description
  - name
inputProperties:
  acceptLanguage:
    type: string
    description: |
      Language code. Valid values are `en` (English), `jp` (Japanese), and `zh` (Chinese). Default is `en`.
  definition:
    $ref: "#/types/aws:servicecatalog/ServiceActionDefinition:ServiceActionDefinition"
    description: |
      Self-service action definition configuration block. Detailed below.
  description:
    type: string
    description: |
      Self-service action description.
  name:
    type: string
    description: |
      Self-service action name.
requiredInputs:
  - definition
stateInputs:
  description: |
    Input properties used for looking up and filtering ServiceAction resources.
  properties:
    acceptLanguage:
      type: string
      description: |
        Language code. Valid values are `en` (English), `jp` (Japanese), and `zh` (Chinese). Default is `en`.
    definition:
      $ref: "#/types/aws:servicecatalog/ServiceActionDefinition:ServiceActionDefinition"
      description: |
        Self-service action definition configuration block. Detailed below.
    description:
      type: string
      description: |
        Self-service action description.
    name:
      type: string
      description: |
        Self-service action name.
  type: object
