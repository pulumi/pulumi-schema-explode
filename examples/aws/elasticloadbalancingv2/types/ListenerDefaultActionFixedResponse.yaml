$token: aws:elasticloadbalancingv2/ListenerDefaultActionFixedResponse:ListenerDefaultActionFixedResponse
properties:
  contentType:
    type: string
    description: |
      Content type. Valid values are `text/plain`, `text/css`, `text/html`, `application/javascript` and `application/json`.
    language:
      python:
        mapCase: false
  messageBody:
    type: string
    description: |
      Message body.
    language:
      python:
        mapCase: false
  statusCode:
    type: string
    description: |
      HTTP redirect code. The redirect is either permanent (`HTTP_301`) or temporary (`HTTP_302`).
    language:
      python:
        mapCase: false
type: object
required:
  - contentType
language:
  nodejs:
    requiredOutputs:
      - contentType
      - statusCode
