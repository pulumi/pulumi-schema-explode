$token: aws:elasticloadbalancingv2/ListenerDefaultActionAuthenticateOidc:ListenerDefaultActionAuthenticateOidc
properties:
  authenticationRequestExtraParams:
    type: object
    additionalProperties:
      type: string
    description: |
      Query parameters to include in the redirect request to the authorization endpoint. Max: 10.
    language:
      python:
        mapCase: false
  authorizationEndpoint:
    type: string
    description: |
      Authorization endpoint of the IdP.
    language:
      python:
        mapCase: false
  clientId:
    type: string
    description: |
      OAuth 2.0 client identifier.
    language:
      python:
        mapCase: false
  clientSecret:
    type: string
    description: |
      OAuth 2.0 client secret.
    language:
      python:
        mapCase: false
  issuer:
    type: string
    description: |
      OIDC issuer identifier of the IdP.
    language:
      python:
        mapCase: false
  onUnauthenticatedRequest:
    type: string
    description: |
      Behavior if the user is not authenticated. Valid values: `deny`, `allow` and `authenticate`
    language:
      python:
        mapCase: false
  scope:
    type: string
    description: |
      Set of user claims to be requested from the IdP.
    language:
      python:
        mapCase: false
  sessionCookieName:
    type: string
    description: |
      Name of the cookie used to maintain session information.
    language:
      python:
        mapCase: false
  sessionTimeout:
    type: integer
    description: |
      Maximum duration of the authentication session, in seconds.
    language:
      python:
        mapCase: false
  tokenEndpoint:
    type: string
    description: |
      Token endpoint of the IdP.
    language:
      python:
        mapCase: false
  userInfoEndpoint:
    type: string
    description: |
      User info endpoint of the IdP.
    language:
      python:
        mapCase: false
type: object
required:
  - authorizationEndpoint
  - clientId
  - clientSecret
  - issuer
  - tokenEndpoint
  - userInfoEndpoint
language:
  nodejs:
    requiredOutputs:
      - authorizationEndpoint
      - clientId
      - clientSecret
      - issuer
      - onUnauthenticatedRequest
      - scope
      - sessionCookieName
      - sessionTimeout
      - tokenEndpoint
      - userInfoEndpoint
