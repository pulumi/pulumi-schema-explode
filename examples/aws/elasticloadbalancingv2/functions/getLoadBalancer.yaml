$token: aws:elasticloadbalancingv2/getLoadBalancer:getLoadBalancer
inputs:
  description: |
    A collection of arguments for invoking getLoadBalancer.
  properties:
    arn:
      type: string
      description: |
        Full ARN of the load balancer.
    name:
      type: string
      description: |
        Unique name of the load balancer.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Mapping of tags, each pair of which must exactly match a pair on the desired load balancer.
  type: object
outputs:
  description: |
    A collection of values returned by getLoadBalancer.
  properties:
    accessLogs:
      $ref: "#/types/aws:elasticloadbalancingv2/getLoadBalancerAccessLogs:getLoadBalancerAccessLogs"
    arn:
      type: string
    arnSuffix:
      type: string
    customerOwnedIpv4Pool:
      type: string
    desyncMitigationMode:
      type: string
    dnsName:
      type: string
    dropInvalidHeaderFields:
      type: boolean
    enableDeletionProtection:
      type: boolean
    enableHttp2:
      type: boolean
    enableWafFailOpen:
      type: boolean
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    idleTimeout:
      type: integer
    internal:
      type: boolean
    ipAddressType:
      type: string
    loadBalancerType:
      type: string
    name:
      type: string
    preserveHostHeader:
      type: boolean
    securityGroups:
      type: array
      items:
        type: string
    subnetMappings:
      type: array
      items:
        $ref: "#/types/aws:elasticloadbalancingv2/getLoadBalancerSubnetMapping:getLoadBalancerSubnetMapping"
    subnets:
      type: array
      items:
        type: string
    tags:
      type: object
      additionalProperties:
        type: string
    vpcId:
      type: string
    zoneId:
      type: string
  type: object
  required:
    - accessLogs
    - arn
    - arnSuffix
    - customerOwnedIpv4Pool
    - desyncMitigationMode
    - dnsName
    - dropInvalidHeaderFields
    - enableDeletionProtection
    - enableHttp2
    - enableWafFailOpen
    - idleTimeout
    - internal
    - ipAddressType
    - loadBalancerType
    - name
    - preserveHostHeader
    - securityGroups
    - subnetMappings
    - subnets
    - tags
    - vpcId
    - zoneId
    - id
deprecationMessage: aws.elasticloadbalancingv2.getLoadBalancer has been deprecated in favor of aws.lb.getLoadBalancer
