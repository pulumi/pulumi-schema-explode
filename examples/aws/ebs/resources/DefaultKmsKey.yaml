$token: aws:ebs/defaultKmsKey:DefaultKmsKey
properties:
  keyArn:
    type: string
    description: |
      The ARN of the AWS Key Management Service (AWS KMS) customer master key (CMK) to use to encrypt the EBS volume.
required:
  - keyArn
inputProperties:
  keyArn:
    type: string
    description: |
      The ARN of the AWS Key Management Service (AWS KMS) customer master key (CMK) to use to encrypt the EBS volume.
    willReplaceOnChanges: true
requiredInputs:
  - keyArn
stateInputs:
  description: |
    Input properties used for looking up and filtering DefaultKmsKey resources.
  properties:
    keyArn:
      type: string
      description: |
        The ARN of the AWS Key Management Service (AWS KMS) customer master key (CMK) to use to encrypt the EBS volume.
      willReplaceOnChanges: true
  type: object
