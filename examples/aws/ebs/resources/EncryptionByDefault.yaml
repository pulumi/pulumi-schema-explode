$token: aws:ebs/encryptionByDefault:EncryptionByDefault
properties:
  enabled:
    type: boolean
    description: |
      Whether or not default EBS encryption is enabled. Valid values are `true` or `false`. Defaults to `true`.
inputProperties:
  enabled:
    type: boolean
    description: |
      Whether or not default EBS encryption is enabled. Valid values are `true` or `false`. Defaults to `true`.
stateInputs:
  description: |
    Input properties used for looking up and filtering EncryptionByDefault resources.
  properties:
    enabled:
      type: boolean
      description: |
        Whether or not default EBS encryption is enabled. Valid values are `true` or `false`. Defaults to `true`.
  type: object
