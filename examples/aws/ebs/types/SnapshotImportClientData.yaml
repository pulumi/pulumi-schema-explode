$token: aws:ebs/SnapshotImportClientData:SnapshotImportClientData
properties:
  comment:
    type: string
    description: |
      A user-defined comment about the disk upload.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  uploadEnd:
    type: string
    description: |
      The time that the disk upload ends.
    language:
      python:
        mapCase: false
  uploadSize:
    type: number
    description: |
      The size of the uploaded disk image, in GiB.
    language:
      python:
        mapCase: false
  uploadStart:
    type: string
    description: |
      The time that the disk upload starts.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - uploadEnd
      - uploadSize
      - uploadStart
