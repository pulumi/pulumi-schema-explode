$token: aws:ebs/SnapshotImportDiskContainer:SnapshotImportDiskContainer
properties:
  description:
    type: string
    description: |
      The description of the disk image being imported.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  format:
    type: string
    description: |
      The format of the disk image being imported. One of `VHD` or `VMDK`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  url:
    type: string
    description: |
      The URL to the Amazon S3-based disk image being imported. It can either be a https URL (https://..) or an Amazon S3 URL (s3://..). One of `url` or `user_bucket` must be set.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  userBucket:
    $ref: "#/types/aws:ebs/SnapshotImportDiskContainerUserBucket:SnapshotImportDiskContainerUserBucket"
    description: |
      The Amazon S3 bucket for the disk image. One of `url` or `user_bucket` must be set. Detailed below.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - format
