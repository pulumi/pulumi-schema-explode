$token: aws:resourcegroups/GroupResourceQuery:GroupResourceQuery
properties:
  query:
    type: string
    description: |
      The resource query as a JSON string.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      The type of the resource query. Defaults to `TAG_FILTERS_1_0`.
    language:
      python:
        mapCase: false
type: object
required:
  - query
