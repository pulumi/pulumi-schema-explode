$token: aws:resourcegroups/group:Group
properties:
  arn:
    type: string
    description: |
      The ARN assigned by AWS for this resource group.
  description:
    type: string
    description: |
      A description of the resource group.
  name:
    type: string
    description: |
      The resource group's name. A resource group name can have a maximum of 127 characters, including letters, numbers, hyphens, dots, and underscores. The name cannot start with `AWS` or `aws`.
  resourceQuery:
    $ref: "#/types/aws:resourcegroups/GroupResourceQuery:GroupResourceQuery"
    description: |
      A `resource_query` block. Resource queries are documented below.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - name
  - resourceQuery
  - tagsAll
inputProperties:
  description:
    type: string
    description: |
      A description of the resource group.
  name:
    type: string
    description: |
      The resource group's name. A resource group name can have a maximum of 127 characters, including letters, numbers, hyphens, dots, and underscores. The name cannot start with `AWS` or `aws`.
    willReplaceOnChanges: true
  resourceQuery:
    $ref: "#/types/aws:resourcegroups/GroupResourceQuery:GroupResourceQuery"
    description: |
      A `resource_query` block. Resource queries are documented below.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - resourceQuery
stateInputs:
  description: |
    Input properties used for looking up and filtering Group resources.
  properties:
    arn:
      type: string
      description: |
        The ARN assigned by AWS for this resource group.
    description:
      type: string
      description: |
        A description of the resource group.
    name:
      type: string
      description: |
        The resource group's name. A resource group name can have a maximum of 127 characters, including letters, numbers, hyphens, dots, and underscores. The name cannot start with `AWS` or `aws`.
      willReplaceOnChanges: true
    resourceQuery:
      $ref: "#/types/aws:resourcegroups/GroupResourceQuery:GroupResourceQuery"
      description: |
        A `resource_query` block. Resource queries are documented below.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
