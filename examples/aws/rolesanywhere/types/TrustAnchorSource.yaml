$token: aws:rolesanywhere/TrustAnchorSource:TrustAnchorSource
properties:
  sourceData:
    $ref: "#/types/aws:rolesanywhere/TrustAnchorSourceSourceData:TrustAnchorSourceSourceData"
    description: |
      The data denoting the source of trust, documented below
    language:
      python:
        mapCase: false
  sourceType:
    type: string
    description: |
      The type of the source of trust. Must be either `AWS_ACM_PCA` or `CERTIFICATE_BUNDLE`.
    language:
      python:
        mapCase: false
type: object
required:
  - sourceData
  - sourceType
