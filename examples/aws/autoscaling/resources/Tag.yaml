$token: aws:autoscaling/tag:Tag
description: "


  ## Import


  `aws_autoscaling_group_tag` can be imported by using the ASG name and key, separated by a comma (`,`), e.g.,


  ```sh

  \ $ pulumi import aws:autoscaling/tag:Tag example asg-example,k8s.io/cluster-autoscaler/node-template/label/eks.amazonaws.com/capacityType

  ```


  \ "
properties:
  autoscalingGroupName:
    type: string
    description: |
      Name of the Autoscaling Group to apply the tag to.
  tag:
    $ref: "#/types/aws:autoscaling/TagTag:TagTag"
    description: |
      Tag to create. The `tag` block is documented below.
    language:
      csharp:
        name: TagDetails
required:
  - autoscalingGroupName
  - tag
inputProperties:
  autoscalingGroupName:
    type: string
    description: |
      Name of the Autoscaling Group to apply the tag to.
    willReplaceOnChanges: true
  tag:
    $ref: "#/types/aws:autoscaling/TagTag:TagTag"
    description: |
      Tag to create. The `tag` block is documented below.
    language:
      csharp:
        name: TagDetails
requiredInputs:
  - autoscalingGroupName
  - tag
stateInputs:
  description: |
    Input properties used for looking up and filtering Tag resources.
  properties:
    autoscalingGroupName:
      type: string
      description: |
        Name of the Autoscaling Group to apply the tag to.
      willReplaceOnChanges: true
    tag:
      $ref: "#/types/aws:autoscaling/TagTag:TagTag"
      description: |
        Tag to create. The `tag` block is documented below.
      language:
        csharp:
          name: TagDetails
  type: object
