$token: aws:autoscaling/PolicyPredictiveScalingConfigurationMetricSpecificationCustomizedLoadMetricSpecificationMetricDataQuery:PolicyPredictiveScalingConfigurationMetricSpecificationCustomizedLoadMetricSpecificationMetricDataQuery
properties:
  expression:
    type: string
    description: |
      Math expression used on the returned metric. You must specify either `expression` or `metric_stat`, but not both.
    language:
      python:
        mapCase: false
  id:
    type: string
    description: |
      Short name for the metric used in predictive scaling policy.
    language:
      python:
        mapCase: false
  label:
    type: string
    description: |
      Human-readable label for this metric or expression.
    language:
      python:
        mapCase: false
  metricStat:
    $ref: "#/types/aws:autoscaling/PolicyPredictiveScalingConfigurationMetricSpecificationCustomizedLoadMetricSpecificationMetricDataQueryMetricStat:PolicyPredictiveScalingConfigurationMetricSpecificationCustomizedLoadMetricSpecificationMetricDataQueryMetricStat"
    description: |
      Structure that defines CloudWatch metric to be used in predictive scaling policy. You must specify either `expression` or `metric_stat`, but not both.
    language:
      python:
        mapCase: false
  returnData:
    type: boolean
    description: |
      Boolean that indicates whether to return the timestamps and raw data values of this metric, the default it true
    language:
      python:
        mapCase: false
type: object
required:
  - id
