$token: aws:autoscaling/GroupLaunchTemplate:GroupLaunchTemplate
properties:
  id:
    type: string
    description: |
      ID of the launch template. Conflicts with `name`.
    language:
      python:
        mapCase: false
  name:
    type: string
    description: |
      Name of the Auto Scaling Group. By default generated by this provider. Conflicts with `name_prefix`.
    language:
      python:
        mapCase: false
  version:
    type: string
    description: |
      Template version. Can be version number, `$Latest`, or `$Default`. (Default: `$Default`).
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - id
      - name
