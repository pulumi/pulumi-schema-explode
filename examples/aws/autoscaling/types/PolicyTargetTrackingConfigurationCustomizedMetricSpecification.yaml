$token: aws:autoscaling/PolicyTargetTrackingConfigurationCustomizedMetricSpecification:PolicyTargetTrackingConfigurationCustomizedMetricSpecification
properties:
  metricDimensions:
    type: array
    items:
      $ref: "#/types/aws:autoscaling/PolicyTargetTrackingConfigurationCustomizedMetricSpecificationMetricDimension:PolicyTargetTrackingConfigurationCustomizedMetricSpecificationMetricDimension"
    description: |
      Dimensions of the metric.
    language:
      python:
        mapCase: false
  metricName:
    type: string
    description: |
      Name of the metric.
    language:
      python:
        mapCase: false
  namespace:
    type: string
    description: |
      Namespace of the metric.
    language:
      python:
        mapCase: false
  statistic:
    type: string
    description: |
      Statistic of the metric.
    language:
      python:
        mapCase: false
  unit:
    type: string
    description: |
      Unit of the metrics to return.
    language:
      python:
        mapCase: false
type: object
required:
  - metricName
  - namespace
  - statistic
