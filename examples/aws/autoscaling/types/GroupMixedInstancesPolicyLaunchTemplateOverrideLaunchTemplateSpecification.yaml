$token: aws:autoscaling/GroupMixedInstancesPolicyLaunchTemplateOverrideLaunchTemplateSpecification:GroupMixedInstancesPolicyLaunchTemplateOverrideLaunchTemplateSpecification
properties:
  launchTemplateId:
    type: string
    description: |
      ID of the launch template. Conflicts with `launch_template_name`.
    language:
      python:
        mapCase: false
  launchTemplateName:
    type: string
    description: |
      Name of the launch template. Conflicts with `launch_template_id`.
    language:
      python:
        mapCase: false
  version:
    type: string
    description: |
      Template version. Can be version number, `$Latest`, or `$Default`. (Default: `$Default`).
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - launchTemplateId
      - launchTemplateName
