$token: aws:autoscaling/PolicyPredictiveScalingConfigurationMetricSpecificationCustomizedScalingMetricSpecificationMetricDataQueryMetricStat:PolicyPredictiveScalingConfigurationMetricSpecificationCustomizedScalingMetricSpecificationMetricDataQueryMetricStat
properties:
  metric:
    $ref: "#/types/aws:autoscaling/PolicyPredictiveScalingConfigurationMetricSpecificationCustomizedScalingMetricSpecificationMetricDataQueryMetricStatMetric:PolicyPredictiveScalingConfigurationMetricSpecificationCustomizedScalingMetricSpecificationMetricDataQueryMetricStatMetric"
    description: |
      Structure that defines the CloudWatch metric to return, including the metric name, namespace, and dimensions.
    language:
      python:
        mapCase: false
  stat:
    type: string
    description: |
      Statistic of the metrics to return.
    language:
      python:
        mapCase: false
  unit:
    type: string
    description: |
      Unit of the metrics to return.
    language:
      python:
        mapCase: false
type: object
required:
  - metric
  - stat
