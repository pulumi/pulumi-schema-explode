$token: aws:autoscaling/PolicyTargetTrackingConfiguration:PolicyTargetTrackingConfiguration
properties:
  customizedMetricSpecification:
    $ref: "#/types/aws:autoscaling/PolicyTargetTrackingConfigurationCustomizedMetricSpecification:PolicyTargetTrackingConfigurationCustomizedMetricSpecification"
    description: |
      Customized metric. Conflicts with `predefined_metric_specification`.
    language:
      python:
        mapCase: false
  disableScaleIn:
    type: boolean
    description: |
      Whether scale in by the target tracking policy is disabled.
    language:
      python:
        mapCase: false
  predefinedMetricSpecification:
    $ref: "#/types/aws:autoscaling/PolicyTargetTrackingConfigurationPredefinedMetricSpecification:PolicyTargetTrackingConfigurationPredefinedMetricSpecification"
    description: |
      Predefined metric. Conflicts with `customized_metric_specification`.
    language:
      python:
        mapCase: false
  targetValue:
    type: number
    description: |
      Target value for the metric.
    language:
      python:
        mapCase: false
type: object
required:
  - targetValue
