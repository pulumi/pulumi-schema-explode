$token: aws:autoscaling/GroupInstanceRefresh:GroupInstanceRefresh
properties:
  preferences:
    $ref: "#/types/aws:autoscaling/GroupInstanceRefreshPreferences:GroupInstanceRefreshPreferences"
    description: |
      Override default parameters for Instance Refresh.
    language:
      python:
        mapCase: false
  strategy:
    type: string
    description: |
      Strategy to use for instance refresh. The only allowed value is `Rolling`. See [StartInstanceRefresh Action](https://docs.aws.amazon.com/autoscaling/ec2/APIReference/API_StartInstanceRefresh.html#API_StartInstanceRefresh_RequestParameters) for more information.
    language:
      python:
        mapCase: false
  triggers:
    type: array
    items:
      type: string
    description: |
      Set of additional property names that will trigger an Instance Refresh. A refresh will always be triggered by a change in any of `launch_configuration`, `launch_template`, or `mixed_instances_policy`.
    language:
      python:
        mapCase: false
type: object
required:
  - strategy
