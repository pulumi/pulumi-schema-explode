$token: aws:autoscaling/getAmiIdsFilter:getAmiIdsFilter
properties:
  name:
    type: string
    description: |
      Name of the DescribeAutoScalingGroup filter. The recommended values are: `tag-key`, `tag-value`, and `tag:<tag name>`
    language:
      python:
        mapCase: false
  values:
    type: array
    items:
      type: string
    description: |
      Value of the filter.
    language:
      python:
        mapCase: false
type: object
required:
  - name
  - values
