$token: aws:autoscaling/PolicyPredictiveScalingConfigurationMetricSpecificationCustomizedLoadMetricSpecificationMetricDataQueryMetricStat:PolicyPredictiveScalingConfigurationMetricSpecificationCustomizedLoadMetricSpecificationMetricDataQueryMetricStat
properties:
  metric:
    $ref: "#/types/aws:autoscaling/PolicyPredictiveScalingConfigurationMetricSpecificationCustomizedLoadMetricSpecificationMetricDataQueryMetricStatMetric:PolicyPredictiveScalingConfigurationMetricSpecificationCustomizedLoadMetricSpecificationMetricDataQueryMetricStatMetric"
    description: |
      Structure that defines the CloudWatch metric to return, including the metric name, namespace, and dimensions.
    language:
      python:
        mapCase: false
  stat:
    type: string
    description: |
      Statistic of the metrics to return.
    language:
      python:
        mapCase: false
  unit:
    type: string
    description: |
      Unit of the metrics to return.
    language:
      python:
        mapCase: false
type: object
required:
  - metric
  - stat
