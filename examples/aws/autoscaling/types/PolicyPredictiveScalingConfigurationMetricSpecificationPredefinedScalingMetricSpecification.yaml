$token: aws:autoscaling/PolicyPredictiveScalingConfigurationMetricSpecificationPredefinedScalingMetricSpecification:PolicyPredictiveScalingConfigurationMetricSpecificationPredefinedScalingMetricSpecification
properties:
  predefinedMetricType:
    type: string
    description: |
      Describes a scaling metric for a predictive scaling policy. Valid values are `ASGAverageCPUUtilization`, `ASGAverageNetworkIn`, `ASGAverageNetworkOut`, or `ALBRequestCountPerTarget`.
    language:
      python:
        mapCase: false
  resourceLabel:
    type: string
    description: |
      Label that uniquely identifies a specific Application Load Balancer target group from which to determine the request count served by your Auto Scaling group.
    language:
      python:
        mapCase: false
type: object
required:
  - predefinedMetricType
  - resourceLabel
