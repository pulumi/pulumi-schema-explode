$token: aws:autoscaling/GroupMixedInstancesPolicy:GroupMixedInstancesPolicy
properties:
  instancesDistribution:
    $ref: "#/types/aws:autoscaling/GroupMixedInstancesPolicyInstancesDistribution:GroupMixedInstancesPolicyInstancesDistribution"
    description: |
      Nested argument containing settings on how to mix on-demand and Spot instances in the Auto Scaling group. Defined below.
    language:
      python:
        mapCase: false
  launchTemplate:
    $ref: "#/types/aws:autoscaling/GroupMixedInstancesPolicyLaunchTemplate:GroupMixedInstancesPolicyLaunchTemplate"
    description: |
      Nested argument containing launch template settings along with the overrides to specify multiple instance types and weights. Defined below.
    language:
      python:
        mapCase: false
type: object
required:
  - launchTemplate
language:
  nodejs:
    requiredOutputs:
      - instancesDistribution
      - launchTemplate
