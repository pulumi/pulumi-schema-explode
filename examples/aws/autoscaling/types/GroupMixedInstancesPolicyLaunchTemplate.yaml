$token: aws:autoscaling/GroupMixedInstancesPolicyLaunchTemplate:GroupMixedInstancesPolicyLaunchTemplate
properties:
  launchTemplateSpecification:
    $ref: "#/types/aws:autoscaling/GroupMixedInstancesPolicyLaunchTemplateLaunchTemplateSpecification:GroupMixedInstancesPolicyLaunchTemplateLaunchTemplateSpecification"
    description: |
      Override the instance launch template specification in the Launch Template.
    language:
      python:
        mapCase: false
  overrides:
    type: array
    items:
      $ref: "#/types/aws:autoscaling/GroupMixedInstancesPolicyLaunchTemplateOverride:GroupMixedInstancesPolicyLaunchTemplateOverride"
    description: |
      List of nested arguments provides the ability to specify multiple instance types. This will override the same parameter in the launch template. For on-demand instances, Auto Scaling considers the order of preference of instance types to launch based on the order specified in the overrides list. Defined below.
    language:
      python:
        mapCase: false
type: object
required:
  - launchTemplateSpecification
