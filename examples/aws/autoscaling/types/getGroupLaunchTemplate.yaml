$token: aws:autoscaling/getGroupLaunchTemplate:getGroupLaunchTemplate
properties:
  id:
    type: string
    description: |
      Name of the Auto Scaling Group.
    language:
      python:
        mapCase: false
  name:
    type: string
    description: |
      Specify the exact name of the desired autoscaling group.
    language:
      python:
        mapCase: false
  version:
    type: string
    language:
      python:
        mapCase: false
type: object
required:
  - id
  - name
  - version
language:
  nodejs:
    requiredInputs: []
