$token: aws:autoscaling/GroupMixedInstancesPolicyLaunchTemplateOverride:GroupMixedInstancesPolicyLaunchTemplateOverride
properties:
  instanceRequirements:
    $ref: "#/types/aws:autoscaling/GroupMixedInstancesPolicyLaunchTemplateOverrideInstanceRequirements:GroupMixedInstancesPolicyLaunchTemplateOverrideInstanceRequirements"
    description: |
      Override the instance type in the Launch Template with instance types that satisfy the requirements.
    language:
      python:
        mapCase: false
  instanceType:
    type: string
    description: |
      Override the instance type in the Launch Template.
    language:
      python:
        mapCase: false
  launchTemplateSpecification:
    $ref: "#/types/aws:autoscaling/GroupMixedInstancesPolicyLaunchTemplateOverrideLaunchTemplateSpecification:GroupMixedInstancesPolicyLaunchTemplateOverrideLaunchTemplateSpecification"
    description: |
      Override the instance launch template specification in the Launch Template.
    language:
      python:
        mapCase: false
  weightedCapacity:
    type: string
    description: |
      Number of capacity units, which gives the instance type a proportional weight to other instance types.
    language:
      python:
        mapCase: false
type: object
