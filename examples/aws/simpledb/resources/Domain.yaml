$token: aws:simpledb/domain:Domain
properties:
  name:
    type: string
    description: |
      The name of the SimpleDB domain
required:
  - name
inputProperties:
  name:
    type: string
    description: |
      The name of the SimpleDB domain
    willReplaceOnChanges: true
stateInputs:
  description: |
    Input properties used for looking up and filtering Domain resources.
  properties:
    name:
      type: string
      description: |
        The name of the SimpleDB domain
      willReplaceOnChanges: true
  type: object
