$token: aws:backup/PlanRule:PlanRule
properties:
  completionWindow:
    type: integer
    description: |
      The amount of time in minutes AWS Backup attempts a backup before canceling the job and returning an error.
    language:
      python:
        mapCase: false
  copyActions:
    type: array
    items:
      $ref: "#/types/aws:backup/PlanRuleCopyAction:PlanRuleCopyAction"
    description: |
      Configuration block(s) with copy operation settings. Detailed below.
    language:
      python:
        mapCase: false
  enableContinuousBackup:
    type: boolean
    description: |
      Enable continuous backups for supported resources.
    language:
      python:
        mapCase: false
  lifecycle:
    $ref: "#/types/aws:backup/PlanRuleLifecycle:PlanRuleLifecycle"
    description: |
      The lifecycle defines when a protected resource is copied over to a backup vault and when it expires.  Fields documented above.
    language:
      python:
        mapCase: false
  recoveryPointTags:
    type: object
    additionalProperties:
      type: string
    description: |
      Metadata that you can assign to help organize the resources that you create.
    language:
      python:
        mapCase: false
  ruleName:
    type: string
    description: |
      An display name for a backup rule.
    language:
      python:
        mapCase: false
  schedule:
    type: string
    description: |
      A CRON expression specifying when AWS Backup initiates a backup job.
    language:
      python:
        mapCase: false
  startWindow:
    type: integer
    description: |
      The amount of time in minutes before beginning a backup.
    language:
      python:
        mapCase: false
  targetVaultName:
    type: string
    description: |
      The name of a logical container where backups are stored.
    language:
      python:
        mapCase: false
type: object
required:
  - ruleName
  - targetVaultName
