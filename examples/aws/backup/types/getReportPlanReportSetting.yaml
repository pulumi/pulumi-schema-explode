$token: aws:backup/getReportPlanReportSetting:getReportPlanReportSetting
properties:
  frameworkArns:
    type: array
    items:
      type: string
    description: |
      ARNs of the frameworks a report covers.
    language:
      python:
        mapCase: false
  numberOfFrameworks:
    type: integer
    description: |
      Specifies the number of frameworks a report covers.
    language:
      python:
        mapCase: false
  reportTemplate:
    type: string
    description: |
      Identifies the report template for the report. Reports are built using a report template.
    language:
      python:
        mapCase: false
type: object
required:
  - frameworkArns
  - numberOfFrameworks
  - reportTemplate
language:
  nodejs:
    requiredInputs: []
