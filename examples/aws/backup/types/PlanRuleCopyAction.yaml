$token: aws:backup/PlanRuleCopyAction:PlanRuleCopyAction
properties:
  destinationVaultArn:
    type: string
    description: |
      An Amazon Resource Name (ARN) that uniquely identifies the destination backup vault for the copied backup.
    language:
      python:
        mapCase: false
  lifecycle:
    $ref: "#/types/aws:backup/PlanRuleCopyActionLifecycle:PlanRuleCopyActionLifecycle"
    description: |
      The lifecycle defines when a protected resource is copied over to a backup vault and when it expires.  Fields documented above.
    language:
      python:
        mapCase: false
type: object
required:
  - destinationVaultArn
