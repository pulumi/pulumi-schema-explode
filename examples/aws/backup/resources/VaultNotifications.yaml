$token: aws:backup/vaultNotifications:VaultNotifications
properties:
  backupVaultArn:
    type: string
    description: |
      The ARN of the vault.
  backupVaultEvents:
    type: array
    items:
      type: string
    description: |
      An array of events that indicate the status of jobs to back up resources to the backup vault.
  backupVaultName:
    type: string
    description: |
      Name of the backup vault to add notifications for.
  snsTopicArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) that specifies the topic for a backup vault’s events
required:
  - backupVaultArn
  - backupVaultEvents
  - backupVaultName
  - snsTopicArn
inputProperties:
  backupVaultEvents:
    type: array
    items:
      type: string
    description: |
      An array of events that indicate the status of jobs to back up resources to the backup vault.
    willReplaceOnChanges: true
  backupVaultName:
    type: string
    description: |
      Name of the backup vault to add notifications for.
    willReplaceOnChanges: true
  snsTopicArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) that specifies the topic for a backup vault’s events
    willReplaceOnChanges: true
requiredInputs:
  - backupVaultEvents
  - backupVaultName
  - snsTopicArn
stateInputs:
  description: |
    Input properties used for looking up and filtering VaultNotifications resources.
  properties:
    backupVaultArn:
      type: string
      description: |
        The ARN of the vault.
    backupVaultEvents:
      type: array
      items:
        type: string
      description: |
        An array of events that indicate the status of jobs to back up resources to the backup vault.
      willReplaceOnChanges: true
    backupVaultName:
      type: string
      description: |
        Name of the backup vault to add notifications for.
      willReplaceOnChanges: true
    snsTopicArn:
      type: string
      description: |
        The Amazon Resource Name (ARN) that specifies the topic for a backup vault’s events
      willReplaceOnChanges: true
  type: object
