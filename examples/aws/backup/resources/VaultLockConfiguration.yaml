$token: aws:backup/vaultLockConfiguration:VaultLockConfiguration
properties:
  backupVaultArn:
    type: string
    description: |
      The ARN of the vault.
  backupVaultName:
    type: string
    description: |
      Name of the backup vault to add a lock configuration for.
  changeableForDays:
    type: integer
    description: |
      The number of days before the lock date.
  maxRetentionDays:
    type: integer
    description: |
      The maximum retention period that the vault retains its recovery points.
  minRetentionDays:
    type: integer
    description: |
      The minimum retention period that the vault retains its recovery points.
required:
  - backupVaultArn
  - backupVaultName
inputProperties:
  backupVaultName:
    type: string
    description: |
      Name of the backup vault to add a lock configuration for.
    willReplaceOnChanges: true
  changeableForDays:
    type: integer
    description: |
      The number of days before the lock date.
    willReplaceOnChanges: true
  maxRetentionDays:
    type: integer
    description: |
      The maximum retention period that the vault retains its recovery points.
    willReplaceOnChanges: true
  minRetentionDays:
    type: integer
    description: |
      The minimum retention period that the vault retains its recovery points.
    willReplaceOnChanges: true
requiredInputs:
  - backupVaultName
stateInputs:
  description: |
    Input properties used for looking up and filtering VaultLockConfiguration resources.
  properties:
    backupVaultArn:
      type: string
      description: |
        The ARN of the vault.
    backupVaultName:
      type: string
      description: |
        Name of the backup vault to add a lock configuration for.
      willReplaceOnChanges: true
    changeableForDays:
      type: integer
      description: |
        The number of days before the lock date.
      willReplaceOnChanges: true
    maxRetentionDays:
      type: integer
      description: |
        The maximum retention period that the vault retains its recovery points.
      willReplaceOnChanges: true
    minRetentionDays:
      type: integer
      description: |
        The minimum retention period that the vault retains its recovery points.
      willReplaceOnChanges: true
  type: object
