$token: aws:backup/plan:Plan
properties:
  advancedBackupSettings:
    type: array
    items:
      $ref: "#/types/aws:backup/PlanAdvancedBackupSetting:PlanAdvancedBackupSetting"
    description: |
      An object that specifies backup options for each resource type.
  arn:
    type: string
    description: |
      The ARN of the backup plan.
  name:
    type: string
    description: |
      The display name of a backup plan.
  rules:
    type: array
    items:
      $ref: "#/types/aws:backup/PlanRule:PlanRule"
    description: |
      A rule object that specifies a scheduled task that is used to back up a selection of resources.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Metadata that you can assign to help organize the plans you create. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  version:
    type: string
    description: |
      Unique, randomly generated, Unicode, UTF-8 encoded string that serves as the version ID of the backup plan.
required:
  - arn
  - name
  - rules
  - tagsAll
  - version
inputProperties:
  advancedBackupSettings:
    type: array
    items:
      $ref: "#/types/aws:backup/PlanAdvancedBackupSetting:PlanAdvancedBackupSetting"
    description: |
      An object that specifies backup options for each resource type.
  name:
    type: string
    description: |
      The display name of a backup plan.
    willReplaceOnChanges: true
  rules:
    type: array
    items:
      $ref: "#/types/aws:backup/PlanRule:PlanRule"
    description: |
      A rule object that specifies a scheduled task that is used to back up a selection of resources.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Metadata that you can assign to help organize the plans you create. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - rules
stateInputs:
  description: |
    Input properties used for looking up and filtering Plan resources.
  properties:
    advancedBackupSettings:
      type: array
      items:
        $ref: "#/types/aws:backup/PlanAdvancedBackupSetting:PlanAdvancedBackupSetting"
      description: |
        An object that specifies backup options for each resource type.
    arn:
      type: string
      description: |
        The ARN of the backup plan.
    name:
      type: string
      description: |
        The display name of a backup plan.
      willReplaceOnChanges: true
    rules:
      type: array
      items:
        $ref: "#/types/aws:backup/PlanRule:PlanRule"
      description: |
        A rule object that specifies a scheduled task that is used to back up a selection of resources.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Metadata that you can assign to help organize the plans you create. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    version:
      type: string
      description: |
        Unique, randomly generated, Unicode, UTF-8 encoded string that serves as the version ID of the backup plan.
  type: object
