$token: aws:backup/vaultPolicy:VaultPolicy
properties:
  backupVaultArn:
    type: string
    description: |
      The ARN of the vault.
  backupVaultName:
    type: string
    description: |
      Name of the backup vault to add policy for.
  policy:
    type: string
    description: |
      The backup vault access policy document in JSON format.
required:
  - backupVaultArn
  - backupVaultName
  - policy
inputProperties:
  backupVaultName:
    type: string
    description: |
      Name of the backup vault to add policy for.
    willReplaceOnChanges: true
  policy:
    type: string
    description: |
      The backup vault access policy document in JSON format.
requiredInputs:
  - backupVaultName
  - policy
stateInputs:
  description: |
    Input properties used for looking up and filtering VaultPolicy resources.
  properties:
    backupVaultArn:
      type: string
      description: |
        The ARN of the vault.
    backupVaultName:
      type: string
      description: |
        Name of the backup vault to add policy for.
      willReplaceOnChanges: true
    policy:
      type: string
      description: |
        The backup vault access policy document in JSON format.
  type: object
