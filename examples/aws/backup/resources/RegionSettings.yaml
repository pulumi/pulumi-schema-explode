$token: aws:backup/regionSettings:RegionSettings
properties:
  resourceTypeManagementPreference:
    type: object
    additionalProperties:
      type: boolean
    description: |
      A map of services along with the management preferences for the Region.
  resourceTypeOptInPreference:
    type: object
    additionalProperties:
      type: boolean
    description: |
      A map of services along with the opt-in preferences for the Region.
required:
  - resourceTypeManagementPreference
  - resourceTypeOptInPreference
inputProperties:
  resourceTypeManagementPreference:
    type: object
    additionalProperties:
      type: boolean
    description: |
      A map of services along with the management preferences for the Region.
  resourceTypeOptInPreference:
    type: object
    additionalProperties:
      type: boolean
    description: |
      A map of services along with the opt-in preferences for the Region.
requiredInputs:
  - resourceTypeOptInPreference
stateInputs:
  description: |
    Input properties used for looking up and filtering RegionSettings resources.
  properties:
    resourceTypeManagementPreference:
      type: object
      additionalProperties:
        type: boolean
      description: |
        A map of services along with the management preferences for the Region.
    resourceTypeOptInPreference:
      type: object
      additionalProperties:
        type: boolean
      description: |
        A map of services along with the opt-in preferences for the Region.
  type: object
