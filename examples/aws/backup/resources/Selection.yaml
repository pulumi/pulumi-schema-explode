$token: aws:backup/selection:Selection
properties:
  conditions:
    type: array
    items:
      $ref: "#/types/aws:backup/SelectionCondition:SelectionCondition"
    description: |
      A list of conditions that you define to assign resources to your backup plans using tags.
  iamRoleArn:
    type: string
    description: |
      The ARN of the IAM role that AWS Backup uses to authenticate when restoring and backing up the target resource. See the [AWS Backup Developer Guide](https://docs.aws.amazon.com/aws-backup/latest/devguide/access-control.html#managed-policies) for additional information about using AWS managed policies or creating custom policies attached to the IAM role.
  name:
    type: string
    description: |
      The display name of a resource selection document.
  notResources:
    type: array
    items:
      type: string
    description: |
      An array of strings that either contain Amazon Resource Names (ARNs) or match patterns of resources to exclude from a backup plan.
  planId:
    type: string
    description: |
      The backup plan ID to be associated with the selection of resources.
  resources:
    type: array
    items:
      type: string
    description: |
      An array of strings that either contain Amazon Resource Names (ARNs) or match patterns of resources to assign to a backup plan.
  selectionTags:
    type: array
    items:
      $ref: "#/types/aws:backup/SelectionSelectionTag:SelectionSelectionTag"
    description: |
      Tag-based conditions used to specify a set of resources to assign to a backup plan.
required:
  - conditions
  - iamRoleArn
  - name
  - notResources
  - planId
inputProperties:
  conditions:
    type: array
    items:
      $ref: "#/types/aws:backup/SelectionCondition:SelectionCondition"
    description: |
      A list of conditions that you define to assign resources to your backup plans using tags.
    willReplaceOnChanges: true
  iamRoleArn:
    type: string
    description: |
      The ARN of the IAM role that AWS Backup uses to authenticate when restoring and backing up the target resource. See the [AWS Backup Developer Guide](https://docs.aws.amazon.com/aws-backup/latest/devguide/access-control.html#managed-policies) for additional information about using AWS managed policies or creating custom policies attached to the IAM role.
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      The display name of a resource selection document.
    willReplaceOnChanges: true
  notResources:
    type: array
    items:
      type: string
    description: |
      An array of strings that either contain Amazon Resource Names (ARNs) or match patterns of resources to exclude from a backup plan.
    willReplaceOnChanges: true
  planId:
    type: string
    description: |
      The backup plan ID to be associated with the selection of resources.
    willReplaceOnChanges: true
  resources:
    type: array
    items:
      type: string
    description: |
      An array of strings that either contain Amazon Resource Names (ARNs) or match patterns of resources to assign to a backup plan.
    willReplaceOnChanges: true
  selectionTags:
    type: array
    items:
      $ref: "#/types/aws:backup/SelectionSelectionTag:SelectionSelectionTag"
    description: |
      Tag-based conditions used to specify a set of resources to assign to a backup plan.
    willReplaceOnChanges: true
requiredInputs:
  - iamRoleArn
  - planId
stateInputs:
  description: |
    Input properties used for looking up and filtering Selection resources.
  properties:
    conditions:
      type: array
      items:
        $ref: "#/types/aws:backup/SelectionCondition:SelectionCondition"
      description: |
        A list of conditions that you define to assign resources to your backup plans using tags.
      willReplaceOnChanges: true
    iamRoleArn:
      type: string
      description: |
        The ARN of the IAM role that AWS Backup uses to authenticate when restoring and backing up the target resource. See the [AWS Backup Developer Guide](https://docs.aws.amazon.com/aws-backup/latest/devguide/access-control.html#managed-policies) for additional information about using AWS managed policies or creating custom policies attached to the IAM role.
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        The display name of a resource selection document.
      willReplaceOnChanges: true
    notResources:
      type: array
      items:
        type: string
      description: |
        An array of strings that either contain Amazon Resource Names (ARNs) or match patterns of resources to exclude from a backup plan.
      willReplaceOnChanges: true
    planId:
      type: string
      description: |
        The backup plan ID to be associated with the selection of resources.
      willReplaceOnChanges: true
    resources:
      type: array
      items:
        type: string
      description: |
        An array of strings that either contain Amazon Resource Names (ARNs) or match patterns of resources to assign to a backup plan.
      willReplaceOnChanges: true
    selectionTags:
      type: array
      items:
        $ref: "#/types/aws:backup/SelectionSelectionTag:SelectionSelectionTag"
      description: |
        Tag-based conditions used to specify a set of resources to assign to a backup plan.
      willReplaceOnChanges: true
  type: object
