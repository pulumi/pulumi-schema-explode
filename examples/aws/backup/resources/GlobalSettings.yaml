$token: aws:backup/globalSettings:GlobalSettings
properties:
  globalSettings:
    type: object
    additionalProperties:
      type: string
    description: |
      A list of resources along with the opt-in preferences for the account.
    language:
      csharp:
        name: GlobalSettingsList
required:
  - globalSettings
inputProperties:
  globalSettings:
    type: object
    additionalProperties:
      type: string
    description: |
      A list of resources along with the opt-in preferences for the account.
    language:
      csharp:
        name: GlobalSettingsList
requiredInputs:
  - globalSettings
stateInputs:
  description: |
    Input properties used for looking up and filtering GlobalSettings resources.
  properties:
    globalSettings:
      type: object
      additionalProperties:
        type: string
      description: |
        A list of resources along with the opt-in preferences for the account.
      language:
        csharp:
          name: GlobalSettingsList
  type: object
