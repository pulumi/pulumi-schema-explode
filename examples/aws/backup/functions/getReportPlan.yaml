$token: aws:backup/getReportPlan:getReportPlan
inputs:
  description: |
    A collection of arguments for invoking getReportPlan.
  properties:
    name:
      type: string
      description: |
        Backup report plan name.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Metadata that you can assign to help organize the report plans you create.
  type: object
  required:
    - name
outputs:
  description: |
    A collection of values returned by getReportPlan.
  properties:
    arn:
      type: string
      description: |
        ARN of the backup report plan.
    creationTime:
      type: string
      description: |
        Date and time that a report plan is created, in Unix format and Coordinated Universal Time (UTC).
    deploymentStatus:
      type: string
      description: |
        Deployment status of a report plan. The statuses are: `CREATE_IN_PROGRESS` | `UPDATE_IN_PROGRESS` | `DELETE_IN_PROGRESS` | `COMPLETED`.
    description:
      type: string
      description: |
        Description of the report plan.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    name:
      type: string
    reportDeliveryChannels:
      type: array
      items:
        $ref: "#/types/aws:backup/getReportPlanReportDeliveryChannel:getReportPlanReportDeliveryChannel"
      description: |
        An object that contains information about where and how to deliver your reports, specifically your Amazon S3 bucket name, S3 key prefix, and the formats of your reports. Detailed below.
    reportSettings:
      type: array
      items:
        $ref: "#/types/aws:backup/getReportPlanReportSetting:getReportPlanReportSetting"
      description: |
        An object that identifies the report template for the report. Reports are built using a report template. Detailed below.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Metadata that you can assign to help organize the report plans you create.
  type: object
  required:
    - arn
    - creationTime
    - deploymentStatus
    - description
    - name
    - reportDeliveryChannels
    - reportSettings
    - tags
    - id
