$token: aws:macie2/organizationAdminAccount:OrganizationAdminAccount
properties:
  adminAccountId:
    type: string
    description: |
      The AWS account ID for the account to designate as the delegated Amazon Macie administrator account for the organization.
required:
  - adminAccountId
inputProperties:
  adminAccountId:
    type: string
    description: |
      The AWS account ID for the account to designate as the delegated Amazon Macie administrator account for the organization.
    willReplaceOnChanges: true
requiredInputs:
  - adminAccountId
stateInputs:
  description: |
    Input properties used for looking up and filtering OrganizationAdminAccount resources.
  properties:
    adminAccountId:
      type: string
      description: |
        The AWS account ID for the account to designate as the delegated Amazon Macie administrator account for the organization.
      willReplaceOnChanges: true
  type: object
