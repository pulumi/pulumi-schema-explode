$token: aws:macie2/classificationExportConfiguration:ClassificationExportConfiguration
properties:
  s3Destination:
    $ref: "#/types/aws:macie2/ClassificationExportConfigurationS3Destination:ClassificationExportConfigurationS3Destination"
    description: |
      Configuration block for a S3 Destination. Defined below
inputProperties:
  s3Destination:
    $ref: "#/types/aws:macie2/ClassificationExportConfigurationS3Destination:ClassificationExportConfigurationS3Destination"
    description: |
      Configuration block for a S3 Destination. Defined below
stateInputs:
  description: |
    Input properties used for looking up and filtering ClassificationExportConfiguration resources.
  properties:
    s3Destination:
      $ref: "#/types/aws:macie2/ClassificationExportConfigurationS3Destination:ClassificationExportConfigurationS3Destination"
      description: |
        Configuration block for a S3 Destination. Defined below
  type: object
