$token: aws:macie2/ClassificationJobS3JobDefinitionScopingExcludes:ClassificationJobS3JobDefinitionScopingExcludes
properties:
  ands:
    type: array
    items:
      $ref: "#/types/aws:macie2/ClassificationJobS3JobDefinitionScopingExcludesAnd:ClassificationJobS3JobDefinitionScopingExcludesAnd"
    description: |
      An array of conditions, one for each condition that determines which objects to include or exclude from the job. (documented below)
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - ands
