$token: aws:macie2/ClassificationJobS3JobDefinitionScopingIncludesAnd:ClassificationJobS3JobDefinitionScopingIncludesAnd
properties:
  simpleScopeTerm:
    $ref: "#/types/aws:macie2/ClassificationJobS3JobDefinitionScopingIncludesAndSimpleScopeTerm:ClassificationJobS3JobDefinitionScopingIncludesAndSimpleScopeTerm"
    description: |
      A property-based condition that defines a property, operator, and one or more values for including or excluding an object from the job. (documented below)
    language:
      python:
        mapCase: false
  tagScopeTerm:
    $ref: "#/types/aws:macie2/ClassificationJobS3JobDefinitionScopingIncludesAndTagScopeTerm:ClassificationJobS3JobDefinitionScopingIncludesAndTagScopeTerm"
    description: |
      A tag-based condition that defines the operator and tag keys or tag key and value pairs for including or excluding an object from the job. (documented below)
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - simpleScopeTerm
      - tagScopeTerm
