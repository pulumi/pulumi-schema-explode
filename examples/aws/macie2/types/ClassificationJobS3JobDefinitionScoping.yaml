$token: aws:macie2/ClassificationJobS3JobDefinitionScoping:ClassificationJobS3JobDefinitionScoping
properties:
  excludes:
    $ref: "#/types/aws:macie2/ClassificationJobS3JobDefinitionScopingExcludes:ClassificationJobS3JobDefinitionScopingExcludes"
    description: |
      The property- or tag-based conditions that determine which objects to exclude from the analysis. (documented below)
    language:
      python:
        mapCase: false
  includes:
    $ref: "#/types/aws:macie2/ClassificationJobS3JobDefinitionScopingIncludes:ClassificationJobS3JobDefinitionScopingIncludes"
    description: |
      The property- or tag-based conditions that determine which objects to include in the analysis. (documented below)
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - excludes
      - includes
