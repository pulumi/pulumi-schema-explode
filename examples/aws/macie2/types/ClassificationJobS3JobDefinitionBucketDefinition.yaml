$token: aws:macie2/ClassificationJobS3JobDefinitionBucketDefinition:ClassificationJobS3JobDefinitionBucketDefinition
properties:
  accountId:
    type: string
    description: |
      The unique identifier for the AWS account that owns the buckets.
    language:
      python:
        mapCase: false
  buckets:
    type: array
    items:
      type: string
    description: |
      An array that lists the names of the buckets.
    language:
      python:
        mapCase: false
type: object
required:
  - accountId
  - buckets
