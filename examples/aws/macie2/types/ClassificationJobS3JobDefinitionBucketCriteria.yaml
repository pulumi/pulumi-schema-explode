$token: aws:macie2/ClassificationJobS3JobDefinitionBucketCriteria:ClassificationJobS3JobDefinitionBucketCriteria
properties:
  excludes:
    $ref: "#/types/aws:macie2/ClassificationJobS3JobDefinitionBucketCriteriaExcludes:ClassificationJobS3JobDefinitionBucketCriteriaExcludes"
    description: |
      The property- or tag-based conditions that determine which S3 buckets to exclude from the analysis. (documented below)
    language:
      python:
        mapCase: false
  includes:
    $ref: "#/types/aws:macie2/ClassificationJobS3JobDefinitionBucketCriteriaIncludes:ClassificationJobS3JobDefinitionBucketCriteriaIncludes"
    description: |
      The property- or tag-based conditions that determine which S3 buckets to include in the analysis. (documented below)
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - excludes
      - includes
