$token: aws:macie2/ClassificationJobS3JobDefinition:ClassificationJobS3JobDefinition
properties:
  bucketCriteria:
    $ref: "#/types/aws:macie2/ClassificationJobS3JobDefinitionBucketCriteria:ClassificationJobS3JobDefinitionBucketCriteria"
    description: |
      The property- and tag-based conditions that determine which S3 buckets to include or exclude from the analysis. Conflicts with `bucket_definitions`. (documented below)
    language:
      python:
        mapCase: false
  bucketDefinitions:
    type: array
    items:
      $ref: "#/types/aws:macie2/ClassificationJobS3JobDefinitionBucketDefinition:ClassificationJobS3JobDefinitionBucketDefinition"
    description: |
      An array of objects, one for each AWS account that owns buckets to analyze. Each object specifies the account ID for an account and one or more buckets to analyze for the account. Conflicts with `bucket_criteria`. (documented below)
    language:
      python:
        mapCase: false
  scoping:
    $ref: "#/types/aws:macie2/ClassificationJobS3JobDefinitionScoping:ClassificationJobS3JobDefinitionScoping"
    description: |
      The property- and tag-based conditions that determine which objects to include or exclude from the analysis. (documented below)
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - bucketCriteria
      - scoping
