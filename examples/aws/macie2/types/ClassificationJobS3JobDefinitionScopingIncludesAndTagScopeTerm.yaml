$token: aws:macie2/ClassificationJobS3JobDefinitionScopingIncludesAndTagScopeTerm:ClassificationJobS3JobDefinitionScopingIncludesAndTagScopeTerm
properties:
  comparator:
    type: string
    description: |
      The operator to use in the condition.
    language:
      python:
        mapCase: false
  key:
    type: string
    description: |
      The tag key to use in the condition. The only valid value is `TAG`.
    language:
      python:
        mapCase: false
  tagValues:
    type: array
    items:
      $ref: "#/types/aws:macie2/ClassificationJobS3JobDefinitionScopingIncludesAndTagScopeTermTagValue:ClassificationJobS3JobDefinitionScopingIncludesAndTagScopeTermTagValue"
    description: |
      The tag keys or tag key and value pairs to use in the condition.
    language:
      python:
        mapCase: false
  target:
    type: string
    description: |
      The type of object to apply the condition to. The only valid value is `S3_OBJECT`.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - comparator
      - key
      - target
