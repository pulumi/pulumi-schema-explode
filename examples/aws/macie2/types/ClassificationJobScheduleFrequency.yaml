$token: aws:macie2/ClassificationJobScheduleFrequency:ClassificationJobScheduleFrequency
properties:
  dailySchedule:
    type: boolean
    description: |
      Specifies a daily recurrence pattern for running the job.
    language:
      python:
        mapCase: false
  monthlySchedule:
    type: integer
    description: |
      Specifies a monthly recurrence pattern for running the job.
    language:
      python:
        mapCase: false
  weeklySchedule:
    type: string
    description: |
      Specifies a weekly recurrence pattern for running the job.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - monthlySchedule
      - weeklySchedule
