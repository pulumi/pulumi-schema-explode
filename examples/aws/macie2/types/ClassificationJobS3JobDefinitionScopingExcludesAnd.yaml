$token: aws:macie2/ClassificationJobS3JobDefinitionScopingExcludesAnd:ClassificationJobS3JobDefinitionScopingExcludesAnd
properties:
  simpleScopeTerm:
    $ref: "#/types/aws:macie2/ClassificationJobS3JobDefinitionScopingExcludesAndSimpleScopeTerm:ClassificationJobS3JobDefinitionScopingExcludesAndSimpleScopeTerm"
    description: |
      A property-based condition that defines a property, operator, and one or more values for including or excluding an object from the job. (documented below)
    language:
      python:
        mapCase: false
  tagScopeTerm:
    $ref: "#/types/aws:macie2/ClassificationJobS3JobDefinitionScopingExcludesAndTagScopeTerm:ClassificationJobS3JobDefinitionScopingExcludesAndTagScopeTerm"
    description: |
      A tag-based condition that defines the operator and tag keys or tag key and value pairs for including or excluding an object from the job. (documented below)
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - simpleScopeTerm
      - tagScopeTerm
