$token: aws:lex/botAlias:BotAlias
properties:
  arn:
    type: string
    description: |
      The ARN of the bot alias.
  botName:
    type: string
    description: |
      The name of the bot.
  botVersion:
    type: string
    description: |
      The name of the bot.
  checksum:
    type: string
    description: |
      Checksum of the bot alias.
  conversationLogs:
    $ref: "#/types/aws:lex/BotAliasConversationLogs:BotAliasConversationLogs"
    description: |
      The settings that determine how Amazon Lex uses conversation logs for the alias. Attributes are documented under conversation_logs.
  createdDate:
    type: string
    description: |
      The date that the bot alias was created.
  description:
    type: string
    description: |
      A description of the alias. Must be less than or equal to 200 characters in length.
  lastUpdatedDate:
    type: string
    description: |
      The date that the bot alias was updated. When you create a resource, the creation date and the last updated date are the same.
  name:
    type: string
    description: |
      The name of the alias. The name is not case sensitive. Must be less than or equal to 100 characters in length.
required:
  - arn
  - botName
  - botVersion
  - checksum
  - createdDate
  - lastUpdatedDate
  - name
inputProperties:
  botName:
    type: string
    description: |
      The name of the bot.
    willReplaceOnChanges: true
  botVersion:
    type: string
    description: |
      The name of the bot.
  conversationLogs:
    $ref: "#/types/aws:lex/BotAliasConversationLogs:BotAliasConversationLogs"
    description: |
      The settings that determine how Amazon Lex uses conversation logs for the alias. Attributes are documented under conversation_logs.
  description:
    type: string
    description: |
      A description of the alias. Must be less than or equal to 200 characters in length.
  name:
    type: string
    description: |
      The name of the alias. The name is not case sensitive. Must be less than or equal to 100 characters in length.
    willReplaceOnChanges: true
requiredInputs:
  - botName
  - botVersion
stateInputs:
  description: |
    Input properties used for looking up and filtering BotAlias resources.
  properties:
    arn:
      type: string
      description: |
        The ARN of the bot alias.
    botName:
      type: string
      description: |
        The name of the bot.
      willReplaceOnChanges: true
    botVersion:
      type: string
      description: |
        The name of the bot.
    checksum:
      type: string
      description: |
        Checksum of the bot alias.
    conversationLogs:
      $ref: "#/types/aws:lex/BotAliasConversationLogs:BotAliasConversationLogs"
      description: |
        The settings that determine how Amazon Lex uses conversation logs for the alias. Attributes are documented under conversation_logs.
    createdDate:
      type: string
      description: |
        The date that the bot alias was created.
    description:
      type: string
      description: |
        A description of the alias. Must be less than or equal to 200 characters in length.
    lastUpdatedDate:
      type: string
      description: |
        The date that the bot alias was updated. When you create a resource, the creation date and the last updated date are the same.
    name:
      type: string
      description: |
        The name of the alias. The name is not case sensitive. Must be less than or equal to 100 characters in length.
      willReplaceOnChanges: true
  type: object
