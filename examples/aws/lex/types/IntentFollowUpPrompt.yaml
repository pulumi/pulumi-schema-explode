$token: aws:lex/IntentFollowUpPrompt:IntentFollowUpPrompt
properties:
  prompt:
    $ref: "#/types/aws:lex/IntentFollowUpPromptPrompt:IntentFollowUpPromptPrompt"
    description: |
      Prompts for information from the user. Attributes are documented under prompt.
    language:
      python:
        mapCase: false
  rejectionStatement:
    $ref: "#/types/aws:lex/IntentFollowUpPromptRejectionStatement:IntentFollowUpPromptRejectionStatement"
    description: |
      If the user answers "no" to the question defined in the prompt field,
      Amazon Lex responds with this statement to acknowledge that the intent was canceled. Attributes are
      documented below under statement.
    language:
      python:
        mapCase: false
type: object
required:
  - prompt
  - rejectionStatement
