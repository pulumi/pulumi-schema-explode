$token: aws:lex/IntentFulfillmentActivity:IntentFulfillmentActivity
properties:
  codeHook:
    $ref: "#/types/aws:lex/IntentFulfillmentActivityCodeHook:IntentFulfillmentActivityCodeHook"
    description: |
      A description of the Lambda function that is run to fulfill the intent.
      Required if type is CodeHook. Attributes are documented under code_hook.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      How the intent should be fulfilled, either by running a Lambda function or by
      returning the slot data to the client application. Type can be either `ReturnIntent` or `CodeHook`, as documented [here](https://docs.aws.amazon.com/lex/latest/dg/API_FulfillmentActivity.html).
    language:
      python:
        mapCase: false
type: object
required:
  - type
