$token: aws:lex/BotIntent:BotIntent
properties:
  intentName:
    type: string
    description: |
      The name of the intent. Must be less than or equal to 100 characters in length.
    language:
      python:
        mapCase: false
  intentVersion:
    type: string
    description: |
      The version of the intent. Must be less than or equal to 64 characters in length.
    language:
      python:
        mapCase: false
type: object
required:
  - intentName
  - intentVersion
