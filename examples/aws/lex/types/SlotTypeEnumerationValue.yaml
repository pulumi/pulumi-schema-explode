$token: aws:lex/SlotTypeEnumerationValue:SlotTypeEnumerationValue
properties:
  synonyms:
    type: array
    items:
      type: string
    description: |
      Additional values related to the slot type value. Each item must be less than or equal to 140 characters in length.
    language:
      python:
        mapCase: false
  value:
    type: string
    description: |
      The value of the slot type. Must be less than or equal to 140 characters in length.
    language:
      python:
        mapCase: false
type: object
required:
  - value
