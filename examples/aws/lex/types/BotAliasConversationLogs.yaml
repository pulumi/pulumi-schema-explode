$token: aws:lex/BotAliasConversationLogs:BotAliasConversationLogs
properties:
  iamRoleArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the IAM role used to write your logs to CloudWatch Logs or an S3 bucket. Must be between 20 and 2048 characters in length.
    language:
      python:
        mapCase: false
  logSettings:
    type: array
    items:
      $ref: "#/types/aws:lex/BotAliasConversationLogsLogSetting:BotAliasConversationLogsLogSetting"
    description: |
      The settings for your conversation logs. You can log text, audio, or both. Attributes are documented under log_settings.
    language:
      python:
        mapCase: false
type: object
required:
  - iamRoleArn
