$token: aws:sagemaker/EndpointConfigurationProductionVariantServerlessConfig:EndpointConfigurationProductionVariantServerlessConfig
properties:
  maxConcurrency:
    type: integer
    description: |
      The maximum number of concurrent invocations your serverless endpoint can process. Valid values are between `1` and `200`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  memorySizeInMb:
    type: integer
    description: |
      The memory size of your serverless endpoint. Valid values are in 1 GB increments: `1024` MB, `2048` MB, `3072` MB, `4096` MB, `5120` MB, or `6144` MB.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - maxConcurrency
  - memorySizeInMb
