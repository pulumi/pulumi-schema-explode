$token: aws:sagemaker/HumanTaskUIUiTemplate:HumanTaskUIUiTemplate
properties:
  content:
    type: string
    description: |
      The content of the Liquid template for the worker user interface.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  contentSha256:
    type: string
    description: |
      The SHA-256 digest of the contents of the template.
    language:
      python:
        mapCase: false
  url:
    type: string
    description: |
      The URL for the user interface template.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - contentSha256
      - url
