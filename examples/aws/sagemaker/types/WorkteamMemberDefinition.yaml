$token: aws:sagemaker/WorkteamMemberDefinition:WorkteamMemberDefinition
properties:
  cognitoMemberDefinition:
    $ref: "#/types/aws:sagemaker/WorkteamMemberDefinitionCognitoMemberDefinition:WorkteamMemberDefinitionCognitoMemberDefinition"
    description: |
      The Amazon Cognito user group that is part of the work team. See Cognito Member Definition details below.
    language:
      python:
        mapCase: false
  oidcMemberDefinition:
    $ref: "#/types/aws:sagemaker/WorkteamMemberDefinitionOidcMemberDefinition:WorkteamMemberDefinitionOidcMemberDefinition"
    description: |
      A list user groups that exist in your OIDC Identity Provider (IdP). One to ten groups can be used to create a single private work team. See Cognito Member Definition details below.
    language:
      python:
        mapCase: false
type: object
