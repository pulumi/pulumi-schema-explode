$token: aws:sagemaker/WorkteamMemberDefinitionCognitoMemberDefinition:WorkteamMemberDefinitionCognitoMemberDefinition
properties:
  clientId:
    type: string
    description: |
      An identifier for an application client. You must create the app client ID using Amazon Cognito.
    language:
      python:
        mapCase: false
  userGroup:
    type: string
    description: |
      An identifier for a user group.
    language:
      python:
        mapCase: false
  userPool:
    type: string
    description: |
      An identifier for a user pool. The user pool must be in the same region as the service that you are calling.
    language:
      python:
        mapCase: false
type: object
required:
  - clientId
  - userGroup
  - userPool
