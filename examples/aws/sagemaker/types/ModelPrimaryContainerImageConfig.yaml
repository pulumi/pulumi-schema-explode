$token: aws:sagemaker/ModelPrimaryContainerImageConfig:ModelPrimaryContainerImageConfig
properties:
  repositoryAccessMode:
    type: string
    description: |
      Specifies whether the model container is in Amazon ECR or a private Docker registry accessible from your Amazon Virtual Private Cloud (VPC). Allowed values are: `Platform` and `Vpc`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  repositoryAuthConfig:
    $ref: "#/types/aws:sagemaker/ModelPrimaryContainerImageConfigRepositoryAuthConfig:ModelPrimaryContainerImageConfigRepositoryAuthConfig"
    description: |
      Specifies an authentication configuration for the private docker registry where your model image is hosted. Specify a value for this property only if you specified Vpc as the value for the RepositoryAccessMode field, and the private Docker registry where the model image is hosted requires authentication. see Repository Auth Config.
    language:
      python:
        mapCase: false
type: object
required:
  - repositoryAccessMode
