$token: aws:sagemaker/AppImageConfigKernelGatewayImageConfigFileSystemConfig:AppImageConfigKernelGatewayImageConfigFileSystemConfig
properties:
  defaultGid:
    type: integer
    description: |
      The default POSIX group ID (GID). If not specified, defaults to `100`. Valid values are `0` and `100`.
    language:
      python:
        mapCase: false
  defaultUid:
    type: integer
    description: |
      The default POSIX user ID (UID). If not specified, defaults to `1000`. Valid values are `0` and `1000`.
    language:
      python:
        mapCase: false
  mountPath:
    type: string
    description: |
      The path within the image to mount the user's EFS home directory. The directory should be empty. If not specified, defaults to `/home/sagemaker-user`.
    language:
      python:
        mapCase: false
type: object
