$token: aws:sagemaker/DomainRetentionPolicy:DomainRetentionPolicy
properties:
  homeEfsFileSystem:
    type: string
    description: |
      The retention policy for data stored on an Amazon Elastic File System (EFS) volume. Valid values are `Retain` or `Delete`.  Default value is `Retain`.
    language:
      python:
        mapCase: false
type: object
