$token: aws:sagemaker/DomainDefaultUserSettings:DomainDefaultUserSettings
properties:
  executionRole:
    type: string
    description: |
      The execution role ARN for the user.
    language:
      python:
        mapCase: false
  jupyterServerAppSettings:
    $ref: "#/types/aws:sagemaker/DomainDefaultUserSettingsJupyterServerAppSettings:DomainDefaultUserSettingsJupyterServerAppSettings"
    description: |
      The Jupyter server's app settings. See Jupyter Server App Settings below.
    language:
      python:
        mapCase: false
  kernelGatewayAppSettings:
    $ref: "#/types/aws:sagemaker/DomainDefaultUserSettingsKernelGatewayAppSettings:DomainDefaultUserSettingsKernelGatewayAppSettings"
    description: |
      The kernel gateway app settings. See Kernel Gateway App Settings below.
    language:
      python:
        mapCase: false
  securityGroups:
    type: array
    items:
      type: string
    description: |
      A list of security group IDs that will be attached to the user.
    language:
      python:
        mapCase: false
  sharingSettings:
    $ref: "#/types/aws:sagemaker/DomainDefaultUserSettingsSharingSettings:DomainDefaultUserSettingsSharingSettings"
    description: |
      The sharing settings. See Sharing Settings below.
    language:
      python:
        mapCase: false
  tensorBoardAppSettings:
    $ref: "#/types/aws:sagemaker/DomainDefaultUserSettingsTensorBoardAppSettings:DomainDefaultUserSettingsTensorBoardAppSettings"
    description: |
      The TensorBoard app settings. See TensorBoard App Settings below.
    language:
      python:
        mapCase: false
type: object
required:
  - executionRole
