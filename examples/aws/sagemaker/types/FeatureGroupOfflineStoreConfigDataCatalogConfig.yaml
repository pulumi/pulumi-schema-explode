$token: aws:sagemaker/FeatureGroupOfflineStoreConfigDataCatalogConfig:FeatureGroupOfflineStoreConfigDataCatalogConfig
properties:
  catalog:
    type: string
    description: |
      The name of the Glue table catalog.
    language:
      python:
        mapCase: false
  database:
    type: string
    description: |
      The name of the Glue table database.
    language:
      python:
        mapCase: false
  tableName:
    type: string
    description: |
      The name of the Glue table.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - catalog
      - database
      - tableName
