$token: aws:sagemaker/ProjectServiceCatalogProvisioningDetails:ProjectServiceCatalogProvisioningDetails
properties:
  pathId:
    type: string
    description: |
      The path identifier of the product. This value is optional if the product has a default path, and required if the product has more than one path.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  productId:
    type: string
    description: |
      The ID of the product to provision.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  provisioningArtifactId:
    type: string
    description: |
      The ID of the provisioning artifact.
    language:
      python:
        mapCase: false
  provisioningParameters:
    type: array
    items:
      $ref: "#/types/aws:sagemaker/ProjectServiceCatalogProvisioningDetailsProvisioningParameter:ProjectServiceCatalogProvisioningDetailsProvisioningParameter"
    description: |
      A list of key value pairs that you specify when you provision a product. See Provisioning Parameter below.
    language:
      python:
        mapCase: false
type: object
required:
  - productId
language:
  nodejs:
    requiredOutputs:
      - productId
      - provisioningArtifactId
