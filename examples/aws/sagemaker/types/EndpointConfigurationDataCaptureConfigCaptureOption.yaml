$token: aws:sagemaker/EndpointConfigurationDataCaptureConfigCaptureOption:EndpointConfigurationDataCaptureConfigCaptureOption
properties:
  captureMode:
    type: string
    description: |
      Specifies the data to be captured. Should be one of `Input` or `Output`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - captureMode
