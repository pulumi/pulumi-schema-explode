$token: aws:sagemaker/EndpointDeploymentConfig:EndpointDeploymentConfig
properties:
  autoRollbackConfiguration:
    $ref: "#/types/aws:sagemaker/EndpointDeploymentConfigAutoRollbackConfiguration:EndpointDeploymentConfigAutoRollbackConfiguration"
    description: |
      Automatic rollback configuration for handling endpoint deployment failures and recovery. See Auto Rollback Configuration.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  blueGreenUpdatePolicy:
    $ref: "#/types/aws:sagemaker/EndpointDeploymentConfigBlueGreenUpdatePolicy:EndpointDeploymentConfigBlueGreenUpdatePolicy"
    description: |
      Update policy for a blue/green deployment. If this update policy is specified, SageMaker creates a new fleet during the deployment while maintaining the old fleet. See Blue Green Update Config.
    language:
      python:
        mapCase: false
type: object
required:
  - blueGreenUpdatePolicy
