$token: aws:sagemaker/EndpointConfigurationAsyncInferenceConfig:EndpointConfigurationAsyncInferenceConfig
properties:
  clientConfig:
    $ref: "#/types/aws:sagemaker/EndpointConfigurationAsyncInferenceConfigClientConfig:EndpointConfigurationAsyncInferenceConfigClientConfig"
    description: |
      Configures the behavior of the client used by Amazon SageMaker to interact with the model container during asynchronous inference.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  outputConfig:
    $ref: "#/types/aws:sagemaker/EndpointConfigurationAsyncInferenceConfigOutputConfig:EndpointConfigurationAsyncInferenceConfigOutputConfig"
    description: |
      Specifies the configuration for asynchronous inference invocation outputs.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - outputConfig
