$token: aws:sagemaker/FeatureGroupOnlineStoreConfig:FeatureGroupOnlineStoreConfig
properties:
  enableOnlineStore:
    type: boolean
    description: |
      Set to `true` to disable the automatic creation of an AWS Glue table when configuring an OfflineStore.
    language:
      python:
        mapCase: false
  securityConfig:
    $ref: "#/types/aws:sagemaker/FeatureGroupOnlineStoreConfigSecurityConfig:FeatureGroupOnlineStoreConfigSecurityConfig"
    description: |
      Security config for at-rest encryption of your OnlineStore. See Security Config Below.
    language:
      python:
        mapCase: false
type: object
