$token: aws:sagemaker/FeatureGroupOfflineStoreConfigS3StorageConfig:FeatureGroupOfflineStoreConfigS3StorageConfig
properties:
  kmsKeyId:
    type: string
    description: |
      The ID of the AWS Key Management Service (AWS KMS) key that SageMaker Feature Store uses to encrypt the Amazon S3 objects at rest using Amazon S3 server-side encryption.
    language:
      python:
        mapCase: false
  s3Uri:
    type: string
    description: |
      The S3 URI, or location in Amazon S3, of OfflineStore.
    language:
      python:
        mapCase: false
type: object
required:
  - s3Uri
