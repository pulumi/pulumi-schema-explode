$token: aws:sagemaker/EndpointDeploymentConfigAutoRollbackConfiguration:EndpointDeploymentConfigAutoRollbackConfiguration
properties:
  alarms:
    type: array
    items:
      $ref: "#/types/aws:sagemaker/EndpointDeploymentConfigAutoRollbackConfigurationAlarm:EndpointDeploymentConfigAutoRollbackConfigurationAlarm"
    description: |
      List of CloudWatch alarms in your account that are configured to monitor metrics on an endpoint. If any alarms are tripped during a deployment, SageMaker rolls back the deployment. See Alarms.
    language:
      python:
        mapCase: false
type: object
