$token: aws:sagemaker/ModelInferenceExecutionConfig:ModelInferenceExecutionConfig
properties:
  mode:
    type: string
    description: |
      The container hosts value `SingleModel/MultiModel`. The default value is `SingleModel`.
    language:
      python:
        mapCase: false
type: object
required:
  - mode
