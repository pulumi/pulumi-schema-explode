$token: aws:sagemaker/FeatureGroupFeatureDefinition:FeatureGroupFeatureDefinition
properties:
  featureName:
    type: string
    description: |
      The name of a feature. `feature_name` cannot be any of the following: `is_deleted`, `write_time`, `api_invocation_time`.
    language:
      python:
        mapCase: false
  featureType:
    type: string
    description: |
      The value type of a feature. Valid values are `Integral`, `Fractional`, or `String`.
    language:
      python:
        mapCase: false
type: object
