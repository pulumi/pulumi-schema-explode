$token: aws:sagemaker/codeRepository:CodeRepository
properties:
  arn:
    type: string
    description: |
      The Amazon Resource Name (ARN) assigned by AWS to this Code Repository.
  codeRepositoryName:
    type: string
    description: |
      The name of the Code Repository (must be unique).
  gitConfig:
    $ref: "#/types/aws:sagemaker/CodeRepositoryGitConfig:CodeRepositoryGitConfig"
    description: |
      Specifies details about the repository. see Git Config details below.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - codeRepositoryName
  - gitConfig
  - tagsAll
inputProperties:
  codeRepositoryName:
    type: string
    description: |
      The name of the Code Repository (must be unique).
    willReplaceOnChanges: true
  gitConfig:
    $ref: "#/types/aws:sagemaker/CodeRepositoryGitConfig:CodeRepositoryGitConfig"
    description: |
      Specifies details about the repository. see Git Config details below.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - codeRepositoryName
  - gitConfig
stateInputs:
  description: |
    Input properties used for looking up and filtering CodeRepository resources.
  properties:
    arn:
      type: string
      description: |
        The Amazon Resource Name (ARN) assigned by AWS to this Code Repository.
    codeRepositoryName:
      type: string
      description: |
        The name of the Code Repository (must be unique).
      willReplaceOnChanges: true
    gitConfig:
      $ref: "#/types/aws:sagemaker/CodeRepositoryGitConfig:CodeRepositoryGitConfig"
      description: |
        Specifies details about the repository. see Git Config details below.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
