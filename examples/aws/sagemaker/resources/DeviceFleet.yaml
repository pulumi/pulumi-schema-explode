$token: aws:sagemaker/deviceFleet:DeviceFleet
properties:
  arn:
    type: string
    description: |
      The Amazon Resource Name (ARN) assigned by AWS to this Device Fleet.
  description:
    type: string
    description: |
      A description of the fleet.
  deviceFleetName:
    type: string
    description: |
      The name of the Device Fleet (must be unique).
  enableIotRoleAlias:
    type: boolean
    description: |
      Whether to create an AWS IoT Role Alias during device fleet creation. The name of the role alias generated will match this pattern: "SageMakerEdge-{DeviceFleetName}".
  iotRoleAlias:
    type: string
  outputConfig:
    $ref: "#/types/aws:sagemaker/DeviceFleetOutputConfig:DeviceFleetOutputConfig"
    description: |
      Specifies details about the repository. see Output Config details below.
  roleArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) that has access to AWS Internet of Things (IoT).
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - deviceFleetName
  - iotRoleAlias
  - outputConfig
  - roleArn
  - tagsAll
inputProperties:
  description:
    type: string
    description: |
      A description of the fleet.
  deviceFleetName:
    type: string
    description: |
      The name of the Device Fleet (must be unique).
    willReplaceOnChanges: true
  enableIotRoleAlias:
    type: boolean
    description: |
      Whether to create an AWS IoT Role Alias during device fleet creation. The name of the role alias generated will match this pattern: "SageMakerEdge-{DeviceFleetName}".
  outputConfig:
    $ref: "#/types/aws:sagemaker/DeviceFleetOutputConfig:DeviceFleetOutputConfig"
    description: |
      Specifies details about the repository. see Output Config details below.
  roleArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) that has access to AWS Internet of Things (IoT).
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - deviceFleetName
  - outputConfig
  - roleArn
stateInputs:
  description: |
    Input properties used for looking up and filtering DeviceFleet resources.
  properties:
    arn:
      type: string
      description: |
        The Amazon Resource Name (ARN) assigned by AWS to this Device Fleet.
    description:
      type: string
      description: |
        A description of the fleet.
    deviceFleetName:
      type: string
      description: |
        The name of the Device Fleet (must be unique).
      willReplaceOnChanges: true
    enableIotRoleAlias:
      type: boolean
      description: |
        Whether to create an AWS IoT Role Alias during device fleet creation. The name of the role alias generated will match this pattern: "SageMakerEdge-{DeviceFleetName}".
    iotRoleAlias:
      type: string
    outputConfig:
      $ref: "#/types/aws:sagemaker/DeviceFleetOutputConfig:DeviceFleetOutputConfig"
      description: |
        Specifies details about the repository. see Output Config details below.
    roleArn:
      type: string
      description: |
        The Amazon Resource Name (ARN) that has access to AWS Internet of Things (IoT).
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
