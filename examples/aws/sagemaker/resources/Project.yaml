$token: aws:sagemaker/project:Project
properties:
  arn:
    type: string
    description: |
      The Amazon Resource Name (ARN) assigned by AWS to this Project.
  projectDescription:
    type: string
    description: |
      A description for the project.
  projectId:
    type: string
    description: |
      The ID of the project.
  projectName:
    type: string
    description: |
      The name of the Project.
  serviceCatalogProvisioningDetails:
    $ref: "#/types/aws:sagemaker/ProjectServiceCatalogProvisioningDetails:ProjectServiceCatalogProvisioningDetails"
    description: |
      The product ID and provisioning artifact ID to provision a service catalog. See Service Catalog Provisioning Details below.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - projectId
  - projectName
  - serviceCatalogProvisioningDetails
  - tagsAll
inputProperties:
  projectDescription:
    type: string
    description: |
      A description for the project.
  projectName:
    type: string
    description: |
      The name of the Project.
    willReplaceOnChanges: true
  serviceCatalogProvisioningDetails:
    $ref: "#/types/aws:sagemaker/ProjectServiceCatalogProvisioningDetails:ProjectServiceCatalogProvisioningDetails"
    description: |
      The product ID and provisioning artifact ID to provision a service catalog. See Service Catalog Provisioning Details below.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - projectName
  - serviceCatalogProvisioningDetails
stateInputs:
  description: |
    Input properties used for looking up and filtering Project resources.
  properties:
    arn:
      type: string
      description: |
        The Amazon Resource Name (ARN) assigned by AWS to this Project.
    projectDescription:
      type: string
      description: |
        A description for the project.
    projectId:
      type: string
      description: |
        The ID of the project.
    projectName:
      type: string
      description: |
        The name of the Project.
      willReplaceOnChanges: true
    serviceCatalogProvisioningDetails:
      $ref: "#/types/aws:sagemaker/ProjectServiceCatalogProvisioningDetails:ProjectServiceCatalogProvisioningDetails"
      description: |
        The product ID and provisioning artifact ID to provision a service catalog. See Service Catalog Provisioning Details below.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
