$token: aws:sagemaker/humanTaskUI:HumanTaskUI
properties:
  arn:
    type: string
    description: |
      The Amazon Resource Name (ARN) assigned by AWS to this Human Task UI.
  humanTaskUiName:
    type: string
    description: |
      The name of the Human Task UI.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  uiTemplate:
    $ref: "#/types/aws:sagemaker/HumanTaskUIUiTemplate:HumanTaskUIUiTemplate"
    description: |
      The Liquid template for the worker user interface. See UI Template below.
required:
  - arn
  - humanTaskUiName
  - tagsAll
  - uiTemplate
inputProperties:
  humanTaskUiName:
    type: string
    description: |
      The name of the Human Task UI.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  uiTemplate:
    $ref: "#/types/aws:sagemaker/HumanTaskUIUiTemplate:HumanTaskUIUiTemplate"
    description: |
      The Liquid template for the worker user interface. See UI Template below.
    willReplaceOnChanges: true
requiredInputs:
  - humanTaskUiName
  - uiTemplate
stateInputs:
  description: |
    Input properties used for looking up and filtering HumanTaskUI resources.
  properties:
    arn:
      type: string
      description: |
        The Amazon Resource Name (ARN) assigned by AWS to this Human Task UI.
    humanTaskUiName:
      type: string
      description: |
        The name of the Human Task UI.
      willReplaceOnChanges: true
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    uiTemplate:
      $ref: "#/types/aws:sagemaker/HumanTaskUIUiTemplate:HumanTaskUIUiTemplate"
      description: |
        The Liquid template for the worker user interface. See UI Template below.
      willReplaceOnChanges: true
  type: object
