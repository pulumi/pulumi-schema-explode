$token: aws:sagemaker/app:App
properties:
  appName:
    type: string
    description: |
      The name of the app.
  appType:
    type: string
    description: |
      The type of app. Valid values are `JupyterServer`, `KernelGateway` and `TensorBoard`.
  arn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the app.
  domainId:
    type: string
    description: |
      The domain ID.
  resourceSpec:
    $ref: "#/types/aws:sagemaker/AppResourceSpec:AppResourceSpec"
    description: |
      The instance type and the Amazon Resource Name (ARN) of the SageMaker image created on the instance.See Resource Spec below.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  userProfileName:
    type: string
    description: |
      The user profile name.
required:
  - appName
  - appType
  - arn
  - domainId
  - resourceSpec
  - tagsAll
  - userProfileName
inputProperties:
  appName:
    type: string
    description: |
      The name of the app.
    willReplaceOnChanges: true
  appType:
    type: string
    description: |
      The type of app. Valid values are `JupyterServer`, `KernelGateway` and `TensorBoard`.
    willReplaceOnChanges: true
  domainId:
    type: string
    description: |
      The domain ID.
    willReplaceOnChanges: true
  resourceSpec:
    $ref: "#/types/aws:sagemaker/AppResourceSpec:AppResourceSpec"
    description: |
      The instance type and the Amazon Resource Name (ARN) of the SageMaker image created on the instance.See Resource Spec below.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  userProfileName:
    type: string
    description: |
      The user profile name.
    willReplaceOnChanges: true
requiredInputs:
  - appName
  - appType
  - domainId
  - userProfileName
stateInputs:
  description: |
    Input properties used for looking up and filtering App resources.
  properties:
    appName:
      type: string
      description: |
        The name of the app.
      willReplaceOnChanges: true
    appType:
      type: string
      description: |
        The type of app. Valid values are `JupyterServer`, `KernelGateway` and `TensorBoard`.
      willReplaceOnChanges: true
    arn:
      type: string
      description: |
        The Amazon Resource Name (ARN) of the app.
    domainId:
      type: string
      description: |
        The domain ID.
      willReplaceOnChanges: true
    resourceSpec:
      $ref: "#/types/aws:sagemaker/AppResourceSpec:AppResourceSpec"
      description: |
        The instance type and the Amazon Resource Name (ARN) of the SageMaker image created on the instance.See Resource Spec below.
      willReplaceOnChanges: true
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    userProfileName:
      type: string
      description: |
        The user profile name.
      willReplaceOnChanges: true
  type: object
