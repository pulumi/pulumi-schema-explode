$token: aws:amplify/webhook:Webhook
properties:
  appId:
    type: string
    description: |
      Unique ID for an Amplify app.
  arn:
    type: string
    description: |
      ARN for the webhook.
  branchName:
    type: string
    description: |
      Name for a branch that is part of the Amplify app.
  description:
    type: string
    description: |
      Description for a webhook.
  url:
    type: string
    description: |
      URL of the webhook.
required:
  - appId
  - arn
  - branchName
  - url
inputProperties:
  appId:
    type: string
    description: |
      Unique ID for an Amplify app.
    willReplaceOnChanges: true
  branchName:
    type: string
    description: |
      Name for a branch that is part of the Amplify app.
  description:
    type: string
    description: |
      Description for a webhook.
requiredInputs:
  - appId
  - branchName
stateInputs:
  description: |
    Input properties used for looking up and filtering Webhook resources.
  properties:
    appId:
      type: string
      description: |
        Unique ID for an Amplify app.
      willReplaceOnChanges: true
    arn:
      type: string
      description: |
        ARN for the webhook.
    branchName:
      type: string
      description: |
        Name for a branch that is part of the Amplify app.
    description:
      type: string
      description: |
        Description for a webhook.
    url:
      type: string
      description: |
        URL of the webhook.
  type: object
