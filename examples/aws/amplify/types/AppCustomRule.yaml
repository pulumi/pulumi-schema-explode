$token: aws:amplify/AppCustomRule:AppCustomRule
properties:
  condition:
    type: string
    description: |
      Condition for a URL rewrite or redirect rule, such as a country code.
    language:
      python:
        mapCase: false
  source:
    type: string
    description: |
      Source pattern for a URL rewrite or redirect rule.
    language:
      python:
        mapCase: false
  status:
    type: string
    description: |
      Status code for a URL rewrite or redirect rule. Valid values: `200`, `301`, `302`, `404`, `404-200`.
    language:
      python:
        mapCase: false
  target:
    type: string
    description: |
      Target pattern for a URL rewrite or redirect rule.
    language:
      python:
        mapCase: false
type: object
required:
  - source
  - target
