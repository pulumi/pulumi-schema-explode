$token: aws:xray/group:Group
properties:
  arn:
    type: string
    description: |
      The ARN of the Group.
  filterExpression:
    type: string
    description: |
      The filter expression defining criteria by which to group traces. more info can be found in official [docs](https://docs.aws.amazon.com/xray/latest/devguide/xray-console-filters.html).
  groupName:
    type: string
    description: |
      The name of the group.
  insightsConfiguration:
    $ref: "#/types/aws:xray/GroupInsightsConfiguration:GroupInsightsConfiguration"
    description: |
      Configuration options for enabling insights.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value mapping of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - filterExpression
  - groupName
  - insightsConfiguration
  - tagsAll
inputProperties:
  filterExpression:
    type: string
    description: |
      The filter expression defining criteria by which to group traces. more info can be found in official [docs](https://docs.aws.amazon.com/xray/latest/devguide/xray-console-filters.html).
  groupName:
    type: string
    description: |
      The name of the group.
    willReplaceOnChanges: true
  insightsConfiguration:
    $ref: "#/types/aws:xray/GroupInsightsConfiguration:GroupInsightsConfiguration"
    description: |
      Configuration options for enabling insights.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value mapping of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level
requiredInputs:
  - filterExpression
  - groupName
stateInputs:
  description: |
    Input properties used for looking up and filtering Group resources.
  properties:
    arn:
      type: string
      description: |
        The ARN of the Group.
    filterExpression:
      type: string
      description: |
        The filter expression defining criteria by which to group traces. more info can be found in official [docs](https://docs.aws.amazon.com/xray/latest/devguide/xray-console-filters.html).
    groupName:
      type: string
      description: |
        The name of the group.
      willReplaceOnChanges: true
    insightsConfiguration:
      $ref: "#/types/aws:xray/GroupInsightsConfiguration:GroupInsightsConfiguration"
      description: |
        Configuration options for enabling insights.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value mapping of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
