$token: aws:efs/accessPoint:AccessPoint
properties:
  arn:
    type: string
    description: |
      ARN of the access point.
  fileSystemArn:
    type: string
    description: |
      ARN of the file system.
  fileSystemId:
    type: string
    description: |
      ID of the file system for which the access point is intended.
  ownerId:
    type: string
  posixUser:
    $ref: "#/types/aws:efs/AccessPointPosixUser:AccessPointPosixUser"
    description: |
      Operating system user and group applied to all file system requests made using the access point. Detailed below.
  rootDirectory:
    $ref: "#/types/aws:efs/AccessPointRootDirectory:AccessPointRootDirectory"
    description: |
      Directory on the Amazon EFS file system that the access point provides access to. Detailed below.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value mapping of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level
  tagsAll:
    type: object
    additionalProperties:
      type: string
required:
  - arn
  - fileSystemArn
  - fileSystemId
  - ownerId
  - rootDirectory
  - tagsAll
inputProperties:
  fileSystemId:
    type: string
    description: |
      ID of the file system for which the access point is intended.
    willReplaceOnChanges: true
  posixUser:
    $ref: "#/types/aws:efs/AccessPointPosixUser:AccessPointPosixUser"
    description: |
      Operating system user and group applied to all file system requests made using the access point. Detailed below.
    willReplaceOnChanges: true
  rootDirectory:
    $ref: "#/types/aws:efs/AccessPointRootDirectory:AccessPointRootDirectory"
    description: |
      Directory on the Amazon EFS file system that the access point provides access to. Detailed below.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value mapping of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level
requiredInputs:
  - fileSystemId
stateInputs:
  description: |
    Input properties used for looking up and filtering AccessPoint resources.
  properties:
    arn:
      type: string
      description: |
        ARN of the access point.
    fileSystemArn:
      type: string
      description: |
        ARN of the file system.
    fileSystemId:
      type: string
      description: |
        ID of the file system for which the access point is intended.
      willReplaceOnChanges: true
    ownerId:
      type: string
    posixUser:
      $ref: "#/types/aws:efs/AccessPointPosixUser:AccessPointPosixUser"
      description: |
        Operating system user and group applied to all file system requests made using the access point. Detailed below.
      willReplaceOnChanges: true
    rootDirectory:
      $ref: "#/types/aws:efs/AccessPointRootDirectory:AccessPointRootDirectory"
      description: |
        Directory on the Amazon EFS file system that the access point provides access to. Detailed below.
      willReplaceOnChanges: true
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value mapping of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level
    tagsAll:
      type: object
      additionalProperties:
        type: string
  type: object
