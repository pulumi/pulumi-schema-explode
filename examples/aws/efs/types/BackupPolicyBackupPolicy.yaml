$token: aws:efs/BackupPolicyBackupPolicy:BackupPolicyBackupPolicy
properties:
  status:
    type: string
    description: |
      A status of the backup policy. Valid values: `ENABLED`, `DISABLED`.
    language:
      python:
        mapCase: false
type: object
required:
  - status
