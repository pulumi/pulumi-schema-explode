$token: aws:efs/getAccessPointPosixUser:getAccessPointPosixUser
properties:
  gid:
    type: integer
    description: |
      Group ID
    language:
      python:
        mapCase: false
  secondaryGids:
    type: array
    items:
      type: integer
    description: |
      Secondary group IDs
    language:
      python:
        mapCase: false
  uid:
    type: integer
    description: |
      User Id
      * `root_directory`- Single element list containing information on the directory on the Amazon EFS file system that the access point provides access to.
    language:
      python:
        mapCase: false
type: object
required:
  - gid
  - secondaryGids
  - uid
language:
  nodejs:
    requiredInputs: []
