$token: aws:efs/getAccessPointRootDirectory:getAccessPointRootDirectory
properties:
  creationInfos:
    type: array
    items:
      $ref: "#/types/aws:efs/getAccessPointRootDirectoryCreationInfo:getAccessPointRootDirectoryCreationInfo"
    description: |
      Single element list containing information on the creation permissions of the directory
    language:
      python:
        mapCase: false
  path:
    type: string
    description: |
      Path exposed as the root directory
    language:
      python:
        mapCase: false
type: object
required:
  - creationInfos
  - path
language:
  nodejs:
    requiredInputs: []
