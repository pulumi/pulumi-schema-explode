$token: aws:efs/AccessPointRootDirectoryCreationInfo:AccessPointRootDirectoryCreationInfo
properties:
  ownerGid:
    type: integer
    description: |
      POSIX group ID to apply to the `root_directory`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  ownerUid:
    type: integer
    description: |
      POSIX user ID to apply to the `root_directory`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  permissions:
    type: string
    description: |
      POSIX permissions to apply to the RootDirectory, in the format of an octal number representing the file's mode bits.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - ownerGid
  - ownerUid
  - permissions
