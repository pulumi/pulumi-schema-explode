$token: aws:efs/AccessPointPosixUser:AccessPointPosixUser
properties:
  gid:
    type: integer
    description: |
      POSIX group ID used for all file system operations using this access point.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  secondaryGids:
    type: array
    items:
      type: integer
    description: |
      Secondary POSIX group IDs used for all file system operations using this access point.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  uid:
    type: integer
    description: |
      POSIX user ID used for all file system operations using this access point.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - gid
  - uid
