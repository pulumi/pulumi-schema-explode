$token: aws:efs/FileSystemSizeInByte:FileSystemSizeInByte
properties:
  value:
    type: integer
    description: |
      The latest known metered size (in bytes) of data stored in the file system.
    language:
      python:
        mapCase: false
  valueInIa:
    type: integer
    description: |
      The latest known metered size (in bytes) of data stored in the Infrequent Access storage class.
    language:
      python:
        mapCase: false
  valueInStandard:
    type: integer
    description: |
      The latest known metered size (in bytes) of data stored in the Standard storage class.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - value
      - valueInIa
      - valueInStandard
