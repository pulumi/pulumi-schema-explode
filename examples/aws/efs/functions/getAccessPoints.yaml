$token: aws:efs/getAccessPoints:getAccessPoints
inputs:
  description: |
    A collection of arguments for invoking getAccessPoints.
  properties:
    fileSystemId:
      type: string
      description: |
        EFS File System identifier.
  type: object
  required:
    - fileSystemId
outputs:
  description: |
    A collection of values returned by getAccessPoints.
  properties:
    arns:
      type: array
      items:
        type: string
      description: |
        Set of Amazon Resource Names (ARNs).
    fileSystemId:
      type: string
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    ids:
      type: array
      items:
        type: string
      description: |
        Set of identifiers.
  type: object
  required:
    - arns
    - fileSystemId
    - ids
    - id
