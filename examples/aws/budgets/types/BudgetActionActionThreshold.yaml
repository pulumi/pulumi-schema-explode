$token: aws:budgets/BudgetActionActionThreshold:BudgetActionActionThreshold
properties:
  actionThresholdType:
    type: string
    description: |
      The type of threshold for a notification. Valid values are `PERCENTAGE` or `ABSOLUTE_VALUE`.
    language:
      python:
        mapCase: false
  actionThresholdValue:
    type: number
    description: |
      The threshold of a notification.
    language:
      python:
        mapCase: false
type: object
required:
  - actionThresholdType
  - actionThresholdValue
