$token: aws:budgets/BudgetActionDefinitionIamActionDefinition:BudgetActionDefinitionIamActionDefinition
properties:
  groups:
    type: array
    items:
      type: string
    description: |
      A list of groups to be attached. There must be at least one group.
    language:
      python:
        mapCase: false
  policyArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the policy to be attached.
    language:
      python:
        mapCase: false
  roles:
    type: array
    items:
      type: string
    description: |
      A list of roles to be attached. There must be at least one role.
    language:
      python:
        mapCase: false
  users:
    type: array
    items:
      type: string
    description: |
      A list of users to be attached. There must be at least one user.
    language:
      python:
        mapCase: false
type: object
required:
  - policyArn
