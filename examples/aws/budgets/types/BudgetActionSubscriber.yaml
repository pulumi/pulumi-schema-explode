$token: aws:budgets/BudgetActionSubscriber:BudgetActionSubscriber
properties:
  address:
    type: string
    description: |
      The address that AWS sends budget notifications to, either an SNS topic or an email.
    language:
      python:
        mapCase: false
  subscriptionType:
    type: string
    description: |
      The type of notification that AWS sends to a subscriber. Valid values are `SNS` or `EMAIL`.
    language:
      python:
        mapCase: false
type: object
required:
  - address
  - subscriptionType
