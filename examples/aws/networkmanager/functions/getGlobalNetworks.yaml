$token: aws:networkmanager/getGlobalNetworks:getGlobalNetworks
inputs:
  description: |
    A collection of arguments for invoking getGlobalNetworks.
  properties:
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Restricts the list to the global networks with these tags.
  type: object
outputs:
  description: |
    A collection of values returned by getGlobalNetworks.
  properties:
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    ids:
      type: array
      items:
        type: string
      description: |
        IDs of the global networks.
    tags:
      type: object
      additionalProperties:
        type: string
  type: object
  required:
    - ids
    - id
