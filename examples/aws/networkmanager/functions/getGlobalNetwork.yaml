$token: aws:networkmanager/getGlobalNetwork:getGlobalNetwork
inputs:
  description: |
    A collection of arguments for invoking getGlobalNetwork.
  properties:
    globalNetworkId:
      type: string
      description: |
        ID of the specific global network to retrieve.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of resource tags.
  type: object
  required:
    - globalNetworkId
outputs:
  description: |
    A collection of values returned by getGlobalNetwork.
  properties:
    arn:
      type: string
      description: |
        ARN of the global network.
    description:
      type: string
      description: |
        Description of the global network.
    globalNetworkId:
      type: string
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of resource tags.
  type: object
  required:
    - arn
    - description
    - globalNetworkId
    - tags
    - id
