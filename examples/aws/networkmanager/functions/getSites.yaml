$token: aws:networkmanager/getSites:getSites
inputs:
  description: |
    A collection of arguments for invoking getSites.
  properties:
    globalNetworkId:
      type: string
      description: |
        ID of the Global Network of the sites to retrieve.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Restricts the list to the sites with these tags.
  type: object
  required:
    - globalNetworkId
outputs:
  description: |
    A collection of values returned by getSites.
  properties:
    globalNetworkId:
      type: string
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    ids:
      type: array
      items:
        type: string
      description: |
        IDs of the sites.
    tags:
      type: object
      additionalProperties:
        type: string
  type: object
  required:
    - globalNetworkId
    - ids
    - id
