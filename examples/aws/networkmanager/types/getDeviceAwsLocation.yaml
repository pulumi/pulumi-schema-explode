$token: aws:networkmanager/getDeviceAwsLocation:getDeviceAwsLocation
properties:
  subnetArn:
    type: string
    description: |
      ARN of the subnet that the device is located in.
    language:
      python:
        mapCase: false
  zone:
    type: string
    description: |
      Zone that the device is located in.
    language:
      python:
        mapCase: false
type: object
required:
  - subnetArn
  - zone
language:
  nodejs:
    requiredInputs: []
