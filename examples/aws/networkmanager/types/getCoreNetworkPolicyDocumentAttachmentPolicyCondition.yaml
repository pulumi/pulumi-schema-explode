$token: aws:networkmanager/getCoreNetworkPolicyDocumentAttachmentPolicyCondition:getCoreNetworkPolicyDocumentAttachmentPolicyCondition
properties:
  key:
    type: string
    description: |
      string value
    language:
      python:
        mapCase: false
  operator:
    type: string
    description: |
      Valid values include: `equals`, `not-equals`, `contains`, `begins-with`.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      Valid values include: `account-id`, `any`, `tag-value`, `tag-exists`, `resource-id`, `region`, `attachment-type`.
    language:
      python:
        mapCase: false
  value:
    type: string
    description: |
      string value
    language:
      python:
        mapCase: false
type: object
required:
  - type
