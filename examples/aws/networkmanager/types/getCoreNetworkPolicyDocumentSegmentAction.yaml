$token: aws:networkmanager/getCoreNetworkPolicyDocumentSegmentAction:getCoreNetworkPolicyDocumentSegmentAction
properties:
  action:
    type: string
    description: |
      Action to take for the chosen segment. Valid values `create-route` or `share`.
    language:
      python:
        mapCase: false
  description:
    type: string
    description: |
      A user-defined string describing the segment action.
    language:
      python:
        mapCase: false
  destinationCidrBlocks:
    type: array
    items:
      type: string
    description: |
      List of strings containing CIDRs. You can define the IPv4 and IPv6 CIDR notation for each AWS Region. For example, `10.1.0.0/16` or `2001:db8::/56`. This is an array of CIDR notation strings.
    language:
      python:
        mapCase: false
  destinations:
    type: array
    items:
      type: string
    description: |
      A list of strings. Valid values include `["blackhole"]` or a list of attachment ids.
    language:
      python:
        mapCase: false
  mode:
    type: string
    description: |
      String. This mode places the attachment and return routes in each of the `share_with` segments. Valid values include: `attachment-route`.
    language:
      python:
        mapCase: false
  segment:
    type: string
    description: |
      Name of the segment.
    language:
      python:
        mapCase: false
  shareWithExcepts:
    type: array
    items:
      type: string
    description: |
      A set subtraction of segments to not share with.
    language:
      python:
        mapCase: false
  shareWiths:
    type: array
    items:
      type: string
    description: |
      A list of strings to share with. Must be a substring is all segments. Valid values include: `["*"]` or `["<segment-names>"]`.
    language:
      python:
        mapCase: false
type: object
required:
  - action
  - segment
