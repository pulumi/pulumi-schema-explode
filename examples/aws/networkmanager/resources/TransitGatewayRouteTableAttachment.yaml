$token: aws:networkmanager/transitGatewayRouteTableAttachment:TransitGatewayRouteTableAttachment
properties:
  arn:
    type: string
    description: |
      Attachment Amazon Resource Name (ARN).
  attachmentPolicyRuleNumber:
    type: integer
    description: |
      The policy rule number associated with the attachment.
  attachmentType:
    type: string
    description: |
      The type of attachment.
  coreNetworkArn:
    type: string
    description: |
      The ARN of the core network.
  coreNetworkId:
    type: string
    description: |
      The ID of the core network.
  edgeLocation:
    type: string
    description: |
      The edge location for the peer.
  ownerAccountId:
    type: string
    description: |
      The ID of the attachment account owner.
  peeringId:
    type: string
    description: |
      The ID of the peer for the attachment.
  resourceArn:
    type: string
    description: |
      The attachment resource ARN.
  segmentName:
    type: string
    description: |
      The name of the segment attachment.
  state:
    type: string
    description: |
      The state of the attachment.
  tags:
    type: object
    additionalProperties:
      type: string
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  transitGatewayRouteTableArn:
    type: string
    description: |
      The ARN of the transit gateway route table for the attachment.
required:
  - arn
  - attachmentPolicyRuleNumber
  - attachmentType
  - coreNetworkArn
  - coreNetworkId
  - edgeLocation
  - ownerAccountId
  - peeringId
  - resourceArn
  - segmentName
  - state
  - tagsAll
  - transitGatewayRouteTableArn
inputProperties:
  peeringId:
    type: string
    description: |
      The ID of the peer for the attachment.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
  transitGatewayRouteTableArn:
    type: string
    description: |
      The ARN of the transit gateway route table for the attachment.
    willReplaceOnChanges: true
requiredInputs:
  - peeringId
  - transitGatewayRouteTableArn
stateInputs:
  description: |
    Input properties used for looking up and filtering TransitGatewayRouteTableAttachment resources.
  properties:
    arn:
      type: string
      description: |
        Attachment Amazon Resource Name (ARN).
    attachmentPolicyRuleNumber:
      type: integer
      description: |
        The policy rule number associated with the attachment.
    attachmentType:
      type: string
      description: |
        The type of attachment.
    coreNetworkArn:
      type: string
      description: |
        The ARN of the core network.
    coreNetworkId:
      type: string
      description: |
        The ID of the core network.
    edgeLocation:
      type: string
      description: |
        The edge location for the peer.
    ownerAccountId:
      type: string
      description: |
        The ID of the attachment account owner.
    peeringId:
      type: string
      description: |
        The ID of the peer for the attachment.
      willReplaceOnChanges: true
    resourceArn:
      type: string
      description: |
        The attachment resource ARN.
    segmentName:
      type: string
      description: |
        The name of the segment attachment.
    state:
      type: string
      description: |
        The state of the attachment.
    tags:
      type: object
      additionalProperties:
        type: string
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    transitGatewayRouteTableArn:
      type: string
      description: |
        The ARN of the transit gateway route table for the attachment.
      willReplaceOnChanges: true
  type: object
