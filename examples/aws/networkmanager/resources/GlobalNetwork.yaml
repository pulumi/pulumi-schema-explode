$token: aws:networkmanager/globalNetwork:GlobalNetwork
properties:
  arn:
    type: string
    description: |
      Global Network Amazon Resource Name (ARN)
  description:
    type: string
    description: |
      Description of the Global Network.
  tags:
    type: object
    additionalProperties:
      type: string
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - tagsAll
inputProperties:
  description:
    type: string
    description: |
      Description of the Global Network.
  tags:
    type: object
    additionalProperties:
      type: string
stateInputs:
  description: |
    Input properties used for looking up and filtering GlobalNetwork resources.
  properties:
    arn:
      type: string
      description: |
        Global Network Amazon Resource Name (ARN)
    description:
      type: string
      description: |
        Description of the Global Network.
    tags:
      type: object
      additionalProperties:
        type: string
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
