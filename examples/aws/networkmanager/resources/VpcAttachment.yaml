$token: aws:networkmanager/vpcAttachment:VpcAttachment
properties:
  arn:
    type: string
    description: |
      The ARN of the attachment.
  attachmentPolicyRuleNumber:
    type: integer
    description: |
      The policy rule number associated with the attachment.
  attachmentType:
    type: string
    description: |
      The type of attachment.
  coreNetworkArn:
    type: string
    description: |
      The ARN of a core network.
  coreNetworkId:
    type: string
    description: |
      The ID of a core network for the VPC attachment.
  edgeLocation:
    type: string
    description: |
      The Region where the edge is located.
  options:
    $ref: "#/types/aws:networkmanager/VpcAttachmentOptions:VpcAttachmentOptions"
    description: |
      Options for the VPC attachment.
  ownerAccountId:
    type: string
    description: |
      The ID of the attachment account owner.
  resourceArn:
    type: string
    description: |
      The attachment resource ARN.
  segmentName:
    type: string
    description: |
      The name of the segment attachment.
  state:
    type: string
    description: |
      The state of the attachment.
  subnetArns:
    type: array
    items:
      type: string
    description: |
      The subnet ARN of the VPC attachment.
  tags:
    type: object
    additionalProperties:
      type: string
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  vpcArn:
    type: string
    description: |
      The ARN of the VPC.
required:
  - arn
  - attachmentPolicyRuleNumber
  - attachmentType
  - coreNetworkArn
  - coreNetworkId
  - edgeLocation
  - ownerAccountId
  - resourceArn
  - segmentName
  - state
  - subnetArns
  - tagsAll
  - vpcArn
inputProperties:
  coreNetworkId:
    type: string
    description: |
      The ID of a core network for the VPC attachment.
    willReplaceOnChanges: true
  options:
    $ref: "#/types/aws:networkmanager/VpcAttachmentOptions:VpcAttachmentOptions"
    description: |
      Options for the VPC attachment.
  subnetArns:
    type: array
    items:
      type: string
    description: |
      The subnet ARN of the VPC attachment.
  tags:
    type: object
    additionalProperties:
      type: string
  vpcArn:
    type: string
    description: |
      The ARN of the VPC.
    willReplaceOnChanges: true
requiredInputs:
  - coreNetworkId
  - subnetArns
  - vpcArn
stateInputs:
  description: |
    Input properties used for looking up and filtering VpcAttachment resources.
  properties:
    arn:
      type: string
      description: |
        The ARN of the attachment.
    attachmentPolicyRuleNumber:
      type: integer
      description: |
        The policy rule number associated with the attachment.
    attachmentType:
      type: string
      description: |
        The type of attachment.
    coreNetworkArn:
      type: string
      description: |
        The ARN of a core network.
    coreNetworkId:
      type: string
      description: |
        The ID of a core network for the VPC attachment.
      willReplaceOnChanges: true
    edgeLocation:
      type: string
      description: |
        The Region where the edge is located.
    options:
      $ref: "#/types/aws:networkmanager/VpcAttachmentOptions:VpcAttachmentOptions"
      description: |
        Options for the VPC attachment.
    ownerAccountId:
      type: string
      description: |
        The ID of the attachment account owner.
    resourceArn:
      type: string
      description: |
        The attachment resource ARN.
    segmentName:
      type: string
      description: |
        The name of the segment attachment.
    state:
      type: string
      description: |
        The state of the attachment.
    subnetArns:
      type: array
      items:
        type: string
      description: |
        The subnet ARN of the VPC attachment.
    tags:
      type: object
      additionalProperties:
        type: string
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    vpcArn:
      type: string
      description: |
        The ARN of the VPC.
      willReplaceOnChanges: true
  type: object
