$token: aws:networkmanager/site:Site
properties:
  arn:
    type: string
    description: |
      Site Amazon Resource Name (ARN)
  description:
    type: string
    description: |
      Description of the Site.
  globalNetworkId:
    type: string
    description: |
      The ID of the Global Network to create the site in.
  location:
    $ref: "#/types/aws:networkmanager/SiteLocation:SiteLocation"
    description: |
      The site location as documented below.
  tags:
    type: object
    additionalProperties:
      type: string
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - globalNetworkId
  - tagsAll
inputProperties:
  description:
    type: string
    description: |
      Description of the Site.
  globalNetworkId:
    type: string
    description: |
      The ID of the Global Network to create the site in.
    willReplaceOnChanges: true
  location:
    $ref: "#/types/aws:networkmanager/SiteLocation:SiteLocation"
    description: |
      The site location as documented below.
  tags:
    type: object
    additionalProperties:
      type: string
requiredInputs:
  - globalNetworkId
stateInputs:
  description: |
    Input properties used for looking up and filtering Site resources.
  properties:
    arn:
      type: string
      description: |
        Site Amazon Resource Name (ARN)
    description:
      type: string
      description: |
        Description of the Site.
    globalNetworkId:
      type: string
      description: |
        The ID of the Global Network to create the site in.
      willReplaceOnChanges: true
    location:
      $ref: "#/types/aws:networkmanager/SiteLocation:SiteLocation"
      description: |
        The site location as documented below.
    tags:
      type: object
      additionalProperties:
        type: string
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
