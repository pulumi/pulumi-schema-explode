$token: aws:networkmanager/device:Device
properties:
  arn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the device.
  awsLocation:
    $ref: "#/types/aws:networkmanager/DeviceAwsLocation:DeviceAwsLocation"
    description: |
      The AWS location of the device. Documented below.
  description:
    type: string
    description: |
      A description of the device.
  globalNetworkId:
    type: string
    description: |
      The ID of the global network.
  location:
    $ref: "#/types/aws:networkmanager/DeviceLocation:DeviceLocation"
    description: |
      The location of the device. Documented below.
  model:
    type: string
    description: |
      The model of device.
  serialNumber:
    type: string
    description: |
      The serial number of the device.
  siteId:
    type: string
    description: |
      The ID of the site.
  tags:
    type: object
    additionalProperties:
      type: string
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type:
    type: string
    description: |
      The type of device.
  vendor:
    type: string
    description: |
      The vendor of the device.
required:
  - arn
  - globalNetworkId
  - tagsAll
inputProperties:
  awsLocation:
    $ref: "#/types/aws:networkmanager/DeviceAwsLocation:DeviceAwsLocation"
    description: |
      The AWS location of the device. Documented below.
  description:
    type: string
    description: |
      A description of the device.
  globalNetworkId:
    type: string
    description: |
      The ID of the global network.
    willReplaceOnChanges: true
  location:
    $ref: "#/types/aws:networkmanager/DeviceLocation:DeviceLocation"
    description: |
      The location of the device. Documented below.
  model:
    type: string
    description: |
      The model of device.
  serialNumber:
    type: string
    description: |
      The serial number of the device.
  siteId:
    type: string
    description: |
      The ID of the site.
  tags:
    type: object
    additionalProperties:
      type: string
  type:
    type: string
    description: |
      The type of device.
  vendor:
    type: string
    description: |
      The vendor of the device.
requiredInputs:
  - globalNetworkId
stateInputs:
  description: |
    Input properties used for looking up and filtering Device resources.
  properties:
    arn:
      type: string
      description: |
        The Amazon Resource Name (ARN) of the device.
    awsLocation:
      $ref: "#/types/aws:networkmanager/DeviceAwsLocation:DeviceAwsLocation"
      description: |
        The AWS location of the device. Documented below.
    description:
      type: string
      description: |
        A description of the device.
    globalNetworkId:
      type: string
      description: |
        The ID of the global network.
      willReplaceOnChanges: true
    location:
      $ref: "#/types/aws:networkmanager/DeviceLocation:DeviceLocation"
      description: |
        The location of the device. Documented below.
    model:
      type: string
      description: |
        The model of device.
    serialNumber:
      type: string
      description: |
        The serial number of the device.
    siteId:
      type: string
      description: |
        The ID of the site.
    tags:
      type: object
      additionalProperties:
        type: string
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    type:
      type: string
      description: |
        The type of device.
    vendor:
      type: string
      description: |
        The vendor of the device.
  type: object
