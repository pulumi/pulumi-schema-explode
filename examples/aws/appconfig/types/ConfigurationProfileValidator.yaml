$token: aws:appconfig/ConfigurationProfileValidator:ConfigurationProfileValidator
properties:
  content:
    type: string
    description: |
      Either the JSON Schema content or the ARN of an AWS Lambda function.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      Type of validator. Valid values: `JSON_SCHEMA` and `LAMBDA`.
    language:
      python:
        mapCase: false
type: object
required:
  - type
