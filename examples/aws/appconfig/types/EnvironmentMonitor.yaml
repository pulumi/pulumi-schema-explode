$token: aws:appconfig/EnvironmentMonitor:EnvironmentMonitor
properties:
  alarmArn:
    type: string
    description: |
      ARN of the Amazon CloudWatch alarm.
    language:
      python:
        mapCase: false
  alarmRoleArn:
    type: string
    description: |
      ARN of an IAM role for AWS AppConfig to monitor `alarm_arn`.
    language:
      python:
        mapCase: false
type: object
required:
  - alarmArn
