$token: aws:chime/voiceConnectorTerminationCredentials:VoiceConnectorTerminationCredentials
properties:
  credentials:
    type: array
    items:
      $ref: "#/types/aws:chime/VoiceConnectorTerminationCredentialsCredential:VoiceConnectorTerminationCredentialsCredential"
    description: |
      List of termination SIP credentials.
  voiceConnectorId:
    type: string
    description: |
      Amazon Chime Voice Connector ID.
required:
  - credentials
  - voiceConnectorId
inputProperties:
  credentials:
    type: array
    items:
      $ref: "#/types/aws:chime/VoiceConnectorTerminationCredentialsCredential:VoiceConnectorTerminationCredentialsCredential"
    description: |
      List of termination SIP credentials.
  voiceConnectorId:
    type: string
    description: |
      Amazon Chime Voice Connector ID.
    willReplaceOnChanges: true
requiredInputs:
  - credentials
  - voiceConnectorId
stateInputs:
  description: |
    Input properties used for looking up and filtering VoiceConnectorTerminationCredentials resources.
  properties:
    credentials:
      type: array
      items:
        $ref: "#/types/aws:chime/VoiceConnectorTerminationCredentialsCredential:VoiceConnectorTerminationCredentialsCredential"
      description: |
        List of termination SIP credentials.
    voiceConnectorId:
      type: string
      description: |
        Amazon Chime Voice Connector ID.
      willReplaceOnChanges: true
  type: object
