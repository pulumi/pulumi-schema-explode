$token: aws:chime/voiceConnectorGroup:VoiceConnectorGroup
properties:
  connectors:
    type: array
    items:
      $ref: "#/types/aws:chime/VoiceConnectorGroupConnector:VoiceConnectorGroupConnector"
    description: |
      The Amazon Chime Voice Connectors to route inbound calls to.
  name:
    type: string
    description: |
      The name of the Amazon Chime Voice Connector group.
required:
  - name
inputProperties:
  connectors:
    type: array
    items:
      $ref: "#/types/aws:chime/VoiceConnectorGroupConnector:VoiceConnectorGroupConnector"
    description: |
      The Amazon Chime Voice Connectors to route inbound calls to.
  name:
    type: string
    description: |
      The name of the Amazon Chime Voice Connector group.
stateInputs:
  description: |
    Input properties used for looking up and filtering VoiceConnectorGroup resources.
  properties:
    connectors:
      type: array
      items:
        $ref: "#/types/aws:chime/VoiceConnectorGroupConnector:VoiceConnectorGroupConnector"
      description: |
        The Amazon Chime Voice Connectors to route inbound calls to.
    name:
      type: string
      description: |
        The name of the Amazon Chime Voice Connector group.
  type: object
