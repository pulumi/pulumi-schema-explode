$token: aws:chime/voiceConnectorStreaming:VoiceConnectorStreaming
properties:
  dataRetention:
    type: integer
    description: |
      The retention period, in hours, for the Amazon Kinesis data.
  disabled:
    type: boolean
    description: |
      When true, media streaming to Amazon Kinesis is turned off. Default: `false`
  streamingNotificationTargets:
    type: array
    items:
      type: string
    description: |
      The streaming notification targets. Valid Values: `EventBridge | SNS | SQS`
  voiceConnectorId:
    type: string
    description: |
      The Amazon Chime Voice Connector ID.
required:
  - dataRetention
  - voiceConnectorId
inputProperties:
  dataRetention:
    type: integer
    description: |
      The retention period, in hours, for the Amazon Kinesis data.
  disabled:
    type: boolean
    description: |
      When true, media streaming to Amazon Kinesis is turned off. Default: `false`
  streamingNotificationTargets:
    type: array
    items:
      type: string
    description: |
      The streaming notification targets. Valid Values: `EventBridge | SNS | SQS`
  voiceConnectorId:
    type: string
    description: |
      The Amazon Chime Voice Connector ID.
    willReplaceOnChanges: true
requiredInputs:
  - dataRetention
  - voiceConnectorId
stateInputs:
  description: |
    Input properties used for looking up and filtering VoiceConnectorStreaming resources.
  properties:
    dataRetention:
      type: integer
      description: |
        The retention period, in hours, for the Amazon Kinesis data.
    disabled:
      type: boolean
      description: |
        When true, media streaming to Amazon Kinesis is turned off. Default: `false`
    streamingNotificationTargets:
      type: array
      items:
        type: string
      description: |
        The streaming notification targets. Valid Values: `EventBridge | SNS | SQS`
    voiceConnectorId:
      type: string
      description: |
        The Amazon Chime Voice Connector ID.
      willReplaceOnChanges: true
  type: object
