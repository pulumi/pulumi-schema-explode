$token: aws:chime/VoiceConnectorTerminationCredentialsCredential:VoiceConnectorTerminationCredentialsCredential
properties:
  password:
    type: string
    description: |
      RFC2617 compliant password associated with the SIP credentials.
    language:
      python:
        mapCase: false
  username:
    type: string
    description: |
      RFC2617 compliant username associated with the SIP credentials.
    language:
      python:
        mapCase: false
type: object
required:
  - password
  - username
