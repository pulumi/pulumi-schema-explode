$token: aws:cloudwatch/EventTargetEcsTargetNetworkConfiguration:EventTargetEcsTargetNetworkConfiguration
properties:
  assignPublicIp:
    type: boolean
    description: |
      Assign a public IP address to the ENI (Fargate launch type only). Valid values are `true` or `false`. Default `false`.
    language:
      python:
        mapCase: false
  securityGroups:
    type: array
    items:
      type: string
    description: |
      The security groups associated with the task or service. If you do not specify a security group, the default security group for the VPC is used.
    language:
      python:
        mapCase: false
  subnets:
    type: array
    items:
      type: string
    description: |
      The subnets associated with the task or service.
    language:
      python:
        mapCase: false
type: object
required:
  - subnets
