$token: aws:cloudwatch/MetricStreamStatisticsConfiguration:MetricStreamStatisticsConfiguration
properties:
  additionalStatistics:
    type: array
    items:
      type: string
    description: |
      The additional statistics to stream for the metrics listed in `include_metrics`.
    language:
      python:
        mapCase: false
  includeMetrics:
    type: array
    items:
      $ref: "#/types/aws:cloudwatch/MetricStreamStatisticsConfigurationIncludeMetric:MetricStreamStatisticsConfigurationIncludeMetric"
    description: |
      An array that defines the metrics that are to have additional statistics streamed. See details below.
    language:
      python:
        mapCase: false
type: object
required:
  - additionalStatistics
  - includeMetrics
