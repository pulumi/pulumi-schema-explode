$token: aws:cloudwatch/EventConnectionAuthParametersOauth:EventConnectionAuthParametersOauth
properties:
  authorizationEndpoint:
    type: string
    description: |
      The URL to the authorization endpoint.
    language:
      python:
        mapCase: false
  clientParameters:
    $ref: "#/types/aws:cloudwatch/EventConnectionAuthParametersOauthClientParameters:EventConnectionAuthParametersOauthClientParameters"
    description: |
      Contains the client parameters for OAuth authorization. Contains the following two parameters.
    language:
      python:
        mapCase: false
  httpMethod:
    type: string
    description: |
      A password for the authorization. Created and stored in AWS Secrets Manager.
    language:
      python:
        mapCase: false
  oauthHttpParameters:
    $ref: "#/types/aws:cloudwatch/EventConnectionAuthParametersOauthOauthHttpParameters:EventConnectionAuthParametersOauthOauthHttpParameters"
    description: |
      OAuth Http Parameters are additional credentials used to sign the request to the authorization endpoint to exchange the OAuth Client information for an access token. Secret values are stored and managed by AWS Secrets Manager. A maximum of 1 are allowed. Documented below.
    language:
      python:
        mapCase: false
type: object
required:
  - authorizationEndpoint
  - httpMethod
  - oauthHttpParameters
