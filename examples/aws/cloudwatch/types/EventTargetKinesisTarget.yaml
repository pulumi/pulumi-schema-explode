$token: aws:cloudwatch/EventTargetKinesisTarget:EventTargetKinesisTarget
properties:
  partitionKeyPath:
    type: string
    description: |
      The JSON path to be extracted from the event and used as the partition key.
    language:
      python:
        mapCase: false
type: object
