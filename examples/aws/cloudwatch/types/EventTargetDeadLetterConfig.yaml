$token: aws:cloudwatch/EventTargetDeadLetterConfig:EventTargetDeadLetterConfig
properties:
  arn:
    type: string
    description: |
      - ARN of the SQS queue specified as the target for the dead-letter queue.
    language:
      python:
        mapCase: false
type: object
