$token: aws:cloudwatch/EventTargetRunCommandTarget:EventTargetRunCommandTarget
properties:
  key:
    type: string
    description: |
      Can be either `tag:tag-key` or `InstanceIds`.
    language:
      python:
        mapCase: false
  values:
    type: array
    items:
      type: string
    description: |
      If Key is `tag:tag-key`, Values is a list of tag values. If Key is `InstanceIds`, Values is a list of Amazon EC2 instance IDs.
    language:
      python:
        mapCase: false
type: object
required:
  - key
  - values
