$token: aws:cloudwatch/EventTargetRedshiftTarget:EventTargetRedshiftTarget
properties:
  database:
    type: string
    description: |
      The name of the database.
    language:
      python:
        mapCase: false
  dbUser:
    type: string
    description: |
      The database user name.
    language:
      python:
        mapCase: false
  secretsManagerArn:
    type: string
    description: |
      The name or ARN of the secret that enables access to the database.
    language:
      python:
        mapCase: false
  sql:
    type: string
    description: |
      The SQL statement text to run.
    language:
      python:
        mapCase: false
  statementName:
    type: string
    description: |
      The name of the SQL statement.
    language:
      python:
        mapCase: false
  withEvent:
    type: boolean
    description: |
      Indicates whether to send an event back to EventBridge after the SQL statement runs.
    language:
      python:
        mapCase: false
type: object
required:
  - database
