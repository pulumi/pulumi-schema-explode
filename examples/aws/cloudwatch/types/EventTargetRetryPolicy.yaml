$token: aws:cloudwatch/EventTargetRetryPolicy:EventTargetRetryPolicy
properties:
  maximumEventAgeInSeconds:
    type: integer
    description: |
      The age in seconds to continue to make retry attempts.
    language:
      python:
        mapCase: false
  maximumRetryAttempts:
    type: integer
    description: |
      maximum number of retry attempts to make before the request fails
    language:
      python:
        mapCase: false
type: object
