$token: aws:cloudwatch/logDestination:LogDestination
properties:
  arn:
    type: string
    description: |
      The Amazon Resource Name (ARN) specifying the log destination.
  name:
    type: string
    description: |
      A name for the log destination
  roleArn:
    type: string
    description: |
      The ARN of an IAM role that grants Amazon CloudWatch Logs permissions to put data into the target
  targetArn:
    type: string
    description: |
      The ARN of the target Amazon Kinesis stream resource for the destination
required:
  - arn
  - name
  - roleArn
  - targetArn
inputProperties:
  name:
    type: string
    description: |
      A name for the log destination
    willReplaceOnChanges: true
  roleArn:
    type: string
    description: |
      The ARN of an IAM role that grants Amazon CloudWatch Logs permissions to put data into the target
  targetArn:
    type: string
    description: |
      The ARN of the target Amazon Kinesis stream resource for the destination
requiredInputs:
  - roleArn
  - targetArn
stateInputs:
  description: |
    Input properties used for looking up and filtering LogDestination resources.
  properties:
    arn:
      type: string
      description: |
        The Amazon Resource Name (ARN) specifying the log destination.
    name:
      type: string
      description: |
        A name for the log destination
      willReplaceOnChanges: true
    roleArn:
      type: string
      description: |
        The ARN of an IAM role that grants Amazon CloudWatch Logs permissions to put data into the target
    targetArn:
      type: string
      description: |
        The ARN of the target Amazon Kinesis stream resource for the destination
  type: object
