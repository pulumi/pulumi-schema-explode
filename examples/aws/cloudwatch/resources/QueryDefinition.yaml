$token: aws:cloudwatch/queryDefinition:QueryDefinition
properties:
  logGroupNames:
    type: array
    items:
      type: string
    description: |
      Specific log groups to use with the query.
  name:
    type: string
    description: |
      The name of the query.
  queryDefinitionId:
    type: string
    description: |
      The query definition ID.
  queryString:
    type: string
    description: |
      The query to save. You can read more about CloudWatch Logs Query Syntax in the [documentation](https://docs.aws.amazon.com/AmazonCloudWatch/latest/logs/CWL_QuerySyntax.html).
required:
  - name
  - queryDefinitionId
  - queryString
inputProperties:
  logGroupNames:
    type: array
    items:
      type: string
    description: |
      Specific log groups to use with the query.
  name:
    type: string
    description: |
      The name of the query.
  queryString:
    type: string
    description: |
      The query to save. You can read more about CloudWatch Logs Query Syntax in the [documentation](https://docs.aws.amazon.com/AmazonCloudWatch/latest/logs/CWL_QuerySyntax.html).
requiredInputs:
  - queryString
stateInputs:
  description: |
    Input properties used for looking up and filtering QueryDefinition resources.
  properties:
    logGroupNames:
      type: array
      items:
        type: string
      description: |
        Specific log groups to use with the query.
    name:
      type: string
      description: |
        The name of the query.
    queryDefinitionId:
      type: string
      description: |
        The query definition ID.
    queryString:
      type: string
      description: |
        The query to save. You can read more about CloudWatch Logs Query Syntax in the [documentation](https://docs.aws.amazon.com/AmazonCloudWatch/latest/logs/CWL_QuerySyntax.html).
  type: object
