$token: aws:cloudwatch/logResourcePolicy:LogResourcePolicy
properties:
  policyDocument:
    type: string
    description: |
      Details of the resource policy, including the identity of the principal that is enabled to put logs to this account. This is formatted as a JSON string. Maximum length of 5120 characters.
  policyName:
    type: string
    description: |
      Name of the resource policy.
required:
  - policyDocument
  - policyName
inputProperties:
  policyDocument:
    type: string
    description: |
      Details of the resource policy, including the identity of the principal that is enabled to put logs to this account. This is formatted as a JSON string. Maximum length of 5120 characters.
  policyName:
    type: string
    description: |
      Name of the resource policy.
    willReplaceOnChanges: true
requiredInputs:
  - policyDocument
  - policyName
stateInputs:
  description: |
    Input properties used for looking up and filtering LogResourcePolicy resources.
  properties:
    policyDocument:
      type: string
      description: |
        Details of the resource policy, including the identity of the principal that is enabled to put logs to this account. This is formatted as a JSON string. Maximum length of 5120 characters.
    policyName:
      type: string
      description: |
        Name of the resource policy.
      willReplaceOnChanges: true
  type: object
