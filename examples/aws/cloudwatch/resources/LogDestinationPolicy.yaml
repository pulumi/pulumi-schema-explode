$token: aws:cloudwatch/logDestinationPolicy:LogDestinationPolicy
properties:
  accessPolicy:
    type: string
    description: |
      The policy document. This is a JSON formatted string.
  destinationName:
    type: string
    description: |
      A name for the subscription filter
  forceUpdate:
    type: boolean
    description: |
      Specify true if you are updating an existing destination policy to grant permission to an organization ID instead of granting permission to individual AWS accounts.
required:
  - accessPolicy
  - destinationName
inputProperties:
  accessPolicy:
    type: string
    description: |
      The policy document. This is a JSON formatted string.
  destinationName:
    type: string
    description: |
      A name for the subscription filter
    willReplaceOnChanges: true
  forceUpdate:
    type: boolean
    description: |
      Specify true if you are updating an existing destination policy to grant permission to an organization ID instead of granting permission to individual AWS accounts.
requiredInputs:
  - accessPolicy
  - destinationName
stateInputs:
  description: |
    Input properties used for looking up and filtering LogDestinationPolicy resources.
  properties:
    accessPolicy:
      type: string
      description: |
        The policy document. This is a JSON formatted string.
    destinationName:
      type: string
      description: |
        A name for the subscription filter
      willReplaceOnChanges: true
    forceUpdate:
      type: boolean
      description: |
        Specify true if you are updating an existing destination policy to grant permission to an organization ID instead of granting permission to individual AWS accounts.
  type: object
