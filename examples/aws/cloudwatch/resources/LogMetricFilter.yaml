$token: aws:cloudwatch/logMetricFilter:LogMetricFilter
properties:
  logGroupName:
    type: string
    description: |
      The name of the log group to associate the metric filter with.
  metricTransformation:
    $ref: "#/types/aws:cloudwatch/LogMetricFilterMetricTransformation:LogMetricFilterMetricTransformation"
    description: |
      A block defining collection of information needed to define how metric data gets emitted. See below.
  name:
    type: string
    description: |
      A name for the metric filter.
  pattern:
    type: string
    description: |
      A valid [CloudWatch Logs filter pattern](https://docs.aws.amazon.com/AmazonCloudWatch/latest/DeveloperGuide/FilterAndPatternSyntax.html)
      for extracting metric data out of ingested log events.
required:
  - logGroupName
  - metricTransformation
  - name
  - pattern
inputProperties:
  logGroupName:
    type: string
    description: |
      The name of the log group to associate the metric filter with.
    willReplaceOnChanges: true
  metricTransformation:
    $ref: "#/types/aws:cloudwatch/LogMetricFilterMetricTransformation:LogMetricFilterMetricTransformation"
    description: |
      A block defining collection of information needed to define how metric data gets emitted. See below.
  name:
    type: string
    description: |
      A name for the metric filter.
    willReplaceOnChanges: true
  pattern:
    type: string
    description: |
      A valid [CloudWatch Logs filter pattern](https://docs.aws.amazon.com/AmazonCloudWatch/latest/DeveloperGuide/FilterAndPatternSyntax.html)
      for extracting metric data out of ingested log events.
requiredInputs:
  - logGroupName
  - metricTransformation
  - pattern
stateInputs:
  description: |
    Input properties used for looking up and filtering LogMetricFilter resources.
  properties:
    logGroupName:
      type: string
      description: |
        The name of the log group to associate the metric filter with.
      willReplaceOnChanges: true
    metricTransformation:
      $ref: "#/types/aws:cloudwatch/LogMetricFilterMetricTransformation:LogMetricFilterMetricTransformation"
      description: |
        A block defining collection of information needed to define how metric data gets emitted. See below.
    name:
      type: string
      description: |
        A name for the metric filter.
      willReplaceOnChanges: true
    pattern:
      type: string
      description: |
        A valid [CloudWatch Logs filter pattern](https://docs.aws.amazon.com/AmazonCloudWatch/latest/DeveloperGuide/FilterAndPatternSyntax.html)
        for extracting metric data out of ingested log events.
  type: object
