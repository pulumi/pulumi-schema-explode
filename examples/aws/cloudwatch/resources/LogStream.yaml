$token: aws:cloudwatch/logStream:LogStream
properties:
  arn:
    type: string
    description: |
      The Amazon Resource Name (ARN) specifying the log stream.
  logGroupName:
    type: string
    description: |
      The name of the log group under which the log stream is to be created.
  name:
    type: string
    description: |
      The name of the log stream. Must not be longer than 512 characters and must not contain `:`
required:
  - arn
  - logGroupName
  - name
inputProperties:
  logGroupName:
    type: string
    description: |
      The name of the log group under which the log stream is to be created.
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      The name of the log stream. Must not be longer than 512 characters and must not contain `:`
    willReplaceOnChanges: true
requiredInputs:
  - logGroupName
stateInputs:
  description: |
    Input properties used for looking up and filtering LogStream resources.
  properties:
    arn:
      type: string
      description: |
        The Amazon Resource Name (ARN) specifying the log stream.
    logGroupName:
      type: string
      description: |
        The name of the log group under which the log stream is to be created.
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        The name of the log stream. Must not be longer than 512 characters and must not contain `:`
      willReplaceOnChanges: true
  type: object
