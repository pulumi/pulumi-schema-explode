$token: aws:cloudwatch/eventConnection:EventConnection
properties:
  arn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the connection.
  authParameters:
    $ref: "#/types/aws:cloudwatch/EventConnectionAuthParameters:EventConnectionAuthParameters"
    description: |
      Parameters used for authorization. A maximum of 1 are allowed. Documented below.
  authorizationType:
    type: string
    description: |
      Choose the type of authorization to use for the connection. One of `API_KEY`,`BASIC`,`OAUTH_CLIENT_CREDENTIALS`.
  description:
    type: string
    description: |
      Enter a description for the connection. Maximum of 512 characters.
  name:
    type: string
    description: |
      The name of the new connection. Maximum of 64 characters consisting of numbers, lower/upper case letters, .,-,_.
  secretArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the secret created from the authorization parameters specified for the connection.
required:
  - arn
  - authParameters
  - authorizationType
  - name
  - secretArn
inputProperties:
  authParameters:
    $ref: "#/types/aws:cloudwatch/EventConnectionAuthParameters:EventConnectionAuthParameters"
    description: |
      Parameters used for authorization. A maximum of 1 are allowed. Documented below.
  authorizationType:
    type: string
    description: |
      Choose the type of authorization to use for the connection. One of `API_KEY`,`BASIC`,`OAUTH_CLIENT_CREDENTIALS`.
  description:
    type: string
    description: |
      Enter a description for the connection. Maximum of 512 characters.
  name:
    type: string
    description: |
      The name of the new connection. Maximum of 64 characters consisting of numbers, lower/upper case letters, .,-,_.
    willReplaceOnChanges: true
requiredInputs:
  - authParameters
  - authorizationType
stateInputs:
  description: |
    Input properties used for looking up and filtering EventConnection resources.
  properties:
    arn:
      type: string
      description: |
        The Amazon Resource Name (ARN) of the connection.
    authParameters:
      $ref: "#/types/aws:cloudwatch/EventConnectionAuthParameters:EventConnectionAuthParameters"
      description: |
        Parameters used for authorization. A maximum of 1 are allowed. Documented below.
    authorizationType:
      type: string
      description: |
        Choose the type of authorization to use for the connection. One of `API_KEY`,`BASIC`,`OAUTH_CLIENT_CREDENTIALS`.
    description:
      type: string
      description: |
        Enter a description for the connection. Maximum of 512 characters.
    name:
      type: string
      description: |
        The name of the new connection. Maximum of 64 characters consisting of numbers, lower/upper case letters, .,-,_.
      willReplaceOnChanges: true
    secretArn:
      type: string
      description: |
        The Amazon Resource Name (ARN) of the secret created from the authorization parameters specified for the connection.
  type: object
