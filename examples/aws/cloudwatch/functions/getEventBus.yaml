$token: aws:cloudwatch/getEventBus:getEventBus
inputs:
  description: |
    A collection of arguments for invoking getEventBus.
  properties:
    name:
      type: string
      description: |
        Friendly EventBridge event bus name.
  type: object
  required:
    - name
outputs:
  description: |
    A collection of values returned by getEventBus.
  properties:
    arn:
      type: string
      description: |
        ARN.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    name:
      type: string
  type: object
  required:
    - arn
    - name
    - id
