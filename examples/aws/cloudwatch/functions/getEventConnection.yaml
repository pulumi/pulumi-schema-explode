$token: aws:cloudwatch/getEventConnection:getEventConnection
inputs:
  description: |
    A collection of arguments for invoking getEventConnection.
  properties:
    name:
      type: string
      description: |
        Name of the connection.
  type: object
  required:
    - name
outputs:
  description: |
    A collection of values returned by getEventConnection.
  properties:
    arn:
      type: string
      description: |
        ARN (Amazon Resource Name) for the connection.
    authorizationType:
      type: string
      description: |
        Type of authorization to use to connect. One of `API_KEY`,`BASIC`,`OAUTH_CLIENT_CREDENTIALS`.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    name:
      type: string
      description: |
        Name of the connection.
    secretArn:
      type: string
      description: |
        ARN (Amazon Resource Name) for the secret created from the authorization parameters specified for the connection.
  type: object
  required:
    - arn
    - authorizationType
    - name
    - secretArn
    - id
