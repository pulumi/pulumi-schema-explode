$token: aws:elasticsearch/DomainAdvancedSecurityOptions:DomainAdvancedSecurityOptions
properties:
  enabled:
    type: boolean
    description: |
      Whether to enable node-to-node encryption. If the `node_to_node_encryption` block is not provided then this defaults to `false`. Enabling node-to-node encryption of a new domain requires an `elasticsearch_version` of `6.0` or greater.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  internalUserDatabaseEnabled:
    type: boolean
    description: |
      Whether the internal user database is enabled. If not set, defaults to `false` by the AWS API.
    language:
      python:
        mapCase: false
  masterUserOptions:
    $ref: "#/types/aws:elasticsearch/DomainAdvancedSecurityOptionsMasterUserOptions:DomainAdvancedSecurityOptionsMasterUserOptions"
    description: |
      Configuration block for the main user. Detailed below.
    language:
      python:
        mapCase: false
type: object
required:
  - enabled
