$token: aws:elasticsearch/DomainAutoTuneOptionsMaintenanceSchedule:DomainAutoTuneOptionsMaintenanceSchedule
properties:
  cronExpressionForRecurrence:
    type: string
    description: |
      A cron expression specifying the recurrence pattern for an Auto-Tune maintenance schedule.
    language:
      python:
        mapCase: false
  duration:
    $ref: "#/types/aws:elasticsearch/DomainAutoTuneOptionsMaintenanceScheduleDuration:DomainAutoTuneOptionsMaintenanceScheduleDuration"
    description: |
      Configuration block for the duration of the Auto-Tune maintenance window. Detailed below.
    language:
      python:
        mapCase: false
  startAt:
    type: string
    description: |
      Date and time at which to start the Auto-Tune maintenance schedule in [RFC3339 format](https://tools.ietf.org/html/rfc3339#section-5.8).
    language:
      python:
        mapCase: false
type: object
required:
  - cronExpressionForRecurrence
  - duration
  - startAt
