$token: aws:elasticsearch/getDomainVpcOption:getDomainVpcOption
properties:
  availabilityZones:
    type: array
    items:
      type: string
    description: |
      The availability zones used by the domain.
    language:
      python:
        mapCase: false
  securityGroupIds:
    type: array
    items:
      type: string
    description: |
      The security groups used by the domain.
    language:
      python:
        mapCase: false
  subnetIds:
    type: array
    items:
      type: string
    description: |
      The subnets used by the domain.
    language:
      python:
        mapCase: false
  vpcId:
    type: string
    description: |
      The VPC used by the domain.
    language:
      python:
        mapCase: false
type: object
required:
  - availabilityZones
  - securityGroupIds
  - subnetIds
  - vpcId
language:
  nodejs:
    requiredInputs: []
