$token: aws:elasticsearch/DomainClusterConfig:DomainClusterConfig
properties:
  coldStorageOptions:
    $ref: "#/types/aws:elasticsearch/DomainClusterConfigColdStorageOptions:DomainClusterConfigColdStorageOptions"
    description: |
      Configuration block containing cold storage configuration. Detailed below.
    language:
      python:
        mapCase: false
  dedicatedMasterCount:
    type: integer
    description: |
      Number of dedicated main nodes in the cluster.
    language:
      python:
        mapCase: false
  dedicatedMasterEnabled:
    type: boolean
    description: |
      Whether dedicated main nodes are enabled for the cluster.
    language:
      python:
        mapCase: false
  dedicatedMasterType:
    type: string
    description: |
      Instance type of the dedicated main nodes in the cluster.
    language:
      python:
        mapCase: false
  instanceCount:
    type: integer
    description: |
      Number of instances in the cluster.
    language:
      python:
        mapCase: false
  instanceType:
    type: string
    description: |
      Instance type of data nodes in the cluster.
    language:
      python:
        mapCase: false
  warmCount:
    type: integer
    description: |
      Number of warm nodes in the cluster. Valid values are between `2` and `150`. `warm_count` can be only and must be set when `warm_enabled` is set to `true`.
    language:
      python:
        mapCase: false
  warmEnabled:
    type: boolean
    description: |
      Whether to enable warm storage.
    language:
      python:
        mapCase: false
  warmType:
    type: string
    description: |
      Instance type for the Elasticsearch cluster's warm nodes. Valid values are `ultrawarm1.medium.elasticsearch`, `ultrawarm1.large.elasticsearch` and `ultrawarm1.xlarge.elasticsearch`. `warm_type` can be only and must be set when `warm_enabled` is set to `true`.
    language:
      python:
        mapCase: false
  zoneAwarenessConfig:
    $ref: "#/types/aws:elasticsearch/DomainClusterConfigZoneAwarenessConfig:DomainClusterConfigZoneAwarenessConfig"
    description: |
      Configuration block containing zone awareness settings. Detailed below.
    language:
      python:
        mapCase: false
  zoneAwarenessEnabled:
    type: boolean
    description: |
      Whether zone awareness is enabled, set to `true` for multi-az deployment. To enable awareness with three Availability Zones, the `availability_zone_count` within the `zone_awareness_config` must be set to `3`.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - coldStorageOptions
