$token: aws:elasticsearch/DomainSamlOptionsSamlOptions:DomainSamlOptionsSamlOptions
properties:
  enabled:
    type: boolean
    description: |
      Whether SAML authentication is enabled.
    language:
      python:
        mapCase: false
  idp:
    $ref: "#/types/aws:elasticsearch/DomainSamlOptionsSamlOptionsIdp:DomainSamlOptionsSamlOptionsIdp"
    description: |
      Information from your identity provider.
    language:
      python:
        mapCase: false
  masterBackendRole:
    type: string
    description: |
      This backend role from the SAML IdP receives full permissions to the cluster, equivalent to a new master user.
    language:
      python:
        mapCase: false
  masterUserName:
    type: string
    description: |
      This username from the SAML IdP receives full permissions to the cluster, equivalent to a new master user.
    language:
      python:
        mapCase: false
  rolesKey:
    type: string
    description: |
      Element of the SAML assertion to use for backend roles. Default is roles.
    language:
      python:
        mapCase: false
  sessionTimeoutMinutes:
    type: integer
    description: |
      Duration of a session in minutes after a user logs in. Default is 60. Maximum value is 1,440.
    language:
      python:
        mapCase: false
  subjectKey:
    type: string
    description: |
      Custom SAML attribute to use for user names. Default is an empty string - `""`. This will cause Elasticsearch to use the `NameID` element of the `Subject`, which is the default location for name identifiers in the SAML specification.
    language:
      python:
        mapCase: false
type: object
