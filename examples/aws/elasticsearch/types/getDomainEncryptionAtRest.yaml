$token: aws:elasticsearch/getDomainEncryptionAtRest:getDomainEncryptionAtRest
properties:
  enabled:
    type: boolean
    description: |
      Whether node to node encryption is enabled.
    language:
      python:
        mapCase: false
  kmsKeyId:
    type: string
    description: |
      The KMS key id used to encrypt data at rest.
    language:
      python:
        mapCase: false
type: object
required:
  - enabled
  - kmsKeyId
language:
  nodejs:
    requiredInputs: []
