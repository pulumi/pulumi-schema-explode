$token: aws:elasticsearch/getDomainLogPublishingOption:getDomainLogPublishingOption
properties:
  cloudwatchLogGroupArn:
    type: string
    description: |
      The CloudWatch Log Group where the logs are published.
    language:
      python:
        mapCase: false
  enabled:
    type: boolean
    description: |
      Whether node to node encryption is enabled.
    language:
      python:
        mapCase: false
  logType:
    type: string
    description: |
      The type of Elasticsearch log being published.
    language:
      python:
        mapCase: false
type: object
required:
  - cloudwatchLogGroupArn
  - enabled
  - logType
language:
  nodejs:
    requiredInputs: []
