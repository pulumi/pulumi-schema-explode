$token: aws:s3control/accessPointPolicy:AccessPointPolicy
properties:
  accessPointArn:
    type: string
    description: |
      The ARN of the access point that you want to associate with the specified policy.
  hasPublicAccessPolicy:
    type: boolean
    description: |
      Indicates whether this access point currently has a policy that allows public access.
  policy:
    type: string
    description: |
      The policy that you want to apply to the specified access point.
required:
  - accessPointArn
  - hasPublicAccessPolicy
  - policy
inputProperties:
  accessPointArn:
    type: string
    description: |
      The ARN of the access point that you want to associate with the specified policy.
    willReplaceOnChanges: true
  policy:
    type: string
    description: |
      The policy that you want to apply to the specified access point.
requiredInputs:
  - accessPointArn
  - policy
stateInputs:
  description: |
    Input properties used for looking up and filtering AccessPointPolicy resources.
  properties:
    accessPointArn:
      type: string
      description: |
        The ARN of the access point that you want to associate with the specified policy.
      willReplaceOnChanges: true
    hasPublicAccessPolicy:
      type: boolean
      description: |
        Indicates whether this access point currently has a policy that allows public access.
    policy:
      type: string
      description: |
        The policy that you want to apply to the specified access point.
  type: object
