$token: aws:s3control/BucketLifecycleConfigurationRuleExpiration:BucketLifecycleConfigurationRuleExpiration
properties:
  date:
    type: string
    description: |
      Date the object is to be deleted. Should be in `YYYY-MM-DD` date format, e.g., `2020-09-30`.
    language:
      python:
        mapCase: false
  days:
    type: integer
    description: |
      Number of days before the object is to be deleted.
    language:
      python:
        mapCase: false
  expiredObjectDeleteMarker:
    type: boolean
    description: |
      Enable to remove a delete marker with no noncurrent versions. Cannot be specified with `date` or `days`.
    language:
      python:
        mapCase: false
type: object
