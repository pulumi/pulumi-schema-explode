$token: aws:s3control/BucketLifecycleConfigurationRule:BucketLifecycleConfigurationRule
properties:
  abortIncompleteMultipartUpload:
    $ref: "#/types/aws:s3control/BucketLifecycleConfigurationRuleAbortIncompleteMultipartUpload:BucketLifecycleConfigurationRuleAbortIncompleteMultipartUpload"
    description: |
      Configuration block containing settings for abort incomplete multipart upload.
    language:
      python:
        mapCase: false
  expiration:
    $ref: "#/types/aws:s3control/BucketLifecycleConfigurationRuleExpiration:BucketLifecycleConfigurationRuleExpiration"
    description: |
      Configuration block containing settings for expiration of objects.
    language:
      python:
        mapCase: false
  filter:
    $ref: "#/types/aws:s3control/BucketLifecycleConfigurationRuleFilter:BucketLifecycleConfigurationRuleFilter"
    description: |
      Configuration block containing settings for filtering.
    language:
      python:
        mapCase: false
  id:
    type: string
    description: |
      Unique identifier for the rule.
    language:
      python:
        mapCase: false
  status:
    type: string
    description: |
      Status of the rule. Valid values: `Enabled` and `Disabled`. Defaults to `Enabled`.
    language:
      python:
        mapCase: false
type: object
required:
  - id
