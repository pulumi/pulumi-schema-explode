$token: aws:sqs/queuePolicy:QueuePolicy
properties:
  policy:
    type: string
    description: |
      The JSON policy for the SQS queue.
  queueUrl:
    type: string
    description: |
      The URL of the SQS Queue to which to attach the policy
required:
  - policy
  - queueUrl
inputProperties:
  policy:
    type: string
    oneOf:
      - type: string
      - type: string
        $ref: "#/types/aws:iam/documents:PolicyDocument"
    description: |
      The JSON policy for the SQS queue.
  queueUrl:
    type: string
    description: |
      The URL of the SQS Queue to which to attach the policy
    willReplaceOnChanges: true
requiredInputs:
  - policy
  - queueUrl
stateInputs:
  description: |
    Input properties used for looking up and filtering QueuePolicy resources.
  properties:
    policy:
      type: string
      oneOf:
        - type: string
        - type: string
          $ref: "#/types/aws:iam/documents:PolicyDocument"
      description: |
        The JSON policy for the SQS queue.
    queueUrl:
      type: string
      description: |
        The URL of the SQS Queue to which to attach the policy
      willReplaceOnChanges: true
  type: object
