$token: aws:sqs/getQueue:getQueue
inputs:
  description: |
    A collection of arguments for invoking getQueue.
  properties:
    name:
      type: string
      description: |
        Name of the queue to match.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags for the resource.
  type: object
  required:
    - name
outputs:
  description: |
    A collection of values returned by getQueue.
  properties:
    arn:
      type: string
      description: |
        ARN of the queue.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    name:
      type: string
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags for the resource.
    url:
      type: string
      description: |
        URL of the queue.
  type: object
  required:
    - arn
    - name
    - tags
    - url
    - id
