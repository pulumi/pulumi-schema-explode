$token: aws:ecr/getCredentials:getCredentials
inputs:
  description: |
    A collection of arguments for invoking getCredentials.
  properties:
    registryId:
      type: string
      willReplaceOnChanges: true
  type: object
  required:
    - registryId
outputs:
  description: |
    A collection of values returned by getCredentials.
  properties:
    authorizationToken:
      type: string
    expiresAt:
      type: string
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    proxyEndpoint:
      type: string
    registryId:
      type: string
  type: object
  required:
    - authorizationToken
    - expiresAt
    - proxyEndpoint
    - registryId
    - id
