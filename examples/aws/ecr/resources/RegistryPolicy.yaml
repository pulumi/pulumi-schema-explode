$token: aws:ecr/registryPolicy:RegistryPolicy
properties:
  policy:
    type: string
  registryId:
    type: string
    description: |
      The registry ID where the registry was created.
required:
  - policy
  - registryId
inputProperties:
  policy:
    type: string
requiredInputs:
  - policy
stateInputs:
  description: |
    Input properties used for looking up and filtering RegistryPolicy resources.
  properties:
    policy:
      type: string
    registryId:
      type: string
      description: |
        The registry ID where the registry was created.
  type: object
