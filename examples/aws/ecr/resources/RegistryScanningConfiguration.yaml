$token: aws:ecr/registryScanningConfiguration:RegistryScanningConfiguration
properties:
  registryId:
    type: string
    description: |
      The registry ID the scanning configuration applies to.
  rules:
    type: array
    items:
      $ref: "#/types/aws:ecr/RegistryScanningConfigurationRule:RegistryScanningConfigurationRule"
    description: |
      One or multiple blocks specifying scanning rules to determine which repository filters are used and at what frequency scanning will occur. See below for schema.
  scanType:
    type: string
    description: |
      the scanning type to set for the registry. Can be either `ENHANCED` or `BASIC`.
required:
  - registryId
  - scanType
inputProperties:
  rules:
    type: array
    items:
      $ref: "#/types/aws:ecr/RegistryScanningConfigurationRule:RegistryScanningConfigurationRule"
    description: |
      One or multiple blocks specifying scanning rules to determine which repository filters are used and at what frequency scanning will occur. See below for schema.
  scanType:
    type: string
    description: |
      the scanning type to set for the registry. Can be either `ENHANCED` or `BASIC`.
requiredInputs:
  - scanType
stateInputs:
  description: |
    Input properties used for looking up and filtering RegistryScanningConfiguration resources.
  properties:
    registryId:
      type: string
      description: |
        The registry ID the scanning configuration applies to.
    rules:
      type: array
      items:
        $ref: "#/types/aws:ecr/RegistryScanningConfigurationRule:RegistryScanningConfigurationRule"
      description: |
        One or multiple blocks specifying scanning rules to determine which repository filters are used and at what frequency scanning will occur. See below for schema.
    scanType:
      type: string
      description: |
        the scanning type to set for the registry. Can be either `ENHANCED` or `BASIC`.
  type: object
