$token: aws:ecr/repositoryPolicy:RepositoryPolicy
properties:
  policy:
    type: string
    description: |
      The policy document. This is a JSON formatted string.
  registryId:
    type: string
    description: |
      The registry ID where the repository was created.
  repository:
    type: string
    description: |
      Name of the repository to apply the policy.
required:
  - policy
  - registryId
  - repository
inputProperties:
  policy:
    type: string
    oneOf:
      - type: string
      - type: string
        $ref: "#/types/aws:iam/documents:PolicyDocument"
    description: |
      The policy document. This is a JSON formatted string.
  repository:
    type: string
    description: |
      Name of the repository to apply the policy.
    willReplaceOnChanges: true
requiredInputs:
  - policy
  - repository
stateInputs:
  description: |
    Input properties used for looking up and filtering RepositoryPolicy resources.
  properties:
    policy:
      type: string
      oneOf:
        - type: string
        - type: string
          $ref: "#/types/aws:iam/documents:PolicyDocument"
      description: |
        The policy document. This is a JSON formatted string.
    registryId:
      type: string
      description: |
        The registry ID where the repository was created.
    repository:
      type: string
      description: |
        Name of the repository to apply the policy.
      willReplaceOnChanges: true
  type: object
