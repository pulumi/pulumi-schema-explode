$token: aws:ecr/pullThroughCacheRule:PullThroughCacheRule
properties:
  ecrRepositoryPrefix:
    type: string
    description: |
      The repository name prefix to use when caching images from the source registry.
  registryId:
    type: string
    description: |
      The registry ID where the repository was created.
  upstreamRegistryUrl:
    type: string
    description: |
      The registry URL of the upstream public registry to use as the source.
required:
  - ecrRepositoryPrefix
  - registryId
  - upstreamRegistryUrl
inputProperties:
  ecrRepositoryPrefix:
    type: string
    description: |
      The repository name prefix to use when caching images from the source registry.
    willReplaceOnChanges: true
  upstreamRegistryUrl:
    type: string
    description: |
      The registry URL of the upstream public registry to use as the source.
    willReplaceOnChanges: true
requiredInputs:
  - ecrRepositoryPrefix
  - upstreamRegistryUrl
stateInputs:
  description: |
    Input properties used for looking up and filtering PullThroughCacheRule resources.
  properties:
    ecrRepositoryPrefix:
      type: string
      description: |
        The repository name prefix to use when caching images from the source registry.
      willReplaceOnChanges: true
    registryId:
      type: string
      description: |
        The registry ID where the repository was created.
    upstreamRegistryUrl:
      type: string
      description: |
        The registry URL of the upstream public registry to use as the source.
      willReplaceOnChanges: true
  type: object
