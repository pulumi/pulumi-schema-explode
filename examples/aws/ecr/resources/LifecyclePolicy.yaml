$token: aws:ecr/lifecyclePolicy:LifecyclePolicy
properties:
  policy:
    type: string
    description: |
      The policy document. This is a JSON formatted string. See more details about [Policy Parameters](http://docs.aws.amazon.com/AmazonECR/latest/userguide/LifecyclePolicies.html#lifecycle_policy_parameters) in the official AWS docs.
  registryId:
    type: string
    description: |
      The registry ID where the repository was created.
  repository:
    type: string
    description: |
      Name of the repository to apply the policy.
required:
  - policy
  - registryId
  - repository
inputProperties:
  policy:
    type: string
    oneOf:
      - type: string
      - type: string
        $ref: "#/types/aws:ecr/lifecyclePolicyDocument:LifecyclePolicyDocument"
    description: |
      The policy document. This is a JSON formatted string. See more details about [Policy Parameters](http://docs.aws.amazon.com/AmazonECR/latest/userguide/LifecyclePolicies.html#lifecycle_policy_parameters) in the official AWS docs.
    willReplaceOnChanges: true
  repository:
    type: string
    description: |
      Name of the repository to apply the policy.
    willReplaceOnChanges: true
requiredInputs:
  - policy
  - repository
stateInputs:
  description: |
    Input properties used for looking up and filtering LifecyclePolicy resources.
  properties:
    policy:
      type: string
      oneOf:
        - type: string
        - type: string
          $ref: "#/types/aws:ecr/lifecyclePolicyDocument:LifecyclePolicyDocument"
      description: |
        The policy document. This is a JSON formatted string. See more details about [Policy Parameters](http://docs.aws.amazon.com/AmazonECR/latest/userguide/LifecyclePolicies.html#lifecycle_policy_parameters) in the official AWS docs.
      willReplaceOnChanges: true
    registryId:
      type: string
      description: |
        The registry ID where the repository was created.
    repository:
      type: string
      description: |
        Name of the repository to apply the policy.
      willReplaceOnChanges: true
  type: object
