$token: aws:ecr/RepositoryEncryptionConfiguration:RepositoryEncryptionConfiguration
properties:
  encryptionType:
    type: string
    description: |
      The encryption type to use for the repository. Valid values are `AES256` or `KMS`. Defaults to `AES256`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  kmsKey:
    type: string
    description: |
      The ARN of the KMS key to use when `encryption_type` is `KMS`. If not specified, uses the default AWS managed key for ECR.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
language:
  nodejs:
    requiredOutputs:
      - kmsKey
