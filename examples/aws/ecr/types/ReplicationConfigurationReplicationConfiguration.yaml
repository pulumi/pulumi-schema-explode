$token: aws:ecr/ReplicationConfigurationReplicationConfiguration:ReplicationConfigurationReplicationConfiguration
properties:
  rules:
    type: array
    items:
      $ref: "#/types/aws:ecr/ReplicationConfigurationReplicationConfigurationRule:ReplicationConfigurationReplicationConfigurationRule"
    description: |
      The replication rules for a replication configuration. A maximum of 10 are allowed per `replication_configuration`. See Rule
    language:
      python:
        mapCase: false
type: object
required:
  - rules
