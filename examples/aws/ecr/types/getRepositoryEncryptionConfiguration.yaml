$token: aws:ecr/getRepositoryEncryptionConfiguration:getRepositoryEncryptionConfiguration
properties:
  encryptionType:
    type: string
    description: |
      Encryption type to use for the repository, either `AES256` or `KMS`.
    language:
      python:
        mapCase: false
  kmsKey:
    type: string
    description: |
      If `encryption_type` is `KMS`, the ARN of the KMS key used.
    language:
      python:
        mapCase: false
type: object
required:
  - encryptionType
  - kmsKey
language:
  nodejs:
    requiredInputs: []
