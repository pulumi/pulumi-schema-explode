$token: aws:ecr/RepositoryImageScanningConfiguration:RepositoryImageScanningConfiguration
properties:
  scanOnPush:
    type: boolean
    description: |
      Indicates whether images are scanned after being pushed to the repository (true) or not scanned (false).
    language:
      python:
        mapCase: false
type: object
required:
  - scanOnPush
