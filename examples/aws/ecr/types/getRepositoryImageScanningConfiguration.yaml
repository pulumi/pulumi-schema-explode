$token: aws:ecr/getRepositoryImageScanningConfiguration:getRepositoryImageScanningConfiguration
properties:
  scanOnPush:
    type: boolean
    description: |
      Whether images are scanned after being pushed to the repository.
    language:
      python:
        mapCase: false
type: object
required:
  - scanOnPush
language:
  nodejs:
    requiredInputs: []
