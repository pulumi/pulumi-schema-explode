$token: aws:ecr/ReplicationConfigurationReplicationConfigurationRuleRepositoryFilter:ReplicationConfigurationReplicationConfigurationRuleRepositoryFilter
properties:
  filter:
    type: string
    description: |
      The repository filter details.
    language:
      python:
        mapCase: false
  filterType:
    type: string
    description: |
      The repository filter type. The only supported value is `PREFIX_MATCH`, which is a repository name prefix specified with the filter parameter.
    language:
      python:
        mapCase: false
type: object
required:
  - filter
  - filterType
