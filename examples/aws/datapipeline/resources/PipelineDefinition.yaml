$token: aws:datapipeline/pipelineDefinition:PipelineDefinition
properties:
  parameterObjects:
    type: array
    items:
      $ref: "#/types/aws:datapipeline/PipelineDefinitionParameterObject:PipelineDefinitionParameterObject"
    description: |
      Configuration block for the parameter objects used in the pipeline definition. See below
  parameterValues:
    type: array
    items:
      $ref: "#/types/aws:datapipeline/PipelineDefinitionParameterValue:PipelineDefinitionParameterValue"
    description: |
      Configuration block for the parameter values used in the pipeline definition. See below
  pipelineId:
    type: string
    description: |
      ID of the pipeline.
  pipelineObjects:
    type: array
    items:
      $ref: "#/types/aws:datapipeline/PipelineDefinitionPipelineObject:PipelineDefinitionPipelineObject"
    description: |
      Configuration block for the objects that define the pipeline. See below
required:
  - pipelineId
  - pipelineObjects
inputProperties:
  parameterObjects:
    type: array
    items:
      $ref: "#/types/aws:datapipeline/PipelineDefinitionParameterObject:PipelineDefinitionParameterObject"
    description: |
      Configuration block for the parameter objects used in the pipeline definition. See below
  parameterValues:
    type: array
    items:
      $ref: "#/types/aws:datapipeline/PipelineDefinitionParameterValue:PipelineDefinitionParameterValue"
    description: |
      Configuration block for the parameter values used in the pipeline definition. See below
  pipelineId:
    type: string
    description: |
      ID of the pipeline.
    willReplaceOnChanges: true
  pipelineObjects:
    type: array
    items:
      $ref: "#/types/aws:datapipeline/PipelineDefinitionPipelineObject:PipelineDefinitionPipelineObject"
    description: |
      Configuration block for the objects that define the pipeline. See below
requiredInputs:
  - pipelineId
  - pipelineObjects
stateInputs:
  description: |
    Input properties used for looking up and filtering PipelineDefinition resources.
  properties:
    parameterObjects:
      type: array
      items:
        $ref: "#/types/aws:datapipeline/PipelineDefinitionParameterObject:PipelineDefinitionParameterObject"
      description: |
        Configuration block for the parameter objects used in the pipeline definition. See below
    parameterValues:
      type: array
      items:
        $ref: "#/types/aws:datapipeline/PipelineDefinitionParameterValue:PipelineDefinitionParameterValue"
      description: |
        Configuration block for the parameter values used in the pipeline definition. See below
    pipelineId:
      type: string
      description: |
        ID of the pipeline.
      willReplaceOnChanges: true
    pipelineObjects:
      type: array
      items:
        $ref: "#/types/aws:datapipeline/PipelineDefinitionPipelineObject:PipelineDefinitionPipelineObject"
      description: |
        Configuration block for the objects that define the pipeline. See below
  type: object
