$token: aws:athena/database:Database
properties:
  aclConfiguration:
    $ref: "#/types/aws:athena/DatabaseAclConfiguration:DatabaseAclConfiguration"
    description: |
      That an Amazon S3 canned ACL should be set to control ownership of stored query results. See ACL Configuration below.
  bucket:
    type: string
    description: |
      Name of S3 bucket to save the results of the query execution.
  comment:
    type: string
    description: |
      Description of the database.
  encryptionConfiguration:
    $ref: "#/types/aws:athena/DatabaseEncryptionConfiguration:DatabaseEncryptionConfiguration"
    description: |
      Encryption key block AWS Athena uses to decrypt the data in S3, such as an AWS Key Management Service (AWS KMS) key. See Encryption Configuration below.
  expectedBucketOwner:
    type: string
    description: |
      AWS account ID that you expect to be the owner of the Amazon S3 bucket.
  forceDestroy:
    type: boolean
    description: |
      Boolean that indicates all tables should be deleted from the database so that the database can be destroyed without error. The tables are *not* recoverable.
  name:
    type: string
    description: |
      Name of the database to create.
  properties:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of custom metadata properties for the database definition.
required:
  - name
inputProperties:
  aclConfiguration:
    $ref: "#/types/aws:athena/DatabaseAclConfiguration:DatabaseAclConfiguration"
    description: |
      That an Amazon S3 canned ACL should be set to control ownership of stored query results. See ACL Configuration below.
    willReplaceOnChanges: true
  bucket:
    type: string
    description: |
      Name of S3 bucket to save the results of the query execution.
  comment:
    type: string
    description: |
      Description of the database.
    willReplaceOnChanges: true
  encryptionConfiguration:
    $ref: "#/types/aws:athena/DatabaseEncryptionConfiguration:DatabaseEncryptionConfiguration"
    description: |
      Encryption key block AWS Athena uses to decrypt the data in S3, such as an AWS Key Management Service (AWS KMS) key. See Encryption Configuration below.
    willReplaceOnChanges: true
  expectedBucketOwner:
    type: string
    description: |
      AWS account ID that you expect to be the owner of the Amazon S3 bucket.
    willReplaceOnChanges: true
  forceDestroy:
    type: boolean
    description: |
      Boolean that indicates all tables should be deleted from the database so that the database can be destroyed without error. The tables are *not* recoverable.
  name:
    type: string
    description: |
      Name of the database to create.
    willReplaceOnChanges: true
  properties:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of custom metadata properties for the database definition.
    willReplaceOnChanges: true
stateInputs:
  description: |
    Input properties used for looking up and filtering Database resources.
  properties:
    aclConfiguration:
      $ref: "#/types/aws:athena/DatabaseAclConfiguration:DatabaseAclConfiguration"
      description: |
        That an Amazon S3 canned ACL should be set to control ownership of stored query results. See ACL Configuration below.
      willReplaceOnChanges: true
    bucket:
      type: string
      description: |
        Name of S3 bucket to save the results of the query execution.
    comment:
      type: string
      description: |
        Description of the database.
      willReplaceOnChanges: true
    encryptionConfiguration:
      $ref: "#/types/aws:athena/DatabaseEncryptionConfiguration:DatabaseEncryptionConfiguration"
      description: |
        Encryption key block AWS Athena uses to decrypt the data in S3, such as an AWS Key Management Service (AWS KMS) key. See Encryption Configuration below.
      willReplaceOnChanges: true
    expectedBucketOwner:
      type: string
      description: |
        AWS account ID that you expect to be the owner of the Amazon S3 bucket.
      willReplaceOnChanges: true
    forceDestroy:
      type: boolean
      description: |
        Boolean that indicates all tables should be deleted from the database so that the database can be destroyed without error. The tables are *not* recoverable.
    name:
      type: string
      description: |
        Name of the database to create.
      willReplaceOnChanges: true
    properties:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of custom metadata properties for the database definition.
      willReplaceOnChanges: true
  type: object
