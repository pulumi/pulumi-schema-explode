$token: aws:athena/DatabaseAclConfiguration:DatabaseAclConfiguration
properties:
  s3AclOption:
    type: string
    description: |
      Amazon S3 canned ACL that Athena should specify when storing query results. Valid value is `BUCKET_OWNER_FULL_CONTROL`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - s3AclOption
