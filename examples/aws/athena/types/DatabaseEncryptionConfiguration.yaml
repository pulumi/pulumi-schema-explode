$token: aws:athena/DatabaseEncryptionConfiguration:DatabaseEncryptionConfiguration
properties:
  encryptionOption:
    type: string
    description: |
      Type of key; one of `SSE_S3`, `SSE_KMS`, `CSE_KMS`
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  kmsKey:
    type: string
    description: |
      KMS key ARN or ID; required for key types `SSE_KMS` and `CSE_KMS`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - encryptionOption
