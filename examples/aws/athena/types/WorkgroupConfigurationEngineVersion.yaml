$token: aws:athena/WorkgroupConfigurationEngineVersion:WorkgroupConfigurationEngineVersion
properties:
  effectiveEngineVersion:
    type: string
    description: |
      The engine version on which the query runs. If `selected_engine_version` is set to `AUTO`, the effective engine version is chosen by Athena.
    language:
      python:
        mapCase: false
  selectedEngineVersion:
    type: string
    description: |
      Requested engine version. Defaults to `AUTO`.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - effectiveEngineVersion
