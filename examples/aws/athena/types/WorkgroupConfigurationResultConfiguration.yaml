$token: aws:athena/WorkgroupConfigurationResultConfiguration:WorkgroupConfigurationResultConfiguration
properties:
  aclConfiguration:
    $ref: "#/types/aws:athena/WorkgroupConfigurationResultConfigurationAclConfiguration:WorkgroupConfigurationResultConfigurationAclConfiguration"
    description: |
      That an Amazon S3 canned ACL should be set to control ownership of stored query results. See ACL Configuration below.
    language:
      python:
        mapCase: false
  encryptionConfiguration:
    $ref: "#/types/aws:athena/WorkgroupConfigurationResultConfigurationEncryptionConfiguration:WorkgroupConfigurationResultConfigurationEncryptionConfiguration"
    description: |
      Configuration block with encryption settings. See Encryption Configuration below.
    language:
      python:
        mapCase: false
  expectedBucketOwner:
    type: string
    description: |
      AWS account ID that you expect to be the owner of the Amazon S3 bucket.
    language:
      python:
        mapCase: false
  outputLocation:
    type: string
    description: |
      Location in Amazon S3 where your query results are stored, such as `s3://path/to/query/bucket/`. For more information, see [Queries and Query Result Files](https://docs.aws.amazon.com/athena/latest/ug/querying.html).
    language:
      python:
        mapCase: false
type: object
