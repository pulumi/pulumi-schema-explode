$token: aws:globalaccelerator/ListenerPortRange:ListenerPortRange
properties:
  fromPort:
    type: integer
    description: |
      The first port in the range of ports, inclusive.
    language:
      python:
        mapCase: false
  toPort:
    type: integer
    description: |
      The last port in the range of ports, inclusive.
    language:
      python:
        mapCase: false
type: object
