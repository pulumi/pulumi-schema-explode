$token: aws:globalaccelerator/AcceleratorAttributes:AcceleratorAttributes
properties:
  flowLogsEnabled:
    type: boolean
    description: |
      Indicates whether flow logs are enabled. Defaults to `false`. Valid values: `true`, `false`.
    language:
      python:
        mapCase: false
  flowLogsS3Bucket:
    type: string
    description: |
      The name of the Amazon S3 bucket for the flow logs. Required if `flow_logs_enabled` is `true`.
    language:
      python:
        mapCase: false
  flowLogsS3Prefix:
    type: string
    description: |
      The prefix for the location in the Amazon S3 bucket for the flow logs. Required if `flow_logs_enabled` is `true`.
    language:
      python:
        mapCase: false
type: object
