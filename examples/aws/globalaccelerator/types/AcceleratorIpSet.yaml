$token: aws:globalaccelerator/AcceleratorIpSet:AcceleratorIpSet
properties:
  ipAddresses:
    type: array
    items:
      type: string
    description: |
      A list of IP addresses in the IP address set.
    language:
      python:
        mapCase: false
  ipFamily:
    type: string
    description: |
      The type of IP addresses included in this IP set.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - ipAddresses
      - ipFamily
