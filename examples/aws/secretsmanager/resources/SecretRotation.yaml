$token: aws:secretsmanager/secretRotation:SecretRotation
properties:
  rotationEnabled:
    type: boolean
    description: |
      Specifies whether automatic rotation is enabled for this secret.
  rotationLambdaArn:
    type: string
    description: |
      Specifies the ARN of the Lambda function that can rotate the secret.
  rotationRules:
    $ref: "#/types/aws:secretsmanager/SecretRotationRotationRules:SecretRotationRotationRules"
    description: |
      A structure that defines the rotation configuration for this secret. Defined below.
  secretId:
    type: string
    description: |
      Specifies the secret to which you want to add a new version. You can specify either the Amazon Resource Name (ARN) or the friendly name of the secret. The secret must already exist.
  tags:
    type: object
    additionalProperties:
      type: string
required:
  - rotationEnabled
  - rotationLambdaArn
  - rotationRules
  - secretId
inputProperties:
  rotationLambdaArn:
    type: string
    description: |
      Specifies the ARN of the Lambda function that can rotate the secret.
  rotationRules:
    $ref: "#/types/aws:secretsmanager/SecretRotationRotationRules:SecretRotationRotationRules"
    description: |
      A structure that defines the rotation configuration for this secret. Defined below.
  secretId:
    type: string
    description: |
      Specifies the secret to which you want to add a new version. You can specify either the Amazon Resource Name (ARN) or the friendly name of the secret. The secret must already exist.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
requiredInputs:
  - rotationLambdaArn
  - rotationRules
  - secretId
stateInputs:
  description: |
    Input properties used for looking up and filtering SecretRotation resources.
  properties:
    rotationEnabled:
      type: boolean
      description: |
        Specifies whether automatic rotation is enabled for this secret.
    rotationLambdaArn:
      type: string
      description: |
        Specifies the ARN of the Lambda function that can rotate the secret.
    rotationRules:
      $ref: "#/types/aws:secretsmanager/SecretRotationRotationRules:SecretRotationRotationRules"
      description: |
        A structure that defines the rotation configuration for this secret. Defined below.
    secretId:
      type: string
      description: |
        Specifies the secret to which you want to add a new version. You can specify either the Amazon Resource Name (ARN) or the friendly name of the secret. The secret must already exist.
      willReplaceOnChanges: true
    tags:
      type: object
      additionalProperties:
        type: string
  type: object
