$token: aws:secretsmanager/getSecrets:getSecrets
inputs:
  description: |
    A collection of arguments for invoking getSecrets.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:secretsmanager/getSecretsFilter:getSecretsFilter"
      description: |
        Configuration block(s) for filtering. Detailed below.
  type: object
outputs:
  description: |
    A collection of values returned by getSecrets.
  properties:
    arns:
      type: array
      items:
        type: string
      description: |
        Set of ARNs of the matched Secrets Manager secrets.
    filters:
      type: array
      items:
        $ref: "#/types/aws:secretsmanager/getSecretsFilter:getSecretsFilter"
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    names:
      type: array
      items:
        type: string
      description: |
        Set of names of the matched Secrets Manager secrets.
  type: object
  required:
    - arns
    - names
    - id
