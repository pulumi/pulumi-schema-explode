$token: aws:secretsmanager/getSecretRotation:getSecretRotation
inputs:
  description: |
    A collection of arguments for invoking getSecretRotation.
  properties:
    secretId:
      type: string
      description: |
        Specifies the secret containing the version that you want to retrieve. You can specify either the ARN or the friendly name of the secret.
  type: object
  required:
    - secretId
outputs:
  description: |
    A collection of values returned by getSecretRotation.
  properties:
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    rotationEnabled:
      type: boolean
      description: |
        ARN of the secret.
    rotationLambdaArn:
      type: string
      description: |
        Decrypted part of the protected secret information that was originally provided as a string.
    rotationRules:
      type: array
      items:
        $ref: "#/types/aws:secretsmanager/getSecretRotationRotationRule:getSecretRotationRotationRule"
      description: |
        Decrypted part of the protected secret information that was originally provided as a binary. Base64 encoded.
    secretId:
      type: string
  type: object
  required:
    - rotationEnabled
    - rotationLambdaArn
    - rotationRules
    - secretId
    - id
