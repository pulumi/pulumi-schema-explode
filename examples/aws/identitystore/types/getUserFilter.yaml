$token: aws:identitystore/getUserFilter:getUserFilter
properties:
  attributePath:
    type: string
    description: |
      Attribute path that is used to specify which attribute name to search. Currently, `UserName` is the only valid attribute path.
    language:
      python:
        mapCase: false
  attributeValue:
    type: string
    description: |
      Value for an attribute.
    language:
      python:
        mapCase: false
type: object
required:
  - attributePath
  - attributeValue
