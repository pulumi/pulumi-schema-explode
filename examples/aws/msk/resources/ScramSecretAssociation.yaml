$token: aws:msk/scramSecretAssociation:ScramSecretAssociation
properties:
  clusterArn:
    type: string
    description: |
      Amazon Resource Name (ARN) of the MSK cluster.
  secretArnLists:
    type: array
    items:
      type: string
    description: |
      List of AWS Secrets Manager secret ARNs.
required:
  - clusterArn
  - secretArnLists
inputProperties:
  clusterArn:
    type: string
    description: |
      Amazon Resource Name (ARN) of the MSK cluster.
    willReplaceOnChanges: true
  secretArnLists:
    type: array
    items:
      type: string
    description: |
      List of AWS Secrets Manager secret ARNs.
requiredInputs:
  - clusterArn
  - secretArnLists
stateInputs:
  description: |
    Input properties used for looking up and filtering ScramSecretAssociation resources.
  properties:
    clusterArn:
      type: string
      description: |
        Amazon Resource Name (ARN) of the MSK cluster.
      willReplaceOnChanges: true
    secretArnLists:
      type: array
      items:
        type: string
      description: |
        List of AWS Secrets Manager secret ARNs.
  type: object
