$token: aws:msk/serverlessCluster:ServerlessCluster
properties:
  arn:
    type: string
    description: |
      The ARN of the serverless cluster.
  clientAuthentication:
    $ref: "#/types/aws:msk/ServerlessClusterClientAuthentication:ServerlessClusterClientAuthentication"
    description: |
      Specifies client authentication information for the serverless cluster. See below.
  clusterName:
    type: string
    description: |
      The name of the serverless cluster.
  tags:
    type: object
    additionalProperties:
      type: string
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  vpcConfigs:
    type: array
    items:
      $ref: "#/types/aws:msk/ServerlessClusterVpcConfig:ServerlessClusterVpcConfig"
    description: |
      VPC configuration information. See below.
required:
  - arn
  - clientAuthentication
  - clusterName
  - tagsAll
  - vpcConfigs
inputProperties:
  clientAuthentication:
    $ref: "#/types/aws:msk/ServerlessClusterClientAuthentication:ServerlessClusterClientAuthentication"
    description: |
      Specifies client authentication information for the serverless cluster. See below.
    willReplaceOnChanges: true
  clusterName:
    type: string
    description: |
      The name of the serverless cluster.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
  vpcConfigs:
    type: array
    items:
      $ref: "#/types/aws:msk/ServerlessClusterVpcConfig:ServerlessClusterVpcConfig"
    description: |
      VPC configuration information. See below.
    willReplaceOnChanges: true
requiredInputs:
  - clientAuthentication
  - vpcConfigs
stateInputs:
  description: |
    Input properties used for looking up and filtering ServerlessCluster resources.
  properties:
    arn:
      type: string
      description: |
        The ARN of the serverless cluster.
    clientAuthentication:
      $ref: "#/types/aws:msk/ServerlessClusterClientAuthentication:ServerlessClusterClientAuthentication"
      description: |
        Specifies client authentication information for the serverless cluster. See below.
      willReplaceOnChanges: true
    clusterName:
      type: string
      description: |
        The name of the serverless cluster.
      willReplaceOnChanges: true
    tags:
      type: object
      additionalProperties:
        type: string
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    vpcConfigs:
      type: array
      items:
        $ref: "#/types/aws:msk/ServerlessClusterVpcConfig:ServerlessClusterVpcConfig"
      description: |
        VPC configuration information. See below.
      willReplaceOnChanges: true
  type: object
