$token: aws:msk/ClusterEncryptionInfoEncryptionInTransit:ClusterEncryptionInfoEncryptionInTransit
properties:
  clientBroker:
    type: string
    description: |
      Encryption setting for data in transit between clients and brokers. Valid values: `TLS`, `TLS_PLAINTEXT`, and `PLAINTEXT`. Default value is `TLS`.
    language:
      python:
        mapCase: false
  inCluster:
    type: boolean
    description: |
      Whether data communication among broker nodes is encrypted. Default value: `true`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
