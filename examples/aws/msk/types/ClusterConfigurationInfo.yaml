$token: aws:msk/ClusterConfigurationInfo:ClusterConfigurationInfo
properties:
  arn:
    type: string
    description: |
      Amazon Resource Name (ARN) of the MSK Configuration to use in the cluster.
    language:
      python:
        mapCase: false
  revision:
    type: integer
    description: |
      Revision of the MSK Configuration to use in the cluster.
    language:
      python:
        mapCase: false
type: object
required:
  - arn
  - revision
