$token: aws:msk/ClusterOpenMonitoring:ClusterOpenMonitoring
properties:
  prometheus:
    $ref: "#/types/aws:msk/ClusterOpenMonitoringPrometheus:ClusterOpenMonitoringPrometheus"
    description: |
      Configuration block for Prometheus settings for open monitoring. See below.
    language:
      python:
        mapCase: false
type: object
required:
  - prometheus
