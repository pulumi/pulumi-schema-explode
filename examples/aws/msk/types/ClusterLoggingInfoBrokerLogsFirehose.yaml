$token: aws:msk/ClusterLoggingInfoBrokerLogsFirehose:ClusterLoggingInfoBrokerLogsFirehose
properties:
  deliveryStream:
    type: string
    description: |
      Name of the Kinesis Data Firehose delivery stream to deliver logs to.
    language:
      python:
        mapCase: false
  enabled:
    type: boolean
    description: |
      Controls whether provisioned throughput is enabled or not. Default value: `false`.
    language:
      python:
        mapCase: false
type: object
required:
  - enabled
