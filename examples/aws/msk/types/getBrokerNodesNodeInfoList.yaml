$token: aws:msk/getBrokerNodesNodeInfoList:getBrokerNodesNodeInfoList
properties:
  attachedEniId:
    type: string
    description: |
      Attached elastic network interface of the broker
    language:
      python:
        mapCase: false
  brokerId:
    type: number
    description: |
      ID of the broker
    language:
      python:
        mapCase: false
  clientSubnet:
    type: string
    description: |
      Client subnet to which this broker node belongs
    language:
      python:
        mapCase: false
  clientVpcIpAddress:
    type: string
    description: |
      The client virtual private cloud (VPC) IP address
    language:
      python:
        mapCase: false
  endpoints:
    type: array
    items:
      type: string
    description: |
      Set of endpoints for accessing the broker. This does not include ports
    language:
      python:
        mapCase: false
  nodeArn:
    type: string
    description: |
      ARN of the node
    language:
      python:
        mapCase: false
type: object
required:
  - attachedEniId
  - brokerId
  - clientSubnet
  - clientVpcIpAddress
  - endpoints
  - nodeArn
language:
  nodejs:
    requiredInputs: []
