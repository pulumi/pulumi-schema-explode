$token: aws:msk/ClusterBrokerNodeGroupInfo:ClusterBrokerNodeGroupInfo
properties:
  azDistribution:
    type: string
    description: |
      The distribution of broker nodes across availability zones ([documentation](https://docs.aws.amazon.com/msk/1.0/apireference/clusters.html#clusters-model-brokerazdistribution)). Currently the only valid value is `DEFAULT`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  clientSubnets:
    type: array
    items:
      type: string
    description: |
      A list of subnets to connect to in client VPC ([documentation](https://docs.aws.amazon.com/msk/1.0/apireference/clusters.html#clusters-prop-brokernodegroupinfo-clientsubnets)).
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  connectivityInfo:
    $ref: "#/types/aws:msk/ClusterBrokerNodeGroupInfoConnectivityInfo:ClusterBrokerNodeGroupInfoConnectivityInfo"
    description: |
      Information about the cluster access configuration. See below. For security reasons, you can't turn on public access while creating an MSK cluster. However, you can update an existing cluster to make it publicly accessible. You can also create a new cluster and then update it to make it publicly accessible ([documentation](https://docs.aws.amazon.com/msk/latest/developerguide/public-access.html)).
    language:
      python:
        mapCase: false
  ebsVolumeSize:
    type: integer
    description: |
      The size in GiB of the EBS volume for the data drive on each broker node.
    deprecationMessage: use 'storage_info' argument instead
    language:
      python:
        mapCase: false
  instanceType:
    type: string
    description: |
      Specify the instance type to use for the kafka brokersE.g., kafka.m5.large. ([Pricing info](https://aws.amazon.com/msk/pricing/))
    language:
      python:
        mapCase: false
  securityGroups:
    type: array
    items:
      type: string
    description: |
      A list of the security groups to associate with the elastic network interfaces to control who can communicate with the cluster.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  storageInfo:
    $ref: "#/types/aws:msk/ClusterBrokerNodeGroupInfoStorageInfo:ClusterBrokerNodeGroupInfoStorageInfo"
    description: |
      A block that contains information about storage volumes attached to MSK broker nodes. See below.
    language:
      python:
        mapCase: false
type: object
required:
  - clientSubnets
  - instanceType
  - securityGroups
language:
  nodejs:
    requiredOutputs:
      - clientSubnets
      - connectivityInfo
      - ebsVolumeSize
      - instanceType
      - securityGroups
      - storageInfo
