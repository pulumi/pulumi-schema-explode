$token: aws:msk/ClusterClientAuthenticationSasl:ClusterClientAuthenticationSasl
properties:
  iam:
    type: boolean
    description: |
      Enables IAM client authentication. Defaults to `false`.
    language:
      python:
        mapCase: false
  scram:
    type: boolean
    description: |
      Enables SCRAM client authentication via AWS Secrets Manager. Defaults to `false`.
    language:
      python:
        mapCase: false
type: object
