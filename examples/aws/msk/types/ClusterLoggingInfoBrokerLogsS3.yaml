$token: aws:msk/ClusterLoggingInfoBrokerLogsS3:ClusterLoggingInfoBrokerLogsS3
properties:
  bucket:
    type: string
    description: |
      Name of the S3 bucket to deliver logs to.
    language:
      python:
        mapCase: false
  enabled:
    type: boolean
    description: |
      Controls whether provisioned throughput is enabled or not. Default value: `false`.
    language:
      python:
        mapCase: false
  prefix:
    type: string
    description: |
      Prefix to append to the folder name.
    language:
      python:
        mapCase: false
type: object
required:
  - enabled
