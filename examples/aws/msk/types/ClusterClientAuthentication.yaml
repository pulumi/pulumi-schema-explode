$token: aws:msk/ClusterClientAuthentication:ClusterClientAuthentication
properties:
  sasl:
    $ref: "#/types/aws:msk/ClusterClientAuthenticationSasl:ClusterClientAuthenticationSasl"
    description: |
      Configuration block for specifying SASL client authentication. See below.
    language:
      python:
        mapCase: false
  tls:
    $ref: "#/types/aws:msk/ClusterClientAuthenticationTls:ClusterClientAuthenticationTls"
    description: |
      Configuration block for specifying TLS client authentication. See below.
    language:
      python:
        mapCase: false
  unauthenticated:
    type: boolean
    description: |
      Enables unauthenticated access.
    language:
      python:
        mapCase: false
type: object
