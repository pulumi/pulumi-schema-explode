$token: aws:msk/ServerlessClusterVpcConfig:ServerlessClusterVpcConfig
properties:
  securityGroupIds:
    type: array
    items:
      type: string
    description: |
      Specifies up to five security groups that control inbound and outbound traffic for the serverless cluster.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  subnetIds:
    type: array
    items:
      type: string
    description: |
      A list of subnets in at least two different Availability Zones that host your client applications.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - subnetIds
