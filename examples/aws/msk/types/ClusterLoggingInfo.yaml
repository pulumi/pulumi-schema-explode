$token: aws:msk/ClusterLoggingInfo:ClusterLoggingInfo
properties:
  brokerLogs:
    $ref: "#/types/aws:msk/ClusterLoggingInfoBrokerLogs:ClusterLoggingInfoBrokerLogs"
    description: |
      Configuration block for Broker Logs settings for logging info. See below.
    language:
      python:
        mapCase: false
type: object
required:
  - brokerLogs
