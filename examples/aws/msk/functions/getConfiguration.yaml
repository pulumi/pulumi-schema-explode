$token: aws:msk/getConfiguration:getConfiguration
inputs:
  description: |
    A collection of arguments for invoking getConfiguration.
  properties:
    name:
      type: string
      description: |
        Name of the configuration.
  type: object
  required:
    - name
outputs:
  description: |
    A collection of values returned by getConfiguration.
  properties:
    arn:
      type: string
      description: |
        ARN of the configuration.
    description:
      type: string
      description: |
        Description of the configuration.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    kafkaVersions:
      type: array
      items:
        type: string
      description: |
        List of Apache Kafka versions which can use this configuration.
    latestRevision:
      type: integer
      description: |
        Latest revision of the configuration.
    name:
      type: string
    serverProperties:
      type: string
      description: |
        Contents of the server.properties file.
  type: object
  required:
    - arn
    - description
    - kafkaVersions
    - latestRevision
    - name
    - serverProperties
    - id
