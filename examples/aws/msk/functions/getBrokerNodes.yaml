$token: aws:msk/getBrokerNodes:getBrokerNodes
inputs:
  description: |
    A collection of arguments for invoking getBrokerNodes.
  properties:
    clusterArn:
      type: string
      description: |
        ARN of the cluster the nodes belong to.
  type: object
  required:
    - clusterArn
outputs:
  description: |
    A collection of values returned by getBrokerNodes.
  properties:
    clusterArn:
      type: string
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    nodeInfoLists:
      type: array
      items:
        $ref: "#/types/aws:msk/getBrokerNodesNodeInfoList:getBrokerNodesNodeInfoList"
  type: object
  required:
    - clusterArn
    - nodeInfoLists
    - id
