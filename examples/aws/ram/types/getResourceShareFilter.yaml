$token: aws:ram/getResourceShareFilter:getResourceShareFilter
properties:
  name:
    type: string
    description: |
      Name of the tag key to filter on.
    language:
      python:
        mapCase: false
  values:
    type: array
    items:
      type: string
    description: |
      Value of the tag key.
    language:
      python:
        mapCase: false
type: object
required:
  - name
  - values
