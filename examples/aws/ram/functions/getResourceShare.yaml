$token: aws:ram/getResourceShare:getResourceShare
inputs:
  description: |
    A collection of arguments for invoking getResourceShare.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ram/getResourceShareFilter:getResourceShareFilter"
      description: |
        Filter used to scope the list e.g., by tags. See [related docs] (https://docs.aws.amazon.com/ram/latest/APIReference/API_TagFilter.html).
    name:
      type: string
      description: |
        Name of the tag key to filter on.
    resourceOwner:
      type: string
      description: |
        Owner of the resource share. Valid values are `SELF` or `OTHER-ACCOUNTS`.
    resourceShareStatus:
      type: string
      description: |
        Specifies that you want to retrieve details of only those resource shares that have this status. Valid values are `PENDING`, `ACTIVE`, `FAILED`, `DELETING`, and `DELETED`.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Tags attached to the RAM share
  type: object
  required:
    - name
    - resourceOwner
outputs:
  description: |
    A collection of values returned by getResourceShare.
  properties:
    arn:
      type: string
      description: |
        ARN of the resource share.
    filters:
      type: array
      items:
        $ref: "#/types/aws:ram/getResourceShareFilter:getResourceShareFilter"
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    name:
      type: string
    owningAccountId:
      type: string
      description: |
        ID of the AWS account that owns the resource share.
    resourceOwner:
      type: string
    resourceShareStatus:
      type: string
    status:
      type: string
      description: |
        Status of the RAM share.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Tags attached to the RAM share
  type: object
  required:
    - arn
    - name
    - owningAccountId
    - resourceOwner
    - status
    - tags
    - id
