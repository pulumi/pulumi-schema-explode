$token: aws:resourcegroupstaggingapi/getResourcesTagFilter:getResourcesTagFilter
properties:
  key:
    type: string
    description: |
      One part of a key-value pair that makes up a tag.
    language:
      python:
        mapCase: false
  values:
    type: array
    items:
      type: string
    description: |
      Optional part of a key-value pair that make up a tag.
    language:
      python:
        mapCase: false
type: object
required:
  - key
