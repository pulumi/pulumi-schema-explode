$token: aws:directconnect/getLocations:getLocations
outputs:
  description: |
    A collection of values returned by getLocations.
  properties:
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    locationCodes:
      type: array
      items:
        type: string
      description: |
        Code for the locations.
  type: object
  required:
    - locationCodes
    - id
