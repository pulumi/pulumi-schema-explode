$token: aws:directconnect/gateway:Gateway
properties:
  amazonSideAsn:
    type: string
    description: |
      The ASN to be configured on the Amazon side of the connection. The ASN must be in the private range of 64,512 to 65,534 or 4,200,000,000 to 4,294,967,294.
  name:
    type: string
    description: |
      The name of the connection.
  ownerAccountId:
    type: string
    description: |
      AWS Account ID of the gateway.
required:
  - amazonSideAsn
  - name
  - ownerAccountId
inputProperties:
  amazonSideAsn:
    type: string
    description: |
      The ASN to be configured on the Amazon side of the connection. The ASN must be in the private range of 64,512 to 65,534 or 4,200,000,000 to 4,294,967,294.
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      The name of the connection.
    willReplaceOnChanges: true
requiredInputs:
  - amazonSideAsn
stateInputs:
  description: |
    Input properties used for looking up and filtering Gateway resources.
  properties:
    amazonSideAsn:
      type: string
      description: |
        The ASN to be configured on the Amazon side of the connection. The ASN must be in the private range of 64,512 to 65,534 or 4,200,000,000 to 4,294,967,294.
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        The name of the connection.
      willReplaceOnChanges: true
    ownerAccountId:
      type: string
      description: |
        AWS Account ID of the gateway.
  type: object
