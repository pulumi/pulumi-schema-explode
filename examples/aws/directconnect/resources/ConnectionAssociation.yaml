$token: aws:directconnect/connectionAssociation:ConnectionAssociation
properties:
  connectionId:
    type: string
    description: |
      The ID of the connection.
  lagId:
    type: string
    description: |
      The ID of the LAG with which to associate the connection.
required:
  - connectionId
  - lagId
inputProperties:
  connectionId:
    type: string
    description: |
      The ID of the connection.
    willReplaceOnChanges: true
  lagId:
    type: string
    description: |
      The ID of the LAG with which to associate the connection.
    willReplaceOnChanges: true
requiredInputs:
  - connectionId
  - lagId
stateInputs:
  description: |
    Input properties used for looking up and filtering ConnectionAssociation resources.
  properties:
    connectionId:
      type: string
      description: |
        The ID of the connection.
      willReplaceOnChanges: true
    lagId:
      type: string
      description: |
        The ID of the LAG with which to associate the connection.
      willReplaceOnChanges: true
  type: object
