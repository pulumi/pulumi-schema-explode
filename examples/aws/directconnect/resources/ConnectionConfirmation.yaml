$token: aws:directconnect/connectionConfirmation:ConnectionConfirmation
properties:
  connectionId:
    type: string
    description: |
      The ID of the hosted connection.
required:
  - connectionId
inputProperties:
  connectionId:
    type: string
    description: |
      The ID of the hosted connection.
    willReplaceOnChanges: true
requiredInputs:
  - connectionId
stateInputs:
  description: |
    Input properties used for looking up and filtering ConnectionConfirmation resources.
  properties:
    connectionId:
      type: string
      description: |
        The ID of the hosted connection.
      willReplaceOnChanges: true
  type: object
