$token: aws:rds/proxyDefaultTargetGroup:ProxyDefaultTargetGroup
properties:
  arn:
    type: string
    description: |
      The Amazon Resource Name (ARN) representing the target group.
  connectionPoolConfig:
    $ref: "#/types/aws:rds/ProxyDefaultTargetGroupConnectionPoolConfig:ProxyDefaultTargetGroupConnectionPoolConfig"
    description: |
      The settings that determine the size and behavior of the connection pool for the target group.
  dbProxyName:
    type: string
    description: |
      Name of the RDS DB Proxy.
  name:
    type: string
    description: |
      The name of the default target group.
required:
  - arn
  - connectionPoolConfig
  - dbProxyName
  - name
inputProperties:
  connectionPoolConfig:
    $ref: "#/types/aws:rds/ProxyDefaultTargetGroupConnectionPoolConfig:ProxyDefaultTargetGroupConnectionPoolConfig"
    description: |
      The settings that determine the size and behavior of the connection pool for the target group.
  dbProxyName:
    type: string
    description: |
      Name of the RDS DB Proxy.
    willReplaceOnChanges: true
requiredInputs:
  - dbProxyName
stateInputs:
  description: |
    Input properties used for looking up and filtering ProxyDefaultTargetGroup resources.
  properties:
    arn:
      type: string
      description: |
        The Amazon Resource Name (ARN) representing the target group.
    connectionPoolConfig:
      $ref: "#/types/aws:rds/ProxyDefaultTargetGroupConnectionPoolConfig:ProxyDefaultTargetGroupConnectionPoolConfig"
      description: |
        The settings that determine the size and behavior of the connection pool for the target group.
    dbProxyName:
      type: string
      description: |
        Name of the RDS DB Proxy.
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        The name of the default target group.
  type: object
