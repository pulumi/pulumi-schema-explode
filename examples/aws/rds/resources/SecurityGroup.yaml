$token: aws:rds/securityGroup:SecurityGroup
properties:
  arn:
    type: string
    description: |
      The arn of the DB security group.
  description:
    type: string
    description: |
      The description of the DB security group. Defaults to "Managed by Pulumi".
    default: Managed by Pulumi
  ingress:
    type: array
    items:
      $ref: "#/types/aws:rds/SecurityGroupIngress:SecurityGroupIngress"
    description: |
      A list of ingress rules.
  name:
    type: string
    description: |
      The name of the DB security group.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - description
  - ingress
  - name
  - tagsAll
inputProperties:
  description:
    type: string
    description: |
      The description of the DB security group. Defaults to "Managed by Pulumi".
    default: Managed by Pulumi
    willReplaceOnChanges: true
  ingress:
    type: array
    items:
      $ref: "#/types/aws:rds/SecurityGroupIngress:SecurityGroupIngress"
    description: |
      A list of ingress rules.
  name:
    type: string
    description: |
      The name of the DB security group.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - ingress
stateInputs:
  description: |
    Input properties used for looking up and filtering SecurityGroup resources.
  properties:
    arn:
      type: string
      description: |
        The arn of the DB security group.
    description:
      type: string
      description: |
        The description of the DB security group. Defaults to "Managed by Pulumi".
      default: Managed by Pulumi
      willReplaceOnChanges: true
    ingress:
      type: array
      items:
        $ref: "#/types/aws:rds/SecurityGroupIngress:SecurityGroupIngress"
      description: |
        A list of ingress rules.
    name:
      type: string
      description: |
        The name of the DB security group.
      willReplaceOnChanges: true
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
