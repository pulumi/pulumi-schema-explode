$token: aws:rds/OptionGroupOption:OptionGroupOption
properties:
  dbSecurityGroupMemberships:
    type: array
    items:
      type: string
    description: |
      A list of DB Security Groups for which the option is enabled.
    language:
      python:
        mapCase: false
  optionName:
    type: string
    description: |
      The Name of the Option (e.g., MEMCACHED).
    language:
      python:
        mapCase: false
  optionSettings:
    type: array
    items:
      $ref: "#/types/aws:rds/OptionGroupOptionOptionSetting:OptionGroupOptionOptionSetting"
    description: |
      A list of option settings to apply.
    language:
      python:
        mapCase: false
  port:
    type: integer
    description: |
      The Port number when connecting to the Option (e.g., 11211).
    language:
      python:
        mapCase: false
  version:
    type: string
    description: |
      The version of the option (e.g., 13.1.0.0).
    language:
      python:
        mapCase: false
  vpcSecurityGroupMemberships:
    type: array
    items:
      type: string
    description: |
      A list of VPC Security Groups for which the option is enabled.
    language:
      python:
        mapCase: false
type: object
required:
  - optionName
