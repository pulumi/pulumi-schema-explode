$token: aws:rds/ClusterS3Import:ClusterS3Import
properties:
  bucketName:
    type: string
    description: |
      The bucket name where your backup is stored
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  bucketPrefix:
    type: string
    description: |
      Can be blank, but is the path to your backup
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  ingestionRole:
    type: string
    description: |
      Role applied to load the data.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  sourceEngine:
    type: string
    description: |
      Source engine for the backup
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  sourceEngineVersion:
    type: string
    description: |
      Version of the source engine used to make the backup
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - bucketName
  - ingestionRole
  - sourceEngine
  - sourceEngineVersion
