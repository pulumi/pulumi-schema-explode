$token: aws:storagegateway/getLocalDisk:getLocalDisk
inputs:
  description: |
    A collection of arguments for invoking getLocalDisk.
  properties:
    diskNode:
      type: string
      description: |
        Device node of the local disk to retrieve. For example, `/dev/sdb`.
    diskPath:
      type: string
      description: |
        Device path of the local disk to retrieve. For example, `/dev/xvdb` or `/dev/nvme1n1`.
    gatewayArn:
      type: string
      description: |
        ARN of the gateway.
  type: object
  required:
    - gatewayArn
outputs:
  description: |
    A collection of values returned by getLocalDisk.
  properties:
    diskId:
      type: string
      description: |
        Disk identifierE.g., `pci-0000:03:00.0-scsi-0:0:0:0`
    diskNode:
      type: string
    diskPath:
      type: string
    gatewayArn:
      type: string
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
  type: object
  required:
    - diskId
    - diskNode
    - diskPath
    - gatewayArn
    - id
