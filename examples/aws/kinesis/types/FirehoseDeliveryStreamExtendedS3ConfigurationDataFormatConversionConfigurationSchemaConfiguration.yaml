$token: aws:kinesis/FirehoseDeliveryStreamExtendedS3ConfigurationDataFormatConversionConfigurationSchemaConfiguration:FirehoseDeliveryStreamExtendedS3ConfigurationDataFormatConversionConfigurationSchemaConfiguration
properties:
  catalogId:
    type: string
    description: |
      The ID of the AWS Glue Data Catalog. If you don't supply this, the AWS account ID is used by default.
    language:
      python:
        mapCase: false
  databaseName:
    type: string
    description: |
      Specifies the name of the AWS Glue database that contains the schema for the output data.
    language:
      python:
        mapCase: false
  region:
    type: string
    description: |
      If you don't specify an AWS Region, the default is the current region.
    language:
      python:
        mapCase: false
  roleArn:
    type: string
    description: |
      The role that Kinesis Data Firehose can use to access AWS Glue. This role must be in the same account you use for Kinesis Data Firehose. Cross-account roles aren't allowed.
    language:
      python:
        mapCase: false
  tableName:
    type: string
    description: |
      Specifies the AWS Glue table that contains the column information that constitutes your data schema.
    language:
      python:
        mapCase: false
  versionId:
    type: string
    description: |
      Specifies the table version for the output data schema. Defaults to `LATEST`.
    language:
      python:
        mapCase: false
type: object
required:
  - databaseName
  - roleArn
  - tableName
language:
  nodejs:
    requiredOutputs:
      - catalogId
      - databaseName
      - region
      - roleArn
      - tableName
