$token: aws:kinesis/AnalyticsApplicationInputsSchema:AnalyticsApplicationInputsSchema
properties:
  recordColumns:
    type: array
    items:
      $ref: "#/types/aws:kinesis/AnalyticsApplicationInputsSchemaRecordColumn:AnalyticsApplicationInputsSchemaRecordColumn"
    description: |
      The Record Column mapping for the streaming source data element.
      See Record Columns below for more details.
    language:
      python:
        mapCase: false
  recordEncoding:
    type: string
    description: |
      The Encoding of the record in the streaming source.
    language:
      python:
        mapCase: false
  recordFormat:
    $ref: "#/types/aws:kinesis/AnalyticsApplicationInputsSchemaRecordFormat:AnalyticsApplicationInputsSchemaRecordFormat"
    description: |
      The Record Format and mapping information to schematize a record.
      See Record Format below for more details.
    language:
      python:
        mapCase: false
type: object
required:
  - recordColumns
  - recordFormat
