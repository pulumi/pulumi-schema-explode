$token: aws:kinesis/FirehoseDeliveryStreamHttpEndpointConfigurationProcessingConfiguration:FirehoseDeliveryStreamHttpEndpointConfigurationProcessingConfiguration
properties:
  enabled:
    type: boolean
    description: |
      Enables or disables data processing.
    language:
      python:
        mapCase: false
  processors:
    type: array
    items:
      $ref: "#/types/aws:kinesis/FirehoseDeliveryStreamHttpEndpointConfigurationProcessingConfigurationProcessor:FirehoseDeliveryStreamHttpEndpointConfigurationProcessingConfigurationProcessor"
    description: |
      Array of data processors. More details are given below
    language:
      python:
        mapCase: false
type: object
