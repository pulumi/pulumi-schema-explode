$token: aws:kinesis/FirehoseDeliveryStreamExtendedS3ConfigurationDynamicPartitioningConfiguration:FirehoseDeliveryStreamExtendedS3ConfigurationDynamicPartitioningConfiguration
properties:
  enabled:
    type: boolean
    description: |
      Enables or disables [dynamic partitioning](https://docs.aws.amazon.com/firehose/latest/dev/dynamic-partitioning.html). Defaults to `false`.
    language:
      python:
        mapCase: false
  retryDuration:
    type: integer
    description: |
      Total amount of seconds Firehose spends on retries. Valid values between 0 and 7200. Default is 300.
    language:
      python:
        mapCase: false
type: object
