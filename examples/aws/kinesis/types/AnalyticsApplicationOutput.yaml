$token: aws:kinesis/AnalyticsApplicationOutput:AnalyticsApplicationOutput
properties:
  id:
    type: string
    description: |
      The ARN of the Kinesis Analytics Application.
    language:
      python:
        mapCase: false
  kinesisFirehose:
    $ref: "#/types/aws:kinesis/AnalyticsApplicationOutputKinesisFirehose:AnalyticsApplicationOutputKinesisFirehose"
    description: |
      The Kinesis Firehose configuration for the destination stream. Conflicts with `kinesis_stream`.
      See Kinesis Firehose below for more details.
    language:
      python:
        mapCase: false
  kinesisStream:
    $ref: "#/types/aws:kinesis/AnalyticsApplicationOutputKinesisStream:AnalyticsApplicationOutputKinesisStream"
    description: |
      The Kinesis Stream configuration for the destination stream. Conflicts with `kinesis_firehose`.
      See Kinesis Stream below for more details.
    language:
      python:
        mapCase: false
  lambda:
    $ref: "#/types/aws:kinesis/AnalyticsApplicationOutputLambda:AnalyticsApplicationOutputLambda"
    description: |
      The Lambda function destination. See Lambda below for more details.
    language:
      python:
        mapCase: false
  name:
    type: string
    description: |
      The Name of the in-application stream.
    language:
      python:
        mapCase: false
  schema:
    $ref: "#/types/aws:kinesis/AnalyticsApplicationOutputSchema:AnalyticsApplicationOutputSchema"
    description: |
      The Schema format of the data written to the destination. See Destination Schema below for more details.
    language:
      python:
        mapCase: false
type: object
required:
  - name
  - schema
language:
  nodejs:
    requiredOutputs:
      - id
      - name
      - schema
