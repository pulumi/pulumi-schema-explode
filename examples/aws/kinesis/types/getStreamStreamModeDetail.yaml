$token: aws:kinesis/getStreamStreamModeDetail:getStreamStreamModeDetail
properties:
  streamMode:
    type: string
    description: |
      Capacity mode of the stream. Either `ON_DEMAND` or `PROVISIONED`.
    language:
      python:
        mapCase: false
type: object
required:
  - streamMode
language:
  nodejs:
    requiredInputs: []
