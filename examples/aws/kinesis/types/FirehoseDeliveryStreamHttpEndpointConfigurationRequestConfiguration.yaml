$token: aws:kinesis/FirehoseDeliveryStreamHttpEndpointConfigurationRequestConfiguration:FirehoseDeliveryStreamHttpEndpointConfigurationRequestConfiguration
properties:
  commonAttributes:
    type: array
    items:
      $ref: "#/types/aws:kinesis/FirehoseDeliveryStreamHttpEndpointConfigurationRequestConfigurationCommonAttribute:FirehoseDeliveryStreamHttpEndpointConfigurationRequestConfigurationCommonAttribute"
    description: |
      Describes the metadata sent to the HTTP endpoint destination. More details are given below
    language:
      python:
        mapCase: false
  contentEncoding:
    type: string
    description: |
      Kinesis Data Firehose uses the content encoding to compress the body of a request before sending the request to the destination. Valid values are `NONE` and `GZIP`.  Default value is `NONE`.
    language:
      python:
        mapCase: false
type: object
