$token: aws:kinesis/AnalyticsApplicationInputs:AnalyticsApplicationInputs
properties:
  id:
    type: string
    description: |
      The ARN of the Kinesis Analytics Application.
    language:
      python:
        mapCase: false
  kinesisFirehose:
    $ref: "#/types/aws:kinesis/AnalyticsApplicationInputsKinesisFirehose:AnalyticsApplicationInputsKinesisFirehose"
    description: |
      The Kinesis Firehose configuration for the streaming source. Conflicts with `kinesis_stream`.
      See Kinesis Firehose below for more details.
    language:
      python:
        mapCase: false
  kinesisStream:
    $ref: "#/types/aws:kinesis/AnalyticsApplicationInputsKinesisStream:AnalyticsApplicationInputsKinesisStream"
    description: |
      The Kinesis Stream configuration for the streaming source. Conflicts with `kinesis_firehose`.
      See Kinesis Stream below for more details.
    language:
      python:
        mapCase: false
  namePrefix:
    type: string
    description: |
      The Name Prefix to use when creating an in-application stream.
    language:
      python:
        mapCase: false
  parallelism:
    $ref: "#/types/aws:kinesis/AnalyticsApplicationInputsParallelism:AnalyticsApplicationInputsParallelism"
    description: |
      The number of Parallel in-application streams to create.
      See Parallelism below for more details.
    language:
      python:
        mapCase: false
  processingConfiguration:
    $ref: "#/types/aws:kinesis/AnalyticsApplicationInputsProcessingConfiguration:AnalyticsApplicationInputsProcessingConfiguration"
    description: |
      The Processing Configuration to transform records as they are received from the stream.
      See Processing Configuration below for more details.
    language:
      python:
        mapCase: false
  schema:
    $ref: "#/types/aws:kinesis/AnalyticsApplicationInputsSchema:AnalyticsApplicationInputsSchema"
    description: |
      The Schema format of the data in the streaming source. See Source Schema below for more details.
    language:
      python:
        mapCase: false
  startingPositionConfigurations:
    type: array
    items:
      $ref: "#/types/aws:kinesis/AnalyticsApplicationInputsStartingPositionConfiguration:AnalyticsApplicationInputsStartingPositionConfiguration"
    description: |
      The point at which the application starts processing records from the streaming source.
      See Starting Position Configuration below for more details.
    language:
      python:
        mapCase: false
  streamNames:
    type: array
    items:
      type: string
    language:
      python:
        mapCase: false
type: object
required:
  - namePrefix
  - schema
language:
  nodejs:
    requiredOutputs:
      - id
      - namePrefix
      - parallelism
      - schema
      - startingPositionConfigurations
      - streamNames
