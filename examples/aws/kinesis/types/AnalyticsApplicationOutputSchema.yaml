$token: aws:kinesis/AnalyticsApplicationOutputSchema:AnalyticsApplicationOutputSchema
properties:
  recordFormatType:
    type: string
    description: |
      The Format Type of the records on the output stream. Can be `CSV` or `JSON`.
    language:
      python:
        mapCase: false
type: object
required:
  - recordFormatType
