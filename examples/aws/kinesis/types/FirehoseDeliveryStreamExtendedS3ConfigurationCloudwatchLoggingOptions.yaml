$token: aws:kinesis/FirehoseDeliveryStreamExtendedS3ConfigurationCloudwatchLoggingOptions:FirehoseDeliveryStreamExtendedS3ConfigurationCloudwatchLoggingOptions
properties:
  enabled:
    type: boolean
    description: |
      Enables or disables the logging. Defaults to `false`.
    language:
      python:
        mapCase: false
  logGroupName:
    type: string
    description: |
      The CloudWatch group name for logging. This value is required if `enabled` is true.
    language:
      python:
        mapCase: false
  logStreamName:
    type: string
    description: |
      The CloudWatch log stream name for logging. This value is required if `enabled` is true.
    language:
      python:
        mapCase: false
type: object
