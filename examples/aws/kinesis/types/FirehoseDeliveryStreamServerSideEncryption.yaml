$token: aws:kinesis/FirehoseDeliveryStreamServerSideEncryption:FirehoseDeliveryStreamServerSideEncryption
properties:
  enabled:
    type: boolean
    description: |
      Whether to enable encryption at rest. Default is `false`.
    language:
      python:
        mapCase: false
  keyArn:
    type: string
    description: |
      Amazon Resource Name (ARN) of the encryption key. Required when `key_type` is `CUSTOMER_MANAGED_CMK`.
    language:
      python:
        mapCase: false
  keyType:
    type: string
    description: |
      Type of encryption key. Default is `AWS_OWNED_CMK`. Valid values are `AWS_OWNED_CMK` and `CUSTOMER_MANAGED_CMK`
    language:
      python:
        mapCase: false
type: object
