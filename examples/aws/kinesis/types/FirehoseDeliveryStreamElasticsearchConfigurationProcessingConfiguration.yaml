$token: aws:kinesis/FirehoseDeliveryStreamElasticsearchConfigurationProcessingConfiguration:FirehoseDeliveryStreamElasticsearchConfigurationProcessingConfiguration
properties:
  enabled:
    type: boolean
    description: |
      Enables or disables data processing.
    language:
      python:
        mapCase: false
  processors:
    type: array
    items:
      $ref: "#/types/aws:kinesis/FirehoseDeliveryStreamElasticsearchConfigurationProcessingConfigurationProcessor:FirehoseDeliveryStreamElasticsearchConfigurationProcessingConfigurationProcessor"
    description: |
      Array of data processors. More details are given below
    language:
      python:
        mapCase: false
type: object
