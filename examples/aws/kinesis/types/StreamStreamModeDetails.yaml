$token: aws:kinesis/StreamStreamModeDetails:StreamStreamModeDetails
properties:
  streamMode:
    type: string
    description: |
      Specifies the capacity mode of the stream. Must be either `PROVISIONED` or `ON_DEMAND`.
    language:
      python:
        mapCase: false
type: object
required:
  - streamMode
