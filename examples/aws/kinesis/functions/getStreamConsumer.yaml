$token: aws:kinesis/getStreamConsumer:getStreamConsumer
inputs:
  description: |
    A collection of arguments for invoking getStreamConsumer.
  properties:
    arn:
      type: string
      description: |
        ARN of the stream consumer.
    name:
      type: string
      description: |
        Name of the stream consumer.
    streamArn:
      type: string
      description: |
        ARN of the data stream the consumer is registered with.
  type: object
  required:
    - streamArn
outputs:
  description: |
    A collection of values returned by getStreamConsumer.
  properties:
    arn:
      type: string
    creationTimestamp:
      type: string
      description: |
        Approximate timestamp in [RFC3339 format](https://tools.ietf.org/html/rfc3339#section-5.8) of when the stream consumer was created.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    name:
      type: string
    status:
      type: string
      description: |
        Current status of the stream consumer.
    streamArn:
      type: string
  type: object
  required:
    - arn
    - creationTimestamp
    - name
    - status
    - streamArn
    - id
