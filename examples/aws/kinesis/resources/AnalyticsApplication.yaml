$token: aws:kinesis/analyticsApplication:AnalyticsApplication
properties:
  arn:
    type: string
    $ref: "#/types/aws:index/aRN:ARN"
    description: |
      The ARN of the Kinesis Analytics Appliation.
  cloudwatchLoggingOptions:
    $ref: "#/types/aws:kinesis/AnalyticsApplicationCloudwatchLoggingOptions:AnalyticsApplicationCloudwatchLoggingOptions"
    description: |
      The CloudWatch log stream options to monitor application errors.
      See CloudWatch Logging Options below for more details.
  code:
    type: string
    description: |
      SQL Code to transform input data, and generate output.
  createTimestamp:
    type: string
    description: |
      The Timestamp when the application version was created.
  description:
    type: string
    description: |
      Description of the application.
  inputs:
    $ref: "#/types/aws:kinesis/AnalyticsApplicationInputs:AnalyticsApplicationInputs"
    description: |
      Input configuration of the application. See Inputs below for more details.
  lastUpdateTimestamp:
    type: string
    description: |
      The Timestamp when the application was last updated.
  name:
    type: string
    description: |
      Name of the Kinesis Analytics Application.
  outputs:
    type: array
    items:
      $ref: "#/types/aws:kinesis/AnalyticsApplicationOutput:AnalyticsApplicationOutput"
    description: |
      Output destination configuration of the application. See Outputs below for more details.
  referenceDataSources:
    $ref: "#/types/aws:kinesis/AnalyticsApplicationReferenceDataSources:AnalyticsApplicationReferenceDataSources"
    description: |
      An S3 Reference Data Source for the application.
      See Reference Data Sources below for more details.
  startApplication:
    type: boolean
    description: |
      Whether to start or stop the Kinesis Analytics Application. To start an application, an input with a defined `starting_position` must be configured.
      To modify an application's starting position, first stop the application by setting `start_application = false`, then update `starting_position` and set `start_application = true`.
  status:
    type: string
    description: |
      The Status of the application.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of tags for the Kinesis Analytics Application. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  version:
    type: integer
    description: |
      The Version of the application.
required:
  - arn
  - createTimestamp
  - lastUpdateTimestamp
  - name
  - status
  - tagsAll
  - version
inputProperties:
  cloudwatchLoggingOptions:
    $ref: "#/types/aws:kinesis/AnalyticsApplicationCloudwatchLoggingOptions:AnalyticsApplicationCloudwatchLoggingOptions"
    description: |
      The CloudWatch log stream options to monitor application errors.
      See CloudWatch Logging Options below for more details.
  code:
    type: string
    description: |
      SQL Code to transform input data, and generate output.
  description:
    type: string
    description: |
      Description of the application.
    willReplaceOnChanges: true
  inputs:
    $ref: "#/types/aws:kinesis/AnalyticsApplicationInputs:AnalyticsApplicationInputs"
    description: |
      Input configuration of the application. See Inputs below for more details.
  name:
    type: string
    description: |
      Name of the Kinesis Analytics Application.
    willReplaceOnChanges: true
  outputs:
    type: array
    items:
      $ref: "#/types/aws:kinesis/AnalyticsApplicationOutput:AnalyticsApplicationOutput"
    description: |
      Output destination configuration of the application. See Outputs below for more details.
  referenceDataSources:
    $ref: "#/types/aws:kinesis/AnalyticsApplicationReferenceDataSources:AnalyticsApplicationReferenceDataSources"
    description: |
      An S3 Reference Data Source for the application.
      See Reference Data Sources below for more details.
  startApplication:
    type: boolean
    description: |
      Whether to start or stop the Kinesis Analytics Application. To start an application, an input with a defined `starting_position` must be configured.
      To modify an application's starting position, first stop the application by setting `start_application = false`, then update `starting_position` and set `start_application = true`.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of tags for the Kinesis Analytics Application. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
stateInputs:
  description: |
    Input properties used for looking up and filtering AnalyticsApplication resources.
  properties:
    arn:
      type: string
      $ref: "#/types/aws:index/aRN:ARN"
      description: |
        The ARN of the Kinesis Analytics Appliation.
    cloudwatchLoggingOptions:
      $ref: "#/types/aws:kinesis/AnalyticsApplicationCloudwatchLoggingOptions:AnalyticsApplicationCloudwatchLoggingOptions"
      description: |
        The CloudWatch log stream options to monitor application errors.
        See CloudWatch Logging Options below for more details.
    code:
      type: string
      description: |
        SQL Code to transform input data, and generate output.
    createTimestamp:
      type: string
      description: |
        The Timestamp when the application version was created.
    description:
      type: string
      description: |
        Description of the application.
      willReplaceOnChanges: true
    inputs:
      $ref: "#/types/aws:kinesis/AnalyticsApplicationInputs:AnalyticsApplicationInputs"
      description: |
        Input configuration of the application. See Inputs below for more details.
    lastUpdateTimestamp:
      type: string
      description: |
        The Timestamp when the application was last updated.
    name:
      type: string
      description: |
        Name of the Kinesis Analytics Application.
      willReplaceOnChanges: true
    outputs:
      type: array
      items:
        $ref: "#/types/aws:kinesis/AnalyticsApplicationOutput:AnalyticsApplicationOutput"
      description: |
        Output destination configuration of the application. See Outputs below for more details.
    referenceDataSources:
      $ref: "#/types/aws:kinesis/AnalyticsApplicationReferenceDataSources:AnalyticsApplicationReferenceDataSources"
      description: |
        An S3 Reference Data Source for the application.
        See Reference Data Sources below for more details.
    startApplication:
      type: boolean
      description: |
        Whether to start or stop the Kinesis Analytics Application. To start an application, an input with a defined `starting_position` must be configured.
        To modify an application's starting position, first stop the application by setting `start_application = false`, then update `starting_position` and set `start_application = true`.
    status:
      type: string
      description: |
        The Status of the application.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of tags for the Kinesis Analytics Application. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    version:
      type: integer
      description: |
        The Version of the application.
  type: object
