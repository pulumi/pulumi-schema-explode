$token: aws:kinesis/streamConsumer:StreamConsumer
properties:
  arn:
    type: string
    description: |
      Amazon Resource Name (ARN) of the stream consumer.
  creationTimestamp:
    type: string
    description: |
      Approximate timestamp in [RFC3339 format](https://tools.ietf.org/html/rfc3339#section-5.8) of when the stream consumer was created.
  name:
    type: string
    description: |
      Name of the stream consumer.
  streamArn:
    type: string
    description: |
      Amazon Resource Name (ARN) of the data stream the consumer is registered with.
required:
  - arn
  - creationTimestamp
  - name
  - streamArn
inputProperties:
  name:
    type: string
    description: |
      Name of the stream consumer.
    willReplaceOnChanges: true
  streamArn:
    type: string
    description: |
      Amazon Resource Name (ARN) of the data stream the consumer is registered with.
    willReplaceOnChanges: true
requiredInputs:
  - streamArn
stateInputs:
  description: |
    Input properties used for looking up and filtering StreamConsumer resources.
  properties:
    arn:
      type: string
      description: |
        Amazon Resource Name (ARN) of the stream consumer.
    creationTimestamp:
      type: string
      description: |
        Approximate timestamp in [RFC3339 format](https://tools.ietf.org/html/rfc3339#section-5.8) of when the stream consumer was created.
    name:
      type: string
      description: |
        Name of the stream consumer.
      willReplaceOnChanges: true
    streamArn:
      type: string
      description: |
        Amazon Resource Name (ARN) of the data stream the consumer is registered with.
      willReplaceOnChanges: true
  type: object
