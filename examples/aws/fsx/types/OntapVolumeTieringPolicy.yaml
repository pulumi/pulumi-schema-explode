$token: aws:fsx/OntapVolumeTieringPolicy:OntapVolumeTieringPolicy
properties:
  coolingPeriod:
    type: integer
    language:
      python:
        mapCase: false
  name:
    type: string
    description: |
      Specifies the tiering policy for the ONTAP volume for moving data to the capacity pool storage. Valid values are `SNAPSHOT_ONLY`, `AUTO`, `ALL`, `NONE`. Default value is `SNAPSHOT_ONLY`.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - name
