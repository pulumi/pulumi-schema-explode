$token: aws:fsx/OntapFileSystemDiskIopsConfiguration:OntapFileSystemDiskIopsConfiguration
properties:
  iops:
    type: integer
    description: |
      - The total number of SSD IOPS provisioned for the file system.
    language:
      python:
        mapCase: false
  mode:
    type: string
    description: |
      - Specifies whether the number of IOPS for the file system is using the system. Valid values are `AUTOMATIC` and `USER_PROVISIONED`. Default value is `AUTOMATIC`.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - iops
