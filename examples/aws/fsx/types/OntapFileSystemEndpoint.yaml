$token: aws:fsx/OntapFileSystemEndpoint:OntapFileSystemEndpoint
properties:
  interclusters:
    type: array
    items:
      $ref: "#/types/aws:fsx/OntapFileSystemEndpointIntercluster:OntapFileSystemEndpointIntercluster"
    description: |
      An endpoint for managing your file system by setting up NetApp SnapMirror with other ONTAP systems. See Endpoint.
    language:
      python:
        mapCase: false
  managements:
    type: array
    items:
      $ref: "#/types/aws:fsx/OntapFileSystemEndpointManagement:OntapFileSystemEndpointManagement"
    description: |
      An endpoint for managing your file system using the NetApp ONTAP CLI and NetApp ONTAP API. See Endpoint.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - interclusters
      - managements
