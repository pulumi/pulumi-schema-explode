$token: aws:fsx/OntapStorageVirtualMachineEndpointNf:OntapStorageVirtualMachineEndpointNf
properties:
  dnsName:
    type: string
    description: |
      The Domain Name Service (DNS) name for the storage virtual machine. You can mount your storage virtual machine using its DNS name.
    language:
      python:
        mapCase: false
  ipAddresses:
    type: array
    items:
      type: string
    description: |
      IP addresses of the storage virtual machine endpoint.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - dnsName
      - ipAddresses
