$token: aws:fsx/DataRepositoryAssociationS3AutoExportPolicy:DataRepositoryAssociationS3AutoExportPolicy
properties:
  events:
    type: array
    items:
      type: string
    description: |
      A list of file event types to automatically export to your linked S3 bucket or import from the linked S3 bucket. Valid values are `NEW`, `CHANGED`, `DELETED`. Max of 3.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - events
