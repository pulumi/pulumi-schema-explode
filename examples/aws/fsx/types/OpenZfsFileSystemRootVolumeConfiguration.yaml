$token: aws:fsx/OpenZfsFileSystemRootVolumeConfiguration:OpenZfsFileSystemRootVolumeConfiguration
properties:
  copyTagsToSnapshots:
    type: boolean
    description: |
      - A boolean flag indicating whether tags for the file system should be copied to snapshots. The default value is false.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  dataCompressionType:
    type: string
    description: |
      - Method used to compress the data on the volume. Valid values are `LZ4`, `NONE` or `ZSTD`. Child volumes that don't specify compression option will inherit from parent volume. This option on file system applies to the root volume.
    language:
      python:
        mapCase: false
  nfsExports:
    $ref: "#/types/aws:fsx/OpenZfsFileSystemRootVolumeConfigurationNfsExports:OpenZfsFileSystemRootVolumeConfigurationNfsExports"
    description: |
      - NFS export configuration for the root volume. Exactly 1 item. See NFS Exports Below.
    language:
      python:
        mapCase: false
  readOnly:
    type: boolean
    description: |
      - specifies whether the volume is read-only. Default is false.
    language:
      python:
        mapCase: false
  recordSizeKib:
    type: integer
    description: |
      - Specifies the record size of an OpenZFS root volume, in kibibytes (KiB). Valid values are `4`, `8`, `16`, `32`, `64`, `128`, `256`, `512`, or `1024` KiB. The default is `128` KiB.
    language:
      python:
        mapCase: false
  userAndGroupQuotas:
    type: array
    items:
      $ref: "#/types/aws:fsx/OpenZfsFileSystemRootVolumeConfigurationUserAndGroupQuota:OpenZfsFileSystemRootVolumeConfigurationUserAndGroupQuota"
    description: |
      - Specify how much storage users or groups can use on the volume. Maximum of 100 items. See User and Group Quotas Below.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - readOnly
      - userAndGroupQuotas
