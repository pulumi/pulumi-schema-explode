$token: aws:fsx/OntapFileSystemEndpointIntercluster:OntapFileSystemEndpointIntercluster
properties:
  dnsName:
    type: string
    description: |
      The Domain Name Service (DNS) name for the file system. You can mount your file system using its DNS name.
    language:
      python:
        mapCase: false
  ipAddresses:
    type: array
    items:
      type: string
    description: |
      IP addresses of the file system endpoint.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - dnsName
      - ipAddresses
