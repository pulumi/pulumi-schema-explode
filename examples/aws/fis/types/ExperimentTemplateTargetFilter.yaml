$token: aws:fis/ExperimentTemplateTargetFilter:ExperimentTemplateTargetFilter
properties:
  path:
    type: string
    description: |
      Attribute path for the filter.
    language:
      python:
        mapCase: false
  values:
    type: array
    items:
      type: string
    description: |
      Set of attribute values for the filter.
    language:
      python:
        mapCase: false
type: object
required:
  - path
  - values
