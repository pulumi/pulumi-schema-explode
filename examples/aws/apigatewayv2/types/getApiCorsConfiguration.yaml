$token: aws:apigatewayv2/getApiCorsConfiguration:getApiCorsConfiguration
properties:
  allowCredentials:
    type: boolean
    description: |
      Whether credentials are included in the CORS request.
    language:
      python:
        mapCase: false
  allowHeaders:
    type: array
    items:
      type: string
    description: |
      Set of allowed HTTP headers.
    language:
      python:
        mapCase: false
  allowMethods:
    type: array
    items:
      type: string
    description: |
      Set of allowed HTTP methods.
    language:
      python:
        mapCase: false
  allowOrigins:
    type: array
    items:
      type: string
    description: |
      Set of allowed origins.
    language:
      python:
        mapCase: false
  exposeHeaders:
    type: array
    items:
      type: string
    description: |
      Set of exposed HTTP headers.
    language:
      python:
        mapCase: false
  maxAge:
    type: integer
    description: |
      Number of seconds that the browser should cache preflight request results.
    language:
      python:
        mapCase: false
type: object
required:
  - allowCredentials
  - allowHeaders
  - allowMethods
  - allowOrigins
  - exposeHeaders
  - maxAge
language:
  nodejs:
    requiredInputs: []
