$token: aws:apigatewayv2/DomainNameDomainNameConfiguration:DomainNameDomainNameConfiguration
properties:
  certificateArn:
    type: string
    description: |
      ARN of an AWS-managed certificate that will be used by the endpoint for the domain name. AWS Certificate Manager is the only supported source. Use the [`aws.acm.Certificate`](https://www.terraform.io/docs/providers/aws/r/acm_certificate.html) resource to configure an ACM certificate.
    language:
      python:
        mapCase: false
  endpointType:
    type: string
    description: |
      Endpoint type. Valid values: `REGIONAL`.
    language:
      python:
        mapCase: false
  hostedZoneId:
    type: string
    description: |
      Amazon Route 53 Hosted Zone ID of the endpoint.
    language:
      python:
        mapCase: false
  ownershipVerificationCertificateArn:
    type: string
    description: |
      ARN of the AWS-issued certificate used to validate custom domain ownership (when `certificate_arn` is issued via an ACM Private CA or `mutual_tls_authentication` is configured with an ACM-imported certificate.)
    language:
      python:
        mapCase: false
  securityPolicy:
    type: string
    description: |
      Transport Layer Security (TLS) version of the [security policy](https://docs.aws.amazon.com/apigateway/latest/developerguide/apigateway-custom-domain-tls-version.html) for the domain name. Valid values: `TLS_1_2`.
    language:
      python:
        mapCase: false
  targetDomainName:
    type: string
    description: |
      Target domain name.
    language:
      python:
        mapCase: false
type: object
required:
  - certificateArn
  - endpointType
  - securityPolicy
language:
  nodejs:
    requiredOutputs:
      - certificateArn
      - endpointType
      - hostedZoneId
      - ownershipVerificationCertificateArn
      - securityPolicy
      - targetDomainName
