$token: aws:apigatewayv2/AuthorizerJwtConfiguration:AuthorizerJwtConfiguration
properties:
  audiences:
    type: array
    items:
      type: string
    description: |
      List of the intended recipients of the JWT. A valid JWT must provide an aud that matches at least one entry in this list.
    language:
      python:
        mapCase: false
  issuer:
    type: string
    description: |
      Base domain of the identity provider that issues JSON Web Tokens, such as the `endpoint` attribute of the `aws.cognito.UserPool` resource.
    language:
      python:
        mapCase: false
type: object
