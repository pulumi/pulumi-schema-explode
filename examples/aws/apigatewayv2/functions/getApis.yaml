$token: aws:apigatewayv2/getApis:getApis
inputs:
  description: |
    A collection of arguments for invoking getApis.
  properties:
    name:
      type: string
      description: |
        API name.
    protocolType:
      type: string
      description: |
        API protocol.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags, each pair of which must exactly match
        a pair on the desired APIs.
  type: object
outputs:
  description: |
    A collection of values returned by getApis.
  properties:
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    ids:
      type: array
      items:
        type: string
      description: |
        Set of API identifiers.
    name:
      type: string
    protocolType:
      type: string
    tags:
      type: object
      additionalProperties:
        type: string
  type: object
  required:
    - ids
    - id
