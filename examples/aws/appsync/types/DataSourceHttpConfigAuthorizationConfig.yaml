$token: aws:appsync/DataSourceHttpConfigAuthorizationConfig:DataSourceHttpConfigAuthorizationConfig
properties:
  authorizationType:
    type: string
    description: |
      Authorization type that the HTTP endpoint requires. Default values is `AWS_IAM`.
    language:
      python:
        mapCase: false
  awsIamConfig:
    $ref: "#/types/aws:appsync/DataSourceHttpConfigAuthorizationConfigAwsIamConfig:DataSourceHttpConfigAuthorizationConfigAwsIamConfig"
    description: |
      Identity and Access Management (IAM) settings. See AWS IAM Config.
    language:
      python:
        mapCase: false
type: object
