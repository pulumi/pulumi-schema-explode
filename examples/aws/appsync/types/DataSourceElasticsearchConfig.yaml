$token: aws:appsync/DataSourceElasticsearchConfig:DataSourceElasticsearchConfig
properties:
  endpoint:
    type: string
    description: |
      HTTP URL.
    language:
      python:
        mapCase: false
  region:
    type: string
    description: |
      AWS Region for RDS HTTP endpoint. Defaults to current region.
    language:
      python:
        mapCase: false
type: object
required:
  - endpoint
language:
  nodejs:
    requiredOutputs:
      - endpoint
      - region
