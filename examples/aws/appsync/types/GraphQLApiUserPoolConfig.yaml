$token: aws:appsync/GraphQLApiUserPoolConfig:GraphQLApiUserPoolConfig
properties:
  appIdClientRegex:
    type: string
    description: |
      Regular expression for validating the incoming Amazon Cognito User Pool app client ID.
    language:
      python:
        mapCase: false
  awsRegion:
    type: string
    description: |
      AWS region in which the user pool was created.
    language:
      python:
        mapCase: false
  defaultAction:
    type: string
    description: |
      Action that you want your GraphQL API to take when a request that uses Amazon Cognito User Pool authentication doesn't match the Amazon Cognito User Pool configuration. Valid: `ALLOW` and `DENY`
    language:
      python:
        mapCase: false
  userPoolId:
    type: string
    description: |
      User pool ID.
    language:
      python:
        mapCase: false
type: object
required:
  - defaultAction
  - userPoolId
language:
  nodejs:
    requiredOutputs:
      - awsRegion
      - defaultAction
      - userPoolId
