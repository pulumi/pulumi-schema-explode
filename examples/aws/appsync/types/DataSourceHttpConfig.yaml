$token: aws:appsync/DataSourceHttpConfig:DataSourceHttpConfig
properties:
  authorizationConfig:
    $ref: "#/types/aws:appsync/DataSourceHttpConfigAuthorizationConfig:DataSourceHttpConfigAuthorizationConfig"
    description: |
      Authorization configuration in case the HTTP endpoint requires authorization. See Authorization Config.
    language:
      python:
        mapCase: false
  endpoint:
    type: string
    description: |
      HTTP URL.
    language:
      python:
        mapCase: false
type: object
required:
  - endpoint
