$token: aws:appsync/DataSourceRelationalDatabaseConfigHttpEndpointConfig:DataSourceRelationalDatabaseConfigHttpEndpointConfig
properties:
  awsSecretStoreArn:
    type: string
    description: |
      AWS secret store ARN for database credentials.
    language:
      python:
        mapCase: false
  databaseName:
    type: string
    description: |
      Logical database name.
    language:
      python:
        mapCase: false
  dbClusterIdentifier:
    type: string
    description: |
      Amazon RDS cluster identifier.
    language:
      python:
        mapCase: false
  region:
    type: string
    description: |
      AWS Region for RDS HTTP endpoint. Defaults to current region.
    language:
      python:
        mapCase: false
  schema:
    type: string
    description: |
      Logical schema name.
    language:
      python:
        mapCase: false
type: object
required:
  - awsSecretStoreArn
  - dbClusterIdentifier
language:
  nodejs:
    requiredOutputs:
      - awsSecretStoreArn
      - dbClusterIdentifier
      - region
