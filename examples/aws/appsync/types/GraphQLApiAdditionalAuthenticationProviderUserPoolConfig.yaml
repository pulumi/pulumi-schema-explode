$token: aws:appsync/GraphQLApiAdditionalAuthenticationProviderUserPoolConfig:GraphQLApiAdditionalAuthenticationProviderUserPoolConfig
properties:
  appIdClientRegex:
    type: string
    description: |
      Regular expression for validating the incoming Amazon Cognito User Pool app client ID.
    language:
      python:
        mapCase: false
  awsRegion:
    type: string
    description: |
      AWS region in which the user pool was created.
    language:
      python:
        mapCase: false
  userPoolId:
    type: string
    description: |
      User pool ID.
    language:
      python:
        mapCase: false
type: object
required:
  - userPoolId
language:
  nodejs:
    requiredOutputs:
      - awsRegion
      - userPoolId
