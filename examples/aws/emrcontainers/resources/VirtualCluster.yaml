$token: aws:emrcontainers/virtualCluster:VirtualCluster
properties:
  arn:
    type: string
    description: |
      ARN of the cluster.
  containerProvider:
    $ref: "#/types/aws:emrcontainers/VirtualClusterContainerProvider:VirtualClusterContainerProvider"
    description: |
      Configuration block for the container provider associated with your cluster.
  name:
    type: string
    description: |
      Name of the virtual cluster.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value mapping of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level
  tagsAll:
    type: object
    additionalProperties:
      type: string
required:
  - arn
  - containerProvider
  - name
  - tagsAll
inputProperties:
  containerProvider:
    $ref: "#/types/aws:emrcontainers/VirtualClusterContainerProvider:VirtualClusterContainerProvider"
    description: |
      Configuration block for the container provider associated with your cluster.
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      Name of the virtual cluster.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value mapping of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level
requiredInputs:
  - containerProvider
stateInputs:
  description: |
    Input properties used for looking up and filtering VirtualCluster resources.
  properties:
    arn:
      type: string
      description: |
        ARN of the cluster.
    containerProvider:
      $ref: "#/types/aws:emrcontainers/VirtualClusterContainerProvider:VirtualClusterContainerProvider"
      description: |
        Configuration block for the container provider associated with your cluster.
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        Name of the virtual cluster.
      willReplaceOnChanges: true
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value mapping of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level
    tagsAll:
      type: object
      additionalProperties:
        type: string
  type: object
