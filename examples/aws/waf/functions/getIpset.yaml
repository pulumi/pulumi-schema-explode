$token: aws:waf/getIpset:getIpset
inputs:
  description: |
    A collection of arguments for invoking getIpset.
  properties:
    name:
      type: string
      description: |
        Name of the WAF IP set.
  type: object
  required:
    - name
outputs:
  description: |
    A collection of values returned by getIpset.
  properties:
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    name:
      type: string
  type: object
  required:
    - name
    - id
