$token: aws:waf/getRateBasedRule:getRateBasedRule
inputs:
  description: |
    A collection of arguments for invoking getRateBasedRule.
  properties:
    name:
      type: string
      description: |
        Name of the WAF rate based rule.
  type: object
  required:
    - name
outputs:
  description: |
    A collection of values returned by getRateBasedRule.
  properties:
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    name:
      type: string
  type: object
  required:
    - name
    - id
