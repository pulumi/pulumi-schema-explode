$token: aws:waf/byteMatchSet:ByteMatchSet
properties:
  byteMatchTuples:
    type: array
    items:
      $ref: "#/types/aws:waf/ByteMatchSetByteMatchTuple:ByteMatchSetByteMatchTuple"
    description: |
      Specifies the bytes (typically a string that corresponds
      with ASCII characters) that you want to search for in web requests,
      the location in requests that you want to search, and other settings.
  name:
    type: string
    description: |
      The name or description of the Byte Match Set.
required:
  - name
inputProperties:
  byteMatchTuples:
    type: array
    items:
      $ref: "#/types/aws:waf/ByteMatchSetByteMatchTuple:ByteMatchSetByteMatchTuple"
    description: |
      Specifies the bytes (typically a string that corresponds
      with ASCII characters) that you want to search for in web requests,
      the location in requests that you want to search, and other settings.
  name:
    type: string
    description: |
      The name or description of the Byte Match Set.
    willReplaceOnChanges: true
stateInputs:
  description: |
    Input properties used for looking up and filtering ByteMatchSet resources.
  properties:
    byteMatchTuples:
      type: array
      items:
        $ref: "#/types/aws:waf/ByteMatchSetByteMatchTuple:ByteMatchSetByteMatchTuple"
      description: |
        Specifies the bytes (typically a string that corresponds
        with ASCII characters) that you want to search for in web requests,
        the location in requests that you want to search, and other settings.
    name:
      type: string
      description: |
        The name or description of the Byte Match Set.
      willReplaceOnChanges: true
  type: object
