$token: aws:waf/regexPatternSet:RegexPatternSet
properties:
  arn:
    type: string
    description: |
      Amazon Resource Name (ARN)
  name:
    type: string
    description: |
      The name or description of the Regex Pattern Set.
  regexPatternStrings:
    type: array
    items:
      type: string
    description: |
      A list of regular expression (regex) patterns that you want AWS WAF to search for, such as `B[a@]dB[o0]t`.
required:
  - arn
  - name
inputProperties:
  name:
    type: string
    description: |
      The name or description of the Regex Pattern Set.
    willReplaceOnChanges: true
  regexPatternStrings:
    type: array
    items:
      type: string
    description: |
      A list of regular expression (regex) patterns that you want AWS WAF to search for, such as `B[a@]dB[o0]t`.
stateInputs:
  description: |
    Input properties used for looking up and filtering RegexPatternSet resources.
  properties:
    arn:
      type: string
      description: |
        Amazon Resource Name (ARN)
    name:
      type: string
      description: |
        The name or description of the Regex Pattern Set.
      willReplaceOnChanges: true
    regexPatternStrings:
      type: array
      items:
        type: string
      description: |
        A list of regular expression (regex) patterns that you want AWS WAF to search for, such as `B[a@]dB[o0]t`.
  type: object
