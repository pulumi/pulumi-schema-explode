$token: aws:waf/regexMatchSet:RegexMatchSet
properties:
  arn:
    type: string
    description: |
      Amazon Resource Name (ARN)
  name:
    type: string
    description: |
      The name or description of the Regex Match Set.
  regexMatchTuples:
    type: array
    items:
      $ref: "#/types/aws:waf/RegexMatchSetRegexMatchTuple:RegexMatchSetRegexMatchTuple"
    description: |
      The regular expression pattern that you want AWS WAF to search for in web requests, the location in requests that you want AWS WAF to search, and other settings. See below.
required:
  - arn
  - name
inputProperties:
  name:
    type: string
    description: |
      The name or description of the Regex Match Set.
    willReplaceOnChanges: true
  regexMatchTuples:
    type: array
    items:
      $ref: "#/types/aws:waf/RegexMatchSetRegexMatchTuple:RegexMatchSetRegexMatchTuple"
    description: |
      The regular expression pattern that you want AWS WAF to search for in web requests, the location in requests that you want AWS WAF to search, and other settings. See below.
stateInputs:
  description: |
    Input properties used for looking up and filtering RegexMatchSet resources.
  properties:
    arn:
      type: string
      description: |
        Amazon Resource Name (ARN)
    name:
      type: string
      description: |
        The name or description of the Regex Match Set.
      willReplaceOnChanges: true
    regexMatchTuples:
      type: array
      items:
        $ref: "#/types/aws:waf/RegexMatchSetRegexMatchTuple:RegexMatchSetRegexMatchTuple"
      description: |
        The regular expression pattern that you want AWS WAF to search for in web requests, the location in requests that you want AWS WAF to search, and other settings. See below.
  type: object
