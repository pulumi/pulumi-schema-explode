$token: aws:waf/rateBasedRule:RateBasedRule
properties:
  arn:
    type: string
    description: |
      Amazon Resource Name (ARN)
  metricName:
    type: string
    description: |
      The name or description for the Amazon CloudWatch metric of this rule.
  name:
    type: string
    description: |
      The name or description of the rule.
  predicates:
    type: array
    items:
      $ref: "#/types/aws:waf/RateBasedRulePredicate:RateBasedRulePredicate"
    description: |
      The objects to include in a rule (documented below).
  rateKey:
    type: string
    description: |
      Valid value is IP.
  rateLimit:
    type: integer
    description: |
      The maximum number of requests, which have an identical value in the field specified by the RateKey, allowed in a five-minute period. Minimum value is 100.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - metricName
  - name
  - rateKey
  - rateLimit
  - tagsAll
inputProperties:
  metricName:
    type: string
    description: |
      The name or description for the Amazon CloudWatch metric of this rule.
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      The name or description of the rule.
    willReplaceOnChanges: true
  predicates:
    type: array
    items:
      $ref: "#/types/aws:waf/RateBasedRulePredicate:RateBasedRulePredicate"
    description: |
      The objects to include in a rule (documented below).
  rateKey:
    type: string
    description: |
      Valid value is IP.
  rateLimit:
    type: integer
    description: |
      The maximum number of requests, which have an identical value in the field specified by the RateKey, allowed in a five-minute period. Minimum value is 100.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - metricName
  - rateKey
  - rateLimit
stateInputs:
  description: |
    Input properties used for looking up and filtering RateBasedRule resources.
  properties:
    arn:
      type: string
      description: |
        Amazon Resource Name (ARN)
    metricName:
      type: string
      description: |
        The name or description for the Amazon CloudWatch metric of this rule.
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        The name or description of the rule.
      willReplaceOnChanges: true
    predicates:
      type: array
      items:
        $ref: "#/types/aws:waf/RateBasedRulePredicate:RateBasedRulePredicate"
      description: |
        The objects to include in a rule (documented below).
    rateKey:
      type: string
      description: |
        Valid value is IP.
    rateLimit:
      type: integer
      description: |
        The maximum number of requests, which have an identical value in the field specified by the RateKey, allowed in a five-minute period. Minimum value is 100.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
