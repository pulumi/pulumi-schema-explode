$token: aws:waf/ByteMatchSetByteMatchTuple:ByteMatchSetByteMatchTuple
properties:
  fieldToMatch:
    $ref: "#/types/aws:waf/ByteMatchSetByteMatchTupleFieldToMatch:ByteMatchSetByteMatchTupleFieldToMatch"
    description: |
      The part of a web request that you want to search, such as a specified header or a query string.
    language:
      python:
        mapCase: false
  positionalConstraint:
    type: string
    description: |
      Within the portion of a web request that you want to search
      (for example, in the query string, if any), specify where you want to search.
      e.g., `CONTAINS`, `CONTAINS_WORD` or `EXACTLY`.
      See [docs](http://docs.aws.amazon.com/waf/latest/APIReference/API_ByteMatchTuple.html#WAF-Type-ByteMatchTuple-PositionalConstraint)
      for all supported values.
    language:
      python:
        mapCase: false
  targetString:
    type: string
    description: |
      The value that you want to search for within the field specified by `field_to_match`, e.g., `badrefer1`.
      See [docs](https://docs.aws.amazon.com/waf/latest/APIReference/API_waf_ByteMatchTuple.html)
      for all supported values.
    language:
      python:
        mapCase: false
  textTransformation:
    type: string
    description: |
      Text transformations used to eliminate unusual formatting that attackers use in web requests in an effort to bypass AWS WAF.
      If you specify a transformation, AWS WAF performs the transformation on `target_string` before inspecting a request for a match.
      e.g., `CMD_LINE`, `HTML_ENTITY_DECODE` or `NONE`.
      See [docs](http://docs.aws.amazon.com/waf/latest/APIReference/API_ByteMatchTuple.html#WAF-Type-ByteMatchTuple-TextTransformation)
      for all supported values.
    language:
      python:
        mapCase: false
type: object
required:
  - fieldToMatch
  - positionalConstraint
  - textTransformation
