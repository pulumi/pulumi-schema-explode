$token: aws:waf/RegexMatchSetRegexMatchTuple:RegexMatchSetRegexMatchTuple
properties:
  fieldToMatch:
    $ref: "#/types/aws:waf/RegexMatchSetRegexMatchTupleFieldToMatch:RegexMatchSetRegexMatchTupleFieldToMatch"
    description: |
      The part of a web request that you want to search, such as a specified header or a query string.
    language:
      python:
        mapCase: false
  regexPatternSetId:
    type: string
    description: |
      The ID of a `WAF Regex Pattern Set`.
    language:
      python:
        mapCase: false
  textTransformation:
    type: string
    description: |
      Text transformations used to eliminate unusual formatting that attackers use in web requests in an effort to bypass AWS WAF.
      e.g. `CMD_LINE`, `HTML_ENTITY_DECODE` or `NONE`.
      See [docs](http://docs.aws.amazon.com/waf/latest/APIReference/API_ByteMatchTuple.html#WAF-Type-ByteMatchTuple-TextTransformation)
      for all supported values.
    language:
      python:
        mapCase: false
type: object
required:
  - fieldToMatch
  - regexPatternSetId
  - textTransformation
