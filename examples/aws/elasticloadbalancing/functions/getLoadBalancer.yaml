$token: aws:elasticloadbalancing/getLoadBalancer:getLoadBalancer
inputs:
  description: |
    A collection of arguments for invoking getLoadBalancer.
  properties:
    name:
      type: string
      description: |
        Unique name of the load balancer.
    tags:
      type: object
      additionalProperties:
        type: string
  type: object
  required:
    - name
outputs:
  description: |
    A collection of values returned by getLoadBalancer.
  properties:
    accessLogs:
      $ref: "#/types/aws:elasticloadbalancing/getLoadBalancerAccessLogs:getLoadBalancerAccessLogs"
    arn:
      type: string
    availabilityZones:
      type: array
      items:
        type: string
    connectionDraining:
      type: boolean
    connectionDrainingTimeout:
      type: integer
    crossZoneLoadBalancing:
      type: boolean
    desyncMitigationMode:
      type: string
    dnsName:
      type: string
    healthCheck:
      $ref: "#/types/aws:elasticloadbalancing/getLoadBalancerHealthCheck:getLoadBalancerHealthCheck"
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    idleTimeout:
      type: integer
    instances:
      type: array
      items:
        type: string
    internal:
      type: boolean
    listeners:
      type: array
      items:
        $ref: "#/types/aws:elasticloadbalancing/getLoadBalancerListener:getLoadBalancerListener"
    name:
      type: string
    securityGroups:
      type: array
      items:
        type: string
    sourceSecurityGroup:
      type: string
    sourceSecurityGroupId:
      type: string
    subnets:
      type: array
      items:
        type: string
    tags:
      type: object
      additionalProperties:
        type: string
    zoneId:
      type: string
  type: object
  required:
    - accessLogs
    - arn
    - availabilityZones
    - connectionDraining
    - connectionDrainingTimeout
    - crossZoneLoadBalancing
    - desyncMitigationMode
    - dnsName
    - healthCheck
    - idleTimeout
    - instances
    - internal
    - listeners
    - name
    - securityGroups
    - sourceSecurityGroup
    - sourceSecurityGroupId
    - subnets
    - tags
    - zoneId
    - id
deprecationMessage: aws.elasticloadbalancing.getLoadBalancer has been deprecated in favor of aws.elb.getLoadBalancer
