$token: aws:elasticloadbalancing/LoadBalancerAccessLogs:LoadBalancerAccessLogs
properties:
  bucket:
    type: string
    description: |
      The S3 bucket name to store the logs in.
    language:
      python:
        mapCase: false
  bucketPrefix:
    type: string
    description: |
      The S3 bucket prefix. Logs are stored in the root if not configured.
    language:
      python:
        mapCase: false
  enabled:
    type: boolean
    description: |
      Boolean to enable / disable `access_logs`. Default is `true`
    language:
      python:
        mapCase: false
  interval:
    type: integer
    description: |
      The publishing interval in minutes. Valid values: `5` and `60`. Default: `60`
    language:
      python:
        mapCase: false
type: object
required:
  - bucket
