$token: aws:elasticloadbalancing/loadBalancerPolicy:LoadBalancerPolicy
properties:
  loadBalancerName:
    type: string
    description: |
      The load balancer on which the policy is defined.
  policyAttributes:
    type: array
    items:
      $ref: "#/types/aws:elasticloadbalancing/LoadBalancerPolicyPolicyAttribute:LoadBalancerPolicyPolicyAttribute"
    description: |
      Policy attribute to apply to the policy.
  policyName:
    type: string
    description: |
      The name of the load balancer policy.
  policyTypeName:
    type: string
    description: |
      The policy type.
required:
  - loadBalancerName
  - policyAttributes
  - policyName
  - policyTypeName
inputProperties:
  loadBalancerName:
    type: string
    description: |
      The load balancer on which the policy is defined.
    willReplaceOnChanges: true
  policyAttributes:
    type: array
    items:
      $ref: "#/types/aws:elasticloadbalancing/LoadBalancerPolicyPolicyAttribute:LoadBalancerPolicyPolicyAttribute"
    description: |
      Policy attribute to apply to the policy.
  policyName:
    type: string
    description: |
      The name of the load balancer policy.
    willReplaceOnChanges: true
  policyTypeName:
    type: string
    description: |
      The policy type.
    willReplaceOnChanges: true
requiredInputs:
  - loadBalancerName
  - policyName
  - policyTypeName
stateInputs:
  description: |
    Input properties used for looking up and filtering LoadBalancerPolicy resources.
  properties:
    loadBalancerName:
      type: string
      description: |
        The load balancer on which the policy is defined.
      willReplaceOnChanges: true
    policyAttributes:
      type: array
      items:
        $ref: "#/types/aws:elasticloadbalancing/LoadBalancerPolicyPolicyAttribute:LoadBalancerPolicyPolicyAttribute"
      description: |
        Policy attribute to apply to the policy.
    policyName:
      type: string
      description: |
        The name of the load balancer policy.
      willReplaceOnChanges: true
    policyTypeName:
      type: string
      description: |
        The policy type.
      willReplaceOnChanges: true
  type: object
deprecationMessage: aws.elasticloadbalancing.LoadBalancerPolicy has been deprecated in favor of aws.elb.LoadBalancerPolicy
