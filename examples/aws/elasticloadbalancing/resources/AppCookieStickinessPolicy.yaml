$token: aws:elasticloadbalancing/appCookieStickinessPolicy:AppCookieStickinessPolicy
properties:
  cookieName:
    type: string
    description: |
      Application cookie whose lifetime the ELB's cookie should follow.
  lbPort:
    type: integer
    description: |
      Load balancer port to which the policy
      should be applied. This must be an active listener on the load
      balancer.
  loadBalancer:
    type: string
    description: |
      Name of load balancer to which the policy
      should be attached.
  name:
    type: string
    description: |
      Name of the stickiness policy.
required:
  - cookieName
  - lbPort
  - loadBalancer
  - name
inputProperties:
  cookieName:
    type: string
    description: |
      Application cookie whose lifetime the ELB's cookie should follow.
    willReplaceOnChanges: true
  lbPort:
    type: integer
    description: |
      Load balancer port to which the policy
      should be applied. This must be an active listener on the load
      balancer.
    willReplaceOnChanges: true
  loadBalancer:
    type: string
    description: |
      Name of load balancer to which the policy
      should be attached.
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      Name of the stickiness policy.
    willReplaceOnChanges: true
requiredInputs:
  - cookieName
  - lbPort
  - loadBalancer
stateInputs:
  description: |
    Input properties used for looking up and filtering AppCookieStickinessPolicy resources.
  properties:
    cookieName:
      type: string
      description: |
        Application cookie whose lifetime the ELB's cookie should follow.
      willReplaceOnChanges: true
    lbPort:
      type: integer
      description: |
        Load balancer port to which the policy
        should be applied. This must be an active listener on the load
        balancer.
      willReplaceOnChanges: true
    loadBalancer:
      type: string
      description: |
        Name of load balancer to which the policy
        should be attached.
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        Name of the stickiness policy.
      willReplaceOnChanges: true
  type: object
deprecationMessage: aws.elasticloadbalancing.AppCookieStickinessPolicy has been deprecated in favor of aws.elb.AppCookieStickinessPolicy
