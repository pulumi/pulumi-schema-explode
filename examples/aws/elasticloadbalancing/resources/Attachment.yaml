$token: aws:elasticloadbalancing/attachment:Attachment
properties:
  elb:
    type: string
    description: |
      The name of the ELB.
  instance:
    type: string
    description: |
      Instance ID to place in the ELB pool.
required:
  - elb
  - instance
inputProperties:
  elb:
    type: string
    description: |
      The name of the ELB.
    willReplaceOnChanges: true
  instance:
    type: string
    description: |
      Instance ID to place in the ELB pool.
    willReplaceOnChanges: true
requiredInputs:
  - elb
  - instance
stateInputs:
  description: |
    Input properties used for looking up and filtering Attachment resources.
  properties:
    elb:
      type: string
      description: |
        The name of the ELB.
      willReplaceOnChanges: true
    instance:
      type: string
      description: |
        Instance ID to place in the ELB pool.
      willReplaceOnChanges: true
  type: object
deprecationMessage: aws.elasticloadbalancing.Attachment has been deprecated in favor of aws.elb.Attachment
