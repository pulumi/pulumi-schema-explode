$token: aws:memorydb/getSubnetGroup:getSubnetGroup
inputs:
  description: |
    A collection of arguments for invoking getSubnetGroup.
  properties:
    name:
      type: string
      description: |
        Name of the subnet group.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags assigned to the subnet group.
  type: object
  required:
    - name
outputs:
  description: |
    A collection of values returned by getSubnetGroup.
  properties:
    arn:
      type: string
      description: |
        ARN of the subnet group.
    description:
      type: string
      description: |
        Description of the subnet group.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    name:
      type: string
    subnetIds:
      type: array
      items:
        type: string
      description: |
        Set of VPC Subnet ID-s of the subnet group.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags assigned to the subnet group.
    vpcId:
      type: string
      description: |
        VPC in which the subnet group exists.
  type: object
  required:
    - arn
    - description
    - name
    - subnetIds
    - tags
    - vpcId
    - id
