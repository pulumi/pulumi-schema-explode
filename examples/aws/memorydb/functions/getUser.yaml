$token: aws:memorydb/getUser:getUser
inputs:
  description: |
    A collection of arguments for invoking getUser.
  properties:
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags assigned to the subnet group.
    userName:
      type: string
      description: |
        Name of the user.
  type: object
  required:
    - userName
outputs:
  description: |
    A collection of values returned by getUser.
  properties:
    accessString:
      type: string
      description: |
        Access permissions string used for this user.
    arn:
      type: string
      description: |
        ARN of the user.
    authenticationModes:
      type: array
      items:
        $ref: "#/types/aws:memorydb/getUserAuthenticationMode:getUserAuthenticationMode"
      description: |
        Denotes the user's authentication properties.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    minimumEngineVersion:
      type: string
      description: |
        The minimum engine version supported for the user.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags assigned to the subnet group.
    userName:
      type: string
  type: object
  required:
    - accessString
    - arn
    - authenticationModes
    - minimumEngineVersion
    - tags
    - userName
    - id
