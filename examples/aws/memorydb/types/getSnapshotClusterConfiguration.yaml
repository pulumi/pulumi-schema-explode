$token: aws:memorydb/getSnapshotClusterConfiguration:getSnapshotClusterConfiguration
properties:
  description:
    type: string
    description: |
      Description for the cluster.
    language:
      python:
        mapCase: false
  engineVersion:
    type: string
    description: |
      Version number of the Redis engine used by the cluster.
    language:
      python:
        mapCase: false
  maintenanceWindow:
    type: string
    description: |
      The weekly time range during which maintenance on the cluster is performed.
    language:
      python:
        mapCase: false
  name:
    type: string
    description: |
      Name of the snapshot.
    language:
      python:
        mapCase: false
  nodeType:
    type: string
    description: |
      Compute and memory capacity of the nodes in the cluster.
    language:
      python:
        mapCase: false
  numShards:
    type: integer
    description: |
      Number of shards in the cluster.
    language:
      python:
        mapCase: false
  parameterGroupName:
    type: string
    description: |
      Name of the parameter group associated with the cluster.
    language:
      python:
        mapCase: false
  port:
    type: integer
    description: |
      Port number on which the cluster accepts connections.
    language:
      python:
        mapCase: false
  snapshotRetentionLimit:
    type: integer
    description: |
      Number of days for which MemoryDB retains automatic snapshots before deleting them.
    language:
      python:
        mapCase: false
  snapshotWindow:
    type: string
    description: |
      The daily time range (in UTC) during which MemoryDB begins taking a daily snapshot of the shard.
    language:
      python:
        mapCase: false
  subnetGroupName:
    type: string
    description: |
      Name of the subnet group used by the cluster.
    language:
      python:
        mapCase: false
  topicArn:
    type: string
    description: |
      ARN of the SNS topic to which cluster notifications are sent.
    language:
      python:
        mapCase: false
  vpcId:
    type: string
    description: |
      The VPC in which the cluster exists.
    language:
      python:
        mapCase: false
type: object
required:
  - description
  - engineVersion
  - maintenanceWindow
  - name
  - nodeType
  - numShards
  - parameterGroupName
  - port
  - snapshotRetentionLimit
  - snapshotWindow
  - subnetGroupName
  - topicArn
  - vpcId
language:
  nodejs:
    requiredInputs: []
