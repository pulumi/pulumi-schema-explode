$token: aws:memorydb/UserAuthenticationMode:UserAuthenticationMode
properties:
  passwordCount:
    type: integer
    description: |
      The number of passwords belonging to the user.
    language:
      python:
        mapCase: false
  passwords:
    type: array
    items:
      type: string
    description: |
      The set of passwords used for authentication. You can create up to two passwords for each user.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      Indicates whether the user requires a password to authenticate. Must be set to `password`.
    language:
      python:
        mapCase: false
type: object
required:
  - passwords
  - type
language:
  nodejs:
    requiredOutputs:
      - passwordCount
      - passwords
      - type
