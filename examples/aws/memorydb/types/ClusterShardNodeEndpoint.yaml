$token: aws:memorydb/ClusterShardNodeEndpoint:ClusterShardNodeEndpoint
properties:
  address:
    type: string
    description: |
      DNS hostname of the node.
    language:
      python:
        mapCase: false
  port:
    type: integer
    description: |
      The port number on which each of the nodes accepts connections. Defaults to `6379`.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - address
      - port
