$token: aws:memorydb/getClusterShard:getClusterShard
properties:
  name:
    type: string
    description: |
      Name of the cluster.
    language:
      python:
        mapCase: false
  nodes:
    type: array
    items:
      $ref: "#/types/aws:memorydb/getClusterShardNode:getClusterShardNode"
    description: |
      Set of nodes in this shard.
    language:
      python:
        mapCase: false
  numNodes:
    type: integer
    description: |
      Number of individual nodes in this shard.
    language:
      python:
        mapCase: false
  slots:
    type: string
    description: |
      Keyspace for this shard. Example: `0-16383`.
    language:
      python:
        mapCase: false
type: object
required:
  - name
  - nodes
  - numNodes
  - slots
language:
  nodejs:
    requiredInputs: []
