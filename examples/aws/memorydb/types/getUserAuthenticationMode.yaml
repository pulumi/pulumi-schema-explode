$token: aws:memorydb/getUserAuthenticationMode:getUserAuthenticationMode
properties:
  passwordCount:
    type: integer
    description: |
      The number of passwords belonging to the user.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      Whether the user requires a password to authenticate.
    language:
      python:
        mapCase: false
type: object
required:
  - passwordCount
  - type
language:
  nodejs:
    requiredInputs: []
