$token: aws:memorydb/getClusterClusterEndpoint:getClusterClusterEndpoint
properties:
  address:
    type: string
    description: |
      DNS hostname of the node.
    language:
      python:
        mapCase: false
  port:
    type: integer
    description: |
      Port number that this node is listening on.
    language:
      python:
        mapCase: false
type: object
required:
  - address
  - port
language:
  nodejs:
    requiredInputs: []
