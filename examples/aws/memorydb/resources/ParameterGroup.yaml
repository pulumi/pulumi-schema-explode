$token: aws:memorydb/parameterGroup:ParameterGroup
properties:
  arn:
    type: string
    description: |
      The ARN of the parameter group.
  description:
    type: string
  family:
    type: string
    description: |
      The engine version that the parameter group can be used with.
  name:
    type: string
    description: |
      The name of the parameter.
  namePrefix:
    type: string
    description: |
      Creates a unique name beginning with the specified prefix. Conflicts with `name`.
  parameters:
    type: array
    items:
      $ref: "#/types/aws:memorydb/ParameterGroupParameter:ParameterGroupParameter"
    description: |
      Set of MemoryDB parameters to apply. Any parameters not specified will fall back to their family defaults. Detailed below.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - family
  - name
  - namePrefix
  - tagsAll
inputProperties:
  description:
    type: string
    willReplaceOnChanges: true
  family:
    type: string
    description: |
      The engine version that the parameter group can be used with.
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      The name of the parameter.
    willReplaceOnChanges: true
  namePrefix:
    type: string
    description: |
      Creates a unique name beginning with the specified prefix. Conflicts with `name`.
    willReplaceOnChanges: true
  parameters:
    type: array
    items:
      $ref: "#/types/aws:memorydb/ParameterGroupParameter:ParameterGroupParameter"
    description: |
      Set of MemoryDB parameters to apply. Any parameters not specified will fall back to their family defaults. Detailed below.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - family
stateInputs:
  description: |
    Input properties used for looking up and filtering ParameterGroup resources.
  properties:
    arn:
      type: string
      description: |
        The ARN of the parameter group.
    description:
      type: string
      willReplaceOnChanges: true
    family:
      type: string
      description: |
        The engine version that the parameter group can be used with.
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        The name of the parameter.
      willReplaceOnChanges: true
    namePrefix:
      type: string
      description: |
        Creates a unique name beginning with the specified prefix. Conflicts with `name`.
      willReplaceOnChanges: true
    parameters:
      type: array
      items:
        $ref: "#/types/aws:memorydb/ParameterGroupParameter:ParameterGroupParameter"
      description: |
        Set of MemoryDB parameters to apply. Any parameters not specified will fall back to their family defaults. Detailed below.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
