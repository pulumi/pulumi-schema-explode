$token: aws:memorydb/snapshot:Snapshot
properties:
  arn:
    type: string
    description: |
      The ARN of the snapshot.
  clusterConfigurations:
    type: array
    items:
      $ref: "#/types/aws:memorydb/SnapshotClusterConfiguration:SnapshotClusterConfiguration"
    description: |
      The configuration of the cluster from which the snapshot was taken.
  clusterName:
    type: string
    description: |
      Name of the MemoryDB cluster to take a snapshot of.
  kmsKeyArn:
    type: string
    description: |
      ARN of the KMS key used to encrypt the snapshot at rest.
  name:
    type: string
    description: |
      Name of the cluster.
  namePrefix:
    type: string
    description: |
      Creates a unique name beginning with the specified prefix. Conflicts with `name`.
  source:
    type: string
    description: |
      Indicates whether the snapshot is from an automatic backup (`automated`) or was created manually (`manual`).
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - clusterConfigurations
  - clusterName
  - name
  - namePrefix
  - source
  - tagsAll
inputProperties:
  clusterName:
    type: string
    description: |
      Name of the MemoryDB cluster to take a snapshot of.
    willReplaceOnChanges: true
  kmsKeyArn:
    type: string
    description: |
      ARN of the KMS key used to encrypt the snapshot at rest.
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      Name of the cluster.
    willReplaceOnChanges: true
  namePrefix:
    type: string
    description: |
      Creates a unique name beginning with the specified prefix. Conflicts with `name`.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - clusterName
stateInputs:
  description: |
    Input properties used for looking up and filtering Snapshot resources.
  properties:
    arn:
      type: string
      description: |
        The ARN of the snapshot.
    clusterConfigurations:
      type: array
      items:
        $ref: "#/types/aws:memorydb/SnapshotClusterConfiguration:SnapshotClusterConfiguration"
      description: |
        The configuration of the cluster from which the snapshot was taken.
    clusterName:
      type: string
      description: |
        Name of the MemoryDB cluster to take a snapshot of.
      willReplaceOnChanges: true
    kmsKeyArn:
      type: string
      description: |
        ARN of the KMS key used to encrypt the snapshot at rest.
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        Name of the cluster.
      willReplaceOnChanges: true
    namePrefix:
      type: string
      description: |
        Creates a unique name beginning with the specified prefix. Conflicts with `name`.
      willReplaceOnChanges: true
    source:
      type: string
      description: |
        Indicates whether the snapshot is from an automatic backup (`automated`) or was created manually (`manual`).
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
