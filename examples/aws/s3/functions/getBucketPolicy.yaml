$token: aws:s3/getBucketPolicy:getBucketPolicy
inputs:
  description: |
    A collection of arguments for invoking getBucketPolicy.
  properties:
    bucket:
      type: string
      description: |
        Bucket name.
  type: object
  required:
    - bucket
outputs:
  description: |
    A collection of values returned by getBucketPolicy.
  properties:
    bucket:
      type: string
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    policy:
      type: string
      description: |
        IAM bucket policy.
  type: object
  required:
    - bucket
    - policy
    - id
