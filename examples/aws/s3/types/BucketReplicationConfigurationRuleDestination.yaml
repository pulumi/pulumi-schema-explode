$token: aws:s3/BucketReplicationConfigurationRuleDestination:BucketReplicationConfigurationRuleDestination
properties:
  accessControlTranslation:
    $ref: "#/types/aws:s3/BucketReplicationConfigurationRuleDestinationAccessControlTranslation:BucketReplicationConfigurationRuleDestinationAccessControlTranslation"
    description: |
      Specifies the overrides to use for object owners on replication. Must be used in conjunction with `account_id` owner override configuration.
    language:
      python:
        mapCase: false
  accountId:
    type: string
    description: |
      The Account ID to use for overriding the object owner on replication. Must be used in conjunction with `access_control_translation` override configuration.
    language:
      python:
        mapCase: false
  bucket:
    type: string
    description: |
      The ARN of the S3 bucket where you want Amazon S3 to store replicas of the object identified by the rule.
    language:
      python:
        mapCase: false
  metrics:
    $ref: "#/types/aws:s3/BucketReplicationConfigurationRuleDestinationMetrics:BucketReplicationConfigurationRuleDestinationMetrics"
    description: |
      Enables replication metrics (required for S3 RTC) (documented below).
    language:
      python:
        mapCase: false
  replicaKmsKeyId:
    type: string
    description: |
      Destination KMS encryption key ARN for SSE-KMS replication. Must be used in conjunction with
      `sse_kms_encrypted_objects` source selection criteria.
    language:
      python:
        mapCase: false
  replicationTime:
    $ref: "#/types/aws:s3/BucketReplicationConfigurationRuleDestinationReplicationTime:BucketReplicationConfigurationRuleDestinationReplicationTime"
    description: |
      Enables S3 Replication Time Control (S3 RTC) (documented below).
    language:
      python:
        mapCase: false
  storageClass:
    type: string
    description: |
      The [storage class](https://docs.aws.amazon.com/AmazonS3/latest/API/API_Destination.html#AmazonS3-Type-Destination-StorageClass) used to store the object. By default, Amazon S3 uses the storage class of the source object to create the object replica.
    language:
      python:
        mapCase: false
type: object
required:
  - bucket
