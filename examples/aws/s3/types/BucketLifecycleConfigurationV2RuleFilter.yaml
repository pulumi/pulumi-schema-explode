$token: aws:s3/BucketLifecycleConfigurationV2RuleFilter:BucketLifecycleConfigurationV2RuleFilter
properties:
  and:
    $ref: "#/types/aws:s3/BucketLifecycleConfigurationV2RuleFilterAnd:BucketLifecycleConfigurationV2RuleFilterAnd"
    description: |
      Configuration block used to apply a logical `AND` to two or more predicates documented below. The Lifecycle Rule will apply to any object matching all the predicates configured inside the `and` block.
    language:
      python:
        mapCase: false
  objectSizeGreaterThan:
    type: string
    description: |
      Minimum object size (in bytes) to which the rule applies.
    language:
      python:
        mapCase: false
  objectSizeLessThan:
    type: string
    description: |
      Maximum object size (in bytes) to which the rule applies.
    language:
      python:
        mapCase: false
  prefix:
    type: string
    description: |
      Prefix identifying one or more objects to which the rule applies. Defaults to an empty string (`""`) if not specified.
    language:
      python:
        mapCase: false
  tag:
    $ref: "#/types/aws:s3/BucketLifecycleConfigurationV2RuleFilterTag:BucketLifecycleConfigurationV2RuleFilterTag"
    description: |
      A configuration block for specifying a tag key and value documented below.
    language:
      python:
        mapCase: false
type: object
