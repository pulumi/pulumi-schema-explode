$token: aws:s3/AnalyticsConfigurationStorageClassAnalysisDataExportDestinationS3BucketDestination:AnalyticsConfigurationStorageClassAnalysisDataExportDestinationS3BucketDestination
properties:
  bucketAccountId:
    type: string
    description: |
      The account ID that owns the destination bucket.
    language:
      python:
        mapCase: false
  bucketArn:
    type: string
    description: |
      The ARN of the destination bucket.
    language:
      python:
        mapCase: false
  format:
    type: string
    description: |
      The output format of exported analytics data. Allowed values: `CSV`. Default value: `CSV`.
    language:
      python:
        mapCase: false
  prefix:
    type: string
    description: |
      Object prefix for filtering.
    language:
      python:
        mapCase: false
type: object
required:
  - bucketArn
