$token: aws:s3/BucketWebsiteConfigurationV2RedirectAllRequestsTo:BucketWebsiteConfigurationV2RedirectAllRequestsTo
properties:
  hostName:
    type: string
    description: |
      Name of the host where requests are redirected.
    language:
      python:
        mapCase: false
  protocol:
    type: string
    description: |
      Protocol to use when redirecting requests. The default is the protocol that is used in the original request. Valid values: `http`, `https`.
    language:
      python:
        mapCase: false
type: object
required:
  - hostName
