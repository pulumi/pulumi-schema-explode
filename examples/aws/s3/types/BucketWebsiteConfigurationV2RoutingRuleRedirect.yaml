$token: aws:s3/BucketWebsiteConfigurationV2RoutingRuleRedirect:BucketWebsiteConfigurationV2RoutingRuleRedirect
properties:
  hostName:
    type: string
    description: |
      The host name to use in the redirect request.
    language:
      python:
        mapCase: false
  httpRedirectCode:
    type: string
    description: |
      The HTTP redirect code to use on the response.
    language:
      python:
        mapCase: false
  protocol:
    type: string
    description: |
      Protocol to use when redirecting requests. The default is the protocol that is used in the original request. Valid values: `http`, `https`.
    language:
      python:
        mapCase: false
  replaceKeyPrefixWith:
    type: string
    description: |
      The object key prefix to use in the redirect request. For example, to redirect requests for all pages with prefix `docs/` (objects in the `docs/` folder) to `documents/`, you can set a `condition` block with `key_prefix_equals` set to `docs/` and in the `redirect` set `replace_key_prefix_with` to `/documents`.
    language:
      python:
        mapCase: false
  replaceKeyWith:
    type: string
    description: |
      The specific object key to use in the redirect request. For example, redirect request to `error.html`.
    language:
      python:
        mapCase: false
type: object
