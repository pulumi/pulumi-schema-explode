$token: aws:s3/BucketObjectLockConfigurationV2RuleDefaultRetention:BucketObjectLockConfigurationV2RuleDefaultRetention
properties:
  days:
    type: integer
    description: |
      The number of days that you want to specify for the default retention period.
    language:
      python:
        mapCase: false
  mode:
    type: string
    description: |
      The default Object Lock retention mode you want to apply to new objects placed in the specified bucket. Valid values: `COMPLIANCE`, `GOVERNANCE`.
    language:
      python:
        mapCase: false
  years:
    type: integer
    description: |
      The number of years that you want to specify for the default retention period.
    language:
      python:
        mapCase: false
type: object
