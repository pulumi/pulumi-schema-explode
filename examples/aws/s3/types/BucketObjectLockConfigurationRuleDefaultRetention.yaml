$token: aws:s3/BucketObjectLockConfigurationRuleDefaultRetention:BucketObjectLockConfigurationRuleDefaultRetention
properties:
  days:
    type: integer
    description: |
      The number of days that you want to specify for the default retention period.
    language:
      python:
        mapCase: false
  mode:
    type: string
    description: |
      The default Object Lock retention mode you want to apply to new objects placed in this bucket. Valid values are `GOVERNANCE` and `COMPLIANCE`.
    language:
      python:
        mapCase: false
  years:
    type: integer
    description: |
      The number of years that you want to specify for the default retention period.
    language:
      python:
        mapCase: false
type: object
required:
  - mode
