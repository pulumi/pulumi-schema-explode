$token: aws:s3/ObjectCopyGrant:ObjectCopyGrant
properties:
  email:
    type: string
    description: |
      Email address of the grantee. Used only when `type` is `AmazonCustomerByEmail`.
    language:
      python:
        mapCase: false
  id:
    type: string
    description: |
      The canonical user ID of the grantee. Used only when `type` is `CanonicalUser`.
    language:
      python:
        mapCase: false
  permissions:
    type: array
    items:
      type: string
    description: |
      List of permissions to grant to grantee. Valid values are `READ`, `READ_ACP`, `WRITE_ACP`, `FULL_CONTROL`.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      - Type of grantee. Valid values are `CanonicalUser`, `Group`, and `AmazonCustomerByEmail`.
    language:
      python:
        mapCase: false
  uri:
    type: string
    description: |
      URI of the grantee group. Used only when `type` is `Group`.
    language:
      python:
        mapCase: false
type: object
required:
  - permissions
  - type
