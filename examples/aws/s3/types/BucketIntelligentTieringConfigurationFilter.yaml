$token: aws:s3/BucketIntelligentTieringConfigurationFilter:BucketIntelligentTieringConfigurationFilter
properties:
  prefix:
    type: string
    description: |
      An object key name prefix that identifies the subset of objects to which the configuration applies.
    language:
      python:
        mapCase: false
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      All of these tags must exist in the object's tag set in order for the configuration to apply.
    language:
      python:
        mapCase: false
type: object
