$token: aws:s3/BucketIntelligentTieringConfigurationTiering:BucketIntelligentTieringConfigurationTiering
properties:
  accessTier:
    type: string
    description: |
      S3 Intelligent-Tiering access tier. Valid values: `ARCHIVE_ACCESS`, `DEEP_ARCHIVE_ACCESS`.
    language:
      python:
        mapCase: false
  days:
    type: integer
    description: |
      The number of consecutive days of no access after which an object will be eligible to be transitioned to the corresponding tier.
    language:
      python:
        mapCase: false
type: object
required:
  - accessTier
  - days
