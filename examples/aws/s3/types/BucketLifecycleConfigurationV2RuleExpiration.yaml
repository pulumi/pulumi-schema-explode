$token: aws:s3/BucketLifecycleConfigurationV2RuleExpiration:BucketLifecycleConfigurationV2RuleExpiration
properties:
  date:
    type: string
    description: |
      The date the object is to be moved or deleted. Should be in [RFC3339 format](https://tools.ietf.org/html/rfc3339#section-5.8).
    language:
      python:
        mapCase: false
  days:
    type: integer
    description: |
      The lifetime, in days, of the objects that are subject to the rule. The value must be a non-zero positive integer.
    language:
      python:
        mapCase: false
  expiredObjectDeleteMarker:
    type: boolean
    description: |
      Indicates whether Amazon S3 will remove a delete marker with no noncurrent versions. If set to `true`, the delete marker will be expired; if set to `false` the policy takes no action.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - expiredObjectDeleteMarker
