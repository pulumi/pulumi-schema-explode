$token: aws:s3/InventoryFilter:InventoryFilter
properties:
  prefix:
    type: string
    description: |
      The prefix that an object must have to be included in the inventory results.
    language:
      python:
        mapCase: false
type: object
