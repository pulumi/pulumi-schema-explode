$token: aws:s3/BucketLifecycleConfigurationV2Rule:BucketLifecycleConfigurationV2Rule
properties:
  abortIncompleteMultipartUpload:
    $ref: "#/types/aws:s3/BucketLifecycleConfigurationV2RuleAbortIncompleteMultipartUpload:BucketLifecycleConfigurationV2RuleAbortIncompleteMultipartUpload"
    description: |
      Configuration block that specifies the days since the initiation of an incomplete multipart upload that Amazon S3 will wait before permanently removing all parts of the upload documented below.
    language:
      python:
        mapCase: false
  expiration:
    $ref: "#/types/aws:s3/BucketLifecycleConfigurationV2RuleExpiration:BucketLifecycleConfigurationV2RuleExpiration"
    description: |
      Configuration block that specifies the expiration for the lifecycle of the object in the form of date, days and, whether the object has a delete marker documented below.
    language:
      python:
        mapCase: false
  filter:
    $ref: "#/types/aws:s3/BucketLifecycleConfigurationV2RuleFilter:BucketLifecycleConfigurationV2RuleFilter"
    description: |
      Configuration block used to identify objects that a Lifecycle Rule applies to documented below. If not specified, the `rule` will default to using `prefix`.
    language:
      python:
        mapCase: false
  id:
    type: string
    description: |
      Unique identifier for the rule. The value cannot be longer than 255 characters.
    language:
      python:
        mapCase: false
  noncurrentVersionExpiration:
    $ref: "#/types/aws:s3/BucketLifecycleConfigurationV2RuleNoncurrentVersionExpiration:BucketLifecycleConfigurationV2RuleNoncurrentVersionExpiration"
    description: |
      Configuration block that specifies when noncurrent object versions expire documented below.
    language:
      python:
        mapCase: false
  noncurrentVersionTransitions:
    type: array
    items:
      $ref: "#/types/aws:s3/BucketLifecycleConfigurationV2RuleNoncurrentVersionTransition:BucketLifecycleConfigurationV2RuleNoncurrentVersionTransition"
    description: |
      Set of configuration blocks that specify the transition rule for the lifecycle rule that describes when noncurrent objects transition to a specific storage class documented below.
    language:
      python:
        mapCase: false
  prefix:
    type: string
    description: |
      **DEPRECATED** Use `filter` instead. This has been deprecated by Amazon S3. Prefix identifying one or more objects to which the rule applies. Defaults to an empty string (`""`) if `filter` is not specified.
    deprecationMessage: Use filter instead
    language:
      python:
        mapCase: false
  status:
    type: string
    description: |
      Whether the rule is currently being applied. Valid values: `Enabled` or `Disabled`.
    language:
      python:
        mapCase: false
  transitions:
    type: array
    items:
      $ref: "#/types/aws:s3/BucketLifecycleConfigurationV2RuleTransition:BucketLifecycleConfigurationV2RuleTransition"
    description: |
      Set of configuration blocks that specify when an Amazon S3 object transitions to a specified storage class documented below.
    language:
      python:
        mapCase: false
type: object
required:
  - id
  - status
