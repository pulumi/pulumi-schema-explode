$token: aws:s3/BucketAclV2AccessControlPolicyGrantGrantee:BucketAclV2AccessControlPolicyGrantGrantee
properties:
  displayName:
    type: string
    description: |
      The display name of the owner.
    language:
      python:
        mapCase: false
  emailAddress:
    type: string
    description: |
      Email address of the grantee. See [Regions and Endpoints](https://docs.aws.amazon.com/general/latest/gr/rande.html#s3_region) for supported AWS regions where this argument can be specified.
    language:
      python:
        mapCase: false
  id:
    type: string
    description: |
      The canonical user ID of the grantee.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      Type of grantee. Valid values: `CanonicalUser`, `AmazonCustomerByEmail`, `Group`.
    language:
      python:
        mapCase: false
  uri:
    type: string
    description: |
      URI of the grantee group.
    language:
      python:
        mapCase: false
type: object
required:
  - type
language:
  nodejs:
    requiredOutputs:
      - displayName
      - type
