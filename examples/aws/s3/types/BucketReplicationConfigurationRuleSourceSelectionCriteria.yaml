$token: aws:s3/BucketReplicationConfigurationRuleSourceSelectionCriteria:BucketReplicationConfigurationRuleSourceSelectionCriteria
properties:
  sseKmsEncryptedObjects:
    $ref: "#/types/aws:s3/BucketReplicationConfigurationRuleSourceSelectionCriteriaSseKmsEncryptedObjects:BucketReplicationConfigurationRuleSourceSelectionCriteriaSseKmsEncryptedObjects"
    description: |
      Match SSE-KMS encrypted objects (documented below). If specified, `replica_kms_key_id`
      in `destination` must be specified as well.
    language:
      python:
        mapCase: false
type: object
