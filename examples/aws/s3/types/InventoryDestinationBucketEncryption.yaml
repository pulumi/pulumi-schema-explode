$token: aws:s3/InventoryDestinationBucketEncryption:InventoryDestinationBucketEncryption
properties:
  sseKms:
    $ref: "#/types/aws:s3/InventoryDestinationBucketEncryptionSseKms:InventoryDestinationBucketEncryptionSseKms"
    description: |
      Specifies to use server-side encryption with AWS KMS-managed keys to encrypt the inventory file (documented below).
    language:
      python:
        mapCase: false
  sseS3:
    $ref: "#/types/aws:s3/InventoryDestinationBucketEncryptionSseS3:InventoryDestinationBucketEncryptionSseS3"
    description: |
      Specifies to use server-side encryption with Amazon S3-managed keys (SSE-S3) to encrypt the inventory file.
    language:
      python:
        mapCase: false
type: object
