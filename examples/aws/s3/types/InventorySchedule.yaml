$token: aws:s3/InventorySchedule:InventorySchedule
properties:
  frequency:
    type: string
    description: |
      Specifies how frequently inventory results are produced. Valid values: `Daily`, `Weekly`.
    language:
      python:
        mapCase: false
type: object
required:
  - frequency
