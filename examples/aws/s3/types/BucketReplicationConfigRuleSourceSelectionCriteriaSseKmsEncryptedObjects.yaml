$token: aws:s3/BucketReplicationConfigRuleSourceSelectionCriteriaSseKmsEncryptedObjects:BucketReplicationConfigRuleSourceSelectionCriteriaSseKmsEncryptedObjects
properties:
  status:
    type: string
    description: |
      Whether the existing objects should be replicated. Either `"Enabled"` or `"Disabled"`.
    language:
      python:
        mapCase: false
type: object
required:
  - status
