$token: aws:s3/BucketV2ReplicationConfigurationRuleSourceSelectionCriteria:BucketV2ReplicationConfigurationRuleSourceSelectionCriteria
properties:
  sseKmsEncryptedObjects:
    type: array
    items:
      $ref: "#/types/aws:s3/BucketV2ReplicationConfigurationRuleSourceSelectionCriteriaSseKmsEncryptedObject:BucketV2ReplicationConfigurationRuleSourceSelectionCriteriaSseKmsEncryptedObject"
    description: |
      Match SSE-KMS encrypted objects (documented below). If specified, `replica_kms_key_id`
      in `destination` must be specified as well.
    language:
      python:
        mapCase: false
type: object
