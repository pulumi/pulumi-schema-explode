$token: aws:s3/BucketLifecycleRule:BucketLifecycleRule
properties:
  abortIncompleteMultipartUploadDays:
    type: integer
    description: |
      Specifies the number of days after initiating a multipart upload when the multipart upload must be completed.
    language:
      python:
        mapCase: false
  enabled:
    type: boolean
    description: |
      Specifies lifecycle rule status.
    language:
      python:
        mapCase: false
  expiration:
    $ref: "#/types/aws:s3/BucketLifecycleRuleExpiration:BucketLifecycleRuleExpiration"
    description: |
      Specifies a period in the object's expire (documented below).
    language:
      python:
        mapCase: false
  id:
    type: string
    description: |
      Unique identifier for the rule. Must be less than or equal to 255 characters in length.
    language:
      python:
        mapCase: false
  noncurrentVersionExpiration:
    $ref: "#/types/aws:s3/BucketLifecycleRuleNoncurrentVersionExpiration:BucketLifecycleRuleNoncurrentVersionExpiration"
    description: |
      Specifies when noncurrent object versions expire (documented below).
    language:
      python:
        mapCase: false
  noncurrentVersionTransitions:
    type: array
    items:
      $ref: "#/types/aws:s3/BucketLifecycleRuleNoncurrentVersionTransition:BucketLifecycleRuleNoncurrentVersionTransition"
    description: |
      Specifies when noncurrent object versions transitions (documented below).
    language:
      python:
        mapCase: false
  prefix:
    type: string
    description: |
      Object key prefix identifying one or more objects to which the rule applies.
    language:
      python:
        mapCase: false
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Specifies object tags key and value.
    language:
      python:
        mapCase: false
  transitions:
    type: array
    items:
      $ref: "#/types/aws:s3/BucketLifecycleRuleTransition:BucketLifecycleRuleTransition"
    description: |
      Specifies a period in the object's transitions (documented below).
    language:
      python:
        mapCase: false
type: object
required:
  - enabled
language:
  nodejs:
    requiredOutputs:
      - enabled
      - id
