$token: aws:s3/BucketV2ServerSideEncryptionConfiguration:BucketV2ServerSideEncryptionConfiguration
properties:
  rules:
    type: array
    items:
      $ref: "#/types/aws:s3/BucketV2ServerSideEncryptionConfigurationRule:BucketV2ServerSideEncryptionConfigurationRule"
    description: |
      A single object for server-side encryption by default configuration. (documented below)
    language:
      python:
        mapCase: false
type: object
required:
  - rules
