$token: aws:s3/BucketWebsiteConfigurationV2RoutingRule:BucketWebsiteConfigurationV2RoutingRule
properties:
  condition:
    $ref: "#/types/aws:s3/BucketWebsiteConfigurationV2RoutingRuleCondition:BucketWebsiteConfigurationV2RoutingRuleCondition"
    description: |
      A configuration block for describing a condition that must be met for the specified redirect to apply detailed below.
    language:
      python:
        mapCase: false
  redirect:
    $ref: "#/types/aws:s3/BucketWebsiteConfigurationV2RoutingRuleRedirect:BucketWebsiteConfigurationV2RoutingRuleRedirect"
    description: |
      A configuration block for redirect information detailed below.
    language:
      python:
        mapCase: false
type: object
required:
  - redirect
