$token: aws:s3/InventoryDestination:InventoryDestination
properties:
  bucket:
    $ref: "#/types/aws:s3/InventoryDestinationBucket:InventoryDestinationBucket"
    description: |
      The S3 bucket configuration where inventory results are published (documented below).
    language:
      python:
        mapCase: false
type: object
required:
  - bucket
