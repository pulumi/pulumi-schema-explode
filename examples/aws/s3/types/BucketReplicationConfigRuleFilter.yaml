$token: aws:s3/BucketReplicationConfigRuleFilter:BucketReplicationConfigRuleFilter
properties:
  and:
    $ref: "#/types/aws:s3/BucketReplicationConfigRuleFilterAnd:BucketReplicationConfigRuleFilterAnd"
    description: |
      A configuration block for specifying rule filters. This element is required only if you specify more than one filter. See and below for more details.
    language:
      python:
        mapCase: false
  prefix:
    type: string
    description: |
      An object key name prefix that identifies subset of objects to which the rule applies. Must be less than or equal to 1024 characters in length.
    language:
      python:
        mapCase: false
  tag:
    $ref: "#/types/aws:s3/BucketReplicationConfigRuleFilterTag:BucketReplicationConfigRuleFilterTag"
    description: |
      A configuration block for specifying a tag key and value documented below.
    language:
      python:
        mapCase: false
type: object
