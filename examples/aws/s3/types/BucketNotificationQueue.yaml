$token: aws:s3/BucketNotificationQueue:BucketNotificationQueue
properties:
  events:
    type: array
    items:
      type: string
    description: |
      [Event](http://docs.aws.amazon.com/AmazonS3/latest/dev/NotificationHowTo.html#notification-how-to-event-types-and-destinations) for which to send notifications.
    language:
      python:
        mapCase: false
  filterPrefix:
    type: string
    description: |
      Object key name prefix.
    language:
      python:
        mapCase: false
  filterSuffix:
    type: string
    description: |
      Object key name suffix.
    language:
      python:
        mapCase: false
  id:
    type: string
    description: |
      Unique identifier for each of the notification configurations.
    language:
      python:
        mapCase: false
  queueArn:
    type: string
    description: |
      SQS queue ARN.
    language:
      python:
        mapCase: false
type: object
required:
  - events
  - queueArn
language:
  nodejs:
    requiredOutputs:
      - events
      - id
      - queueArn
