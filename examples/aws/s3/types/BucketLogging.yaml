$token: aws:s3/BucketLogging:BucketLogging
properties:
  targetBucket:
    type: string
    description: |
      The name of the bucket that will receive the log objects.
    language:
      python:
        mapCase: false
  targetPrefix:
    type: string
    description: |
      To specify a key prefix for log objects.
    language:
      python:
        mapCase: false
type: object
required:
  - targetBucket
