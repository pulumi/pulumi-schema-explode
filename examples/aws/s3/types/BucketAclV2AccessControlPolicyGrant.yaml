$token: aws:s3/BucketAclV2AccessControlPolicyGrant:BucketAclV2AccessControlPolicyGrant
properties:
  grantee:
    $ref: "#/types/aws:s3/BucketAclV2AccessControlPolicyGrantGrantee:BucketAclV2AccessControlPolicyGrantGrantee"
    description: |
      Configuration block for the person being granted permissions documented below.
    language:
      python:
        mapCase: false
  permission:
    type: string
    description: |
      Logging permissions assigned to the grantee for the bucket.
    language:
      python:
        mapCase: false
type: object
required:
  - permission
