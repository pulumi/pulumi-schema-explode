$token: aws:s3/BucketAclV2AccessControlPolicy:BucketAclV2AccessControlPolicy
properties:
  grants:
    type: array
    items:
      $ref: "#/types/aws:s3/BucketAclV2AccessControlPolicyGrant:BucketAclV2AccessControlPolicyGrant"
    description: |
      Set of `grant` configuration blocks documented below.
    language:
      python:
        mapCase: false
  owner:
    $ref: "#/types/aws:s3/BucketAclV2AccessControlPolicyOwner:BucketAclV2AccessControlPolicyOwner"
    description: |
      Configuration block of the bucket owner's display name and ID documented below.
    language:
      python:
        mapCase: false
type: object
required:
  - owner
