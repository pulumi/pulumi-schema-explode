$token: aws:s3/BucketLifecycleConfigurationV2RuleNoncurrentVersionTransition:BucketLifecycleConfigurationV2RuleNoncurrentVersionTransition
properties:
  newerNoncurrentVersions:
    type: string
    description: |
      The number of noncurrent versions Amazon S3 will retain. Must be a non-zero positive integer.
    language:
      python:
        mapCase: false
  noncurrentDays:
    type: integer
    description: |
      The number of days an object is noncurrent before Amazon S3 can perform the associated action.
    language:
      python:
        mapCase: false
  storageClass:
    type: string
    description: |
      The class of storage used to store the object. Valid Values: `GLACIER`, `STANDARD_IA`, `ONEZONE_IA`, `INTELLIGENT_TIERING`, `DEEP_ARCHIVE`, `GLACIER_IR`.
    language:
      python:
        mapCase: false
type: object
required:
  - storageClass
