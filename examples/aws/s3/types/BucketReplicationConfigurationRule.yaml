$token: aws:s3/BucketReplicationConfigurationRule:BucketReplicationConfigurationRule
properties:
  deleteMarkerReplicationStatus:
    type: string
    description: |
      Whether delete markers are replicated. The only valid value is `Enabled`. To disable, omit this argument. This argument is only valid with V2 replication configurations (i.e., when `filter` is used).
    language:
      python:
        mapCase: false
  destination:
    $ref: "#/types/aws:s3/BucketReplicationConfigurationRuleDestination:BucketReplicationConfigurationRuleDestination"
    description: |
      Specifies the destination for the rule (documented below).
    language:
      python:
        mapCase: false
  filter:
    $ref: "#/types/aws:s3/BucketReplicationConfigurationRuleFilter:BucketReplicationConfigurationRuleFilter"
    description: |
      Filter that identifies subset of objects to which the replication rule applies (documented below).
    language:
      python:
        mapCase: false
  id:
    type: string
    description: |
      Unique identifier for the rule. Must be less than or equal to 255 characters in length.
    language:
      python:
        mapCase: false
  prefix:
    type: string
    description: |
      Object keyname prefix identifying one or more objects to which the rule applies. Must be less than or equal to 1024 characters in length.
    language:
      python:
        mapCase: false
  priority:
    type: integer
    description: |
      The priority associated with the rule. Priority should only be set if `filter` is configured. If not provided, defaults to `0`. Priority must be unique between multiple rules.
    language:
      python:
        mapCase: false
  sourceSelectionCriteria:
    $ref: "#/types/aws:s3/BucketReplicationConfigurationRuleSourceSelectionCriteria:BucketReplicationConfigurationRuleSourceSelectionCriteria"
    description: |
      Specifies special object selection criteria (documented below).
    language:
      python:
        mapCase: false
  status:
    type: string
    description: |
      The status of the rule. Either `Enabled` or `Disabled`. The rule is ignored if status is not Enabled.
    language:
      python:
        mapCase: false
type: object
required:
  - destination
  - status
