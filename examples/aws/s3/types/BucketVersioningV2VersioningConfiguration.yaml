$token: aws:s3/BucketVersioningV2VersioningConfiguration:BucketVersioningV2VersioningConfiguration
properties:
  mfaDelete:
    type: string
    description: |
      Specifies whether MFA delete is enabled in the bucket versioning configuration. Valid values: `Enabled` or `Disabled`.
    language:
      python:
        mapCase: false
  status:
    type: string
    description: |
      The versioning state of the bucket. Valid values: `Enabled`, `Suspended`, or `Disabled`. `Disabled` should only be used when creating or importing resources that correspond to unversioned S3 buckets.
    language:
      python:
        mapCase: false
type: object
required:
  - status
language:
  nodejs:
    requiredOutputs:
      - mfaDelete
      - status
