$token: aws:s3/BucketCorsRule:BucketCorsRule
properties:
  allowedHeaders:
    type: array
    items:
      type: string
    description: |
      Specifies which headers are allowed.
    language:
      python:
        mapCase: false
  allowedMethods:
    type: array
    items:
      type: string
    description: |
      Specifies which methods are allowed. Can be `GET`, `PUT`, `POST`, `DELETE` or `HEAD`.
    language:
      python:
        mapCase: false
  allowedOrigins:
    type: array
    items:
      type: string
    description: |
      Specifies which origins are allowed.
    language:
      python:
        mapCase: false
  exposeHeaders:
    type: array
    items:
      type: string
    description: |
      Specifies expose header in the response.
    language:
      python:
        mapCase: false
  maxAgeSeconds:
    type: integer
    description: |
      Specifies time in seconds that browser can cache the response for a preflight request.
    language:
      python:
        mapCase: false
type: object
required:
  - allowedMethods
  - allowedOrigins
