$token: aws:s3/BucketV2LifecycleRule:BucketV2LifecycleRule
properties:
  abortIncompleteMultipartUploadDays:
    type: integer
    description: |
      Specifies the number of days after initiating a multipart upload when the multipart upload must be completed.
    language:
      python:
        mapCase: false
  enabled:
    type: boolean
    description: |
      Specifies lifecycle rule status.
    language:
      python:
        mapCase: false
  expirations:
    type: array
    items:
      $ref: "#/types/aws:s3/BucketV2LifecycleRuleExpiration:BucketV2LifecycleRuleExpiration"
    description: |
      Specifies a period in the object's expire. See Expiration below for details.
    language:
      python:
        mapCase: false
  id:
    type: string
    description: |
      Unique identifier for the rule. Must be less than or equal to 255 characters in length.
    language:
      python:
        mapCase: false
  noncurrentVersionExpirations:
    type: array
    items:
      $ref: "#/types/aws:s3/BucketV2LifecycleRuleNoncurrentVersionExpiration:BucketV2LifecycleRuleNoncurrentVersionExpiration"
    description: |
      Specifies when noncurrent object versions expire. See Noncurrent Version Expiration below for details.
    language:
      python:
        mapCase: false
  noncurrentVersionTransitions:
    type: array
    items:
      $ref: "#/types/aws:s3/BucketV2LifecycleRuleNoncurrentVersionTransition:BucketV2LifecycleRuleNoncurrentVersionTransition"
    description: |
      Specifies when noncurrent object versions transitions. See Noncurrent Version Transition below for details.
    language:
      python:
        mapCase: false
  prefix:
    type: string
    description: |
      Object key prefix identifying one or more objects to which the rule applies.
    language:
      python:
        mapCase: false
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Specifies object tags key and value.
    language:
      python:
        mapCase: false
  transitions:
    type: array
    items:
      $ref: "#/types/aws:s3/BucketV2LifecycleRuleTransition:BucketV2LifecycleRuleTransition"
    description: |
      Specifies a period in the object's transitions. See Transition below for details.
    language:
      python:
        mapCase: false
type: object
required:
  - enabled
language:
  nodejs:
    requiredOutputs:
      - enabled
      - id
