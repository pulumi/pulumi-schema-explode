$token: aws:s3/BucketReplicationConfigRuleDestinationMetrics:BucketReplicationConfigRuleDestinationMetrics
properties:
  eventThreshold:
    $ref: "#/types/aws:s3/BucketReplicationConfigRuleDestinationMetricsEventThreshold:BucketReplicationConfigRuleDestinationMetricsEventThreshold"
    description: |
      A configuration block that specifies the time threshold for emitting the `s3:Replication:OperationMissedThreshold` event documented below.
    language:
      python:
        mapCase: false
  status:
    type: string
    description: |
      The status of the Destination Metrics. Either `"Enabled"` or `"Disabled"`.
    language:
      python:
        mapCase: false
type: object
required:
  - status
