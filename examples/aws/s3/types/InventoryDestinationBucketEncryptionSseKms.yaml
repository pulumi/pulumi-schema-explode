$token: aws:s3/InventoryDestinationBucketEncryptionSseKms:InventoryDestinationBucketEncryptionSseKms
properties:
  keyId:
    type: string
    description: |
      The ARN of the KMS customer master key (CMK) used to encrypt the inventory file.
    language:
      python:
        mapCase: false
type: object
required:
  - keyId
