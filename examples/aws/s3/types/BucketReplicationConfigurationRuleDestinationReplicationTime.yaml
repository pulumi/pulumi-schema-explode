$token: aws:s3/BucketReplicationConfigurationRuleDestinationReplicationTime:BucketReplicationConfigurationRuleDestinationReplicationTime
properties:
  minutes:
    type: integer
    description: |
      Threshold within which objects are to be replicated. The only valid value is `15`.
    language:
      python:
        mapCase: false
  status:
    type: string
    description: |
      The status of RTC. Either `Enabled` or `Disabled`.
    language:
      python:
        mapCase: false
type: object
