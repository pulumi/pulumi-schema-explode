$token: aws:s3/BucketMetricFilter:BucketMetricFilter
properties:
  prefix:
    type: string
    description: |
      Object prefix for filtering (singular).
    language:
      python:
        mapCase: false
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Object tags for filtering (up to 10).
    language:
      python:
        mapCase: false
type: object
