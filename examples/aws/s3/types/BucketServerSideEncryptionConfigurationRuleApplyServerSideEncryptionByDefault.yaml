$token: aws:s3/BucketServerSideEncryptionConfigurationRuleApplyServerSideEncryptionByDefault:BucketServerSideEncryptionConfigurationRuleApplyServerSideEncryptionByDefault
properties:
  kmsMasterKeyId:
    type: string
    description: |
      The AWS KMS master key ID used for the SSE-KMS encryption. This can only be used when you set the value of `sse_algorithm` as `aws:kms`. The default `aws/s3` AWS KMS master key is used if this element is absent while the `sse_algorithm` is `aws:kms`.
    language:
      python:
        mapCase: false
  sseAlgorithm:
    type: string
    description: |
      The server-side encryption algorithm to use. Valid values are `AES256` and `aws:kms`
    language:
      python:
        mapCase: false
type: object
required:
  - sseAlgorithm
