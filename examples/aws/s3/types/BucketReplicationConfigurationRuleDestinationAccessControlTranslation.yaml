$token: aws:s3/BucketReplicationConfigurationRuleDestinationAccessControlTranslation:BucketReplicationConfigurationRuleDestinationAccessControlTranslation
properties:
  owner:
    type: string
    description: |
      The override value for the owner on replicated objects. Currently only `Destination` is supported.
    language:
      python:
        mapCase: false
type: object
required:
  - owner
