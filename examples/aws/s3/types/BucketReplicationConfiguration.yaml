$token: aws:s3/BucketReplicationConfiguration:BucketReplicationConfiguration
properties:
  role:
    type: string
    description: |
      The ARN of the IAM role for Amazon S3 to assume when replicating the objects.
    language:
      python:
        mapCase: false
  rules:
    type: array
    items:
      $ref: "#/types/aws:s3/BucketReplicationConfigurationRule:BucketReplicationConfigurationRule"
    description: |
      Specifies the rules managing the replication (documented below).
    language:
      python:
        mapCase: false
type: object
required:
  - role
  - rules
