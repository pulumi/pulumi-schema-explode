$token: aws:s3/bucketLoggingV2:BucketLoggingV2
properties:
  bucket:
    type: string
    description: |
      The name of the bucket.
  expectedBucketOwner:
    type: string
    description: |
      The account ID of the expected bucket owner.
  targetBucket:
    type: string
    description: |
      The name of the bucket where you want Amazon S3 to store server access logs.
  targetGrants:
    type: array
    items:
      $ref: "#/types/aws:s3/BucketLoggingV2TargetGrant:BucketLoggingV2TargetGrant"
    description: |
      Set of configuration blocks with information for granting permissions documented below.
  targetPrefix:
    type: string
    description: |
      A prefix for all log object keys.
required:
  - bucket
  - targetBucket
  - targetPrefix
inputProperties:
  bucket:
    type: string
    description: |
      The name of the bucket.
    willReplaceOnChanges: true
  expectedBucketOwner:
    type: string
    description: |
      The account ID of the expected bucket owner.
    willReplaceOnChanges: true
  targetBucket:
    type: string
    description: |
      The name of the bucket where you want Amazon S3 to store server access logs.
  targetGrants:
    type: array
    items:
      $ref: "#/types/aws:s3/BucketLoggingV2TargetGrant:BucketLoggingV2TargetGrant"
    description: |
      Set of configuration blocks with information for granting permissions documented below.
  targetPrefix:
    type: string
    description: |
      A prefix for all log object keys.
requiredInputs:
  - bucket
  - targetBucket
  - targetPrefix
stateInputs:
  description: |
    Input properties used for looking up and filtering BucketLoggingV2 resources.
  properties:
    bucket:
      type: string
      description: |
        The name of the bucket.
      willReplaceOnChanges: true
    expectedBucketOwner:
      type: string
      description: |
        The account ID of the expected bucket owner.
      willReplaceOnChanges: true
    targetBucket:
      type: string
      description: |
        The name of the bucket where you want Amazon S3 to store server access logs.
    targetGrants:
      type: array
      items:
        $ref: "#/types/aws:s3/BucketLoggingV2TargetGrant:BucketLoggingV2TargetGrant"
      description: |
        Set of configuration blocks with information for granting permissions documented below.
    targetPrefix:
      type: string
      description: |
        A prefix for all log object keys.
  type: object
