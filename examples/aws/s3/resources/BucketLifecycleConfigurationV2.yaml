$token: aws:s3/bucketLifecycleConfigurationV2:BucketLifecycleConfigurationV2
properties:
  bucket:
    type: string
    description: |
      The name of the source S3 bucket you want Amazon S3 to monitor.
  expectedBucketOwner:
    type: string
    description: |
      The account ID of the expected bucket owner. If the bucket is owned by a different account, the request will fail with an HTTP 403 (Access Denied) error.
  rules:
    type: array
    items:
      $ref: "#/types/aws:s3/BucketLifecycleConfigurationV2Rule:BucketLifecycleConfigurationV2Rule"
    description: |
      List of configuration blocks describing the rules managing the replication documented below.
required:
  - bucket
  - rules
inputProperties:
  bucket:
    type: string
    description: |
      The name of the source S3 bucket you want Amazon S3 to monitor.
    willReplaceOnChanges: true
  expectedBucketOwner:
    type: string
    description: |
      The account ID of the expected bucket owner. If the bucket is owned by a different account, the request will fail with an HTTP 403 (Access Denied) error.
    willReplaceOnChanges: true
  rules:
    type: array
    items:
      $ref: "#/types/aws:s3/BucketLifecycleConfigurationV2Rule:BucketLifecycleConfigurationV2Rule"
    description: |
      List of configuration blocks describing the rules managing the replication documented below.
requiredInputs:
  - bucket
  - rules
stateInputs:
  description: |
    Input properties used for looking up and filtering BucketLifecycleConfigurationV2 resources.
  properties:
    bucket:
      type: string
      description: |
        The name of the source S3 bucket you want Amazon S3 to monitor.
      willReplaceOnChanges: true
    expectedBucketOwner:
      type: string
      description: |
        The account ID of the expected bucket owner. If the bucket is owned by a different account, the request will fail with an HTTP 403 (Access Denied) error.
      willReplaceOnChanges: true
    rules:
      type: array
      items:
        $ref: "#/types/aws:s3/BucketLifecycleConfigurationV2Rule:BucketLifecycleConfigurationV2Rule"
      description: |
        List of configuration blocks describing the rules managing the replication documented below.
  type: object
