$token: aws:s3/bucketIntelligentTieringConfiguration:BucketIntelligentTieringConfiguration
properties:
  bucket:
    type: string
    description: |
      The name of the bucket this intelligent tiering configuration is associated with.
  filter:
    $ref: "#/types/aws:s3/BucketIntelligentTieringConfigurationFilter:BucketIntelligentTieringConfigurationFilter"
    description: |
      A bucket filter. The configuration only includes objects that meet the filter's criteria (documented below).
  name:
    type: string
    description: |
      The unique name used to identify the S3 Intelligent-Tiering configuration for the bucket.
  status:
    type: string
    description: |
      Specifies the status of the configuration. Valid values: `Enabled`, `Disabled`.
  tierings:
    type: array
    items:
      $ref: "#/types/aws:s3/BucketIntelligentTieringConfigurationTiering:BucketIntelligentTieringConfigurationTiering"
    description: |
      The S3 Intelligent-Tiering storage class tiers of the configuration (documented below).
required:
  - bucket
  - name
  - tierings
inputProperties:
  bucket:
    type: string
    description: |
      The name of the bucket this intelligent tiering configuration is associated with.
    willReplaceOnChanges: true
  filter:
    $ref: "#/types/aws:s3/BucketIntelligentTieringConfigurationFilter:BucketIntelligentTieringConfigurationFilter"
    description: |
      A bucket filter. The configuration only includes objects that meet the filter's criteria (documented below).
  name:
    type: string
    description: |
      The unique name used to identify the S3 Intelligent-Tiering configuration for the bucket.
    willReplaceOnChanges: true
  status:
    type: string
    description: |
      Specifies the status of the configuration. Valid values: `Enabled`, `Disabled`.
  tierings:
    type: array
    items:
      $ref: "#/types/aws:s3/BucketIntelligentTieringConfigurationTiering:BucketIntelligentTieringConfigurationTiering"
    description: |
      The S3 Intelligent-Tiering storage class tiers of the configuration (documented below).
requiredInputs:
  - bucket
  - tierings
stateInputs:
  description: |
    Input properties used for looking up and filtering BucketIntelligentTieringConfiguration resources.
  properties:
    bucket:
      type: string
      description: |
        The name of the bucket this intelligent tiering configuration is associated with.
      willReplaceOnChanges: true
    filter:
      $ref: "#/types/aws:s3/BucketIntelligentTieringConfigurationFilter:BucketIntelligentTieringConfigurationFilter"
      description: |
        A bucket filter. The configuration only includes objects that meet the filter's criteria (documented below).
    name:
      type: string
      description: |
        The unique name used to identify the S3 Intelligent-Tiering configuration for the bucket.
      willReplaceOnChanges: true
    status:
      type: string
      description: |
        Specifies the status of the configuration. Valid values: `Enabled`, `Disabled`.
    tierings:
      type: array
      items:
        $ref: "#/types/aws:s3/BucketIntelligentTieringConfigurationTiering:BucketIntelligentTieringConfigurationTiering"
      description: |
        The S3 Intelligent-Tiering storage class tiers of the configuration (documented below).
  type: object
