$token: aws:s3/inventory:Inventory
properties:
  bucket:
    type: string
    description: |
      The name of the source bucket that inventory lists the objects for.
  destination:
    $ref: "#/types/aws:s3/InventoryDestination:InventoryDestination"
    description: |
      Contains information about where to publish the inventory results (documented below).
  enabled:
    type: boolean
    description: |
      Specifies whether the inventory is enabled or disabled.
  filter:
    $ref: "#/types/aws:s3/InventoryFilter:InventoryFilter"
    description: |
      Specifies an inventory filter. The inventory only includes objects that meet the filter's criteria (documented below).
  includedObjectVersions:
    type: string
    description: |
      Object versions to include in the inventory list. Valid values: `All`, `Current`.
  name:
    type: string
    description: |
      Unique identifier of the inventory configuration for the bucket.
  optionalFields:
    type: array
    items:
      type: string
    description: |
      List of optional fields that are included in the inventory results. Please refer to the S3 [documentation](https://docs.aws.amazon.com/AmazonS3/latest/API/API_InventoryConfiguration.html#AmazonS3-Type-InventoryConfiguration-OptionalFields) for more details.
  schedule:
    $ref: "#/types/aws:s3/InventorySchedule:InventorySchedule"
    description: |
      Specifies the schedule for generating inventory results (documented below).
required:
  - bucket
  - destination
  - includedObjectVersions
  - name
  - schedule
inputProperties:
  bucket:
    type: string
    description: |
      The name of the source bucket that inventory lists the objects for.
    willReplaceOnChanges: true
  destination:
    $ref: "#/types/aws:s3/InventoryDestination:InventoryDestination"
    description: |
      Contains information about where to publish the inventory results (documented below).
  enabled:
    type: boolean
    description: |
      Specifies whether the inventory is enabled or disabled.
  filter:
    $ref: "#/types/aws:s3/InventoryFilter:InventoryFilter"
    description: |
      Specifies an inventory filter. The inventory only includes objects that meet the filter's criteria (documented below).
  includedObjectVersions:
    type: string
    description: |
      Object versions to include in the inventory list. Valid values: `All`, `Current`.
  name:
    type: string
    description: |
      Unique identifier of the inventory configuration for the bucket.
    willReplaceOnChanges: true
  optionalFields:
    type: array
    items:
      type: string
    description: |
      List of optional fields that are included in the inventory results. Please refer to the S3 [documentation](https://docs.aws.amazon.com/AmazonS3/latest/API/API_InventoryConfiguration.html#AmazonS3-Type-InventoryConfiguration-OptionalFields) for more details.
  schedule:
    $ref: "#/types/aws:s3/InventorySchedule:InventorySchedule"
    description: |
      Specifies the schedule for generating inventory results (documented below).
requiredInputs:
  - bucket
  - destination
  - includedObjectVersions
  - schedule
stateInputs:
  description: |
    Input properties used for looking up and filtering Inventory resources.
  properties:
    bucket:
      type: string
      description: |
        The name of the source bucket that inventory lists the objects for.
      willReplaceOnChanges: true
    destination:
      $ref: "#/types/aws:s3/InventoryDestination:InventoryDestination"
      description: |
        Contains information about where to publish the inventory results (documented below).
    enabled:
      type: boolean
      description: |
        Specifies whether the inventory is enabled or disabled.
    filter:
      $ref: "#/types/aws:s3/InventoryFilter:InventoryFilter"
      description: |
        Specifies an inventory filter. The inventory only includes objects that meet the filter's criteria (documented below).
    includedObjectVersions:
      type: string
      description: |
        Object versions to include in the inventory list. Valid values: `All`, `Current`.
    name:
      type: string
      description: |
        Unique identifier of the inventory configuration for the bucket.
      willReplaceOnChanges: true
    optionalFields:
      type: array
      items:
        type: string
      description: |
        List of optional fields that are included in the inventory results. Please refer to the S3 [documentation](https://docs.aws.amazon.com/AmazonS3/latest/API/API_InventoryConfiguration.html#AmazonS3-Type-InventoryConfiguration-OptionalFields) for more details.
    schedule:
      $ref: "#/types/aws:s3/InventorySchedule:InventorySchedule"
      description: |
        Specifies the schedule for generating inventory results (documented below).
  type: object
