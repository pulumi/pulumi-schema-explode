$token: aws:ssm/AssociationTarget:AssociationTarget
properties:
  key:
    type: string
    description: |
      Either `InstanceIds` or `tag:Tag Name` to specify an EC2 tag.
    language:
      python:
        mapCase: false
  values:
    type: array
    items:
      type: string
    description: |
      A list of instance IDs or tag values. AWS currently limits this list size to one value.
    language:
      python:
        mapCase: false
type: object
required:
  - key
  - values
