$token: aws:ssm/DocumentAttachmentsSource:DocumentAttachmentsSource
properties:
  key:
    type: string
    description: |
      The key describing the location of an attachment to a document. Valid key types include: `SourceUrl` and `S3FileUrl`
    language:
      python:
        mapCase: false
  name:
    type: string
    description: |
      The name of the document attachment file
    language:
      python:
        mapCase: false
  values:
    type: array
    items:
      type: string
    description: |
      The value describing the location of an attachment to a document
    language:
      python:
        mapCase: false
type: object
required:
  - key
  - values
