$token: aws:ssm/MaintenanceWindowTaskTaskInvocationParametersRunCommandParameters:MaintenanceWindowTaskTaskInvocationParametersRunCommandParameters
properties:
  cloudwatchConfig:
    $ref: "#/types/aws:ssm/MaintenanceWindowTaskTaskInvocationParametersRunCommandParametersCloudwatchConfig:MaintenanceWindowTaskTaskInvocationParametersRunCommandParametersCloudwatchConfig"
    description: |
      Configuration options for sending command output to CloudWatch Logs. Documented below.
    language:
      python:
        mapCase: false
  comment:
    type: string
    description: |
      Information about the command(s) to execute.
    language:
      python:
        mapCase: false
  documentHash:
    type: string
    description: |
      The SHA-256 or SHA-1 hash created by the system when the document was created. SHA-1 hashes have been deprecated.
    language:
      python:
        mapCase: false
  documentHashType:
    type: string
    description: |
      SHA-256 or SHA-1. SHA-1 hashes have been deprecated. Valid values: `Sha256` and `Sha1`
    language:
      python:
        mapCase: false
  documentVersion:
    type: string
    description: |
      The version of an Automation document to use during task execution.
    language:
      python:
        mapCase: false
  notificationConfig:
    $ref: "#/types/aws:ssm/MaintenanceWindowTaskTaskInvocationParametersRunCommandParametersNotificationConfig:MaintenanceWindowTaskTaskInvocationParametersRunCommandParametersNotificationConfig"
    description: |
      Configurations for sending notifications about command status changes on a per-instance basis. Documented below.
    language:
      python:
        mapCase: false
  outputS3Bucket:
    type: string
    description: |
      The name of the Amazon S3 bucket.
    language:
      python:
        mapCase: false
  outputS3KeyPrefix:
    type: string
    description: |
      The Amazon S3 bucket subfolder.
    language:
      python:
        mapCase: false
  parameters:
    type: array
    items:
      $ref: "#/types/aws:ssm/MaintenanceWindowTaskTaskInvocationParametersRunCommandParametersParameter:MaintenanceWindowTaskTaskInvocationParametersRunCommandParametersParameter"
    description: |
      The parameters for the RUN_COMMAND task execution. Documented below.
    language:
      python:
        mapCase: false
  serviceRoleArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the AWS Identity and Access Management (IAM) service role to use to publish Amazon Simple Notification Service (Amazon SNS) notifications for maintenance window Run Command tasks.
    language:
      python:
        mapCase: false
  timeoutSeconds:
    type: integer
    description: |
      If this time is reached and the command has not already started executing, it doesn't run.
    language:
      python:
        mapCase: false
type: object
