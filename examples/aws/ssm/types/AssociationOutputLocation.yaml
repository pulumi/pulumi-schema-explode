$token: aws:ssm/AssociationOutputLocation:AssociationOutputLocation
properties:
  s3BucketName:
    type: string
    description: |
      The S3 bucket name.
    language:
      python:
        mapCase: false
  s3KeyPrefix:
    type: string
    description: |
      The S3 bucket prefix. Results stored in the root if not configured.
    language:
      python:
        mapCase: false
  s3Region:
    type: string
    description: |
      The S3 bucket region.
    language:
      python:
        mapCase: false
type: object
required:
  - s3BucketName
