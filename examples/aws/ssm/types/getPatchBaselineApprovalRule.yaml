$token: aws:ssm/getPatchBaselineApprovalRule:getPatchBaselineApprovalRule
properties:
  approveAfterDays:
    type: integer
    description: |
      The number of days after the release date of each patch matched by the rule the patch is marked as approved in the patch baseline.
    language:
      python:
        mapCase: false
  approveUntilDate:
    type: string
    description: |
      The cutoff date for auto approval of released patches. Any patches released on or before this date are installed automatically. Date is formatted as `YYYY-MM-DD`. Conflicts with `approve_after_days`
    language:
      python:
        mapCase: false
  complianceLevel:
    type: string
    description: |
      The compliance level for patches approved by this rule.
    language:
      python:
        mapCase: false
  enableNonSecurity:
    type: boolean
    description: |
      Boolean enabling the application of non-security updates.
    language:
      python:
        mapCase: false
  patchFilters:
    type: array
    items:
      $ref: "#/types/aws:ssm/getPatchBaselineApprovalRulePatchFilter:getPatchBaselineApprovalRulePatchFilter"
    description: |
      The patch filter group that defines the criteria for the rule.
    language:
      python:
        mapCase: false
type: object
required:
  - approveAfterDays
  - approveUntilDate
  - complianceLevel
  - enableNonSecurity
  - patchFilters
language:
  nodejs:
    requiredInputs: []
