$token: aws:ssm/serviceSetting:ServiceSetting
properties:
  arn:
    type: string
    description: |
      ARN of the service setting.
  settingId:
    type: string
    description: |
      ID of the service setting.
  settingValue:
    type: string
    description: |
      Value of the service setting.
  status:
    type: string
    description: |
      Status of the service setting. Value can be `Default`, `Customized` or `PendingUpdate`.
required:
  - arn
  - settingId
  - settingValue
  - status
inputProperties:
  settingId:
    type: string
    description: |
      ID of the service setting.
  settingValue:
    type: string
    description: |
      Value of the service setting.
requiredInputs:
  - settingId
  - settingValue
stateInputs:
  description: |
    Input properties used for looking up and filtering ServiceSetting resources.
  properties:
    arn:
      type: string
      description: |
        ARN of the service setting.
    settingId:
      type: string
      description: |
        ID of the service setting.
    settingValue:
      type: string
      description: |
        Value of the service setting.
    status:
      type: string
      description: |
        Status of the service setting. Value can be `Default`, `Customized` or `PendingUpdate`.
  type: object
