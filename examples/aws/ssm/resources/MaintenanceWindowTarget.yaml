$token: aws:ssm/maintenanceWindowTarget:MaintenanceWindowTarget
properties:
  description:
    type: string
    description: |
      The description of the maintenance window target.
  name:
    type: string
    description: |
      The name of the maintenance window target.
  ownerInformation:
    type: string
    description: |
      User-provided value that will be included in any CloudWatch events raised while running tasks for these targets in this Maintenance Window.
  resourceType:
    type: string
    description: |
      The type of target being registered with the Maintenance Window. Possible values are `INSTANCE` and `RESOURCE_GROUP`.
  targets:
    type: array
    items:
      $ref: "#/types/aws:ssm/MaintenanceWindowTargetTarget:MaintenanceWindowTargetTarget"
    description: |
      The targets to register with the maintenance window. In other words, the instances to run commands on when the maintenance window runs. You can specify targets using instance IDs, resource group names, or tags that have been applied to instances. For more information about these examples formats see
      (https://docs.aws.amazon.com/systems-manager/latest/userguide/mw-cli-tutorial-targets-examples.html)
  windowId:
    type: string
    description: |
      The Id of the maintenance window to register the target with.
required:
  - name
  - resourceType
  - targets
  - windowId
inputProperties:
  description:
    type: string
    description: |
      The description of the maintenance window target.
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      The name of the maintenance window target.
    willReplaceOnChanges: true
  ownerInformation:
    type: string
    description: |
      User-provided value that will be included in any CloudWatch events raised while running tasks for these targets in this Maintenance Window.
  resourceType:
    type: string
    description: |
      The type of target being registered with the Maintenance Window. Possible values are `INSTANCE` and `RESOURCE_GROUP`.
    willReplaceOnChanges: true
  targets:
    type: array
    items:
      $ref: "#/types/aws:ssm/MaintenanceWindowTargetTarget:MaintenanceWindowTargetTarget"
    description: |
      The targets to register with the maintenance window. In other words, the instances to run commands on when the maintenance window runs. You can specify targets using instance IDs, resource group names, or tags that have been applied to instances. For more information about these examples formats see
      (https://docs.aws.amazon.com/systems-manager/latest/userguide/mw-cli-tutorial-targets-examples.html)
  windowId:
    type: string
    description: |
      The Id of the maintenance window to register the target with.
    willReplaceOnChanges: true
requiredInputs:
  - resourceType
  - targets
  - windowId
stateInputs:
  description: |
    Input properties used for looking up and filtering MaintenanceWindowTarget resources.
  properties:
    description:
      type: string
      description: |
        The description of the maintenance window target.
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        The name of the maintenance window target.
      willReplaceOnChanges: true
    ownerInformation:
      type: string
      description: |
        User-provided value that will be included in any CloudWatch events raised while running tasks for these targets in this Maintenance Window.
    resourceType:
      type: string
      description: |
        The type of target being registered with the Maintenance Window. Possible values are `INSTANCE` and `RESOURCE_GROUP`.
      willReplaceOnChanges: true
    targets:
      type: array
      items:
        $ref: "#/types/aws:ssm/MaintenanceWindowTargetTarget:MaintenanceWindowTargetTarget"
      description: |
        The targets to register with the maintenance window. In other words, the instances to run commands on when the maintenance window runs. You can specify targets using instance IDs, resource group names, or tags that have been applied to instances. For more information about these examples formats see
        (https://docs.aws.amazon.com/systems-manager/latest/userguide/mw-cli-tutorial-targets-examples.html)
    windowId:
      type: string
      description: |
        The Id of the maintenance window to register the target with.
      willReplaceOnChanges: true
  type: object
