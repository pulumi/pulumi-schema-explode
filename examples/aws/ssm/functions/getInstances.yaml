$token: aws:ssm/getInstances:getInstances
inputs:
  description: |
    A collection of arguments for invoking getInstances.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ssm/getInstancesFilter:getInstancesFilter"
      description: |
        Configuration block(s) for filtering. Detailed below.
  type: object
outputs:
  description: |
    A collection of values returned by getInstances.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ssm/getInstancesFilter:getInstancesFilter"
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    ids:
      type: array
      items:
        type: string
      description: |
        Set of instance IDs of the matched SSM managed instances.
  type: object
  required:
    - ids
    - id
