$token: aws:detective/invitationAccepter:InvitationAccepter
properties:
  graphArn:
    type: string
    description: |
      ARN of the behavior graph that the member account is accepting the invitation for.
required:
  - graphArn
inputProperties:
  graphArn:
    type: string
    description: |
      ARN of the behavior graph that the member account is accepting the invitation for.
    willReplaceOnChanges: true
requiredInputs:
  - graphArn
stateInputs:
  description: |
    Input properties used for looking up and filtering InvitationAccepter resources.
  properties:
    graphArn:
      type: string
      description: |
        ARN of the behavior graph that the member account is accepting the invitation for.
      willReplaceOnChanges: true
  type: object
