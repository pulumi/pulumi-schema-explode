$token: aws:cfg/RemediationConfigurationParameter:RemediationConfigurationParameter
properties:
  name:
    type: string
    description: |
      Name of the attribute.
    language:
      python:
        mapCase: false
  resourceValue:
    type: string
    description: |
      Value is dynamic and changes at run-time.
    language:
      python:
        mapCase: false
  staticValue:
    type: string
    description: |
      Value is static and does not change at run-time.
    language:
      python:
        mapCase: false
  staticValues:
    type: array
    items:
      type: string
    description: |
      List of static values.
    language:
      python:
        mapCase: false
type: object
required:
  - name
