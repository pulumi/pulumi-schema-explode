$token: aws:cfg/ConfigurationAggregatorOrganizationAggregationSource:ConfigurationAggregatorOrganizationAggregationSource
properties:
  allRegions:
    type: boolean
    description: |
      If true, aggregate existing AWS Config regions and future regions.
    language:
      python:
        mapCase: false
  regions:
    type: array
    items:
      type: string
    description: |
      List of source regions being aggregated.
    language:
      python:
        mapCase: false
  roleArn:
    type: string
    description: |
      ARN of the IAM role used to retrieve AWS Organization details associated with the aggregator account.
    language:
      python:
        mapCase: false
type: object
required:
  - roleArn
