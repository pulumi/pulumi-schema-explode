$token: aws:cfg/ConfigurationAggregatorAccountAggregationSource:ConfigurationAggregatorAccountAggregationSource
properties:
  accountIds:
    type: array
    items:
      type: string
    description: |
      List of 12-digit account IDs of the account(s) being aggregated.
    language:
      python:
        mapCase: false
  allRegions:
    type: boolean
    description: |
      If true, aggregate existing AWS Config regions and future regions.
    language:
      python:
        mapCase: false
  regions:
    type: array
    items:
      type: string
    description: |
      List of source regions being aggregated.
    language:
      python:
        mapCase: false
type: object
required:
  - accountIds
