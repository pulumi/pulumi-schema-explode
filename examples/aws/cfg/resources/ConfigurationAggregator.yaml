$token: aws:cfg/configurationAggregator:ConfigurationAggregator
properties:
  accountAggregationSource:
    $ref: "#/types/aws:cfg/ConfigurationAggregatorAccountAggregationSource:ConfigurationAggregatorAccountAggregationSource"
    description: |
      The account(s) to aggregate config data from as documented below.
  arn:
    type: string
    description: |
      The ARN of the aggregator
  name:
    type: string
    description: |
      The name of the configuration aggregator.
  organizationAggregationSource:
    $ref: "#/types/aws:cfg/ConfigurationAggregatorOrganizationAggregationSource:ConfigurationAggregatorOrganizationAggregationSource"
    description: |
      The organization to aggregate config data from as documented below.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - name
  - tagsAll
inputProperties:
  accountAggregationSource:
    $ref: "#/types/aws:cfg/ConfigurationAggregatorAccountAggregationSource:ConfigurationAggregatorAccountAggregationSource"
    description: |
      The account(s) to aggregate config data from as documented below.
  name:
    type: string
    description: |
      The name of the configuration aggregator.
    willReplaceOnChanges: true
  organizationAggregationSource:
    $ref: "#/types/aws:cfg/ConfigurationAggregatorOrganizationAggregationSource:ConfigurationAggregatorOrganizationAggregationSource"
    description: |
      The organization to aggregate config data from as documented below.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
stateInputs:
  description: |
    Input properties used for looking up and filtering ConfigurationAggregator resources.
  properties:
    accountAggregationSource:
      $ref: "#/types/aws:cfg/ConfigurationAggregatorAccountAggregationSource:ConfigurationAggregatorAccountAggregationSource"
      description: |
        The account(s) to aggregate config data from as documented below.
    arn:
      type: string
      description: |
        The ARN of the aggregator
    name:
      type: string
      description: |
        The name of the configuration aggregator.
      willReplaceOnChanges: true
    organizationAggregationSource:
      $ref: "#/types/aws:cfg/ConfigurationAggregatorOrganizationAggregationSource:ConfigurationAggregatorOrganizationAggregationSource"
      description: |
        The organization to aggregate config data from as documented below.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
