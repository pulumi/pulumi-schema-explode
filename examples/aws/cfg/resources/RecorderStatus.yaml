$token: aws:cfg/recorderStatus:RecorderStatus
properties:
  isEnabled:
    type: boolean
    description: |
      Whether the configuration recorder should be enabled or disabled.
  name:
    type: string
    description: |
      The name of the recorder
required:
  - isEnabled
  - name
inputProperties:
  isEnabled:
    type: boolean
    description: |
      Whether the configuration recorder should be enabled or disabled.
  name:
    type: string
    description: |
      The name of the recorder
requiredInputs:
  - isEnabled
stateInputs:
  description: |
    Input properties used for looking up and filtering RecorderStatus resources.
  properties:
    isEnabled:
      type: boolean
      description: |
        Whether the configuration recorder should be enabled or disabled.
    name:
      type: string
      description: |
        The name of the recorder
  type: object
