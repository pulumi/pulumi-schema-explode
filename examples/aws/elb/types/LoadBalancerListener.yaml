$token: aws:elb/LoadBalancerListener:LoadBalancerListener
properties:
  instancePort:
    type: integer
    description: |
      The port on the instance to route to
    language:
      python:
        mapCase: false
  instanceProtocol:
    type: string
    description: |
      The protocol to use to the instance. Valid
      values are `HTTP`, `HTTPS`, `TCP`, or `SSL`
    language:
      python:
        mapCase: false
  lbPort:
    type: integer
    description: |
      The port to listen on for the load balancer
    language:
      python:
        mapCase: false
  lbProtocol:
    type: string
    description: |
      The protocol to listen on. Valid values are `HTTP`,
      `HTTPS`, `TCP`, or `SSL`
    language:
      python:
        mapCase: false
  sslCertificateId:
    type: string
    description: |
      The ARN of an SSL certificate you have
      uploaded to AWS IAM. **Note ECDSA-specific restrictions below.  Only valid when `lb_protocol` is either HTTPS or SSL**
    language:
      python:
        mapCase: false
type: object
required:
  - instancePort
  - instanceProtocol
  - lbPort
  - lbProtocol
