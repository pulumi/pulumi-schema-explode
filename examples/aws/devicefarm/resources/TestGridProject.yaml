$token: aws:devicefarm/testGridProject:TestGridProject
properties:
  arn:
    type: string
    description: |
      The Amazon Resource Name of this Test Grid Project.
  description:
    type: string
    description: |
      Human-readable description of the project.
  name:
    type: string
    description: |
      The name of the Selenium testing project.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  vpcConfig:
    $ref: "#/types/aws:devicefarm/TestGridProjectVpcConfig:TestGridProjectVpcConfig"
    description: |
      The VPC security groups and subnets that are attached to a project. See VPC Config below.
required:
  - arn
  - name
  - tagsAll
inputProperties:
  description:
    type: string
    description: |
      Human-readable description of the project.
  name:
    type: string
    description: |
      The name of the Selenium testing project.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  vpcConfig:
    $ref: "#/types/aws:devicefarm/TestGridProjectVpcConfig:TestGridProjectVpcConfig"
    description: |
      The VPC security groups and subnets that are attached to a project. See VPC Config below.
stateInputs:
  description: |
    Input properties used for looking up and filtering TestGridProject resources.
  properties:
    arn:
      type: string
      description: |
        The Amazon Resource Name of this Test Grid Project.
    description:
      type: string
      description: |
        Human-readable description of the project.
    name:
      type: string
      description: |
        The name of the Selenium testing project.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    vpcConfig:
      $ref: "#/types/aws:devicefarm/TestGridProjectVpcConfig:TestGridProjectVpcConfig"
      description: |
        The VPC security groups and subnets that are attached to a project. See VPC Config below.
  type: object
