$token: aws:devicefarm/devicePool:DevicePool
properties:
  arn:
    type: string
    description: |
      The Amazon Resource Name of this Device Pool
  description:
    type: string
    description: |
      The device pool's description.
  maxDevices:
    type: integer
    description: |
      The number of devices that Device Farm can add to your device pool.
  name:
    type: string
    description: |
      The name of the Device Pool
  projectArn:
    type: string
    description: |
      The ARN of the project for the device pool.
  rules:
    type: array
    items:
      $ref: "#/types/aws:devicefarm/DevicePoolRule:DevicePoolRule"
    description: |
      The device pool's rules. See Rule.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type:
    type: string
required:
  - arn
  - name
  - projectArn
  - rules
  - tagsAll
  - type
inputProperties:
  description:
    type: string
    description: |
      The device pool's description.
  maxDevices:
    type: integer
    description: |
      The number of devices that Device Farm can add to your device pool.
  name:
    type: string
    description: |
      The name of the Device Pool
  projectArn:
    type: string
    description: |
      The ARN of the project for the device pool.
  rules:
    type: array
    items:
      $ref: "#/types/aws:devicefarm/DevicePoolRule:DevicePoolRule"
    description: |
      The device pool's rules. See Rule.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - projectArn
  - rules
stateInputs:
  description: |
    Input properties used for looking up and filtering DevicePool resources.
  properties:
    arn:
      type: string
      description: |
        The Amazon Resource Name of this Device Pool
    description:
      type: string
      description: |
        The device pool's description.
    maxDevices:
      type: integer
      description: |
        The number of devices that Device Farm can add to your device pool.
    name:
      type: string
      description: |
        The name of the Device Pool
    projectArn:
      type: string
      description: |
        The ARN of the project for the device pool.
    rules:
      type: array
      items:
        $ref: "#/types/aws:devicefarm/DevicePoolRule:DevicePoolRule"
      description: |
        The device pool's rules. See Rule.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    type:
      type: string
  type: object
