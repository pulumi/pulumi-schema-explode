$token: aws:servicediscovery/getDnsNamespace:getDnsNamespace
inputs:
  description: |
    A collection of arguments for invoking getDnsNamespace.
  properties:
    name:
      type: string
      description: |
        Name of the namespace.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags for the resource.
    type:
      type: string
      description: |
        Type of the namespace. Allowed values are `DNS_PUBLIC` or `DNS_PRIVATE`.
  type: object
  required:
    - name
    - type
outputs:
  description: |
    A collection of values returned by getDnsNamespace.
  properties:
    arn:
      type: string
      description: |
        ARN of the namespace.
    description:
      type: string
      description: |
        Description of the namespace.
    hostedZone:
      type: string
      description: |
        ID for the hosted zone that Amazon Route 53 creates when you create a namespace.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    name:
      type: string
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags for the resource.
    type:
      type: string
  type: object
  required:
    - arn
    - description
    - hostedZone
    - name
    - tags
    - type
    - id
