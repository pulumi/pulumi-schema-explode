$token: aws:servicediscovery/getHttpNamespace:getHttpNamespace
inputs:
  description: |
    A collection of arguments for invoking getHttpNamespace.
  properties:
    name:
      type: string
      description: |
        Name of the http namespace.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags for the resource.
  type: object
  required:
    - name
outputs:
  description: |
    A collection of values returned by getHttpNamespace.
  properties:
    arn:
      type: string
      description: |
        ARN that Amazon Route 53 assigns to the namespace when you create it.
    description:
      type: string
      description: |
        Description that you specify for the namespace when you create it.
    httpName:
      type: string
      description: |
        Name of an HTTP namespace.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    name:
      type: string
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags for the resource.
  type: object
  required:
    - arn
    - description
    - httpName
    - name
    - tags
    - id
