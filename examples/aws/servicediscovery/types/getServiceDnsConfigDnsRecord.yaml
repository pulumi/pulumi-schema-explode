$token: aws:servicediscovery/getServiceDnsConfigDnsRecord:getServiceDnsConfigDnsRecord
properties:
  ttl:
    type: integer
    description: |
      Amount of time, in seconds, that you want DNS resolvers to cache the settings for this resource record set.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      The type of health check that you want to create, which indicates how Route 53 determines whether an endpoint is healthy. Valid Values: HTTP, HTTPS, TCP
    language:
      python:
        mapCase: false
type: object
required:
  - ttl
  - type
language:
  nodejs:
    requiredInputs: []
