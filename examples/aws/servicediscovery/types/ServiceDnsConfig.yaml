$token: aws:servicediscovery/ServiceDnsConfig:ServiceDnsConfig
properties:
  dnsRecords:
    type: array
    items:
      $ref: "#/types/aws:servicediscovery/ServiceDnsConfigDnsRecord:ServiceDnsConfigDnsRecord"
    description: |
      An array that contains one DnsRecord object for each resource record set.
    language:
      python:
        mapCase: false
  namespaceId:
    type: string
    description: |
      The ID of the namespace to use for DNS configuration.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  routingPolicy:
    type: string
    description: |
      The routing policy that you want to apply to all records that Route 53 creates when you register an instance and specify the service. Valid Values: MULTIVALUE, WEIGHTED
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - dnsRecords
  - namespaceId
