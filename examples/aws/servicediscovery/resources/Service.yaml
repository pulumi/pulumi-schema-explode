$token: aws:servicediscovery/service:Service
properties:
  arn:
    type: string
    description: |
      The ARN of the service.
  description:
    type: string
    description: |
      The description of the service.
  dnsConfig:
    $ref: "#/types/aws:servicediscovery/ServiceDnsConfig:ServiceDnsConfig"
    description: |
      A complex type that contains information about the resource record sets that you want Amazon Route 53 to create when you register an instance.
  forceDestroy:
    type: boolean
    description: |
      A boolean that indicates all instances should be deleted from the service so that the service can be destroyed without error. These instances are not recoverable.
  healthCheckConfig:
    $ref: "#/types/aws:servicediscovery/ServiceHealthCheckConfig:ServiceHealthCheckConfig"
    description: |
      A complex type that contains settings for an optional health check. Only for Public DNS namespaces.
  healthCheckCustomConfig:
    $ref: "#/types/aws:servicediscovery/ServiceHealthCheckCustomConfig:ServiceHealthCheckCustomConfig"
    description: |
      A complex type that contains settings for ECS managed health checks.
  name:
    type: string
    description: |
      The name of the service.
  namespaceId:
    type: string
    description: |
      The ID of the namespace to use for DNS configuration.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the service. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - name
  - namespaceId
  - tagsAll
inputProperties:
  description:
    type: string
    description: |
      The description of the service.
  dnsConfig:
    $ref: "#/types/aws:servicediscovery/ServiceDnsConfig:ServiceDnsConfig"
    description: |
      A complex type that contains information about the resource record sets that you want Amazon Route 53 to create when you register an instance.
  forceDestroy:
    type: boolean
    description: |
      A boolean that indicates all instances should be deleted from the service so that the service can be destroyed without error. These instances are not recoverable.
  healthCheckConfig:
    $ref: "#/types/aws:servicediscovery/ServiceHealthCheckConfig:ServiceHealthCheckConfig"
    description: |
      A complex type that contains settings for an optional health check. Only for Public DNS namespaces.
  healthCheckCustomConfig:
    $ref: "#/types/aws:servicediscovery/ServiceHealthCheckCustomConfig:ServiceHealthCheckCustomConfig"
    description: |
      A complex type that contains settings for ECS managed health checks.
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      The name of the service.
    willReplaceOnChanges: true
  namespaceId:
    type: string
    description: |
      The ID of the namespace to use for DNS configuration.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the service. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
stateInputs:
  description: |
    Input properties used for looking up and filtering Service resources.
  properties:
    arn:
      type: string
      description: |
        The ARN of the service.
    description:
      type: string
      description: |
        The description of the service.
    dnsConfig:
      $ref: "#/types/aws:servicediscovery/ServiceDnsConfig:ServiceDnsConfig"
      description: |
        A complex type that contains information about the resource record sets that you want Amazon Route 53 to create when you register an instance.
    forceDestroy:
      type: boolean
      description: |
        A boolean that indicates all instances should be deleted from the service so that the service can be destroyed without error. These instances are not recoverable.
    healthCheckConfig:
      $ref: "#/types/aws:servicediscovery/ServiceHealthCheckConfig:ServiceHealthCheckConfig"
      description: |
        A complex type that contains settings for an optional health check. Only for Public DNS namespaces.
    healthCheckCustomConfig:
      $ref: "#/types/aws:servicediscovery/ServiceHealthCheckCustomConfig:ServiceHealthCheckCustomConfig"
      description: |
        A complex type that contains settings for ECS managed health checks.
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        The name of the service.
      willReplaceOnChanges: true
    namespaceId:
      type: string
      description: |
        The ID of the namespace to use for DNS configuration.
      willReplaceOnChanges: true
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the service. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
