$token: aws:appautoscaling/PolicyTargetTrackingScalingPolicyConfiguration:PolicyTargetTrackingScalingPolicyConfiguration
properties:
  customizedMetricSpecification:
    $ref: "#/types/aws:appautoscaling/PolicyTargetTrackingScalingPolicyConfigurationCustomizedMetricSpecification:PolicyTargetTrackingScalingPolicyConfigurationCustomizedMetricSpecification"
    description: |
      Custom CloudWatch metric. Documentation can be found  at: [AWS Customized Metric Specification](https://docs.aws.amazon.com/autoscaling/ec2/APIReference/API_CustomizedMetricSpecification.html). See supported fields below.
    language:
      python:
        mapCase: false
  disableScaleIn:
    type: boolean
    description: |
      Whether scale in by the target tracking policy is disabled. If the value is true, scale in is disabled and the target tracking policy won't remove capacity from the scalable resource. Otherwise, scale in is enabled and the target tracking policy can remove capacity from the scalable resource. The default value is `false`.
    language:
      python:
        mapCase: false
  predefinedMetricSpecification:
    $ref: "#/types/aws:appautoscaling/PolicyTargetTrackingScalingPolicyConfigurationPredefinedMetricSpecification:PolicyTargetTrackingScalingPolicyConfigurationPredefinedMetricSpecification"
    description: |
      Predefined metric. See supported fields below.
    language:
      python:
        mapCase: false
  scaleInCooldown:
    type: integer
    description: |
      Amount of time, in seconds, after a scale in activity completes before another scale in activity can start.
    language:
      python:
        mapCase: false
  scaleOutCooldown:
    type: integer
    description: |
      Amount of time, in seconds, after a scale out activity completes before another scale out activity can start.
    language:
      python:
        mapCase: false
  targetValue:
    type: number
    description: |
      Target value for the metric.
    language:
      python:
        mapCase: false
type: object
required:
  - targetValue
