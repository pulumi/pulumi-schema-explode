$token: aws:appautoscaling/ScheduledActionScalableTargetAction:ScheduledActionScalableTargetAction
properties:
  maxCapacity:
    type: integer
    description: |
      Maximum capacity. At least one of `max_capacity` or `min_capacity` must be set.
    language:
      python:
        mapCase: false
  minCapacity:
    type: integer
    description: |
      Minimum capacity. At least one of `min_capacity` or `max_capacity` must be set.
    language:
      python:
        mapCase: false
type: object
