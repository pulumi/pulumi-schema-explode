$token: aws:networkfirewall/RuleGroupRuleGroup:RuleGroupRuleGroup
properties:
  ruleVariables:
    $ref: "#/types/aws:networkfirewall/RuleGroupRuleGroupRuleVariables:RuleGroupRuleGroupRuleVariables"
    description: |
      A configuration block that defines additional settings available to use in the rules defined in the rule group. Can only be specified for **stateful** rule groups. See Rule Variables below for details.
    language:
      python:
        mapCase: false
  rulesSource:
    $ref: "#/types/aws:networkfirewall/RuleGroupRuleGroupRulesSource:RuleGroupRuleGroupRulesSource"
    description: |
      A configuration block that defines the stateful or stateless rules for the rule group. See Rules Source below for details.
    language:
      python:
        mapCase: false
  statefulRuleOptions:
    $ref: "#/types/aws:networkfirewall/RuleGroupRuleGroupStatefulRuleOptions:RuleGroupRuleGroupStatefulRuleOptions"
    description: |
      A configuration block that defines stateful rule options for the rule group. See Stateful Rule Options below for details.
    language:
      python:
        mapCase: false
type: object
required:
  - rulesSource
