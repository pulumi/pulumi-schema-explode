$token: aws:networkfirewall/FirewallFirewallStatusSyncStateAttachment:FirewallFirewallStatusSyncStateAttachment
properties:
  endpointId:
    type: string
    description: |
      The identifier of the firewall endpoint that AWS Network Firewall has instantiated in the subnet. You use this to identify the firewall endpoint in the VPC route tables, when you redirect the VPC traffic through the endpoint.
    language:
      python:
        mapCase: false
  subnetId:
    type: string
    description: |
      The unique identifier for the subnet.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - endpointId
      - subnetId
