$token: aws:networkfirewall/getFirewallFirewallStatus:getFirewallFirewallStatus
properties:
  capacityUsageSummaries:
    type: array
    items:
      $ref: "#/types/aws:networkfirewall/getFirewallFirewallStatusCapacityUsageSummary:getFirewallFirewallStatusCapacityUsageSummary"
    description: |
      Aggregated count of all resources used by reference sets in a firewall.
    language:
      python:
        mapCase: false
  configurationSyncStateSummary:
    type: string
    description: |
      Summary of sync states for all availability zones in which the firewall is configured.
    language:
      python:
        mapCase: false
  status:
    type: string
    language:
      python:
        mapCase: false
  syncStates:
    type: array
    items:
      $ref: "#/types/aws:networkfirewall/getFirewallFirewallStatusSyncState:getFirewallFirewallStatusSyncState"
    description: |
      Set of subnets configured for use by the firewall.
    language:
      python:
        mapCase: false
type: object
required:
  - capacityUsageSummaries
  - configurationSyncStateSummary
  - status
  - syncStates
language:
  nodejs:
    requiredInputs: []
