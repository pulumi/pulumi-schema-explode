$token: aws:networkfirewall/RuleGroupRuleGroupStatefulRuleOptions:RuleGroupRuleGroupStatefulRuleOptions
properties:
  ruleOrder:
    type: string
    description: |
      Indicates how to manage the order of the rule evaluation for the rule group. Default value: `DEFAULT_ACTION_ORDER`. Valid values: `DEFAULT_ACTION_ORDER`, `STRICT_ORDER`.
    language:
      python:
        mapCase: false
type: object
required:
  - ruleOrder
