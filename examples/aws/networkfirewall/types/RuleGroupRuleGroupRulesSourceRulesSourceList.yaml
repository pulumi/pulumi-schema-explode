$token: aws:networkfirewall/RuleGroupRuleGroupRulesSourceRulesSourceList:RuleGroupRuleGroupRulesSourceRulesSourceList
properties:
  generatedRulesType:
    type: string
    description: |
      String value to specify whether domains in the target list are allowed or denied access. Valid values: `ALLOWLIST`, `DENYLIST`.
    language:
      python:
        mapCase: false
  targetTypes:
    type: array
    items:
      type: string
    description: |
      Set of types of domain specifications that are provided in the `targets` argument. Valid values: `HTTP_HOST`, `TLS_SNI`.
    language:
      python:
        mapCase: false
  targets:
    type: array
    items:
      type: string
    description: |
      Set of domains that you want to inspect for in your traffic flows.
    language:
      python:
        mapCase: false
type: object
required:
  - generatedRulesType
  - targetTypes
  - targets
