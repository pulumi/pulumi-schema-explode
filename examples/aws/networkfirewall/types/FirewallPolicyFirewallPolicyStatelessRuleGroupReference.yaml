$token: aws:networkfirewall/FirewallPolicyFirewallPolicyStatelessRuleGroupReference:FirewallPolicyFirewallPolicyStatelessRuleGroupReference
properties:
  priority:
    type: integer
    description: |
      An integer setting that indicates the order in which to run the stateless rule groups in a single policy. AWS Network Firewall applies each stateless rule group to a packet starting with the group that has the lowest priority setting.
    language:
      python:
        mapCase: false
  resourceArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the stateless rule group.
    language:
      python:
        mapCase: false
type: object
required:
  - priority
  - resourceArn
