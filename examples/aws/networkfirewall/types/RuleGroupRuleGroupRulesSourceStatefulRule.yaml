$token: aws:networkfirewall/RuleGroupRuleGroupRulesSourceStatefulRule:RuleGroupRuleGroupRulesSourceStatefulRule
properties:
  action:
    type: string
    description: |
      Action to take with packets in a traffic flow when the flow matches the stateful rule criteria. For all actions, AWS Network Firewall performs the specified action and discontinues stateful inspection of the traffic flow. Valid values: `ALERT`, `DROP` or `PASS`.
    language:
      python:
        mapCase: false
  header:
    $ref: "#/types/aws:networkfirewall/RuleGroupRuleGroupRulesSourceStatefulRuleHeader:RuleGroupRuleGroupRulesSourceStatefulRuleHeader"
    description: |
      A configuration block containing the stateful 5-tuple inspection criteria for the rule, used to inspect traffic flows. See Header below for details.
    language:
      python:
        mapCase: false
  ruleOptions:
    type: array
    items:
      $ref: "#/types/aws:networkfirewall/RuleGroupRuleGroupRulesSourceStatefulRuleRuleOption:RuleGroupRuleGroupRulesSourceStatefulRuleRuleOption"
    description: |
      Set of configuration blocks containing additional settings for a stateful rule. See Rule Option below for details.
    language:
      python:
        mapCase: false
type: object
required:
  - action
  - header
  - ruleOptions
