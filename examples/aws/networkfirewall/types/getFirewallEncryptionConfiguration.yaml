$token: aws:networkfirewall/getFirewallEncryptionConfiguration:getFirewallEncryptionConfiguration
properties:
  keyId:
    type: string
    description: |
      The ID of the AWS Key Management Service (AWS KMS) customer managed key.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      The type of the AWS Key Management Service (AWS KMS) key use by the firewall.
    language:
      python:
        mapCase: false
type: object
required:
  - keyId
  - type
language:
  nodejs:
    requiredInputs: []
