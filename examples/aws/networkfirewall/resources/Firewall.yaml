$token: aws:networkfirewall/firewall:Firewall
properties:
  arn:
    type: string
    description: |
      The Amazon Resource Name (ARN) that identifies the firewall.
  deleteProtection:
    type: boolean
    description: |
      A boolean flag indicating whether it is possible to delete the firewall. Defaults to `false`.
  description:
    type: string
    description: |
      A friendly description of the firewall.
  firewallPolicyArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the VPC Firewall policy.
  firewallPolicyChangeProtection:
    type: boolean
    description: |
      A boolean flag indicating whether it is possible to change the associated firewall policy. Defaults to `false`.
  firewallStatuses:
    type: array
    items:
      $ref: "#/types/aws:networkfirewall/FirewallFirewallStatus:FirewallFirewallStatus"
    description: |
      Nested list of information about the current status of the firewall.
  name:
    type: string
    description: |
      A friendly name of the firewall.
  subnetChangeProtection:
    type: boolean
    description: |
      A boolean flag indicating whether it is possible to change the associated subnet(s). Defaults to `false`.
  subnetMappings:
    type: array
    items:
      $ref: "#/types/aws:networkfirewall/FirewallSubnetMapping:FirewallSubnetMapping"
    description: |
      Set of configuration blocks describing the public subnets. Each subnet must belong to a different Availability Zone in the VPC. AWS Network Firewall creates a firewall endpoint in each subnet. See Subnet Mapping below for details.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Map of resource tags to associate with the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  updateToken:
    type: string
    description: |
      A string token used when updating a firewall.
  vpcId:
    type: string
    description: |
      The unique identifier of the VPC where AWS Network Firewall should create the firewall.
required:
  - arn
  - firewallPolicyArn
  - firewallStatuses
  - name
  - subnetMappings
  - tagsAll
  - updateToken
  - vpcId
inputProperties:
  deleteProtection:
    type: boolean
    description: |
      A boolean flag indicating whether it is possible to delete the firewall. Defaults to `false`.
  description:
    type: string
    description: |
      A friendly description of the firewall.
  firewallPolicyArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the VPC Firewall policy.
  firewallPolicyChangeProtection:
    type: boolean
    description: |
      A boolean flag indicating whether it is possible to change the associated firewall policy. Defaults to `false`.
  name:
    type: string
    description: |
      A friendly name of the firewall.
    willReplaceOnChanges: true
  subnetChangeProtection:
    type: boolean
    description: |
      A boolean flag indicating whether it is possible to change the associated subnet(s). Defaults to `false`.
  subnetMappings:
    type: array
    items:
      $ref: "#/types/aws:networkfirewall/FirewallSubnetMapping:FirewallSubnetMapping"
    description: |
      Set of configuration blocks describing the public subnets. Each subnet must belong to a different Availability Zone in the VPC. AWS Network Firewall creates a firewall endpoint in each subnet. See Subnet Mapping below for details.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Map of resource tags to associate with the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  vpcId:
    type: string
    description: |
      The unique identifier of the VPC where AWS Network Firewall should create the firewall.
    willReplaceOnChanges: true
requiredInputs:
  - firewallPolicyArn
  - subnetMappings
  - vpcId
stateInputs:
  description: |
    Input properties used for looking up and filtering Firewall resources.
  properties:
    arn:
      type: string
      description: |
        The Amazon Resource Name (ARN) that identifies the firewall.
    deleteProtection:
      type: boolean
      description: |
        A boolean flag indicating whether it is possible to delete the firewall. Defaults to `false`.
    description:
      type: string
      description: |
        A friendly description of the firewall.
    firewallPolicyArn:
      type: string
      description: |
        The Amazon Resource Name (ARN) of the VPC Firewall policy.
    firewallPolicyChangeProtection:
      type: boolean
      description: |
        A boolean flag indicating whether it is possible to change the associated firewall policy. Defaults to `false`.
    firewallStatuses:
      type: array
      items:
        $ref: "#/types/aws:networkfirewall/FirewallFirewallStatus:FirewallFirewallStatus"
      description: |
        Nested list of information about the current status of the firewall.
    name:
      type: string
      description: |
        A friendly name of the firewall.
      willReplaceOnChanges: true
    subnetChangeProtection:
      type: boolean
      description: |
        A boolean flag indicating whether it is possible to change the associated subnet(s). Defaults to `false`.
    subnetMappings:
      type: array
      items:
        $ref: "#/types/aws:networkfirewall/FirewallSubnetMapping:FirewallSubnetMapping"
      description: |
        Set of configuration blocks describing the public subnets. Each subnet must belong to a different Availability Zone in the VPC. AWS Network Firewall creates a firewall endpoint in each subnet. See Subnet Mapping below for details.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of resource tags to associate with the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    updateToken:
      type: string
      description: |
        A string token used when updating a firewall.
    vpcId:
      type: string
      description: |
        The unique identifier of the VPC where AWS Network Firewall should create the firewall.
      willReplaceOnChanges: true
  type: object
