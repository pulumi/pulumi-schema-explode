$token: aws:iot/TopicRuleFirehose:TopicRuleFirehose
properties:
  deliveryStreamName:
    type: string
    description: |
      The delivery stream name.
    language:
      python:
        mapCase: false
  roleArn:
    type: string
    description: |
      The IAM role ARN that grants access to the Amazon Kinesis Firehose stream.
    language:
      python:
        mapCase: false
  separator:
    type: string
    description: |
      A character separator that is used to separate records written to the Firehose stream. Valid values are: '\n' (newline), '\t' (tab), '\r\n' (Windows newline), ',' (comma).
    language:
      python:
        mapCase: false
type: object
required:
  - deliveryStreamName
  - roleArn
