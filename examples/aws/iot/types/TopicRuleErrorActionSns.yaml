$token: aws:iot/TopicRuleErrorActionSns:TopicRuleErrorActionSns
properties:
  messageFormat:
    type: string
    description: |
      The message format of the message to publish. Accepted values are "JSON" and "RAW".
    language:
      python:
        mapCase: false
  roleArn:
    type: string
    description: |
      The ARN of the IAM role that grants access.
    language:
      python:
        mapCase: false
  targetArn:
    type: string
    description: |
      The ARN of the SNS topic.
    language:
      python:
        mapCase: false
type: object
required:
  - roleArn
  - targetArn
