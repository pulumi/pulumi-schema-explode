$token: aws:iot/TopicRuleIotEvent:TopicRuleIotEvent
properties:
  inputName:
    type: string
    description: |
      The name of the AWS IoT Events input.
    language:
      python:
        mapCase: false
  messageId:
    type: string
    description: |
      Use this to ensure that only one input (message) with a given messageId is processed by an AWS IoT Events detector.
    language:
      python:
        mapCase: false
  roleArn:
    type: string
    description: |
      The ARN of the IAM role that grants access.
    language:
      python:
        mapCase: false
type: object
required:
  - inputName
  - roleArn
