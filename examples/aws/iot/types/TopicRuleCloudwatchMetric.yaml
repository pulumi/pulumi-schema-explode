$token: aws:iot/TopicRuleCloudwatchMetric:TopicRuleCloudwatchMetric
properties:
  metricName:
    type: string
    description: |
      The CloudWatch metric name.
    language:
      python:
        mapCase: false
  metricNamespace:
    type: string
    description: |
      The CloudWatch metric namespace name.
    language:
      python:
        mapCase: false
  metricTimestamp:
    type: string
    description: |
      An optional Unix timestamp (http://docs.aws.amazon.com/AmazonCloudWatch/latest/DeveloperGuide/cloudwatch_concepts.html#about_timestamp).
    language:
      python:
        mapCase: false
  metricUnit:
    type: string
    description: |
      The metric unit (supported units can be found here: http://docs.aws.amazon.com/AmazonCloudWatch/latest/DeveloperGuide/cloudwatch_concepts.html#Unit)
    language:
      python:
        mapCase: false
  metricValue:
    type: string
    description: |
      The CloudWatch metric value.
    language:
      python:
        mapCase: false
  roleArn:
    type: string
    description: |
      The IAM role ARN that allows access to the CloudWatch metric.
    language:
      python:
        mapCase: false
type: object
required:
  - metricName
  - metricNamespace
  - metricUnit
  - metricValue
  - roleArn
