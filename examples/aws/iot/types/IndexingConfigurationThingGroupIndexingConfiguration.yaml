$token: aws:iot/IndexingConfigurationThingGroupIndexingConfiguration:IndexingConfigurationThingGroupIndexingConfiguration
properties:
  customFields:
    type: array
    items:
      $ref: "#/types/aws:iot/IndexingConfigurationThingGroupIndexingConfigurationCustomField:IndexingConfigurationThingGroupIndexingConfigurationCustomField"
    description: |
      A list of thing group fields to index. This list cannot contain any managed fields. See below.
    language:
      python:
        mapCase: false
  managedFields:
    type: array
    items:
      $ref: "#/types/aws:iot/IndexingConfigurationThingGroupIndexingConfigurationManagedField:IndexingConfigurationThingGroupIndexingConfigurationManagedField"
    description: |
      Contains fields that are indexed and whose types are already known by the Fleet Indexing service. See below.
    language:
      python:
        mapCase: false
  thingGroupIndexingMode:
    type: string
    description: |
      Thing group indexing mode. Valid values: `OFF`, `ON`.
    language:
      python:
        mapCase: false
type: object
required:
  - thingGroupIndexingMode
language:
  nodejs:
    requiredOutputs:
      - managedFields
      - thingGroupIndexingMode
