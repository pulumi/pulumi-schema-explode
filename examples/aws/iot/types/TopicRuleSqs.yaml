$token: aws:iot/TopicRuleSqs:TopicRuleSqs
properties:
  queueUrl:
    type: string
    description: |
      The URL of the Amazon SQS queue.
    language:
      python:
        mapCase: false
  roleArn:
    type: string
    description: |
      The ARN of the IAM role that grants access.
    language:
      python:
        mapCase: false
  useBase64:
    type: boolean
    description: |
      Specifies whether to use Base64 encoding.
    language:
      python:
        mapCase: false
type: object
required:
  - queueUrl
  - roleArn
  - useBase64
