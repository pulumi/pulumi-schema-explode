$token: aws:iot/TopicRuleTimestreamTimestamp:TopicRuleTimestreamTimestamp
properties:
  unit:
    type: string
    description: |
      The precision of the timestamp value that results from the expression described in value. Valid values: `SECONDS`, `MILLISECONDS`, `MICROSECONDS`, `NANOSECONDS`.
    language:
      python:
        mapCase: false
  value:
    type: string
    description: |
      The value of the HTTP header.
    language:
      python:
        mapCase: false
type: object
required:
  - unit
  - value
