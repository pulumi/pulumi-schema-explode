$token: aws:iot/TopicRuleStepFunction:TopicRuleStepFunction
properties:
  executionNamePrefix:
    type: string
    description: |
      The prefix used to generate, along with a UUID, the unique state machine execution name.
    language:
      python:
        mapCase: false
  roleArn:
    type: string
    description: |
      The ARN of the IAM role that grants access to start execution of the state machine.
    language:
      python:
        mapCase: false
  stateMachineName:
    type: string
    description: |
      The name of the Step Functions state machine whose execution will be started.
    language:
      python:
        mapCase: false
type: object
required:
  - roleArn
  - stateMachineName
