$token: aws:iot/TopicRuleErrorActionHttp:TopicRuleErrorActionHttp
properties:
  confirmationUrl:
    type: string
    description: |
      The HTTPS URL used to verify ownership of `url`.
    language:
      python:
        mapCase: false
  httpHeaders:
    type: array
    items:
      $ref: "#/types/aws:iot/TopicRuleErrorActionHttpHttpHeader:TopicRuleErrorActionHttpHttpHeader"
    description: |
      Custom HTTP header IoT Core should send. It is possible to define more than one custom header.
    language:
      python:
        mapCase: false
  url:
    type: string
    description: |
      The HTTPS URL.
    language:
      python:
        mapCase: false
type: object
required:
  - url
