$token: aws:iot/IndexingConfigurationThingIndexingConfiguration:IndexingConfigurationThingIndexingConfiguration
properties:
  customFields:
    type: array
    items:
      $ref: "#/types/aws:iot/IndexingConfigurationThingIndexingConfigurationCustomField:IndexingConfigurationThingIndexingConfigurationCustomField"
    description: |
      Contains custom field names and their data type. See below.
    language:
      python:
        mapCase: false
  deviceDefenderIndexingMode:
    type: string
    description: |
      Device Defender indexing mode. Valid values: `VIOLATIONS`, `OFF`. Default: `OFF`.
    language:
      python:
        mapCase: false
  managedFields:
    type: array
    items:
      $ref: "#/types/aws:iot/IndexingConfigurationThingIndexingConfigurationManagedField:IndexingConfigurationThingIndexingConfigurationManagedField"
    description: |
      Contains fields that are indexed and whose types are already known by the Fleet Indexing service. See below.
    language:
      python:
        mapCase: false
  namedShadowIndexingMode:
    type: string
    description: |
      [Named shadow](https://docs.aws.amazon.com/iot/latest/developerguide/iot-device-shadows.html) indexing mode. Valid values: `ON`, `OFF`. Default: `OFF`.
    language:
      python:
        mapCase: false
  thingConnectivityIndexingMode:
    type: string
    description: |
      Thing connectivity indexing mode. Valid values: `STATUS`, `OFF`. Default: `OFF`.
    language:
      python:
        mapCase: false
  thingIndexingMode:
    type: string
    description: |
      Thing indexing mode. Valid values: `REGISTRY`, `REGISTRY_AND_SHADOW`, `OFF`.
    language:
      python:
        mapCase: false
type: object
required:
  - thingIndexingMode
language:
  nodejs:
    requiredOutputs:
      - managedFields
      - thingIndexingMode
