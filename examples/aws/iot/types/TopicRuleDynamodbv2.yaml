$token: aws:iot/TopicRuleDynamodbv2:TopicRuleDynamodbv2
properties:
  putItem:
    $ref: "#/types/aws:iot/TopicRuleDynamodbv2PutItem:TopicRuleDynamodbv2PutItem"
    description: |
      Configuration block with DynamoDB Table to which the message will be written. Nested arguments below.
    language:
      python:
        mapCase: false
  roleArn:
    type: string
    description: |
      The IAM role ARN that allows access to the CloudWatch alarm.
    language:
      python:
        mapCase: false
type: object
required:
  - roleArn
