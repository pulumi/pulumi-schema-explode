$token: aws:iot/ProvisioningTemplatePreProvisioningHook:ProvisioningTemplatePreProvisioningHook
properties:
  payloadVersion:
    type: string
    description: |
      The version of the payload that was sent to the target function. The only valid (and the default) payload version is `"2020-04-01"`.
    language:
      python:
        mapCase: false
  targetArn:
    type: string
    language:
      python:
        mapCase: false
type: object
required:
  - targetArn
