$token: aws:iot/thingGroupMembership:ThingGroupMembership
properties:
  overrideDynamicGroup:
    type: boolean
    description: |
      Override dynamic thing groups with static thing groups when 10-group limit is reached. If a thing belongs to 10 thing groups, and one or more of those groups are dynamic thing groups, adding a thing to a static group removes the thing from the last dynamic group.
  thingGroupName:
    type: string
    description: |
      The name of the group to which you are adding a thing.
  thingName:
    type: string
    description: |
      The name of the thing to add to a group.
required:
  - thingGroupName
  - thingName
inputProperties:
  overrideDynamicGroup:
    type: boolean
    description: |
      Override dynamic thing groups with static thing groups when 10-group limit is reached. If a thing belongs to 10 thing groups, and one or more of those groups are dynamic thing groups, adding a thing to a static group removes the thing from the last dynamic group.
    willReplaceOnChanges: true
  thingGroupName:
    type: string
    description: |
      The name of the group to which you are adding a thing.
    willReplaceOnChanges: true
  thingName:
    type: string
    description: |
      The name of the thing to add to a group.
    willReplaceOnChanges: true
requiredInputs:
  - thingGroupName
  - thingName
stateInputs:
  description: |
    Input properties used for looking up and filtering ThingGroupMembership resources.
  properties:
    overrideDynamicGroup:
      type: boolean
      description: |
        Override dynamic thing groups with static thing groups when 10-group limit is reached. If a thing belongs to 10 thing groups, and one or more of those groups are dynamic thing groups, adding a thing to a static group removes the thing from the last dynamic group.
      willReplaceOnChanges: true
    thingGroupName:
      type: string
      description: |
        The name of the group to which you are adding a thing.
      willReplaceOnChanges: true
    thingName:
      type: string
      description: |
        The name of the thing to add to a group.
      willReplaceOnChanges: true
  type: object
