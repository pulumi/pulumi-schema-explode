$token: aws:iot/provisioningTemplate:ProvisioningTemplate
properties:
  arn:
    type: string
    description: |
      The ARN that identifies the provisioning template.
  defaultVersionId:
    type: integer
    description: |
      The default version of the fleet provisioning template.
  description:
    type: string
    description: |
      The description of the fleet provisioning template.
  enabled:
    type: boolean
    description: |
      True to enable the fleet provisioning template, otherwise false.
  name:
    type: string
    description: |
      The name of the fleet provisioning template.
  preProvisioningHook:
    $ref: "#/types/aws:iot/ProvisioningTemplatePreProvisioningHook:ProvisioningTemplatePreProvisioningHook"
    description: |
      Creates a pre-provisioning hook template. Details below.
  provisioningRoleArn:
    type: string
    description: |
      The role ARN for the role associated with the fleet provisioning template. This IoT role grants permission to provision a device.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  templateBody:
    type: string
    description: |
      The JSON formatted contents of the fleet provisioning template.
required:
  - arn
  - defaultVersionId
  - name
  - provisioningRoleArn
  - tagsAll
  - templateBody
inputProperties:
  description:
    type: string
    description: |
      The description of the fleet provisioning template.
  enabled:
    type: boolean
    description: |
      True to enable the fleet provisioning template, otherwise false.
  name:
    type: string
    description: |
      The name of the fleet provisioning template.
    willReplaceOnChanges: true
  preProvisioningHook:
    $ref: "#/types/aws:iot/ProvisioningTemplatePreProvisioningHook:ProvisioningTemplatePreProvisioningHook"
    description: |
      Creates a pre-provisioning hook template. Details below.
  provisioningRoleArn:
    type: string
    description: |
      The role ARN for the role associated with the fleet provisioning template. This IoT role grants permission to provision a device.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  templateBody:
    type: string
    description: |
      The JSON formatted contents of the fleet provisioning template.
requiredInputs:
  - provisioningRoleArn
  - templateBody
stateInputs:
  description: |
    Input properties used for looking up and filtering ProvisioningTemplate resources.
  properties:
    arn:
      type: string
      description: |
        The ARN that identifies the provisioning template.
    defaultVersionId:
      type: integer
      description: |
        The default version of the fleet provisioning template.
    description:
      type: string
      description: |
        The description of the fleet provisioning template.
    enabled:
      type: boolean
      description: |
        True to enable the fleet provisioning template, otherwise false.
    name:
      type: string
      description: |
        The name of the fleet provisioning template.
      willReplaceOnChanges: true
    preProvisioningHook:
      $ref: "#/types/aws:iot/ProvisioningTemplatePreProvisioningHook:ProvisioningTemplatePreProvisioningHook"
      description: |
        Creates a pre-provisioning hook template. Details below.
    provisioningRoleArn:
      type: string
      description: |
        The role ARN for the role associated with the fleet provisioning template. This IoT role grants permission to provision a device.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    templateBody:
      type: string
      description: |
        The JSON formatted contents of the fleet provisioning template.
  type: object
