$token: aws:iot/indexingConfiguration:IndexingConfiguration
properties:
  thingGroupIndexingConfiguration:
    $ref: "#/types/aws:iot/IndexingConfigurationThingGroupIndexingConfiguration:IndexingConfigurationThingGroupIndexingConfiguration"
    description: |
      Thing group indexing configuration. See below.
  thingIndexingConfiguration:
    $ref: "#/types/aws:iot/IndexingConfigurationThingIndexingConfiguration:IndexingConfigurationThingIndexingConfiguration"
    description: |
      Thing indexing configuration. See below.
required:
  - thingGroupIndexingConfiguration
  - thingIndexingConfiguration
inputProperties:
  thingGroupIndexingConfiguration:
    $ref: "#/types/aws:iot/IndexingConfigurationThingGroupIndexingConfiguration:IndexingConfigurationThingGroupIndexingConfiguration"
    description: |
      Thing group indexing configuration. See below.
  thingIndexingConfiguration:
    $ref: "#/types/aws:iot/IndexingConfigurationThingIndexingConfiguration:IndexingConfigurationThingIndexingConfiguration"
    description: |
      Thing indexing configuration. See below.
stateInputs:
  description: |
    Input properties used for looking up and filtering IndexingConfiguration resources.
  properties:
    thingGroupIndexingConfiguration:
      $ref: "#/types/aws:iot/IndexingConfigurationThingGroupIndexingConfiguration:IndexingConfigurationThingGroupIndexingConfiguration"
      description: |
        Thing group indexing configuration. See below.
    thingIndexingConfiguration:
      $ref: "#/types/aws:iot/IndexingConfigurationThingIndexingConfiguration:IndexingConfigurationThingIndexingConfiguration"
      description: |
        Thing indexing configuration. See below.
  type: object
