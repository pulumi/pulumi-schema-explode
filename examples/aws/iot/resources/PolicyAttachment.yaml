$token: aws:iot/policyAttachment:PolicyAttachment
properties:
  policy:
    type: string
    description: |
      The name of the policy to attach.
  target:
    type: string
    $ref: "#/types/aws:index/aRN:ARN"
    description: |
      The identity to which the policy is attached.
required:
  - policy
  - target
inputProperties:
  policy:
    type: string
    oneOf:
      - type: string
      - type: string
        $ref: "#/types/aws:iot/policy:Policy"
    description: |
      The name of the policy to attach.
    willReplaceOnChanges: true
  target:
    type: string
    $ref: "#/types/aws:index/aRN:ARN"
    description: |
      The identity to which the policy is attached.
    willReplaceOnChanges: true
requiredInputs:
  - policy
  - target
stateInputs:
  description: |
    Input properties used for looking up and filtering PolicyAttachment resources.
  properties:
    policy:
      type: string
      oneOf:
        - type: string
        - type: string
          $ref: "#/types/aws:iot/policy:Policy"
      description: |
        The name of the policy to attach.
      willReplaceOnChanges: true
    target:
      type: string
      $ref: "#/types/aws:index/aRN:ARN"
      description: |
        The identity to which the policy is attached.
      willReplaceOnChanges: true
  type: object
