$token: aws:iot/thingPrincipalAttachment:ThingPrincipalAttachment
properties:
  principal:
    type: string
    $ref: "#/types/aws:index/aRN:ARN"
    description: |
      The AWS IoT Certificate ARN or Amazon Cognito Identity ID.
  thing:
    type: string
    description: |
      The name of the thing.
required:
  - principal
  - thing
inputProperties:
  principal:
    type: string
    $ref: "#/types/aws:index/aRN:ARN"
    description: |
      The AWS IoT Certificate ARN or Amazon Cognito Identity ID.
    willReplaceOnChanges: true
  thing:
    type: string
    description: |
      The name of the thing.
    willReplaceOnChanges: true
requiredInputs:
  - principal
  - thing
stateInputs:
  description: |
    Input properties used for looking up and filtering ThingPrincipalAttachment resources.
  properties:
    principal:
      type: string
      $ref: "#/types/aws:index/aRN:ARN"
      description: |
        The AWS IoT Certificate ARN or Amazon Cognito Identity ID.
      willReplaceOnChanges: true
    thing:
      type: string
      description: |
        The name of the thing.
      willReplaceOnChanges: true
  type: object
