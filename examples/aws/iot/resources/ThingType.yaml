$token: aws:iot/thingType:ThingType
properties:
  arn:
    type: string
    description: |
      The ARN of the created AWS IoT Thing Type.
  deprecated:
    type: boolean
    description: |
      Whether the thing type is deprecated. If true, no new things could be associated with this type.
  name:
    type: string
    description: |
      The name of the thing type.
  properties:
    $ref: "#/types/aws:iot/ThingTypeProperties:ThingTypeProperties"
    description: |
      , Configuration block that can contain the following properties of the thing type:
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value mapping of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      Map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - name
  - tagsAll
inputProperties:
  deprecated:
    type: boolean
    description: |
      Whether the thing type is deprecated. If true, no new things could be associated with this type.
  name:
    type: string
    description: |
      The name of the thing type.
    willReplaceOnChanges: true
  properties:
    $ref: "#/types/aws:iot/ThingTypeProperties:ThingTypeProperties"
    description: |
      , Configuration block that can contain the following properties of the thing type:
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value mapping of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level
stateInputs:
  description: |
    Input properties used for looking up and filtering ThingType resources.
  properties:
    arn:
      type: string
      description: |
        The ARN of the created AWS IoT Thing Type.
    deprecated:
      type: boolean
      description: |
        Whether the thing type is deprecated. If true, no new things could be associated with this type.
    name:
      type: string
      description: |
        The name of the thing type.
      willReplaceOnChanges: true
    properties:
      $ref: "#/types/aws:iot/ThingTypeProperties:ThingTypeProperties"
      description: |
        , Configuration block that can contain the following properties of the thing type:
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value mapping of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
