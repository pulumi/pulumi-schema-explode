$token: aws:iot/loggingOptions:LoggingOptions
properties:
  defaultLogLevel:
    type: string
    description: |
      The default logging level. Valid Values: `"DEBUG"`, `"INFO"`, `"ERROR"`, `"WARN"`, `"DISABLED"`.
  disableAllLogs:
    type: boolean
    description: |
      If `true` all logs are disabled. The default is `false`.
  roleArn:
    type: string
    description: |
      The ARN of the role that allows IoT to write to Cloudwatch logs.
required:
  - defaultLogLevel
  - roleArn
inputProperties:
  defaultLogLevel:
    type: string
    description: |
      The default logging level. Valid Values: `"DEBUG"`, `"INFO"`, `"ERROR"`, `"WARN"`, `"DISABLED"`.
  disableAllLogs:
    type: boolean
    description: |
      If `true` all logs are disabled. The default is `false`.
  roleArn:
    type: string
    description: |
      The ARN of the role that allows IoT to write to Cloudwatch logs.
requiredInputs:
  - defaultLogLevel
  - roleArn
stateInputs:
  description: |
    Input properties used for looking up and filtering LoggingOptions resources.
  properties:
    defaultLogLevel:
      type: string
      description: |
        The default logging level. Valid Values: `"DEBUG"`, `"INFO"`, `"ERROR"`, `"WARN"`, `"DISABLED"`.
    disableAllLogs:
      type: boolean
      description: |
        If `true` all logs are disabled. The default is `false`.
    roleArn:
      type: string
      description: |
        The ARN of the role that allows IoT to write to Cloudwatch logs.
  type: object
