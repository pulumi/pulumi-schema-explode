$token: aws:lightsail/ContainerServiceDeploymentVersionPublicEndpoint:ContainerServiceDeploymentVersionPublicEndpoint
properties:
  containerName:
    type: string
    description: |
      The name of the container for the endpoint.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  containerPort:
    type: integer
    description: |
      The port of the container to which traffic is forwarded to.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  healthCheck:
    $ref: "#/types/aws:lightsail/ContainerServiceDeploymentVersionPublicEndpointHealthCheck:ContainerServiceDeploymentVersionPublicEndpointHealthCheck"
    description: |
      A configuration block that describes the health check configuration of the container. Detailed below.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - containerName
  - containerPort
  - healthCheck
