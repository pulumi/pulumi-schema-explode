$token: aws:lightsail/InstancePublicPortsPortInfo:InstancePublicPortsPortInfo
properties:
  cidrs:
    type: array
    items:
      type: string
    description: |
      Set of CIDR blocks.
    language:
      python:
        mapCase: false
  fromPort:
    type: integer
    description: |
      First port in a range of open ports on an instance.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  protocol:
    type: string
    description: |
      IP protocol name. Valid values are `tcp`, `all`, `udp`, and `icmp`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  toPort:
    type: integer
    description: |
      Last port in a range of open ports on an instance.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - fromPort
  - protocol
  - toPort
language:
  nodejs:
    requiredOutputs:
      - cidrs
      - fromPort
      - protocol
      - toPort
