$token: aws:lightsail/staticIpAttachment:StaticIpAttachment
properties:
  instanceName:
    type: string
    description: |
      The name of the Lightsail instance to attach the IP to
  ipAddress:
    type: string
    description: |
      The allocated static IP address
  staticIpName:
    type: string
    description: |
      The name of the allocated static IP
required:
  - instanceName
  - ipAddress
  - staticIpName
inputProperties:
  instanceName:
    type: string
    description: |
      The name of the Lightsail instance to attach the IP to
    willReplaceOnChanges: true
  staticIpName:
    type: string
    description: |
      The name of the allocated static IP
    willReplaceOnChanges: true
requiredInputs:
  - instanceName
  - staticIpName
stateInputs:
  description: |
    Input properties used for looking up and filtering StaticIpAttachment resources.
  properties:
    instanceName:
      type: string
      description: |
        The name of the Lightsail instance to attach the IP to
      willReplaceOnChanges: true
    ipAddress:
      type: string
      description: |
        The allocated static IP address
    staticIpName:
      type: string
      description: |
        The name of the allocated static IP
      willReplaceOnChanges: true
  type: object
