$token: aws:lightsail/domain:Domain
properties:
  arn:
    type: string
    description: |
      The ARN of the Lightsail domain
  domainName:
    type: string
    description: |
      The name of the Lightsail domain to manage
required:
  - arn
  - domainName
inputProperties:
  domainName:
    type: string
    description: |
      The name of the Lightsail domain to manage
    willReplaceOnChanges: true
requiredInputs:
  - domainName
stateInputs:
  description: |
    Input properties used for looking up and filtering Domain resources.
  properties:
    arn:
      type: string
      description: |
        The ARN of the Lightsail domain
    domainName:
      type: string
      description: |
        The name of the Lightsail domain to manage
      willReplaceOnChanges: true
  type: object
