$token: aws:lightsail/staticIp:StaticIp
properties:
  arn:
    type: string
    description: |
      The ARN of the Lightsail static IP
  ipAddress:
    type: string
    description: |
      The allocated static IP address
  name:
    type: string
    description: |
      The name for the allocated static IP
  supportCode:
    type: string
    description: |
      The support code.
required:
  - arn
  - ipAddress
  - name
  - supportCode
inputProperties:
  name:
    type: string
    description: |
      The name for the allocated static IP
    willReplaceOnChanges: true
stateInputs:
  description: |
    Input properties used for looking up and filtering StaticIp resources.
  properties:
    arn:
      type: string
      description: |
        The ARN of the Lightsail static IP
    ipAddress:
      type: string
      description: |
        The allocated static IP address
    name:
      type: string
      description: |
        The name for the allocated static IP
      willReplaceOnChanges: true
    supportCode:
      type: string
      description: |
        The support code.
  type: object
