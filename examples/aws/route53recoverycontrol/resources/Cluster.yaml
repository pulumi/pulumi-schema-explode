$token: aws:route53recoverycontrol/cluster:Cluster
properties:
  arn:
    type: string
    description: |
      ARN of the cluster
  clusterEndpoints:
    type: array
    items:
      $ref: "#/types/aws:route53recoverycontrol/ClusterClusterEndpoint:ClusterClusterEndpoint"
    description: |
      List of 5 endpoints in 5 regions that can be used to talk to the cluster. See below.
  name:
    type: string
    description: |
      Unique name describing the cluster.
  status:
    type: string
    description: |
      Status of cluster. `PENDING` when it is being created, `PENDING_DELETION` when it is being deleted and `DEPLOYED` otherwise.
required:
  - arn
  - clusterEndpoints
  - name
  - status
inputProperties:
  name:
    type: string
    description: |
      Unique name describing the cluster.
    willReplaceOnChanges: true
stateInputs:
  description: |
    Input properties used for looking up and filtering Cluster resources.
  properties:
    arn:
      type: string
      description: |
        ARN of the cluster
    clusterEndpoints:
      type: array
      items:
        $ref: "#/types/aws:route53recoverycontrol/ClusterClusterEndpoint:ClusterClusterEndpoint"
      description: |
        List of 5 endpoints in 5 regions that can be used to talk to the cluster. See below.
    name:
      type: string
      description: |
        Unique name describing the cluster.
      willReplaceOnChanges: true
    status:
      type: string
      description: |
        Status of cluster. `PENDING` when it is being created, `PENDING_DELETION` when it is being deleted and `DEPLOYED` otherwise.
  type: object
