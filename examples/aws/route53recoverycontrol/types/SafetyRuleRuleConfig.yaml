$token: aws:route53recoverycontrol/SafetyRuleRuleConfig:SafetyRuleRuleConfig
properties:
  inverted:
    type: boolean
    description: |
      Logical negation of the rule.
    language:
      python:
        mapCase: false
  threshold:
    type: integer
    description: |
      Number of controls that must be set when you specify an `ATLEAST` type rule.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      Rule type. Valid values are `ATLEAST`, `AND`, and `OR`.
    language:
      python:
        mapCase: false
type: object
required:
  - inverted
  - threshold
  - type
