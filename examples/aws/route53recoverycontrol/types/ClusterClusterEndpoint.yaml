$token: aws:route53recoverycontrol/ClusterClusterEndpoint:ClusterClusterEndpoint
properties:
  endpoint:
    type: string
    description: |
      Cluster endpoint.
    language:
      python:
        mapCase: false
  region:
    type: string
    description: |
      Region of the endpoint.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - endpoint
      - region
