$token: aws:dlm/LifecyclePolicyPolicyDetailsActionCrossRegionCopy:LifecyclePolicyPolicyDetailsActionCrossRegionCopy
properties:
  encryptionConfiguration:
    $ref: "#/types/aws:dlm/LifecyclePolicyPolicyDetailsActionCrossRegionCopyEncryptionConfiguration:LifecyclePolicyPolicyDetailsActionCrossRegionCopyEncryptionConfiguration"
    description: |
      The encryption settings for the copied snapshot. See the `encryption_configuration` block. Max of 1 per action.
    language:
      python:
        mapCase: false
  retainRule:
    $ref: "#/types/aws:dlm/LifecyclePolicyPolicyDetailsActionCrossRegionCopyRetainRule:LifecyclePolicyPolicyDetailsActionCrossRegionCopyRetainRule"
    description: |
      The retention rule that indicates how long snapshot copies are to be retained in the destination Region. See the `retain_rule` block. Max of 1 per schedule.
    language:
      python:
        mapCase: false
  target:
    type: string
    description: |
      The target Region or the Amazon Resource Name (ARN) of the target Outpost for the snapshot copies.
    language:
      python:
        mapCase: false
type: object
required:
  - encryptionConfiguration
  - target
