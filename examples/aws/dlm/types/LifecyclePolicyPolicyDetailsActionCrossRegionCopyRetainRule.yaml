$token: aws:dlm/LifecyclePolicyPolicyDetailsActionCrossRegionCopyRetainRule:LifecyclePolicyPolicyDetailsActionCrossRegionCopyRetainRule
properties:
  interval:
    type: integer
    description: |
      The amount of time to retain each snapshot. The maximum is 100 years. This is equivalent to 1200 months, 5200 weeks, or 36500 days.
    language:
      python:
        mapCase: false
  intervalUnit:
    type: string
    description: |
      The unit of time for time-based retention. Valid values: `DAYS`, `WEEKS`, `MONTHS`, or `YEARS`.
    language:
      python:
        mapCase: false
type: object
required:
  - interval
  - intervalUnit
