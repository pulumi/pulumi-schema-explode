$token: aws:dlm/LifecyclePolicyPolicyDetailsScheduleShareRule:LifecyclePolicyPolicyDetailsScheduleShareRule
properties:
  targetAccounts:
    type: array
    items:
      type: string
    description: |
      The IDs of the AWS accounts with which to share the snapshots.
    language:
      python:
        mapCase: false
  unshareInterval:
    type: integer
    language:
      python:
        mapCase: false
  unshareIntervalUnit:
    type: string
    language:
      python:
        mapCase: false
type: object
required:
  - targetAccounts
