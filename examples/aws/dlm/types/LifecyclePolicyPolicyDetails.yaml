$token: aws:dlm/LifecyclePolicyPolicyDetails:LifecyclePolicyPolicyDetails
properties:
  action:
    $ref: "#/types/aws:dlm/LifecyclePolicyPolicyDetailsAction:LifecyclePolicyPolicyDetailsAction"
    description: |
      The actions to be performed when the event-based policy is triggered. You can specify only one action per policy. This parameter is required for event-based policies only. If you are creating a snapshot or AMI policy, omit this parameter. See the `action` configuration block.
    language:
      python:
        mapCase: false
  eventSource:
    $ref: "#/types/aws:dlm/LifecyclePolicyPolicyDetailsEventSource:LifecyclePolicyPolicyDetailsEventSource"
    description: |
      The event that triggers the event-based policy. This parameter is required for event-based policies only. If you are creating a snapshot or AMI policy, omit this parameter. See the `event_source` configuration block.
    language:
      python:
        mapCase: false
  parameters:
    $ref: "#/types/aws:dlm/LifecyclePolicyPolicyDetailsParameters:LifecyclePolicyPolicyDetailsParameters"
    description: |
      Information about the event. See the `parameters` configuration block.
    language:
      python:
        mapCase: false
  policyType:
    type: string
    description: |
      The valid target resource types and actions a policy can manage. Specify `EBS_SNAPSHOT_MANAGEMENT` to create a lifecycle policy that manages the lifecycle of Amazon EBS snapshots. Specify `IMAGE_MANAGEMENT` to create a lifecycle policy that manages the lifecycle of EBS-backed AMIs. Specify `EVENT_BASED_POLICY` to create an event-based policy that performs specific actions when a defined event occurs in your AWS account. Default value is `EBS_SNAPSHOT_MANAGEMENT`.
    language:
      python:
        mapCase: false
  resourceLocations:
    type: string
    description: |
      The location of the resources to backup. If the source resources are located in an AWS Region, specify `CLOUD`. If the source resources are located on an Outpost in your account, specify `OUTPOST`. If you specify `OUTPOST`, Amazon Data Lifecycle Manager backs up all resources of the specified type with matching target tags across all of the Outposts in your account. Valid values are `CLOUD` and `OUTPOST`.
    language:
      python:
        mapCase: false
  resourceTypes:
    type: array
    items:
      type: string
    description: |
      A list of resource types that should be targeted by the lifecycle policy. Valid values are `VOLUME` and `INSTANCE`.
    language:
      python:
        mapCase: false
  schedules:
    type: array
    items:
      $ref: "#/types/aws:dlm/LifecyclePolicyPolicyDetailsSchedule:LifecyclePolicyPolicyDetailsSchedule"
    description: |
      See the `schedule` configuration block.
    language:
      python:
        mapCase: false
  targetTags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tag keys and their values. Any resources that match the `resource_types` and are tagged with _any_ of these tags will be targeted.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - resourceLocations
