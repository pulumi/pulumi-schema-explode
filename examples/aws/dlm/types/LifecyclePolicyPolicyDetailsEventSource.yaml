$token: aws:dlm/LifecyclePolicyPolicyDetailsEventSource:LifecyclePolicyPolicyDetailsEventSource
properties:
  parameters:
    $ref: "#/types/aws:dlm/LifecyclePolicyPolicyDetailsEventSourceParameters:LifecyclePolicyPolicyDetailsEventSourceParameters"
    description: |
      Information about the event. See the `parameters` configuration block.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      The source of the event. Currently only managed CloudWatch Events rules are supported. Valid values are `MANAGED_CWE`.
    language:
      python:
        mapCase: false
type: object
required:
  - parameters
  - type
