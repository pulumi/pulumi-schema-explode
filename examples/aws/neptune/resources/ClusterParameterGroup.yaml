$token: aws:neptune/clusterParameterGroup:ClusterParameterGroup
properties:
  arn:
    type: string
    description: |
      The ARN of the neptune cluster parameter group.
  description:
    type: string
    description: |
      The description of the neptune cluster parameter group. Defaults to "Managed by Pulumi".
  family:
    type: string
    description: |
      The family of the neptune cluster parameter group.
  name:
    type: string
    description: |
      The name of the neptune parameter.
  namePrefix:
    type: string
    description: |
      Creates a unique name beginning with the specified prefix. Conflicts with `name`.
  parameters:
    type: array
    items:
      $ref: "#/types/aws:neptune/ClusterParameterGroupParameter:ClusterParameterGroupParameter"
    description: |
      A list of neptune parameters to apply.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - family
  - name
  - namePrefix
  - tagsAll
inputProperties:
  description:
    type: string
    description: |
      The description of the neptune cluster parameter group. Defaults to "Managed by Pulumi".
    willReplaceOnChanges: true
  family:
    type: string
    description: |
      The family of the neptune cluster parameter group.
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      The name of the neptune parameter.
    willReplaceOnChanges: true
  namePrefix:
    type: string
    description: |
      Creates a unique name beginning with the specified prefix. Conflicts with `name`.
    willReplaceOnChanges: true
  parameters:
    type: array
    items:
      $ref: "#/types/aws:neptune/ClusterParameterGroupParameter:ClusterParameterGroupParameter"
    description: |
      A list of neptune parameters to apply.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - family
stateInputs:
  description: |
    Input properties used for looking up and filtering ClusterParameterGroup resources.
  properties:
    arn:
      type: string
      description: |
        The ARN of the neptune cluster parameter group.
    description:
      type: string
      description: |
        The description of the neptune cluster parameter group. Defaults to "Managed by Pulumi".
      willReplaceOnChanges: true
    family:
      type: string
      description: |
        The family of the neptune cluster parameter group.
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        The name of the neptune parameter.
      willReplaceOnChanges: true
    namePrefix:
      type: string
      description: |
        Creates a unique name beginning with the specified prefix. Conflicts with `name`.
      willReplaceOnChanges: true
    parameters:
      type: array
      items:
        $ref: "#/types/aws:neptune/ClusterParameterGroupParameter:ClusterParameterGroupParameter"
      description: |
        A list of neptune parameters to apply.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
