$token: aws:neptune/ParameterGroupParameter:ParameterGroupParameter
properties:
  applyMethod:
    type: string
    description: |
      The apply method of the Neptune parameter. Valid values are `immediate` and `pending-reboot`. Defaults to `pending-reboot`.
    language:
      python:
        mapCase: false
  name:
    type: string
    description: |
      The name of the Neptune parameter.
    language:
      python:
        mapCase: false
  value:
    type: string
    description: |
      The value of the Neptune parameter.
    language:
      python:
        mapCase: false
type: object
required:
  - name
  - value
