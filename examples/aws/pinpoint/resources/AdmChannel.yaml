$token: aws:pinpoint/admChannel:AdmChannel
properties:
  applicationId:
    type: string
    description: |
      The application ID.
  clientId:
    type: string
    description: |
      Client ID (part of OAuth Credentials) obtained via Amazon Developer Account.
  clientSecret:
    type: string
    description: |
      Client Secret (part of OAuth Credentials) obtained via Amazon Developer Account.
  enabled:
    type: boolean
    description: |
      Specifies whether to enable the channel. Defaults to `true`.
required:
  - applicationId
  - clientId
  - clientSecret
inputProperties:
  applicationId:
    type: string
    description: |
      The application ID.
    willReplaceOnChanges: true
  clientId:
    type: string
    description: |
      Client ID (part of OAuth Credentials) obtained via Amazon Developer Account.
  clientSecret:
    type: string
    description: |
      Client Secret (part of OAuth Credentials) obtained via Amazon Developer Account.
  enabled:
    type: boolean
    description: |
      Specifies whether to enable the channel. Defaults to `true`.
requiredInputs:
  - applicationId
  - clientId
  - clientSecret
stateInputs:
  description: |
    Input properties used for looking up and filtering AdmChannel resources.
  properties:
    applicationId:
      type: string
      description: |
        The application ID.
      willReplaceOnChanges: true
    clientId:
      type: string
      description: |
        Client ID (part of OAuth Credentials) obtained via Amazon Developer Account.
    clientSecret:
      type: string
      description: |
        Client Secret (part of OAuth Credentials) obtained via Amazon Developer Account.
    enabled:
      type: boolean
      description: |
        Specifies whether to enable the channel. Defaults to `true`.
  type: object
