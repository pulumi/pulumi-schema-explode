$token: aws:pinpoint/gcmChannel:GcmChannel
properties:
  apiKey:
    type: string
    description: |
      Platform credential API key from Google.
  applicationId:
    type: string
    description: |
      The application ID.
  enabled:
    type: boolean
    description: |
      Whether the channel is enabled or disabled. Defaults to `true`.
required:
  - apiKey
  - applicationId
inputProperties:
  apiKey:
    type: string
    description: |
      Platform credential API key from Google.
  applicationId:
    type: string
    description: |
      The application ID.
    willReplaceOnChanges: true
  enabled:
    type: boolean
    description: |
      Whether the channel is enabled or disabled. Defaults to `true`.
requiredInputs:
  - apiKey
  - applicationId
stateInputs:
  description: |
    Input properties used for looking up and filtering GcmChannel resources.
  properties:
    apiKey:
      type: string
      description: |
        Platform credential API key from Google.
    applicationId:
      type: string
      description: |
        The application ID.
      willReplaceOnChanges: true
    enabled:
      type: boolean
      description: |
        Whether the channel is enabled or disabled. Defaults to `true`.
  type: object
