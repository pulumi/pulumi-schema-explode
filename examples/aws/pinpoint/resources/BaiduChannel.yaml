$token: aws:pinpoint/baiduChannel:BaiduChannel
properties:
  apiKey:
    type: string
    description: |
      Platform credential API key from Baidu.
  applicationId:
    type: string
    description: |
      The application ID.
  enabled:
    type: boolean
    description: |
      Specifies whether to enable the channel. Defaults to `true`.
  secretKey:
    type: string
    description: |
      Platform credential Secret key from Baidu.
required:
  - apiKey
  - applicationId
  - secretKey
inputProperties:
  apiKey:
    type: string
    description: |
      Platform credential API key from Baidu.
  applicationId:
    type: string
    description: |
      The application ID.
    willReplaceOnChanges: true
  enabled:
    type: boolean
    description: |
      Specifies whether to enable the channel. Defaults to `true`.
  secretKey:
    type: string
    description: |
      Platform credential Secret key from Baidu.
requiredInputs:
  - apiKey
  - applicationId
  - secretKey
stateInputs:
  description: |
    Input properties used for looking up and filtering BaiduChannel resources.
  properties:
    apiKey:
      type: string
      description: |
        Platform credential API key from Baidu.
    applicationId:
      type: string
      description: |
        The application ID.
      willReplaceOnChanges: true
    enabled:
      type: boolean
      description: |
        Specifies whether to enable the channel. Defaults to `true`.
    secretKey:
      type: string
      description: |
        Platform credential Secret key from Baidu.
  type: object
