$token: aws:pinpoint/AppQuietTime:AppQuietTime
properties:
  end:
    type: string
    description: |
      The default end time for quiet time in ISO 8601 format. Required if `start` is set
    language:
      python:
        mapCase: false
  start:
    type: string
    description: |
      The default start time for quiet time in ISO 8601 format. Required if `end` is set
    language:
      python:
        mapCase: false
type: object
