$token: aws:redshiftserverless/EndpointAccessVpcEndpoint:EndpointAccessVpcEndpoint
properties:
  networkInterfaces:
    type: array
    items:
      $ref: "#/types/aws:redshiftserverless/EndpointAccessVpcEndpointNetworkInterface:EndpointAccessVpcEndpointNetworkInterface"
    description: |
      The network interfaces of the endpoint.. See `Network Interface` below.
    language:
      python:
        mapCase: false
  vpcEndpointId:
    type: string
    description: |
      The DNS address of the VPC endpoint.
    language:
      python:
        mapCase: false
  vpcId:
    type: string
    description: |
      The port that Amazon Redshift Serverless listens on.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - networkInterfaces
      - vpcEndpointId
      - vpcId
