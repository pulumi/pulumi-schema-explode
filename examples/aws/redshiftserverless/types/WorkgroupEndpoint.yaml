$token: aws:redshiftserverless/WorkgroupEndpoint:WorkgroupEndpoint
properties:
  address:
    type: string
    description: |
      The DNS address of the VPC endpoint.
    language:
      python:
        mapCase: false
  port:
    type: integer
    description: |
      The port that Amazon Redshift Serverless listens on.
    language:
      python:
        mapCase: false
  vpcEndpoints:
    type: array
    items:
      $ref: "#/types/aws:redshiftserverless/WorkgroupEndpointVpcEndpoint:WorkgroupEndpointVpcEndpoint"
    description: |
      The VPC endpoint or the Redshift Serverless workgroup. See `VPC Endpoint` below.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - address
      - port
      - vpcEndpoints
