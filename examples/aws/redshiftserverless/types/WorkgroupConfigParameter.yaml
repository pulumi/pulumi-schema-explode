$token: aws:redshiftserverless/WorkgroupConfigParameter:WorkgroupConfigParameter
properties:
  parameterKey:
    type: string
    description: |
      The key of the parameter. The options are `datestyle`, `enable_user_activity_logging`, `query_group`, `search_path`, and `max_query_execution_time`.
    language:
      python:
        mapCase: false
  parameterValue:
    type: string
    description: |
      The value of the parameter to set.
    language:
      python:
        mapCase: false
type: object
required:
  - parameterKey
  - parameterValue
