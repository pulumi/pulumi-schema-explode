$token: aws:elastictranscoder/preset:Preset
properties:
  arn:
    type: string
    description: |
      Amazon Resource Name (ARN) of the Elastic Transcoder Preset.
  audio:
    $ref: "#/types/aws:elastictranscoder/PresetAudio:PresetAudio"
    description: |
      Audio parameters object (documented below).
  audioCodecOptions:
    $ref: "#/types/aws:elastictranscoder/PresetAudioCodecOptions:PresetAudioCodecOptions"
    description: |
      Codec options for the audio parameters (documented below)
  container:
    type: string
    description: |
      The container type for the output file. Valid values are `flac`, `flv`, `fmp4`, `gif`, `mp3`, `mp4`, `mpg`, `mxf`, `oga`, `ogg`, `ts`, and `webm`.
  description:
    type: string
    description: |
      A description of the preset (maximum 255 characters)
  name:
    type: string
    description: |
      The name of the preset. (maximum 40 characters)
  thumbnails:
    $ref: "#/types/aws:elastictranscoder/PresetThumbnails:PresetThumbnails"
    description: |
      Thumbnail parameters object (documented below)
  type:
    type: string
  video:
    $ref: "#/types/aws:elastictranscoder/PresetVideo:PresetVideo"
    description: |
      Video parameters object (documented below)
  videoCodecOptions:
    type: object
    additionalProperties:
      type: string
    description: |
      Codec options for the video parameters
  videoWatermarks:
    type: array
    items:
      $ref: "#/types/aws:elastictranscoder/PresetVideoWatermark:PresetVideoWatermark"
    description: |
      Watermark parameters for the video parameters (documented below)
required:
  - arn
  - audioCodecOptions
  - container
  - name
  - type
inputProperties:
  audio:
    $ref: "#/types/aws:elastictranscoder/PresetAudio:PresetAudio"
    description: |
      Audio parameters object (documented below).
    willReplaceOnChanges: true
  audioCodecOptions:
    $ref: "#/types/aws:elastictranscoder/PresetAudioCodecOptions:PresetAudioCodecOptions"
    description: |
      Codec options for the audio parameters (documented below)
    willReplaceOnChanges: true
  container:
    type: string
    description: |
      The container type for the output file. Valid values are `flac`, `flv`, `fmp4`, `gif`, `mp3`, `mp4`, `mpg`, `mxf`, `oga`, `ogg`, `ts`, and `webm`.
    willReplaceOnChanges: true
  description:
    type: string
    description: |
      A description of the preset (maximum 255 characters)
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      The name of the preset. (maximum 40 characters)
    willReplaceOnChanges: true
  thumbnails:
    $ref: "#/types/aws:elastictranscoder/PresetThumbnails:PresetThumbnails"
    description: |
      Thumbnail parameters object (documented below)
    willReplaceOnChanges: true
  type:
    type: string
  video:
    $ref: "#/types/aws:elastictranscoder/PresetVideo:PresetVideo"
    description: |
      Video parameters object (documented below)
    willReplaceOnChanges: true
  videoCodecOptions:
    type: object
    additionalProperties:
      type: string
    description: |
      Codec options for the video parameters
    willReplaceOnChanges: true
  videoWatermarks:
    type: array
    items:
      $ref: "#/types/aws:elastictranscoder/PresetVideoWatermark:PresetVideoWatermark"
    description: |
      Watermark parameters for the video parameters (documented below)
    willReplaceOnChanges: true
requiredInputs:
  - container
stateInputs:
  description: |
    Input properties used for looking up and filtering Preset resources.
  properties:
    arn:
      type: string
      description: |
        Amazon Resource Name (ARN) of the Elastic Transcoder Preset.
    audio:
      $ref: "#/types/aws:elastictranscoder/PresetAudio:PresetAudio"
      description: |
        Audio parameters object (documented below).
      willReplaceOnChanges: true
    audioCodecOptions:
      $ref: "#/types/aws:elastictranscoder/PresetAudioCodecOptions:PresetAudioCodecOptions"
      description: |
        Codec options for the audio parameters (documented below)
      willReplaceOnChanges: true
    container:
      type: string
      description: |
        The container type for the output file. Valid values are `flac`, `flv`, `fmp4`, `gif`, `mp3`, `mp4`, `mpg`, `mxf`, `oga`, `ogg`, `ts`, and `webm`.
      willReplaceOnChanges: true
    description:
      type: string
      description: |
        A description of the preset (maximum 255 characters)
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        The name of the preset. (maximum 40 characters)
      willReplaceOnChanges: true
    thumbnails:
      $ref: "#/types/aws:elastictranscoder/PresetThumbnails:PresetThumbnails"
      description: |
        Thumbnail parameters object (documented below)
      willReplaceOnChanges: true
    type:
      type: string
    video:
      $ref: "#/types/aws:elastictranscoder/PresetVideo:PresetVideo"
      description: |
        Video parameters object (documented below)
      willReplaceOnChanges: true
    videoCodecOptions:
      type: object
      additionalProperties:
        type: string
      description: |
        Codec options for the video parameters
      willReplaceOnChanges: true
    videoWatermarks:
      type: array
      items:
        $ref: "#/types/aws:elastictranscoder/PresetVideoWatermark:PresetVideoWatermark"
      description: |
        Watermark parameters for the video parameters (documented below)
      willReplaceOnChanges: true
  type: object
