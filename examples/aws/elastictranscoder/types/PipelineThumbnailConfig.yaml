$token: aws:elastictranscoder/PipelineThumbnailConfig:PipelineThumbnailConfig
properties:
  bucket:
    type: string
    description: |
      The Amazon S3 bucket in which you want Elastic Transcoder to save thumbnail files.
    language:
      python:
        mapCase: false
  storageClass:
    type: string
    description: |
      The Amazon S3 storage class, Standard or ReducedRedundancy, that you want Elastic Transcoder to assign to the thumbnails that it stores in your Amazon S3 bucket.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - bucket
