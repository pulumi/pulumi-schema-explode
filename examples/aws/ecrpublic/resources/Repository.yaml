$token: aws:ecrpublic/repository:Repository
properties:
  arn:
    type: string
    description: |
      Full ARN of the repository.
  catalogData:
    $ref: "#/types/aws:ecrpublic/RepositoryCatalogData:RepositoryCatalogData"
    description: |
      Catalog data configuration for the repository. See below for schema.
  forceDestroy:
    type: boolean
  registryId:
    type: string
    description: |
      The registry ID where the repository was created.
  repositoryName:
    type: string
    description: |
      Name of the repository.
  repositoryUri:
    type: string
    description: |
      The URI of the repository.
  tags:
    type: object
    additionalProperties:
      type: string
  tagsAll:
    type: object
    additionalProperties:
      type: string
required:
  - arn
  - registryId
  - repositoryName
  - repositoryUri
  - tagsAll
inputProperties:
  catalogData:
    $ref: "#/types/aws:ecrpublic/RepositoryCatalogData:RepositoryCatalogData"
    description: |
      Catalog data configuration for the repository. See below for schema.
  forceDestroy:
    type: boolean
  repositoryName:
    type: string
    description: |
      Name of the repository.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
requiredInputs:
  - repositoryName
stateInputs:
  description: |
    Input properties used for looking up and filtering Repository resources.
  properties:
    arn:
      type: string
      description: |
        Full ARN of the repository.
    catalogData:
      $ref: "#/types/aws:ecrpublic/RepositoryCatalogData:RepositoryCatalogData"
      description: |
        Catalog data configuration for the repository. See below for schema.
    forceDestroy:
      type: boolean
    registryId:
      type: string
      description: |
        The registry ID where the repository was created.
    repositoryName:
      type: string
      description: |
        Name of the repository.
      willReplaceOnChanges: true
    repositoryUri:
      type: string
      description: |
        The URI of the repository.
    tags:
      type: object
      additionalProperties:
        type: string
    tagsAll:
      type: object
      additionalProperties:
        type: string
  type: object
