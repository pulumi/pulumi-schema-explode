$token: aws:codedeploy/DeploymentGroupLoadBalancerInfoTargetGroupPairInfo:DeploymentGroupLoadBalancerInfoTargetGroupPairInfo
properties:
  prodTrafficRoute:
    $ref: "#/types/aws:codedeploy/DeploymentGroupLoadBalancerInfoTargetGroupPairInfoProdTrafficRoute:DeploymentGroupLoadBalancerInfoTargetGroupPairInfoProdTrafficRoute"
    description: |
      Configuration block for the production traffic route (documented below).
    language:
      python:
        mapCase: false
  targetGroups:
    type: array
    items:
      $ref: "#/types/aws:codedeploy/DeploymentGroupLoadBalancerInfoTargetGroupPairInfoTargetGroup:DeploymentGroupLoadBalancerInfoTargetGroupPairInfoTargetGroup"
    description: |
      Configuration blocks for a target group within a target group pair (documented below).
    language:
      python:
        mapCase: false
  testTrafficRoute:
    $ref: "#/types/aws:codedeploy/DeploymentGroupLoadBalancerInfoTargetGroupPairInfoTestTrafficRoute:DeploymentGroupLoadBalancerInfoTargetGroupPairInfoTestTrafficRoute"
    description: |
      Configuration block for the test traffic route (documented below).
    language:
      python:
        mapCase: false
type: object
required:
  - prodTrafficRoute
  - targetGroups
