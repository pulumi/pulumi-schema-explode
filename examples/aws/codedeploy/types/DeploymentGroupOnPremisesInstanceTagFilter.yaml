$token: aws:codedeploy/DeploymentGroupOnPremisesInstanceTagFilter:DeploymentGroupOnPremisesInstanceTagFilter
properties:
  key:
    type: string
    description: |
      The key of the tag filter.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      The type of the tag filter, either `KEY_ONLY`, `VALUE_ONLY`, or `KEY_AND_VALUE`.
    language:
      python:
        mapCase: false
  value:
    type: string
    description: |
      The value of the tag filter.
    language:
      python:
        mapCase: false
type: object
