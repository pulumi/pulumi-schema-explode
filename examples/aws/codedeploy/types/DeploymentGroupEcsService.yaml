$token: aws:codedeploy/DeploymentGroupEcsService:DeploymentGroupEcsService
properties:
  clusterName:
    type: string
    description: |
      The name of the ECS cluster.
    language:
      python:
        mapCase: false
  serviceName:
    type: string
    description: |
      The name of the ECS service.
    language:
      python:
        mapCase: false
type: object
required:
  - clusterName
  - serviceName
