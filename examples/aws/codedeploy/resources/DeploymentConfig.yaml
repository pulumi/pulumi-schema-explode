$token: aws:codedeploy/deploymentConfig:DeploymentConfig
properties:
  computePlatform:
    type: string
    description: |
      The compute platform can be `Server`, `Lambda`, or `ECS`. Default is `Server`.
  deploymentConfigId:
    type: string
    description: |
      The AWS Assigned deployment config id
  deploymentConfigName:
    type: string
    description: |
      The name of the deployment config.
  minimumHealthyHosts:
    $ref: "#/types/aws:codedeploy/DeploymentConfigMinimumHealthyHosts:DeploymentConfigMinimumHealthyHosts"
    description: |
      A minimum_healthy_hosts block. Required for `Server` compute platform. Minimum Healthy Hosts are documented below.
  trafficRoutingConfig:
    $ref: "#/types/aws:codedeploy/DeploymentConfigTrafficRoutingConfig:DeploymentConfigTrafficRoutingConfig"
    description: |
      A traffic_routing_config block. Traffic Routing Config is documented below.
required:
  - deploymentConfigId
  - deploymentConfigName
inputProperties:
  computePlatform:
    type: string
    description: |
      The compute platform can be `Server`, `Lambda`, or `ECS`. Default is `Server`.
    willReplaceOnChanges: true
  deploymentConfigName:
    type: string
    description: |
      The name of the deployment config.
    willReplaceOnChanges: true
  minimumHealthyHosts:
    $ref: "#/types/aws:codedeploy/DeploymentConfigMinimumHealthyHosts:DeploymentConfigMinimumHealthyHosts"
    description: |
      A minimum_healthy_hosts block. Required for `Server` compute platform. Minimum Healthy Hosts are documented below.
    willReplaceOnChanges: true
  trafficRoutingConfig:
    $ref: "#/types/aws:codedeploy/DeploymentConfigTrafficRoutingConfig:DeploymentConfigTrafficRoutingConfig"
    description: |
      A traffic_routing_config block. Traffic Routing Config is documented below.
    willReplaceOnChanges: true
stateInputs:
  description: |
    Input properties used for looking up and filtering DeploymentConfig resources.
  properties:
    computePlatform:
      type: string
      description: |
        The compute platform can be `Server`, `Lambda`, or `ECS`. Default is `Server`.
      willReplaceOnChanges: true
    deploymentConfigId:
      type: string
      description: |
        The AWS Assigned deployment config id
    deploymentConfigName:
      type: string
      description: |
        The name of the deployment config.
      willReplaceOnChanges: true
    minimumHealthyHosts:
      $ref: "#/types/aws:codedeploy/DeploymentConfigMinimumHealthyHosts:DeploymentConfigMinimumHealthyHosts"
      description: |
        A minimum_healthy_hosts block. Required for `Server` compute platform. Minimum Healthy Hosts are documented below.
      willReplaceOnChanges: true
    trafficRoutingConfig:
      $ref: "#/types/aws:codedeploy/DeploymentConfigTrafficRoutingConfig:DeploymentConfigTrafficRoutingConfig"
      description: |
        A traffic_routing_config block. Traffic Routing Config is documented below.
      willReplaceOnChanges: true
  type: object
