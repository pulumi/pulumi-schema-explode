$token: aws:alb/listenerRule:ListenerRule
properties:
  actions:
    type: array
    items:
      $ref: "#/types/aws:alb/ListenerRuleAction:ListenerRuleAction"
    description: |
      An Action block. Action blocks are documented below.
  arn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the target group.
  conditions:
    type: array
    items:
      $ref: "#/types/aws:alb/ListenerRuleCondition:ListenerRuleCondition"
    description: |
      A Condition block. Multiple condition blocks of different types can be set and all must be satisfied for the rule to match. Condition blocks are documented below.
  listenerArn:
    type: string
    description: |
      The ARN of the listener to which to attach the rule.
  priority:
    type: integer
    description: |
      The priority for the rule between `1` and `50000`. Leaving it unset will automatically set the rule with next available priority after currently existing highest rule. A listener can't have multiple rules with the same priority.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - actions
  - arn
  - conditions
  - listenerArn
  - priority
  - tagsAll
inputProperties:
  actions:
    type: array
    items:
      $ref: "#/types/aws:alb/ListenerRuleAction:ListenerRuleAction"
    description: |
      An Action block. Action blocks are documented below.
  conditions:
    type: array
    items:
      $ref: "#/types/aws:alb/ListenerRuleCondition:ListenerRuleCondition"
    description: |
      A Condition block. Multiple condition blocks of different types can be set and all must be satisfied for the rule to match. Condition blocks are documented below.
  listenerArn:
    type: string
    description: |
      The ARN of the listener to which to attach the rule.
    willReplaceOnChanges: true
  priority:
    type: integer
    description: |
      The priority for the rule between `1` and `50000`. Leaving it unset will automatically set the rule with next available priority after currently existing highest rule. A listener can't have multiple rules with the same priority.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - actions
  - conditions
  - listenerArn
stateInputs:
  description: |
    Input properties used for looking up and filtering ListenerRule resources.
  properties:
    actions:
      type: array
      items:
        $ref: "#/types/aws:alb/ListenerRuleAction:ListenerRuleAction"
      description: |
        An Action block. Action blocks are documented below.
    arn:
      type: string
      description: |
        The Amazon Resource Name (ARN) of the target group.
    conditions:
      type: array
      items:
        $ref: "#/types/aws:alb/ListenerRuleCondition:ListenerRuleCondition"
      description: |
        A Condition block. Multiple condition blocks of different types can be set and all must be satisfied for the rule to match. Condition blocks are documented below.
    listenerArn:
      type: string
      description: |
        The ARN of the listener to which to attach the rule.
      willReplaceOnChanges: true
    priority:
      type: integer
      description: |
        The priority for the rule between `1` and `50000`. Leaving it unset will automatically set the rule with next available priority after currently existing highest rule. A listener can't have multiple rules with the same priority.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
aliases:
  - type: aws:applicationloadbalancing/listenerRule:ListenerRule
