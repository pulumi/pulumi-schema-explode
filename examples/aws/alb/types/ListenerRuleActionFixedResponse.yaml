$token: aws:alb/ListenerRuleActionFixedResponse:ListenerRuleActionFixedResponse
properties:
  contentType:
    type: string
    description: |
      The content type. Valid values are `text/plain`, `text/css`, `text/html`, `application/javascript` and `application/json`.
    language:
      python:
        mapCase: false
  messageBody:
    type: string
    description: |
      The message body.
    language:
      python:
        mapCase: false
  statusCode:
    type: string
    description: |
      The HTTP response code. Valid values are `2XX`, `4XX`, or `5XX`.
    language:
      python:
        mapCase: false
type: object
required:
  - contentType
language:
  nodejs:
    requiredOutputs:
      - contentType
      - statusCode
