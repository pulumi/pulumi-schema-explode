$token: aws:alb/ListenerRuleActionForwardStickiness:ListenerRuleActionForwardStickiness
properties:
  duration:
    type: integer
    description: |
      The time period, in seconds, during which requests from a client should be routed to the same target group. The range is 1-604800 seconds (7 days).
    language:
      python:
        mapCase: false
  enabled:
    type: boolean
    description: |
      Indicates whether target group stickiness is enabled.
    language:
      python:
        mapCase: false
type: object
required:
  - duration
