$token: aws:securityhub/standardsSubscription:StandardsSubscription
properties:
  standardsArn:
    type: string
    description: |
      The ARN of a standard - see below.
required:
  - standardsArn
inputProperties:
  standardsArn:
    type: string
    description: |
      The ARN of a standard - see below.
    willReplaceOnChanges: true
requiredInputs:
  - standardsArn
stateInputs:
  description: |
    Input properties used for looking up and filtering StandardsSubscription resources.
  properties:
    standardsArn:
      type: string
      description: |
        The ARN of a standard - see below.
      willReplaceOnChanges: true
  type: object
