$token: aws:securityhub/organizationConfiguration:OrganizationConfiguration
properties:
  autoEnable:
    type: boolean
    description: |
      Whether to automatically enable Security Hub for new accounts in the organization.
required:
  - autoEnable
inputProperties:
  autoEnable:
    type: boolean
    description: |
      Whether to automatically enable Security Hub for new accounts in the organization.
requiredInputs:
  - autoEnable
stateInputs:
  description: |
    Input properties used for looking up and filtering OrganizationConfiguration resources.
  properties:
    autoEnable:
      type: boolean
      description: |
        Whether to automatically enable Security Hub for new accounts in the organization.
  type: object
