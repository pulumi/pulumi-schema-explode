$token: aws:securityhub/actionTarget:ActionTarget
properties:
  arn:
    type: string
    description: |
      Amazon Resource Name (ARN) of the Security Hub custom action target.
  description:
    type: string
    description: |
      The name of the custom action target.
  identifier:
    type: string
    description: |
      The ID for the custom action target.
  name:
    type: string
    description: |
      The description for the custom action target.
required:
  - arn
  - description
  - identifier
  - name
inputProperties:
  description:
    type: string
    description: |
      The name of the custom action target.
  identifier:
    type: string
    description: |
      The ID for the custom action target.
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      The description for the custom action target.
requiredInputs:
  - description
  - identifier
stateInputs:
  description: |
    Input properties used for looking up and filtering ActionTarget resources.
  properties:
    arn:
      type: string
      description: |
        Amazon Resource Name (ARN) of the Security Hub custom action target.
    description:
      type: string
      description: |
        The name of the custom action target.
    identifier:
      type: string
      description: |
        The ID for the custom action target.
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        The description for the custom action target.
  type: object
