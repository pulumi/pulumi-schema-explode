$token: aws:securityhub/organizationAdminAccount:OrganizationAdminAccount
properties:
  adminAccountId:
    type: string
    description: |
      The AWS account identifier of the account to designate as the Security Hub administrator account.
required:
  - adminAccountId
inputProperties:
  adminAccountId:
    type: string
    description: |
      The AWS account identifier of the account to designate as the Security Hub administrator account.
    willReplaceOnChanges: true
requiredInputs:
  - adminAccountId
stateInputs:
  description: |
    Input properties used for looking up and filtering OrganizationAdminAccount resources.
  properties:
    adminAccountId:
      type: string
      description: |
        The AWS account identifier of the account to designate as the Security Hub administrator account.
      willReplaceOnChanges: true
  type: object
