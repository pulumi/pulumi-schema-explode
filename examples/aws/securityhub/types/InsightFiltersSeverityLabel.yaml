$token: aws:securityhub/InsightFiltersSeverityLabel:InsightFiltersSeverityLabel
properties:
  comparison:
    type: string
    description: |
      The condition to apply to a string value when querying for findings. Valid values include: `EQUALS` and `NOT_EQUALS`.
    language:
      python:
        mapCase: false
  value:
    type: string
    description: |
      A date range value for the date filter, provided as an Integer.
    language:
      python:
        mapCase: false
type: object
required:
  - comparison
  - value
