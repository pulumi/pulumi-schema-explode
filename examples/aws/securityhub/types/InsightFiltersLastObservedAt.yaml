$token: aws:securityhub/InsightFiltersLastObservedAt:InsightFiltersLastObservedAt
properties:
  dateRange:
    $ref: "#/types/aws:securityhub/InsightFiltersLastObservedAtDateRange:InsightFiltersLastObservedAtDateRange"
    description: |
      A configuration block of the date range for the date filter. See date_range below for more details.
    language:
      python:
        mapCase: false
  end:
    type: string
    description: |
      An end date for the date filter. Required with `start` if `date_range` is not specified.
    language:
      python:
        mapCase: false
  start:
    type: string
    description: |
      A start date for the date filter. Required with `end` if `date_range` is not specified.
    language:
      python:
        mapCase: false
type: object
