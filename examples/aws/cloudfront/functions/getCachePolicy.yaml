$token: aws:cloudfront/getCachePolicy:getCachePolicy
inputs:
  description: |
    A collection of arguments for invoking getCachePolicy.
  properties:
    id:
      type: string
      description: |
        Identifier for the cache policy.
    name:
      type: string
      description: |
        Unique name to identify the cache policy.
  type: object
outputs:
  description: |
    A collection of values returned by getCachePolicy.
  properties:
    comment:
      type: string
      description: |
        Comment to describe the cache policy.
    defaultTtl:
      type: integer
      description: |
        Default amount of time, in seconds, that you want objects to stay in the CloudFront cache before CloudFront sends another request to the origin to see if the object has been updated.
    etag:
      type: string
      description: |
        Current version of the cache policy.
    id:
      type: string
    maxTtl:
      type: integer
      description: |
        Maximum amount of time, in seconds, that objects stay in the CloudFront cache before CloudFront sends another request to the origin to see if the object has been updated.
    minTtl:
      type: integer
      description: |
        Minimum amount of time, in seconds, that you want objects to stay in the CloudFront cache before CloudFront sends another request to the origin to see if the object has been updated.
    name:
      type: string
    parametersInCacheKeyAndForwardedToOrigins:
      type: array
      items:
        $ref: "#/types/aws:cloudfront/getCachePolicyParametersInCacheKeyAndForwardedToOrigin:getCachePolicyParametersInCacheKeyAndForwardedToOrigin"
      description: |
        The HTTP headers, cookies, and URL query strings to include in the cache key. See Parameters In Cache Key And Forwarded To Origin for more information.
  type: object
  required:
    - comment
    - defaultTtl
    - etag
    - maxTtl
    - minTtl
    - parametersInCacheKeyAndForwardedToOrigins
