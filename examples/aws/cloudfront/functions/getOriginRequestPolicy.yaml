$token: aws:cloudfront/getOriginRequestPolicy:getOriginRequestPolicy
inputs:
  description: |
    A collection of arguments for invoking getOriginRequestPolicy.
  properties:
    id:
      type: string
      description: |
        Identifier for the origin request policy.
    name:
      type: string
      description: |
        Unique name to identify the origin request policy.
  type: object
outputs:
  description: |
    A collection of values returned by getOriginRequestPolicy.
  properties:
    comment:
      type: string
      description: |
        Comment to describe the origin request policy.
    cookiesConfigs:
      type: array
      items:
        $ref: "#/types/aws:cloudfront/getOriginRequestPolicyCookiesConfig:getOriginRequestPolicyCookiesConfig"
      description: |
        Object that determines whether any cookies in viewer requests (and if so, which cookies) are included in the origin request key and automatically included in requests that CloudFront sends to the origin. See Cookies Config for more information.
    etag:
      type: string
      description: |
        Current version of the origin request policy.
    headersConfigs:
      type: array
      items:
        $ref: "#/types/aws:cloudfront/getOriginRequestPolicyHeadersConfig:getOriginRequestPolicyHeadersConfig"
      description: |
        Object that determines whether any HTTP headers (and if so, which headers) are included in the origin request key and automatically included in requests that CloudFront sends to the origin. See Headers Config for more information.
    id:
      type: string
    name:
      type: string
    queryStringsConfigs:
      type: array
      items:
        $ref: "#/types/aws:cloudfront/getOriginRequestPolicyQueryStringsConfig:getOriginRequestPolicyQueryStringsConfig"
      description: |
        Object that determines whether any URL query strings in viewer requests (and if so, which query strings) are included in the origin request key and automatically included in requests that CloudFront sends to the origin. See Query String Config for more information.
  type: object
  required:
    - comment
    - cookiesConfigs
    - etag
    - headersConfigs
    - queryStringsConfigs
