$token: aws:cloudfront/DistributionOriginOriginShield:DistributionOriginOriginShield
properties:
  enabled:
    type: boolean
    description: |
      A flag that specifies whether Origin Shield is enabled.
    language:
      python:
        mapCase: false
  originShieldRegion:
    type: string
    description: |
      The AWS Region for Origin Shield. To specify a region, use the region code, not the region name. For example, specify the US East (Ohio) region as us-east-2.
    language:
      python:
        mapCase: false
type: object
required:
  - enabled
  - originShieldRegion
