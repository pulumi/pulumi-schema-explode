$token: aws:cloudfront/DistributionOriginGroupFailoverCriteria:DistributionOriginGroupFailoverCriteria
properties:
  statusCodes:
    type: array
    items:
      type: integer
    description: |
      A list of HTTP status codes for the origin group
    language:
      python:
        mapCase: false
type: object
required:
  - statusCodes
