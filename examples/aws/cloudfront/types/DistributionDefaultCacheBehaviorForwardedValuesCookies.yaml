$token: aws:cloudfront/DistributionDefaultCacheBehaviorForwardedValuesCookies:DistributionDefaultCacheBehaviorForwardedValuesCookies
properties:
  forward:
    type: string
    description: |
      Whether you want CloudFront to forward
      cookies to the origin that is associated with this cache behavior. You can
      specify `all`, `none` or `whitelist`. If `whitelist`, you must include the
      subsequent `whitelisted_names`
    language:
      python:
        mapCase: false
  whitelistedNames:
    type: array
    items:
      type: string
    description: |
      If you have specified `whitelist` to
      `forward`, the whitelisted cookies that you want CloudFront to forward to
      your origin.
    language:
      python:
        mapCase: false
type: object
required:
  - forward
language:
  nodejs:
    requiredOutputs:
      - forward
      - whitelistedNames
