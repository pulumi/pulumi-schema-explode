$token: aws:cloudfront/RealtimeLogConfigEndpoint:RealtimeLogConfigEndpoint
properties:
  kinesisStreamConfig:
    $ref: "#/types/aws:cloudfront/RealtimeLogConfigEndpointKinesisStreamConfig:RealtimeLogConfigEndpointKinesisStreamConfig"
    description: |
      The Amazon Kinesis data stream configuration.
    language:
      python:
        mapCase: false
  streamType:
    type: string
    description: |
      The type of data stream where real-time log data is sent. The only valid value is `Kinesis`.
    language:
      python:
        mapCase: false
type: object
required:
  - kinesisStreamConfig
  - streamType
