$token: aws:cloudfront/FieldLevelEncryptionConfigContentTypeProfileConfigContentTypeProfilesItem:FieldLevelEncryptionConfigContentTypeProfileConfigContentTypeProfilesItem
properties:
  contentType:
    type: string
    description: |
      he content type for a field-level encryption content type-profile mapping. Valid value is `application/x-www-form-urlencoded`.
    language:
      python:
        mapCase: false
  format:
    type: string
    description: |
      The format for a field-level encryption content type-profile mapping. Valid value is `URLEncoded`.
    language:
      python:
        mapCase: false
  profileId:
    type: string
    description: |
      ID of profile to use for field-level encryption query argument-profile mapping
    language:
      python:
        mapCase: false
type: object
required:
  - contentType
  - format
