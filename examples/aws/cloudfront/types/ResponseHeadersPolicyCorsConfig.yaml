$token: aws:cloudfront/ResponseHeadersPolicyCorsConfig:ResponseHeadersPolicyCorsConfig
properties:
  accessControlAllowCredentials:
    type: boolean
    description: |
      A Boolean value that CloudFront uses as the value for the `Access-Control-Allow-Credentials` HTTP response header.
    language:
      python:
        mapCase: false
  accessControlAllowHeaders:
    $ref: "#/types/aws:cloudfront/ResponseHeadersPolicyCorsConfigAccessControlAllowHeaders:ResponseHeadersPolicyCorsConfigAccessControlAllowHeaders"
    description: |
      Object that contains an attribute `items` that contains a list of HTTP header names that CloudFront includes as values for the `Access-Control-Allow-Headers` HTTP response header.
    language:
      python:
        mapCase: false
  accessControlAllowMethods:
    $ref: "#/types/aws:cloudfront/ResponseHeadersPolicyCorsConfigAccessControlAllowMethods:ResponseHeadersPolicyCorsConfigAccessControlAllowMethods"
    description: |
      Object that contains an attribute `items` that contains a list of HTTP methods that CloudFront includes as values for the `Access-Control-Allow-Methods` HTTP response header. Valid values: `GET` | `POST` | `OPTIONS` | `PUT` | `DELETE` | `HEAD` | `ALL`
    language:
      python:
        mapCase: false
  accessControlAllowOrigins:
    $ref: "#/types/aws:cloudfront/ResponseHeadersPolicyCorsConfigAccessControlAllowOrigins:ResponseHeadersPolicyCorsConfigAccessControlAllowOrigins"
    description: |
      Object that contains an attribute `items` that contains a list of origins that CloudFront can use as the value for the `Access-Control-Allow-Origin` HTTP response header.
    language:
      python:
        mapCase: false
  accessControlExposeHeaders:
    $ref: "#/types/aws:cloudfront/ResponseHeadersPolicyCorsConfigAccessControlExposeHeaders:ResponseHeadersPolicyCorsConfigAccessControlExposeHeaders"
    description: |
      Object that contains an attribute `items` that contains a list of HTTP headers that CloudFront includes as values for the `Access-Control-Expose-Headers` HTTP response header.
    language:
      python:
        mapCase: false
  accessControlMaxAgeSec:
    type: integer
    description: |
      A number that CloudFront uses as the value for the `max-age` directive in the `Strict-Transport-Security` HTTP response header.
    language:
      python:
        mapCase: false
  originOverride:
    type: boolean
    description: |
      A Boolean value that determines how CloudFront behaves for the HTTP response header.
    language:
      python:
        mapCase: false
type: object
required:
  - accessControlAllowCredentials
  - accessControlAllowHeaders
  - accessControlAllowMethods
  - accessControlAllowOrigins
  - originOverride
