$token: aws:cloudfront/FieldLevelEncryptionConfigContentTypeProfileConfig:FieldLevelEncryptionConfigContentTypeProfileConfig
properties:
  contentTypeProfiles:
    $ref: "#/types/aws:cloudfront/FieldLevelEncryptionConfigContentTypeProfileConfigContentTypeProfiles:FieldLevelEncryptionConfigContentTypeProfileConfigContentTypeProfiles"
    description: |
      Object that contains an attribute `items` that contains the list of configurations for a field-level encryption content type-profile. See Content Type Profile.
    language:
      python:
        mapCase: false
  forwardWhenContentTypeIsUnknown:
    type: boolean
    description: |
      specifies what to do when an unknown content type is provided for the profile. If true, content is forwarded without being encrypted when the content type is unknown. If false (the default), an error is returned when the content type is unknown.
    language:
      python:
        mapCase: false
type: object
required:
  - contentTypeProfiles
  - forwardWhenContentTypeIsUnknown
