$token: aws:cloudfront/FieldLevelEncryptionProfileEncryptionEntitiesItem:FieldLevelEncryptionProfileEncryptionEntitiesItem
properties:
  fieldPatterns:
    $ref: "#/types/aws:cloudfront/FieldLevelEncryptionProfileEncryptionEntitiesItemFieldPatterns:FieldLevelEncryptionProfileEncryptionEntitiesItemFieldPatterns"
    description: |
      Object that contains an attribute `items` that contains the list of field patterns in a field-level encryption content type profile specify the fields that you want to be encrypted.
    language:
      python:
        mapCase: false
  providerId:
    type: string
    description: |
      The provider associated with the public key being used for encryption.
    language:
      python:
        mapCase: false
  publicKeyId:
    type: string
    description: |
      The public key associated with a set of field-level encryption patterns, to be used when encrypting the fields that match the patterns.
    language:
      python:
        mapCase: false
type: object
required:
  - fieldPatterns
  - providerId
  - publicKeyId
