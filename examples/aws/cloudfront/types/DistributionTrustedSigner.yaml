$token: aws:cloudfront/DistributionTrustedSigner:DistributionTrustedSigner
properties:
  enabled:
    type: boolean
    description: |
      A flag that specifies whether Origin Shield is enabled.
    language:
      python:
        mapCase: false
  items:
    type: array
    items:
      $ref: "#/types/aws:cloudfront/DistributionTrustedSignerItem:DistributionTrustedSignerItem"
    description: |
      List of nested attributes for each trusted signer
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - enabled
      - items
