$token: aws:cloudfront/getResponseHeadersPolicySecurityHeadersConfigFrameOption:getResponseHeadersPolicySecurityHeadersConfigFrameOption
properties:
  frameOption:
    type: string
    description: |
      Value of the X-Frame-Options HTTP response header. Valid values: `DENY` | `SAMEORIGIN`
    language:
      python:
        mapCase: false
  override:
    type: boolean
    description: |
      Whether CloudFront overrides the X-XSS-Protection HTTP response header received from the origin with the one specified in this response headers policy.
    language:
      python:
        mapCase: false
type: object
required:
  - frameOption
  - override
language:
  nodejs:
    requiredInputs: []
