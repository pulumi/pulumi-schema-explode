$token: aws:cloudfront/DistributionCustomErrorResponse:DistributionCustomErrorResponse
properties:
  errorCachingMinTtl:
    type: integer
    description: |
      The minimum amount of time you want
      HTTP error codes to stay in CloudFront caches before CloudFront queries your
      origin to see whether the object has been updated.
    language:
      python:
        mapCase: false
  errorCode:
    type: integer
    description: |
      The 4xx or 5xx HTTP status code that you want to
      customize.
    language:
      python:
        mapCase: false
  responseCode:
    type: integer
    description: |
      The HTTP status code that you want CloudFront
      to return with the custom error page to the viewer.
    language:
      python:
        mapCase: false
  responsePagePath:
    type: string
    description: |
      The path of the custom error page (for
      example, `/custom_404.html`).
    language:
      python:
        mapCase: false
type: object
required:
  - errorCode
