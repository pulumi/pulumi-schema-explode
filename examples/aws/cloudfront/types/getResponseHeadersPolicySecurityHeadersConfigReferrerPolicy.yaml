$token: aws:cloudfront/getResponseHeadersPolicySecurityHeadersConfigReferrerPolicy:getResponseHeadersPolicySecurityHeadersConfigReferrerPolicy
properties:
  override:
    type: boolean
    description: |
      Whether CloudFront overrides the X-XSS-Protection HTTP response header received from the origin with the one specified in this response headers policy.
    language:
      python:
        mapCase: false
  referrerPolicy:
    type: string
    description: |
      Value of the Referrer-Policy HTTP response header. Valid Values: `no-referrer` | `no-referrer-when-downgrade` | `origin` | `origin-when-cross-origin` | `same-origin` | `strict-origin` | `strict-origin-when-cross-origin` | `unsafe-url`
    language:
      python:
        mapCase: false
type: object
required:
  - override
  - referrerPolicy
language:
  nodejs:
    requiredInputs: []
