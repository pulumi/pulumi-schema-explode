$token: aws:cloudfront/ResponseHeadersPolicyCustomHeadersConfigItem:ResponseHeadersPolicyCustomHeadersConfigItem
properties:
  header:
    type: string
    description: |
      The HTTP response header name.
    language:
      python:
        mapCase: false
  override:
    type: boolean
    description: |
      Whether CloudFront overrides the `X-XSS-Protection` HTTP response header received from the origin with the one specified in this response headers policy.
    language:
      python:
        mapCase: false
  value:
    type: string
    description: |
      The value for the HTTP response header.
    language:
      python:
        mapCase: false
type: object
required:
  - header
  - override
  - value
