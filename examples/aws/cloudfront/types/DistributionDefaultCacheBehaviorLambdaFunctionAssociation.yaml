$token: aws:cloudfront/DistributionDefaultCacheBehaviorLambdaFunctionAssociation:DistributionDefaultCacheBehaviorLambdaFunctionAssociation
properties:
  eventType:
    type: string
    description: |
      The specific event to trigger this function.
      Valid values: `viewer-request` or `viewer-response`
    language:
      python:
        mapCase: false
  includeBody:
    type: boolean
    description: |
      When set to true it exposes the request body to the lambda function. Defaults to false. Valid values: `true`, `false`.
    language:
      python:
        mapCase: false
  lambdaArn:
    type: string
    description: |
      ARN of the Lambda function.
    language:
      python:
        mapCase: false
type: object
required:
  - eventType
  - lambdaArn
