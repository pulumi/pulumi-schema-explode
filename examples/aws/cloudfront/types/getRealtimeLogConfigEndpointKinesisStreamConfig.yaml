$token: aws:cloudfront/getRealtimeLogConfigEndpointKinesisStreamConfig:getRealtimeLogConfigEndpointKinesisStreamConfig
properties:
  roleArn:
    type: string
    description: |
      (Required) ARN of an IAM role that CloudFront can use to send real-time log data to the Kinesis data stream.
      See the [AWS documentation](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/real-time-logs.html#understand-real-time-log-config-iam-role) for more information.
    language:
      python:
        mapCase: false
  streamArn:
    type: string
    description: |
      (Required) ARN of the Kinesis data stream.
    language:
      python:
        mapCase: false
type: object
required:
  - roleArn
  - streamArn
language:
  nodejs:
    requiredInputs: []
