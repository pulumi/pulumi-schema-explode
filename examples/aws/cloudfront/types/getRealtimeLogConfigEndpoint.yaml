$token: aws:cloudfront/getRealtimeLogConfigEndpoint:getRealtimeLogConfigEndpoint
properties:
  kinesisStreamConfigs:
    type: array
    items:
      $ref: "#/types/aws:cloudfront/getRealtimeLogConfigEndpointKinesisStreamConfig:getRealtimeLogConfigEndpointKinesisStreamConfig"
    description: |
      (Required) Amazon Kinesis data stream configuration.
    language:
      python:
        mapCase: false
  streamType:
    type: string
    description: |
      (Required) Type of data stream where real-time log data is sent. The only valid value is `Kinesis`.
    language:
      python:
        mapCase: false
type: object
required:
  - kinesisStreamConfigs
  - streamType
language:
  nodejs:
    requiredInputs: []
