$token: aws:cloudfront/DistributionOriginS3OriginConfig:DistributionOriginS3OriginConfig
properties:
  originAccessIdentity:
    type: string
    description: |
      The [CloudFront origin access
      identity][5] to associate with the origin.
    language:
      python:
        mapCase: false
type: object
required:
  - originAccessIdentity
