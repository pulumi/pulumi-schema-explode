$token: aws:cloudfront/DistributionTrustedKeyGroupItem:DistributionTrustedKeyGroupItem
properties:
  keyGroupId:
    type: string
    description: |
      The ID of the key group that contains the public keys
    language:
      python:
        mapCase: false
  keyPairIds:
    type: array
    items:
      type: string
    description: |
      Set of active CloudFront key pairs associated with the signer account
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - keyGroupId
      - keyPairIds
