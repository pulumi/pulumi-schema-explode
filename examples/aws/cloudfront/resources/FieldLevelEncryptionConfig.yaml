$token: aws:cloudfront/fieldLevelEncryptionConfig:FieldLevelEncryptionConfig
properties:
  callerReference:
    type: string
    description: |
      Internal value used by CloudFront to allow future updates to the Field Level Encryption Config.
  comment:
    type: string
    description: |
      An optional comment about the Field Level Encryption Config.
  contentTypeProfileConfig:
    $ref: "#/types/aws:cloudfront/FieldLevelEncryptionConfigContentTypeProfileConfig:FieldLevelEncryptionConfigContentTypeProfileConfig"
    description: |
      Content Type Profile Config specifies when to forward content if a content type isn't recognized and profiles to use as by default in a request if a query argument doesn't specify a profile to use.
  etag:
    type: string
    description: |
      The current version of the Field Level Encryption Config. For example: `E2QWRUHAPOMQZL`.
  queryArgProfileConfig:
    $ref: "#/types/aws:cloudfront/FieldLevelEncryptionConfigQueryArgProfileConfig:FieldLevelEncryptionConfigQueryArgProfileConfig"
    description: |
      Query Arg Profile Config that specifies when to forward content if a profile isn't found and the profile that can be provided as a query argument in a request.
required:
  - callerReference
  - contentTypeProfileConfig
  - etag
  - queryArgProfileConfig
inputProperties:
  comment:
    type: string
    description: |
      An optional comment about the Field Level Encryption Config.
  contentTypeProfileConfig:
    $ref: "#/types/aws:cloudfront/FieldLevelEncryptionConfigContentTypeProfileConfig:FieldLevelEncryptionConfigContentTypeProfileConfig"
    description: |
      Content Type Profile Config specifies when to forward content if a content type isn't recognized and profiles to use as by default in a request if a query argument doesn't specify a profile to use.
  queryArgProfileConfig:
    $ref: "#/types/aws:cloudfront/FieldLevelEncryptionConfigQueryArgProfileConfig:FieldLevelEncryptionConfigQueryArgProfileConfig"
    description: |
      Query Arg Profile Config that specifies when to forward content if a profile isn't found and the profile that can be provided as a query argument in a request.
requiredInputs:
  - contentTypeProfileConfig
  - queryArgProfileConfig
stateInputs:
  description: |
    Input properties used for looking up and filtering FieldLevelEncryptionConfig resources.
  properties:
    callerReference:
      type: string
      description: |
        Internal value used by CloudFront to allow future updates to the Field Level Encryption Config.
    comment:
      type: string
      description: |
        An optional comment about the Field Level Encryption Config.
    contentTypeProfileConfig:
      $ref: "#/types/aws:cloudfront/FieldLevelEncryptionConfigContentTypeProfileConfig:FieldLevelEncryptionConfigContentTypeProfileConfig"
      description: |
        Content Type Profile Config specifies when to forward content if a content type isn't recognized and profiles to use as by default in a request if a query argument doesn't specify a profile to use.
    etag:
      type: string
      description: |
        The current version of the Field Level Encryption Config. For example: `E2QWRUHAPOMQZL`.
    queryArgProfileConfig:
      $ref: "#/types/aws:cloudfront/FieldLevelEncryptionConfigQueryArgProfileConfig:FieldLevelEncryptionConfigQueryArgProfileConfig"
      description: |
        Query Arg Profile Config that specifies when to forward content if a profile isn't found and the profile that can be provided as a query argument in a request.
  type: object
