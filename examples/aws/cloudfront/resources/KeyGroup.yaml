$token: aws:cloudfront/keyGroup:KeyGroup
properties:
  comment:
    type: string
    description: |
      A comment to describe the key group..
  etag:
    type: string
    description: |
      The identifier for this version of the key group.
  items:
    type: array
    items:
      type: string
    description: |
      A list of the identifiers of the public keys in the key group.
  name:
    type: string
    description: |
      A name to identify the key group.
required:
  - etag
  - items
  - name
inputProperties:
  comment:
    type: string
    description: |
      A comment to describe the key group..
  items:
    type: array
    items:
      type: string
    description: |
      A list of the identifiers of the public keys in the key group.
  name:
    type: string
    description: |
      A name to identify the key group.
requiredInputs:
  - items
stateInputs:
  description: |
    Input properties used for looking up and filtering KeyGroup resources.
  properties:
    comment:
      type: string
      description: |
        A comment to describe the key group..
    etag:
      type: string
      description: |
        The identifier for this version of the key group.
    items:
      type: array
      items:
        type: string
      description: |
        A list of the identifiers of the public keys in the key group.
    name:
      type: string
      description: |
        A name to identify the key group.
  type: object
