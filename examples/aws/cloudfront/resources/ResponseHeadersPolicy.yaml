$token: aws:cloudfront/responseHeadersPolicy:ResponseHeadersPolicy
properties:
  comment:
    type: string
    description: |
      A comment to describe the response headers policy. The comment cannot be longer than 128 characters.
  corsConfig:
    $ref: "#/types/aws:cloudfront/ResponseHeadersPolicyCorsConfig:ResponseHeadersPolicyCorsConfig"
    description: |
      A configuration for a set of HTTP response headers that are used for Cross-Origin Resource Sharing (CORS). See Cors Config for more information.
  customHeadersConfig:
    $ref: "#/types/aws:cloudfront/ResponseHeadersPolicyCustomHeadersConfig:ResponseHeadersPolicyCustomHeadersConfig"
    description: |
      Object that contains an attribute `items` that contains a list of custom headers. See Custom Header for more information.
  etag:
    type: string
    description: |
      The current version of the response headers policy.
  name:
    type: string
    description: |
      A unique name to identify the response headers policy.
  securityHeadersConfig:
    $ref: "#/types/aws:cloudfront/ResponseHeadersPolicySecurityHeadersConfig:ResponseHeadersPolicySecurityHeadersConfig"
    description: |
      A configuration for a set of security-related HTTP response headers. See Security Headers Config for more information.
  serverTimingHeadersConfig:
    $ref: "#/types/aws:cloudfront/ResponseHeadersPolicyServerTimingHeadersConfig:ResponseHeadersPolicyServerTimingHeadersConfig"
    description: |
      A configuration for enabling the Server-Timing header in HTTP responses sent from CloudFront. See Server Timing Headers Config for more information.
required:
  - etag
  - name
inputProperties:
  comment:
    type: string
    description: |
      A comment to describe the response headers policy. The comment cannot be longer than 128 characters.
  corsConfig:
    $ref: "#/types/aws:cloudfront/ResponseHeadersPolicyCorsConfig:ResponseHeadersPolicyCorsConfig"
    description: |
      A configuration for a set of HTTP response headers that are used for Cross-Origin Resource Sharing (CORS). See Cors Config for more information.
  customHeadersConfig:
    $ref: "#/types/aws:cloudfront/ResponseHeadersPolicyCustomHeadersConfig:ResponseHeadersPolicyCustomHeadersConfig"
    description: |
      Object that contains an attribute `items` that contains a list of custom headers. See Custom Header for more information.
  etag:
    type: string
    description: |
      The current version of the response headers policy.
  name:
    type: string
    description: |
      A unique name to identify the response headers policy.
  securityHeadersConfig:
    $ref: "#/types/aws:cloudfront/ResponseHeadersPolicySecurityHeadersConfig:ResponseHeadersPolicySecurityHeadersConfig"
    description: |
      A configuration for a set of security-related HTTP response headers. See Security Headers Config for more information.
  serverTimingHeadersConfig:
    $ref: "#/types/aws:cloudfront/ResponseHeadersPolicyServerTimingHeadersConfig:ResponseHeadersPolicyServerTimingHeadersConfig"
    description: |
      A configuration for enabling the Server-Timing header in HTTP responses sent from CloudFront. See Server Timing Headers Config for more information.
stateInputs:
  description: |
    Input properties used for looking up and filtering ResponseHeadersPolicy resources.
  properties:
    comment:
      type: string
      description: |
        A comment to describe the response headers policy. The comment cannot be longer than 128 characters.
    corsConfig:
      $ref: "#/types/aws:cloudfront/ResponseHeadersPolicyCorsConfig:ResponseHeadersPolicyCorsConfig"
      description: |
        A configuration for a set of HTTP response headers that are used for Cross-Origin Resource Sharing (CORS). See Cors Config for more information.
    customHeadersConfig:
      $ref: "#/types/aws:cloudfront/ResponseHeadersPolicyCustomHeadersConfig:ResponseHeadersPolicyCustomHeadersConfig"
      description: |
        Object that contains an attribute `items` that contains a list of custom headers. See Custom Header for more information.
    etag:
      type: string
      description: |
        The current version of the response headers policy.
    name:
      type: string
      description: |
        A unique name to identify the response headers policy.
    securityHeadersConfig:
      $ref: "#/types/aws:cloudfront/ResponseHeadersPolicySecurityHeadersConfig:ResponseHeadersPolicySecurityHeadersConfig"
      description: |
        A configuration for a set of security-related HTTP response headers. See Security Headers Config for more information.
    serverTimingHeadersConfig:
      $ref: "#/types/aws:cloudfront/ResponseHeadersPolicyServerTimingHeadersConfig:ResponseHeadersPolicyServerTimingHeadersConfig"
      description: |
        A configuration for enabling the Server-Timing header in HTTP responses sent from CloudFront. See Server Timing Headers Config for more information.
  type: object
