$token: aws:cloudfront/realtimeLogConfig:RealtimeLogConfig
properties:
  arn:
    type: string
    description: |
      The ARN (Amazon Resource Name) of the CloudFront real-time log configuration.
  endpoint:
    $ref: "#/types/aws:cloudfront/RealtimeLogConfigEndpoint:RealtimeLogConfigEndpoint"
    description: |
      The Amazon Kinesis data streams where real-time log data is sent.
  fields:
    type: array
    items:
      type: string
    description: |
      The fields that are included in each real-time log record. See the [AWS documentation](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/real-time-logs.html#understand-real-time-log-config-fields) for supported values.
  name:
    type: string
    description: |
      The unique name to identify this real-time log configuration.
  samplingRate:
    type: integer
    description: |
      The sampling rate for this real-time log configuration. The sampling rate determines the percentage of viewer requests that are represented in the real-time log data. An integer between `1` and `100`, inclusive.
required:
  - arn
  - endpoint
  - fields
  - name
  - samplingRate
inputProperties:
  endpoint:
    $ref: "#/types/aws:cloudfront/RealtimeLogConfigEndpoint:RealtimeLogConfigEndpoint"
    description: |
      The Amazon Kinesis data streams where real-time log data is sent.
  fields:
    type: array
    items:
      type: string
    description: |
      The fields that are included in each real-time log record. See the [AWS documentation](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/real-time-logs.html#understand-real-time-log-config-fields) for supported values.
  name:
    type: string
    description: |
      The unique name to identify this real-time log configuration.
    willReplaceOnChanges: true
  samplingRate:
    type: integer
    description: |
      The sampling rate for this real-time log configuration. The sampling rate determines the percentage of viewer requests that are represented in the real-time log data. An integer between `1` and `100`, inclusive.
requiredInputs:
  - endpoint
  - fields
  - samplingRate
stateInputs:
  description: |
    Input properties used for looking up and filtering RealtimeLogConfig resources.
  properties:
    arn:
      type: string
      description: |
        The ARN (Amazon Resource Name) of the CloudFront real-time log configuration.
    endpoint:
      $ref: "#/types/aws:cloudfront/RealtimeLogConfigEndpoint:RealtimeLogConfigEndpoint"
      description: |
        The Amazon Kinesis data streams where real-time log data is sent.
    fields:
      type: array
      items:
        type: string
      description: |
        The fields that are included in each real-time log record. See the [AWS documentation](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/real-time-logs.html#understand-real-time-log-config-fields) for supported values.
    name:
      type: string
      description: |
        The unique name to identify this real-time log configuration.
      willReplaceOnChanges: true
    samplingRate:
      type: integer
      description: |
        The sampling rate for this real-time log configuration. The sampling rate determines the percentage of viewer requests that are represented in the real-time log data. An integer between `1` and `100`, inclusive.
  type: object
