$token: aws:cloudfront/monitoringSubscription:MonitoringSubscription
properties:
  distributionId:
    type: string
    description: |
      The ID of the distribution that you are enabling metrics for.
  monitoringSubscription:
    $ref: "#/types/aws:cloudfront/MonitoringSubscriptionMonitoringSubscription:MonitoringSubscriptionMonitoringSubscription"
    description: |
      A monitoring subscription. This structure contains information about whether additional CloudWatch metrics are enabled for a given CloudFront distribution.
    language:
      csharp:
        name: MonitoringSubscriptionDetails
required:
  - distributionId
  - monitoringSubscription
inputProperties:
  distributionId:
    type: string
    description: |
      The ID of the distribution that you are enabling metrics for.
    willReplaceOnChanges: true
  monitoringSubscription:
    $ref: "#/types/aws:cloudfront/MonitoringSubscriptionMonitoringSubscription:MonitoringSubscriptionMonitoringSubscription"
    description: |
      A monitoring subscription. This structure contains information about whether additional CloudWatch metrics are enabled for a given CloudFront distribution.
    language:
      csharp:
        name: MonitoringSubscriptionDetails
requiredInputs:
  - distributionId
  - monitoringSubscription
stateInputs:
  description: |
    Input properties used for looking up and filtering MonitoringSubscription resources.
  properties:
    distributionId:
      type: string
      description: |
        The ID of the distribution that you are enabling metrics for.
      willReplaceOnChanges: true
    monitoringSubscription:
      $ref: "#/types/aws:cloudfront/MonitoringSubscriptionMonitoringSubscription:MonitoringSubscriptionMonitoringSubscription"
      description: |
        A monitoring subscription. This structure contains information about whether additional CloudWatch metrics are enabled for a given CloudFront distribution.
      language:
        csharp:
          name: MonitoringSubscriptionDetails
  type: object
