$token: aws:cloudfront/fieldLevelEncryptionProfile:FieldLevelEncryptionProfile
properties:
  callerReference:
    type: string
    description: |
      Internal value used by CloudFront to allow future updates to the Field Level Encryption Profile.
  comment:
    type: string
    description: |
      An optional comment about the Field Level Encryption Profile.
  encryptionEntities:
    $ref: "#/types/aws:cloudfront/FieldLevelEncryptionProfileEncryptionEntities:FieldLevelEncryptionProfileEncryptionEntities"
    description: |
      The encryption entities config block for field-level encryption profiles that contains an attribute `items` which includes the encryption key and field pattern specifications.
  etag:
    type: string
    description: |
      The current version of the Field Level Encryption Profile. For example: `E2QWRUHAPOMQZL`.
  name:
    type: string
    description: |
      The name of the Field Level Encryption Profile.
required:
  - callerReference
  - encryptionEntities
  - etag
  - name
inputProperties:
  comment:
    type: string
    description: |
      An optional comment about the Field Level Encryption Profile.
  encryptionEntities:
    $ref: "#/types/aws:cloudfront/FieldLevelEncryptionProfileEncryptionEntities:FieldLevelEncryptionProfileEncryptionEntities"
    description: |
      The encryption entities config block for field-level encryption profiles that contains an attribute `items` which includes the encryption key and field pattern specifications.
  name:
    type: string
    description: |
      The name of the Field Level Encryption Profile.
requiredInputs:
  - encryptionEntities
stateInputs:
  description: |
    Input properties used for looking up and filtering FieldLevelEncryptionProfile resources.
  properties:
    callerReference:
      type: string
      description: |
        Internal value used by CloudFront to allow future updates to the Field Level Encryption Profile.
    comment:
      type: string
      description: |
        An optional comment about the Field Level Encryption Profile.
    encryptionEntities:
      $ref: "#/types/aws:cloudfront/FieldLevelEncryptionProfileEncryptionEntities:FieldLevelEncryptionProfileEncryptionEntities"
      description: |
        The encryption entities config block for field-level encryption profiles that contains an attribute `items` which includes the encryption key and field pattern specifications.
    etag:
      type: string
      description: |
        The current version of the Field Level Encryption Profile. For example: `E2QWRUHAPOMQZL`.
    name:
      type: string
      description: |
        The name of the Field Level Encryption Profile.
  type: object
