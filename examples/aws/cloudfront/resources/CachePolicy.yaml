$token: aws:cloudfront/cachePolicy:CachePolicy
properties:
  comment:
    type: string
    description: |
      A comment to describe the cache policy.
  defaultTtl:
    type: integer
    description: |
      The default amount of time, in seconds, that you want objects to stay in the CloudFront cache before CloudFront sends another request to the origin to see if the object has been updated.
  etag:
    type: string
    description: |
      The current version of the cache policy.
  maxTtl:
    type: integer
    description: |
      The maximum amount of time, in seconds, that objects stay in the CloudFront cache before CloudFront sends another request to the origin to see if the object has been updated.
  minTtl:
    type: integer
    description: |
      The minimum amount of time, in seconds, that you want objects to stay in the CloudFront cache before CloudFront sends another request to the origin to see if the object has been updated.
  name:
    type: string
    description: |
      A unique name to identify the cache policy.
  parametersInCacheKeyAndForwardedToOrigin:
    $ref: "#/types/aws:cloudfront/CachePolicyParametersInCacheKeyAndForwardedToOrigin:CachePolicyParametersInCacheKeyAndForwardedToOrigin"
    description: |
      The HTTP headers, cookies, and URL query strings to include in the cache key. See Parameters In Cache Key And Forwarded To Origin for more information.
required:
  - etag
  - name
  - parametersInCacheKeyAndForwardedToOrigin
inputProperties:
  comment:
    type: string
    description: |
      A comment to describe the cache policy.
  defaultTtl:
    type: integer
    description: |
      The default amount of time, in seconds, that you want objects to stay in the CloudFront cache before CloudFront sends another request to the origin to see if the object has been updated.
  maxTtl:
    type: integer
    description: |
      The maximum amount of time, in seconds, that objects stay in the CloudFront cache before CloudFront sends another request to the origin to see if the object has been updated.
  minTtl:
    type: integer
    description: |
      The minimum amount of time, in seconds, that you want objects to stay in the CloudFront cache before CloudFront sends another request to the origin to see if the object has been updated.
  name:
    type: string
    description: |
      A unique name to identify the cache policy.
  parametersInCacheKeyAndForwardedToOrigin:
    $ref: "#/types/aws:cloudfront/CachePolicyParametersInCacheKeyAndForwardedToOrigin:CachePolicyParametersInCacheKeyAndForwardedToOrigin"
    description: |
      The HTTP headers, cookies, and URL query strings to include in the cache key. See Parameters In Cache Key And Forwarded To Origin for more information.
requiredInputs:
  - parametersInCacheKeyAndForwardedToOrigin
stateInputs:
  description: |
    Input properties used for looking up and filtering CachePolicy resources.
  properties:
    comment:
      type: string
      description: |
        A comment to describe the cache policy.
    defaultTtl:
      type: integer
      description: |
        The default amount of time, in seconds, that you want objects to stay in the CloudFront cache before CloudFront sends another request to the origin to see if the object has been updated.
    etag:
      type: string
      description: |
        The current version of the cache policy.
    maxTtl:
      type: integer
      description: |
        The maximum amount of time, in seconds, that objects stay in the CloudFront cache before CloudFront sends another request to the origin to see if the object has been updated.
    minTtl:
      type: integer
      description: |
        The minimum amount of time, in seconds, that you want objects to stay in the CloudFront cache before CloudFront sends another request to the origin to see if the object has been updated.
    name:
      type: string
      description: |
        A unique name to identify the cache policy.
    parametersInCacheKeyAndForwardedToOrigin:
      $ref: "#/types/aws:cloudfront/CachePolicyParametersInCacheKeyAndForwardedToOrigin:CachePolicyParametersInCacheKeyAndForwardedToOrigin"
      description: |
        The HTTP headers, cookies, and URL query strings to include in the cache key. See Parameters In Cache Key And Forwarded To Origin for more information.
  type: object
