$token: aws:qldb/getLedger:getLedger
inputs:
  description: |
    A collection of arguments for invoking getLedger.
  properties:
    name:
      type: string
      description: |
        Friendly name of the ledger to match.
    tags:
      type: object
      additionalProperties:
        type: string
  type: object
  required:
    - name
outputs:
  description: |
    A collection of values returned by getLedger.
  properties:
    arn:
      type: string
    deletionProtection:
      type: boolean
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    kmsKey:
      type: string
    name:
      type: string
    permissionsMode:
      type: string
    tags:
      type: object
      additionalProperties:
        type: string
  type: object
  required:
    - arn
    - deletionProtection
    - kmsKey
    - name
    - permissionsMode
    - tags
    - id
