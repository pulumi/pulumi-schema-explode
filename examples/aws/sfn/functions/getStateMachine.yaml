$token: aws:sfn/getStateMachine:getStateMachine
inputs:
  description: |
    A collection of arguments for invoking getStateMachine.
  properties:
    name:
      type: string
      description: |
        Friendly name of the state machine to match.
  type: object
  required:
    - name
outputs:
  description: |
    A collection of values returned by getStateMachine.
  properties:
    arn:
      type: string
      description: |
        Set to the arn of the state function.
    creationDate:
      type: string
      description: |
        Date the state machine was created.
    definition:
      type: string
      description: |
        Set to the state machine definition.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    name:
      type: string
    roleArn:
      type: string
      description: |
        Set to the role_arn used by the state function.
    status:
      type: string
      description: |
        Set to the current status of the state machine.
  type: object
  required:
    - arn
    - creationDate
    - definition
    - name
    - roleArn
    - status
    - id
