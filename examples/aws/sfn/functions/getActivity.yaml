$token: aws:sfn/getActivity:getActivity
inputs:
  description: |
    A collection of arguments for invoking getActivity.
  properties:
    arn:
      type: string
      description: |
        ARN that identifies the activity.
    name:
      type: string
      description: |
        Name that identifies the activity.
  type: object
outputs:
  description: |
    A collection of values returned by getActivity.
  properties:
    arn:
      type: string
    creationDate:
      type: string
      description: |
        Date the activity was created.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    name:
      type: string
  type: object
  required:
    - arn
    - creationDate
    - name
    - id
