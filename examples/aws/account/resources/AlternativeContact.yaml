$token: aws:account/alternativeContact:AlternativeContact
properties:
  accountId:
    type: string
    description: |
      ID of the target account when managing member accounts. Will manage current user's account by default if omitted.
  alternateContactType:
    type: string
    description: |
      Type of the alternate contact. Allowed values are: `BILLING`, `OPERATIONS`, `SECURITY`.
  emailAddress:
    type: string
    description: |
      An email address for the alternate contact.
  name:
    type: string
    description: |
      Name of the alternate contact.
  phoneNumber:
    type: string
    description: |
      Phone number for the alternate contact.
  title:
    type: string
    description: |
      Title for the alternate contact.
required:
  - alternateContactType
  - emailAddress
  - name
  - phoneNumber
  - title
inputProperties:
  accountId:
    type: string
    description: |
      ID of the target account when managing member accounts. Will manage current user's account by default if omitted.
    willReplaceOnChanges: true
  alternateContactType:
    type: string
    description: |
      Type of the alternate contact. Allowed values are: `BILLING`, `OPERATIONS`, `SECURITY`.
    willReplaceOnChanges: true
  emailAddress:
    type: string
    description: |
      An email address for the alternate contact.
  name:
    type: string
    description: |
      Name of the alternate contact.
  phoneNumber:
    type: string
    description: |
      Phone number for the alternate contact.
  title:
    type: string
    description: |
      Title for the alternate contact.
requiredInputs:
  - alternateContactType
  - emailAddress
  - phoneNumber
  - title
stateInputs:
  description: |
    Input properties used for looking up and filtering AlternativeContact resources.
  properties:
    accountId:
      type: string
      description: |
        ID of the target account when managing member accounts. Will manage current user's account by default if omitted.
      willReplaceOnChanges: true
    alternateContactType:
      type: string
      description: |
        Type of the alternate contact. Allowed values are: `BILLING`, `OPERATIONS`, `SECURITY`.
      willReplaceOnChanges: true
    emailAddress:
      type: string
      description: |
        An email address for the alternate contact.
    name:
      type: string
      description: |
        Name of the alternate contact.
    phoneNumber:
      type: string
      description: |
        Phone number for the alternate contact.
    title:
      type: string
      description: |
        Title for the alternate contact.
  type: object
