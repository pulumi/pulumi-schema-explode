$token: aws:lb/getHostedZoneId:getHostedZoneId
inputs:
  description: |
    A collection of arguments for invoking getHostedZoneId.
  properties:
    loadBalancerType:
      type: string
      description: |
        Type of load balancer to create. Possible values are `application` or `network`. The default value is `application`.
    region:
      type: string
      description: |
        Name of the region whose AWS ELB HostedZoneId is desired.
        Defaults to the region from the AWS provider configuration.
  type: object
outputs:
  description: |
    A collection of values returned by getHostedZoneId.
  properties:
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    loadBalancerType:
      type: string
    region:
      type: string
  type: object
  required:
    - id
