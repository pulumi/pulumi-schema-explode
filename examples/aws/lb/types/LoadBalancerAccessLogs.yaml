$token: aws:lb/LoadBalancerAccessLogs:LoadBalancerAccessLogs
properties:
  bucket:
    type: string
    description: |
      The S3 bucket name to store the logs in.
    language:
      python:
        mapCase: false
  enabled:
    type: boolean
    description: |
      Boolean to enable / disable `access_logs`. Defaults to `false`, even when `bucket` is specified.
    language:
      python:
        mapCase: false
  prefix:
    type: string
    description: |
      The S3 bucket prefix. Logs are stored in the root if not configured.
    language:
      python:
        mapCase: false
type: object
required:
  - bucket
