$token: aws:lb/ListenerDefaultActionAuthenticateCognito:ListenerDefaultActionAuthenticateCognito
properties:
  authenticationRequestExtraParams:
    type: object
    additionalProperties:
      type: string
    description: |
      Query parameters to include in the redirect request to the authorization endpoint. Max: 10.
    language:
      python:
        mapCase: false
  onUnauthenticatedRequest:
    type: string
    description: |
      Behavior if the user is not authenticated. Valid values: `deny`, `allow` and `authenticate`
    language:
      python:
        mapCase: false
  scope:
    type: string
    description: |
      Set of user claims to be requested from the IdP.
    language:
      python:
        mapCase: false
  sessionCookieName:
    type: string
    description: |
      Name of the cookie used to maintain session information.
    language:
      python:
        mapCase: false
  sessionTimeout:
    type: integer
    description: |
      Maximum duration of the authentication session, in seconds.
    language:
      python:
        mapCase: false
  userPoolArn:
    type: string
    description: |
      ARN of the Cognito user pool.
    language:
      python:
        mapCase: false
  userPoolClientId:
    type: string
    description: |
      ID of the Cognito user pool client.
    language:
      python:
        mapCase: false
  userPoolDomain:
    type: string
    description: |
      Domain prefix or fully-qualified domain name of the Cognito user pool.
    language:
      python:
        mapCase: false
type: object
required:
  - userPoolArn
  - userPoolClientId
  - userPoolDomain
language:
  nodejs:
    requiredOutputs:
      - onUnauthenticatedRequest
      - scope
      - sessionCookieName
      - sessionTimeout
      - userPoolArn
      - userPoolClientId
      - userPoolDomain
