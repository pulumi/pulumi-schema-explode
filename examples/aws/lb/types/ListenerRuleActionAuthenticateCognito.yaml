$token: aws:lb/ListenerRuleActionAuthenticateCognito:ListenerRuleActionAuthenticateCognito
properties:
  authenticationRequestExtraParams:
    type: object
    additionalProperties:
      type: string
    description: |
      The query parameters to include in the redirect request to the authorization endpoint. Max: 10.
    language:
      python:
        mapCase: false
  onUnauthenticatedRequest:
    type: string
    description: |
      The behavior if the user is not authenticated. Valid values: `deny`, `allow` and `authenticate`
    language:
      python:
        mapCase: false
  scope:
    type: string
    description: |
      The set of user claims to be requested from the IdP.
    language:
      python:
        mapCase: false
  sessionCookieName:
    type: string
    description: |
      The name of the cookie used to maintain session information.
    language:
      python:
        mapCase: false
  sessionTimeout:
    type: integer
    description: |
      The maximum duration of the authentication session, in seconds.
    language:
      python:
        mapCase: false
  userPoolArn:
    type: string
    description: |
      The ARN of the Cognito user pool.
    language:
      python:
        mapCase: false
  userPoolClientId:
    type: string
    description: |
      The ID of the Cognito user pool client.
    language:
      python:
        mapCase: false
  userPoolDomain:
    type: string
    description: |
      The domain prefix or fully-qualified domain name of the Cognito user pool.
    language:
      python:
        mapCase: false
type: object
required:
  - userPoolArn
  - userPoolClientId
  - userPoolDomain
language:
  nodejs:
    requiredOutputs:
      - onUnauthenticatedRequest
      - userPoolArn
      - userPoolClientId
      - userPoolDomain
