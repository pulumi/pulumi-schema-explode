$token: aws:lb/ListenerDefaultActionForwardStickiness:ListenerDefaultActionForwardStickiness
properties:
  duration:
    type: integer
    description: |
      Time period, in seconds, during which requests from a client should be routed to the same target group. The range is 1-604800 seconds (7 days).
    language:
      python:
        mapCase: false
  enabled:
    type: boolean
    description: |
      Whether target group stickiness is enabled. Default is `false`.
    language:
      python:
        mapCase: false
type: object
required:
  - duration
