$token: aws:lb/ListenerRuleAction:ListenerRuleAction
properties:
  authenticateCognito:
    $ref: "#/types/aws:lb/ListenerRuleActionAuthenticateCognito:ListenerRuleActionAuthenticateCognito"
    description: |
      Information for creating an authenticate action using Cognito. Required if `type` is `authenticate-cognito`.
    language:
      python:
        mapCase: false
  authenticateOidc:
    $ref: "#/types/aws:lb/ListenerRuleActionAuthenticateOidc:ListenerRuleActionAuthenticateOidc"
    description: |
      Information for creating an authenticate action using OIDC. Required if `type` is `authenticate-oidc`.
    language:
      python:
        mapCase: false
  fixedResponse:
    $ref: "#/types/aws:lb/ListenerRuleActionFixedResponse:ListenerRuleActionFixedResponse"
    description: |
      Information for creating an action that returns a custom HTTP response. Required if `type` is `fixed-response`.
    language:
      python:
        mapCase: false
  forward:
    $ref: "#/types/aws:lb/ListenerRuleActionForward:ListenerRuleActionForward"
    description: |
      Information for creating an action that distributes requests among one or more target groups. Specify only if `type` is `forward`. If you specify both `forward` block and `target_group_arn` attribute, you can specify only one target group using `forward` and it must be the same target group specified in `target_group_arn`.
    language:
      python:
        mapCase: false
  order:
    type: integer
    language:
      python:
        mapCase: false
  redirect:
    $ref: "#/types/aws:lb/ListenerRuleActionRedirect:ListenerRuleActionRedirect"
    description: |
      Information for creating a redirect action. Required if `type` is `redirect`.
    language:
      python:
        mapCase: false
  targetGroupArn:
    type: string
    description: |
      The ARN of the Target Group to which to route traffic. Specify only if `type` is `forward` and you want to route to a single target group. To route to one or more target groups, use a `forward` block instead.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      The type of routing action. Valid values are `forward`, `redirect`, `fixed-response`, `authenticate-cognito` and `authenticate-oidc`.
    language:
      python:
        mapCase: false
type: object
required:
  - type
language:
  nodejs:
    requiredOutputs:
      - order
      - type
