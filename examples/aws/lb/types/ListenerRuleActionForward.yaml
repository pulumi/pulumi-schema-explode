$token: aws:lb/ListenerRuleActionForward:ListenerRuleActionForward
properties:
  stickiness:
    $ref: "#/types/aws:lb/ListenerRuleActionForwardStickiness:ListenerRuleActionForwardStickiness"
    description: |
      The target group stickiness for the rule.
    language:
      python:
        mapCase: false
  targetGroups:
    type: array
    items:
      $ref: "#/types/aws:lb/ListenerRuleActionForwardTargetGroup:ListenerRuleActionForwardTargetGroup"
    description: |
      One or more target groups block.
    language:
      python:
        mapCase: false
type: object
required:
  - targetGroups
