$token: aws:apprunner/customDomainAssociation:CustomDomainAssociation
properties:
  certificateValidationRecords:
    type: array
    items:
      $ref: "#/types/aws:apprunner/CustomDomainAssociationCertificateValidationRecord:CustomDomainAssociationCertificateValidationRecord"
    description: |
      A set of certificate CNAME records used for this domain name. See Certificate Validation Records below for more details.
  dnsTarget:
    type: string
    description: |
      App Runner subdomain of the App Runner service. The custom domain name is mapped to this target name. Attribute only available if resource created (not imported) with this provider.
  domainName:
    type: string
    description: |
      Custom domain endpoint to association. Specify a base domain e.g., `example.com` or a subdomain e.g., `subdomain.example.com`.
  enableWwwSubdomain:
    type: boolean
    description: |
      Whether to associate the subdomain with the App Runner service in addition to the base domain. Defaults to `true`.
  serviceArn:
    type: string
    description: |
      ARN of the App Runner service.
  status:
    type: string
    description: |
      Current state of the certificate CNAME record validation. It should change to `SUCCESS` after App Runner completes validation with your DNS.
required:
  - certificateValidationRecords
  - dnsTarget
  - domainName
  - serviceArn
  - status
inputProperties:
  domainName:
    type: string
    description: |
      Custom domain endpoint to association. Specify a base domain e.g., `example.com` or a subdomain e.g., `subdomain.example.com`.
    willReplaceOnChanges: true
  enableWwwSubdomain:
    type: boolean
    description: |
      Whether to associate the subdomain with the App Runner service in addition to the base domain. Defaults to `true`.
    willReplaceOnChanges: true
  serviceArn:
    type: string
    description: |
      ARN of the App Runner service.
    willReplaceOnChanges: true
requiredInputs:
  - domainName
  - serviceArn
stateInputs:
  description: |
    Input properties used for looking up and filtering CustomDomainAssociation resources.
  properties:
    certificateValidationRecords:
      type: array
      items:
        $ref: "#/types/aws:apprunner/CustomDomainAssociationCertificateValidationRecord:CustomDomainAssociationCertificateValidationRecord"
      description: |
        A set of certificate CNAME records used for this domain name. See Certificate Validation Records below for more details.
    dnsTarget:
      type: string
      description: |
        App Runner subdomain of the App Runner service. The custom domain name is mapped to this target name. Attribute only available if resource created (not imported) with this provider.
    domainName:
      type: string
      description: |
        Custom domain endpoint to association. Specify a base domain e.g., `example.com` or a subdomain e.g., `subdomain.example.com`.
      willReplaceOnChanges: true
    enableWwwSubdomain:
      type: boolean
      description: |
        Whether to associate the subdomain with the App Runner service in addition to the base domain. Defaults to `true`.
      willReplaceOnChanges: true
    serviceArn:
      type: string
      description: |
        ARN of the App Runner service.
      willReplaceOnChanges: true
    status:
      type: string
      description: |
        Current state of the certificate CNAME record validation. It should change to `SUCCESS` after App Runner completes validation with your DNS.
  type: object
