$token: aws:apprunner/ServiceSourceConfigurationCodeRepositoryCodeConfigurationCodeConfigurationValues:ServiceSourceConfigurationCodeRepositoryCodeConfigurationCodeConfigurationValues
properties:
  buildCommand:
    type: string
    description: |
      Command App Runner runs to build your application.
    language:
      python:
        mapCase: false
  port:
    type: string
    description: |
      Port that your application listens to in the container. Defaults to `"8080"`.
    language:
      python:
        mapCase: false
  runtime:
    type: string
    description: |
      Runtime environment type for building and running an App Runner service. Represents a programming language runtime. Valid values: `PYTHON_3`, `NODEJS_12`.
    language:
      python:
        mapCase: false
  runtimeEnvironmentVariables:
    type: object
    additionalProperties:
      type: string
    description: |
      Environment variables available to your running App Runner service. A map of key/value pairs. Keys with a prefix of `AWSAPPRUNNER` are reserved for system use and aren't valid.
    language:
      python:
        mapCase: false
  startCommand:
    type: string
    description: |
      Command App Runner runs to start your application.
    language:
      python:
        mapCase: false
type: object
required:
  - runtime
