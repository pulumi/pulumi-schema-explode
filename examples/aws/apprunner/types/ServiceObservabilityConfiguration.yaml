$token: aws:apprunner/ServiceObservabilityConfiguration:ServiceObservabilityConfiguration
properties:
  observabilityConfigurationArn:
    type: string
    description: |
      ARN of the observability configuration that is associated with the service.
    language:
      python:
        mapCase: false
  observabilityEnabled:
    type: boolean
    description: |
      When `true`, an observability configuration resource is associated with the service.
    language:
      python:
        mapCase: false
type: object
required:
  - observabilityConfigurationArn
  - observabilityEnabled
