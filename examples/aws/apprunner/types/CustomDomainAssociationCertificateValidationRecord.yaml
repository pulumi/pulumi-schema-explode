$token: aws:apprunner/CustomDomainAssociationCertificateValidationRecord:CustomDomainAssociationCertificateValidationRecord
properties:
  name:
    type: string
    description: |
      Certificate CNAME record name.
    language:
      python:
        mapCase: false
  status:
    type: string
    description: |
      Current state of the certificate CNAME record validation. It should change to `SUCCESS` after App Runner completes validation with your DNS.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      Record type, always `CNAME`.
    language:
      python:
        mapCase: false
  value:
    type: string
    description: |
      Certificate CNAME record value.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - name
      - status
      - type
      - value
