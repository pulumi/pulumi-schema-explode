$token: aws:apprunner/ServiceSourceConfigurationImageRepository:ServiceSourceConfigurationImageRepository
properties:
  imageConfiguration:
    $ref: "#/types/aws:apprunner/ServiceSourceConfigurationImageRepositoryImageConfiguration:ServiceSourceConfigurationImageRepositoryImageConfiguration"
    description: |
      Configuration for running the identified image. See Image Configuration below for more details.
    language:
      python:
        mapCase: false
  imageIdentifier:
    type: string
    description: |
      Identifier of an image. For an image in Amazon Elastic Container Registry (Amazon ECR), this is an image name. For the
      image name format, see Pulling an image in the Amazon ECR User Guide.
    language:
      python:
        mapCase: false
  imageRepositoryType:
    type: string
    description: |
      Type of the image repository. This reflects the repository provider and whether the repository is private or public. Valid values: `ECR` , `ECR_PUBLIC`.
    language:
      python:
        mapCase: false
type: object
required:
  - imageIdentifier
  - imageRepositoryType
