$token: aws:apprunner/ServiceSourceConfigurationImageRepositoryImageConfiguration:ServiceSourceConfigurationImageRepositoryImageConfiguration
properties:
  port:
    type: string
    description: |
      Port that your application listens to in the container. Defaults to `"8080"`.
    language:
      python:
        mapCase: false
  runtimeEnvironmentVariables:
    type: object
    additionalProperties:
      type: string
    description: |
      Environment variables available to your running App Runner service. A map of key/value pairs. Keys with a prefix of `AWSAPPRUNNER` are reserved for system use and aren't valid.
    language:
      python:
        mapCase: false
  startCommand:
    type: string
    description: |
      Command App Runner runs to start the application in the source image. If specified, this command overrides the Docker image’s default start command.
    language:
      python:
        mapCase: false
type: object
