$token: aws:apprunner/ServiceSourceConfigurationCodeRepositoryCodeConfiguration:ServiceSourceConfigurationCodeRepositoryCodeConfiguration
properties:
  codeConfigurationValues:
    $ref: "#/types/aws:apprunner/ServiceSourceConfigurationCodeRepositoryCodeConfigurationCodeConfigurationValues:ServiceSourceConfigurationCodeRepositoryCodeConfigurationCodeConfigurationValues"
    description: |
      Basic configuration for building and running the App Runner service. Use this parameter to quickly launch an App Runner service without providing an apprunner.yaml file in the source code repository (or ignoring the file if it exists). See Code Configuration Values below for more details.
    language:
      python:
        mapCase: false
  configurationSource:
    type: string
    description: |
      Source of the App Runner configuration. Valid values: `REPOSITORY`, `API`. Values are interpreted as follows:
    language:
      python:
        mapCase: false
type: object
required:
  - configurationSource
