$token: aws:apprunner/ServiceSourceConfigurationCodeRepositorySourceCodeVersion:ServiceSourceConfigurationCodeRepositorySourceCodeVersion
properties:
  type:
    type: string
    description: |
      Type of version identifier. For a git-based repository, branches represent versions. Valid values: `BRANCH`.
    language:
      python:
        mapCase: false
  value:
    type: string
    description: |
      Source code version. For a git-based repository, a branch name maps to a specific version. App Runner uses the most recent commit to the branch.
    language:
      python:
        mapCase: false
type: object
required:
  - type
  - value
