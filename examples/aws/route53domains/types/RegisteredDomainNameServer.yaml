$token: aws:route53domains/RegisteredDomainNameServer:RegisteredDomainNameServer
properties:
  glueIps:
    type: array
    items:
      type: string
    description: |
      Glue IP addresses of a name server. The list can contain only one IPv4 and one IPv6 address.
    language:
      python:
        mapCase: false
  name:
    type: string
    description: |
      The fully qualified host name of the name server.
    language:
      python:
        mapCase: false
type: object
required:
  - name
