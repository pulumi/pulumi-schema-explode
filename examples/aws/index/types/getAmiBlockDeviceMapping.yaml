$token: aws:index/getAmiBlockDeviceMapping:getAmiBlockDeviceMapping
properties:
  deviceName:
    type: string
    description: |
      Physical name of the device.
    language:
      python:
        mapCase: false
  ebs:
    type: object
    additionalProperties:
      type: string
    description: |
      Map containing EBS information, if the device is EBS based. Unlike most object attributes, these are accessed directly (e.g., `ebs.volume_size` or `ebs["volume_size"]`) rather than accessed through the first element of a list (e.g., `ebs[0].volume_size`).
    language:
      python:
        mapCase: false
  noDevice:
    type: string
    description: |
      Suppresses the specified device included in the block device mapping of the AMI.
    language:
      python:
        mapCase: false
  virtualName:
    type: string
    description: |
      Virtual device name (for instance stores).
    language:
      python:
        mapCase: false
type: object
required:
  - deviceName
  - ebs
  - noDevice
  - virtualName
language:
  nodejs:
    requiredInputs: []
