$token: aws:index/getDefaultTags:getDefaultTags
inputs:
  description: |
    A collection of arguments for invoking getDefaultTags.
  properties:
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Blocks of default tags set on the provider. See details below.
  type: object
outputs:
  description: |
    A collection of values returned by getDefaultTags.
  properties:
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Blocks of default tags set on the provider. See details below.
  type: object
  required:
    - tags
    - id
