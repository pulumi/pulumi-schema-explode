$token: aws:index/getBillingServiceAccount:getBillingServiceAccount
outputs:
  description: |
    A collection of values returned by getBillingServiceAccount.
  properties:
    arn:
      type: string
      description: |
        ARN of the AWS billing service account.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
  type: object
  required:
    - arn
    - id
