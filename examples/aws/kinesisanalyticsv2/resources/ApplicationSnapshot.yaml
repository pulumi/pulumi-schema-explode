$token: aws:kinesisanalyticsv2/applicationSnapshot:ApplicationSnapshot
properties:
  applicationName:
    type: string
    description: |
      The name of an existing  Kinesis Analytics v2 Application. Note that the application must be running for a snapshot to be created.
  applicationVersionId:
    type: integer
    description: |
      The current application version ID when the snapshot was created.
  snapshotCreationTimestamp:
    type: string
    description: |
      The timestamp of the application snapshot.
  snapshotName:
    type: string
    description: |
      The name of the application snapshot.
required:
  - applicationName
  - applicationVersionId
  - snapshotCreationTimestamp
  - snapshotName
inputProperties:
  applicationName:
    type: string
    description: |
      The name of an existing  Kinesis Analytics v2 Application. Note that the application must be running for a snapshot to be created.
    willReplaceOnChanges: true
  snapshotName:
    type: string
    description: |
      The name of the application snapshot.
    willReplaceOnChanges: true
requiredInputs:
  - applicationName
  - snapshotName
stateInputs:
  description: |
    Input properties used for looking up and filtering ApplicationSnapshot resources.
  properties:
    applicationName:
      type: string
      description: |
        The name of an existing  Kinesis Analytics v2 Application. Note that the application must be running for a snapshot to be created.
      willReplaceOnChanges: true
    applicationVersionId:
      type: integer
      description: |
        The current application version ID when the snapshot was created.
    snapshotCreationTimestamp:
      type: string
      description: |
        The timestamp of the application snapshot.
    snapshotName:
      type: string
      description: |
        The name of the application snapshot.
      willReplaceOnChanges: true
  type: object
