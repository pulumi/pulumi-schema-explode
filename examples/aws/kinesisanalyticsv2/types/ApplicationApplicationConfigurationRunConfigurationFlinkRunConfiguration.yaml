$token: aws:kinesisanalyticsv2/ApplicationApplicationConfigurationRunConfigurationFlinkRunConfiguration:ApplicationApplicationConfigurationRunConfigurationFlinkRunConfiguration
properties:
  allowNonRestoredState:
    type: boolean
    description: |
      When restoring from a snapshot, specifies whether the runtime is allowed to skip a state that cannot be mapped to the new program. Default is `false`.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - allowNonRestoredState
