$token: aws:kinesisanalyticsv2/ApplicationApplicationConfigurationFlinkApplicationConfigurationCheckpointConfiguration:ApplicationApplicationConfigurationFlinkApplicationConfigurationCheckpointConfiguration
properties:
  checkpointInterval:
    type: integer
    description: |
      Describes the interval in milliseconds between checkpoint operations.
    language:
      python:
        mapCase: false
  checkpointingEnabled:
    type: boolean
    description: |
      Describes whether checkpointing is enabled for a Flink-based Kinesis Data Analytics application.
    language:
      python:
        mapCase: false
  configurationType:
    type: string
    description: |
      Describes whether the application uses Kinesis Data Analytics' default checkpointing behavior. Valid values: `CUSTOM`, `DEFAULT`. Set this attribute to `CUSTOM` in order for any specified `checkpointing_enabled`, `checkpoint_interval`, or `min_pause_between_checkpoints` attribute values to be effective. If this attribute is set to `DEFAULT`, the application will always use the following values:
      * `checkpointing_enabled = true`
      * `checkpoint_interval = 60000`
      * `min_pause_between_checkpoints = 5000`
    language:
      python:
        mapCase: false
  minPauseBetweenCheckpoints:
    type: integer
    description: |
      Describes the minimum time in milliseconds after a checkpoint operation completes that a new checkpoint operation can start.
    language:
      python:
        mapCase: false
type: object
required:
  - configurationType
language:
  nodejs:
    requiredOutputs:
      - checkpointInterval
      - checkpointingEnabled
      - configurationType
      - minPauseBetweenCheckpoints
