$token: aws:kinesisanalyticsv2/ApplicationApplicationConfiguration:ApplicationApplicationConfiguration
properties:
  applicationCodeConfiguration:
    $ref: "#/types/aws:kinesisanalyticsv2/ApplicationApplicationConfigurationApplicationCodeConfiguration:ApplicationApplicationConfigurationApplicationCodeConfiguration"
    description: |
      The code location and type parameters for the application.
    language:
      python:
        mapCase: false
  applicationSnapshotConfiguration:
    $ref: "#/types/aws:kinesisanalyticsv2/ApplicationApplicationConfigurationApplicationSnapshotConfiguration:ApplicationApplicationConfigurationApplicationSnapshotConfiguration"
    description: |
      Describes whether snapshots are enabled for a Flink-based application.
    language:
      python:
        mapCase: false
  environmentProperties:
    $ref: "#/types/aws:kinesisanalyticsv2/ApplicationApplicationConfigurationEnvironmentProperties:ApplicationApplicationConfigurationEnvironmentProperties"
    description: |
      Describes execution properties for a Flink-based application.
    language:
      python:
        mapCase: false
  flinkApplicationConfiguration:
    $ref: "#/types/aws:kinesisanalyticsv2/ApplicationApplicationConfigurationFlinkApplicationConfiguration:ApplicationApplicationConfigurationFlinkApplicationConfiguration"
    description: |
      The configuration of a Flink-based application.
    language:
      python:
        mapCase: false
  runConfiguration:
    $ref: "#/types/aws:kinesisanalyticsv2/ApplicationApplicationConfigurationRunConfiguration:ApplicationApplicationConfigurationRunConfiguration"
    description: |
      Describes the starting properties for a Flink-based application.
    language:
      python:
        mapCase: false
  sqlApplicationConfiguration:
    $ref: "#/types/aws:kinesisanalyticsv2/ApplicationApplicationConfigurationSqlApplicationConfiguration:ApplicationApplicationConfigurationSqlApplicationConfiguration"
    description: |
      The configuration of a SQL-based application.
    language:
      python:
        mapCase: false
  vpcConfiguration:
    $ref: "#/types/aws:kinesisanalyticsv2/ApplicationApplicationConfigurationVpcConfiguration:ApplicationApplicationConfigurationVpcConfiguration"
    description: |
      The VPC configuration of a Flink-based application.
    language:
      python:
        mapCase: false
type: object
required:
  - applicationCodeConfiguration
language:
  nodejs:
    requiredOutputs:
      - applicationCodeConfiguration
      - applicationSnapshotConfiguration
      - flinkApplicationConfiguration
      - runConfiguration
