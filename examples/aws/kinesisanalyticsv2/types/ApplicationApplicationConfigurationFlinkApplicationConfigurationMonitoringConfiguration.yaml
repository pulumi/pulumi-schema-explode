$token: aws:kinesisanalyticsv2/ApplicationApplicationConfigurationFlinkApplicationConfigurationMonitoringConfiguration:ApplicationApplicationConfigurationFlinkApplicationConfigurationMonitoringConfiguration
properties:
  configurationType:
    type: string
    description: |
      Describes whether to use the default CloudWatch logging configuration for an application. Valid values: `CUSTOM`, `DEFAULT`. Set this attribute to `CUSTOM` in order for any specified `log_level` or `metrics_level` attribute values to be effective.
    language:
      python:
        mapCase: false
  logLevel:
    type: string
    description: |
      Describes the verbosity of the CloudWatch Logs for an application. Valid values: `DEBUG`, `ERROR`, `INFO`, `WARN`.
    language:
      python:
        mapCase: false
  metricsLevel:
    type: string
    description: |
      Describes the granularity of the CloudWatch Logs for an application. Valid values: `APPLICATION`, `OPERATOR`, `PARALLELISM`, `TASK`.
    language:
      python:
        mapCase: false
type: object
required:
  - configurationType
language:
  nodejs:
    requiredOutputs:
      - configurationType
      - logLevel
      - metricsLevel
