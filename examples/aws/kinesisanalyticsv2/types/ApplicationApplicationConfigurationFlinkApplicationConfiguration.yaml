$token: aws:kinesisanalyticsv2/ApplicationApplicationConfigurationFlinkApplicationConfiguration:ApplicationApplicationConfigurationFlinkApplicationConfiguration
properties:
  checkpointConfiguration:
    $ref: "#/types/aws:kinesisanalyticsv2/ApplicationApplicationConfigurationFlinkApplicationConfigurationCheckpointConfiguration:ApplicationApplicationConfigurationFlinkApplicationConfigurationCheckpointConfiguration"
    description: |
      Describes an application's checkpointing configuration.
    language:
      python:
        mapCase: false
  monitoringConfiguration:
    $ref: "#/types/aws:kinesisanalyticsv2/ApplicationApplicationConfigurationFlinkApplicationConfigurationMonitoringConfiguration:ApplicationApplicationConfigurationFlinkApplicationConfigurationMonitoringConfiguration"
    description: |
      Describes configuration parameters for CloudWatch logging for an application.
    language:
      python:
        mapCase: false
  parallelismConfiguration:
    $ref: "#/types/aws:kinesisanalyticsv2/ApplicationApplicationConfigurationFlinkApplicationConfigurationParallelismConfiguration:ApplicationApplicationConfigurationFlinkApplicationConfigurationParallelismConfiguration"
    description: |
      Describes parameters for how an application executes multiple tasks simultaneously.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - checkpointConfiguration
      - monitoringConfiguration
      - parallelismConfiguration
