$token: aws:costexplorer/anomalySubscription:AnomalySubscription
properties:
  accountId:
    type: string
    description: |
      The unique identifier for the AWS account in which the anomaly subscription ought to be created.
  arn:
    type: string
    description: |
      ARN of the anomaly subscription.
  frequency:
    type: string
    description: |
      The frequency that anomaly reports are sent. Valid Values: `DAILY` | `IMMEDIATE` | `WEEKLY`.
  monitorArnLists:
    type: array
    items:
      type: string
    description: |
      A list of cost anomaly monitors.
  name:
    type: string
    description: |
      The name for the subscription.
  subscribers:
    type: array
    items:
      $ref: "#/types/aws:costexplorer/AnomalySubscriptionSubscriber:AnomalySubscriptionSubscriber"
    description: |
      A subscriber configuration. Multiple subscribers can be defined.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  threshold:
    type: number
    description: |
      The dollar value that triggers a notification if the threshold is exceeded.
required:
  - accountId
  - arn
  - frequency
  - monitorArnLists
  - name
  - subscribers
  - tagsAll
  - threshold
inputProperties:
  accountId:
    type: string
    description: |
      The unique identifier for the AWS account in which the anomaly subscription ought to be created.
  frequency:
    type: string
    description: |
      The frequency that anomaly reports are sent. Valid Values: `DAILY` | `IMMEDIATE` | `WEEKLY`.
  monitorArnLists:
    type: array
    items:
      type: string
    description: |
      A list of cost anomaly monitors.
  name:
    type: string
    description: |
      The name for the subscription.
    willReplaceOnChanges: true
  subscribers:
    type: array
    items:
      $ref: "#/types/aws:costexplorer/AnomalySubscriptionSubscriber:AnomalySubscriptionSubscriber"
    description: |
      A subscriber configuration. Multiple subscribers can be defined.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  threshold:
    type: number
    description: |
      The dollar value that triggers a notification if the threshold is exceeded.
requiredInputs:
  - frequency
  - monitorArnLists
  - subscribers
  - threshold
stateInputs:
  description: |
    Input properties used for looking up and filtering AnomalySubscription resources.
  properties:
    accountId:
      type: string
      description: |
        The unique identifier for the AWS account in which the anomaly subscription ought to be created.
    arn:
      type: string
      description: |
        ARN of the anomaly subscription.
    frequency:
      type: string
      description: |
        The frequency that anomaly reports are sent. Valid Values: `DAILY` | `IMMEDIATE` | `WEEKLY`.
    monitorArnLists:
      type: array
      items:
        type: string
      description: |
        A list of cost anomaly monitors.
    name:
      type: string
      description: |
        The name for the subscription.
      willReplaceOnChanges: true
    subscribers:
      type: array
      items:
        $ref: "#/types/aws:costexplorer/AnomalySubscriptionSubscriber:AnomalySubscriptionSubscriber"
      description: |
        A subscriber configuration. Multiple subscribers can be defined.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    threshold:
      type: number
      description: |
        The dollar value that triggers a notification if the threshold is exceeded.
  type: object
