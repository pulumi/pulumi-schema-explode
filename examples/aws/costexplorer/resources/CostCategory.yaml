$token: aws:costexplorer/costCategory:CostCategory
properties:
  arn:
    type: string
    description: |
      ARN of the cost category.
  defaultValue:
    type: string
    description: |
      Default value for the cost category.
  effectiveEnd:
    type: string
    description: |
      Effective end data of your Cost Category.
  effectiveStart:
    type: string
    description: |
      Effective state data of your Cost Category.
  name:
    type: string
    description: |
      Unique name for the Cost Category.
  ruleVersion:
    type: string
    description: |
      Rule schema version in this particular Cost Category.
  rules:
    type: array
    items:
      $ref: "#/types/aws:costexplorer/CostCategoryRule:CostCategoryRule"
    description: |
      Configuration block for the `Expression` object used to categorize costs. See below.
  splitChargeRules:
    type: array
    items:
      $ref: "#/types/aws:costexplorer/CostCategorySplitChargeRule:CostCategorySplitChargeRule"
    description: |
      Configuration block for the split charge rules used to allocate your charges between your Cost Category values. See below.
  tags:
    type: object
    additionalProperties:
      type: string
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - effectiveEnd
  - effectiveStart
  - name
  - rules
  - ruleVersion
  - tagsAll
inputProperties:
  defaultValue:
    type: string
    description: |
      Default value for the cost category.
  name:
    type: string
    description: |
      Unique name for the Cost Category.
    willReplaceOnChanges: true
  ruleVersion:
    type: string
    description: |
      Rule schema version in this particular Cost Category.
    willReplaceOnChanges: true
  rules:
    type: array
    items:
      $ref: "#/types/aws:costexplorer/CostCategoryRule:CostCategoryRule"
    description: |
      Configuration block for the `Expression` object used to categorize costs. See below.
  splitChargeRules:
    type: array
    items:
      $ref: "#/types/aws:costexplorer/CostCategorySplitChargeRule:CostCategorySplitChargeRule"
    description: |
      Configuration block for the split charge rules used to allocate your charges between your Cost Category values. See below.
  tags:
    type: object
    additionalProperties:
      type: string
requiredInputs:
  - rules
  - ruleVersion
stateInputs:
  description: |
    Input properties used for looking up and filtering CostCategory resources.
  properties:
    arn:
      type: string
      description: |
        ARN of the cost category.
    defaultValue:
      type: string
      description: |
        Default value for the cost category.
    effectiveEnd:
      type: string
      description: |
        Effective end data of your Cost Category.
    effectiveStart:
      type: string
      description: |
        Effective state data of your Cost Category.
    name:
      type: string
      description: |
        Unique name for the Cost Category.
      willReplaceOnChanges: true
    ruleVersion:
      type: string
      description: |
        Rule schema version in this particular Cost Category.
      willReplaceOnChanges: true
    rules:
      type: array
      items:
        $ref: "#/types/aws:costexplorer/CostCategoryRule:CostCategoryRule"
      description: |
        Configuration block for the `Expression` object used to categorize costs. See below.
    splitChargeRules:
      type: array
      items:
        $ref: "#/types/aws:costexplorer/CostCategorySplitChargeRule:CostCategorySplitChargeRule"
      description: |
        Configuration block for the split charge rules used to allocate your charges between your Cost Category values. See below.
    tags:
      type: object
      additionalProperties:
        type: string
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
