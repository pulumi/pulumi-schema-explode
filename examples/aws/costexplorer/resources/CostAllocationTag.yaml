$token: aws:costexplorer/costAllocationTag:CostAllocationTag
properties:
  status:
    type: string
    description: |
      The status of a cost allocation tag. Valid values are `Active` and `Inactive`.
  tagKey:
    type: string
    description: |
      The key for the cost allocation tag.
  type:
    type: string
    description: |
      The type of cost allocation tag.
required:
  - status
  - tagKey
  - type
inputProperties:
  status:
    type: string
    description: |
      The status of a cost allocation tag. Valid values are `Active` and `Inactive`.
  tagKey:
    type: string
    description: |
      The key for the cost allocation tag.
requiredInputs:
  - status
  - tagKey
stateInputs:
  description: |
    Input properties used for looking up and filtering CostAllocationTag resources.
  properties:
    status:
      type: string
      description: |
        The status of a cost allocation tag. Valid values are `Active` and `Inactive`.
    tagKey:
      type: string
      description: |
        The key for the cost allocation tag.
    type:
      type: string
      description: |
        The type of cost allocation tag.
  type: object
