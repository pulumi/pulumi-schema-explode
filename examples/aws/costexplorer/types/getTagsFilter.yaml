$token: aws:costexplorer/getTagsFilter:getTagsFilter
properties:
  ands:
    type: array
    items:
      $ref: "#/types/aws:costexplorer/getTagsFilterAnd:getTagsFilterAnd"
    description: |
      Return results that match both `Dimension` objects.
    language:
      python:
        mapCase: false
  costCategory:
    $ref: "#/types/aws:costexplorer/getTagsFilterCostCategory:getTagsFilterCostCategory"
    description: |
      Configuration block for the filter that's based on `CostCategory` values. See below.
    language:
      python:
        mapCase: false
  dimension:
    $ref: "#/types/aws:costexplorer/getTagsFilterDimension:getTagsFilterDimension"
    description: |
      Configuration block for the specific `Dimension` to use for `Expression`. See below.
    language:
      python:
        mapCase: false
  not:
    $ref: "#/types/aws:costexplorer/getTagsFilterNot:getTagsFilterNot"
    description: |
      Return results that match both `Dimension` object.
    language:
      python:
        mapCase: false
  ors:
    type: array
    items:
      $ref: "#/types/aws:costexplorer/getTagsFilterOr:getTagsFilterOr"
    description: |
      Return results that match both `Dimension` object.
    language:
      python:
        mapCase: false
  tags:
    $ref: "#/types/aws:costexplorer/getTagsFilterTags:getTagsFilterTags"
    description: |
      Tags that match your request.
    language:
      python:
        mapCase: false
type: object
