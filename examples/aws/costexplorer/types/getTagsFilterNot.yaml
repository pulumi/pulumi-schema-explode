$token: aws:costexplorer/getTagsFilterNot:getTagsFilterNot
properties:
  costCategory:
    $ref: "#/types/aws:costexplorer/getTagsFilterNotCostCategory:getTagsFilterNotCostCategory"
    description: |
      Configuration block for the filter that's based on `CostCategory` values. See below.
    language:
      python:
        mapCase: false
  dimension:
    $ref: "#/types/aws:costexplorer/getTagsFilterNotDimension:getTagsFilterNotDimension"
    description: |
      Configuration block for the specific `Dimension` to use for `Expression`. See below.
    language:
      python:
        mapCase: false
  tags:
    $ref: "#/types/aws:costexplorer/getTagsFilterNotTags:getTagsFilterNotTags"
    description: |
      Tags that match your request.
    language:
      python:
        mapCase: false
type: object
