$token: aws:costexplorer/getCostCategorySplitChargeRule:getCostCategorySplitChargeRule
properties:
  method:
    type: string
    description: |
      Method that's used to define how to split your source costs across your targets. Valid values are `FIXED`, `PROPORTIONAL`, `EVEN`
    language:
      python:
        mapCase: false
  parameters:
    type: array
    items:
      $ref: "#/types/aws:costexplorer/getCostCategorySplitChargeRuleParameter:getCostCategorySplitChargeRuleParameter"
    description: |
      Configuration block for the parameters for a split charge method. This is only required for the `FIXED` method. See below.
    language:
      python:
        mapCase: false
  source:
    type: string
    description: |
      Cost Category value that you want to split.
    language:
      python:
        mapCase: false
  targets:
    type: array
    items:
      type: string
    description: |
      Cost Category values that you want to split costs across. These values can't be used as a source in other split charge rules.
    language:
      python:
        mapCase: false
type: object
required:
  - method
  - parameters
  - source
  - targets
language:
  nodejs:
    requiredInputs: []
