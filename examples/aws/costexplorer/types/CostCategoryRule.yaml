$token: aws:costexplorer/CostCategoryRule:CostCategoryRule
properties:
  inheritedValue:
    $ref: "#/types/aws:costexplorer/CostCategoryRuleInheritedValue:CostCategoryRuleInheritedValue"
    description: |
      Configuration block for the value the line item is categorized as if the line item contains the matched dimension. See below.
    language:
      python:
        mapCase: false
  rule:
    $ref: "#/types/aws:costexplorer/CostCategoryRuleRule:CostCategoryRuleRule"
    description: |
      Configuration block for the `Expression` object used to categorize costs. See below.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      Parameter type.
    language:
      python:
        mapCase: false
  value:
    type: string
    description: |
      Default value for the cost category.
    language:
      python:
        mapCase: false
type: object
