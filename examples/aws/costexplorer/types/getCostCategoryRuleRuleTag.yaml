$token: aws:costexplorer/getCostCategoryRuleRuleTag:getCostCategoryRuleRuleTag
properties:
  key:
    type: string
    description: |
      Key for the tag.
    language:
      python:
        mapCase: false
  matchOptions:
    type: array
    items:
      type: string
    description: |
      Match options that you can use to filter your results. MatchOptions is only applicable for actions related to cost category. The default values for MatchOptions is `EQUALS` and `CASE_SENSITIVE`. Valid values are: `EQUALS`,  `ABSENT`, `STARTS_WITH`, `ENDS_WITH`, `CONTAINS`, `CASE_SENSITIVE`, `CASE_INSENSITIVE`.
    language:
      python:
        mapCase: false
  values:
    type: array
    items:
      type: string
    description: |
      Parameter values.
    language:
      python:
        mapCase: false
type: object
required:
  - key
  - matchOptions
  - values
language:
  nodejs:
    requiredInputs: []
