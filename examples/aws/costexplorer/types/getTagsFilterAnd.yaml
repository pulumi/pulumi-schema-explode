$token: aws:costexplorer/getTagsFilterAnd:getTagsFilterAnd
properties:
  costCategory:
    $ref: "#/types/aws:costexplorer/getTagsFilterAndCostCategory:getTagsFilterAndCostCategory"
    description: |
      Configuration block for the filter that's based on `CostCategory` values. See below.
    language:
      python:
        mapCase: false
  dimension:
    $ref: "#/types/aws:costexplorer/getTagsFilterAndDimension:getTagsFilterAndDimension"
    description: |
      Configuration block for the specific `Dimension` to use for `Expression`. See below.
    language:
      python:
        mapCase: false
  tags:
    $ref: "#/types/aws:costexplorer/getTagsFilterAndTags:getTagsFilterAndTags"
    description: |
      Tags that match your request.
    language:
      python:
        mapCase: false
type: object
