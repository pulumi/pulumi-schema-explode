$token: aws:costexplorer/getTagsFilterOr:getTagsFilterOr
properties:
  costCategory:
    $ref: "#/types/aws:costexplorer/getTagsFilterOrCostCategory:getTagsFilterOrCostCategory"
    description: |
      Configuration block for the filter that's based on `CostCategory` values. See below.
    language:
      python:
        mapCase: false
  dimension:
    $ref: "#/types/aws:costexplorer/getTagsFilterOrDimension:getTagsFilterOrDimension"
    description: |
      Configuration block for the specific `Dimension` to use for `Expression`. See below.
    language:
      python:
        mapCase: false
  tags:
    $ref: "#/types/aws:costexplorer/getTagsFilterOrTags:getTagsFilterOrTags"
    description: |
      Tags that match your request.
    language:
      python:
        mapCase: false
type: object
