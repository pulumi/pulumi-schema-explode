$token: aws:costexplorer/getCostCategoryRuleRuleOr:getCostCategoryRuleRuleOr
properties:
  costCategories:
    type: array
    items:
      $ref: "#/types/aws:costexplorer/getCostCategoryRuleRuleOrCostCategory:getCostCategoryRuleRuleOrCostCategory"
    description: |
      Configuration block for the filter that's based on `CostCategory` values. See below.
    language:
      python:
        mapCase: false
  dimensions:
    type: array
    items:
      $ref: "#/types/aws:costexplorer/getCostCategoryRuleRuleOrDimension:getCostCategoryRuleRuleOrDimension"
    description: |
      Configuration block for the specific `Dimension` to use for `Expression`. See below.
    language:
      python:
        mapCase: false
  tags:
    type: array
    items:
      $ref: "#/types/aws:costexplorer/getCostCategoryRuleRuleOrTag:getCostCategoryRuleRuleOrTag"
    description: |
      Resource tags.
    language:
      python:
        mapCase: false
type: object
required:
  - costCategories
  - dimensions
  - tags
language:
  nodejs:
    requiredInputs: []
