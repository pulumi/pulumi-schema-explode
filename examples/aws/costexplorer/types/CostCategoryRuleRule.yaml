$token: aws:costexplorer/CostCategoryRuleRule:CostCategoryRuleRule
properties:
  ands:
    type: array
    items:
      $ref: "#/types/aws:costexplorer/CostCategoryRuleRuleAnd:CostCategoryRuleRuleAnd"
    description: |
      Return results that match both `Dimension` objects.
    language:
      python:
        mapCase: false
  costCategory:
    $ref: "#/types/aws:costexplorer/CostCategoryRuleRuleCostCategory:CostCategoryRuleRuleCostCategory"
    description: |
      Configuration block for the filter that's based on `CostCategory` values. See below.
    language:
      python:
        mapCase: false
  dimension:
    $ref: "#/types/aws:costexplorer/CostCategoryRuleRuleDimension:CostCategoryRuleRuleDimension"
    description: |
      Configuration block for the specific `Dimension` to use for `Expression`. See below.
    language:
      python:
        mapCase: false
  not:
    $ref: "#/types/aws:costexplorer/CostCategoryRuleRuleNot:CostCategoryRuleRuleNot"
    description: |
      Return results that match both `Dimension` object.
    language:
      python:
        mapCase: false
  ors:
    type: array
    items:
      $ref: "#/types/aws:costexplorer/CostCategoryRuleRuleOr:CostCategoryRuleRuleOr"
    description: |
      Return results that match both `Dimension` object.
    language:
      python:
        mapCase: false
  tags:
    $ref: "#/types/aws:costexplorer/CostCategoryRuleRuleTags:CostCategoryRuleRuleTags"
    language:
      python:
        mapCase: false
type: object
