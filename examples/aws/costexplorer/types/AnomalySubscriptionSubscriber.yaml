$token: aws:costexplorer/AnomalySubscriptionSubscriber:AnomalySubscriptionSubscriber
properties:
  address:
    type: string
    description: |
      The address of the subscriber. If type is `SNS`, this will be the arn of the sns topic. If type is `EMAIL`, this will be the destination email address.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      The type of subscription. Valid Values: `SNS` | `EMAIL`.
    language:
      python:
        mapCase: false
type: object
required:
  - address
  - type
