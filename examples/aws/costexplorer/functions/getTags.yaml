$token: aws:costexplorer/getTags:getTags
inputs:
  description: |
    A collection of arguments for invoking getTags.
  properties:
    filter:
      $ref: "#/types/aws:costexplorer/getTagsFilter:getTagsFilter"
      description: |
        Configuration block for the `Expression` object used to categorize costs. See below.
    searchString:
      type: string
      description: |
        Value that you want to search for.
    sortBies:
      type: array
      items:
        $ref: "#/types/aws:costexplorer/getTagsSortBy:getTagsSortBy"
      description: |
        Configuration block for the value by which you want to sort the data. See below.
    tagKey:
      type: string
      description: |
        Key of the tag that you want to return values for.
    timePeriod:
      $ref: "#/types/aws:costexplorer/getTagsTimePeriod:getTagsTimePeriod"
      description: |
        Configuration block for the start and end dates for retrieving the dimension values.
  type: object
  required:
    - timePeriod
outputs:
  description: |
    A collection of values returned by getTags.
  properties:
    filter:
      $ref: "#/types/aws:costexplorer/getTagsFilter:getTagsFilter"
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    searchString:
      type: string
    sortBies:
      type: array
      items:
        $ref: "#/types/aws:costexplorer/getTagsSortBy:getTagsSortBy"
    tagKey:
      type: string
    tags:
      type: array
      items:
        type: string
      description: |
        Tags that match your request.
    timePeriod:
      $ref: "#/types/aws:costexplorer/getTagsTimePeriod:getTagsTimePeriod"
  type: object
  required:
    - tags
    - timePeriod
    - id
