$token: aws:serverlessrepository/getApplication:getApplication
inputs:
  description: |
    A collection of arguments for invoking getApplication.
  properties:
    applicationId:
      type: string
      description: |
        ARN of the application.
    semanticVersion:
      type: string
      description: |
        Requested version of the application. By default, retrieves the latest version.
  type: object
  required:
    - applicationId
outputs:
  description: |
    A collection of values returned by getApplication.
  properties:
    applicationId:
      type: string
      description: |
        ARN of the application.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    name:
      type: string
      description: |
        Name of the application.
    requiredCapabilities:
      type: array
      items:
        type: string
      description: |
        A list of capabilities describing the permissions needed to deploy the application.
    semanticVersion:
      type: string
    sourceCodeUrl:
      type: string
      description: |
        URL pointing to the source code of the application version.
    templateUrl:
      type: string
      description: |
        URL pointing to the Cloud Formation template for the application version.
  type: object
  required:
    - applicationId
    - name
    - requiredCapabilities
    - semanticVersion
    - sourceCodeUrl
    - templateUrl
    - id
