$token: aws:connect/quickConnect:QuickConnect
properties:
  arn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the Quick Connect.
  description:
    type: string
    description: |
      Specifies the description of the Quick Connect.
  instanceId:
    type: string
    description: |
      Specifies the identifier of the hosting Amazon Connect Instance.
  name:
    type: string
    description: |
      Specifies the name of the Quick Connect.
  quickConnectConfig:
    $ref: "#/types/aws:connect/QuickConnectQuickConnectConfig:QuickConnectQuickConnectConfig"
    description: |
      A block that defines the configuration information for the Quick Connect: `quick_connect_type` and one of `phone_config`, `queue_config`, `user_config` . The Quick Connect Config block is documented below.
  quickConnectId:
    type: string
    description: |
      The identifier for the Quick Connect.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Tags to apply to the Quick Connect. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - instanceId
  - name
  - quickConnectConfig
  - quickConnectId
  - tagsAll
inputProperties:
  description:
    type: string
    description: |
      Specifies the description of the Quick Connect.
  instanceId:
    type: string
    description: |
      Specifies the identifier of the hosting Amazon Connect Instance.
  name:
    type: string
    description: |
      Specifies the name of the Quick Connect.
  quickConnectConfig:
    $ref: "#/types/aws:connect/QuickConnectQuickConnectConfig:QuickConnectQuickConnectConfig"
    description: |
      A block that defines the configuration information for the Quick Connect: `quick_connect_type` and one of `phone_config`, `queue_config`, `user_config` . The Quick Connect Config block is documented below.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Tags to apply to the Quick Connect. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - instanceId
  - quickConnectConfig
stateInputs:
  description: |
    Input properties used for looking up and filtering QuickConnect resources.
  properties:
    arn:
      type: string
      description: |
        The Amazon Resource Name (ARN) of the Quick Connect.
    description:
      type: string
      description: |
        Specifies the description of the Quick Connect.
    instanceId:
      type: string
      description: |
        Specifies the identifier of the hosting Amazon Connect Instance.
    name:
      type: string
      description: |
        Specifies the name of the Quick Connect.
    quickConnectConfig:
      $ref: "#/types/aws:connect/QuickConnectQuickConnectConfig:QuickConnectQuickConnectConfig"
      description: |
        A block that defines the configuration information for the Quick Connect: `quick_connect_type` and one of `phone_config`, `queue_config`, `user_config` . The Quick Connect Config block is documented below.
    quickConnectId:
      type: string
      description: |
        The identifier for the Quick Connect.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Tags to apply to the Quick Connect. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
