$token: aws:connect/hoursOfOperation:HoursOfOperation
properties:
  arn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the Hours of Operation.
  configs:
    type: array
    items:
      $ref: "#/types/aws:connect/HoursOfOperationConfig:HoursOfOperationConfig"
    description: |
      One or more config blocks which define the configuration information for the hours of operation: day, start time, and end time . Config blocks are documented below.
  description:
    type: string
    description: |
      Specifies the description of the Hours of Operation.
  hoursOfOperationArn:
    type: string
    description: |
      (**Deprecated**) The Amazon Resource Name (ARN) of the Hours of Operation.
    deprecationMessage: use 'arn' attribute instead
  hoursOfOperationId:
    type: string
    description: |
      The identifier for the hours of operation.
  instanceId:
    type: string
    description: |
      Specifies the identifier of the hosting Amazon Connect Instance.
  name:
    type: string
    description: |
      Specifies the name of the Hours of Operation.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Tags to apply to the Hours of Operation. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  timeZone:
    type: string
    description: |
      Specifies the time zone of the Hours of Operation.
required:
  - arn
  - configs
  - hoursOfOperationArn
  - hoursOfOperationId
  - instanceId
  - name
  - tagsAll
  - timeZone
inputProperties:
  configs:
    type: array
    items:
      $ref: "#/types/aws:connect/HoursOfOperationConfig:HoursOfOperationConfig"
    description: |
      One or more config blocks which define the configuration information for the hours of operation: day, start time, and end time . Config blocks are documented below.
  description:
    type: string
    description: |
      Specifies the description of the Hours of Operation.
  instanceId:
    type: string
    description: |
      Specifies the identifier of the hosting Amazon Connect Instance.
  name:
    type: string
    description: |
      Specifies the name of the Hours of Operation.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Tags to apply to the Hours of Operation. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  timeZone:
    type: string
    description: |
      Specifies the time zone of the Hours of Operation.
requiredInputs:
  - configs
  - instanceId
  - timeZone
stateInputs:
  description: |
    Input properties used for looking up and filtering HoursOfOperation resources.
  properties:
    arn:
      type: string
      description: |
        The Amazon Resource Name (ARN) of the Hours of Operation.
    configs:
      type: array
      items:
        $ref: "#/types/aws:connect/HoursOfOperationConfig:HoursOfOperationConfig"
      description: |
        One or more config blocks which define the configuration information for the hours of operation: day, start time, and end time . Config blocks are documented below.
    description:
      type: string
      description: |
        Specifies the description of the Hours of Operation.
    hoursOfOperationArn:
      type: string
      description: |
        (**Deprecated**) The Amazon Resource Name (ARN) of the Hours of Operation.
      deprecationMessage: use 'arn' attribute instead
    hoursOfOperationId:
      type: string
      description: |
        The identifier for the hours of operation.
    instanceId:
      type: string
      description: |
        Specifies the identifier of the hosting Amazon Connect Instance.
    name:
      type: string
      description: |
        Specifies the name of the Hours of Operation.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Tags to apply to the Hours of Operation. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    timeZone:
      type: string
      description: |
        Specifies the time zone of the Hours of Operation.
  type: object
