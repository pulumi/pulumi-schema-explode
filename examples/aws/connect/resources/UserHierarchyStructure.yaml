$token: aws:connect/userHierarchyStructure:UserHierarchyStructure
properties:
  hierarchyStructure:
    $ref: "#/types/aws:connect/UserHierarchyStructureHierarchyStructure:UserHierarchyStructureHierarchyStructure"
    description: |
      A block that defines the hierarchy structure's levels. The `hierarchy_structure` block is documented below.
  instanceId:
    type: string
    description: |
      Specifies the identifier of the hosting Amazon Connect Instance.
required:
  - hierarchyStructure
  - instanceId
inputProperties:
  hierarchyStructure:
    $ref: "#/types/aws:connect/UserHierarchyStructureHierarchyStructure:UserHierarchyStructureHierarchyStructure"
    description: |
      A block that defines the hierarchy structure's levels. The `hierarchy_structure` block is documented below.
  instanceId:
    type: string
    description: |
      Specifies the identifier of the hosting Amazon Connect Instance.
requiredInputs:
  - hierarchyStructure
  - instanceId
stateInputs:
  description: |
    Input properties used for looking up and filtering UserHierarchyStructure resources.
  properties:
    hierarchyStructure:
      $ref: "#/types/aws:connect/UserHierarchyStructureHierarchyStructure:UserHierarchyStructureHierarchyStructure"
      description: |
        A block that defines the hierarchy structure's levels. The `hierarchy_structure` block is documented below.
    instanceId:
      type: string
      description: |
        Specifies the identifier of the hosting Amazon Connect Instance.
  type: object
