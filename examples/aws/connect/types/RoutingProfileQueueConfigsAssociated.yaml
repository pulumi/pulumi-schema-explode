$token: aws:connect/RoutingProfileQueueConfigsAssociated:RoutingProfileQueueConfigsAssociated
properties:
  channel:
    type: string
    description: |
      Specifies the channels that agents can handle in the Contact Control Panel (CCP). Valid values are `VOICE`, `CHAT`, `TASK`.
    language:
      python:
        mapCase: false
  delay:
    type: integer
    description: |
      Specifies the delay, in seconds, that a contact should be in the queue before they are routed to an available agent
    language:
      python:
        mapCase: false
  priority:
    type: integer
    description: |
      Specifies the order in which contacts are to be handled for the queue.
    language:
      python:
        mapCase: false
  queueArn:
    type: string
    description: |
      ARN for the queue.
    language:
      python:
        mapCase: false
  queueId:
    type: string
    description: |
      Specifies the identifier for the queue.
    language:
      python:
        mapCase: false
  queueName:
    type: string
    description: |
      Name for the queue.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - channel
      - delay
      - priority
      - queueArn
      - queueId
      - queueName
