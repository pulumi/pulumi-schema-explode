$token: aws:connect/getQuickConnectQuickConnectConfig:getQuickConnectQuickConnectConfig
properties:
  phoneConfigs:
    type: array
    items:
      $ref: "#/types/aws:connect/getQuickConnectQuickConnectConfigPhoneConfig:getQuickConnectQuickConnectConfigPhoneConfig"
    description: |
      Phone configuration of the Quick Connect. This is returned only if `quick_connect_type` is `PHONE_NUMBER`. The `phone_config` block is documented below.
    language:
      python:
        mapCase: false
  queueConfigs:
    type: array
    items:
      $ref: "#/types/aws:connect/getQuickConnectQuickConnectConfigQueueConfig:getQuickConnectQuickConnectConfigQueueConfig"
    description: |
      Queue configuration of the Quick Connect. This is returned only if `quick_connect_type` is `QUEUE`. The `queue_config` block is documented below.
    language:
      python:
        mapCase: false
  quickConnectType:
    type: string
    description: |
      Configuration type of the Quick Connect. Valid values are `PHONE_NUMBER`, `QUEUE`, `USER`.
    language:
      python:
        mapCase: false
  userConfigs:
    type: array
    items:
      $ref: "#/types/aws:connect/getQuickConnectQuickConnectConfigUserConfig:getQuickConnectQuickConnectConfigUserConfig"
    description: |
      User configuration of the Quick Connect. This is returned only if `quick_connect_type` is `USER`. The `user_config` block is documented below.
    language:
      python:
        mapCase: false
type: object
required:
  - phoneConfigs
  - queueConfigs
  - quickConnectType
  - userConfigs
language:
  nodejs:
    requiredInputs: []
