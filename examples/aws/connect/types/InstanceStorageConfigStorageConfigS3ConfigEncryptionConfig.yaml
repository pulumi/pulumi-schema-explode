$token: aws:connect/InstanceStorageConfigStorageConfigS3ConfigEncryptionConfig:InstanceStorageConfigStorageConfigS3ConfigEncryptionConfig
properties:
  encryptionType:
    type: string
    description: |
      The type of encryption. Valid Values: `KMS`.
    language:
      python:
        mapCase: false
  keyId:
    type: string
    description: |
      The full ARN of the encryption key. Be sure to provide the full ARN of the encryption key, not just the ID.
    language:
      python:
        mapCase: false
type: object
required:
  - encryptionType
  - keyId
