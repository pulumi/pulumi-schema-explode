$token: aws:connect/getHoursOfOperationConfig:getHoursOfOperationConfig
properties:
  day:
    type: string
    description: |
      Day that the hours of operation applies to.
    language:
      python:
        mapCase: false
  endTimes:
    type: array
    items:
      $ref: "#/types/aws:connect/getHoursOfOperationConfigEndTime:getHoursOfOperationConfigEndTime"
    description: |
      End time block specifies the time that your contact center closes. The `end_time` is documented below.
    language:
      python:
        mapCase: false
  startTimes:
    type: array
    items:
      $ref: "#/types/aws:connect/getHoursOfOperationConfigStartTime:getHoursOfOperationConfigStartTime"
    description: |
      Start time block specifies the time that your contact center opens. The `start_time` is documented below.
    language:
      python:
        mapCase: false
type: object
required:
  - day
  - endTimes
  - startTimes
language:
  nodejs:
    requiredInputs: []
