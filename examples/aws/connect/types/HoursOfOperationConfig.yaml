$token: aws:connect/HoursOfOperationConfig:HoursOfOperationConfig
properties:
  day:
    type: string
    description: |
      Specifies the day that the hours of operation applies to.
    language:
      python:
        mapCase: false
  endTime:
    $ref: "#/types/aws:connect/HoursOfOperationConfigEndTime:HoursOfOperationConfigEndTime"
    description: |
      A end time block specifies the time that your contact center closes. The `end_time` is documented below.
    language:
      python:
        mapCase: false
  startTime:
    $ref: "#/types/aws:connect/HoursOfOperationConfigStartTime:HoursOfOperationConfigStartTime"
    description: |
      A start time block specifies the time that your contact center opens. The `start_time` is documented below.
    language:
      python:
        mapCase: false
type: object
required:
  - day
  - endTime
  - startTime
