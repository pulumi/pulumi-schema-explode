$token: aws:connect/InstanceStorageConfigStorageConfigS3Config:InstanceStorageConfigStorageConfigS3Config
properties:
  bucketName:
    type: string
    description: |
      The S3 bucket name.
    language:
      python:
        mapCase: false
  bucketPrefix:
    type: string
    description: |
      The S3 bucket prefix.
    language:
      python:
        mapCase: false
  encryptionConfig:
    $ref: "#/types/aws:connect/InstanceStorageConfigStorageConfigS3ConfigEncryptionConfig:InstanceStorageConfigStorageConfigS3ConfigEncryptionConfig"
    description: |
      The encryption configuration. Documented below.
    language:
      python:
        mapCase: false
type: object
required:
  - bucketName
  - bucketPrefix
