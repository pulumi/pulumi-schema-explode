$token: aws:connect/BotAssociationLexBot:BotAssociationLexBot
properties:
  lexRegion:
    type: string
    description: |
      The Region that the Amazon Lex (V1) bot was created in. Defaults to current region.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      The name of the Amazon Lex (V1) bot.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - name
language:
  nodejs:
    requiredOutputs:
      - lexRegion
      - name
