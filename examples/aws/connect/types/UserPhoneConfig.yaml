$token: aws:connect/UserPhoneConfig:UserPhoneConfig
properties:
  afterContactWorkTimeLimit:
    type: integer
    description: |
      The After Call Work (ACW) timeout setting, in seconds. Minimum value of 0.
    language:
      python:
        mapCase: false
  autoAccept:
    type: boolean
    description: |
      When Auto-Accept Call is enabled for an available agent, the agent connects to contacts automatically.
    language:
      python:
        mapCase: false
  deskPhoneNumber:
    type: string
    description: |
      The phone number for the user's desk phone. Required if `phone_type` is set as `DESK_PHONE`.
    language:
      python:
        mapCase: false
  phoneType:
    type: string
    description: |
      The phone type. Valid values are `DESK_PHONE` and `SOFT_PHONE`.
    language:
      python:
        mapCase: false
type: object
required:
  - phoneType
