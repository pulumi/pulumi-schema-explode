$token: aws:connect/QuickConnectQuickConnectConfig:QuickConnectQuickConnectConfig
properties:
  phoneConfigs:
    type: array
    items:
      $ref: "#/types/aws:connect/QuickConnectQuickConnectConfigPhoneConfig:QuickConnectQuickConnectConfigPhoneConfig"
    description: |
      Specifies the phone configuration of the Quick Connect. This is required only if `quick_connect_type` is `PHONE_NUMBER`. The `phone_config` block is documented below.
    language:
      python:
        mapCase: false
  queueConfigs:
    type: array
    items:
      $ref: "#/types/aws:connect/QuickConnectQuickConnectConfigQueueConfig:QuickConnectQuickConnectConfigQueueConfig"
    description: |
      Specifies the queue configuration of the Quick Connect. This is required only if `quick_connect_type` is `QUEUE`. The `queue_config` block is documented below.
    language:
      python:
        mapCase: false
  quickConnectType:
    type: string
    description: |
      Specifies the configuration type of the quick connect. valid values are `PHONE_NUMBER`, `QUEUE`, `USER`.
    language:
      python:
        mapCase: false
  userConfigs:
    type: array
    items:
      $ref: "#/types/aws:connect/QuickConnectQuickConnectConfigUserConfig:QuickConnectQuickConnectConfigUserConfig"
    description: |
      Specifies the user configuration of the Quick Connect. This is required only if `quick_connect_type` is `USER`. The `user_config` block is documented below.
    language:
      python:
        mapCase: false
type: object
required:
  - quickConnectType
