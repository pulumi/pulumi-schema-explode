$token: aws:connect/getBotAssociation:getBotAssociation
inputs:
  description: |
    A collection of arguments for invoking getBotAssociation.
  properties:
    instanceId:
      type: string
      description: |
        Identifier of the Amazon Connect instance. You can find the instanceId in the ARN of the instance.
    lexBot:
      $ref: "#/types/aws:connect/getBotAssociationLexBot:getBotAssociationLexBot"
      description: |
        Configuration information of an Amazon Lex (V1) bot. Detailed below.
  type: object
  required:
    - instanceId
    - lexBot
outputs:
  description: |
    A collection of values returned by getBotAssociation.
  properties:
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    instanceId:
      type: string
    lexBot:
      $ref: "#/types/aws:connect/getBotAssociationLexBot:getBotAssociationLexBot"
  type: object
  required:
    - instanceId
    - lexBot
    - id
