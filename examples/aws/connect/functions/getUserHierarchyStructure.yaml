$token: aws:connect/getUserHierarchyStructure:getUserHierarchyStructure
inputs:
  description: |
    A collection of arguments for invoking getUserHierarchyStructure.
  properties:
    instanceId:
      type: string
      description: |
        Reference to the hosting Amazon Connect Instance
  type: object
  required:
    - instanceId
outputs:
  description: |
    A collection of values returned by getUserHierarchyStructure.
  properties:
    hierarchyStructures:
      type: array
      items:
        $ref: "#/types/aws:connect/getUserHierarchyStructureHierarchyStructure:getUserHierarchyStructureHierarchyStructure"
      description: |
        Block that defines the hierarchy structure's levels. The `hierarchy_structure` block is documented below.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    instanceId:
      type: string
  type: object
  required:
    - hierarchyStructures
    - instanceId
    - id
