$token: aws:ec2transitgateway/getConnectFilter:getConnectFilter
properties:
  name:
    type: string
    description: |
      Name of the filter.
    language:
      python:
        mapCase: false
  values:
    type: array
    items:
      type: string
    description: |
      List of one or more values for the filter.
    language:
      python:
        mapCase: false
type: object
required:
  - name
  - values
