$token: aws:ec2transitgateway/getAttachmentFilter:getAttachmentFilter
properties:
  name:
    type: string
    description: |
      Name of the field to filter by, as defined by the [underlying AWS API](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_DescribeTransitGatewayAttachments.html).
    language:
      python:
        mapCase: false
  values:
    type: array
    items:
      type: string
    description: |
      List of one or more values for the filter.
    language:
      python:
        mapCase: false
type: object
required:
  - name
  - values
