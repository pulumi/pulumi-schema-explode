$token: aws:ec2transitgateway/getVpcAttachments:getVpcAttachments
description: |-
  Get information on EC2 Transit Gateway VPC Attachments.

  {{% examples %}}
  ## Example Usage
  {{% /examples %}}
inputs:
  description: |
    A collection of arguments for invoking getVpcAttachments.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2transitgateway/getVpcAttachmentsFilter:getVpcAttachmentsFilter"
      description: |
        One or more configuration blocks containing name-values filters. Detailed below.
  type: object
outputs:
  description: |
    A collection of values returned by getVpcAttachments.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2transitgateway/getVpcAttachmentsFilter:getVpcAttachmentsFilter"
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    ids:
      type: array
      items:
        type: string
  type: object
  required:
    - ids
    - id
