$token: aws:ec2transitgateway/getConnect:getConnect
inputs:
  description: |
    A collection of arguments for invoking getConnect.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2transitgateway/getConnectFilter:getConnectFilter"
      description: |
        One or more configuration blocks containing name-values filters. Detailed below.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value tags for the EC2 Transit Gateway Connect
    transitGatewayConnectId:
      type: string
      description: |
        Identifier of the EC2 Transit Gateway Connect.
  type: object
outputs:
  description: |
    A collection of values returned by getConnect.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2transitgateway/getConnectFilter:getConnectFilter"
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    protocol:
      type: string
      description: |
        Tunnel protocol
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value tags for the EC2 Transit Gateway Connect
    transitGatewayConnectId:
      type: string
    transitGatewayId:
      type: string
      description: |
        EC2 Transit Gateway identifier
    transportAttachmentId:
      type: string
      description: |
        The underlaying VPC attachment
  type: object
  required:
    - protocol
    - tags
    - transitGatewayConnectId
    - transitGatewayId
    - transportAttachmentId
    - id
