$token: aws:ec2transitgateway/getTransitGateway:getTransitGateway
inputs:
  description: |
    A collection of arguments for invoking getTransitGateway.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2transitgateway/getTransitGatewayFilter:getTransitGatewayFilter"
      description: |
        One or more configuration blocks containing name-values filters. Detailed below.
    id:
      type: string
      description: |
        Identifier of the EC2 Transit Gateway.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value tags for the EC2 Transit Gateway
  type: object
outputs:
  description: |
    A collection of values returned by getTransitGateway.
  properties:
    amazonSideAsn:
      type: integer
      description: |
        Private Autonomous System Number (ASN) for the Amazon side of a BGP session
    arn:
      type: string
      description: |
        EC2 Transit Gateway ARN
    associationDefaultRouteTableId:
      type: string
      description: |
        Identifier of the default association route table
    autoAcceptSharedAttachments:
      type: string
      description: |
        Whether resource attachment requests are automatically accepted
    defaultRouteTableAssociation:
      type: string
      description: |
        Whether resource attachments are automatically associated with the default association route table
    defaultRouteTablePropagation:
      type: string
      description: |
        Whether resource attachments automatically propagate routes to the default propagation route table
    description:
      type: string
      description: |
        Description of the EC2 Transit Gateway
    dnsSupport:
      type: string
      description: |
        Whether DNS support is enabled
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2transitgateway/getTransitGatewayFilter:getTransitGatewayFilter"
    id:
      type: string
      description: |
        EC2 Transit Gateway identifier
    multicastSupport:
      type: string
      description: |
        Whether Multicast support is enabled
    ownerId:
      type: string
      description: |
        Identifier of the AWS account that owns the EC2 Transit Gateway
    propagationDefaultRouteTableId:
      type: string
      description: |
        Identifier of the default propagation route table
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value tags for the EC2 Transit Gateway
    transitGatewayCidrBlocks:
      type: array
      items:
        type: string
      description: |
        The list of associated CIDR blocks
    vpnEcmpSupport:
      type: string
      description: |
        Whether VPN Equal Cost Multipath Protocol support is enabled
  type: object
  required:
    - amazonSideAsn
    - arn
    - associationDefaultRouteTableId
    - autoAcceptSharedAttachments
    - defaultRouteTableAssociation
    - defaultRouteTablePropagation
    - description
    - dnsSupport
    - id
    - multicastSupport
    - ownerId
    - propagationDefaultRouteTableId
    - tags
    - transitGatewayCidrBlocks
    - vpnEcmpSupport
