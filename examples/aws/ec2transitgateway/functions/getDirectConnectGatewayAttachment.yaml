$token: aws:ec2transitgateway/getDirectConnectGatewayAttachment:getDirectConnectGatewayAttachment
inputs:
  description: |
    A collection of arguments for invoking getDirectConnectGatewayAttachment.
  properties:
    dxGatewayId:
      type: string
      description: |
        Identifier of the Direct Connect Gateway.
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2transitgateway/getDirectConnectGatewayAttachmentFilter:getDirectConnectGatewayAttachmentFilter"
      description: |
        Configuration block(s) for filtering. Detailed below.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags, each pair of which must exactly match a pair on the desired Transit Gateway Direct Connect Gateway Attachment.
    transitGatewayId:
      type: string
      description: |
        Identifier of the EC2 Transit Gateway.
  type: object
outputs:
  description: |
    A collection of values returned by getDirectConnectGatewayAttachment.
  properties:
    dxGatewayId:
      type: string
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2transitgateway/getDirectConnectGatewayAttachmentFilter:getDirectConnectGatewayAttachmentFilter"
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value tags for the EC2 Transit Gateway Attachment
    transitGatewayId:
      type: string
  type: object
  required:
    - tags
    - id
