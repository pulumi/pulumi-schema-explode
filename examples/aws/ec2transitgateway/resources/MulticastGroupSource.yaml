$token: aws:ec2transitgateway/multicastGroupSource:MulticastGroupSource
properties:
  groupIpAddress:
    type: string
    description: |
      The IP address assigned to the transit gateway multicast group.
  networkInterfaceId:
    type: string
    description: |
      The group members' network interface ID to register with the transit gateway multicast group.
  transitGatewayMulticastDomainId:
    type: string
    description: |
      The ID of the transit gateway multicast domain.
required:
  - groupIpAddress
  - networkInterfaceId
  - transitGatewayMulticastDomainId
inputProperties:
  groupIpAddress:
    type: string
    description: |
      The IP address assigned to the transit gateway multicast group.
    willReplaceOnChanges: true
  networkInterfaceId:
    type: string
    description: |
      The group members' network interface ID to register with the transit gateway multicast group.
    willReplaceOnChanges: true
  transitGatewayMulticastDomainId:
    type: string
    description: |
      The ID of the transit gateway multicast domain.
    willReplaceOnChanges: true
requiredInputs:
  - groupIpAddress
  - networkInterfaceId
  - transitGatewayMulticastDomainId
stateInputs:
  description: |
    Input properties used for looking up and filtering MulticastGroupSource resources.
  properties:
    groupIpAddress:
      type: string
      description: |
        The IP address assigned to the transit gateway multicast group.
      willReplaceOnChanges: true
    networkInterfaceId:
      type: string
      description: |
        The group members' network interface ID to register with the transit gateway multicast group.
      willReplaceOnChanges: true
    transitGatewayMulticastDomainId:
      type: string
      description: |
        The ID of the transit gateway multicast domain.
      willReplaceOnChanges: true
  type: object
