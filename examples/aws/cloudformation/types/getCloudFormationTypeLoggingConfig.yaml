$token: aws:cloudformation/getCloudFormationTypeLoggingConfig:getCloudFormationTypeLoggingConfig
properties:
  logGroupName:
    type: string
    description: |
      Name of the CloudWatch Log Group where CloudFormation sends error logging information when invoking the type's handlers.
    language:
      python:
        mapCase: false
  logRoleArn:
    type: string
    description: |
      ARN of the IAM Role CloudFormation assumes when sending error logging information to CloudWatch Logs.
    language:
      python:
        mapCase: false
type: object
required:
  - logGroupName
  - logRoleArn
language:
  nodejs:
    requiredInputs: []
