$token: aws:cloudformation/StackSetAutoDeployment:StackSetAutoDeployment
properties:
  enabled:
    type: boolean
    description: |
      Whether or not auto-deployment is enabled.
    language:
      python:
        mapCase: false
  retainStacksOnAccountRemoval:
    type: boolean
    description: |
      Whether or not to retain stacks when the account is removed.
    language:
      python:
        mapCase: false
type: object
