$token: aws:transcribe/languageModel:LanguageModel
properties:
  arn:
    type: string
    description: |
      ARN of the LanguageModel.
  baseModelName:
    type: string
    description: |
      Name of reference base model.
  inputDataConfig:
    $ref: "#/types/aws:transcribe/LanguageModelInputDataConfig:LanguageModelInputDataConfig"
    description: |
      The input data config for the LanguageModel. See Input Data Config for more details.
  languageCode:
    type: string
    description: |
      The language code you selected for your language model. Refer to the [supported languages](https://docs.aws.amazon.com/transcribe/latest/dg/supported-languages.html) page for accepted codes.
  modelName:
    type: string
    description: |
      The model name.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the LanguageModel. If configured with a provider [`default_tags` configuration block](https://www.terraform.io/docs/providers/aws/index.html#default_tags-configuration-block) present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
required:
  - arn
  - baseModelName
  - inputDataConfig
  - languageCode
  - modelName
  - tagsAll
inputProperties:
  baseModelName:
    type: string
    description: |
      Name of reference base model.
    willReplaceOnChanges: true
  inputDataConfig:
    $ref: "#/types/aws:transcribe/LanguageModelInputDataConfig:LanguageModelInputDataConfig"
    description: |
      The input data config for the LanguageModel. See Input Data Config for more details.
    willReplaceOnChanges: true
  languageCode:
    type: string
    description: |
      The language code you selected for your language model. Refer to the [supported languages](https://docs.aws.amazon.com/transcribe/latest/dg/supported-languages.html) page for accepted codes.
    willReplaceOnChanges: true
  modelName:
    type: string
    description: |
      The model name.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the LanguageModel. If configured with a provider [`default_tags` configuration block](https://www.terraform.io/docs/providers/aws/index.html#default_tags-configuration-block) present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - baseModelName
  - inputDataConfig
  - languageCode
  - modelName
stateInputs:
  description: |
    Input properties used for looking up and filtering LanguageModel resources.
  properties:
    arn:
      type: string
      description: |
        ARN of the LanguageModel.
    baseModelName:
      type: string
      description: |
        Name of reference base model.
      willReplaceOnChanges: true
    inputDataConfig:
      $ref: "#/types/aws:transcribe/LanguageModelInputDataConfig:LanguageModelInputDataConfig"
      description: |
        The input data config for the LanguageModel. See Input Data Config for more details.
      willReplaceOnChanges: true
    languageCode:
      type: string
      description: |
        The language code you selected for your language model. Refer to the [supported languages](https://docs.aws.amazon.com/transcribe/latest/dg/supported-languages.html) page for accepted codes.
      willReplaceOnChanges: true
    modelName:
      type: string
      description: |
        The model name.
      willReplaceOnChanges: true
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the LanguageModel. If configured with a provider [`default_tags` configuration block](https://www.terraform.io/docs/providers/aws/index.html#default_tags-configuration-block) present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
  type: object
