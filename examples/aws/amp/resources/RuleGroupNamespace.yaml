$token: aws:amp/ruleGroupNamespace:RuleGroupNamespace
properties:
  data:
    type: string
    description: |
      the rule group namespace data that you want to be applied. See more [in AWS Docs](https://docs.aws.amazon.com/prometheus/latest/userguide/AMP-Ruler.html).
  name:
    type: string
    description: |
      The name of the rule group namespace
  workspaceId:
    type: string
    description: |
      ID of the prometheus workspace the rule group namespace should be linked to
required:
  - data
  - name
  - workspaceId
inputProperties:
  data:
    type: string
    description: |
      the rule group namespace data that you want to be applied. See more [in AWS Docs](https://docs.aws.amazon.com/prometheus/latest/userguide/AMP-Ruler.html).
  name:
    type: string
    description: |
      The name of the rule group namespace
    willReplaceOnChanges: true
  workspaceId:
    type: string
    description: |
      ID of the prometheus workspace the rule group namespace should be linked to
    willReplaceOnChanges: true
requiredInputs:
  - data
  - workspaceId
stateInputs:
  description: |
    Input properties used for looking up and filtering RuleGroupNamespace resources.
  properties:
    data:
      type: string
      description: |
        the rule group namespace data that you want to be applied. See more [in AWS Docs](https://docs.aws.amazon.com/prometheus/latest/userguide/AMP-Ruler.html).
    name:
      type: string
      description: |
        The name of the rule group namespace
      willReplaceOnChanges: true
    workspaceId:
      type: string
      description: |
        ID of the prometheus workspace the rule group namespace should be linked to
      willReplaceOnChanges: true
  type: object
