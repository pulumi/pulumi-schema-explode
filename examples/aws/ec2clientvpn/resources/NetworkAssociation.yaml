$token: aws:ec2clientvpn/networkAssociation:NetworkAssociation
properties:
  associationId:
    type: string
    description: |
      The unique ID of the target network association.
  clientVpnEndpointId:
    type: string
    description: |
      The ID of the Client VPN endpoint.
  securityGroups:
    type: array
    items:
      type: string
    description: |
      A list of up to five custom security groups to apply to the target network. If not specified, the VPC's default security group is assigned.
    deprecationMessage: Use the `security_group_ids` attribute of the `aws_ec2_client_vpn_endpoint` resource instead.
  status:
    type: string
    description: |
      **Deprecated** The current state of the target network association.
    deprecationMessage: This attribute has been deprecated.
  subnetId:
    type: string
    description: |
      The ID of the subnet to associate with the Client VPN endpoint.
  vpcId:
    type: string
    description: |
      The ID of the VPC in which the target subnet is located.
required:
  - associationId
  - clientVpnEndpointId
  - securityGroups
  - status
  - subnetId
  - vpcId
inputProperties:
  clientVpnEndpointId:
    type: string
    description: |
      The ID of the Client VPN endpoint.
    willReplaceOnChanges: true
  securityGroups:
    type: array
    items:
      type: string
    description: |
      A list of up to five custom security groups to apply to the target network. If not specified, the VPC's default security group is assigned.
    deprecationMessage: Use the `security_group_ids` attribute of the `aws_ec2_client_vpn_endpoint` resource instead.
  subnetId:
    type: string
    description: |
      The ID of the subnet to associate with the Client VPN endpoint.
    willReplaceOnChanges: true
requiredInputs:
  - clientVpnEndpointId
  - subnetId
stateInputs:
  description: |
    Input properties used for looking up and filtering NetworkAssociation resources.
  properties:
    associationId:
      type: string
      description: |
        The unique ID of the target network association.
    clientVpnEndpointId:
      type: string
      description: |
        The ID of the Client VPN endpoint.
      willReplaceOnChanges: true
    securityGroups:
      type: array
      items:
        type: string
      description: |
        A list of up to five custom security groups to apply to the target network. If not specified, the VPC's default security group is assigned.
      deprecationMessage: Use the `security_group_ids` attribute of the `aws_ec2_client_vpn_endpoint` resource instead.
    status:
      type: string
      description: |
        **Deprecated** The current state of the target network association.
      deprecationMessage: This attribute has been deprecated.
    subnetId:
      type: string
      description: |
        The ID of the subnet to associate with the Client VPN endpoint.
      willReplaceOnChanges: true
    vpcId:
      type: string
      description: |
        The ID of the VPC in which the target subnet is located.
  type: object
