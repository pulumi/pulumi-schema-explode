$token: aws:ec2clientvpn/EndpointConnectionLogOptions:EndpointConnectionLogOptions
properties:
  cloudwatchLogGroup:
    type: string
    description: |
      The name of the CloudWatch Logs log group.
    language:
      python:
        mapCase: false
  cloudwatchLogStream:
    type: string
    description: |
      The name of the CloudWatch Logs log stream to which the connection data is published.
    language:
      python:
        mapCase: false
  enabled:
    type: boolean
    description: |
      Indicates whether connection logging is enabled.
    language:
      python:
        mapCase: false
type: object
required:
  - enabled
language:
  nodejs:
    requiredOutputs:
      - cloudwatchLogStream
      - enabled
