$token: aws:ec2clientvpn/EndpointClientConnectOptions:EndpointClientConnectOptions
properties:
  enabled:
    type: boolean
    description: |
      Indicates whether connection logging is enabled.
    language:
      python:
        mapCase: false
  lambdaFunctionArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the Lambda function used for connection authorization.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - enabled
      - lambdaFunctionArn
