$token: aws:ec2clientvpn/EndpointClientLoginBannerOptions:EndpointClientLoginBannerOptions
properties:
  bannerText:
    type: string
    description: |
      Customizable text that will be displayed in a banner on AWS provided clients when a VPN session is established. UTF-8 encoded characters only. Maximum of 1400 characters.
    language:
      python:
        mapCase: false
  enabled:
    type: boolean
    description: |
      Indicates whether connection logging is enabled.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - bannerText
      - enabled
