$token: aws:codepipeline/PipelineStage:PipelineStage
properties:
  actions:
    type: array
    items:
      $ref: "#/types/aws:codepipeline/PipelineStageAction:PipelineStageAction"
    description: |
      The action(s) to include in the stage. Defined as an `action` block below
    language:
      python:
        mapCase: false
  name:
    type: string
    description: |
      The name of the stage.
    language:
      python:
        mapCase: false
type: object
required:
  - actions
  - name
