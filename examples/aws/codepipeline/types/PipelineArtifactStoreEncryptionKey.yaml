$token: aws:codepipeline/PipelineArtifactStoreEncryptionKey:PipelineArtifactStoreEncryptionKey
properties:
  id:
    type: string
    description: |
      The KMS key ARN or ID
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      The type of key; currently only `KMS` is supported
    language:
      python:
        mapCase: false
type: object
required:
  - id
  - type
