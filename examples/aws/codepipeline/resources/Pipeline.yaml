$token: aws:codepipeline/pipeline:Pipeline
properties:
  arn:
    type: string
    description: |
      The codepipeline ARN.
  artifactStores:
    type: array
    items:
      $ref: "#/types/aws:codepipeline/PipelineArtifactStore:PipelineArtifactStore"
    description: |
      One or more artifact_store blocks. Artifact stores are documented below.
  name:
    type: string
    description: |
      The name of the pipeline.
  roleArn:
    type: string
    description: |
      A service role Amazon Resource Name (ARN) that grants AWS CodePipeline permission to make calls to AWS services on your behalf.
  stages:
    type: array
    items:
      $ref: "#/types/aws:codepipeline/PipelineStage:PipelineStage"
    description: |
      A stage block. Stages are documented below.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - artifactStores
  - name
  - roleArn
  - stages
  - tagsAll
inputProperties:
  artifactStores:
    type: array
    items:
      $ref: "#/types/aws:codepipeline/PipelineArtifactStore:PipelineArtifactStore"
    description: |
      One or more artifact_store blocks. Artifact stores are documented below.
  name:
    type: string
    description: |
      The name of the pipeline.
    willReplaceOnChanges: true
  roleArn:
    type: string
    description: |
      A service role Amazon Resource Name (ARN) that grants AWS CodePipeline permission to make calls to AWS services on your behalf.
  stages:
    type: array
    items:
      $ref: "#/types/aws:codepipeline/PipelineStage:PipelineStage"
    description: |
      A stage block. Stages are documented below.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - artifactStores
  - roleArn
  - stages
stateInputs:
  description: |
    Input properties used for looking up and filtering Pipeline resources.
  properties:
    arn:
      type: string
      description: |
        The codepipeline ARN.
    artifactStores:
      type: array
      items:
        $ref: "#/types/aws:codepipeline/PipelineArtifactStore:PipelineArtifactStore"
      description: |
        One or more artifact_store blocks. Artifact stores are documented below.
    name:
      type: string
      description: |
        The name of the pipeline.
      willReplaceOnChanges: true
    roleArn:
      type: string
      description: |
        A service role Amazon Resource Name (ARN) that grants AWS CodePipeline permission to make calls to AWS services on your behalf.
    stages:
      type: array
      items:
        $ref: "#/types/aws:codepipeline/PipelineStage:PipelineStage"
      description: |
        A stage block. Stages are documented below.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
