$token: aws:wafregional/RuleGroupActivatedRule:RuleGroupActivatedRule
properties:
  action:
    $ref: "#/types/aws:wafregional/RuleGroupActivatedRuleAction:RuleGroupActivatedRuleAction"
    description: |
      Specifies the action that CloudFront or AWS WAF takes when a web request matches the conditions in the rule.
    language:
      python:
        mapCase: false
  priority:
    type: integer
    description: |
      Specifies the order in which the rules are evaluated. Rules with a lower value are evaluated before rules with a higher value.
    language:
      python:
        mapCase: false
  ruleId:
    type: string
    description: |
      The ID of a `waf_regional_rule`
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      The rule type, either `REGULAR`, `RATE_BASED`, or `GROUP`. Defaults to `REGULAR`.
    language:
      python:
        mapCase: false
type: object
required:
  - action
  - priority
  - ruleId
