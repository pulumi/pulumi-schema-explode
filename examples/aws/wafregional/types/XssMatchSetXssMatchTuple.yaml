$token: aws:wafregional/XssMatchSetXssMatchTuple:XssMatchSetXssMatchTuple
properties:
  fieldToMatch:
    $ref: "#/types/aws:wafregional/XssMatchSetXssMatchTupleFieldToMatch:XssMatchSetXssMatchTupleFieldToMatch"
    description: |
      Specifies where in a web request to look for cross-site scripting attacks.
    language:
      python:
        mapCase: false
  textTransformation:
    type: string
    description: |
      Which text transformation, if any, to perform on the web request before inspecting the request for cross-site scripting attacks.
    language:
      python:
        mapCase: false
type: object
required:
  - fieldToMatch
  - textTransformation
