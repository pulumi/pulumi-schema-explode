$token: aws:wafregional/RuleGroupActivatedRuleAction:RuleGroupActivatedRuleAction
properties:
  type:
    type: string
    description: |
      The rule type, either `REGULAR`, `RATE_BASED`, or `GROUP`. Defaults to `REGULAR`.
    language:
      python:
        mapCase: false
type: object
required:
  - type
