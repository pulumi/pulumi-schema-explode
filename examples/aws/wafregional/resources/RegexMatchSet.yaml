$token: aws:wafregional/regexMatchSet:RegexMatchSet
properties:
  name:
    type: string
    description: |
      The name or description of the Regex Match Set.
  regexMatchTuples:
    type: array
    items:
      $ref: "#/types/aws:wafregional/RegexMatchSetRegexMatchTuple:RegexMatchSetRegexMatchTuple"
    description: |
      The regular expression pattern that you want AWS WAF to search for in web requests, the location in requests that you want AWS WAF to search, and other settings. See below.
required:
  - name
inputProperties:
  name:
    type: string
    description: |
      The name or description of the Regex Match Set.
    willReplaceOnChanges: true
  regexMatchTuples:
    type: array
    items:
      $ref: "#/types/aws:wafregional/RegexMatchSetRegexMatchTuple:RegexMatchSetRegexMatchTuple"
    description: |
      The regular expression pattern that you want AWS WAF to search for in web requests, the location in requests that you want AWS WAF to search, and other settings. See below.
stateInputs:
  description: |
    Input properties used for looking up and filtering RegexMatchSet resources.
  properties:
    name:
      type: string
      description: |
        The name or description of the Regex Match Set.
      willReplaceOnChanges: true
    regexMatchTuples:
      type: array
      items:
        $ref: "#/types/aws:wafregional/RegexMatchSetRegexMatchTuple:RegexMatchSetRegexMatchTuple"
      description: |
        The regular expression pattern that you want AWS WAF to search for in web requests, the location in requests that you want AWS WAF to search, and other settings. See below.
  type: object
