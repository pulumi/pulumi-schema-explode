$token: aws:wafregional/ipSet:IpSet
properties:
  arn:
    type: string
    description: |
      The ARN of the WAF IPSet.
  ipSetDescriptors:
    type: array
    items:
      $ref: "#/types/aws:wafregional/IpSetIpSetDescriptor:IpSetIpSetDescriptor"
    description: |
      One or more pairs specifying the IP address type (IPV4 or IPV6) and the IP address range (in CIDR notation) from which web requests originate.
  name:
    type: string
    description: |
      The name or description of the IPSet.
required:
  - arn
  - name
inputProperties:
  ipSetDescriptors:
    type: array
    items:
      $ref: "#/types/aws:wafregional/IpSetIpSetDescriptor:IpSetIpSetDescriptor"
    description: |
      One or more pairs specifying the IP address type (IPV4 or IPV6) and the IP address range (in CIDR notation) from which web requests originate.
  name:
    type: string
    description: |
      The name or description of the IPSet.
    willReplaceOnChanges: true
stateInputs:
  description: |
    Input properties used for looking up and filtering IpSet resources.
  properties:
    arn:
      type: string
      description: |
        The ARN of the WAF IPSet.
    ipSetDescriptors:
      type: array
      items:
        $ref: "#/types/aws:wafregional/IpSetIpSetDescriptor:IpSetIpSetDescriptor"
      description: |
        One or more pairs specifying the IP address type (IPV4 or IPV6) and the IP address range (in CIDR notation) from which web requests originate.
    name:
      type: string
      description: |
        The name or description of the IPSet.
      willReplaceOnChanges: true
  type: object
