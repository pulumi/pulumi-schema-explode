$token: aws:wafregional/geoMatchSet:GeoMatchSet
properties:
  geoMatchConstraints:
    type: array
    items:
      $ref: "#/types/aws:wafregional/GeoMatchSetGeoMatchConstraint:GeoMatchSetGeoMatchConstraint"
    description: |
      The Geo Match Constraint objects which contain the country that you want AWS WAF to search for.
  name:
    type: string
    description: |
      The name or description of the Geo Match Set.
required:
  - name
inputProperties:
  geoMatchConstraints:
    type: array
    items:
      $ref: "#/types/aws:wafregional/GeoMatchSetGeoMatchConstraint:GeoMatchSetGeoMatchConstraint"
    description: |
      The Geo Match Constraint objects which contain the country that you want AWS WAF to search for.
  name:
    type: string
    description: |
      The name or description of the Geo Match Set.
    willReplaceOnChanges: true
stateInputs:
  description: |
    Input properties used for looking up and filtering GeoMatchSet resources.
  properties:
    geoMatchConstraints:
      type: array
      items:
        $ref: "#/types/aws:wafregional/GeoMatchSetGeoMatchConstraint:GeoMatchSetGeoMatchConstraint"
      description: |
        The Geo Match Constraint objects which contain the country that you want AWS WAF to search for.
    name:
      type: string
      description: |
        The name or description of the Geo Match Set.
      willReplaceOnChanges: true
  type: object
