$token: aws:wafregional/rule:Rule
properties:
  arn:
    type: string
    description: |
      The ARN of the WAF Regional Rule.
  metricName:
    type: string
    description: |
      The name or description for the Amazon CloudWatch metric of this rule.
  name:
    type: string
    description: |
      The name or description of the rule.
  predicates:
    type: array
    items:
      $ref: "#/types/aws:wafregional/RulePredicate:RulePredicate"
    description: |
      The objects to include in a rule (documented below).
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - metricName
  - name
  - tagsAll
inputProperties:
  metricName:
    type: string
    description: |
      The name or description for the Amazon CloudWatch metric of this rule.
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      The name or description of the rule.
    willReplaceOnChanges: true
  predicates:
    type: array
    items:
      $ref: "#/types/aws:wafregional/RulePredicate:RulePredicate"
    description: |
      The objects to include in a rule (documented below).
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - metricName
stateInputs:
  description: |
    Input properties used for looking up and filtering Rule resources.
  properties:
    arn:
      type: string
      description: |
        The ARN of the WAF Regional Rule.
    metricName:
      type: string
      description: |
        The name or description for the Amazon CloudWatch metric of this rule.
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        The name or description of the rule.
      willReplaceOnChanges: true
    predicates:
      type: array
      items:
        $ref: "#/types/aws:wafregional/RulePredicate:RulePredicate"
      description: |
        The objects to include in a rule (documented below).
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
