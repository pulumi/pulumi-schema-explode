$token: aws:wafregional/sizeConstraintSet:SizeConstraintSet
properties:
  arn:
    type: string
  name:
    type: string
    description: |
      The name or description of the Size Constraint Set.
  sizeConstraints:
    type: array
    items:
      $ref: "#/types/aws:wafregional/SizeConstraintSetSizeConstraint:SizeConstraintSetSizeConstraint"
    description: |
      Specifies the parts of web requests that you want to inspect the size of.
required:
  - arn
  - name
inputProperties:
  name:
    type: string
    description: |
      The name or description of the Size Constraint Set.
    willReplaceOnChanges: true
  sizeConstraints:
    type: array
    items:
      $ref: "#/types/aws:wafregional/SizeConstraintSetSizeConstraint:SizeConstraintSetSizeConstraint"
    description: |
      Specifies the parts of web requests that you want to inspect the size of.
stateInputs:
  description: |
    Input properties used for looking up and filtering SizeConstraintSet resources.
  properties:
    arn:
      type: string
    name:
      type: string
      description: |
        The name or description of the Size Constraint Set.
      willReplaceOnChanges: true
    sizeConstraints:
      type: array
      items:
        $ref: "#/types/aws:wafregional/SizeConstraintSetSizeConstraint:SizeConstraintSetSizeConstraint"
      description: |
        Specifies the parts of web requests that you want to inspect the size of.
  type: object
