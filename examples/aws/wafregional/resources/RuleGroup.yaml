$token: aws:wafregional/ruleGroup:RuleGroup
properties:
  activatedRules:
    type: array
    items:
      $ref: "#/types/aws:wafregional/RuleGroupActivatedRule:RuleGroupActivatedRule"
    description: |
      A list of activated rules, see below
  arn:
    type: string
    description: |
      The ARN of the WAF Regional Rule Group.
  metricName:
    type: string
    description: |
      A friendly name for the metrics from the rule group
  name:
    type: string
    description: |
      A friendly name of the rule group
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - metricName
  - name
  - tagsAll
inputProperties:
  activatedRules:
    type: array
    items:
      $ref: "#/types/aws:wafregional/RuleGroupActivatedRule:RuleGroupActivatedRule"
    description: |
      A list of activated rules, see below
  metricName:
    type: string
    description: |
      A friendly name for the metrics from the rule group
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      A friendly name of the rule group
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - metricName
stateInputs:
  description: |
    Input properties used for looking up and filtering RuleGroup resources.
  properties:
    activatedRules:
      type: array
      items:
        $ref: "#/types/aws:wafregional/RuleGroupActivatedRule:RuleGroupActivatedRule"
      description: |
        A list of activated rules, see below
    arn:
      type: string
      description: |
        The ARN of the WAF Regional Rule Group.
    metricName:
      type: string
      description: |
        A friendly name for the metrics from the rule group
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        A friendly name of the rule group
      willReplaceOnChanges: true
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
