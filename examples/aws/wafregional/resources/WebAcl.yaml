$token: aws:wafregional/webAcl:WebAcl
properties:
  arn:
    type: string
    description: |
      Amazon Resource Name (ARN) of the WAF Regional WebACL.
  defaultAction:
    $ref: "#/types/aws:wafregional/WebAclDefaultAction:WebAclDefaultAction"
    description: |
      The action that you want AWS WAF Regional to take when a request doesn't match the criteria in any of the rules that are associated with the web ACL.
  loggingConfiguration:
    $ref: "#/types/aws:wafregional/WebAclLoggingConfiguration:WebAclLoggingConfiguration"
    description: |
      Configuration block to enable WAF logging. Detailed below.
  metricName:
    type: string
    description: |
      The name or description for the Amazon CloudWatch metric of this web ACL.
  name:
    type: string
    description: |
      The name or description of the web ACL.
  rules:
    type: array
    items:
      $ref: "#/types/aws:wafregional/WebAclRule:WebAclRule"
    description: |
      Set of configuration blocks containing rules for the web ACL. Detailed below.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - defaultAction
  - metricName
  - name
  - tagsAll
inputProperties:
  defaultAction:
    $ref: "#/types/aws:wafregional/WebAclDefaultAction:WebAclDefaultAction"
    description: |
      The action that you want AWS WAF Regional to take when a request doesn't match the criteria in any of the rules that are associated with the web ACL.
  loggingConfiguration:
    $ref: "#/types/aws:wafregional/WebAclLoggingConfiguration:WebAclLoggingConfiguration"
    description: |
      Configuration block to enable WAF logging. Detailed below.
  metricName:
    type: string
    description: |
      The name or description for the Amazon CloudWatch metric of this web ACL.
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      The name or description of the web ACL.
    willReplaceOnChanges: true
  rules:
    type: array
    items:
      $ref: "#/types/aws:wafregional/WebAclRule:WebAclRule"
    description: |
      Set of configuration blocks containing rules for the web ACL. Detailed below.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - defaultAction
  - metricName
stateInputs:
  description: |
    Input properties used for looking up and filtering WebAcl resources.
  properties:
    arn:
      type: string
      description: |
        Amazon Resource Name (ARN) of the WAF Regional WebACL.
    defaultAction:
      $ref: "#/types/aws:wafregional/WebAclDefaultAction:WebAclDefaultAction"
      description: |
        The action that you want AWS WAF Regional to take when a request doesn't match the criteria in any of the rules that are associated with the web ACL.
    loggingConfiguration:
      $ref: "#/types/aws:wafregional/WebAclLoggingConfiguration:WebAclLoggingConfiguration"
      description: |
        Configuration block to enable WAF logging. Detailed below.
    metricName:
      type: string
      description: |
        The name or description for the Amazon CloudWatch metric of this web ACL.
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        The name or description of the web ACL.
      willReplaceOnChanges: true
    rules:
      type: array
      items:
        $ref: "#/types/aws:wafregional/WebAclRule:WebAclRule"
      description: |
        Set of configuration blocks containing rules for the web ACL. Detailed below.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
