$token: aws:wafregional/byteMatchSet:ByteMatchSet
properties:
  byteMatchTuples:
    type: array
    items:
      $ref: "#/types/aws:wafregional/ByteMatchSetByteMatchTuple:ByteMatchSetByteMatchTuple"
    description: |
      Settings for the ByteMatchSet, such as the bytes (typically a string that corresponds with ASCII characters) that you want AWS WAF to search for in web requests. ByteMatchTuple documented below.
  name:
    type: string
    description: |
      The name or description of the ByteMatchSet.
required:
  - name
inputProperties:
  byteMatchTuples:
    type: array
    items:
      $ref: "#/types/aws:wafregional/ByteMatchSetByteMatchTuple:ByteMatchSetByteMatchTuple"
    description: |
      Settings for the ByteMatchSet, such as the bytes (typically a string that corresponds with ASCII characters) that you want AWS WAF to search for in web requests. ByteMatchTuple documented below.
  name:
    type: string
    description: |
      The name or description of the ByteMatchSet.
    willReplaceOnChanges: true
stateInputs:
  description: |
    Input properties used for looking up and filtering ByteMatchSet resources.
  properties:
    byteMatchTuples:
      type: array
      items:
        $ref: "#/types/aws:wafregional/ByteMatchSetByteMatchTuple:ByteMatchSetByteMatchTuple"
      description: |
        Settings for the ByteMatchSet, such as the bytes (typically a string that corresponds with ASCII characters) that you want AWS WAF to search for in web requests. ByteMatchTuple documented below.
    name:
      type: string
      description: |
        The name or description of the ByteMatchSet.
      willReplaceOnChanges: true
  type: object
