$token: aws:wafregional/getRateBasedMod:getRateBasedMod
inputs:
  description: |
    A collection of arguments for invoking getRateBasedMod.
  properties:
    name:
      type: string
      description: |
        Name of the WAF Regional rate based rule.
  type: object
  required:
    - name
outputs:
  description: |
    A collection of values returned by getRateBasedMod.
  properties:
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    name:
      type: string
  type: object
  required:
    - name
    - id
