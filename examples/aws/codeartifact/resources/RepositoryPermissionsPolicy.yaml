$token: aws:codeartifact/repositoryPermissionsPolicy:RepositoryPermissionsPolicy
properties:
  domain:
    type: string
    description: |
      The name of the domain on which to set the resource policy.
  domainOwner:
    type: string
    description: |
      The account number of the AWS account that owns the domain.
  policyDocument:
    type: string
    description: |
      A JSON policy string to be set as the access control resource policy on the provided domain.
  policyRevision:
    type: string
    description: |
      The current revision of the resource policy to be set. This revision is used for optimistic locking, which prevents others from overwriting your changes to the domain's resource policy.
  repository:
    type: string
    description: |
      The name of the repository to set the resource policy on.
  resourceArn:
    type: string
    description: |
      The ARN of the resource associated with the resource policy.
required:
  - domain
  - domainOwner
  - policyDocument
  - policyRevision
  - repository
  - resourceArn
inputProperties:
  domain:
    type: string
    description: |
      The name of the domain on which to set the resource policy.
    willReplaceOnChanges: true
  domainOwner:
    type: string
    description: |
      The account number of the AWS account that owns the domain.
    willReplaceOnChanges: true
  policyDocument:
    type: string
    description: |
      A JSON policy string to be set as the access control resource policy on the provided domain.
  policyRevision:
    type: string
    description: |
      The current revision of the resource policy to be set. This revision is used for optimistic locking, which prevents others from overwriting your changes to the domain's resource policy.
  repository:
    type: string
    description: |
      The name of the repository to set the resource policy on.
    willReplaceOnChanges: true
requiredInputs:
  - domain
  - policyDocument
  - repository
stateInputs:
  description: |
    Input properties used for looking up and filtering RepositoryPermissionsPolicy resources.
  properties:
    domain:
      type: string
      description: |
        The name of the domain on which to set the resource policy.
      willReplaceOnChanges: true
    domainOwner:
      type: string
      description: |
        The account number of the AWS account that owns the domain.
      willReplaceOnChanges: true
    policyDocument:
      type: string
      description: |
        A JSON policy string to be set as the access control resource policy on the provided domain.
    policyRevision:
      type: string
      description: |
        The current revision of the resource policy to be set. This revision is used for optimistic locking, which prevents others from overwriting your changes to the domain's resource policy.
    repository:
      type: string
      description: |
        The name of the repository to set the resource policy on.
      willReplaceOnChanges: true
    resourceArn:
      type: string
      description: |
        The ARN of the resource associated with the resource policy.
  type: object
