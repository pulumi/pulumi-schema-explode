$token: aws:codeartifact/getRepositoryEndpoint:getRepositoryEndpoint
inputs:
  description: |
    A collection of arguments for invoking getRepositoryEndpoint.
  properties:
    domain:
      type: string
      description: |
        Name of the domain that contains the repository.
    domainOwner:
      type: string
      description: |
        Account number of the AWS account that owns the domain.
    format:
      type: string
      description: |
        Which endpoint of a repository to return. A repository has one endpoint for each package format: `npm`, `pypi`, `maven`, and `nuget`.
    repository:
      type: string
      description: |
        Name of the repository.
  type: object
  required:
    - domain
    - format
    - repository
outputs:
  description: |
    A collection of values returned by getRepositoryEndpoint.
  properties:
    domain:
      type: string
    domainOwner:
      type: string
    format:
      type: string
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    repository:
      type: string
    repositoryEndpoint:
      type: string
      description: |
        URL of the returned endpoint.
  type: object
  required:
    - domain
    - domainOwner
    - format
    - repository
    - repositoryEndpoint
    - id
