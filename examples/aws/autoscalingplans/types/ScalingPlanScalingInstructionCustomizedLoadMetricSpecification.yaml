$token: aws:autoscalingplans/ScalingPlanScalingInstructionCustomizedLoadMetricSpecification:ScalingPlanScalingInstructionCustomizedLoadMetricSpecification
properties:
  dimensions:
    type: object
    additionalProperties:
      type: string
    description: |
      Dimensions of the metric.
    language:
      python:
        mapCase: false
  metricName:
    type: string
    description: |
      Name of the metric.
    language:
      python:
        mapCase: false
  namespace:
    type: string
    description: |
      Namespace of the metric.
    language:
      python:
        mapCase: false
  statistic:
    type: string
    description: |
      Statistic of the metric. Currently, the value must always be `Sum`.
    language:
      python:
        mapCase: false
  unit:
    type: string
    description: |
      Unit of the metric.
    language:
      python:
        mapCase: false
type: object
required:
  - metricName
  - namespace
  - statistic
