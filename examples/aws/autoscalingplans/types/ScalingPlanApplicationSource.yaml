$token: aws:autoscalingplans/ScalingPlanApplicationSource:ScalingPlanApplicationSource
properties:
  cloudformationStackArn:
    type: string
    description: |
      ARN of a AWS CloudFormation stack.
    language:
      python:
        mapCase: false
  tagFilters:
    type: array
    items:
      $ref: "#/types/aws:autoscalingplans/ScalingPlanApplicationSourceTagFilter:ScalingPlanApplicationSourceTagFilter"
    description: |
      Set of tags.
    language:
      python:
        mapCase: false
type: object
