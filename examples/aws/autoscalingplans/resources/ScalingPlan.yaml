$token: aws:autoscalingplans/scalingPlan:ScalingPlan
properties:
  applicationSource:
    $ref: "#/types/aws:autoscalingplans/ScalingPlanApplicationSource:ScalingPlanApplicationSource"
    description: |
      CloudFormation stack or set of tags. You can create one scaling plan per application source.
  name:
    type: string
    description: |
      Name of the scaling plan. Names cannot contain vertical bars, colons, or forward slashes.
  scalingInstructions:
    type: array
    items:
      $ref: "#/types/aws:autoscalingplans/ScalingPlanScalingInstruction:ScalingPlanScalingInstruction"
    description: |
      Scaling instructions. More details can be found in the [AWS Auto Scaling API Reference](https://docs.aws.amazon.com/autoscaling/plans/APIReference/API_ScalingInstruction.html).
  scalingPlanVersion:
    type: integer
    description: |
      The version number of the scaling plan. This value is always 1.
required:
  - applicationSource
  - name
  - scalingInstructions
  - scalingPlanVersion
inputProperties:
  applicationSource:
    $ref: "#/types/aws:autoscalingplans/ScalingPlanApplicationSource:ScalingPlanApplicationSource"
    description: |
      CloudFormation stack or set of tags. You can create one scaling plan per application source.
  name:
    type: string
    description: |
      Name of the scaling plan. Names cannot contain vertical bars, colons, or forward slashes.
    willReplaceOnChanges: true
  scalingInstructions:
    type: array
    items:
      $ref: "#/types/aws:autoscalingplans/ScalingPlanScalingInstruction:ScalingPlanScalingInstruction"
    description: |
      Scaling instructions. More details can be found in the [AWS Auto Scaling API Reference](https://docs.aws.amazon.com/autoscaling/plans/APIReference/API_ScalingInstruction.html).
requiredInputs:
  - applicationSource
  - scalingInstructions
stateInputs:
  description: |
    Input properties used for looking up and filtering ScalingPlan resources.
  properties:
    applicationSource:
      $ref: "#/types/aws:autoscalingplans/ScalingPlanApplicationSource:ScalingPlanApplicationSource"
      description: |
        CloudFormation stack or set of tags. You can create one scaling plan per application source.
    name:
      type: string
      description: |
        Name of the scaling plan. Names cannot contain vertical bars, colons, or forward slashes.
      willReplaceOnChanges: true
    scalingInstructions:
      type: array
      items:
        $ref: "#/types/aws:autoscalingplans/ScalingPlanScalingInstruction:ScalingPlanScalingInstruction"
      description: |
        Scaling instructions. More details can be found in the [AWS Auto Scaling API Reference](https://docs.aws.amazon.com/autoscaling/plans/APIReference/API_ScalingInstruction.html).
    scalingPlanVersion:
      type: integer
      description: |
        The version number of the scaling plan. This value is always 1.
  type: object
