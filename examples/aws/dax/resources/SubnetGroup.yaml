$token: aws:dax/subnetGroup:SubnetGroup
properties:
  description:
    type: string
    description: |
      A description of the subnet group.
  name:
    type: string
    description: |
      The name of the subnet group.
  subnetIds:
    type: array
    items:
      type: string
    description: |
      A list of VPC subnet IDs for the subnet group.
  vpcId:
    type: string
    description: |
      VPC ID of the subnet group.
required:
  - name
  - subnetIds
  - vpcId
inputProperties:
  description:
    type: string
    description: |
      A description of the subnet group.
  name:
    type: string
    description: |
      The name of the subnet group.
    willReplaceOnChanges: true
  subnetIds:
    type: array
    items:
      type: string
    description: |
      A list of VPC subnet IDs for the subnet group.
requiredInputs:
  - subnetIds
stateInputs:
  description: |
    Input properties used for looking up and filtering SubnetGroup resources.
  properties:
    description:
      type: string
      description: |
        A description of the subnet group.
    name:
      type: string
      description: |
        The name of the subnet group.
      willReplaceOnChanges: true
    subnetIds:
      type: array
      items:
        type: string
      description: |
        A list of VPC subnet IDs for the subnet group.
    vpcId:
      type: string
      description: |
        VPC ID of the subnet group.
  type: object
