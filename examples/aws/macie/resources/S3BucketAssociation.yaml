$token: aws:macie/s3BucketAssociation:S3BucketAssociation
properties:
  bucketName:
    type: string
    description: |
      The name of the S3 bucket that you want to associate with Amazon Macie.
  classificationType:
    $ref: "#/types/aws:macie/S3BucketAssociationClassificationType:S3BucketAssociationClassificationType"
    description: |
      The configuration of how Amazon Macie classifies the S3 objects.
  memberAccountId:
    type: string
    description: |
      The ID of the Amazon Macie member account whose S3 resources you want to associate with Macie. If `member_account_id` isn't specified, the action associates specified S3 resources with Macie for the current master account.
  prefix:
    type: string
    description: |
      Object key prefix identifying one or more S3 objects to which the association applies.
required:
  - bucketName
  - classificationType
inputProperties:
  bucketName:
    type: string
    description: |
      The name of the S3 bucket that you want to associate with Amazon Macie.
    willReplaceOnChanges: true
  classificationType:
    $ref: "#/types/aws:macie/S3BucketAssociationClassificationType:S3BucketAssociationClassificationType"
    description: |
      The configuration of how Amazon Macie classifies the S3 objects.
  memberAccountId:
    type: string
    description: |
      The ID of the Amazon Macie member account whose S3 resources you want to associate with Macie. If `member_account_id` isn't specified, the action associates specified S3 resources with Macie for the current master account.
    willReplaceOnChanges: true
  prefix:
    type: string
    description: |
      Object key prefix identifying one or more S3 objects to which the association applies.
    willReplaceOnChanges: true
requiredInputs:
  - bucketName
stateInputs:
  description: |
    Input properties used for looking up and filtering S3BucketAssociation resources.
  properties:
    bucketName:
      type: string
      description: |
        The name of the S3 bucket that you want to associate with Amazon Macie.
      willReplaceOnChanges: true
    classificationType:
      $ref: "#/types/aws:macie/S3BucketAssociationClassificationType:S3BucketAssociationClassificationType"
      description: |
        The configuration of how Amazon Macie classifies the S3 objects.
    memberAccountId:
      type: string
      description: |
        The ID of the Amazon Macie member account whose S3 resources you want to associate with Macie. If `member_account_id` isn't specified, the action associates specified S3 resources with Macie for the current master account.
      willReplaceOnChanges: true
    prefix:
      type: string
      description: |
        Object key prefix identifying one or more S3 objects to which the association applies.
      willReplaceOnChanges: true
  type: object
