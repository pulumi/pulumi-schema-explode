$token: aws:directoryservice/SharedDirectoryTarget:SharedDirectoryTarget
properties:
  id:
    type: string
    description: |
      Identifier of the directory consumer account.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  type:
    type: string
    description: |
      Type of identifier to be used in the `id` field. Valid value is `ACCOUNT`. Default is `ACCOUNT`.
    language:
      python:
        mapCase: false
type: object
required:
  - id
