$token: aws:directoryservice/getDirectoryConnectSetting:getDirectoryConnectSetting
properties:
  availabilityZones:
    type: array
    items:
      type: string
    language:
      python:
        mapCase: false
  connectIps:
    type: array
    items:
      type: string
    description: |
      IP addresses of the AD Connector servers.
    language:
      python:
        mapCase: false
  customerDnsIps:
    type: array
    items:
      type: string
    description: |
      DNS IP addresses of the domain to connect to.
    language:
      python:
        mapCase: false
  customerUsername:
    type: string
    description: |
      Username corresponding to the password provided.
    language:
      python:
        mapCase: false
  subnetIds:
    type: array
    items:
      type: string
    description: |
      Identifiers of the subnets for the connector servers (2 subnets in 2 different AZs).
    language:
      python:
        mapCase: false
  vpcId:
    type: string
    description: |
      ID of the VPC that the connector is in.
    language:
      python:
        mapCase: false
type: object
required:
  - availabilityZones
  - connectIps
  - customerDnsIps
  - customerUsername
  - subnetIds
  - vpcId
language:
  nodejs:
    requiredInputs: []
