$token: aws:directoryservice/getDirectoryRadiusSetting:getDirectoryRadiusSetting
properties:
  authenticationProtocol:
    type: string
    description: |
      The protocol specified for your RADIUS endpoints.
    language:
      python:
        mapCase: false
  displayLabel:
    type: string
    description: |
      Display label.
    language:
      python:
        mapCase: false
  radiusPort:
    type: integer
    description: |
      Port that your RADIUS server is using for communications.
    language:
      python:
        mapCase: false
  radiusRetries:
    type: integer
    description: |
      Maximum number of times that communication with the RADIUS server is attempted.
    language:
      python:
        mapCase: false
  radiusServers:
    type: array
    items:
      type: string
    description: |
      Set of strings that contains the fully qualified domain name (FQDN) or IP addresses of the RADIUS server endpoints, or the FQDN or IP addresses of your RADIUS server load balancer.
    language:
      python:
        mapCase: false
  radiusTimeout:
    type: integer
    description: |
      Amount of time, in seconds, to wait for the RADIUS server to respond.
    language:
      python:
        mapCase: false
  useSameUsername:
    type: boolean
    description: |
      Not currently used.
    language:
      python:
        mapCase: false
type: object
required:
  - authenticationProtocol
  - displayLabel
  - radiusPort
  - radiusRetries
  - radiusServers
  - radiusTimeout
  - useSameUsername
language:
  nodejs:
    requiredInputs: []
