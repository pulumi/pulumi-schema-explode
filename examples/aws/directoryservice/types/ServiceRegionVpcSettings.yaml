$token: aws:directoryservice/ServiceRegionVpcSettings:ServiceRegionVpcSettings
properties:
  subnetIds:
    type: array
    items:
      type: string
    description: |
      The identifiers of the subnets for the directory servers.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  vpcId:
    type: string
    description: |
      The identifier of the VPC in which to create the directory.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - subnetIds
  - vpcId
