$token: aws:directoryservice/serviceRegion:ServiceRegion
description: "Manages a replicated Region and directory for Multi-Region replication.

  Multi-Region replication is only supported for the Enterprise Edition of AWS Managed Microsoft AD.



  ## Import


  Replicated Regions can be imported using directory ID,Region name e.g.,


  ```sh

  \ $ pulumi import aws:directoryservice/serviceRegion:ServiceRegion example d-9267651497,us-east-2

  ```


  \ "
properties:
  desiredNumberOfDomainControllers:
    type: integer
    description: |
      The number of domain controllers desired in the replicated directory. Minimum value of `2`.
  directoryId:
    type: string
    description: |
      The identifier of the directory to which you want to add Region replication.
  regionName:
    type: string
    description: |
      The name of the Region where you want to add domain controllers for replication.
  tags:
    type: object
    additionalProperties:
      type: string
  tagsAll:
    type: object
    additionalProperties:
      type: string
  vpcSettings:
    $ref: "#/types/aws:directoryservice/ServiceRegionVpcSettings:ServiceRegionVpcSettings"
    description: |
      VPC information in the replicated Region. Detailed below.
required:
  - desiredNumberOfDomainControllers
  - directoryId
  - regionName
  - tagsAll
  - vpcSettings
inputProperties:
  desiredNumberOfDomainControllers:
    type: integer
    description: |
      The number of domain controllers desired in the replicated directory. Minimum value of `2`.
  directoryId:
    type: string
    description: |
      The identifier of the directory to which you want to add Region replication.
    willReplaceOnChanges: true
  regionName:
    type: string
    description: |
      The name of the Region where you want to add domain controllers for replication.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
  vpcSettings:
    $ref: "#/types/aws:directoryservice/ServiceRegionVpcSettings:ServiceRegionVpcSettings"
    description: |
      VPC information in the replicated Region. Detailed below.
    willReplaceOnChanges: true
requiredInputs:
  - directoryId
  - regionName
  - vpcSettings
stateInputs:
  description: |
    Input properties used for looking up and filtering ServiceRegion resources.
  properties:
    desiredNumberOfDomainControllers:
      type: integer
      description: |
        The number of domain controllers desired in the replicated directory. Minimum value of `2`.
    directoryId:
      type: string
      description: |
        The identifier of the directory to which you want to add Region replication.
      willReplaceOnChanges: true
    regionName:
      type: string
      description: |
        The name of the Region where you want to add domain controllers for replication.
      willReplaceOnChanges: true
    tags:
      type: object
      additionalProperties:
        type: string
    tagsAll:
      type: object
      additionalProperties:
        type: string
    vpcSettings:
      $ref: "#/types/aws:directoryservice/ServiceRegionVpcSettings:ServiceRegionVpcSettings"
      description: |
        VPC information in the replicated Region. Detailed below.
      willReplaceOnChanges: true
  type: object
