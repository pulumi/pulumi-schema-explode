$token: aws:directoryservice/conditionalForwader:ConditionalForwader
properties:
  directoryId:
    type: string
    description: |
      ID of directory.
  dnsIps:
    type: array
    items:
      type: string
    description: |
      A list of forwarder IP addresses.
  remoteDomainName:
    type: string
    description: |
      The fully qualified domain name of the remote domain for which forwarders will be used.
required:
  - directoryId
  - dnsIps
  - remoteDomainName
inputProperties:
  directoryId:
    type: string
    description: |
      ID of directory.
    willReplaceOnChanges: true
  dnsIps:
    type: array
    items:
      type: string
    description: |
      A list of forwarder IP addresses.
  remoteDomainName:
    type: string
    description: |
      The fully qualified domain name of the remote domain for which forwarders will be used.
    willReplaceOnChanges: true
requiredInputs:
  - directoryId
  - dnsIps
  - remoteDomainName
stateInputs:
  description: |
    Input properties used for looking up and filtering ConditionalForwader resources.
  properties:
    directoryId:
      type: string
      description: |
        ID of directory.
      willReplaceOnChanges: true
    dnsIps:
      type: array
      items:
        type: string
      description: |
        A list of forwarder IP addresses.
    remoteDomainName:
      type: string
      description: |
        The fully qualified domain name of the remote domain for which forwarders will be used.
      willReplaceOnChanges: true
  type: object
