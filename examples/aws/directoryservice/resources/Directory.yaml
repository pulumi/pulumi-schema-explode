$token: aws:directoryservice/directory:Directory
properties:
  accessUrl:
    type: string
    description: |
      The access URL for the directory, such as `http://alias.awsapps.com`.
  alias:
    type: string
    description: |
      The alias for the directory (must be unique amongst all aliases in AWS). Required for `enable_sso`.
  connectSettings:
    $ref: "#/types/aws:directoryservice/DirectoryConnectSettings:DirectoryConnectSettings"
    description: |
      Connector related information about the directory. Fields documented below.
  description:
    type: string
    description: |
      A textual description for the directory.
  desiredNumberOfDomainControllers:
    type: integer
    description: |
      The number of domain controllers desired in the directory. Minimum value of `2`. Scaling of domain controllers is only supported for `MicrosoftAD` directories.
  dnsIpAddresses:
    type: array
    items:
      type: string
    description: |
      A list of IP addresses of the DNS servers for the directory or connector.
  edition:
    type: string
    description: |
      The MicrosoftAD edition (`Standard` or `Enterprise`). Defaults to `Enterprise` (applies to MicrosoftAD type only).
  enableSso:
    type: boolean
    description: |
      Whether to enable single-sign on for the directory. Requires `alias`. Defaults to `false`.
  name:
    type: string
    description: |
      The fully qualified name for the directory, such as `corp.example.com`
  password:
    type: string
    description: |
      The password for the directory administrator or connector user.
  securityGroupId:
    type: string
    description: |
      The ID of the security group created by the directory.
  shortName:
    type: string
    description: |
      The short name of the directory, such as `CORP`.
  size:
    type: string
    description: |
      The size of the directory (`Small` or `Large` are accepted values).
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type:
    type: string
    description: |
      The directory type (`SimpleAD`, `ADConnector` or `MicrosoftAD` are accepted values). Defaults to `SimpleAD`.
  vpcSettings:
    $ref: "#/types/aws:directoryservice/DirectoryVpcSettings:DirectoryVpcSettings"
    description: |
      VPC related information about the directory. Fields documented below.
required:
  - accessUrl
  - alias
  - desiredNumberOfDomainControllers
  - dnsIpAddresses
  - edition
  - name
  - password
  - securityGroupId
  - shortName
  - size
  - tagsAll
inputProperties:
  alias:
    type: string
    description: |
      The alias for the directory (must be unique amongst all aliases in AWS). Required for `enable_sso`.
    willReplaceOnChanges: true
  connectSettings:
    $ref: "#/types/aws:directoryservice/DirectoryConnectSettings:DirectoryConnectSettings"
    description: |
      Connector related information about the directory. Fields documented below.
    willReplaceOnChanges: true
  description:
    type: string
    description: |
      A textual description for the directory.
    willReplaceOnChanges: true
  desiredNumberOfDomainControllers:
    type: integer
    description: |
      The number of domain controllers desired in the directory. Minimum value of `2`. Scaling of domain controllers is only supported for `MicrosoftAD` directories.
  edition:
    type: string
    description: |
      The MicrosoftAD edition (`Standard` or `Enterprise`). Defaults to `Enterprise` (applies to MicrosoftAD type only).
    willReplaceOnChanges: true
  enableSso:
    type: boolean
    description: |
      Whether to enable single-sign on for the directory. Requires `alias`. Defaults to `false`.
  name:
    type: string
    description: |
      The fully qualified name for the directory, such as `corp.example.com`
    willReplaceOnChanges: true
  password:
    type: string
    description: |
      The password for the directory administrator or connector user.
    willReplaceOnChanges: true
  shortName:
    type: string
    description: |
      The short name of the directory, such as `CORP`.
    willReplaceOnChanges: true
  size:
    type: string
    description: |
      The size of the directory (`Small` or `Large` are accepted values).
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  type:
    type: string
    description: |
      The directory type (`SimpleAD`, `ADConnector` or `MicrosoftAD` are accepted values). Defaults to `SimpleAD`.
    willReplaceOnChanges: true
  vpcSettings:
    $ref: "#/types/aws:directoryservice/DirectoryVpcSettings:DirectoryVpcSettings"
    description: |
      VPC related information about the directory. Fields documented below.
    willReplaceOnChanges: true
requiredInputs:
  - name
  - password
stateInputs:
  description: |
    Input properties used for looking up and filtering Directory resources.
  properties:
    accessUrl:
      type: string
      description: |
        The access URL for the directory, such as `http://alias.awsapps.com`.
    alias:
      type: string
      description: |
        The alias for the directory (must be unique amongst all aliases in AWS). Required for `enable_sso`.
      willReplaceOnChanges: true
    connectSettings:
      $ref: "#/types/aws:directoryservice/DirectoryConnectSettings:DirectoryConnectSettings"
      description: |
        Connector related information about the directory. Fields documented below.
      willReplaceOnChanges: true
    description:
      type: string
      description: |
        A textual description for the directory.
      willReplaceOnChanges: true
    desiredNumberOfDomainControllers:
      type: integer
      description: |
        The number of domain controllers desired in the directory. Minimum value of `2`. Scaling of domain controllers is only supported for `MicrosoftAD` directories.
    dnsIpAddresses:
      type: array
      items:
        type: string
      description: |
        A list of IP addresses of the DNS servers for the directory or connector.
    edition:
      type: string
      description: |
        The MicrosoftAD edition (`Standard` or `Enterprise`). Defaults to `Enterprise` (applies to MicrosoftAD type only).
      willReplaceOnChanges: true
    enableSso:
      type: boolean
      description: |
        Whether to enable single-sign on for the directory. Requires `alias`. Defaults to `false`.
    name:
      type: string
      description: |
        The fully qualified name for the directory, such as `corp.example.com`
      willReplaceOnChanges: true
    password:
      type: string
      description: |
        The password for the directory administrator or connector user.
      willReplaceOnChanges: true
    securityGroupId:
      type: string
      description: |
        The ID of the security group created by the directory.
    shortName:
      type: string
      description: |
        The short name of the directory, such as `CORP`.
      willReplaceOnChanges: true
    size:
      type: string
      description: |
        The size of the directory (`Small` or `Large` are accepted values).
      willReplaceOnChanges: true
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    type:
      type: string
      description: |
        The directory type (`SimpleAD`, `ADConnector` or `MicrosoftAD` are accepted values). Defaults to `SimpleAD`.
      willReplaceOnChanges: true
    vpcSettings:
      $ref: "#/types/aws:directoryservice/DirectoryVpcSettings:DirectoryVpcSettings"
      description: |
        VPC related information about the directory. Fields documented below.
      willReplaceOnChanges: true
  type: object
