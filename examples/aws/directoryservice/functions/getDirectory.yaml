$token: aws:directoryservice/getDirectory:getDirectory
inputs:
  description: |
    A collection of arguments for invoking getDirectory.
  properties:
    directoryId:
      type: string
      description: |
        ID of the directory.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the directory/connector.
  type: object
  required:
    - directoryId
outputs:
  description: |
    A collection of values returned by getDirectory.
  properties:
    accessUrl:
      type: string
      description: |
        Access URL for the directory/connector, such as http://alias.awsapps.com.
    alias:
      type: string
      description: |
        Alias for the directory/connector, such as `d-991708b282.awsapps.com`.
    connectSettings:
      type: array
      items:
        $ref: "#/types/aws:directoryservice/getDirectoryConnectSetting:getDirectoryConnectSetting"
    description:
      type: string
      description: |
        Textual description for the directory/connector.
    directoryId:
      type: string
    dnsIpAddresses:
      type: array
      items:
        type: string
      description: |
        List of IP addresses of the DNS servers for the directory/connector.
    edition:
      type: string
      description: |
        (for `MicrosoftAD`) Microsoft AD edition (`Standard` or `Enterprise`).
    enableSso:
      type: boolean
      description: |
        Directory/connector single-sign on status.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    name:
      type: string
      description: |
        Fully qualified name for the directory/connector.
    radiusSettings:
      type: array
      items:
        $ref: "#/types/aws:directoryservice/getDirectoryRadiusSetting:getDirectoryRadiusSetting"
    securityGroupId:
      type: string
      description: |
        ID of the security group created by the directory/connector.
    shortName:
      type: string
      description: |
        Short name of the directory/connector, such as `CORP`.
    size:
      type: string
      description: |
        (for `SimpleAD` and `ADConnector`) Size of the directory/connector (`Small` or `Large`).
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the directory/connector.
    type:
      type: string
      description: |
        Directory type (`SimpleAD`, `ADConnector` or `MicrosoftAD`).
    vpcSettings:
      type: array
      items:
        $ref: "#/types/aws:directoryservice/getDirectoryVpcSetting:getDirectoryVpcSetting"
  type: object
  required:
    - accessUrl
    - alias
    - connectSettings
    - description
    - directoryId
    - dnsIpAddresses
    - edition
    - enableSso
    - name
    - radiusSettings
    - securityGroupId
    - shortName
    - size
    - tags
    - type
    - vpcSettings
    - id
