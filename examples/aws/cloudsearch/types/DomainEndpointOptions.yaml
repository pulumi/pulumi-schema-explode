$token: aws:cloudsearch/DomainEndpointOptions:DomainEndpointOptions
properties:
  enforceHttps:
    type: boolean
    description: |
      Enables or disables the requirement that all requests to the domain arrive over HTTPS.
    language:
      python:
        mapCase: false
  tlsSecurityPolicy:
    type: string
    description: |
      The minimum required TLS version. See the [AWS documentation](https://docs.aws.amazon.com/cloudsearch/latest/developerguide/API_DomainEndpointOptions.html) for valid values.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - enforceHttps
      - tlsSecurityPolicy
