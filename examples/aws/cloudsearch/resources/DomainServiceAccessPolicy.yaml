$token: aws:cloudsearch/domainServiceAccessPolicy:DomainServiceAccessPolicy
properties:
  accessPolicy:
    type: string
    description: |
      The access rules you want to configure. These rules replace any existing rules. See the [AWS documentation](https://docs.aws.amazon.com/cloudsearch/latest/developerguide/configuring-access.html) for details.
  domainName:
    type: string
    description: |
      The CloudSearch domain name the policy applies to.
required:
  - accessPolicy
  - domainName
inputProperties:
  accessPolicy:
    type: string
    description: |
      The access rules you want to configure. These rules replace any existing rules. See the [AWS documentation](https://docs.aws.amazon.com/cloudsearch/latest/developerguide/configuring-access.html) for details.
  domainName:
    type: string
    description: |
      The CloudSearch domain name the policy applies to.
requiredInputs:
  - accessPolicy
  - domainName
stateInputs:
  description: |
    Input properties used for looking up and filtering DomainServiceAccessPolicy resources.
  properties:
    accessPolicy:
      type: string
      description: |
        The access rules you want to configure. These rules replace any existing rules. See the [AWS documentation](https://docs.aws.amazon.com/cloudsearch/latest/developerguide/configuring-access.html) for details.
    domainName:
      type: string
      description: |
        The CloudSearch domain name the policy applies to.
  type: object
