$token: aws:cloudsearch/domain:Domain
properties:
  arn:
    type: string
    description: |
      The domain's ARN.
  documentServiceEndpoint:
    type: string
    description: |
      The service endpoint for updating documents in a search domain.
  domainId:
    type: string
    description: |
      An internally generated unique identifier for the domain.
  endpointOptions:
    $ref: "#/types/aws:cloudsearch/DomainEndpointOptions:DomainEndpointOptions"
    description: |
      Domain endpoint options. Documented below.
  indexFields:
    type: array
    items:
      $ref: "#/types/aws:cloudsearch/DomainIndexField:DomainIndexField"
    description: |
      The index fields for documents added to the domain. Documented below.
  multiAz:
    type: boolean
    description: |
      Whether or not to maintain extra instances for the domain in a second Availability Zone to ensure high availability.
  name:
    type: string
    description: |
      A unique name for the field. Field names must begin with a letter and be at least 3 and no more than 64 characters long. The allowed characters are: `a`-`z` (lower-case letters), `0`-`9`, and `_` (underscore). The name `score` is reserved and cannot be used as a field name.
  scalingParameters:
    $ref: "#/types/aws:cloudsearch/DomainScalingParameters:DomainScalingParameters"
    description: |
      Domain scaling parameters. Documented below.
  searchServiceEndpoint:
    type: string
    description: |
      The service endpoint for requesting search results from a search domain.
required:
  - arn
  - documentServiceEndpoint
  - domainId
  - endpointOptions
  - multiAz
  - name
  - scalingParameters
  - searchServiceEndpoint
inputProperties:
  endpointOptions:
    $ref: "#/types/aws:cloudsearch/DomainEndpointOptions:DomainEndpointOptions"
    description: |
      Domain endpoint options. Documented below.
  indexFields:
    type: array
    items:
      $ref: "#/types/aws:cloudsearch/DomainIndexField:DomainIndexField"
    description: |
      The index fields for documents added to the domain. Documented below.
  multiAz:
    type: boolean
    description: |
      Whether or not to maintain extra instances for the domain in a second Availability Zone to ensure high availability.
  name:
    type: string
    description: |
      A unique name for the field. Field names must begin with a letter and be at least 3 and no more than 64 characters long. The allowed characters are: `a`-`z` (lower-case letters), `0`-`9`, and `_` (underscore). The name `score` is reserved and cannot be used as a field name.
    willReplaceOnChanges: true
  scalingParameters:
    $ref: "#/types/aws:cloudsearch/DomainScalingParameters:DomainScalingParameters"
    description: |
      Domain scaling parameters. Documented below.
stateInputs:
  description: |
    Input properties used for looking up and filtering Domain resources.
  properties:
    arn:
      type: string
      description: |
        The domain's ARN.
    documentServiceEndpoint:
      type: string
      description: |
        The service endpoint for updating documents in a search domain.
    domainId:
      type: string
      description: |
        An internally generated unique identifier for the domain.
    endpointOptions:
      $ref: "#/types/aws:cloudsearch/DomainEndpointOptions:DomainEndpointOptions"
      description: |
        Domain endpoint options. Documented below.
    indexFields:
      type: array
      items:
        $ref: "#/types/aws:cloudsearch/DomainIndexField:DomainIndexField"
      description: |
        The index fields for documents added to the domain. Documented below.
    multiAz:
      type: boolean
      description: |
        Whether or not to maintain extra instances for the domain in a second Availability Zone to ensure high availability.
    name:
      type: string
      description: |
        A unique name for the field. Field names must begin with a letter and be at least 3 and no more than 64 characters long. The allowed characters are: `a`-`z` (lower-case letters), `0`-`9`, and `_` (underscore). The name `score` is reserved and cannot be used as a field name.
      willReplaceOnChanges: true
    scalingParameters:
      $ref: "#/types/aws:cloudsearch/DomainScalingParameters:DomainScalingParameters"
      description: |
        Domain scaling parameters. Documented below.
    searchServiceEndpoint:
      type: string
      description: |
        The service endpoint for requesting search results from a search domain.
  type: object
