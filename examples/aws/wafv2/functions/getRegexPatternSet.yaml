$token: aws:wafv2/getRegexPatternSet:getRegexPatternSet
inputs:
  description: |
    A collection of arguments for invoking getRegexPatternSet.
  properties:
    name:
      type: string
      description: |
        Name of the WAFv2 Regex Pattern Set.
    scope:
      type: string
      description: |
        Specifies whether this is for an AWS CloudFront distribution or for a regional application. Valid values are `CLOUDFRONT` or `REGIONAL`. To work with CloudFront, you must also specify the region `us-east-1` (N. Virginia) on the AWS provider.
  type: object
  required:
    - name
    - scope
outputs:
  description: |
    A collection of values returned by getRegexPatternSet.
  properties:
    arn:
      type: string
      description: |
        ARN of the entity.
    description:
      type: string
      description: |
        Description of the set that helps with identification.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    name:
      type: string
    regularExpressions:
      type: array
      items:
        $ref: "#/types/aws:wafv2/getRegexPatternSetRegularExpression:getRegexPatternSetRegularExpression"
      description: |
        One or more blocks of regular expression patterns that AWS WAF is searching for. See Regular Expression below for details.
    scope:
      type: string
  type: object
  required:
    - arn
    - description
    - name
    - regularExpressions
    - scope
    - id
