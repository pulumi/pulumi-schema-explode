$token: aws:wafv2/webAclLoggingConfiguration:WebAclLoggingConfiguration
properties:
  logDestinationConfigs:
    type: array
    items:
      type: string
    description: |
      The Amazon Kinesis Data Firehose, Cloudwatch Log log group, or S3 bucket Amazon Resource Names (ARNs) that you want to associate with the web ACL.
  loggingFilter:
    $ref: "#/types/aws:wafv2/WebAclLoggingConfigurationLoggingFilter:WebAclLoggingConfigurationLoggingFilter"
    description: |
      A configuration block that specifies which web requests are kept in the logs and which are dropped. You can filter on the rule action and on the web request labels that were applied by matching rules during web ACL evaluation. See Logging Filter below for more details.
  redactedFields:
    type: array
    items:
      $ref: "#/types/aws:wafv2/WebAclLoggingConfigurationRedactedField:WebAclLoggingConfigurationRedactedField"
    description: |
      The parts of the request that you want to keep out of the logs. Up to 100 `redacted_fields` blocks are supported. See Redacted Fields below for more details.
  resourceArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the web ACL that you want to associate with `log_destination_configs`.
required:
  - logDestinationConfigs
  - resourceArn
inputProperties:
  logDestinationConfigs:
    type: array
    items:
      type: string
    description: |
      The Amazon Kinesis Data Firehose, Cloudwatch Log log group, or S3 bucket Amazon Resource Names (ARNs) that you want to associate with the web ACL.
    willReplaceOnChanges: true
  loggingFilter:
    $ref: "#/types/aws:wafv2/WebAclLoggingConfigurationLoggingFilter:WebAclLoggingConfigurationLoggingFilter"
    description: |
      A configuration block that specifies which web requests are kept in the logs and which are dropped. You can filter on the rule action and on the web request labels that were applied by matching rules during web ACL evaluation. See Logging Filter below for more details.
  redactedFields:
    type: array
    items:
      $ref: "#/types/aws:wafv2/WebAclLoggingConfigurationRedactedField:WebAclLoggingConfigurationRedactedField"
    description: |
      The parts of the request that you want to keep out of the logs. Up to 100 `redacted_fields` blocks are supported. See Redacted Fields below for more details.
  resourceArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the web ACL that you want to associate with `log_destination_configs`.
    willReplaceOnChanges: true
requiredInputs:
  - logDestinationConfigs
  - resourceArn
stateInputs:
  description: |
    Input properties used for looking up and filtering WebAclLoggingConfiguration resources.
  properties:
    logDestinationConfigs:
      type: array
      items:
        type: string
      description: |
        The Amazon Kinesis Data Firehose, Cloudwatch Log log group, or S3 bucket Amazon Resource Names (ARNs) that you want to associate with the web ACL.
      willReplaceOnChanges: true
    loggingFilter:
      $ref: "#/types/aws:wafv2/WebAclLoggingConfigurationLoggingFilter:WebAclLoggingConfigurationLoggingFilter"
      description: |
        A configuration block that specifies which web requests are kept in the logs and which are dropped. You can filter on the rule action and on the web request labels that were applied by matching rules during web ACL evaluation. See Logging Filter below for more details.
    redactedFields:
      type: array
      items:
        $ref: "#/types/aws:wafv2/WebAclLoggingConfigurationRedactedField:WebAclLoggingConfigurationRedactedField"
      description: |
        The parts of the request that you want to keep out of the logs. Up to 100 `redacted_fields` blocks are supported. See Redacted Fields below for more details.
    resourceArn:
      type: string
      description: |
        The Amazon Resource Name (ARN) of the web ACL that you want to associate with `log_destination_configs`.
      willReplaceOnChanges: true
  type: object
