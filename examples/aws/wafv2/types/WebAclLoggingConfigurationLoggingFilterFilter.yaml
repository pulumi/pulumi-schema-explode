$token: aws:wafv2/WebAclLoggingConfigurationLoggingFilterFilter:WebAclLoggingConfigurationLoggingFilterFilter
properties:
  behavior:
    type: string
    description: |
      How to handle logs that satisfy the filter's conditions and requirement. Valid values: `KEEP` or `DROP`.
    language:
      python:
        mapCase: false
  conditions:
    type: array
    items:
      $ref: "#/types/aws:wafv2/WebAclLoggingConfigurationLoggingFilterFilterCondition:WebAclLoggingConfigurationLoggingFilterFilterCondition"
    description: |
      Match condition(s) for the filter. See Condition below for more details.
    language:
      python:
        mapCase: false
  requirement:
    type: string
    description: |
      Logic to apply to the filtering conditions. You can specify that, in order to satisfy the filter, a log must match all conditions or must match at least one condition. Valid values: `MEETS_ALL` or `MEETS_ANY`.
    language:
      python:
        mapCase: false
type: object
required:
  - behavior
  - conditions
  - requirement
