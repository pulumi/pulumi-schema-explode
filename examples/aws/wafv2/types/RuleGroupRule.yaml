$token: aws:wafv2/RuleGroupRule:RuleGroupRule
properties:
  action:
    $ref: "#/types/aws:wafv2/RuleGroupRuleAction:RuleGroupRuleAction"
    description: |
      The action that AWS WAF should take on a web request when it matches the rule's statement. Settings at the `aws.wafv2.WebAcl` level can override the rule action setting. See Action below for details.
    language:
      python:
        mapCase: false
  name:
    type: string
    description: |
      A friendly name of the rule.
    language:
      python:
        mapCase: false
  priority:
    type: integer
    description: |
      If you define more than one Rule in a WebACL, AWS WAF evaluates each request against the `rules` in order based on the value of `priority`. AWS WAF processes rules with lower priority first.
    language:
      python:
        mapCase: false
  ruleLabels:
    type: array
    items:
      $ref: "#/types/aws:wafv2/RuleGroupRuleRuleLabel:RuleGroupRuleRuleLabel"
    description: |
      Labels to apply to web requests that match the rule match statement. See Rule Label below for details.
    language:
      python:
        mapCase: false
  statement:
    $ref: "#/types/aws:wafv2/RuleGroupRuleStatement:RuleGroupRuleStatement"
    description: |
      The AWS WAF processing statement for the rule, for example `byte_match_statement` or `geo_match_statement`. See Statement below for details.
    language:
      python:
        mapCase: false
  visibilityConfig:
    $ref: "#/types/aws:wafv2/RuleGroupRuleVisibilityConfig:RuleGroupRuleVisibilityConfig"
    description: |
      Defines and enables Amazon CloudWatch metrics and web request sample collection. See Visibility Configuration below for details.
    language:
      python:
        mapCase: false
type: object
required:
  - action
  - name
  - priority
  - statement
  - visibilityConfig
