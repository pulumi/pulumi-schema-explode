$token: aws:wafv2/RuleGroupRuleActionBlockCustomResponse:RuleGroupRuleActionBlockCustomResponse
properties:
  customResponseBodyKey:
    type: string
    description: |
      References the response body that you want AWS WAF to return to the web request client. This must reference a `key` defined in a `custom_response_body` block of this resource.
    language:
      python:
        mapCase: false
  responseCode:
    type: integer
    description: |
      The HTTP status code to return to the client.
    language:
      python:
        mapCase: false
  responseHeaders:
    type: array
    items:
      $ref: "#/types/aws:wafv2/RuleGroupRuleActionBlockCustomResponseResponseHeader:RuleGroupRuleActionBlockCustomResponseResponseHeader"
    description: |
      The `response_header` blocks used to define the HTTP response headers added to the response. See Custom HTTP Header below for details.
    language:
      python:
        mapCase: false
type: object
required:
  - responseCode
