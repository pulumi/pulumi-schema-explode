$token: aws:wafv2/WebAclRuleOverrideAction:WebAclRuleOverrideAction
properties:
  count:
    $ref: "#/types/aws:wafv2/WebAclRuleOverrideActionCount:WebAclRuleOverrideActionCount"
    description: |
      Override the rule action setting to count (i.e., only count matches). Configured as an empty block `{}`.
    language:
      python:
        mapCase: false
  none:
    $ref: "#/types/aws:wafv2/WebAclRuleOverrideActionNone:WebAclRuleOverrideActionNone"
    description: |
      Don't override the rule action setting. Configured as an empty block `{}`.
    language:
      python:
        mapCase: false
type: object
