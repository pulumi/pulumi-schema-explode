$token: aws:wafv2/WebAclCustomResponseBody:WebAclCustomResponseBody
properties:
  content:
    type: string
    description: |
      Payload of the custom response.
    language:
      python:
        mapCase: false
  contentType:
    type: string
    description: |
      Type of content in the payload that you are defining in the `content` argument. Valid values are `TEXT_PLAIN`, `TEXT_HTML`, or `APPLICATION_JSON`.
    language:
      python:
        mapCase: false
  key:
    type: string
    description: |
      Unique key identifying the custom response body. This is referenced by the `custom_response_body_key` argument in the Custom Response block.
    language:
      python:
        mapCase: false
type: object
required:
  - content
  - contentType
  - key
