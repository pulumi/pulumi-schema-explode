$token: aws:mediaconvert/QueueReservationPlanSettings:QueueReservationPlanSettings
properties:
  commitment:
    type: string
    description: |
      The length of the term of your reserved queue pricing plan commitment. Valid value is `ONE_YEAR`.
    language:
      python:
        mapCase: false
  renewalType:
    type: string
    description: |
      Specifies whether the term of your reserved queue pricing plan. Valid values are `AUTO_RENEW` or `EXPIRE`.
    language:
      python:
        mapCase: false
  reservedSlots:
    type: integer
    description: |
      Specifies the number of reserved transcode slots (RTS) for queue.
    language:
      python:
        mapCase: false
type: object
required:
  - commitment
  - renewalType
  - reservedSlots
