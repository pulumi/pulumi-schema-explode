$token: aws:mediaconvert/queue:Queue
properties:
  arn:
    type: string
    description: |
      The Arn of the queue
  description:
    type: string
    description: |
      A description of the queue
  name:
    type: string
    description: |
      A unique identifier describing the queue
  pricingPlan:
    type: string
    description: |
      Specifies whether the pricing plan for the queue is on-demand or reserved. Valid values are `ON_DEMAND` or `RESERVED`. Default to `ON_DEMAND`.
  reservationPlanSettings:
    $ref: "#/types/aws:mediaconvert/QueueReservationPlanSettings:QueueReservationPlanSettings"
    description: |
      A detail pricing plan of the  reserved queue. See below.
  status:
    type: string
    description: |
      A status of the queue. Valid values are `ACTIVE` or `RESERVED`. Default to `PAUSED`.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - name
  - reservationPlanSettings
  - tagsAll
inputProperties:
  description:
    type: string
    description: |
      A description of the queue
  name:
    type: string
    description: |
      A unique identifier describing the queue
    willReplaceOnChanges: true
  pricingPlan:
    type: string
    description: |
      Specifies whether the pricing plan for the queue is on-demand or reserved. Valid values are `ON_DEMAND` or `RESERVED`. Default to `ON_DEMAND`.
    willReplaceOnChanges: true
  reservationPlanSettings:
    $ref: "#/types/aws:mediaconvert/QueueReservationPlanSettings:QueueReservationPlanSettings"
    description: |
      A detail pricing plan of the  reserved queue. See below.
  status:
    type: string
    description: |
      A status of the queue. Valid values are `ACTIVE` or `RESERVED`. Default to `PAUSED`.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
stateInputs:
  description: |
    Input properties used for looking up and filtering Queue resources.
  properties:
    arn:
      type: string
      description: |
        The Arn of the queue
    description:
      type: string
      description: |
        A description of the queue
    name:
      type: string
      description: |
        A unique identifier describing the queue
      willReplaceOnChanges: true
    pricingPlan:
      type: string
      description: |
        Specifies whether the pricing plan for the queue is on-demand or reserved. Valid values are `ON_DEMAND` or `RESERVED`. Default to `ON_DEMAND`.
      willReplaceOnChanges: true
    reservationPlanSettings:
      $ref: "#/types/aws:mediaconvert/QueueReservationPlanSettings:QueueReservationPlanSettings"
      description: |
        A detail pricing plan of the  reserved queue. See below.
    status:
      type: string
      description: |
        A status of the queue. Valid values are `ACTIVE` or `RESERVED`. Default to `PAUSED`.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
