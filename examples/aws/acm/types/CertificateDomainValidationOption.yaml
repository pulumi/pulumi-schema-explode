$token: aws:acm/CertificateDomainValidationOption:CertificateDomainValidationOption
properties:
  domainName:
    type: string
    description: |
      Fully qualified domain name (FQDN) in the certificate.
    language:
      python:
        mapCase: false
  resourceRecordName:
    type: string
    description: |
      The name of the DNS record to create to validate the certificate
    language:
      python:
        mapCase: false
  resourceRecordType:
    type: string
    description: |
      The type of DNS record to create
    language:
      python:
        mapCase: false
  resourceRecordValue:
    type: string
    description: |
      The value the DNS record needs to have
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - domainName
      - resourceRecordName
      - resourceRecordType
      - resourceRecordValue
