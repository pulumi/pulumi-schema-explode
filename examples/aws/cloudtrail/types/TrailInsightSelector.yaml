$token: aws:cloudtrail/TrailInsightSelector:TrailInsightSelector
properties:
  insightType:
    type: string
    description: |
      Type of insights to log on a trail. Valid values are: `ApiCallRateInsight` and `ApiErrorRateInsight`.
    language:
      python:
        mapCase: false
type: object
required:
  - insightType
