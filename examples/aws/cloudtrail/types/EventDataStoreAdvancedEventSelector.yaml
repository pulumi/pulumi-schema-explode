$token: aws:cloudtrail/EventDataStoreAdvancedEventSelector:EventDataStoreAdvancedEventSelector
properties:
  fieldSelectors:
    type: array
    items:
      $ref: "#/types/aws:cloudtrail/EventDataStoreAdvancedEventSelectorFieldSelector:EventDataStoreAdvancedEventSelectorFieldSelector"
    description: |
      Specifies the selector statements in an advanced event selector. Fields documented below.
    language:
      python:
        mapCase: false
  name:
    type: string
    description: |
      Specifies the name of the advanced event selector.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - fieldSelectors
      - name
