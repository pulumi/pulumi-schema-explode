$token: aws:cloudtrail/TrailEventSelector:TrailEventSelector
properties:
  dataResources:
    type: array
    items:
      $ref: "#/types/aws:cloudtrail/TrailEventSelectorDataResource:TrailEventSelectorDataResource"
    description: |
      Configuration block for data events. See details below.
    language:
      python:
        mapCase: false
  excludeManagementEventSources:
    type: array
    items:
      type: string
    description: |
      A set of event sources to exclude. Valid values include: `kms.amazonaws.com` and `rdsdata.amazonaws.com`. `include_management_events` must be set to`true` to allow this.
    language:
      python:
        mapCase: false
  includeManagementEvents:
    type: boolean
    description: |
      Whether to include management events for your trail. Defaults to `true`.
    language:
      python:
        mapCase: false
  readWriteType:
    type: string
    description: |
      Type of events to log. Valid values are `ReadOnly`, `WriteOnly`, `All`. Default value is `All`.
    language:
      python:
        mapCase: false
type: object
