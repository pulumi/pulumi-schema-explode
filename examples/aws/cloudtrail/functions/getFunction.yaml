$token: aws:cloudtrail/getFunction:getFunction
inputs:
  description: |
    A collection of arguments for invoking getFunction.
  properties:
    name:
      type: string
      description: |
        Name of the CloudFront function.
    stage:
      type: string
      description: |
        Function’s stage, either `DEVELOPMENT` or `LIVE`.
  type: object
  required:
    - name
    - stage
outputs:
  description: |
    A collection of values returned by getFunction.
  properties:
    arn:
      type: string
      description: |
        ARN identifying your CloudFront Function.
    code:
      type: string
      description: |
        Source code of the function
    comment:
      type: string
      description: |
        Comment.
    etag:
      type: string
      description: |
        ETag hash of the function
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    lastModifiedTime:
      type: string
      description: |
        When this resource was last modified.
    name:
      type: string
    runtime:
      type: string
      description: |
        Identifier of the function's runtime.
    stage:
      type: string
    status:
      type: string
      description: |
        Status of the function. Can be `UNPUBLISHED`, `UNASSOCIATED` or `ASSOCIATED`.
  type: object
  required:
    - arn
    - code
    - comment
    - etag
    - lastModifiedTime
    - name
    - runtime
    - stage
    - status
    - id
deprecationMessage: aws.cloudtrail.getFunction has been deprecated in favor of aws.cloudfront.getFunction
