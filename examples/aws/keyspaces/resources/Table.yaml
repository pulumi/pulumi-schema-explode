$token: aws:keyspaces/table:Table
properties:
  arn:
    type: string
    description: |
      The ARN of the table.
  capacitySpecification:
    $ref: "#/types/aws:keyspaces/TableCapacitySpecification:TableCapacitySpecification"
    description: |
      Specifies the read/write throughput capacity mode for the table.
  comment:
    $ref: "#/types/aws:keyspaces/TableComment:TableComment"
    description: |
      A description of the table.
  defaultTimeToLive:
    type: integer
    description: |
      The default Time to Live setting in seconds for the table. More information can be found in the [Developer Guide](https://docs.aws.amazon.com/keyspaces/latest/devguide/TTL-how-it-works.html#ttl-howitworks_default_ttl).
  encryptionSpecification:
    $ref: "#/types/aws:keyspaces/TableEncryptionSpecification:TableEncryptionSpecification"
    description: |
      Specifies how the encryption key for encryption at rest is managed for the table. More information can be found in the [Developer Guide](https://docs.aws.amazon.com/keyspaces/latest/devguide/EncryptionAtRest.html).
  keyspaceName:
    type: string
    description: |
      The name of the keyspace that the table is going to be created in.
  pointInTimeRecovery:
    $ref: "#/types/aws:keyspaces/TablePointInTimeRecovery:TablePointInTimeRecovery"
    description: |
      Specifies if point-in-time recovery is enabled or disabled for the table. More information can be found in the [Developer Guide](https://docs.aws.amazon.com/keyspaces/latest/devguide/PointInTimeRecovery.html).
  schemaDefinition:
    $ref: "#/types/aws:keyspaces/TableSchemaDefinition:TableSchemaDefinition"
    description: |
      Describes the schema of the table.
  tableName:
    type: string
    description: |
      The name of the table.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  ttl:
    $ref: "#/types/aws:keyspaces/TableTtl:TableTtl"
    description: |
      Enables Time to Live custom settings for the table. More information can be found in the [Developer Guide](https://docs.aws.amazon.com/keyspaces/latest/devguide/TTL.html).
required:
  - arn
  - capacitySpecification
  - comment
  - encryptionSpecification
  - keyspaceName
  - pointInTimeRecovery
  - schemaDefinition
  - tableName
  - tagsAll
inputProperties:
  capacitySpecification:
    $ref: "#/types/aws:keyspaces/TableCapacitySpecification:TableCapacitySpecification"
    description: |
      Specifies the read/write throughput capacity mode for the table.
  comment:
    $ref: "#/types/aws:keyspaces/TableComment:TableComment"
    description: |
      A description of the table.
    willReplaceOnChanges: true
  defaultTimeToLive:
    type: integer
    description: |
      The default Time to Live setting in seconds for the table. More information can be found in the [Developer Guide](https://docs.aws.amazon.com/keyspaces/latest/devguide/TTL-how-it-works.html#ttl-howitworks_default_ttl).
  encryptionSpecification:
    $ref: "#/types/aws:keyspaces/TableEncryptionSpecification:TableEncryptionSpecification"
    description: |
      Specifies how the encryption key for encryption at rest is managed for the table. More information can be found in the [Developer Guide](https://docs.aws.amazon.com/keyspaces/latest/devguide/EncryptionAtRest.html).
  keyspaceName:
    type: string
    description: |
      The name of the keyspace that the table is going to be created in.
    willReplaceOnChanges: true
  pointInTimeRecovery:
    $ref: "#/types/aws:keyspaces/TablePointInTimeRecovery:TablePointInTimeRecovery"
    description: |
      Specifies if point-in-time recovery is enabled or disabled for the table. More information can be found in the [Developer Guide](https://docs.aws.amazon.com/keyspaces/latest/devguide/PointInTimeRecovery.html).
  schemaDefinition:
    $ref: "#/types/aws:keyspaces/TableSchemaDefinition:TableSchemaDefinition"
    description: |
      Describes the schema of the table.
  tableName:
    type: string
    description: |
      The name of the table.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  ttl:
    $ref: "#/types/aws:keyspaces/TableTtl:TableTtl"
    description: |
      Enables Time to Live custom settings for the table. More information can be found in the [Developer Guide](https://docs.aws.amazon.com/keyspaces/latest/devguide/TTL.html).
requiredInputs:
  - keyspaceName
  - schemaDefinition
  - tableName
stateInputs:
  description: |
    Input properties used for looking up and filtering Table resources.
  properties:
    arn:
      type: string
      description: |
        The ARN of the table.
    capacitySpecification:
      $ref: "#/types/aws:keyspaces/TableCapacitySpecification:TableCapacitySpecification"
      description: |
        Specifies the read/write throughput capacity mode for the table.
    comment:
      $ref: "#/types/aws:keyspaces/TableComment:TableComment"
      description: |
        A description of the table.
      willReplaceOnChanges: true
    defaultTimeToLive:
      type: integer
      description: |
        The default Time to Live setting in seconds for the table. More information can be found in the [Developer Guide](https://docs.aws.amazon.com/keyspaces/latest/devguide/TTL-how-it-works.html#ttl-howitworks_default_ttl).
    encryptionSpecification:
      $ref: "#/types/aws:keyspaces/TableEncryptionSpecification:TableEncryptionSpecification"
      description: |
        Specifies how the encryption key for encryption at rest is managed for the table. More information can be found in the [Developer Guide](https://docs.aws.amazon.com/keyspaces/latest/devguide/EncryptionAtRest.html).
    keyspaceName:
      type: string
      description: |
        The name of the keyspace that the table is going to be created in.
      willReplaceOnChanges: true
    pointInTimeRecovery:
      $ref: "#/types/aws:keyspaces/TablePointInTimeRecovery:TablePointInTimeRecovery"
      description: |
        Specifies if point-in-time recovery is enabled or disabled for the table. More information can be found in the [Developer Guide](https://docs.aws.amazon.com/keyspaces/latest/devguide/PointInTimeRecovery.html).
    schemaDefinition:
      $ref: "#/types/aws:keyspaces/TableSchemaDefinition:TableSchemaDefinition"
      description: |
        Describes the schema of the table.
    tableName:
      type: string
      description: |
        The name of the table.
      willReplaceOnChanges: true
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    ttl:
      $ref: "#/types/aws:keyspaces/TableTtl:TableTtl"
      description: |
        Enables Time to Live custom settings for the table. More information can be found in the [Developer Guide](https://docs.aws.amazon.com/keyspaces/latest/devguide/TTL.html).
  type: object
