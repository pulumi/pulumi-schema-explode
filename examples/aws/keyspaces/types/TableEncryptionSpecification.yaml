$token: aws:keyspaces/TableEncryptionSpecification:TableEncryptionSpecification
properties:
  kmsKeyIdentifier:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the customer managed KMS key.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      The encryption option specified for the table. Valid values: `AWS_OWNED_KMS_KEY`, `CUSTOMER_MANAGED_KMS_KEY`. The default value is `AWS_OWNED_KMS_KEY`.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - type
