$token: aws:keyspaces/TableCapacitySpecification:TableCapacitySpecification
properties:
  readCapacityUnits:
    type: integer
    description: |
      The throughput capacity specified for read operations defined in read capacity units (RCUs).
    language:
      python:
        mapCase: false
  throughputMode:
    type: string
    description: |
      The read/write throughput capacity mode for a table. Valid values: `PAY_PER_REQUEST`, `PROVISIONED`. The default value is `PAY_PER_REQUEST`.
    language:
      python:
        mapCase: false
  writeCapacityUnits:
    type: integer
    description: |
      The throughput capacity specified for write operations defined in write capacity units (WCUs).
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - throughputMode
