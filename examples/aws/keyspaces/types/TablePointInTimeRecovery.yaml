$token: aws:keyspaces/TablePointInTimeRecovery:TablePointInTimeRecovery
properties:
  status:
    type: string
    description: |
      Valid values: `ENABLED`, `DISABLED`. The default value is `DISABLED`.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - status
