$token: aws:kendra/getExperienceConfigurationContentSourceConfiguration:getExperienceConfigurationContentSourceConfiguration
properties:
  dataSourceIds:
    type: array
    items:
      type: string
    description: |
      Identifiers of the data sources you want to use for your Amazon Kendra Experience.
    language:
      python:
        mapCase: false
  directPutContent:
    type: boolean
    description: |
      Whether to use documents you indexed directly using the `BatchPutDocument API`.
    language:
      python:
        mapCase: false
  faqIds:
    type: array
    items:
      type: string
    description: |
      Identifier of the FAQs that you want to use for your Amazon Kendra Experience.
    language:
      python:
        mapCase: false
type: object
required:
  - dataSourceIds
  - directPutContent
  - faqIds
language:
  nodejs:
    requiredInputs: []
