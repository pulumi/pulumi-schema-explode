$token: aws:kendra/ExperienceConfiguration:ExperienceConfiguration
properties:
  contentSourceConfiguration:
    $ref: "#/types/aws:kendra/ExperienceConfigurationContentSourceConfiguration:ExperienceConfigurationContentSourceConfiguration"
    language:
      python:
        mapCase: false
  userIdentityConfiguration:
    $ref: "#/types/aws:kendra/ExperienceConfigurationUserIdentityConfiguration:ExperienceConfigurationUserIdentityConfiguration"
    description: |
      The AWS SSO field name that contains the identifiers of your users, such as their emails. Detailed below.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - contentSourceConfiguration
