$token: aws:kendra/getIndexServerSideEncryptionConfiguration:getIndexServerSideEncryptionConfiguration
properties:
  kmsKeyId:
    type: string
    description: |
      Identifier of the AWS KMScustomer master key (CMK). Amazon Kendra doesn't support asymmetric CMKs.
    language:
      python:
        mapCase: false
type: object
required:
  - kmsKeyId
language:
  nodejs:
    requiredInputs: []
