$token: aws:kendra/getIndexDocumentMetadataConfigurationUpdate:getIndexDocumentMetadataConfigurationUpdate
properties:
  name:
    type: string
    description: |
      Name of the index field. Minimum length of 1. Maximum length of 30.
    language:
      python:
        mapCase: false
  relevances:
    type: array
    items:
      $ref: "#/types/aws:kendra/getIndexDocumentMetadataConfigurationUpdateRelevance:getIndexDocumentMetadataConfigurationUpdateRelevance"
    description: |
      Block that provides manual tuning parameters to determine how the field affects the search results. Documented below.
    language:
      python:
        mapCase: false
  searches:
    type: array
    items:
      $ref: "#/types/aws:kendra/getIndexDocumentMetadataConfigurationUpdateSearch:getIndexDocumentMetadataConfigurationUpdateSearch"
    description: |
      Block that provides information about how the field is used during a search. Documented below.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      Data type of the index field. Valid values are `STRING_VALUE`, `STRING_LIST_VALUE`, `LONG_VALUE`, `DATE_VALUE`.
    language:
      python:
        mapCase: false
type: object
required:
  - name
  - relevances
  - searches
  - type
language:
  nodejs:
    requiredInputs: []
