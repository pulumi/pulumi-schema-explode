$token: aws:kendra/DataSourceConfigurationWebCrawlerConfigurationUrlsSeedUrlConfiguration:DataSourceConfigurationWebCrawlerConfigurationUrlsSeedUrlConfiguration
properties:
  seedUrls:
    type: array
    items:
      type: string
    description: |
      The list of seed or starting point URLs of the websites you want to crawl. The list can include a maximum of `100` seed URLs. Array Members: Minimum number of `0` items. Maximum number of `100` items. Length Constraints: Minimum length of `1`. Maximum length of `2048`.
    language:
      python:
        mapCase: false
  webCrawlerMode:
    type: string
    description: |
      The default mode is set to `HOST_ONLY`. You can choose one of the following modes:
    language:
      python:
        mapCase: false
type: object
required:
  - seedUrls
