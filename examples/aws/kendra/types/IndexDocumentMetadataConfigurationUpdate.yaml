$token: aws:kendra/IndexDocumentMetadataConfigurationUpdate:IndexDocumentMetadataConfigurationUpdate
properties:
  name:
    type: string
    description: |
      The name of the index field. Minimum length of 1. Maximum length of 30.
    language:
      python:
        mapCase: false
  relevance:
    $ref: "#/types/aws:kendra/IndexDocumentMetadataConfigurationUpdateRelevance:IndexDocumentMetadataConfigurationUpdateRelevance"
    description: |
      A block that provides manual tuning parameters to determine how the field affects the search results. Detailed below
    language:
      python:
        mapCase: false
  search:
    $ref: "#/types/aws:kendra/IndexDocumentMetadataConfigurationUpdateSearch:IndexDocumentMetadataConfigurationUpdateSearch"
    description: |
      A block that provides information about how the field is used during a search. Documented below. Detailed below
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      The data type of the index field. Valid values are `STRING_VALUE`, `STRING_LIST_VALUE`, `LONG_VALUE`, `DATE_VALUE`.
    language:
      python:
        mapCase: false
type: object
required:
  - name
  - type
language:
  nodejs:
    requiredOutputs:
      - name
      - relevance
      - search
      - type
