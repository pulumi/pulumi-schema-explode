$token: aws:kendra/DataSourceConfigurationWebCrawlerConfigurationAuthenticationConfigurationBasicAuthentication:DataSourceConfigurationWebCrawlerConfigurationAuthenticationConfigurationBasicAuthentication
properties:
  credentials:
    type: string
    description: |
      Your secret ARN, which you can create in AWS Secrets Manager. You use a secret if basic authentication credentials are required to connect to a website. The secret stores your credentials of user name and password.
    language:
      python:
        mapCase: false
  host:
    type: string
    description: |
      The name of the website host you want to connect to using authentication credentials. For example, the host name of `https://a.example.com/page1.html` is `"a.example.com"`.
    language:
      python:
        mapCase: false
  port:
    type: integer
    description: |
      The port number of the website host you want to connect to using authentication credentials. For example, the port for `https://a.example.com/page1.html` is `443`, the standard port for HTTPS.
    language:
      python:
        mapCase: false
type: object
required:
  - credentials
  - host
  - port
