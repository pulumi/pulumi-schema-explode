$token: aws:kendra/getIndexUserTokenConfigurationJwtTokenTypeConfiguration:getIndexUserTokenConfigurationJwtTokenTypeConfiguration
properties:
  claimRegex:
    type: string
    description: |
      Regular expression that identifies the claim.
    language:
      python:
        mapCase: false
  groupAttributeField:
    type: string
    description: |
      The group attribute field.
    language:
      python:
        mapCase: false
  issuer:
    type: string
    description: |
      Issuer of the token.
    language:
      python:
        mapCase: false
  keyLocation:
    type: string
    description: |
      Location of the key. Valid values are `URL` or `SECRET_MANAGER`
    language:
      python:
        mapCase: false
  secretsManagerArn:
    type: string
    description: |
      ARN of the secret.
    language:
      python:
        mapCase: false
  url:
    type: string
    description: |
      Signing key URL.
    language:
      python:
        mapCase: false
  userNameAttributeField:
    type: string
    description: |
      The user name attribute field.
    language:
      python:
        mapCase: false
type: object
required:
  - claimRegex
  - groupAttributeField
  - issuer
  - keyLocation
  - secretsManagerArn
  - url
  - userNameAttributeField
language:
  nodejs:
    requiredInputs: []
