$token: aws:kendra/ExperienceConfigurationContentSourceConfiguration:ExperienceConfigurationContentSourceConfiguration
properties:
  dataSourceIds:
    type: array
    items:
      type: string
    description: |
      The identifiers of the data sources you want to use for your Amazon Kendra experience. Maximum number of 100 items.
    language:
      python:
        mapCase: false
  directPutContent:
    type: boolean
    description: |
      Whether to use documents you indexed directly using the `BatchPutDocument API`. Defaults to `false`.
    language:
      python:
        mapCase: false
  faqIds:
    type: array
    items:
      type: string
    description: |
      The identifier of the FAQs that you want to use for your Amazon Kendra experience. Maximum number of 100 items.
    language:
      python:
        mapCase: false
type: object
