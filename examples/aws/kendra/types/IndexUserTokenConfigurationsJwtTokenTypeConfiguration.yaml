$token: aws:kendra/IndexUserTokenConfigurationsJwtTokenTypeConfiguration:IndexUserTokenConfigurationsJwtTokenTypeConfiguration
properties:
  claimRegex:
    type: string
    description: |
      The regular expression that identifies the claim. Minimum length of 1. Maximum length of 100.
    language:
      python:
        mapCase: false
  groupAttributeField:
    type: string
    description: |
      The group attribute field. Minimum length of 1. Maximum length of 100.
    language:
      python:
        mapCase: false
  issuer:
    type: string
    description: |
      The issuer of the token. Minimum length of 1. Maximum length of 65.
    language:
      python:
        mapCase: false
  keyLocation:
    type: string
    description: |
      The location of the key. Valid values are `URL` or `SECRET_MANAGER`
    language:
      python:
        mapCase: false
  secretsManagerArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the secret.
    language:
      python:
        mapCase: false
  url:
    type: string
    description: |
      The signing key URL. Valid pattern is `^(https?|ftp|file):\/\/([^\s]*)`
    language:
      python:
        mapCase: false
  userNameAttributeField:
    type: string
    description: |
      The user name attribute field. Minimum length of 1. Maximum length of 100.
    language:
      python:
        mapCase: false
type: object
required:
  - keyLocation
