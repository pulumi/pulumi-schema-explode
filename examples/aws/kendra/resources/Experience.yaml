$token: aws:kendra/experience:Experience
properties:
  arn:
    type: string
    description: |
      ARN of the Experience.
  configuration:
    $ref: "#/types/aws:kendra/ExperienceConfiguration:ExperienceConfiguration"
  description:
    type: string
    description: |
      A description for your Amazon Kendra experience.
  endpoints:
    type: array
    items:
      $ref: "#/types/aws:kendra/ExperienceEndpoint:ExperienceEndpoint"
    description: |
      Shows the endpoint URLs for your Amazon Kendra experiences. The URLs are unique and fully hosted by AWS.
  experienceId:
    type: string
    description: |
      The unique identifier of the experience.
  indexId:
    type: string
    description: |
      The identifier of the index for your Amazon Kendra experience.
  name:
    type: string
    description: |
      A name for your Amazon Kendra experience.
  roleArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of a role with permission to access `Query API`, `QuerySuggestions API`, `SubmitFeedback API`, and `AWS SSO` that stores your user and group information. For more information, see [IAM roles for Amazon Kendra](https://docs.aws.amazon.com/kendra/latest/dg/iam-roles.html).
  status:
    type: string
    description: |
      The current processing status of your Amazon Kendra experience.
required:
  - arn
  - configuration
  - endpoints
  - experienceId
  - indexId
  - name
  - roleArn
  - status
inputProperties:
  configuration:
    $ref: "#/types/aws:kendra/ExperienceConfiguration:ExperienceConfiguration"
  description:
    type: string
    description: |
      A description for your Amazon Kendra experience.
  indexId:
    type: string
    description: |
      The identifier of the index for your Amazon Kendra experience.
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      A name for your Amazon Kendra experience.
  roleArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of a role with permission to access `Query API`, `QuerySuggestions API`, `SubmitFeedback API`, and `AWS SSO` that stores your user and group information. For more information, see [IAM roles for Amazon Kendra](https://docs.aws.amazon.com/kendra/latest/dg/iam-roles.html).
requiredInputs:
  - indexId
  - roleArn
stateInputs:
  description: |
    Input properties used for looking up and filtering Experience resources.
  properties:
    arn:
      type: string
      description: |
        ARN of the Experience.
    configuration:
      $ref: "#/types/aws:kendra/ExperienceConfiguration:ExperienceConfiguration"
    description:
      type: string
      description: |
        A description for your Amazon Kendra experience.
    endpoints:
      type: array
      items:
        $ref: "#/types/aws:kendra/ExperienceEndpoint:ExperienceEndpoint"
      description: |
        Shows the endpoint URLs for your Amazon Kendra experiences. The URLs are unique and fully hosted by AWS.
    experienceId:
      type: string
      description: |
        The unique identifier of the experience.
    indexId:
      type: string
      description: |
        The identifier of the index for your Amazon Kendra experience.
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        A name for your Amazon Kendra experience.
    roleArn:
      type: string
      description: |
        The Amazon Resource Name (ARN) of a role with permission to access `Query API`, `QuerySuggestions API`, `SubmitFeedback API`, and `AWS SSO` that stores your user and group information. For more information, see [IAM roles for Amazon Kendra](https://docs.aws.amazon.com/kendra/latest/dg/iam-roles.html).
    status:
      type: string
      description: |
        The current processing status of your Amazon Kendra experience.
  type: object
