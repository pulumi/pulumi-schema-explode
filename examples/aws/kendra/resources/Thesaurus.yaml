$token: aws:kendra/thesaurus:Thesaurus
properties:
  arn:
    type: string
    description: |
      ARN of the thesaurus.
  description:
    type: string
    description: |
      The description for a thesaurus.
  indexId:
    type: string
    description: |
      The identifier of the index for a thesaurus.
  name:
    type: string
    description: |
      The name for the thesaurus.
  roleArn:
    type: string
    description: |
      The IAM (Identity and Access Management) role used to access the thesaurus file in S3.
  sourceS3Path:
    $ref: "#/types/aws:kendra/ThesaurusSourceS3Path:ThesaurusSourceS3Path"
    description: |
      The S3 path where your thesaurus file sits in S3. Detailed below.
  status:
    type: string
    description: |
      The current status of the thesaurus.
  tags:
    type: object
    additionalProperties:
      type: string
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  thesaurusId:
    type: string
required:
  - arn
  - indexId
  - name
  - roleArn
  - sourceS3Path
  - status
  - tagsAll
  - thesaurusId
inputProperties:
  description:
    type: string
    description: |
      The description for a thesaurus.
  indexId:
    type: string
    description: |
      The identifier of the index for a thesaurus.
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      The name for the thesaurus.
  roleArn:
    type: string
    description: |
      The IAM (Identity and Access Management) role used to access the thesaurus file in S3.
  sourceS3Path:
    $ref: "#/types/aws:kendra/ThesaurusSourceS3Path:ThesaurusSourceS3Path"
    description: |
      The S3 path where your thesaurus file sits in S3. Detailed below.
  tags:
    type: object
    additionalProperties:
      type: string
requiredInputs:
  - indexId
  - roleArn
  - sourceS3Path
stateInputs:
  description: |
    Input properties used for looking up and filtering Thesaurus resources.
  properties:
    arn:
      type: string
      description: |
        ARN of the thesaurus.
    description:
      type: string
      description: |
        The description for a thesaurus.
    indexId:
      type: string
      description: |
        The identifier of the index for a thesaurus.
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        The name for the thesaurus.
    roleArn:
      type: string
      description: |
        The IAM (Identity and Access Management) role used to access the thesaurus file in S3.
    sourceS3Path:
      $ref: "#/types/aws:kendra/ThesaurusSourceS3Path:ThesaurusSourceS3Path"
      description: |
        The S3 path where your thesaurus file sits in S3. Detailed below.
    status:
      type: string
      description: |
        The current status of the thesaurus.
    tags:
      type: object
      additionalProperties:
        type: string
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    thesaurusId:
      type: string
  type: object
