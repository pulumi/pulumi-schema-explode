$token: aws:opensearch/DomainAutoTuneOptions:DomainAutoTuneOptions
properties:
  desiredState:
    type: string
    description: |
      Auto-Tune desired state for the domain. Valid values: `ENABLED` or `DISABLED`.
    language:
      python:
        mapCase: false
  maintenanceSchedules:
    type: array
    items:
      $ref: "#/types/aws:opensearch/DomainAutoTuneOptionsMaintenanceSchedule:DomainAutoTuneOptionsMaintenanceSchedule"
    description: |
      Configuration block for Auto-Tune maintenance windows. Can be specified multiple times for each maintenance window. Detailed below.
    language:
      python:
        mapCase: false
  rollbackOnDisable:
    type: string
    description: |
      Whether to roll back to default Auto-Tune settings when disabling Auto-Tune. Valid values: `DEFAULT_ROLLBACK` or `NO_ROLLBACK`.
    language:
      python:
        mapCase: false
type: object
required:
  - desiredState
language:
  nodejs:
    requiredOutputs:
      - desiredState
      - maintenanceSchedules
      - rollbackOnDisable
