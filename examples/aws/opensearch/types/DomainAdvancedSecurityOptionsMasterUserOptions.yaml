$token: aws:opensearch/DomainAdvancedSecurityOptionsMasterUserOptions:DomainAdvancedSecurityOptionsMasterUserOptions
properties:
  masterUserArn:
    type: string
    description: |
      ARN for the main user. Only specify if `internal_user_database_enabled` is not set or set to `false`.
    language:
      python:
        mapCase: false
  masterUserName:
    type: string
    description: |
      Main user's username, which is stored in the Amazon OpenSearch Service domain's internal database. Only specify if `internal_user_database_enabled` is set to `true`.
    language:
      python:
        mapCase: false
  masterUserPassword:
    type: string
    description: |
      Main user's password, which is stored in the Amazon OpenSearch Service domain's internal database. Only specify if `internal_user_database_enabled` is set to `true`.
    language:
      python:
        mapCase: false
type: object
