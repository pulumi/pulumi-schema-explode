$token: aws:opensearch/getDomainEbsOption:getDomainEbsOption
properties:
  ebsEnabled:
    type: boolean
    description: |
      Whether EBS volumes are attached to data nodes in the domain.
    language:
      python:
        mapCase: false
  iops:
    type: integer
    description: |
      Baseline input/output (I/O) performance of EBS volumes attached to data nodes.
    language:
      python:
        mapCase: false
  throughput:
    type: integer
    description: |
      The throughput (in MiB/s) of the EBS volumes attached to data nodes.
    language:
      python:
        mapCase: false
  volumeSize:
    type: integer
    description: |
      Size of EBS volumes attached to data nodes (in GB).
    language:
      python:
        mapCase: false
  volumeType:
    type: string
    description: |
      Type of EBS volumes attached to data nodes.
    language:
      python:
        mapCase: false
type: object
required:
  - ebsEnabled
  - iops
  - throughput
  - volumeSize
  - volumeType
language:
  nodejs:
    requiredInputs: []
