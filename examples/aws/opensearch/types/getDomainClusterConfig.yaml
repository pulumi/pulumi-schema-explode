$token: aws:opensearch/getDomainClusterConfig:getDomainClusterConfig
properties:
  coldStorageOptions:
    type: array
    items:
      $ref: "#/types/aws:opensearch/getDomainClusterConfigColdStorageOption:getDomainClusterConfigColdStorageOption"
    description: |
      Configuration block containing cold storage configuration.
    language:
      python:
        mapCase: false
  dedicatedMasterCount:
    type: integer
    description: |
      Number of dedicated master nodes in the cluster.
    language:
      python:
        mapCase: false
  dedicatedMasterEnabled:
    type: boolean
    description: |
      Indicates whether dedicated master nodes are enabled for the cluster.
    language:
      python:
        mapCase: false
  dedicatedMasterType:
    type: string
    description: |
      Instance type of the dedicated master nodes in the cluster.
    language:
      python:
        mapCase: false
  instanceCount:
    type: integer
    description: |
      Number of instances in the cluster.
    language:
      python:
        mapCase: false
  instanceType:
    type: string
    description: |
      Instance type of data nodes in the cluster.
    language:
      python:
        mapCase: false
  warmCount:
    type: integer
    description: |
      Number of warm nodes in the cluster.
    language:
      python:
        mapCase: false
  warmEnabled:
    type: boolean
    description: |
      Warm storage is enabled.
    language:
      python:
        mapCase: false
  warmType:
    type: string
    description: |
      Instance type for the OpenSearch cluster's warm nodes.
    language:
      python:
        mapCase: false
  zoneAwarenessConfigs:
    type: array
    items:
      $ref: "#/types/aws:opensearch/getDomainClusterConfigZoneAwarenessConfig:getDomainClusterConfigZoneAwarenessConfig"
    description: |
      Configuration block containing zone awareness settings.
    language:
      python:
        mapCase: false
  zoneAwarenessEnabled:
    type: boolean
    description: |
      Indicates whether zone awareness is enabled.
    language:
      python:
        mapCase: false
type: object
required:
  - coldStorageOptions
  - dedicatedMasterCount
  - dedicatedMasterEnabled
  - dedicatedMasterType
  - instanceCount
  - instanceType
  - warmCount
  - warmType
  - zoneAwarenessConfigs
  - zoneAwarenessEnabled
language:
  nodejs:
    requiredInputs: []
