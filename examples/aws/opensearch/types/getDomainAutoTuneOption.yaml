$token: aws:opensearch/getDomainAutoTuneOption:getDomainAutoTuneOption
properties:
  desiredState:
    type: string
    description: |
      Auto-Tune desired state for the domain.
    language:
      python:
        mapCase: false
  maintenanceSchedules:
    type: array
    items:
      $ref: "#/types/aws:opensearch/getDomainAutoTuneOptionMaintenanceSchedule:getDomainAutoTuneOptionMaintenanceSchedule"
    description: |
      A list of the nested configurations for the Auto-Tune maintenance windows of the domain.
    language:
      python:
        mapCase: false
  rollbackOnDisable:
    type: string
    description: |
      Whether the domain is set to roll back to default Auto-Tune settings when disabling Auto-Tune.
    language:
      python:
        mapCase: false
type: object
required:
  - desiredState
  - maintenanceSchedules
  - rollbackOnDisable
language:
  nodejs:
    requiredInputs: []
