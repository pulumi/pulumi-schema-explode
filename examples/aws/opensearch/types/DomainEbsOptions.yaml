$token: aws:opensearch/DomainEbsOptions:DomainEbsOptions
properties:
  ebsEnabled:
    type: boolean
    description: |
      Whether EBS volumes are attached to data nodes in the domain.
    language:
      python:
        mapCase: false
  iops:
    type: integer
    description: |
      Baseline input/output (I/O) performance of EBS volumes attached to data nodes. Applicable only for the GP3 and Provisioned IOPS EBS volume types.
    language:
      python:
        mapCase: false
  throughput:
    type: integer
    description: |
      Specifies the throughput (in MiB/s) of the EBS volumes attached to data nodes. Applicable only for the gp3 volume type. Valid values are between `125` and `1000`.
    language:
      python:
        mapCase: false
  volumeSize:
    type: integer
    description: |
      Size of EBS volumes attached to data nodes (in GiB).
    language:
      python:
        mapCase: false
  volumeType:
    type: string
    description: |
      Type of EBS volumes attached to data nodes.
    language:
      python:
        mapCase: false
type: object
required:
  - ebsEnabled
language:
  nodejs:
    requiredOutputs:
      - ebsEnabled
      - iops
      - throughput
      - volumeType
