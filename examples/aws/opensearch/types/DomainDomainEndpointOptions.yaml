$token: aws:opensearch/DomainDomainEndpointOptions:DomainDomainEndpointOptions
properties:
  customEndpoint:
    type: string
    description: |
      Fully qualified domain for your custom endpoint.
    language:
      python:
        mapCase: false
  customEndpointCertificateArn:
    type: string
    description: |
      ACM certificate ARN for your custom endpoint.
    language:
      python:
        mapCase: false
  customEndpointEnabled:
    type: boolean
    description: |
      Whether to enable custom endpoint for the OpenSearch domain.
    language:
      python:
        mapCase: false
  enforceHttps:
    type: boolean
    description: |
      Whether or not to require HTTPS. Defaults to `true`.
    language:
      python:
        mapCase: false
  tlsSecurityPolicy:
    type: string
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - tlsSecurityPolicy
