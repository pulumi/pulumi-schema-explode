$token: aws:opensearch/DomainVpcOptions:DomainVpcOptions
properties:
  availabilityZones:
    type: array
    items:
      type: string
    language:
      python:
        mapCase: false
  securityGroupIds:
    type: array
    items:
      type: string
    description: |
      List of VPC Security Group IDs to be applied to the OpenSearch domain endpoints. If omitted, the default Security Group for the VPC will be used.
    language:
      python:
        mapCase: false
  subnetIds:
    type: array
    items:
      type: string
    description: |
      List of VPC Subnet IDs for the OpenSearch domain endpoints to be created in.
    language:
      python:
        mapCase: false
  vpcId:
    type: string
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - availabilityZones
      - vpcId
