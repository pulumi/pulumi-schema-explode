$token: aws:opensearch/DomainClusterConfigZoneAwarenessConfig:DomainClusterConfigZoneAwarenessConfig
properties:
  availabilityZoneCount:
    type: integer
    description: |
      Number of Availability Zones for the domain to use with `zone_awareness_enabled`. Defaults to `2`. Valid values: `2` or `3`.
    language:
      python:
        mapCase: false
type: object
