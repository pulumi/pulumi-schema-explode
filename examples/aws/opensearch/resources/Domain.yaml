$token: aws:opensearch/domain:Domain
properties:
  accessPolicies:
    type: string
    description: |
      IAM policy document specifying the access policies for the domain.
  advancedOptions:
    type: object
    additionalProperties:
      type: string
  advancedSecurityOptions:
    $ref: "#/types/aws:opensearch/DomainAdvancedSecurityOptions:DomainAdvancedSecurityOptions"
    description: |
      Configuration block for [fine-grained access control](https://docs.aws.amazon.com/opensearch-service/latest/developerguide/fgac.html). Detailed below.
  arn:
    type: string
    description: |
      ARN of the domain.
  autoTuneOptions:
    $ref: "#/types/aws:opensearch/DomainAutoTuneOptions:DomainAutoTuneOptions"
    description: |
      Configuration block for the Auto-Tune options of the domain. Detailed below.
  clusterConfig:
    $ref: "#/types/aws:opensearch/DomainClusterConfig:DomainClusterConfig"
    description: |
      Configuration block for the cluster of the domain. Detailed below.
  cognitoOptions:
    $ref: "#/types/aws:opensearch/DomainCognitoOptions:DomainCognitoOptions"
    description: |
      Configuration block for authenticating Kibana with Cognito. Detailed below.
  domainEndpointOptions:
    $ref: "#/types/aws:opensearch/DomainDomainEndpointOptions:DomainDomainEndpointOptions"
    description: |
      Configuration block for domain endpoint HTTP(S) related options. Detailed below.
  domainId:
    type: string
    description: |
      Unique identifier for the domain.
  domainName:
    type: string
    description: |
      Name of the domain.
  ebsOptions:
    $ref: "#/types/aws:opensearch/DomainEbsOptions:DomainEbsOptions"
    description: |
      Configuration block for EBS related options, may be required based on chosen [instance size](https://aws.amazon.com/opensearch-service/pricing/). Detailed below.
  encryptAtRest:
    $ref: "#/types/aws:opensearch/DomainEncryptAtRest:DomainEncryptAtRest"
    description: |
      Configuration block for encrypt at rest options. Only available for [certain instance types](https://docs.aws.amazon.com/opensearch-service/latest/developerguide/encryption-at-rest.html). Detailed below.
  endpoint:
    type: string
    description: |
      Domain-specific endpoint used to submit index, search, and data upload requests.
  engineVersion:
    type: string
    description: |
      Either `Elasticsearch_X.Y` or `OpenSearch_X.Y` to specify the engine version for the Amazon OpenSearch Service domain. For example, `OpenSearch_1.0` or `Elasticsearch_7.9`. See [Creating and managing Amazon OpenSearch Service domains](http://docs.aws.amazon.com/opensearch-service/latest/developerguide/createupdatedomains.html#createdomains). Defaults to `OpenSearch_1.1`.
  kibanaEndpoint:
    type: string
    description: |
      Domain-specific endpoint for kibana without https scheme.
  logPublishingOptions:
    type: array
    items:
      $ref: "#/types/aws:opensearch/DomainLogPublishingOption:DomainLogPublishingOption"
    description: |
      Configuration block for publishing slow and application logs to CloudWatch Logs. This block can be declared multiple times, for each log_type, within the same resource. Detailed below.
  nodeToNodeEncryption:
    $ref: "#/types/aws:opensearch/DomainNodeToNodeEncryption:DomainNodeToNodeEncryption"
    description: |
      Configuration block for node-to-node encryption options. Detailed below.
  snapshotOptions:
    $ref: "#/types/aws:opensearch/DomainSnapshotOptions:DomainSnapshotOptions"
    description: |
      Configuration block for snapshot related options. Detailed below. DEPRECATED. For domains running OpenSearch 5.3 and later, Amazon OpenSearch takes hourly automated snapshots, making this setting irrelevant. For domains running earlier versions, OpenSearch takes daily automated snapshots.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
  vpcOptions:
    $ref: "#/types/aws:opensearch/DomainVpcOptions:DomainVpcOptions"
    description: |
      Configuration block for VPC related options. Adding or removing this configuration forces a new resource ([documentation](https://docs.aws.amazon.com/opensearch-service/latest/developerguide/vpc.html)). Detailed below.
required:
  - accessPolicies
  - advancedOptions
  - advancedSecurityOptions
  - arn
  - autoTuneOptions
  - clusterConfig
  - domainEndpointOptions
  - domainId
  - domainName
  - ebsOptions
  - encryptAtRest
  - endpoint
  - kibanaEndpoint
  - nodeToNodeEncryption
  - tagsAll
inputProperties:
  accessPolicies:
    type: string
    description: |
      IAM policy document specifying the access policies for the domain.
  advancedOptions:
    type: object
    additionalProperties:
      type: string
  advancedSecurityOptions:
    $ref: "#/types/aws:opensearch/DomainAdvancedSecurityOptions:DomainAdvancedSecurityOptions"
    description: |
      Configuration block for [fine-grained access control](https://docs.aws.amazon.com/opensearch-service/latest/developerguide/fgac.html). Detailed below.
  autoTuneOptions:
    $ref: "#/types/aws:opensearch/DomainAutoTuneOptions:DomainAutoTuneOptions"
    description: |
      Configuration block for the Auto-Tune options of the domain. Detailed below.
  clusterConfig:
    $ref: "#/types/aws:opensearch/DomainClusterConfig:DomainClusterConfig"
    description: |
      Configuration block for the cluster of the domain. Detailed below.
  cognitoOptions:
    $ref: "#/types/aws:opensearch/DomainCognitoOptions:DomainCognitoOptions"
    description: |
      Configuration block for authenticating Kibana with Cognito. Detailed below.
  domainEndpointOptions:
    $ref: "#/types/aws:opensearch/DomainDomainEndpointOptions:DomainDomainEndpointOptions"
    description: |
      Configuration block for domain endpoint HTTP(S) related options. Detailed below.
  domainName:
    type: string
    description: |
      Name of the domain.
    willReplaceOnChanges: true
  ebsOptions:
    $ref: "#/types/aws:opensearch/DomainEbsOptions:DomainEbsOptions"
    description: |
      Configuration block for EBS related options, may be required based on chosen [instance size](https://aws.amazon.com/opensearch-service/pricing/). Detailed below.
  encryptAtRest:
    $ref: "#/types/aws:opensearch/DomainEncryptAtRest:DomainEncryptAtRest"
    description: |
      Configuration block for encrypt at rest options. Only available for [certain instance types](https://docs.aws.amazon.com/opensearch-service/latest/developerguide/encryption-at-rest.html). Detailed below.
  engineVersion:
    type: string
    description: |
      Either `Elasticsearch_X.Y` or `OpenSearch_X.Y` to specify the engine version for the Amazon OpenSearch Service domain. For example, `OpenSearch_1.0` or `Elasticsearch_7.9`. See [Creating and managing Amazon OpenSearch Service domains](http://docs.aws.amazon.com/opensearch-service/latest/developerguide/createupdatedomains.html#createdomains). Defaults to `OpenSearch_1.1`.
  logPublishingOptions:
    type: array
    items:
      $ref: "#/types/aws:opensearch/DomainLogPublishingOption:DomainLogPublishingOption"
    description: |
      Configuration block for publishing slow and application logs to CloudWatch Logs. This block can be declared multiple times, for each log_type, within the same resource. Detailed below.
  nodeToNodeEncryption:
    $ref: "#/types/aws:opensearch/DomainNodeToNodeEncryption:DomainNodeToNodeEncryption"
    description: |
      Configuration block for node-to-node encryption options. Detailed below.
  snapshotOptions:
    $ref: "#/types/aws:opensearch/DomainSnapshotOptions:DomainSnapshotOptions"
    description: |
      Configuration block for snapshot related options. Detailed below. DEPRECATED. For domains running OpenSearch 5.3 and later, Amazon OpenSearch takes hourly automated snapshots, making this setting irrelevant. For domains running earlier versions, OpenSearch takes daily automated snapshots.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  vpcOptions:
    $ref: "#/types/aws:opensearch/DomainVpcOptions:DomainVpcOptions"
    description: |
      Configuration block for VPC related options. Adding or removing this configuration forces a new resource ([documentation](https://docs.aws.amazon.com/opensearch-service/latest/developerguide/vpc.html)). Detailed below.
    willReplaceOnChanges: true
stateInputs:
  description: |
    Input properties used for looking up and filtering Domain resources.
  properties:
    accessPolicies:
      type: string
      description: |
        IAM policy document specifying the access policies for the domain.
    advancedOptions:
      type: object
      additionalProperties:
        type: string
    advancedSecurityOptions:
      $ref: "#/types/aws:opensearch/DomainAdvancedSecurityOptions:DomainAdvancedSecurityOptions"
      description: |
        Configuration block for [fine-grained access control](https://docs.aws.amazon.com/opensearch-service/latest/developerguide/fgac.html). Detailed below.
    arn:
      type: string
      description: |
        ARN of the domain.
    autoTuneOptions:
      $ref: "#/types/aws:opensearch/DomainAutoTuneOptions:DomainAutoTuneOptions"
      description: |
        Configuration block for the Auto-Tune options of the domain. Detailed below.
    clusterConfig:
      $ref: "#/types/aws:opensearch/DomainClusterConfig:DomainClusterConfig"
      description: |
        Configuration block for the cluster of the domain. Detailed below.
    cognitoOptions:
      $ref: "#/types/aws:opensearch/DomainCognitoOptions:DomainCognitoOptions"
      description: |
        Configuration block for authenticating Kibana with Cognito. Detailed below.
    domainEndpointOptions:
      $ref: "#/types/aws:opensearch/DomainDomainEndpointOptions:DomainDomainEndpointOptions"
      description: |
        Configuration block for domain endpoint HTTP(S) related options. Detailed below.
    domainId:
      type: string
      description: |
        Unique identifier for the domain.
    domainName:
      type: string
      description: |
        Name of the domain.
      willReplaceOnChanges: true
    ebsOptions:
      $ref: "#/types/aws:opensearch/DomainEbsOptions:DomainEbsOptions"
      description: |
        Configuration block for EBS related options, may be required based on chosen [instance size](https://aws.amazon.com/opensearch-service/pricing/). Detailed below.
    encryptAtRest:
      $ref: "#/types/aws:opensearch/DomainEncryptAtRest:DomainEncryptAtRest"
      description: |
        Configuration block for encrypt at rest options. Only available for [certain instance types](https://docs.aws.amazon.com/opensearch-service/latest/developerguide/encryption-at-rest.html). Detailed below.
    endpoint:
      type: string
      description: |
        Domain-specific endpoint used to submit index, search, and data upload requests.
    engineVersion:
      type: string
      description: |
        Either `Elasticsearch_X.Y` or `OpenSearch_X.Y` to specify the engine version for the Amazon OpenSearch Service domain. For example, `OpenSearch_1.0` or `Elasticsearch_7.9`. See [Creating and managing Amazon OpenSearch Service domains](http://docs.aws.amazon.com/opensearch-service/latest/developerguide/createupdatedomains.html#createdomains). Defaults to `OpenSearch_1.1`.
    kibanaEndpoint:
      type: string
      description: |
        Domain-specific endpoint for kibana without https scheme.
    logPublishingOptions:
      type: array
      items:
        $ref: "#/types/aws:opensearch/DomainLogPublishingOption:DomainLogPublishingOption"
      description: |
        Configuration block for publishing slow and application logs to CloudWatch Logs. This block can be declared multiple times, for each log_type, within the same resource. Detailed below.
    nodeToNodeEncryption:
      $ref: "#/types/aws:opensearch/DomainNodeToNodeEncryption:DomainNodeToNodeEncryption"
      description: |
        Configuration block for node-to-node encryption options. Detailed below.
    snapshotOptions:
      $ref: "#/types/aws:opensearch/DomainSnapshotOptions:DomainSnapshotOptions"
      description: |
        Configuration block for snapshot related options. Detailed below. DEPRECATED. For domains running OpenSearch 5.3 and later, Amazon OpenSearch takes hourly automated snapshots, making this setting irrelevant. For domains running earlier versions, OpenSearch takes daily automated snapshots.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
    vpcOptions:
      $ref: "#/types/aws:opensearch/DomainVpcOptions:DomainVpcOptions"
      description: |
        Configuration block for VPC related options. Adding or removing this configuration forces a new resource ([documentation](https://docs.aws.amazon.com/opensearch-service/latest/developerguide/vpc.html)). Detailed below.
      willReplaceOnChanges: true
  type: object
