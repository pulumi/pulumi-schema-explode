$token: aws:guardduty/getDetector:getDetector
inputs:
  description: |
    A collection of arguments for invoking getDetector.
  properties:
    id:
      type: string
      description: |
        ID of the detector.
  type: object
outputs:
  description: |
    A collection of values returned by getDetector.
  properties:
    findingPublishingFrequency:
      type: string
      description: |
        The frequency of notifications sent about subsequent finding occurrences.
    id:
      type: string
    serviceRoleArn:
      type: string
      description: |
        Service-linked role that grants GuardDuty access to the resources in the AWS account.
    status:
      type: string
      description: |
        Current status of the detector.
  type: object
  required:
    - findingPublishingFrequency
    - id
    - serviceRoleArn
    - status
