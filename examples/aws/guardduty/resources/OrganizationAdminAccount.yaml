$token: aws:guardduty/organizationAdminAccount:OrganizationAdminAccount
properties:
  adminAccountId:
    type: string
    description: |
      AWS account identifier to designate as a delegated administrator for GuardDuty.
required:
  - adminAccountId
inputProperties:
  adminAccountId:
    type: string
    description: |
      AWS account identifier to designate as a delegated administrator for GuardDuty.
    willReplaceOnChanges: true
requiredInputs:
  - adminAccountId
stateInputs:
  description: |
    Input properties used for looking up and filtering OrganizationAdminAccount resources.
  properties:
    adminAccountId:
      type: string
      description: |
        AWS account identifier to designate as a delegated administrator for GuardDuty.
      willReplaceOnChanges: true
  type: object
