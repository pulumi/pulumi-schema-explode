$token: aws:guardduty/organizationConfiguration:OrganizationConfiguration
properties:
  autoEnable:
    type: boolean
    description: |
      Set to `true` if you want S3 data event logs to be automatically enabled for new members of the organization. Default: `false`
  datasources:
    $ref: "#/types/aws:guardduty/OrganizationConfigurationDatasources:OrganizationConfigurationDatasources"
    description: |
      Configuration for the collected datasources.
  detectorId:
    type: string
    description: |
      The detector ID of the GuardDuty account.
required:
  - autoEnable
  - datasources
  - detectorId
inputProperties:
  autoEnable:
    type: boolean
    description: |
      Set to `true` if you want S3 data event logs to be automatically enabled for new members of the organization. Default: `false`
  datasources:
    $ref: "#/types/aws:guardduty/OrganizationConfigurationDatasources:OrganizationConfigurationDatasources"
    description: |
      Configuration for the collected datasources.
  detectorId:
    type: string
    description: |
      The detector ID of the GuardDuty account.
    willReplaceOnChanges: true
requiredInputs:
  - autoEnable
  - detectorId
stateInputs:
  description: |
    Input properties used for looking up and filtering OrganizationConfiguration resources.
  properties:
    autoEnable:
      type: boolean
      description: |
        Set to `true` if you want S3 data event logs to be automatically enabled for new members of the organization. Default: `false`
    datasources:
      $ref: "#/types/aws:guardduty/OrganizationConfigurationDatasources:OrganizationConfigurationDatasources"
      description: |
        Configuration for the collected datasources.
    detectorId:
      type: string
      description: |
        The detector ID of the GuardDuty account.
      willReplaceOnChanges: true
  type: object
