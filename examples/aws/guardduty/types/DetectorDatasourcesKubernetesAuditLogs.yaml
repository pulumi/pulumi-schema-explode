$token: aws:guardduty/DetectorDatasourcesKubernetesAuditLogs:DetectorDatasourcesKubernetesAuditLogs
properties:
  enable:
    type: boolean
    description: |
      If true, enables Kubernetes audit logs as a data source for [Kubernetes protection](https://docs.aws.amazon.com/guardduty/latest/ug/kubernetes-protection.html).
      Defaults to `true`.
    language:
      python:
        mapCase: false
type: object
required:
  - enable
