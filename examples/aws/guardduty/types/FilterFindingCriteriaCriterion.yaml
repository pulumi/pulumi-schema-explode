$token: aws:guardduty/FilterFindingCriteriaCriterion:FilterFindingCriteriaCriterion
properties:
  equals:
    type: array
    items:
      type: string
    description: |
      List of string values to be evaluated.
    language:
      python:
        mapCase: false
  field:
    type: string
    description: |
      The name of the field to be evaluated. The full list of field names can be found in [AWS documentation](https://docs.aws.amazon.com/guardduty/latest/ug/guardduty_filter-findings.html#filter_criteria).
    language:
      python:
        mapCase: false
  greaterThan:
    type: string
    description: |
      A value to be evaluated. Accepts either an integer or a date in [RFC 3339 format](https://tools.ietf.org/html/rfc3339#section-5.8).
    language:
      python:
        mapCase: false
  greaterThanOrEqual:
    type: string
    description: |
      A value to be evaluated. Accepts either an integer or a date in [RFC 3339 format](https://tools.ietf.org/html/rfc3339#section-5.8).
    language:
      python:
        mapCase: false
  lessThan:
    type: string
    description: |
      A value to be evaluated. Accepts either an integer or a date in [RFC 3339 format](https://tools.ietf.org/html/rfc3339#section-5.8).
    language:
      python:
        mapCase: false
  lessThanOrEqual:
    type: string
    description: |
      A value to be evaluated. Accepts either an integer or a date in [RFC 3339 format](https://tools.ietf.org/html/rfc3339#section-5.8).
    language:
      python:
        mapCase: false
  notEquals:
    type: array
    items:
      type: string
    description: |
      List of string values to be evaluated.
    language:
      python:
        mapCase: false
type: object
required:
  - field
