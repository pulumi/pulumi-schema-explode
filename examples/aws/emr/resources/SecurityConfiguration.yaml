$token: aws:emr/securityConfiguration:SecurityConfiguration
properties:
  configuration:
    type: string
    description: |
      A JSON formatted Security Configuration
  creationDate:
    type: string
    description: |
      Date the Security Configuration was created
  name:
    type: string
    description: |
      The name of the EMR Security Configuration. By default generated by this provider.
  namePrefix:
    type: string
    description: |
      Creates a unique name beginning with the specified
      prefix. Conflicts with `name`.
required:
  - configuration
  - creationDate
  - name
inputProperties:
  configuration:
    type: string
    description: |
      A JSON formatted Security Configuration
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      The name of the EMR Security Configuration. By default generated by this provider.
    willReplaceOnChanges: true
  namePrefix:
    type: string
    description: |
      Creates a unique name beginning with the specified
      prefix. Conflicts with `name`.
    willReplaceOnChanges: true
requiredInputs:
  - configuration
stateInputs:
  description: |
    Input properties used for looking up and filtering SecurityConfiguration resources.
  properties:
    configuration:
      type: string
      description: |
        A JSON formatted Security Configuration
      willReplaceOnChanges: true
    creationDate:
      type: string
      description: |
        Date the Security Configuration was created
    name:
      type: string
      description: |
        The name of the EMR Security Configuration. By default generated by this provider.
      willReplaceOnChanges: true
    namePrefix:
      type: string
      description: |
        Creates a unique name beginning with the specified
        prefix. Conflicts with `name`.
      willReplaceOnChanges: true
  type: object
