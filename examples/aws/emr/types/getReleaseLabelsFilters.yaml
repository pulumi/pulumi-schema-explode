$token: aws:emr/getReleaseLabelsFilters:getReleaseLabelsFilters
properties:
  application:
    type: string
    description: |
      Optional release label application filter. For example, `Spark@2.1.0` or `Spark`.
    language:
      python:
        mapCase: false
  prefix:
    type: string
    description: |
      Optional release label version prefix filter. For example, `emr-5`.
    language:
      python:
        mapCase: false
type: object
