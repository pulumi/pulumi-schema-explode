$token: aws:emr/InstanceGroupEbsConfig:InstanceGroupEbsConfig
properties:
  iops:
    type: integer
    description: |
      The number of I/O operations per second (IOPS) that the volume supports.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  size:
    type: integer
    description: |
      The volume size, in gibibytes (GiB). This can be a number from 1 - 1024. If the volume type is EBS-optimized, the minimum value is 10.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  type:
    type: string
    description: |
      The volume type. Valid options are 'gp2', 'io1' and 'standard'.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  volumesPerInstance:
    type: integer
    description: |
      The number of EBS Volumes to attach per instance.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - size
  - type
