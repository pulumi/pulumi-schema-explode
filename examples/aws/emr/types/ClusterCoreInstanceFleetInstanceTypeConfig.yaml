$token: aws:emr/ClusterCoreInstanceFleetInstanceTypeConfig:ClusterCoreInstanceFleetInstanceTypeConfig
properties:
  bidPrice:
    type: string
    description: |
      Bid price for each EC2 instance in the instance group, expressed in USD. By setting this attribute, the instance group is being declared as a Spot Instance, and will implicitly create a Spot request. Leave this blank to use On-Demand Instances.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  bidPriceAsPercentageOfOnDemandPrice:
    type: number
    description: |
      Bid price, as a percentage of On-Demand price, for each EC2 Spot instance as defined by `instance_type`. Expressed as a number (for example, 20 specifies 20%). If neither `bid_price` nor `bid_price_as_percentage_of_on_demand_price` is provided, `bid_price_as_percentage_of_on_demand_price` defaults to 100%.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  configurations:
    type: array
    items:
      $ref: "#/types/aws:emr/ClusterCoreInstanceFleetInstanceTypeConfigConfiguration:ClusterCoreInstanceFleetInstanceTypeConfigConfiguration"
    description: |
      Configuration classification that applies when provisioning cluster instances, which can include configurations for applications and software that run on the cluster. List of `configuration` blocks.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  ebsConfigs:
    type: array
    items:
      $ref: "#/types/aws:emr/ClusterCoreInstanceFleetInstanceTypeConfigEbsConfig:ClusterCoreInstanceFleetInstanceTypeConfigEbsConfig"
    description: |
      Configuration block(s) for EBS volumes attached to each instance in the instance group. Detailed below.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  instanceType:
    type: string
    description: |
      EC2 instance type for all instances in the instance group.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  weightedCapacity:
    type: integer
    description: |
      Number of units that a provisioned instance of this type provides toward fulfilling the target capacities defined in `aws.emr.InstanceFleet`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - instanceType
language:
  nodejs:
    requiredOutputs:
      - ebsConfigs
      - instanceType
