$token: aws:emr/ClusterBootstrapAction:ClusterBootstrapAction
properties:
  args:
    type: array
    items:
      type: string
    description: |
      List of command line arguments passed to the JAR file's main function when executed.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      Name of the step.
    language:
      python:
        mapCase: false
  path:
    type: string
    description: |
      Location of the script to run during a bootstrap action. Can be either a location in Amazon S3 or on a local file system.
    language:
      python:
        mapCase: false
type: object
required:
  - name
  - path
