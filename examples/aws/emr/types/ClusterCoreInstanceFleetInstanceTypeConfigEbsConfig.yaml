$token: aws:emr/ClusterCoreInstanceFleetInstanceTypeConfigEbsConfig:ClusterCoreInstanceFleetInstanceTypeConfigEbsConfig
properties:
  iops:
    type: integer
    description: |
      Number of I/O operations per second (IOPS) that the volume supports.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  size:
    type: integer
    description: |
      Volume size, in gibibytes (GiB).
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  type:
    type: string
    description: |
      Volume type. Valid options are `gp3`, `gp2`, `io1`, `standard`, `st1` and `sc1`. See [EBS Volume Types](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/EBSVolumeTypes.html).
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  volumesPerInstance:
    type: integer
    description: |
      Number of EBS volumes with this configuration to attach to each EC2 instance in the instance group (default is 1).
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - size
  - type
