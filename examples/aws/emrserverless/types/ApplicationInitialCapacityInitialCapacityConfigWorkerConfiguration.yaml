$token: aws:emrserverless/ApplicationInitialCapacityInitialCapacityConfigWorkerConfiguration:ApplicationInitialCapacityInitialCapacityConfigWorkerConfiguration
properties:
  cpu:
    type: string
    description: |
      The CPU requirements for every worker instance of the worker type.
    language:
      python:
        mapCase: false
  disk:
    type: string
    description: |
      The disk requirements for every worker instance of the worker type.
    language:
      python:
        mapCase: false
  memory:
    type: string
    description: |
      The memory requirements for every worker instance of the worker type.
    language:
      python:
        mapCase: false
type: object
required:
  - cpu
  - memory
language:
  nodejs:
    requiredOutputs:
      - cpu
      - disk
      - memory
