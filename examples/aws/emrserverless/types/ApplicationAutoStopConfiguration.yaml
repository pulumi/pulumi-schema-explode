$token: aws:emrserverless/ApplicationAutoStopConfiguration:ApplicationAutoStopConfiguration
properties:
  enabled:
    type: boolean
    description: |
      Enables the application to automatically stop after a certain amount of time being idle. Defaults to `true`.
    language:
      python:
        mapCase: false
  idleTimeoutMinutes:
    type: integer
    description: |
      The amount of idle time in minutes after which your application will automatically stop. Defaults to `15` minutes.
    language:
      python:
        mapCase: false
type: object
