$token: aws:emrserverless/ApplicationInitialCapacityInitialCapacityConfig:ApplicationInitialCapacityInitialCapacityConfig
properties:
  workerConfiguration:
    $ref: "#/types/aws:emrserverless/ApplicationInitialCapacityInitialCapacityConfigWorkerConfiguration:ApplicationInitialCapacityInitialCapacityConfigWorkerConfiguration"
    description: |
      The resource configuration of the initial capacity configuration.
    language:
      python:
        mapCase: false
  workerCount:
    type: integer
    description: |
      The number of workers in the initial capacity configuration.
    language:
      python:
        mapCase: false
type: object
required:
  - workerCount
