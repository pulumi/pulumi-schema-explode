$token: aws:emrserverless/ApplicationInitialCapacity:ApplicationInitialCapacity
properties:
  initialCapacityConfig:
    $ref: "#/types/aws:emrserverless/ApplicationInitialCapacityInitialCapacityConfig:ApplicationInitialCapacityInitialCapacityConfig"
    description: |
      The initial capacity configuration per worker.
    language:
      python:
        mapCase: false
  initialCapacityType:
    type: string
    description: |
      The worker type for an analytics framework. For Spark applications, the key can either be set to `Driver` or `Executor`. For Hive applications, it can be set to `HiveDriver` or `TezTask`.
    language:
      python:
        mapCase: false
type: object
required:
  - initialCapacityType
