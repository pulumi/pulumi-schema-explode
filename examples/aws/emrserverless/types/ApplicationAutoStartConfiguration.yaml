$token: aws:emrserverless/ApplicationAutoStartConfiguration:ApplicationAutoStartConfiguration
properties:
  enabled:
    type: boolean
    description: |
      Enables the application to automatically stop after a certain amount of time being idle. Defaults to `true`.
    language:
      python:
        mapCase: false
type: object
