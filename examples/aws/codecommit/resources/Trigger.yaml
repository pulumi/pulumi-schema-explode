$token: aws:codecommit/trigger:Trigger
properties:
  configurationId:
    type: string
    description: |
      System-generated unique identifier.
  repositoryName:
    type: string
    description: |
      The name for the repository. This needs to be less than 100 characters.
  triggers:
    type: array
    items:
      $ref: "#/types/aws:codecommit/TriggerTrigger:TriggerTrigger"
required:
  - configurationId
  - repositoryName
  - triggers
inputProperties:
  repositoryName:
    type: string
    description: |
      The name for the repository. This needs to be less than 100 characters.
    willReplaceOnChanges: true
  triggers:
    type: array
    items:
      $ref: "#/types/aws:codecommit/TriggerTrigger:TriggerTrigger"
    willReplaceOnChanges: true
requiredInputs:
  - repositoryName
  - triggers
stateInputs:
  description: |
    Input properties used for looking up and filtering Trigger resources.
  properties:
    configurationId:
      type: string
      description: |
        System-generated unique identifier.
    repositoryName:
      type: string
      description: |
        The name for the repository. This needs to be less than 100 characters.
      willReplaceOnChanges: true
    triggers:
      type: array
      items:
        $ref: "#/types/aws:codecommit/TriggerTrigger:TriggerTrigger"
      willReplaceOnChanges: true
  type: object
