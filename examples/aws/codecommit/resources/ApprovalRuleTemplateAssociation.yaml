$token: aws:codecommit/approvalRuleTemplateAssociation:ApprovalRuleTemplateAssociation
properties:
  approvalRuleTemplateName:
    type: string
    description: |
      The name for the approval rule template.
  repositoryName:
    type: string
    description: |
      The name of the repository that you want to associate with the template.
required:
  - approvalRuleTemplateName
  - repositoryName
inputProperties:
  approvalRuleTemplateName:
    type: string
    description: |
      The name for the approval rule template.
    willReplaceOnChanges: true
  repositoryName:
    type: string
    description: |
      The name of the repository that you want to associate with the template.
    willReplaceOnChanges: true
requiredInputs:
  - approvalRuleTemplateName
  - repositoryName
stateInputs:
  description: |
    Input properties used for looking up and filtering ApprovalRuleTemplateAssociation resources.
  properties:
    approvalRuleTemplateName:
      type: string
      description: |
        The name for the approval rule template.
      willReplaceOnChanges: true
    repositoryName:
      type: string
      description: |
        The name of the repository that you want to associate with the template.
      willReplaceOnChanges: true
  type: object
