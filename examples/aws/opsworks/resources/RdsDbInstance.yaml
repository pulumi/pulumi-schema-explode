$token: aws:opsworks/rdsDbInstance:RdsDbInstance
properties:
  dbPassword:
    type: string
    description: |
      A db password
  dbUser:
    type: string
    description: |
      A db username
  rdsDbInstanceArn:
    type: string
    description: |
      The db instance to register for this stack. Changing this will force a new resource.
  stackId:
    type: string
    description: |
      The stack to register a db instance for. Changing this will force a new resource.
required:
  - dbPassword
  - dbUser
  - rdsDbInstanceArn
  - stackId
inputProperties:
  dbPassword:
    type: string
    description: |
      A db password
  dbUser:
    type: string
    description: |
      A db username
  rdsDbInstanceArn:
    type: string
    description: |
      The db instance to register for this stack. Changing this will force a new resource.
    willReplaceOnChanges: true
  stackId:
    type: string
    description: |
      The stack to register a db instance for. Changing this will force a new resource.
    willReplaceOnChanges: true
requiredInputs:
  - dbPassword
  - dbUser
  - rdsDbInstanceArn
  - stackId
stateInputs:
  description: |
    Input properties used for looking up and filtering RdsDbInstance resources.
  properties:
    dbPassword:
      type: string
      description: |
        A db password
    dbUser:
      type: string
      description: |
        A db username
    rdsDbInstanceArn:
      type: string
      description: |
        The db instance to register for this stack. Changing this will force a new resource.
      willReplaceOnChanges: true
    stackId:
      type: string
      description: |
        The stack to register a db instance for. Changing this will force a new resource.
      willReplaceOnChanges: true
  type: object
