$token: aws:opsworks/application:Application
properties:
  appSources:
    type: array
    items:
      $ref: "#/types/aws:opsworks/ApplicationAppSource:ApplicationAppSource"
    description: |
      SCM configuration of the app as described below.
  autoBundleOnDeploy:
    type: string
    description: |
      Run bundle install when deploying for application of type `rails`.
  awsFlowRubySettings:
    type: string
    description: |
      Specify activity and workflow workers for your app using the aws-flow gem.
  dataSourceArn:
    type: string
    description: |
      The data source's ARN.
  dataSourceDatabaseName:
    type: string
    description: |
      The database name.
  dataSourceType:
    type: string
    description: |
      The data source's type one of `AutoSelectOpsworksMysqlInstance`, `OpsworksMysqlInstance`, or `RdsDbInstance`.
  description:
    type: string
    description: |
      A description of the app.
  documentRoot:
    type: string
    description: |
      Subfolder for the document root for application of type `rails`.
  domains:
    type: array
    items:
      type: string
    description: |
      A list of virtual host alias.
  enableSsl:
    type: boolean
    description: |
      Whether to enable SSL for the app. This must be set in order to let `ssl_configuration.private_key`, `ssl_configuration.certificate` and `ssl_configuration.chain` take effect.
  environments:
    type: array
    items:
      $ref: "#/types/aws:opsworks/ApplicationEnvironment:ApplicationEnvironment"
    description: |
      Object to define environment variables.  Object is described below.
  name:
    type: string
    description: |
      A human-readable name for the application.
  railsEnv:
    type: string
    description: |
      The name of the Rails environment for application of type `rails`.
  shortName:
    type: string
    description: |
      A short, machine-readable name for the application. This can only be defined on resource creation and ignored on resource update.
  sslConfigurations:
    type: array
    items:
      $ref: "#/types/aws:opsworks/ApplicationSslConfiguration:ApplicationSslConfiguration"
    description: |
      The SSL configuration of the app. Object is described below.
  stackId:
    type: string
    description: |
      ID of the stack the application will belong to.
  type:
    type: string
    description: |
      Opsworks application type. One of `aws-flow-ruby`, `java`, `rails`, `php`, `nodejs`, `static` or `other`.
required:
  - appSources
  - name
  - shortName
  - stackId
  - type
inputProperties:
  appSources:
    type: array
    items:
      $ref: "#/types/aws:opsworks/ApplicationAppSource:ApplicationAppSource"
    description: |
      SCM configuration of the app as described below.
  autoBundleOnDeploy:
    type: string
    description: |
      Run bundle install when deploying for application of type `rails`.
  awsFlowRubySettings:
    type: string
    description: |
      Specify activity and workflow workers for your app using the aws-flow gem.
  dataSourceArn:
    type: string
    description: |
      The data source's ARN.
  dataSourceDatabaseName:
    type: string
    description: |
      The database name.
  dataSourceType:
    type: string
    description: |
      The data source's type one of `AutoSelectOpsworksMysqlInstance`, `OpsworksMysqlInstance`, or `RdsDbInstance`.
  description:
    type: string
    description: |
      A description of the app.
  documentRoot:
    type: string
    description: |
      Subfolder for the document root for application of type `rails`.
  domains:
    type: array
    items:
      type: string
    description: |
      A list of virtual host alias.
  enableSsl:
    type: boolean
    description: |
      Whether to enable SSL for the app. This must be set in order to let `ssl_configuration.private_key`, `ssl_configuration.certificate` and `ssl_configuration.chain` take effect.
  environments:
    type: array
    items:
      $ref: "#/types/aws:opsworks/ApplicationEnvironment:ApplicationEnvironment"
    description: |
      Object to define environment variables.  Object is described below.
  name:
    type: string
    description: |
      A human-readable name for the application.
  railsEnv:
    type: string
    description: |
      The name of the Rails environment for application of type `rails`.
  shortName:
    type: string
    description: |
      A short, machine-readable name for the application. This can only be defined on resource creation and ignored on resource update.
    willReplaceOnChanges: true
  sslConfigurations:
    type: array
    items:
      $ref: "#/types/aws:opsworks/ApplicationSslConfiguration:ApplicationSslConfiguration"
    description: |
      The SSL configuration of the app. Object is described below.
  stackId:
    type: string
    description: |
      ID of the stack the application will belong to.
  type:
    type: string
    description: |
      Opsworks application type. One of `aws-flow-ruby`, `java`, `rails`, `php`, `nodejs`, `static` or `other`.
requiredInputs:
  - stackId
  - type
stateInputs:
  description: |
    Input properties used for looking up and filtering Application resources.
  properties:
    appSources:
      type: array
      items:
        $ref: "#/types/aws:opsworks/ApplicationAppSource:ApplicationAppSource"
      description: |
        SCM configuration of the app as described below.
    autoBundleOnDeploy:
      type: string
      description: |
        Run bundle install when deploying for application of type `rails`.
    awsFlowRubySettings:
      type: string
      description: |
        Specify activity and workflow workers for your app using the aws-flow gem.
    dataSourceArn:
      type: string
      description: |
        The data source's ARN.
    dataSourceDatabaseName:
      type: string
      description: |
        The database name.
    dataSourceType:
      type: string
      description: |
        The data source's type one of `AutoSelectOpsworksMysqlInstance`, `OpsworksMysqlInstance`, or `RdsDbInstance`.
    description:
      type: string
      description: |
        A description of the app.
    documentRoot:
      type: string
      description: |
        Subfolder for the document root for application of type `rails`.
    domains:
      type: array
      items:
        type: string
      description: |
        A list of virtual host alias.
    enableSsl:
      type: boolean
      description: |
        Whether to enable SSL for the app. This must be set in order to let `ssl_configuration.private_key`, `ssl_configuration.certificate` and `ssl_configuration.chain` take effect.
    environments:
      type: array
      items:
        $ref: "#/types/aws:opsworks/ApplicationEnvironment:ApplicationEnvironment"
      description: |
        Object to define environment variables.  Object is described below.
    name:
      type: string
      description: |
        A human-readable name for the application.
    railsEnv:
      type: string
      description: |
        The name of the Rails environment for application of type `rails`.
    shortName:
      type: string
      description: |
        A short, machine-readable name for the application. This can only be defined on resource creation and ignored on resource update.
      willReplaceOnChanges: true
    sslConfigurations:
      type: array
      items:
        $ref: "#/types/aws:opsworks/ApplicationSslConfiguration:ApplicationSslConfiguration"
      description: |
        The SSL configuration of the app. Object is described below.
    stackId:
      type: string
      description: |
        ID of the stack the application will belong to.
    type:
      type: string
      description: |
        Opsworks application type. One of `aws-flow-ruby`, `java`, `rails`, `php`, `nodejs`, `static` or `other`.
  type: object
