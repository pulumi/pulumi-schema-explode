$token: aws:opsworks/userProfile:UserProfile
properties:
  allowSelfManagement:
    type: boolean
    description: |
      Whether users can specify their own SSH public key through the My Settings page
  sshPublicKey:
    type: string
    description: |
      The users public key
  sshUsername:
    type: string
    description: |
      The ssh username, with witch this user wants to log in
  userArn:
    type: string
    description: |
      The user's IAM ARN
required:
  - sshUsername
  - userArn
inputProperties:
  allowSelfManagement:
    type: boolean
    description: |
      Whether users can specify their own SSH public key through the My Settings page
  sshPublicKey:
    type: string
    description: |
      The users public key
  sshUsername:
    type: string
    description: |
      The ssh username, with witch this user wants to log in
  userArn:
    type: string
    description: |
      The user's IAM ARN
    willReplaceOnChanges: true
requiredInputs:
  - sshUsername
  - userArn
stateInputs:
  description: |
    Input properties used for looking up and filtering UserProfile resources.
  properties:
    allowSelfManagement:
      type: boolean
      description: |
        Whether users can specify their own SSH public key through the My Settings page
    sshPublicKey:
      type: string
      description: |
        The users public key
    sshUsername:
      type: string
      description: |
        The ssh username, with witch this user wants to log in
    userArn:
      type: string
      description: |
        The user's IAM ARN
      willReplaceOnChanges: true
  type: object
