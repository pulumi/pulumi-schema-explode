$token: aws:opsworks/ecsClusterLayer:EcsClusterLayer
properties:
  arn:
    type: string
    description: |
      The Amazon Resource Name(ARN) of the layer.
  autoAssignElasticIps:
    type: boolean
    description: |
      Whether to automatically assign an elastic IP address to the layer's instances.
  autoAssignPublicIps:
    type: boolean
    description: |
      For stacks belonging to a VPC, whether to automatically assign a public IP address to each of the layer's instances.
  autoHealing:
    type: boolean
    description: |
      Whether to enable auto-healing for the layer.
  cloudwatchConfiguration:
    $ref: "#/types/aws:opsworks/EcsClusterLayerCloudwatchConfiguration:EcsClusterLayerCloudwatchConfiguration"
  customConfigureRecipes:
    type: array
    items:
      type: string
  customDeployRecipes:
    type: array
    items:
      type: string
  customInstanceProfileArn:
    type: string
    description: |
      The ARN of an IAM profile that will be used for the layer's instances.
  customJson:
    type: string
    description: |
      Custom JSON attributes to apply to the layer.
  customSecurityGroupIds:
    type: array
    items:
      type: string
    description: |
      Ids for a set of security groups to apply to the layer's instances.
  customSetupRecipes:
    type: array
    items:
      type: string
  customShutdownRecipes:
    type: array
    items:
      type: string
  customUndeployRecipes:
    type: array
    items:
      type: string
  drainElbOnShutdown:
    type: boolean
    description: |
      Whether to enable Elastic Load Balancing connection draining.
  ebsVolumes:
    type: array
    items:
      $ref: "#/types/aws:opsworks/EcsClusterLayerEbsVolume:EcsClusterLayerEbsVolume"
    description: |
      `ebs_volume` blocks, as described below, will each create an EBS volume and connect it to the layer's instances.
  ecsClusterArn:
    type: string
    description: |
      The ECS Cluster ARN of the layer.
  elasticLoadBalancer:
    type: string
    description: |
      Name of an Elastic Load Balancer to attach to this layer
  installUpdatesOnBoot:
    type: boolean
    description: |
      Whether to install OS and package updates on each instance when it boots.
  instanceShutdownTimeout:
    type: integer
    description: |
      The time, in seconds, that OpsWorks will wait for Chef to complete after triggering the Shutdown event.
  name:
    type: string
    description: |
      A human-readable name for the layer.
  stackId:
    type: string
    description: |
      ID of the stack the layer will belong to.
  systemPackages:
    type: array
    items:
      type: string
    description: |
      Names of a set of system packages to install on the layer's instances.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A mapping of tags to assign to the resource.
  tagsAll:
    type: object
    additionalProperties:
      type: string
  useEbsOptimizedInstances:
    type: boolean
    description: |
      Whether to use EBS-optimized instances.
required:
  - arn
  - ebsVolumes
  - ecsClusterArn
  - name
  - stackId
  - tagsAll
inputProperties:
  autoAssignElasticIps:
    type: boolean
    description: |
      Whether to automatically assign an elastic IP address to the layer's instances.
  autoAssignPublicIps:
    type: boolean
    description: |
      For stacks belonging to a VPC, whether to automatically assign a public IP address to each of the layer's instances.
  autoHealing:
    type: boolean
    description: |
      Whether to enable auto-healing for the layer.
  cloudwatchConfiguration:
    $ref: "#/types/aws:opsworks/EcsClusterLayerCloudwatchConfiguration:EcsClusterLayerCloudwatchConfiguration"
  customConfigureRecipes:
    type: array
    items:
      type: string
  customDeployRecipes:
    type: array
    items:
      type: string
  customInstanceProfileArn:
    type: string
    description: |
      The ARN of an IAM profile that will be used for the layer's instances.
  customJson:
    type: string
    description: |
      Custom JSON attributes to apply to the layer.
  customSecurityGroupIds:
    type: array
    items:
      type: string
    description: |
      Ids for a set of security groups to apply to the layer's instances.
  customSetupRecipes:
    type: array
    items:
      type: string
  customShutdownRecipes:
    type: array
    items:
      type: string
  customUndeployRecipes:
    type: array
    items:
      type: string
  drainElbOnShutdown:
    type: boolean
    description: |
      Whether to enable Elastic Load Balancing connection draining.
  ebsVolumes:
    type: array
    items:
      $ref: "#/types/aws:opsworks/EcsClusterLayerEbsVolume:EcsClusterLayerEbsVolume"
    description: |
      `ebs_volume` blocks, as described below, will each create an EBS volume and connect it to the layer's instances.
  ecsClusterArn:
    type: string
    description: |
      The ECS Cluster ARN of the layer.
    willReplaceOnChanges: true
  elasticLoadBalancer:
    type: string
    description: |
      Name of an Elastic Load Balancer to attach to this layer
  installUpdatesOnBoot:
    type: boolean
    description: |
      Whether to install OS and package updates on each instance when it boots.
  instanceShutdownTimeout:
    type: integer
    description: |
      The time, in seconds, that OpsWorks will wait for Chef to complete after triggering the Shutdown event.
  name:
    type: string
    description: |
      A human-readable name for the layer.
  stackId:
    type: string
    description: |
      ID of the stack the layer will belong to.
    willReplaceOnChanges: true
  systemPackages:
    type: array
    items:
      type: string
    description: |
      Names of a set of system packages to install on the layer's instances.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A mapping of tags to assign to the resource.
  useEbsOptimizedInstances:
    type: boolean
    description: |
      Whether to use EBS-optimized instances.
requiredInputs:
  - ecsClusterArn
  - stackId
stateInputs:
  description: |
    Input properties used for looking up and filtering EcsClusterLayer resources.
  properties:
    arn:
      type: string
      description: |
        The Amazon Resource Name(ARN) of the layer.
    autoAssignElasticIps:
      type: boolean
      description: |
        Whether to automatically assign an elastic IP address to the layer's instances.
    autoAssignPublicIps:
      type: boolean
      description: |
        For stacks belonging to a VPC, whether to automatically assign a public IP address to each of the layer's instances.
    autoHealing:
      type: boolean
      description: |
        Whether to enable auto-healing for the layer.
    cloudwatchConfiguration:
      $ref: "#/types/aws:opsworks/EcsClusterLayerCloudwatchConfiguration:EcsClusterLayerCloudwatchConfiguration"
    customConfigureRecipes:
      type: array
      items:
        type: string
    customDeployRecipes:
      type: array
      items:
        type: string
    customInstanceProfileArn:
      type: string
      description: |
        The ARN of an IAM profile that will be used for the layer's instances.
    customJson:
      type: string
      description: |
        Custom JSON attributes to apply to the layer.
    customSecurityGroupIds:
      type: array
      items:
        type: string
      description: |
        Ids for a set of security groups to apply to the layer's instances.
    customSetupRecipes:
      type: array
      items:
        type: string
    customShutdownRecipes:
      type: array
      items:
        type: string
    customUndeployRecipes:
      type: array
      items:
        type: string
    drainElbOnShutdown:
      type: boolean
      description: |
        Whether to enable Elastic Load Balancing connection draining.
    ebsVolumes:
      type: array
      items:
        $ref: "#/types/aws:opsworks/EcsClusterLayerEbsVolume:EcsClusterLayerEbsVolume"
      description: |
        `ebs_volume` blocks, as described below, will each create an EBS volume and connect it to the layer's instances.
    ecsClusterArn:
      type: string
      description: |
        The ECS Cluster ARN of the layer.
      willReplaceOnChanges: true
    elasticLoadBalancer:
      type: string
      description: |
        Name of an Elastic Load Balancer to attach to this layer
    installUpdatesOnBoot:
      type: boolean
      description: |
        Whether to install OS and package updates on each instance when it boots.
    instanceShutdownTimeout:
      type: integer
      description: |
        The time, in seconds, that OpsWorks will wait for Chef to complete after triggering the Shutdown event.
    name:
      type: string
      description: |
        A human-readable name for the layer.
    stackId:
      type: string
      description: |
        ID of the stack the layer will belong to.
      willReplaceOnChanges: true
    systemPackages:
      type: array
      items:
        type: string
      description: |
        Names of a set of system packages to install on the layer's instances.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A mapping of tags to assign to the resource.
    tagsAll:
      type: object
      additionalProperties:
        type: string
    useEbsOptimizedInstances:
      type: boolean
      description: |
        Whether to use EBS-optimized instances.
  type: object
