$token: aws:opsworks/GangliaLayerEbsVolume:GangliaLayerEbsVolume
properties:
  encrypted:
    type: boolean
    language:
      python:
        mapCase: false
  iops:
    type: integer
    description: |
      For PIOPS volumes, the IOPS per disk.
    language:
      python:
        mapCase: false
  mountPoint:
    type: string
    description: |
      The path to mount the EBS volume on the layer's instances.
    language:
      python:
        mapCase: false
  numberOfDisks:
    type: integer
    description: |
      The number of disks to use for the EBS volume.
    language:
      python:
        mapCase: false
  raidLevel:
    type: string
    description: |
      The RAID level to use for the volume.
    language:
      python:
        mapCase: false
  size:
    type: integer
    description: |
      The size of the volume in gigabytes.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      The type of volume to create. This may be `standard` (the default), `io1` or `gp2`.
    language:
      python:
        mapCase: false
type: object
required:
  - mountPoint
  - numberOfDisks
  - size
