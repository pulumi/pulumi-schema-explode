$token: aws:opsworks/ApplicationAppSource:ApplicationAppSource
properties:
  password:
    type: string
    description: |
      Password to use when authenticating to the source. This provider cannot perform drift detection of this configuration.
    language:
      python:
        mapCase: false
  revision:
    type: string
    description: |
      For sources that are version-aware, the revision to use.
    language:
      python:
        mapCase: false
  sshKey:
    type: string
    description: |
      SSH key to use when authenticating to the source. This provider cannot perform drift detection of this configuration.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      The type of source to use. For example, "archive".
    language:
      python:
        mapCase: false
  url:
    type: string
    description: |
      The URL where the app resource can be found.
    language:
      python:
        mapCase: false
  username:
    type: string
    description: |
      Username to use when authenticating to the source.
    language:
      python:
        mapCase: false
type: object
required:
  - type
