$token: aws:opsworks/CustomLayerCloudwatchConfiguration:CustomLayerCloudwatchConfiguration
properties:
  enabled:
    type: boolean
    language:
      python:
        mapCase: false
  logStreams:
    type: array
    items:
      $ref: "#/types/aws:opsworks/CustomLayerCloudwatchConfigurationLogStream:CustomLayerCloudwatchConfigurationLogStream"
    description: |
      A block the specifies how an opsworks logs look like. See Log Streams.
    language:
      python:
        mapCase: false
type: object
