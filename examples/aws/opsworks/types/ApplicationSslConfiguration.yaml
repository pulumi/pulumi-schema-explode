$token: aws:opsworks/ApplicationSslConfiguration:ApplicationSslConfiguration
properties:
  certificate:
    type: string
    description: |
      The contents of the certificate's domain.crt file.
    language:
      python:
        mapCase: false
  chain:
    type: string
    description: |
      Can be used to specify an intermediate certificate authority key or client authentication.
    language:
      python:
        mapCase: false
  privateKey:
    type: string
    description: |
      The private key; the contents of the certificate's domain.key file.
    language:
      python:
        mapCase: false
type: object
required:
  - certificate
  - privateKey
