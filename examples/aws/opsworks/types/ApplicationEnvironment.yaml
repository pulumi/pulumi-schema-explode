$token: aws:opsworks/ApplicationEnvironment:ApplicationEnvironment
properties:
  key:
    type: string
    description: |
      Variable name.
    language:
      python:
        mapCase: false
  secure:
    type: boolean
    description: |
      Set visibility of the variable value to `true` or `false`.
    language:
      python:
        mapCase: false
  value:
    type: string
    description: |
      Variable value.
    language:
      python:
        mapCase: false
type: object
required:
  - key
  - value
