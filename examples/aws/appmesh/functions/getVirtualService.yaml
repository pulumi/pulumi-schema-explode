$token: aws:appmesh/getVirtualService:getVirtualService
inputs:
  description: |
    A collection of arguments for invoking getVirtualService.
  properties:
    meshName:
      type: string
      description: |
        Name of the service mesh in which the virtual service exists.
    meshOwner:
      type: string
      description: |
        AWS account ID of the service mesh's owner.
    name:
      type: string
      description: |
        Name of the virtual service.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags.
  type: object
  required:
    - meshName
    - name
outputs:
  description: |
    A collection of values returned by getVirtualService.
  properties:
    arn:
      type: string
      description: |
        ARN of the virtual service.
    createdDate:
      type: string
      description: |
        Creation date of the virtual service.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    lastUpdatedDate:
      type: string
      description: |
        Last update date of the virtual service.
    meshName:
      type: string
    meshOwner:
      type: string
    name:
      type: string
    resourceOwner:
      type: string
      description: |
        Resource owner's AWS account ID.
    specs:
      type: array
      items:
        $ref: "#/types/aws:appmesh/getVirtualServiceSpec:getVirtualServiceSpec"
      description: |
        Virtual service specification
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags.
  type: object
  required:
    - arn
    - createdDate
    - lastUpdatedDate
    - meshName
    - meshOwner
    - name
    - resourceOwner
    - specs
    - id
