$token: aws:appmesh/VirtualNodeSpecBackendDefaultsClientPolicyTls:VirtualNodeSpecBackendDefaultsClientPolicyTls
properties:
  certificate:
    $ref: "#/types/aws:appmesh/VirtualNodeSpecBackendDefaultsClientPolicyTlsCertificate:VirtualNodeSpecBackendDefaultsClientPolicyTlsCertificate"
    description: |
      Listener's TLS certificate.
    language:
      python:
        mapCase: false
  enforce:
    type: boolean
    description: |
      Whether the policy is enforced. Default is `true`.
    language:
      python:
        mapCase: false
  ports:
    type: array
    items:
      type: integer
    description: |
      One or more ports that the policy is enforced for.
    language:
      python:
        mapCase: false
  validation:
    $ref: "#/types/aws:appmesh/VirtualNodeSpecBackendDefaultsClientPolicyTlsValidation:VirtualNodeSpecBackendDefaultsClientPolicyTlsValidation"
    description: |
      Listener's Transport Layer Security (TLS) validation context.
    language:
      python:
        mapCase: false
type: object
required:
  - validation
