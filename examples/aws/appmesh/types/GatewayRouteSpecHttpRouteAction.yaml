$token: aws:appmesh/GatewayRouteSpecHttpRouteAction:GatewayRouteSpecHttpRouteAction
properties:
  rewrite:
    $ref: "#/types/aws:appmesh/GatewayRouteSpecHttpRouteActionRewrite:GatewayRouteSpecHttpRouteActionRewrite"
    description: |
      Gateway route action to rewrite.
    language:
      python:
        mapCase: false
  target:
    $ref: "#/types/aws:appmesh/GatewayRouteSpecHttpRouteActionTarget:GatewayRouteSpecHttpRouteActionTarget"
    description: |
      Target that traffic is routed to when a request matches the gateway route.
    language:
      python:
        mapCase: false
type: object
required:
  - target
