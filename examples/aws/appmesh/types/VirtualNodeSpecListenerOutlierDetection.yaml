$token: aws:appmesh/VirtualNodeSpecListenerOutlierDetection:VirtualNodeSpecListenerOutlierDetection
properties:
  baseEjectionDuration:
    $ref: "#/types/aws:appmesh/VirtualNodeSpecListenerOutlierDetectionBaseEjectionDuration:VirtualNodeSpecListenerOutlierDetectionBaseEjectionDuration"
    description: |
      Base amount of time for which a host is ejected.
    language:
      python:
        mapCase: false
  interval:
    $ref: "#/types/aws:appmesh/VirtualNodeSpecListenerOutlierDetectionInterval:VirtualNodeSpecListenerOutlierDetectionInterval"
    description: |
      Time interval between ejection sweep analysis.
    language:
      python:
        mapCase: false
  maxEjectionPercent:
    type: integer
    description: |
      Maximum percentage of hosts in load balancing pool for upstream service that can be ejected. Will eject at least one host regardless of the value.
      Minimum value of `0`. Maximum value of `100`.
    language:
      python:
        mapCase: false
  maxServerErrors:
    type: integer
    description: |
      Number of consecutive `5xx` errors required for ejection. Minimum value of `1`.
    language:
      python:
        mapCase: false
type: object
required:
  - baseEjectionDuration
  - interval
  - maxEjectionPercent
  - maxServerErrors
