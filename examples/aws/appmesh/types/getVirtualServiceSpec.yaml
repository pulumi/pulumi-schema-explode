$token: aws:appmesh/getVirtualServiceSpec:getVirtualServiceSpec
properties:
  providers:
    type: array
    items:
      $ref: "#/types/aws:appmesh/getVirtualServiceSpecProvider:getVirtualServiceSpecProvider"
    description: |
      App Mesh object that is acting as the provider for a virtual service.
    language:
      python:
        mapCase: false
type: object
required:
  - providers
language:
  nodejs:
    requiredInputs: []
