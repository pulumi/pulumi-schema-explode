$token: aws:appmesh/GatewayRouteSpecHttpRoute:GatewayRouteSpecHttpRoute
properties:
  action:
    $ref: "#/types/aws:appmesh/GatewayRouteSpecHttpRouteAction:GatewayRouteSpecHttpRouteAction"
    description: |
      Action to take if a match is determined.
    language:
      python:
        mapCase: false
  match:
    $ref: "#/types/aws:appmesh/GatewayRouteSpecHttpRouteMatch:GatewayRouteSpecHttpRouteMatch"
    description: |
      Criteria for determining a request match.
    language:
      python:
        mapCase: false
type: object
required:
  - action
  - match
