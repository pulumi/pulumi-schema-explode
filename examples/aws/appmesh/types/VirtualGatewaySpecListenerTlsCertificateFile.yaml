$token: aws:appmesh/VirtualGatewaySpecListenerTlsCertificateFile:VirtualGatewaySpecListenerTlsCertificateFile
properties:
  certificateChain:
    type: string
    description: |
      Certificate trust chain for a certificate stored on the file system of the mesh endpoint that the proxy is running on. Must be between 1 and 255 characters in length.
    language:
      python:
        mapCase: false
  privateKey:
    type: string
    description: |
      Private key for a certificate stored on the file system of the mesh endpoint that the proxy is running on. Must be between 1 and 255 characters in length.
    language:
      python:
        mapCase: false
type: object
required:
  - certificateChain
  - privateKey
