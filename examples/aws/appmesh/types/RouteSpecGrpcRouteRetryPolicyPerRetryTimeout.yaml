$token: aws:appmesh/RouteSpecGrpcRouteRetryPolicyPerRetryTimeout:RouteSpecGrpcRouteRetryPolicyPerRetryTimeout
properties:
  unit:
    type: string
    description: |
      Retry unit. Valid values: `ms`, `s`.
    language:
      python:
        mapCase: false
  value:
    type: integer
    description: |
      Retry value.
    language:
      python:
        mapCase: false
type: object
required:
  - unit
  - value
