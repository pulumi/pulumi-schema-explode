$token: aws:appmesh/VirtualGatewaySpecListenerPortMapping:VirtualGatewaySpecListenerPortMapping
properties:
  port:
    type: integer
    description: |
      Port used for the port mapping.
    language:
      python:
        mapCase: false
  protocol:
    type: string
    description: |
      Protocol used for the port mapping. Valid values are `http`, `http2`, `tcp` and `grpc`.
    language:
      python:
        mapCase: false
type: object
required:
  - port
  - protocol
