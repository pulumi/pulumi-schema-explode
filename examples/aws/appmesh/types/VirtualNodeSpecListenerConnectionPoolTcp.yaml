$token: aws:appmesh/VirtualNodeSpecListenerConnectionPoolTcp:VirtualNodeSpecListenerConnectionPoolTcp
properties:
  maxConnections:
    type: integer
    description: |
      Maximum number of outbound TCP connections Envoy can establish concurrently with all hosts in upstream cluster. Minimum value of `1`.
    language:
      python:
        mapCase: false
type: object
required:
  - maxConnections
