$token: aws:appmesh/VirtualNodeSpecListenerTimeout:VirtualNodeSpecListenerTimeout
properties:
  grpc:
    $ref: "#/types/aws:appmesh/VirtualNodeSpecListenerTimeoutGrpc:VirtualNodeSpecListenerTimeoutGrpc"
    description: |
      Timeouts for gRPC listeners.
    language:
      python:
        mapCase: false
  http:
    $ref: "#/types/aws:appmesh/VirtualNodeSpecListenerTimeoutHttp:VirtualNodeSpecListenerTimeoutHttp"
    description: |
      Timeouts for HTTP listeners.
    language:
      python:
        mapCase: false
  http2:
    $ref: "#/types/aws:appmesh/VirtualNodeSpecListenerTimeoutHttp2:VirtualNodeSpecListenerTimeoutHttp2"
    description: |
      Timeouts for HTTP2 listeners.
    language:
      python:
        mapCase: false
  tcp:
    $ref: "#/types/aws:appmesh/VirtualNodeSpecListenerTimeoutTcp:VirtualNodeSpecListenerTimeoutTcp"
    description: |
      Timeouts for TCP listeners.
    language:
      python:
        mapCase: false
type: object
