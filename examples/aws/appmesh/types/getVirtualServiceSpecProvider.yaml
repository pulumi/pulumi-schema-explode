$token: aws:appmesh/getVirtualServiceSpecProvider:getVirtualServiceSpecProvider
properties:
  virtualNodes:
    type: array
    items:
      $ref: "#/types/aws:appmesh/getVirtualServiceSpecProviderVirtualNode:getVirtualServiceSpecProviderVirtualNode"
    description: |
      Virtual node associated with the virtual service.
    language:
      python:
        mapCase: false
  virtualRouters:
    type: array
    items:
      $ref: "#/types/aws:appmesh/getVirtualServiceSpecProviderVirtualRouter:getVirtualServiceSpecProviderVirtualRouter"
    description: |
      Virtual router associated with the virtual service.
    language:
      python:
        mapCase: false
type: object
required:
  - virtualNodes
  - virtualRouters
language:
  nodejs:
    requiredInputs: []
