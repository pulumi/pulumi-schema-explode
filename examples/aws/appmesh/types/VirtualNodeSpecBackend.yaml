$token: aws:appmesh/VirtualNodeSpecBackend:VirtualNodeSpecBackend
properties:
  virtualService:
    $ref: "#/types/aws:appmesh/VirtualNodeSpecBackendVirtualService:VirtualNodeSpecBackendVirtualService"
    description: |
      Virtual service to use as a backend for a virtual node.
    language:
      python:
        mapCase: false
type: object
required:
  - virtualService
