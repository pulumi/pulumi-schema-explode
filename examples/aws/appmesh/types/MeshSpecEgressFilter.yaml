$token: aws:appmesh/MeshSpecEgressFilter:MeshSpecEgressFilter
properties:
  type:
    type: string
    description: |
      Egress filter type. By default, the type is `DROP_ALL`.
      Valid values are `ALLOW_ALL` and `DROP_ALL`.
    language:
      python:
        mapCase: false
type: object
