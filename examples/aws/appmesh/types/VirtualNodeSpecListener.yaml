$token: aws:appmesh/VirtualNodeSpecListener:VirtualNodeSpecListener
properties:
  connectionPool:
    $ref: "#/types/aws:appmesh/VirtualNodeSpecListenerConnectionPool:VirtualNodeSpecListenerConnectionPool"
    description: |
      Connection pool information for the listener.
    language:
      python:
        mapCase: false
  healthCheck:
    $ref: "#/types/aws:appmesh/VirtualNodeSpecListenerHealthCheck:VirtualNodeSpecListenerHealthCheck"
    description: |
      Health check information for the listener.
    language:
      python:
        mapCase: false
  outlierDetection:
    $ref: "#/types/aws:appmesh/VirtualNodeSpecListenerOutlierDetection:VirtualNodeSpecListenerOutlierDetection"
    description: |
      Outlier detection information for the listener.
    language:
      python:
        mapCase: false
  portMapping:
    $ref: "#/types/aws:appmesh/VirtualNodeSpecListenerPortMapping:VirtualNodeSpecListenerPortMapping"
    description: |
      Port mapping information for the listener.
    language:
      python:
        mapCase: false
  timeout:
    $ref: "#/types/aws:appmesh/VirtualNodeSpecListenerTimeout:VirtualNodeSpecListenerTimeout"
    description: |
      Timeouts for different protocols.
    language:
      python:
        mapCase: false
  tls:
    $ref: "#/types/aws:appmesh/VirtualNodeSpecListenerTls:VirtualNodeSpecListenerTls"
    description: |
      Transport Layer Security (TLS) properties for the listener
    language:
      python:
        mapCase: false
type: object
required:
  - portMapping
