$token: aws:appmesh/VirtualNodeSpecBackendDefaults:VirtualNodeSpecBackendDefaults
properties:
  clientPolicy:
    $ref: "#/types/aws:appmesh/VirtualNodeSpecBackendDefaultsClientPolicy:VirtualNodeSpecBackendDefaultsClientPolicy"
    description: |
      Default client policy for virtual service backends. See above for details.
    language:
      python:
        mapCase: false
type: object
