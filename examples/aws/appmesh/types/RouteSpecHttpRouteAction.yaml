$token: aws:appmesh/RouteSpecHttpRouteAction:RouteSpecHttpRouteAction
properties:
  weightedTargets:
    type: array
    items:
      $ref: "#/types/aws:appmesh/RouteSpecHttpRouteActionWeightedTarget:RouteSpecHttpRouteActionWeightedTarget"
    description: |
      Targets that traffic is routed to when a request matches the route.
      You can specify one or more targets and their relative weights with which to distribute traffic.
    language:
      python:
        mapCase: false
type: object
required:
  - weightedTargets
