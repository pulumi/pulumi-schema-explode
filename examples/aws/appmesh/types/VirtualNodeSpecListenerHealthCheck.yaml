$token: aws:appmesh/VirtualNodeSpecListenerHealthCheck:VirtualNodeSpecListenerHealthCheck
properties:
  healthyThreshold:
    type: integer
    description: |
      Number of consecutive successful health checks that must occur before declaring listener healthy.
    language:
      python:
        mapCase: false
  intervalMillis:
    type: integer
    description: |
      Time period in milliseconds between each health check execution.
    language:
      python:
        mapCase: false
  path:
    type: string
    description: |
      Destination path for the health check request. This is only required if the specified protocol is `http` or `http2`.
    language:
      python:
        mapCase: false
  port:
    type: integer
    description: |
      Destination port for the health check request. This port must match the port defined in the `port_mapping` for the listener.
    language:
      python:
        mapCase: false
  protocol:
    type: string
    description: |
      Protocol for the health check request. Valid values are `http`, `http2`, `tcp` and `grpc`.
    language:
      python:
        mapCase: false
  timeoutMillis:
    type: integer
    description: |
      Amount of time to wait when receiving a response from the health check, in milliseconds.
    language:
      python:
        mapCase: false
  unhealthyThreshold:
    type: integer
    description: |
      Number of consecutive failed health checks that must occur before declaring a virtual node unhealthy.
    language:
      python:
        mapCase: false
type: object
required:
  - healthyThreshold
  - intervalMillis
  - protocol
  - timeoutMillis
  - unhealthyThreshold
language:
  nodejs:
    requiredOutputs:
      - healthyThreshold
      - intervalMillis
      - port
      - protocol
      - timeoutMillis
      - unhealthyThreshold
