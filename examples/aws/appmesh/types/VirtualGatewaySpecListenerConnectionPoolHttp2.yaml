$token: aws:appmesh/VirtualGatewaySpecListenerConnectionPoolHttp2:VirtualGatewaySpecListenerConnectionPoolHttp2
properties:
  maxRequests:
    type: integer
    description: |
      Maximum number of inflight requests Envoy can concurrently support across hosts in upstream cluster. Minimum value of `1`.
    language:
      python:
        mapCase: false
type: object
required:
  - maxRequests
