$token: aws:appmesh/VirtualGatewaySpec:VirtualGatewaySpec
properties:
  backendDefaults:
    $ref: "#/types/aws:appmesh/VirtualGatewaySpecBackendDefaults:VirtualGatewaySpecBackendDefaults"
    description: |
      Defaults for backends.
    language:
      python:
        mapCase: false
  listener:
    $ref: "#/types/aws:appmesh/VirtualGatewaySpecListener:VirtualGatewaySpecListener"
    description: |
      Listeners that the mesh endpoint is expected to receive inbound traffic from. You can specify one listener.
    language:
      python:
        mapCase: false
  logging:
    $ref: "#/types/aws:appmesh/VirtualGatewaySpecLogging:VirtualGatewaySpecLogging"
    description: |
      Inbound and outbound access logging information for the virtual gateway.
    language:
      python:
        mapCase: false
type: object
required:
  - listener
