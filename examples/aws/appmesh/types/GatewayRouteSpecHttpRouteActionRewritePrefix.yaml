$token: aws:appmesh/GatewayRouteSpecHttpRouteActionRewritePrefix:GatewayRouteSpecHttpRouteActionRewritePrefix
properties:
  defaultPrefix:
    type: string
    description: |
      Default prefix used to replace the incoming route prefix when rewritten. Valid values: `ENABLED`, `DISABLED`.
    language:
      python:
        mapCase: false
  value:
    type: string
    description: |
      Value used to replace the incoming route prefix when rewritten.
    language:
      python:
        mapCase: false
type: object
