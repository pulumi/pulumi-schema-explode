$token: aws:appmesh/VirtualGatewaySpecListenerConnectionPoolHttp:VirtualGatewaySpecListenerConnectionPoolHttp
properties:
  maxConnections:
    type: integer
    description: |
      Maximum number of outbound TCP connections Envoy can establish concurrently with all hosts in upstream cluster. Minimum value of `1`.
    language:
      python:
        mapCase: false
  maxPendingRequests:
    type: integer
    description: |
      Number of overflowing requests after `max_connections` Envoy will queue to upstream cluster. Minimum value of `1`.
    language:
      python:
        mapCase: false
type: object
required:
  - maxConnections
