$token: aws:appmesh/VirtualNodeSpecBackendVirtualService:VirtualNodeSpecBackendVirtualService
properties:
  clientPolicy:
    $ref: "#/types/aws:appmesh/VirtualNodeSpecBackendVirtualServiceClientPolicy:VirtualNodeSpecBackendVirtualServiceClientPolicy"
    description: |
      Client policy for the backend.
    language:
      python:
        mapCase: false
  virtualServiceName:
    type: string
    description: |
      Name of the virtual service that is acting as a virtual node backend. Must be between 1 and 255 characters in length.
    language:
      python:
        mapCase: false
type: object
required:
  - virtualServiceName
