$token: aws:appmesh/VirtualNodeSpec:VirtualNodeSpec
properties:
  backendDefaults:
    $ref: "#/types/aws:appmesh/VirtualNodeSpecBackendDefaults:VirtualNodeSpecBackendDefaults"
    description: |
      Defaults for backends.
    language:
      python:
        mapCase: false
  backends:
    type: array
    items:
      $ref: "#/types/aws:appmesh/VirtualNodeSpecBackend:VirtualNodeSpecBackend"
    description: |
      Backends to which the virtual node is expected to send outbound traffic.
    language:
      python:
        mapCase: false
  listener:
    $ref: "#/types/aws:appmesh/VirtualNodeSpecListener:VirtualNodeSpecListener"
    description: |
      Listeners from which the virtual node is expected to receive inbound traffic.
    language:
      python:
        mapCase: false
  logging:
    $ref: "#/types/aws:appmesh/VirtualNodeSpecLogging:VirtualNodeSpecLogging"
    description: |
      Inbound and outbound access logging information for the virtual node.
    language:
      python:
        mapCase: false
  serviceDiscovery:
    $ref: "#/types/aws:appmesh/VirtualNodeSpecServiceDiscovery:VirtualNodeSpecServiceDiscovery"
    description: |
      Service discovery information for the virtual node.
    language:
      python:
        mapCase: false
type: object
