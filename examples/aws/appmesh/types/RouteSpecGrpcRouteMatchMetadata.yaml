$token: aws:appmesh/RouteSpecGrpcRouteMatchMetadata:RouteSpecGrpcRouteMatchMetadata
properties:
  invert:
    type: boolean
    description: |
      If `true`, the match is on the opposite of the `match` criteria. Default is `false`.
    language:
      python:
        mapCase: false
  match:
    $ref: "#/types/aws:appmesh/RouteSpecGrpcRouteMatchMetadataMatch:RouteSpecGrpcRouteMatchMetadataMatch"
    description: |
      Data to match from the request.
    language:
      python:
        mapCase: false
  name:
    type: string
    description: |
      Name of the route. Must be between 1 and 50 characters in length.
    language:
      python:
        mapCase: false
type: object
required:
  - name
