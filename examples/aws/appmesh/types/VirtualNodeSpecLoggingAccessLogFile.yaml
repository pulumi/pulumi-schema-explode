$token: aws:appmesh/VirtualNodeSpecLoggingAccessLogFile:VirtualNodeSpecLoggingAccessLogFile
properties:
  path:
    type: string
    description: |
      File path to write access logs to. You can use `/dev/stdout` to send access logs to standard out. Must be between 1 and 255 characters in length.
    language:
      python:
        mapCase: false
type: object
required:
  - path
