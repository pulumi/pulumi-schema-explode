$token: aws:appmesh/RouteSpecHttpRoute:RouteSpecHttpRoute
properties:
  action:
    $ref: "#/types/aws:appmesh/RouteSpecHttpRouteAction:RouteSpecHttpRouteAction"
    description: |
      Action to take if a match is determined.
    language:
      python:
        mapCase: false
  match:
    $ref: "#/types/aws:appmesh/RouteSpecHttpRouteMatch:RouteSpecHttpRouteMatch"
    description: |
      Criteria for determining an HTTP request match.
    language:
      python:
        mapCase: false
  retryPolicy:
    $ref: "#/types/aws:appmesh/RouteSpecHttpRouteRetryPolicy:RouteSpecHttpRouteRetryPolicy"
    description: |
      Retry policy.
    language:
      python:
        mapCase: false
  timeout:
    $ref: "#/types/aws:appmesh/RouteSpecHttpRouteTimeout:RouteSpecHttpRouteTimeout"
    description: |
      Types of timeouts.
    language:
      python:
        mapCase: false
type: object
required:
  - action
  - match
