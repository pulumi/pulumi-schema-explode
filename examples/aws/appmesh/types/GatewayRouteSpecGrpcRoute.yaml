$token: aws:appmesh/GatewayRouteSpecGrpcRoute:GatewayRouteSpecGrpcRoute
properties:
  action:
    $ref: "#/types/aws:appmesh/GatewayRouteSpecGrpcRouteAction:GatewayRouteSpecGrpcRouteAction"
    description: |
      Action to take if a match is determined.
    language:
      python:
        mapCase: false
  match:
    $ref: "#/types/aws:appmesh/GatewayRouteSpecGrpcRouteMatch:GatewayRouteSpecGrpcRouteMatch"
    description: |
      Criteria for determining a request match.
    language:
      python:
        mapCase: false
type: object
required:
  - action
  - match
