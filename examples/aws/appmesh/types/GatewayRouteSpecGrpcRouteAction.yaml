$token: aws:appmesh/GatewayRouteSpecGrpcRouteAction:GatewayRouteSpecGrpcRouteAction
properties:
  target:
    $ref: "#/types/aws:appmesh/GatewayRouteSpecGrpcRouteActionTarget:GatewayRouteSpecGrpcRouteActionTarget"
    description: |
      Target that traffic is routed to when a request matches the gateway route.
    language:
      python:
        mapCase: false
type: object
required:
  - target
