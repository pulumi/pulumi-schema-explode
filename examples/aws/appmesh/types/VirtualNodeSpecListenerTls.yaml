$token: aws:appmesh/VirtualNodeSpecListenerTls:VirtualNodeSpecListenerTls
properties:
  certificate:
    $ref: "#/types/aws:appmesh/VirtualNodeSpecListenerTlsCertificate:VirtualNodeSpecListenerTlsCertificate"
    description: |
      Listener's TLS certificate.
    language:
      python:
        mapCase: false
  mode:
    type: string
    description: |
      Listener's TLS mode. Valid values: `DISABLED`, `PERMISSIVE`, `STRICT`.
    language:
      python:
        mapCase: false
  validation:
    $ref: "#/types/aws:appmesh/VirtualNodeSpecListenerTlsValidation:VirtualNodeSpecListenerTlsValidation"
    description: |
      Listener's Transport Layer Security (TLS) validation context.
    language:
      python:
        mapCase: false
type: object
required:
  - certificate
  - mode
