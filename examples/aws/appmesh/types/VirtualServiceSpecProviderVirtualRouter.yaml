$token: aws:appmesh/VirtualServiceSpecProviderVirtualRouter:VirtualServiceSpecProviderVirtualRouter
properties:
  virtualRouterName:
    type: string
    description: |
      Name of the virtual router that is acting as a service provider. Must be between 1 and 255 characters in length.
    language:
      python:
        mapCase: false
type: object
required:
  - virtualRouterName
