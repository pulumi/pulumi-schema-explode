$token: aws:appmesh/RouteSpecGrpcRoute:RouteSpecGrpcRoute
properties:
  action:
    $ref: "#/types/aws:appmesh/RouteSpecGrpcRouteAction:RouteSpecGrpcRouteAction"
    description: |
      Action to take if a match is determined.
    language:
      python:
        mapCase: false
  match:
    $ref: "#/types/aws:appmesh/RouteSpecGrpcRouteMatch:RouteSpecGrpcRouteMatch"
    description: |
      Criteria for determining an gRPC request match.
    language:
      python:
        mapCase: false
  retryPolicy:
    $ref: "#/types/aws:appmesh/RouteSpecGrpcRouteRetryPolicy:RouteSpecGrpcRouteRetryPolicy"
    description: |
      Retry policy.
    language:
      python:
        mapCase: false
  timeout:
    $ref: "#/types/aws:appmesh/RouteSpecGrpcRouteTimeout:RouteSpecGrpcRouteTimeout"
    description: |
      Types of timeouts.
    language:
      python:
        mapCase: false
type: object
required:
  - action
