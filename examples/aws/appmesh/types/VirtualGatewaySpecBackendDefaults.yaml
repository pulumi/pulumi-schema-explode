$token: aws:appmesh/VirtualGatewaySpecBackendDefaults:VirtualGatewaySpecBackendDefaults
properties:
  clientPolicy:
    $ref: "#/types/aws:appmesh/VirtualGatewaySpecBackendDefaultsClientPolicy:VirtualGatewaySpecBackendDefaultsClientPolicy"
    description: |
      Default client policy for virtual gateway backends.
    language:
      python:
        mapCase: false
type: object
