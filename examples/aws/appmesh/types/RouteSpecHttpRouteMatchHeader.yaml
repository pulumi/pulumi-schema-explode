$token: aws:appmesh/RouteSpecHttpRouteMatchHeader:RouteSpecHttpRouteMatchHeader
properties:
  invert:
    type: boolean
    description: |
      If `true`, the match is on the opposite of the `match` method and value. Default is `false`.
    language:
      python:
        mapCase: false
  match:
    $ref: "#/types/aws:appmesh/RouteSpecHttpRouteMatchHeaderMatch:RouteSpecHttpRouteMatchHeaderMatch"
    description: |
      Method and value to match the header value sent with a request. Specify one match method.
    language:
      python:
        mapCase: false
  name:
    type: string
    description: |
      Name for the HTTP header in the client request that will be matched on.
    language:
      python:
        mapCase: false
type: object
required:
  - name
