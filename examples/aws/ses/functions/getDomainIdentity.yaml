$token: aws:ses/getDomainIdentity:getDomainIdentity
inputs:
  description: |
    A collection of arguments for invoking getDomainIdentity.
  properties:
    domain:
      type: string
      description: |
        Name of the domain
  type: object
  required:
    - domain
outputs:
  description: |
    A collection of values returned by getDomainIdentity.
  properties:
    arn:
      type: string
      description: |
        ARN of the domain identity.
    domain:
      type: string
      description: |
        Name of the domain
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    verificationToken:
      type: string
      description: |
        Code which when added to the domain as a TXT record will signal to SES that the owner of the domain has authorized SES to act on their behalf.
  type: object
  required:
    - arn
    - domain
    - verificationToken
    - id
