$token: aws:ses/getActiveReceiptRuleSet:getActiveReceiptRuleSet
outputs:
  description: |
    A collection of values returned by getActiveReceiptRuleSet.
  properties:
    arn:
      type: string
      description: |
        SES receipt rule set ARN.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    ruleSetName:
      type: string
      description: |
        Name of the rule set
  type: object
  required:
    - arn
    - ruleSetName
    - id
