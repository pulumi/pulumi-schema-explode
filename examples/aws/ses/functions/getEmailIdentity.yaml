$token: aws:ses/getEmailIdentity:getEmailIdentity
inputs:
  description: |
    A collection of arguments for invoking getEmailIdentity.
  properties:
    email:
      type: string
      description: |
        Email identity.
  type: object
  required:
    - email
outputs:
  description: |
    A collection of values returned by getEmailIdentity.
  properties:
    arn:
      type: string
      description: |
        The ARN of the email identity.
    email:
      type: string
      description: |
        Email identity.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
  type: object
  required:
    - arn
    - email
    - id
