$token: aws:ses/ReceiptRuleSnsAction:ReceiptRuleSnsAction
properties:
  encoding:
    type: string
    description: |
      The encoding to use for the email within the Amazon SNS notification. Default value is `UTF-8`.
    language:
      python:
        mapCase: false
  position:
    type: integer
    description: |
      The position of the action in the receipt rule
    language:
      python:
        mapCase: false
  topicArn:
    type: string
    description: |
      The ARN of an SNS topic to notify
    language:
      python:
        mapCase: false
type: object
required:
  - position
  - topicArn
