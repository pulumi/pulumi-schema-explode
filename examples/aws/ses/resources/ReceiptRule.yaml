$token: aws:ses/receiptRule:ReceiptRule
properties:
  addHeaderActions:
    type: array
    items:
      $ref: "#/types/aws:ses/ReceiptRuleAddHeaderAction:ReceiptRuleAddHeaderAction"
    description: |
      A list of Add Header Action blocks. Documented below.
  after:
    type: string
    description: |
      The name of the rule to place this rule after
  arn:
    type: string
    description: |
      The SES receipt rule ARN.
  bounceActions:
    type: array
    items:
      $ref: "#/types/aws:ses/ReceiptRuleBounceAction:ReceiptRuleBounceAction"
    description: |
      A list of Bounce Action blocks. Documented below.
  enabled:
    type: boolean
    description: |
      If true, the rule will be enabled
  lambdaActions:
    type: array
    items:
      $ref: "#/types/aws:ses/ReceiptRuleLambdaAction:ReceiptRuleLambdaAction"
    description: |
      A list of Lambda Action blocks. Documented below.
  name:
    type: string
    description: |
      The name of the rule
  recipients:
    type: array
    items:
      type: string
    description: |
      A list of email addresses
  ruleSetName:
    type: string
    description: |
      The name of the rule set
  s3Actions:
    type: array
    items:
      $ref: "#/types/aws:ses/ReceiptRuleS3Action:ReceiptRuleS3Action"
    description: |
      A list of S3 Action blocks. Documented below.
  scanEnabled:
    type: boolean
    description: |
      If true, incoming emails will be scanned for spam and viruses
  snsActions:
    type: array
    items:
      $ref: "#/types/aws:ses/ReceiptRuleSnsAction:ReceiptRuleSnsAction"
    description: |
      A list of SNS Action blocks. Documented below.
  stopActions:
    type: array
    items:
      $ref: "#/types/aws:ses/ReceiptRuleStopAction:ReceiptRuleStopAction"
    description: |
      A list of Stop Action blocks. Documented below.
  tlsPolicy:
    type: string
    description: |
      `Require` or `Optional`
  workmailActions:
    type: array
    items:
      $ref: "#/types/aws:ses/ReceiptRuleWorkmailAction:ReceiptRuleWorkmailAction"
    description: |
      A list of WorkMail Action blocks. Documented below.
required:
  - arn
  - name
  - ruleSetName
  - tlsPolicy
inputProperties:
  addHeaderActions:
    type: array
    items:
      $ref: "#/types/aws:ses/ReceiptRuleAddHeaderAction:ReceiptRuleAddHeaderAction"
    description: |
      A list of Add Header Action blocks. Documented below.
  after:
    type: string
    description: |
      The name of the rule to place this rule after
  bounceActions:
    type: array
    items:
      $ref: "#/types/aws:ses/ReceiptRuleBounceAction:ReceiptRuleBounceAction"
    description: |
      A list of Bounce Action blocks. Documented below.
  enabled:
    type: boolean
    description: |
      If true, the rule will be enabled
  lambdaActions:
    type: array
    items:
      $ref: "#/types/aws:ses/ReceiptRuleLambdaAction:ReceiptRuleLambdaAction"
    description: |
      A list of Lambda Action blocks. Documented below.
  name:
    type: string
    description: |
      The name of the rule
    willReplaceOnChanges: true
  recipients:
    type: array
    items:
      type: string
    description: |
      A list of email addresses
  ruleSetName:
    type: string
    description: |
      The name of the rule set
    willReplaceOnChanges: true
  s3Actions:
    type: array
    items:
      $ref: "#/types/aws:ses/ReceiptRuleS3Action:ReceiptRuleS3Action"
    description: |
      A list of S3 Action blocks. Documented below.
  scanEnabled:
    type: boolean
    description: |
      If true, incoming emails will be scanned for spam and viruses
  snsActions:
    type: array
    items:
      $ref: "#/types/aws:ses/ReceiptRuleSnsAction:ReceiptRuleSnsAction"
    description: |
      A list of SNS Action blocks. Documented below.
  stopActions:
    type: array
    items:
      $ref: "#/types/aws:ses/ReceiptRuleStopAction:ReceiptRuleStopAction"
    description: |
      A list of Stop Action blocks. Documented below.
  tlsPolicy:
    type: string
    description: |
      `Require` or `Optional`
  workmailActions:
    type: array
    items:
      $ref: "#/types/aws:ses/ReceiptRuleWorkmailAction:ReceiptRuleWorkmailAction"
    description: |
      A list of WorkMail Action blocks. Documented below.
requiredInputs:
  - ruleSetName
stateInputs:
  description: |
    Input properties used for looking up and filtering ReceiptRule resources.
  properties:
    addHeaderActions:
      type: array
      items:
        $ref: "#/types/aws:ses/ReceiptRuleAddHeaderAction:ReceiptRuleAddHeaderAction"
      description: |
        A list of Add Header Action blocks. Documented below.
    after:
      type: string
      description: |
        The name of the rule to place this rule after
    arn:
      type: string
      description: |
        The SES receipt rule ARN.
    bounceActions:
      type: array
      items:
        $ref: "#/types/aws:ses/ReceiptRuleBounceAction:ReceiptRuleBounceAction"
      description: |
        A list of Bounce Action blocks. Documented below.
    enabled:
      type: boolean
      description: |
        If true, the rule will be enabled
    lambdaActions:
      type: array
      items:
        $ref: "#/types/aws:ses/ReceiptRuleLambdaAction:ReceiptRuleLambdaAction"
      description: |
        A list of Lambda Action blocks. Documented below.
    name:
      type: string
      description: |
        The name of the rule
      willReplaceOnChanges: true
    recipients:
      type: array
      items:
        type: string
      description: |
        A list of email addresses
    ruleSetName:
      type: string
      description: |
        The name of the rule set
      willReplaceOnChanges: true
    s3Actions:
      type: array
      items:
        $ref: "#/types/aws:ses/ReceiptRuleS3Action:ReceiptRuleS3Action"
      description: |
        A list of S3 Action blocks. Documented below.
    scanEnabled:
      type: boolean
      description: |
        If true, incoming emails will be scanned for spam and viruses
    snsActions:
      type: array
      items:
        $ref: "#/types/aws:ses/ReceiptRuleSnsAction:ReceiptRuleSnsAction"
      description: |
        A list of SNS Action blocks. Documented below.
    stopActions:
      type: array
      items:
        $ref: "#/types/aws:ses/ReceiptRuleStopAction:ReceiptRuleStopAction"
      description: |
        A list of Stop Action blocks. Documented below.
    tlsPolicy:
      type: string
      description: |
        `Require` or `Optional`
    workmailActions:
      type: array
      items:
        $ref: "#/types/aws:ses/ReceiptRuleWorkmailAction:ReceiptRuleWorkmailAction"
      description: |
        A list of WorkMail Action blocks. Documented below.
  type: object
