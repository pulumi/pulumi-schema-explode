$token: aws:ses/receiptFilter:ReceiptFilter
properties:
  arn:
    type: string
    description: |
      The SES receipt filter ARN.
  cidr:
    type: string
    description: |
      The IP address or address range to filter, in CIDR notation
  name:
    type: string
    description: |
      The name of the filter
  policy:
    type: string
    description: |
      Block or Allow
required:
  - arn
  - cidr
  - name
  - policy
inputProperties:
  cidr:
    type: string
    description: |
      The IP address or address range to filter, in CIDR notation
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      The name of the filter
    willReplaceOnChanges: true
  policy:
    type: string
    description: |
      Block or Allow
    willReplaceOnChanges: true
requiredInputs:
  - cidr
  - policy
stateInputs:
  description: |
    Input properties used for looking up and filtering ReceiptFilter resources.
  properties:
    arn:
      type: string
      description: |
        The SES receipt filter ARN.
    cidr:
      type: string
      description: |
        The IP address or address range to filter, in CIDR notation
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        The name of the filter
      willReplaceOnChanges: true
    policy:
      type: string
      description: |
        Block or Allow
      willReplaceOnChanges: true
  type: object
