$token: aws:ses/domainIdentityVerification:DomainIdentityVerification
properties:
  arn:
    type: string
    description: |
      The ARN of the domain identity.
  domain:
    type: string
    description: |
      The domain name of the SES domain identity to verify.
required:
  - arn
  - domain
inputProperties:
  domain:
    type: string
    description: |
      The domain name of the SES domain identity to verify.
    willReplaceOnChanges: true
requiredInputs:
  - domain
stateInputs:
  description: |
    Input properties used for looking up and filtering DomainIdentityVerification resources.
  properties:
    arn:
      type: string
      description: |
        The ARN of the domain identity.
    domain:
      type: string
      description: |
        The domain name of the SES domain identity to verify.
      willReplaceOnChanges: true
  type: object
