$token: aws:ses/activeReceiptRuleSet:ActiveReceiptRuleSet
properties:
  arn:
    type: string
    description: |
      The SES receipt rule set ARN.
  ruleSetName:
    type: string
    description: |
      The name of the rule set
required:
  - arn
  - ruleSetName
inputProperties:
  ruleSetName:
    type: string
    description: |
      The name of the rule set
requiredInputs:
  - ruleSetName
stateInputs:
  description: |
    Input properties used for looking up and filtering ActiveReceiptRuleSet resources.
  properties:
    arn:
      type: string
      description: |
        The SES receipt rule set ARN.
    ruleSetName:
      type: string
      description: |
        The name of the rule set
  type: object
