$token: aws:elasticache/userGroup:UserGroup
properties:
  arn:
    type: string
  engine:
    type: string
    description: |
      The current supported value is `REDIS`.
  tags:
    type: object
    additionalProperties:
      type: string
  tagsAll:
    type: object
    additionalProperties:
      type: string
  userGroupId:
    type: string
    description: |
      The ID of the user group.
  userIds:
    type: array
    items:
      type: string
    description: |
      The list of user IDs that belong to the user group.
required:
  - arn
  - engine
  - tagsAll
  - userGroupId
inputProperties:
  arn:
    type: string
  engine:
    type: string
    description: |
      The current supported value is `REDIS`.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
  userGroupId:
    type: string
    description: |
      The ID of the user group.
  userIds:
    type: array
    items:
      type: string
    description: |
      The list of user IDs that belong to the user group.
requiredInputs:
  - engine
  - userGroupId
stateInputs:
  description: |
    Input properties used for looking up and filtering UserGroup resources.
  properties:
    arn:
      type: string
    engine:
      type: string
      description: |
        The current supported value is `REDIS`.
      willReplaceOnChanges: true
    tags:
      type: object
      additionalProperties:
        type: string
    tagsAll:
      type: object
      additionalProperties:
        type: string
    userGroupId:
      type: string
      description: |
        The ID of the user group.
    userIds:
      type: array
      items:
        type: string
      description: |
        The list of user IDs that belong to the user group.
  type: object
