$token: aws:elasticache/parameterGroup:ParameterGroup
properties:
  arn:
    type: string
    description: |
      The AWS ARN associated with the parameter group.
  description:
    type: string
    description: |
      The description of the ElastiCache parameter group. Defaults to "Managed by Pulumi".
    default: Managed by Pulumi
  family:
    type: string
    description: |
      The family of the ElastiCache parameter group.
  name:
    type: string
    description: |
      The name of the ElastiCache parameter.
  parameters:
    type: array
    items:
      $ref: "#/types/aws:elasticache/ParameterGroupParameter:ParameterGroupParameter"
    description: |
      A list of ElastiCache parameters to apply.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value mapping of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - description
  - family
  - name
  - tagsAll
inputProperties:
  description:
    type: string
    description: |
      The description of the ElastiCache parameter group. Defaults to "Managed by Pulumi".
    default: Managed by Pulumi
    willReplaceOnChanges: true
  family:
    type: string
    description: |
      The family of the ElastiCache parameter group.
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      The name of the ElastiCache parameter.
    willReplaceOnChanges: true
  parameters:
    type: array
    items:
      $ref: "#/types/aws:elasticache/ParameterGroupParameter:ParameterGroupParameter"
    description: |
      A list of ElastiCache parameters to apply.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value mapping of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level
requiredInputs:
  - family
stateInputs:
  description: |
    Input properties used for looking up and filtering ParameterGroup resources.
  properties:
    arn:
      type: string
      description: |
        The AWS ARN associated with the parameter group.
    description:
      type: string
      description: |
        The description of the ElastiCache parameter group. Defaults to "Managed by Pulumi".
      default: Managed by Pulumi
      willReplaceOnChanges: true
    family:
      type: string
      description: |
        The family of the ElastiCache parameter group.
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        The name of the ElastiCache parameter.
      willReplaceOnChanges: true
    parameters:
      type: array
      items:
        $ref: "#/types/aws:elasticache/ParameterGroupParameter:ParameterGroupParameter"
      description: |
        A list of ElastiCache parameters to apply.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value mapping of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
