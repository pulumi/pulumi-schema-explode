$token: aws:elasticache/getUser:getUser
inputs:
  description: |
    A collection of arguments for invoking getUser.
  properties:
    accessString:
      type: string
      description: |
        String for what access a user possesses within the associated ElastiCache replication groups or clusters.
    engine:
      type: string
    noPasswordRequired:
      type: boolean
    passwords:
      type: array
      items:
        type: string
    userId:
      type: string
      description: |
        Identifier for the user.
    userName:
      type: string
      description: |
        User name of the user.
  type: object
  required:
    - userId
outputs:
  description: |
    A collection of values returned by getUser.
  properties:
    accessString:
      type: string
      description: |
        String for what access a user possesses within the associated ElastiCache replication groups or clusters.
    engine:
      type: string
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    noPasswordRequired:
      type: boolean
    passwords:
      type: array
      items:
        type: string
    userId:
      type: string
      description: |
        Identifier for the user.
    userName:
      type: string
      description: |
        User name of the user.
  type: object
  required:
    - userId
    - id
