$token: aws:elasticache/getClusterCacheNode:getClusterCacheNode
properties:
  address:
    type: string
    language:
      python:
        mapCase: false
  availabilityZone:
    type: string
    description: |
      Availability Zone for the cache cluster.
    language:
      python:
        mapCase: false
  id:
    type: string
    language:
      python:
        mapCase: false
  port:
    type: integer
    description: |
      The port number on which each of the cache nodes will
      accept connections.
    language:
      python:
        mapCase: false
type: object
required:
  - address
  - availabilityZone
  - id
  - port
language:
  nodejs:
    requiredInputs: []
