$token: aws:accessanalyzer/archiveRule:ArchiveRule
properties:
  analyzerName:
    type: string
    description: |
      Analyzer name.
  filters:
    type: array
    items:
      $ref: "#/types/aws:accessanalyzer/ArchiveRuleFilter:ArchiveRuleFilter"
    description: |
      Filter criteria for the archive rule. See Filter for more details.
  ruleName:
    type: string
    description: |
      Rule name.
required:
  - analyzerName
  - filters
  - ruleName
inputProperties:
  analyzerName:
    type: string
    description: |
      Analyzer name.
    willReplaceOnChanges: true
  filters:
    type: array
    items:
      $ref: "#/types/aws:accessanalyzer/ArchiveRuleFilter:ArchiveRuleFilter"
    description: |
      Filter criteria for the archive rule. See Filter for more details.
  ruleName:
    type: string
    description: |
      Rule name.
    willReplaceOnChanges: true
requiredInputs:
  - analyzerName
  - filters
  - ruleName
stateInputs:
  description: |
    Input properties used for looking up and filtering ArchiveRule resources.
  properties:
    analyzerName:
      type: string
      description: |
        Analyzer name.
      willReplaceOnChanges: true
    filters:
      type: array
      items:
        $ref: "#/types/aws:accessanalyzer/ArchiveRuleFilter:ArchiveRuleFilter"
      description: |
        Filter criteria for the archive rule. See Filter for more details.
    ruleName:
      type: string
      description: |
        Rule name.
      willReplaceOnChanges: true
  type: object
