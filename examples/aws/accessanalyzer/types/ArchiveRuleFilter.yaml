$token: aws:accessanalyzer/ArchiveRuleFilter:ArchiveRuleFilter
properties:
  contains:
    type: array
    items:
      type: string
    description: |
      Contains comparator.
    language:
      python:
        mapCase: false
  criteria:
    type: string
    description: |
      Filter criteria.
    language:
      python:
        mapCase: false
  eqs:
    type: array
    items:
      type: string
    description: |
      Equals comparator.
    language:
      python:
        mapCase: false
  exists:
    type: string
    description: |
      Boolean comparator.
    language:
      python:
        mapCase: false
  neqs:
    type: array
    items:
      type: string
    description: |
      Not Equals comparator.
    language:
      python:
        mapCase: false
type: object
required:
  - criteria
language:
  nodejs:
    requiredOutputs:
      - contains
      - criteria
      - eqs
      - exists
      - neqs
