$token: aws:appflow/FlowDestinationFlowConfigDestinationConnectorPropertiesCustomConnector:FlowDestinationFlowConfigDestinationConnectorPropertiesCustomConnector
properties:
  customProperties:
    type: object
    additionalProperties:
      type: string
    description: |
      Custom properties that are specific to the connector when it's used as a source in the flow. Maximum of 50 items.
    language:
      python:
        mapCase: false
  entityName:
    type: string
    description: |
      Entity specified in the custom connector as a source in the flow.
    language:
      python:
        mapCase: false
  errorHandlingConfig:
    $ref: "#/types/aws:appflow/FlowDestinationFlowConfigDestinationConnectorPropertiesCustomConnectorErrorHandlingConfig:FlowDestinationFlowConfigDestinationConnectorPropertiesCustomConnectorErrorHandlingConfig"
    description: |
      Settings that determine how Amazon AppFlow handles an error when placing data in the destination. See Error Handling Config for more details.
    language:
      python:
        mapCase: false
  idFieldNames:
    type: array
    items:
      type: string
    description: |
      Name of the field that Amazon AppFlow uses as an ID when performing a write operation such as update or delete.
    language:
      python:
        mapCase: false
  writeOperationType:
    type: string
    description: |
      This specifies the type of write operation to be performed in Zendesk. When the value is `UPSERT`, then `id_field_names` is required. Valid values are `INSERT`, `UPSERT`, `UPDATE`, and `DELETE`.
    language:
      python:
        mapCase: false
type: object
required:
  - entityName
