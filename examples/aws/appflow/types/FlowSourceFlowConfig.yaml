$token: aws:appflow/FlowSourceFlowConfig:FlowSourceFlowConfig
properties:
  apiVersion:
    type: string
    description: |
      API version that the destination connector uses.
    language:
      python:
        mapCase: false
  connectorProfileName:
    type: string
    description: |
      Name of the connector profile. This name must be unique for each connector profile in the AWS account.
    language:
      python:
        mapCase: false
  connectorType:
    type: string
    description: |
      Type of connector, such as Salesforce, Amplitude, and so on. Valid values are `Salesforce`, `Singular`, `Slack`, `Redshift`, `S3`, `Marketo`, `Googleanalytics`, `Zendesk`, `Servicenow`, `Datadog`, `Trendmicro`, `Snowflake`, `Dynatrace`, `Infornexus`, `Amplitude`, `Veeva`, `EventBridge`, `LookoutMetrics`, `Upsolver`, `Honeycode`, `CustomerProfiles`, `SAPOData`, and `CustomConnector`.
    language:
      python:
        mapCase: false
  incrementalPullConfig:
    $ref: "#/types/aws:appflow/FlowSourceFlowConfigIncrementalPullConfig:FlowSourceFlowConfigIncrementalPullConfig"
    description: |
      Defines the configuration for a scheduled incremental data pull. If a valid configuration is provided, the fields specified in the configuration are used when querying for the incremental data pull. See Incremental Pull Config for more details.
    language:
      python:
        mapCase: false
  sourceConnectorProperties:
    $ref: "#/types/aws:appflow/FlowSourceFlowConfigSourceConnectorProperties:FlowSourceFlowConfigSourceConnectorProperties"
    description: |
      Information that is required to query a particular source connector. See Source Connector Properties for details.
    language:
      python:
        mapCase: false
type: object
required:
  - connectorType
  - sourceConnectorProperties
