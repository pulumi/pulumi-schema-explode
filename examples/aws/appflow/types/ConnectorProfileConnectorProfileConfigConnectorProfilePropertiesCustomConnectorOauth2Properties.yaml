$token: aws:appflow/ConnectorProfileConnectorProfileConfigConnectorProfilePropertiesCustomConnectorOauth2Properties:ConnectorProfileConnectorProfileConfigConnectorProfilePropertiesCustomConnectorOauth2Properties
properties:
  oauth2GrantType:
    type: string
    description: |
      The OAuth 2.0 grant type used by connector for OAuth 2.0 authentication. One of: `AUTHORIZATION_CODE`, `CLIENT_CREDENTIALS`.
    language:
      python:
        mapCase: false
  tokenUrl:
    type: string
    description: |
      The token url required to fetch access/refresh tokens using authorization code and also to refresh expired access token using refresh token.
    language:
      python:
        mapCase: false
  tokenUrlCustomProperties:
    type: object
    additionalProperties:
      type: string
    description: |
      Associates your token URL with a map of properties that you define. Use this parameter to provide any additional details that the connector requires to authenticate your request.
    language:
      python:
        mapCase: false
type: object
required:
  - oauth2GrantType
  - tokenUrl
