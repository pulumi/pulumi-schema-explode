$token: aws:appflow/FlowDestinationFlowConfigDestinationConnectorPropertiesMarketoErrorHandlingConfig:FlowDestinationFlowConfigDestinationConnectorPropertiesMarketoErrorHandlingConfig
properties:
  bucketName:
    type: string
    description: |
      Amazon S3 bucket name where the source files are stored.
    language:
      python:
        mapCase: false
  bucketPrefix:
    type: string
    description: |
      Object key for the Amazon S3 bucket in which the source files are stored.
    language:
      python:
        mapCase: false
  failOnFirstDestinationError:
    type: boolean
    description: |
      If the flow should fail after the first instance of a failure when attempting to place data in the destination.
    language:
      python:
        mapCase: false
type: object
