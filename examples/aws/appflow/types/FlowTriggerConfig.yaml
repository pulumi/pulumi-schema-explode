$token: aws:appflow/FlowTriggerConfig:FlowTriggerConfig
properties:
  triggerProperties:
    $ref: "#/types/aws:appflow/FlowTriggerConfigTriggerProperties:FlowTriggerConfigTriggerProperties"
    description: |
      Configuration details of a schedule-triggered flow as defined by the user. Currently, these settings only apply to the `Scheduled` trigger type. See Scheduled Trigger Properties for details.
    language:
      python:
        mapCase: false
  triggerType:
    type: string
    description: |
      Type of flow trigger. Valid values are `Scheduled`, `Event`, and `OnDemand`.
    language:
      python:
        mapCase: false
type: object
required:
  - triggerType
language:
  nodejs:
    requiredOutputs:
      - triggerProperties
      - triggerType
