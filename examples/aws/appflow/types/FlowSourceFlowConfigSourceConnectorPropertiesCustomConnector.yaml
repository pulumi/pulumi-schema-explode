$token: aws:appflow/FlowSourceFlowConfigSourceConnectorPropertiesCustomConnector:FlowSourceFlowConfigSourceConnectorPropertiesCustomConnector
properties:
  customProperties:
    type: object
    additionalProperties:
      type: string
    description: |
      Custom properties that are specific to the connector when it's used as a source in the flow. Maximum of 50 items.
    language:
      python:
        mapCase: false
  entityName:
    type: string
    description: |
      Entity specified in the custom connector as a source in the flow.
    language:
      python:
        mapCase: false
type: object
required:
  - entityName
