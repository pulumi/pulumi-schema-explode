$token: aws:appflow/ConnectorProfileConnectorProfileConfigConnectorProfilePropertiesSapoDataOauthProperties:ConnectorProfileConnectorProfileConfigConnectorProfilePropertiesSapoDataOauthProperties
properties:
  authCodeUrl:
    type: string
    description: |
      The authorization code url required to redirect to SAP Login Page to fetch authorization code for OAuth type authentication.
    language:
      python:
        mapCase: false
  oauthScopes:
    type: array
    items:
      type: string
    description: |
      The OAuth scopes required for OAuth type authentication.
    language:
      python:
        mapCase: false
  tokenUrl:
    type: string
    description: |
      The token url required to fetch access/refresh tokens using authorization code and also to refresh expired access token using refresh token.
    language:
      python:
        mapCase: false
type: object
required:
  - authCodeUrl
  - oauthScopes
  - tokenUrl
