$token: aws:lakeformation/getPermissions:getPermissions
inputs:
  description: |
    A collection of arguments for invoking getPermissions.
  properties:
    catalogId:
      type: string
      description: |
        Identifier for the Data Catalog. By default, it is the account ID of the caller.
    catalogResource:
      type: boolean
      description: |
        Whether the permissions are to be granted for the Data Catalog. Defaults to `false`.
    dataLocation:
      $ref: "#/types/aws:lakeformation/getPermissionsDataLocation:getPermissionsDataLocation"
      description: |
        Configuration block for a data location resource. Detailed below.
    database:
      $ref: "#/types/aws:lakeformation/getPermissionsDatabase:getPermissionsDatabase"
      description: |
        Configuration block for a database resource. Detailed below.
    lfTag:
      $ref: "#/types/aws:lakeformation/getPermissionsLfTag:getPermissionsLfTag"
      description: |
        Configuration block for an LF-tag resource. Detailed below.
    lfTagPolicy:
      $ref: "#/types/aws:lakeformation/getPermissionsLfTagPolicy:getPermissionsLfTagPolicy"
      description: |
        Configuration block for an LF-tag policy resource. Detailed below.
    principal:
      type: string
      description: |
        Principal to be granted the permissions on the resource. Supported principals are IAM users or IAM roles.
    table:
      $ref: "#/types/aws:lakeformation/getPermissionsTable:getPermissionsTable"
      description: |
        Configuration block for a table resource. Detailed below.
    tableWithColumns:
      $ref: "#/types/aws:lakeformation/getPermissionsTableWithColumns:getPermissionsTableWithColumns"
      description: |
        Configuration block for a table with columns resource. Detailed below.
  type: object
  required:
    - principal
outputs:
  description: |
    A collection of values returned by getPermissions.
  properties:
    catalogId:
      type: string
    catalogResource:
      type: boolean
    dataLocation:
      $ref: "#/types/aws:lakeformation/getPermissionsDataLocation:getPermissionsDataLocation"
    database:
      $ref: "#/types/aws:lakeformation/getPermissionsDatabase:getPermissionsDatabase"
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    lfTag:
      $ref: "#/types/aws:lakeformation/getPermissionsLfTag:getPermissionsLfTag"
    lfTagPolicy:
      $ref: "#/types/aws:lakeformation/getPermissionsLfTagPolicy:getPermissionsLfTagPolicy"
    permissions:
      type: array
      items:
        type: string
      description: |
        List of permissions granted to the principal. For details on permissions, see [Lake Formation Permissions Reference](https://docs.aws.amazon.com/lake-formation/latest/dg/lf-permissions-reference.html).
    permissionsWithGrantOptions:
      type: array
      items:
        type: string
      description: |
        Subset of `permissions` which the principal can pass.
    principal:
      type: string
    table:
      $ref: "#/types/aws:lakeformation/getPermissionsTable:getPermissionsTable"
    tableWithColumns:
      $ref: "#/types/aws:lakeformation/getPermissionsTableWithColumns:getPermissionsTableWithColumns"
  type: object
  required:
    - dataLocation
    - database
    - lfTag
    - lfTagPolicy
    - permissions
    - permissionsWithGrantOptions
    - principal
    - table
    - tableWithColumns
    - id
