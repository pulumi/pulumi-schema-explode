$token: aws:lakeformation/getDataLakeSettings:getDataLakeSettings
inputs:
  description: |
    A collection of arguments for invoking getDataLakeSettings.
  properties:
    catalogId:
      type: string
      description: |
        Identifier for the Data Catalog. By default, the account ID.
  type: object
outputs:
  description: |
    A collection of values returned by getDataLakeSettings.
  properties:
    admins:
      type: array
      items:
        type: string
      description: |
        List of ARNs of AWS Lake Formation principals (IAM users or roles).
    catalogId:
      type: string
    createDatabaseDefaultPermissions:
      type: array
      items:
        $ref: "#/types/aws:lakeformation/getDataLakeSettingsCreateDatabaseDefaultPermission:getDataLakeSettingsCreateDatabaseDefaultPermission"
      description: |
        Up to three configuration blocks of principal permissions for default create database permissions. Detailed below.
    createTableDefaultPermissions:
      type: array
      items:
        $ref: "#/types/aws:lakeformation/getDataLakeSettingsCreateTableDefaultPermission:getDataLakeSettingsCreateTableDefaultPermission"
      description: |
        Up to three configuration blocks of principal permissions for default create table permissions. Detailed below.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    trustedResourceOwners:
      type: array
      items:
        type: string
      description: |
        List of the resource-owning account IDs that the caller's account can use to share their user access details (user ARNs).
  type: object
  required:
    - admins
    - createDatabaseDefaultPermissions
    - createTableDefaultPermissions
    - trustedResourceOwners
    - id
