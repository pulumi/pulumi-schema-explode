$token: aws:lakeformation/getPermissionsDatabase:getPermissionsDatabase
properties:
  catalogId:
    type: string
    description: |
      Identifier for the Data Catalog. By default, it is the account ID of the caller.
    language:
      python:
        mapCase: false
  name:
    type: string
    description: |
      Name of the table resource.
    language:
      python:
        mapCase: false
type: object
required:
  - catalogId
  - name
language:
  nodejs:
    requiredInputs:
      - name
