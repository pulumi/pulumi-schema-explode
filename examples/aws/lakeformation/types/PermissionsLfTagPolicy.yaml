$token: aws:lakeformation/PermissionsLfTagPolicy:PermissionsLfTagPolicy
properties:
  catalogId:
    type: string
    description: |
      Identifier for the Data Catalog. By default, it is the account ID of the caller.
    language:
      python:
        mapCase: false
  expressions:
    type: array
    items:
      $ref: "#/types/aws:lakeformation/PermissionsLfTagPolicyExpression:PermissionsLfTagPolicyExpression"
    description: |
      A list of tag conditions that apply to the resource's tag policy. Configuration block for tag conditions that apply to the policy. See `expression` below.
    language:
      python:
        mapCase: false
  resourceType:
    type: string
    description: |
      The resource type for which the tag policy applies. Valid values are `DATABASE` and `TABLE`.
    language:
      python:
        mapCase: false
type: object
required:
  - expressions
  - resourceType
language:
  nodejs:
    requiredOutputs:
      - catalogId
      - expressions
      - resourceType
