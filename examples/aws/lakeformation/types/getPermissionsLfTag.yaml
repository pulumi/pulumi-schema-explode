$token: aws:lakeformation/getPermissionsLfTag:getPermissionsLfTag
properties:
  catalogId:
    type: string
    description: |
      Identifier for the Data Catalog. By default, it is the account ID of the caller.
    language:
      python:
        mapCase: false
  key:
    type: string
    description: |
      Key-name of an LF-Tag.
    language:
      python:
        mapCase: false
  values:
    type: array
    items:
      type: string
    description: |
      List of possible values of an LF-Tag.
    language:
      python:
        mapCase: false
type: object
required:
  - catalogId
  - key
  - values
language:
  nodejs:
    requiredInputs:
      - key
      - values
