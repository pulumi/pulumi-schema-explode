$token: aws:lakeformation/getPermissionsDataLocation:getPermissionsDataLocation
properties:
  arn:
    type: string
    description: |
      ARN that uniquely identifies the data location resource.
    language:
      python:
        mapCase: false
  catalogId:
    type: string
    description: |
      Identifier for the Data Catalog. By default, it is the account ID of the caller.
    language:
      python:
        mapCase: false
type: object
required:
  - arn
  - catalogId
language:
  nodejs:
    requiredInputs:
      - arn
