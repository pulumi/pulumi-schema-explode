$token: aws:lakeformation/getPermissionsTable:getPermissionsTable
properties:
  catalogId:
    type: string
    description: |
      Identifier for the Data Catalog. By default, it is the account ID of the caller.
    language:
      python:
        mapCase: false
  databaseName:
    type: string
    description: |
      Name of the database for the table with columns resource. Unique to the Data Catalog.
    language:
      python:
        mapCase: false
  name:
    type: string
    description: |
      Name of the table resource.
    language:
      python:
        mapCase: false
  wildcard:
    type: boolean
    description: |
      Whether to use a wildcard representing every table under a database. At least one of `name` or `wildcard` is required. Defaults to `false`.
    language:
      python:
        mapCase: false
type: object
required:
  - catalogId
  - databaseName
  - name
language:
  nodejs:
    requiredInputs:
      - databaseName
