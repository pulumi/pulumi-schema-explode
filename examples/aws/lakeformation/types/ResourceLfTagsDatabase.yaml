$token: aws:lakeformation/ResourceLfTagsDatabase:ResourceLfTagsDatabase
properties:
  catalogId:
    type: string
    description: |
      Identifier for the Data Catalog. By default, it is the account ID of the caller.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      Name of the table resource.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - name
language:
  nodejs:
    requiredOutputs:
      - catalogId
      - name
