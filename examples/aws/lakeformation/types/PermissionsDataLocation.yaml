$token: aws:lakeformation/PermissionsDataLocation:PermissionsDataLocation
properties:
  arn:
    type: string
    description: |
      Amazon Resource Name (ARN) that uniquely identifies the data location resource.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  catalogId:
    type: string
    description: |
      Identifier for the Data Catalog. By default, it is the account ID of the caller.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - arn
language:
  nodejs:
    requiredOutputs:
      - arn
      - catalogId
