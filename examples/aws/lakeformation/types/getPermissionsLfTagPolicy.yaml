$token: aws:lakeformation/getPermissionsLfTagPolicy:getPermissionsLfTagPolicy
properties:
  catalogId:
    type: string
    description: |
      Identifier for the Data Catalog. By default, it is the account ID of the caller.
    language:
      python:
        mapCase: false
  expressions:
    type: array
    items:
      $ref: "#/types/aws:lakeformation/getPermissionsLfTagPolicyExpression:getPermissionsLfTagPolicyExpression"
    description: |
      List of tag conditions that apply to the resource's tag policy. Configuration block for tag conditions that apply to the policy. See `expression` below.
    language:
      python:
        mapCase: false
  resourceType:
    type: string
    description: |
      Resource type for which the tag policy applies. Valid values are `DATABASE` and `TABLE`.
    language:
      python:
        mapCase: false
type: object
required:
  - catalogId
  - expressions
  - resourceType
language:
  nodejs:
    requiredInputs:
      - expressions
      - resourceType
