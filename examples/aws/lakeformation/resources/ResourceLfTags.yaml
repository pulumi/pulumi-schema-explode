$token: aws:lakeformation/resourceLfTags:ResourceLfTags
properties:
  catalogId:
    type: string
    description: |
      Identifier for the Data Catalog. By default, it is the account ID of the caller.
  database:
    $ref: "#/types/aws:lakeformation/ResourceLfTagsDatabase:ResourceLfTagsDatabase"
    description: |
      Configuration block for a database resource. See below.
  lfTags:
    type: array
    items:
      $ref: "#/types/aws:lakeformation/ResourceLfTagsLfTag:ResourceLfTagsLfTag"
    description: |
      Set of LF-tags to attach to the resource. See below.
  table:
    $ref: "#/types/aws:lakeformation/ResourceLfTagsTable:ResourceLfTagsTable"
    description: |
      Configuration block for a table resource. See below.
  tableWithColumns:
    $ref: "#/types/aws:lakeformation/ResourceLfTagsTableWithColumns:ResourceLfTagsTableWithColumns"
    description: |
      Configuration block for a table with columns resource. See below.
required:
  - catalogId
  - database
  - lfTags
  - table
  - tableWithColumns
inputProperties:
  catalogId:
    type: string
    description: |
      Identifier for the Data Catalog. By default, it is the account ID of the caller.
    willReplaceOnChanges: true
  database:
    $ref: "#/types/aws:lakeformation/ResourceLfTagsDatabase:ResourceLfTagsDatabase"
    description: |
      Configuration block for a database resource. See below.
    willReplaceOnChanges: true
  lfTags:
    type: array
    items:
      $ref: "#/types/aws:lakeformation/ResourceLfTagsLfTag:ResourceLfTagsLfTag"
    description: |
      Set of LF-tags to attach to the resource. See below.
    willReplaceOnChanges: true
  table:
    $ref: "#/types/aws:lakeformation/ResourceLfTagsTable:ResourceLfTagsTable"
    description: |
      Configuration block for a table resource. See below.
    willReplaceOnChanges: true
  tableWithColumns:
    $ref: "#/types/aws:lakeformation/ResourceLfTagsTableWithColumns:ResourceLfTagsTableWithColumns"
    description: |
      Configuration block for a table with columns resource. See below.
    willReplaceOnChanges: true
requiredInputs:
  - lfTags
stateInputs:
  description: |
    Input properties used for looking up and filtering ResourceLfTags resources.
  properties:
    catalogId:
      type: string
      description: |
        Identifier for the Data Catalog. By default, it is the account ID of the caller.
      willReplaceOnChanges: true
    database:
      $ref: "#/types/aws:lakeformation/ResourceLfTagsDatabase:ResourceLfTagsDatabase"
      description: |
        Configuration block for a database resource. See below.
      willReplaceOnChanges: true
    lfTags:
      type: array
      items:
        $ref: "#/types/aws:lakeformation/ResourceLfTagsLfTag:ResourceLfTagsLfTag"
      description: |
        Set of LF-tags to attach to the resource. See below.
      willReplaceOnChanges: true
    table:
      $ref: "#/types/aws:lakeformation/ResourceLfTagsTable:ResourceLfTagsTable"
      description: |
        Configuration block for a table resource. See below.
      willReplaceOnChanges: true
    tableWithColumns:
      $ref: "#/types/aws:lakeformation/ResourceLfTagsTableWithColumns:ResourceLfTagsTableWithColumns"
      description: |
        Configuration block for a table with columns resource. See below.
      willReplaceOnChanges: true
  type: object
