$token: aws:lakeformation/lfTag:LfTag
properties:
  catalogId:
    type: string
    description: |
      ID of the Data Catalog to create the tag in. If omitted, this defaults to the AWS Account ID.
  key:
    type: string
    description: |
      Key-name for the tag.
  values:
    type: array
    items:
      type: string
    description: |
      List of possible values an attribute can take.
required:
  - catalogId
  - key
  - values
inputProperties:
  catalogId:
    type: string
    description: |
      ID of the Data Catalog to create the tag in. If omitted, this defaults to the AWS Account ID.
    willReplaceOnChanges: true
  key:
    type: string
    description: |
      Key-name for the tag.
    willReplaceOnChanges: true
  values:
    type: array
    items:
      type: string
    description: |
      List of possible values an attribute can take.
requiredInputs:
  - key
  - values
stateInputs:
  description: |
    Input properties used for looking up and filtering LfTag resources.
  properties:
    catalogId:
      type: string
      description: |
        ID of the Data Catalog to create the tag in. If omitted, this defaults to the AWS Account ID.
      willReplaceOnChanges: true
    key:
      type: string
      description: |
        Key-name for the tag.
      willReplaceOnChanges: true
    values:
      type: array
      items:
        type: string
      description: |
        List of possible values an attribute can take.
  type: object
