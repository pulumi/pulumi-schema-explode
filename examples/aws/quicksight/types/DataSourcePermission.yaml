$token: aws:quicksight/DataSourcePermission:DataSourcePermission
properties:
  actions:
    type: array
    items:
      type: string
    description: |
      Set of IAM actions to grant or revoke permissions on. Max of 16 items.
    language:
      python:
        mapCase: false
  principal:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the principal.
    language:
      python:
        mapCase: false
type: object
required:
  - actions
  - principal
