$token: aws:quicksight/DataSourceParametersAurora:DataSourceParametersAurora
properties:
  database:
    type: string
    description: |
      The database to which to connect.
    language:
      python:
        mapCase: false
  host:
    type: string
    description: |
      The host to which to connect.
    language:
      python:
        mapCase: false
  port:
    type: integer
    description: |
      The port to which to connect.
    language:
      python:
        mapCase: false
type: object
required:
  - database
  - host
  - port
