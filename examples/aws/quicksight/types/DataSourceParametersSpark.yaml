$token: aws:quicksight/DataSourceParametersSpark:DataSourceParametersSpark
properties:
  host:
    type: string
    description: |
      The host to which to connect.
    language:
      python:
        mapCase: false
  port:
    type: integer
    description: |
      The warehouse to which to connect.
    language:
      python:
        mapCase: false
type: object
required:
  - host
  - port
