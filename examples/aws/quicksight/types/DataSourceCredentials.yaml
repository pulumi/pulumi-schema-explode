$token: aws:quicksight/DataSourceCredentials:DataSourceCredentials
properties:
  copySourceArn:
    type: string
    description: |
      - The Amazon Resource Name (ARN) of a data source that has the credential pair that you want to use.
      When the value is not null, the `credential_pair` from the data source in the ARN is used.
    language:
      python:
        mapCase: false
  credentialPair:
    $ref: "#/types/aws:quicksight/DataSourceCredentialsCredentialPair:DataSourceCredentialsCredentialPair"
    description: |
      - Credential pair. See Credential Pair below for more details.
    language:
      python:
        mapCase: false
type: object
