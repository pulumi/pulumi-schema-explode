$token: aws:quicksight/groupMembership:GroupMembership
properties:
  arn:
    type: string
  awsAccountId:
    type: string
    description: |
      The ID for the AWS account that the group is in. Currently, you use the ID for the AWS account that contains your Amazon QuickSight account.
  groupName:
    type: string
    description: |
      The name of the group in which the member will be added.
  memberName:
    type: string
    description: |
      The name of the member to add to the group.
  namespace:
    type: string
    description: |
      The namespace. Defaults to `default`. Currently only `default` is supported.
required:
  - arn
  - awsAccountId
  - groupName
  - memberName
inputProperties:
  awsAccountId:
    type: string
    description: |
      The ID for the AWS account that the group is in. Currently, you use the ID for the AWS account that contains your Amazon QuickSight account.
    willReplaceOnChanges: true
  groupName:
    type: string
    description: |
      The name of the group in which the member will be added.
    willReplaceOnChanges: true
  memberName:
    type: string
    description: |
      The name of the member to add to the group.
    willReplaceOnChanges: true
  namespace:
    type: string
    description: |
      The namespace. Defaults to `default`. Currently only `default` is supported.
    willReplaceOnChanges: true
requiredInputs:
  - groupName
  - memberName
stateInputs:
  description: |
    Input properties used for looking up and filtering GroupMembership resources.
  properties:
    arn:
      type: string
    awsAccountId:
      type: string
      description: |
        The ID for the AWS account that the group is in. Currently, you use the ID for the AWS account that contains your Amazon QuickSight account.
      willReplaceOnChanges: true
    groupName:
      type: string
      description: |
        The name of the group in which the member will be added.
      willReplaceOnChanges: true
    memberName:
      type: string
      description: |
        The name of the member to add to the group.
      willReplaceOnChanges: true
    namespace:
      type: string
      description: |
        The namespace. Defaults to `default`. Currently only `default` is supported.
      willReplaceOnChanges: true
  type: object
