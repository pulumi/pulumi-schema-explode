$token: aws:eks/NodeGroupTaint:NodeGroupTaint
properties:
  effect:
    type: string
    description: |
      The effect of the taint. Valid values: `NO_SCHEDULE`, `NO_EXECUTE`, `PREFER_NO_SCHEDULE`.
    language:
      python:
        mapCase: false
  key:
    type: string
    description: |
      The key of the taint. Maximum length of 63.
    language:
      python:
        mapCase: false
  value:
    type: string
    description: |
      The value of the taint. Maximum length of 63.
    language:
      python:
        mapCase: false
type: object
required:
  - effect
  - key
