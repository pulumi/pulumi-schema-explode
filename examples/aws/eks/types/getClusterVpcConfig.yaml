$token: aws:eks/getClusterVpcConfig:getClusterVpcConfig
properties:
  clusterSecurityGroupId:
    type: string
    description: |
      The cluster security group that was created by Amazon EKS for the cluster.
    language:
      python:
        mapCase: false
  endpointPrivateAccess:
    type: boolean
    description: |
      Indicates whether or not the Amazon EKS private API server endpoint is enabled.
    language:
      python:
        mapCase: false
  endpointPublicAccess:
    type: boolean
    description: |
      Indicates whether or not the Amazon EKS public API server endpoint is enabled.
    language:
      python:
        mapCase: false
  publicAccessCidrs:
    type: array
    items:
      type: string
    description: |
      List of CIDR blocks. Indicates which CIDR blocks can access the Amazon EKS public API server endpoint.
    language:
      python:
        mapCase: false
  securityGroupIds:
    type: array
    items:
      type: string
    description: |
      List of security group IDs
    language:
      python:
        mapCase: false
  subnetIds:
    type: array
    items:
      type: string
    description: |
      List of subnet IDs
    language:
      python:
        mapCase: false
  vpcId:
    type: string
    description: |
      The VPC associated with your cluster.
    language:
      python:
        mapCase: false
type: object
required:
  - clusterSecurityGroupId
  - endpointPrivateAccess
  - endpointPublicAccess
  - publicAccessCidrs
  - securityGroupIds
  - subnetIds
  - vpcId
language:
  nodejs:
    requiredInputs: []
