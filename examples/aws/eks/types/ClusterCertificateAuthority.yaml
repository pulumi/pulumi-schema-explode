$token: aws:eks/ClusterCertificateAuthority:ClusterCertificateAuthority
properties:
  data:
    type: string
    description: |
      Base64 encoded certificate data required to communicate with your cluster. Add this to the `certificate-authority-data` section of the `kubeconfig` file for your cluster.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - data
