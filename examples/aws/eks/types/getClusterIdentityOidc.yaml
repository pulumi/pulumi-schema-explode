$token: aws:eks/getClusterIdentityOidc:getClusterIdentityOidc
properties:
  issuer:
    type: string
    description: |
      Issuer URL for the OpenID Connect identity provider.
    language:
      python:
        mapCase: false
type: object
required:
  - issuer
language:
  nodejs:
    requiredInputs: []
