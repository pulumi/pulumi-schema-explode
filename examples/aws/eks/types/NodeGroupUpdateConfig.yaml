$token: aws:eks/NodeGroupUpdateConfig:NodeGroupUpdateConfig
properties:
  maxUnavailable:
    type: integer
    description: |
      Desired max number of unavailable worker nodes during node group update.
    language:
      python:
        mapCase: false
  maxUnavailablePercentage:
    type: integer
    description: |
      Desired max percentage of unavailable worker nodes during node group update.
    language:
      python:
        mapCase: false
type: object
