$token: aws:eks/getClusterIdentity:getClusterIdentity
properties:
  oidcs:
    type: array
    items:
      $ref: "#/types/aws:eks/getClusterIdentityOidc:getClusterIdentityOidc"
    description: |
      Nested attribute containing [OpenID Connect](https://openid.net/connect/) identity provider information for the cluster.
    language:
      python:
        mapCase: false
type: object
required:
  - oidcs
language:
  nodejs:
    requiredInputs: []
