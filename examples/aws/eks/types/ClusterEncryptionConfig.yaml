$token: aws:eks/ClusterEncryptionConfig:ClusterEncryptionConfig
properties:
  provider:
    $ref: "#/types/aws:eks/ClusterEncryptionConfigProvider:ClusterEncryptionConfigProvider"
    description: |
      Configuration block with provider for encryption. Detailed below.
    language:
      python:
        mapCase: false
  resources:
    type: array
    items:
      type: string
    description: |
      List of strings with resources to be encrypted. Valid values: `secrets`.
    language:
      python:
        mapCase: false
type: object
required:
  - provider
  - resources
