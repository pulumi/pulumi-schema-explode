$token: aws:eks/NodeGroupResource:NodeGroupResource
properties:
  autoscalingGroups:
    type: array
    items:
      $ref: "#/types/aws:eks/NodeGroupResourceAutoscalingGroup:NodeGroupResourceAutoscalingGroup"
    description: |
      List of objects containing information about AutoScaling Groups.
    language:
      python:
        mapCase: false
  remoteAccessSecurityGroupId:
    type: string
    description: |
      Identifier of the remote access EC2 Security Group.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - autoscalingGroups
      - remoteAccessSecurityGroupId
