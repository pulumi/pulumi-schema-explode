$token: aws:eks/ClusterIdentityOidc:ClusterIdentityOidc
properties:
  issuer:
    type: string
    description: |
      Issuer URL for the OpenID Connect identity provider.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - issuer
