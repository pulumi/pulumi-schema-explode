$token: aws:eks/getClusterCertificateAuthority:getClusterCertificateAuthority
properties:
  data:
    type: string
    description: |
      The base64 encoded certificate data required to communicate with your cluster. Add this to the `certificate-authority-data` section of the `kubeconfig` file for your cluster.
    language:
      python:
        mapCase: false
type: object
required:
  - data
language:
  nodejs:
    requiredInputs: []
