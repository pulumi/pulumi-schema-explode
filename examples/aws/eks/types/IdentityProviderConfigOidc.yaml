$token: aws:eks/IdentityProviderConfigOidc:IdentityProviderConfigOidc
properties:
  clientId:
    type: string
    description: |
      Client ID for the OpenID Connect identity provider.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  groupsClaim:
    type: string
    description: |
      The JWT claim that the provider will use to return groups.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  groupsPrefix:
    type: string
    description: |
      A prefix that is prepended to group claims e.g., `oidc:`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  identityProviderConfigName:
    type: string
    description: |
      The name of the identity provider config.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  issuerUrl:
    type: string
    description: |
      Issuer URL for the OpenID Connect identity provider.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  requiredClaims:
    type: object
    additionalProperties:
      type: string
    description: |
      The key value pairs that describe required claims in the identity token.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  usernameClaim:
    type: string
    description: |
      The JWT claim that the provider will use as the username.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  usernamePrefix:
    type: string
    description: |
      A prefix that is prepended to username claims.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - clientId
  - identityProviderConfigName
  - issuerUrl
