$token: aws:eks/NodeGroupScalingConfig:NodeGroupScalingConfig
properties:
  desiredSize:
    type: integer
    description: |
      Desired number of worker nodes.
    language:
      python:
        mapCase: false
  maxSize:
    type: integer
    description: |
      Maximum number of worker nodes.
    language:
      python:
        mapCase: false
  minSize:
    type: integer
    description: |
      Minimum number of worker nodes.
    language:
      python:
        mapCase: false
type: object
required:
  - desiredSize
  - maxSize
  - minSize
