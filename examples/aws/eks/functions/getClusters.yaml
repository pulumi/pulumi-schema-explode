$token: aws:eks/getClusters:getClusters
description: |
  Retrieve EKS Clusters list
outputs:
  description: |
    A collection of values returned by getClusters.
  properties:
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    names:
      type: array
      items:
        type: string
      description: |
        Set of EKS clusters names
  type: object
  required:
    - names
    - id
