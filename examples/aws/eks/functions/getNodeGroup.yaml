$token: aws:eks/getNodeGroup:getNodeGroup
inputs:
  description: |
    A collection of arguments for invoking getNodeGroup.
  properties:
    clusterName:
      type: string
      description: |
        Name of the cluster.
    nodeGroupName:
      type: string
      description: |
        Name of the node group.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags.
  type: object
  required:
    - clusterName
    - nodeGroupName
outputs:
  description: |
    A collection of values returned by getNodeGroup.
  properties:
    amiType:
      type: string
      description: |
        Type of Amazon Machine Image (AMI) associated with the EKS Node Group.
    arn:
      type: string
      description: |
        ARN of the EKS Node Group.
    capacityType:
      type: string
      description: |
        Type of capacity associated with the EKS Node Group. Valid values: `ON_DEMAND`, `SPOT`.
    clusterName:
      type: string
    diskSize:
      type: integer
      description: |
        Disk size in GiB for worker nodes.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    instanceTypes:
      type: array
      items:
        type: string
      description: |
        Set of instance types associated with the EKS Node Group.
    labels:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of Kubernetes labels. Only labels that are applied with the EKS API are managed by this argument. Other Kubernetes labels applied to the EKS Node Group will not be managed.
    nodeGroupName:
      type: string
    nodeRoleArn:
      type: string
      description: |
        ARN of the IAM Role that provides permissions for the EKS Node Group.
    releaseVersion:
      type: string
      description: |
        AMI version of the EKS Node Group.
    remoteAccesses:
      type: array
      items:
        $ref: "#/types/aws:eks/getNodeGroupRemoteAccess:getNodeGroupRemoteAccess"
      description: |
        Configuration block with remote access settings.
    resources:
      type: array
      items:
        $ref: "#/types/aws:eks/getNodeGroupResource:getNodeGroupResource"
      description: |
        List of objects containing information about underlying resources.
    scalingConfigs:
      type: array
      items:
        $ref: "#/types/aws:eks/getNodeGroupScalingConfig:getNodeGroupScalingConfig"
      description: |
        Configuration block with scaling settings.
    status:
      type: string
      description: |
        Status of the EKS Node Group.
    subnetIds:
      type: array
      items:
        type: string
      description: |
        Identifiers of EC2 Subnets to associate with the EKS Node Group.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags.
    taints:
      type: array
      items:
        $ref: "#/types/aws:eks/getNodeGroupTaint:getNodeGroupTaint"
      description: |
        List of objects containing information about taints applied to the nodes in the EKS Node Group.
    version:
      type: string
      description: |
        Kubernetes version.
  type: object
  required:
    - amiType
    - arn
    - capacityType
    - clusterName
    - diskSize
    - instanceTypes
    - labels
    - nodeGroupName
    - nodeRoleArn
    - releaseVersion
    - remoteAccesses
    - resources
    - scalingConfigs
    - status
    - subnetIds
    - tags
    - taints
    - version
    - id
