$token: aws:lambda/invocation:Invocation
properties:
  functionName:
    type: string
    description: |
      Name of the lambda function.
  input:
    type: string
    description: |
      JSON payload to the lambda function.
  qualifier:
    type: string
    description: |
      Qualifier (i.e., version) of the lambda function. Defaults to `$LATEST`.
  result:
    type: string
    description: |
      String result of the lambda function invocation.
  triggers:
    type: object
    additionalProperties:
      type: string
required:
  - functionName
  - input
  - result
inputProperties:
  functionName:
    type: string
    description: |
      Name of the lambda function.
    willReplaceOnChanges: true
  input:
    type: string
    description: |
      JSON payload to the lambda function.
    willReplaceOnChanges: true
  qualifier:
    type: string
    description: |
      Qualifier (i.e., version) of the lambda function. Defaults to `$LATEST`.
    willReplaceOnChanges: true
  triggers:
    type: object
    additionalProperties:
      type: string
    willReplaceOnChanges: true
requiredInputs:
  - functionName
  - input
stateInputs:
  description: |
    Input properties used for looking up and filtering Invocation resources.
  properties:
    functionName:
      type: string
      description: |
        Name of the lambda function.
      willReplaceOnChanges: true
    input:
      type: string
      description: |
        JSON payload to the lambda function.
      willReplaceOnChanges: true
    qualifier:
      type: string
      description: |
        Qualifier (i.e., version) of the lambda function. Defaults to `$LATEST`.
      willReplaceOnChanges: true
    result:
      type: string
      description: |
        String result of the lambda function invocation.
    triggers:
      type: object
      additionalProperties:
        type: string
      willReplaceOnChanges: true
  type: object
