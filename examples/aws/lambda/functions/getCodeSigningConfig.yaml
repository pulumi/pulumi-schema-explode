$token: aws:lambda/getCodeSigningConfig:getCodeSigningConfig
inputs:
  description: |
    A collection of arguments for invoking getCodeSigningConfig.
  properties:
    arn:
      type: string
      description: |
        ARN of the code signing configuration.
  type: object
  required:
    - arn
outputs:
  description: |
    A collection of values returned by getCodeSigningConfig.
  properties:
    allowedPublishers:
      type: array
      items:
        $ref: "#/types/aws:lambda/getCodeSigningConfigAllowedPublisher:getCodeSigningConfigAllowedPublisher"
      description: |
        List of allowed publishers as signing profiles for this code signing configuration.
    arn:
      type: string
    configId:
      type: string
      description: |
        Unique identifier for the code signing configuration.
    description:
      type: string
      description: |
        Code signing configuration description.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    lastModified:
      type: string
      description: |
        Date and time that the code signing configuration was last modified.
    policies:
      type: array
      items:
        $ref: "#/types/aws:lambda/getCodeSigningConfigPolicy:getCodeSigningConfigPolicy"
      description: |
        List of code signing policies that control the validation failure action for signature mismatch or expiry.
  type: object
  required:
    - allowedPublishers
    - arn
    - configId
    - description
    - lastModified
    - policies
    - id
