$token: aws:lambda/FunctionEnvironment:FunctionEnvironment
properties:
  variables:
    type: object
    additionalProperties:
      type: string
    description: |
      Map of environment variables that are accessible from the function code during execution.
    language:
      python:
        mapCase: false
type: object
