$token: aws:lambda/FunctionImageConfig:FunctionImageConfig
properties:
  commands:
    type: array
    items:
      type: string
    description: |
      Parameters that you want to pass in with `entry_point`.
    language:
      python:
        mapCase: false
  entryPoints:
    type: array
    items:
      type: string
    description: |
      Entry point to your application, which is typically the location of the runtime executable.
    language:
      python:
        mapCase: false
  workingDirectory:
    type: string
    description: |
      Working directory.
    language:
      python:
        mapCase: false
type: object
