$token: aws:lambda/getFunctionTracingConfig:getFunctionTracingConfig
properties:
  mode:
    type: string
    language:
      python:
        mapCase: false
type: object
required:
  - mode
language:
  nodejs:
    requiredInputs: []
