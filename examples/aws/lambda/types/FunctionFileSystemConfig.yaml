$token: aws:lambda/FunctionFileSystemConfig:FunctionFileSystemConfig
properties:
  arn:
    type: string
    description: |
      Amazon Resource Name (ARN) of the Amazon EFS Access Point that provides access to the file system.
    language:
      python:
        mapCase: false
  localMountPath:
    type: string
    description: |
      Path where the function can access the file system, starting with /mnt/.
    language:
      python:
        mapCase: false
type: object
required:
  - arn
  - localMountPath
