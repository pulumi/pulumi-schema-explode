$token: aws:lambda/EventSourceMappingFilterCriteria:EventSourceMappingFilterCriteria
properties:
  filters:
    type: array
    items:
      $ref: "#/types/aws:lambda/EventSourceMappingFilterCriteriaFilter:EventSourceMappingFilterCriteriaFilter"
    description: |
      A set of up to 5 filter. If an event satisfies at least one, Lambda sends the event to the function or adds it to the next batch. Detailed below.
    language:
      python:
        mapCase: false
type: object
