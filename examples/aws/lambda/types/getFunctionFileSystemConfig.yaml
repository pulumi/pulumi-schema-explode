$token: aws:lambda/getFunctionFileSystemConfig:getFunctionFileSystemConfig
properties:
  arn:
    type: string
    description: |
      Unqualified (no `:QUALIFIER` or `:VERSION` suffix) ARN identifying your Lambda Function. See also `qualified_arn`.
    language:
      python:
        mapCase: false
  localMountPath:
    type: string
    language:
      python:
        mapCase: false
type: object
required:
  - arn
  - localMountPath
language:
  nodejs:
    requiredInputs: []
