$token: aws:lambda/getCodeSigningConfigAllowedPublisher:getCodeSigningConfigAllowedPublisher
properties:
  signingProfileVersionArns:
    type: array
    items:
      type: string
    description: |
      The ARN for each of the signing profiles. A signing profile defines a trusted user who can sign a code package.
    language:
      python:
        mapCase: false
type: object
required:
  - signingProfileVersionArns
language:
  nodejs:
    requiredInputs: []
