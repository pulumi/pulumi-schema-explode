$token: aws:mq/BrokerMaintenanceWindowStartTime:BrokerMaintenanceWindowStartTime
properties:
  dayOfWeek:
    type: string
    description: |
      Day of the week, e.g., `MONDAY`, `TUESDAY`, or `WEDNESDAY`.
    language:
      python:
        mapCase: false
  timeOfDay:
    type: string
    description: |
      Time, in 24-hour format, e.g., `02:00`.
    language:
      python:
        mapCase: false
  timeZone:
    type: string
    description: |
      Time zone in either the Country/City format or the UTC offset format, e.g., `CET`.
    language:
      python:
        mapCase: false
type: object
required:
  - dayOfWeek
  - timeOfDay
  - timeZone
