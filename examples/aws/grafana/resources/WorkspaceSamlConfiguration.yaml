$token: aws:grafana/workspaceSamlConfiguration:WorkspaceSamlConfiguration
properties:
  adminRoleValues:
    type: array
    items:
      type: string
    description: |
      The admin role values.
  allowedOrganizations:
    type: array
    items:
      type: string
    description: |
      The allowed organizations.
  editorRoleValues:
    type: array
    items:
      type: string
    description: |
      The editor role values.
  emailAssertion:
    type: string
    description: |
      The email assertion.
  groupsAssertion:
    type: string
    description: |
      The groups assertion.
  idpMetadataUrl:
    type: string
    description: |
      The IDP Metadata URL. Note that either `idp_metadata_url` or `idp_metadata_xml` (but not both) must be specified.
  idpMetadataXml:
    type: string
    description: |
      The IDP Metadata XML. Note that either `idp_metadata_url` or `idp_metadata_xml` (but not both) must be specified.
  loginAssertion:
    type: string
    description: |
      The login assertion.
  loginValidityDuration:
    type: integer
    description: |
      The login validity duration.
  nameAssertion:
    type: string
    description: |
      The name assertion.
  orgAssertion:
    type: string
    description: |
      The org assertion.
  roleAssertion:
    type: string
    description: |
      The role assertion.
  status:
    type: string
    description: |
      The status of the SAML configuration.
  workspaceId:
    type: string
    description: |
      The workspace id.
required:
  - editorRoleValues
  - emailAssertion
  - loginAssertion
  - loginValidityDuration
  - nameAssertion
  - status
  - workspaceId
inputProperties:
  adminRoleValues:
    type: array
    items:
      type: string
    description: |
      The admin role values.
  allowedOrganizations:
    type: array
    items:
      type: string
    description: |
      The allowed organizations.
  editorRoleValues:
    type: array
    items:
      type: string
    description: |
      The editor role values.
  emailAssertion:
    type: string
    description: |
      The email assertion.
  groupsAssertion:
    type: string
    description: |
      The groups assertion.
  idpMetadataUrl:
    type: string
    description: |
      The IDP Metadata URL. Note that either `idp_metadata_url` or `idp_metadata_xml` (but not both) must be specified.
  idpMetadataXml:
    type: string
    description: |
      The IDP Metadata XML. Note that either `idp_metadata_url` or `idp_metadata_xml` (but not both) must be specified.
  loginAssertion:
    type: string
    description: |
      The login assertion.
  loginValidityDuration:
    type: integer
    description: |
      The login validity duration.
  nameAssertion:
    type: string
    description: |
      The name assertion.
  orgAssertion:
    type: string
    description: |
      The org assertion.
  roleAssertion:
    type: string
    description: |
      The role assertion.
  workspaceId:
    type: string
    description: |
      The workspace id.
    willReplaceOnChanges: true
requiredInputs:
  - editorRoleValues
  - workspaceId
stateInputs:
  description: |
    Input properties used for looking up and filtering WorkspaceSamlConfiguration resources.
  properties:
    adminRoleValues:
      type: array
      items:
        type: string
      description: |
        The admin role values.
    allowedOrganizations:
      type: array
      items:
        type: string
      description: |
        The allowed organizations.
    editorRoleValues:
      type: array
      items:
        type: string
      description: |
        The editor role values.
    emailAssertion:
      type: string
      description: |
        The email assertion.
    groupsAssertion:
      type: string
      description: |
        The groups assertion.
    idpMetadataUrl:
      type: string
      description: |
        The IDP Metadata URL. Note that either `idp_metadata_url` or `idp_metadata_xml` (but not both) must be specified.
    idpMetadataXml:
      type: string
      description: |
        The IDP Metadata XML. Note that either `idp_metadata_url` or `idp_metadata_xml` (but not both) must be specified.
    loginAssertion:
      type: string
      description: |
        The login assertion.
    loginValidityDuration:
      type: integer
      description: |
        The login validity duration.
    nameAssertion:
      type: string
      description: |
        The name assertion.
    orgAssertion:
      type: string
      description: |
        The org assertion.
    roleAssertion:
      type: string
      description: |
        The role assertion.
    status:
      type: string
      description: |
        The status of the SAML configuration.
    workspaceId:
      type: string
      description: |
        The workspace id.
      willReplaceOnChanges: true
  type: object
