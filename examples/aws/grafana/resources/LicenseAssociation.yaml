$token: aws:grafana/licenseAssociation:LicenseAssociation
properties:
  freeTrialExpiration:
    type: string
    description: |
      If `license_type` is set to `ENTERPRISE_FREE_TRIAL`, this is the expiration date of the free trial.
  licenseExpiration:
    type: string
    description: |
      If `license_type` is set to `ENTERPRISE`, this is the expiration date of the enterprise license.
  licenseType:
    type: string
    description: |
      The type of license for the workspace license association. Valid values are `ENTERPRISE` and `ENTERPRISE_FREE_TRIAL`.
  workspaceId:
    type: string
    description: |
      The workspace id.
required:
  - freeTrialExpiration
  - licenseExpiration
  - licenseType
  - workspaceId
inputProperties:
  licenseType:
    type: string
    description: |
      The type of license for the workspace license association. Valid values are `ENTERPRISE` and `ENTERPRISE_FREE_TRIAL`.
    willReplaceOnChanges: true
  workspaceId:
    type: string
    description: |
      The workspace id.
    willReplaceOnChanges: true
requiredInputs:
  - licenseType
  - workspaceId
stateInputs:
  description: |
    Input properties used for looking up and filtering LicenseAssociation resources.
  properties:
    freeTrialExpiration:
      type: string
      description: |
        If `license_type` is set to `ENTERPRISE_FREE_TRIAL`, this is the expiration date of the free trial.
    licenseExpiration:
      type: string
      description: |
        If `license_type` is set to `ENTERPRISE`, this is the expiration date of the enterprise license.
    licenseType:
      type: string
      description: |
        The type of license for the workspace license association. Valid values are `ENTERPRISE` and `ENTERPRISE_FREE_TRIAL`.
      willReplaceOnChanges: true
    workspaceId:
      type: string
      description: |
        The workspace id.
      willReplaceOnChanges: true
  type: object
