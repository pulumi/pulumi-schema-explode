$token: aws:apigateway/UsagePlanThrottleSettings:UsagePlanThrottleSettings
properties:
  burstLimit:
    type: integer
    description: |
      The API request burst limit, the maximum rate limit over a time ranging from one to a few seconds, depending upon whether the underlying token bucket is at its full capacity.
    language:
      python:
        mapCase: false
  rateLimit:
    type: number
    description: |
      The API request steady-state rate limit.
    language:
      python:
        mapCase: false
type: object
