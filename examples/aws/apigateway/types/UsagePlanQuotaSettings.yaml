$token: aws:apigateway/UsagePlanQuotaSettings:UsagePlanQuotaSettings
properties:
  limit:
    type: integer
    description: |
      Maximum number of requests that can be made in a given time period.
    language:
      python:
        mapCase: false
  offset:
    type: integer
    description: |
      Number of requests subtracted from the given limit in the initial time period.
    language:
      python:
        mapCase: false
  period:
    type: string
    description: |
      Time period in which the limit applies. Valid values are "DAY", "WEEK" or "MONTH".
    language:
      python:
        mapCase: false
type: object
required:
  - limit
  - period
