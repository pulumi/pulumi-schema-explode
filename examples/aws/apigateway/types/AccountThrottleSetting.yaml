$token: aws:apigateway/AccountThrottleSetting:AccountThrottleSetting
properties:
  burstLimit:
    type: integer
    description: |
      Absolute maximum number of times API Gateway allows the API to be called per second (RPS).
    language:
      python:
        mapCase: false
  rateLimit:
    type: number
    description: |
      Number of times API Gateway allows the API to be called per second on average (RPS).
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - burstLimit
      - rateLimit
