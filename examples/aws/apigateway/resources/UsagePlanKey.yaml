$token: aws:apigateway/usagePlanKey:UsagePlanKey
properties:
  keyId:
    type: string
    description: |
      Identifier of the API key resource.
  keyType:
    type: string
    description: |
      Type of the API key resource. Currently, the valid key type is API_KEY.
  name:
    type: string
    description: |
      Name of a usage plan key.
  usagePlanId:
    type: string
    description: |
      Id of the usage plan resource representing to associate the key to.
  value:
    type: string
    description: |
      Value of a usage plan key.
required:
  - keyId
  - keyType
  - name
  - usagePlanId
  - value
inputProperties:
  keyId:
    type: string
    description: |
      Identifier of the API key resource.
    willReplaceOnChanges: true
  keyType:
    type: string
    description: |
      Type of the API key resource. Currently, the valid key type is API_KEY.
    willReplaceOnChanges: true
  usagePlanId:
    type: string
    description: |
      Id of the usage plan resource representing to associate the key to.
    willReplaceOnChanges: true
requiredInputs:
  - keyId
  - keyType
  - usagePlanId
stateInputs:
  description: |
    Input properties used for looking up and filtering UsagePlanKey resources.
  properties:
    keyId:
      type: string
      description: |
        Identifier of the API key resource.
      willReplaceOnChanges: true
    keyType:
      type: string
      description: |
        Type of the API key resource. Currently, the valid key type is API_KEY.
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        Name of a usage plan key.
    usagePlanId:
      type: string
      description: |
        Id of the usage plan resource representing to associate the key to.
      willReplaceOnChanges: true
    value:
      type: string
      description: |
        Value of a usage plan key.
  type: object
