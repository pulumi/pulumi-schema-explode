$token: aws:apigateway/requestValidator:RequestValidator
properties:
  name:
    type: string
    description: |
      Name of the request validator
  restApi:
    type: string
    description: |
      ID of the associated Rest API
  validateRequestBody:
    type: boolean
    description: |
      Boolean whether to validate request body. Defaults to `false`.
  validateRequestParameters:
    type: boolean
    description: |
      Boolean whether to validate request parameters. Defaults to `false`.
required:
  - name
  - restApi
inputProperties:
  name:
    type: string
    description: |
      Name of the request validator
  restApi:
    type: string
    oneOf:
      - type: string
      - type: string
        $ref: "#/types/aws:apigateway/restApi:RestApi"
    description: |
      ID of the associated Rest API
    willReplaceOnChanges: true
  validateRequestBody:
    type: boolean
    description: |
      Boolean whether to validate request body. Defaults to `false`.
  validateRequestParameters:
    type: boolean
    description: |
      Boolean whether to validate request parameters. Defaults to `false`.
requiredInputs:
  - restApi
stateInputs:
  description: |
    Input properties used for looking up and filtering RequestValidator resources.
  properties:
    name:
      type: string
      description: |
        Name of the request validator
    restApi:
      type: string
      oneOf:
        - type: string
        - type: string
          $ref: "#/types/aws:apigateway/restApi:RestApi"
      description: |
        ID of the associated Rest API
      willReplaceOnChanges: true
    validateRequestBody:
      type: boolean
      description: |
        Boolean whether to validate request body. Defaults to `false`.
    validateRequestParameters:
      type: boolean
      description: |
        Boolean whether to validate request parameters. Defaults to `false`.
  type: object
