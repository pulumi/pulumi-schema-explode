$token: aws:apigateway/resource:Resource
properties:
  parentId:
    type: string
    description: |
      ID of the parent API resource
  path:
    type: string
    description: |
      Complete path for this API resource, including all parent paths.
  pathPart:
    type: string
    description: |
      Last path segment of this API resource.
  restApi:
    type: string
    description: |
      ID of the associated REST API
required:
  - parentId
  - path
  - pathPart
  - restApi
inputProperties:
  parentId:
    type: string
    description: |
      ID of the parent API resource
  pathPart:
    type: string
    description: |
      Last path segment of this API resource.
  restApi:
    type: string
    oneOf:
      - type: string
      - type: string
        $ref: "#/types/aws:apigateway/restApi:RestApi"
    description: |
      ID of the associated REST API
    willReplaceOnChanges: true
requiredInputs:
  - parentId
  - pathPart
  - restApi
stateInputs:
  description: |
    Input properties used for looking up and filtering Resource resources.
  properties:
    parentId:
      type: string
      description: |
        ID of the parent API resource
    path:
      type: string
      description: |
        Complete path for this API resource, including all parent paths.
    pathPart:
      type: string
      description: |
        Last path segment of this API resource.
    restApi:
      type: string
      oneOf:
        - type: string
        - type: string
          $ref: "#/types/aws:apigateway/restApi:RestApi"
      description: |
        ID of the associated REST API
      willReplaceOnChanges: true
  type: object
