$token: aws:apigateway/restApiPolicy:RestApiPolicy
properties:
  policy:
    type: string
    description: |
      JSON formatted policy document that controls access to the API Gateway.
  restApiId:
    type: string
    description: |
      ID of the REST API.
required:
  - policy
  - restApiId
inputProperties:
  policy:
    type: string
    description: |
      JSON formatted policy document that controls access to the API Gateway.
  restApiId:
    type: string
    description: |
      ID of the REST API.
    willReplaceOnChanges: true
requiredInputs:
  - policy
  - restApiId
stateInputs:
  description: |
    Input properties used for looking up and filtering RestApiPolicy resources.
  properties:
    policy:
      type: string
      description: |
        JSON formatted policy document that controls access to the API Gateway.
    restApiId:
      type: string
      description: |
        ID of the REST API.
      willReplaceOnChanges: true
  type: object
