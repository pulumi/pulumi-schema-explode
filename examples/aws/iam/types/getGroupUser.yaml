$token: aws:iam/getGroupUser:getGroupUser
properties:
  arn:
    type: string
    description: |
      User ARN.
    language:
      python:
        mapCase: false
  path:
    type: string
    description: |
      Path to the IAM user.
    language:
      python:
        mapCase: false
  userId:
    type: string
    description: |
      Stable and unique string identifying the IAM user.
    language:
      python:
        mapCase: false
  userName:
    type: string
    description: |
      Name of the IAM user.
    language:
      python:
        mapCase: false
type: object
required:
  - arn
  - path
  - userId
  - userName
language:
  nodejs:
    requiredInputs: []
