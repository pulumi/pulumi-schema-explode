$token: aws:iam/getPolicyDocumentStatement:getPolicyDocumentStatement
properties:
  actions:
    type: array
    items:
      type: string
    description: |
      List of actions that this statement either allows or denies. For example, `["ec2:RunInstances", "s3:*"]`.
    language:
      python:
        mapCase: false
  conditions:
    type: array
    items:
      $ref: "#/types/aws:iam/getPolicyDocumentStatementCondition:getPolicyDocumentStatementCondition"
    description: |
      Configuration block for a condition. Detailed below.
    language:
      python:
        mapCase: false
  effect:
    type: string
    description: |
      Whether this statement allows or denies the given actions. Valid values are `Allow` and `Deny`. Defaults to `Allow`.
    language:
      python:
        mapCase: false
  notActions:
    type: array
    items:
      type: string
    description: |
      List of actions that this statement does *not* apply to. Use to apply a policy statement to all actions *except* those listed.
    language:
      python:
        mapCase: false
  notPrincipals:
    type: array
    items:
      $ref: "#/types/aws:iam/getPolicyDocumentStatementNotPrincipal:getPolicyDocumentStatementNotPrincipal"
    description: |
      Like `principals` except these are principals that the statement does *not* apply to.
    language:
      python:
        mapCase: false
  notResources:
    type: array
    items:
      type: string
    description: |
      List of resource ARNs that this statement does *not* apply to. Use to apply a policy statement to all resources *except* those listed. Conflicts with `resources`.
    language:
      python:
        mapCase: false
  principals:
    type: array
    items:
      $ref: "#/types/aws:iam/getPolicyDocumentStatementPrincipal:getPolicyDocumentStatementPrincipal"
    description: |
      Configuration block for principals. Detailed below.
    language:
      python:
        mapCase: false
  resources:
    type: array
    items:
      type: string
    description: |
      List of resource ARNs that this statement applies to. This is required by AWS if used for an IAM policy. Conflicts with `not_resources`.
    language:
      python:
        mapCase: false
  sid:
    type: string
    description: |
      Sid (statement ID) is an identifier for a policy statement.
    language:
      python:
        mapCase: false
type: object
