$token: aws:iam/userPolicyAttachment:UserPolicyAttachment
properties:
  policyArn:
    type: string
    $ref: "#/types/aws:index/aRN:ARN"
    description: |
      The ARN of the policy you want to apply
  user:
    type: string
    description: |
      The user the policy should be applied to
required:
  - policyArn
  - user
inputProperties:
  policyArn:
    type: string
    $ref: "#/types/aws:index/aRN:ARN"
    description: |
      The ARN of the policy you want to apply
    willReplaceOnChanges: true
  user:
    type: string
    oneOf:
      - type: string
      - type: string
        $ref: "#/types/aws:iam/user:User"
    description: |
      The user the policy should be applied to
    willReplaceOnChanges: true
requiredInputs:
  - policyArn
  - user
stateInputs:
  description: |
    Input properties used for looking up and filtering UserPolicyAttachment resources.
  properties:
    policyArn:
      type: string
      $ref: "#/types/aws:index/aRN:ARN"
      description: |
        The ARN of the policy you want to apply
      willReplaceOnChanges: true
    user:
      type: string
      oneOf:
        - type: string
        - type: string
          $ref: "#/types/aws:iam/user:User"
      description: |
        The user the policy should be applied to
      willReplaceOnChanges: true
  type: object
