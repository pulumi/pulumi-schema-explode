$token: aws:iam/userGroupMembership:UserGroupMembership
properties:
  groups:
    type: array
    items:
      type: string
    description: |
      A list of IAM Groups to add the user to
  user:
    type: string
    description: |
      The name of the IAM User to add to groups
required:
  - groups
  - user
inputProperties:
  groups:
    type: array
    items:
      type: string
    description: |
      A list of IAM Groups to add the user to
  user:
    type: string
    description: |
      The name of the IAM User to add to groups
    willReplaceOnChanges: true
requiredInputs:
  - groups
  - user
stateInputs:
  description: |
    Input properties used for looking up and filtering UserGroupMembership resources.
  properties:
    groups:
      type: array
      items:
        type: string
      description: |
        A list of IAM Groups to add the user to
    user:
      type: string
      description: |
        The name of the IAM User to add to groups
      willReplaceOnChanges: true
  type: object
