$token: aws:iam/signingCertificate:SigningCertificate
properties:
  certificateBody:
    type: string
    description: |
      The contents of the signing certificate in PEM-encoded format.
  certificateId:
    type: string
    description: |
      The ID for the signing certificate.
  status:
    type: string
    description: |
      The status you want to assign to the certificate. `Active` means that the certificate can be used for programmatic calls to Amazon Web Services `Inactive` means that the certificate cannot be used.
  userName:
    type: string
    description: |
      The name of the user the signing certificate is for.
required:
  - certificateBody
  - certificateId
  - userName
inputProperties:
  certificateBody:
    type: string
    description: |
      The contents of the signing certificate in PEM-encoded format.
    willReplaceOnChanges: true
  status:
    type: string
    description: |
      The status you want to assign to the certificate. `Active` means that the certificate can be used for programmatic calls to Amazon Web Services `Inactive` means that the certificate cannot be used.
  userName:
    type: string
    description: |
      The name of the user the signing certificate is for.
    willReplaceOnChanges: true
requiredInputs:
  - certificateBody
  - userName
stateInputs:
  description: |
    Input properties used for looking up and filtering SigningCertificate resources.
  properties:
    certificateBody:
      type: string
      description: |
        The contents of the signing certificate in PEM-encoded format.
      willReplaceOnChanges: true
    certificateId:
      type: string
      description: |
        The ID for the signing certificate.
    status:
      type: string
      description: |
        The status you want to assign to the certificate. `Active` means that the certificate can be used for programmatic calls to Amazon Web Services `Inactive` means that the certificate cannot be used.
    userName:
      type: string
      description: |
        The name of the user the signing certificate is for.
      willReplaceOnChanges: true
  type: object
