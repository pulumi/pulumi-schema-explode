$token: aws:iam/groupPolicyAttachment:GroupPolicyAttachment
properties:
  group:
    type: string
    description: |
      The group the policy should be applied to
  policyArn:
    type: string
    $ref: "#/types/aws:index/aRN:ARN"
    description: |
      The ARN of the policy you want to apply
required:
  - group
  - policyArn
inputProperties:
  group:
    type: string
    oneOf:
      - type: string
      - type: string
        $ref: "#/types/aws:iam/group:Group"
    description: |
      The group the policy should be applied to
    willReplaceOnChanges: true
  policyArn:
    type: string
    $ref: "#/types/aws:index/aRN:ARN"
    description: |
      The ARN of the policy you want to apply
    willReplaceOnChanges: true
requiredInputs:
  - group
  - policyArn
stateInputs:
  description: |
    Input properties used for looking up and filtering GroupPolicyAttachment resources.
  properties:
    group:
      type: string
      oneOf:
        - type: string
        - type: string
          $ref: "#/types/aws:iam/group:Group"
      description: |
        The group the policy should be applied to
      willReplaceOnChanges: true
    policyArn:
      type: string
      $ref: "#/types/aws:index/aRN:ARN"
      description: |
        The ARN of the policy you want to apply
      willReplaceOnChanges: true
  type: object
