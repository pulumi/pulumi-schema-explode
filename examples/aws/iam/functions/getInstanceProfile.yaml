$token: aws:iam/getInstanceProfile:getInstanceProfile
inputs:
  description: |
    A collection of arguments for invoking getInstanceProfile.
  properties:
    name:
      type: string
      description: |
        Friendly IAM instance profile name to match.
  type: object
  required:
    - name
outputs:
  description: |
    A collection of values returned by getInstanceProfile.
  properties:
    arn:
      type: string
      description: |
        ARN.
    createDate:
      type: string
      description: |
        String representation of the date the instance profile was created.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    name:
      type: string
    path:
      type: string
      description: |
        Path to the instance profile.
    roleArn:
      type: string
      description: |
        Role ARN associated with this instance profile.
    roleId:
      type: string
      description: |
        Role ID associated with this instance profile.
    roleName:
      type: string
      description: |
        Role name associated with this instance profile.
  type: object
  required:
    - arn
    - createDate
    - name
    - path
    - roleArn
    - roleId
    - roleName
    - id
