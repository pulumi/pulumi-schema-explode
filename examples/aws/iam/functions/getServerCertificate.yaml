$token: aws:iam/getServerCertificate:getServerCertificate
inputs:
  description: |
    A collection of arguments for invoking getServerCertificate.
  properties:
    latest:
      type: boolean
      description: |
        sort results by expiration date. returns the certificate with expiration date in furthest in the future.
    name:
      type: string
      description: |
        exact name of the cert to lookup
    namePrefix:
      type: string
      description: |
        prefix of cert to filter by
    pathPrefix:
      type: string
      description: |
        prefix of path to filter by
  type: object
outputs:
  description: |
    A collection of values returned by getServerCertificate.
  properties:
    arn:
      type: string
    certificateBody:
      type: string
    certificateChain:
      type: string
    expirationDate:
      type: string
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    latest:
      type: boolean
    name:
      type: string
    namePrefix:
      type: string
    path:
      type: string
    pathPrefix:
      type: string
    uploadDate:
      type: string
  type: object
  required:
    - arn
    - certificateBody
    - certificateChain
    - expirationDate
    - name
    - path
    - uploadDate
    - id
