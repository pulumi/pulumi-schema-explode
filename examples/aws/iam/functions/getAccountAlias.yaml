$token: aws:iam/getAccountAlias:getAccountAlias
outputs:
  description: |
    A collection of values returned by getAccountAlias.
  properties:
    accountAlias:
      type: string
      description: |
        Alias associated with the AWS account.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
  type: object
  required:
    - accountAlias
    - id
