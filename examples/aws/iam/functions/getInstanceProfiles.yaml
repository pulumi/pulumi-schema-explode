$token: aws:iam/getInstanceProfiles:getInstanceProfiles
inputs:
  description: |
    A collection of arguments for invoking getInstanceProfiles.
  properties:
    roleName:
      type: string
      description: |
        IAM role name.
  type: object
  required:
    - roleName
outputs:
  description: |
    A collection of values returned by getInstanceProfiles.
  properties:
    arns:
      type: array
      items:
        type: string
      description: |
        Set of ARNs of instance profiles.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    names:
      type: array
      items:
        type: string
      description: |
        Set of IAM instance profile names.
    paths:
      type: array
      items:
        type: string
      description: |
        Set of IAM instance profile paths.
    roleName:
      type: string
  type: object
  required:
    - arns
    - names
    - paths
    - roleName
    - id
