$token: aws:transfer/workflow:Workflow
properties:
  arn:
    type: string
    description: |
      The Workflow ARN.
  description:
    type: string
    description: |
      A textual description for the workflow.
  onExceptionSteps:
    type: array
    items:
      $ref: "#/types/aws:transfer/WorkflowOnExceptionStep:WorkflowOnExceptionStep"
    description: |
      Specifies the steps (actions) to take if errors are encountered during execution of the workflow. See Workflow Steps below.
  steps:
    type: array
    items:
      $ref: "#/types/aws:transfer/WorkflowStep:WorkflowStep"
    description: |
      Specifies the details for the steps that are in the specified workflow. See Workflow Steps below.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Array that contains from 1 to 10 key/value pairs. See S3 Tags below.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - steps
  - tagsAll
inputProperties:
  description:
    type: string
    description: |
      A textual description for the workflow.
    willReplaceOnChanges: true
  onExceptionSteps:
    type: array
    items:
      $ref: "#/types/aws:transfer/WorkflowOnExceptionStep:WorkflowOnExceptionStep"
    description: |
      Specifies the steps (actions) to take if errors are encountered during execution of the workflow. See Workflow Steps below.
    willReplaceOnChanges: true
  steps:
    type: array
    items:
      $ref: "#/types/aws:transfer/WorkflowStep:WorkflowStep"
    description: |
      Specifies the details for the steps that are in the specified workflow. See Workflow Steps below.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Array that contains from 1 to 10 key/value pairs. See S3 Tags below.
requiredInputs:
  - steps
stateInputs:
  description: |
    Input properties used for looking up and filtering Workflow resources.
  properties:
    arn:
      type: string
      description: |
        The Workflow ARN.
    description:
      type: string
      description: |
        A textual description for the workflow.
      willReplaceOnChanges: true
    onExceptionSteps:
      type: array
      items:
        $ref: "#/types/aws:transfer/WorkflowOnExceptionStep:WorkflowOnExceptionStep"
      description: |
        Specifies the steps (actions) to take if errors are encountered during execution of the workflow. See Workflow Steps below.
      willReplaceOnChanges: true
    steps:
      type: array
      items:
        $ref: "#/types/aws:transfer/WorkflowStep:WorkflowStep"
      description: |
        Specifies the details for the steps that are in the specified workflow. See Workflow Steps below.
      willReplaceOnChanges: true
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Array that contains from 1 to 10 key/value pairs. See S3 Tags below.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
