$token: aws:transfer/UserPosixProfile:UserPosixProfile
properties:
  gid:
    type: integer
    description: |
      The POSIX group ID used for all EFS operations by this user.
    language:
      python:
        mapCase: false
  secondaryGids:
    type: array
    items:
      type: integer
    description: |
      The secondary POSIX group IDs used for all EFS operations by this user.
    language:
      python:
        mapCase: false
  uid:
    type: integer
    description: |
      The POSIX user ID used for all EFS operations by this user.
    language:
      python:
        mapCase: false
type: object
required:
  - gid
  - uid
