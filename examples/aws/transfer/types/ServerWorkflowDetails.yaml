$token: aws:transfer/ServerWorkflowDetails:ServerWorkflowDetails
properties:
  onUpload:
    $ref: "#/types/aws:transfer/ServerWorkflowDetailsOnUpload:ServerWorkflowDetailsOnUpload"
    description: |
      A trigger that starts a workflow: the workflow begins to execute after a file is uploaded. See Workflow Detail below.
    language:
      python:
        mapCase: false
type: object
