$token: aws:transfer/UserHomeDirectoryMapping:UserHomeDirectoryMapping
properties:
  entry:
    type: string
    description: |
      Represents an entry and a target.
    language:
      python:
        mapCase: false
  target:
    type: string
    description: |
      Represents the map target.
    language:
      python:
        mapCase: false
type: object
required:
  - entry
  - target
