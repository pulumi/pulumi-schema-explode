$token: aws:transfer/WorkflowStep:WorkflowStep
properties:
  copyStepDetails:
    $ref: "#/types/aws:transfer/WorkflowStepCopyStepDetails:WorkflowStepCopyStepDetails"
    description: |
      Details for a step that performs a file copy. See Copy Step Details below.
    language:
      python:
        mapCase: false
  customStepDetails:
    $ref: "#/types/aws:transfer/WorkflowStepCustomStepDetails:WorkflowStepCustomStepDetails"
    description: |
      Details for a step that invokes a lambda function.
    language:
      python:
        mapCase: false
  deleteStepDetails:
    $ref: "#/types/aws:transfer/WorkflowStepDeleteStepDetails:WorkflowStepDeleteStepDetails"
    description: |
      Details for a step that deletes the file.
    language:
      python:
        mapCase: false
  tagStepDetails:
    $ref: "#/types/aws:transfer/WorkflowStepTagStepDetails:WorkflowStepTagStepDetails"
    description: |
      Details for a step that creates one or more tags.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      One of the following step types are supported. `COPY`, `CUSTOM`, `DELETE`, and `TAG`.
    language:
      python:
        mapCase: false
type: object
required:
  - type
