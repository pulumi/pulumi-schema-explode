$token: aws:transfer/ServerWorkflowDetailsOnUpload:ServerWorkflowDetailsOnUpload
properties:
  executionRole:
    type: string
    description: |
      Includes the necessary permissions for S3, EFS, and Lambda operations that Transfer can assume, so that all workflow steps can operate on the required resources.
    language:
      python:
        mapCase: false
  workflowId:
    type: string
    description: |
      A unique identifier for the workflow.
    language:
      python:
        mapCase: false
type: object
required:
  - executionRole
  - workflowId
