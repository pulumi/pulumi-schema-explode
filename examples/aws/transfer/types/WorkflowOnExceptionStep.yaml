$token: aws:transfer/WorkflowOnExceptionStep:WorkflowOnExceptionStep
properties:
  copyStepDetails:
    $ref: "#/types/aws:transfer/WorkflowOnExceptionStepCopyStepDetails:WorkflowOnExceptionStepCopyStepDetails"
    description: |
      Details for a step that performs a file copy. See Copy Step Details below.
    language:
      python:
        mapCase: false
  customStepDetails:
    $ref: "#/types/aws:transfer/WorkflowOnExceptionStepCustomStepDetails:WorkflowOnExceptionStepCustomStepDetails"
    description: |
      Details for a step that invokes a lambda function.
    language:
      python:
        mapCase: false
  deleteStepDetails:
    $ref: "#/types/aws:transfer/WorkflowOnExceptionStepDeleteStepDetails:WorkflowOnExceptionStepDeleteStepDetails"
    description: |
      Details for a step that deletes the file.
    language:
      python:
        mapCase: false
  tagStepDetails:
    $ref: "#/types/aws:transfer/WorkflowOnExceptionStepTagStepDetails:WorkflowOnExceptionStepTagStepDetails"
    description: |
      Details for a step that creates one or more tags.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      One of the following step types are supported. `COPY`, `CUSTOM`, `DELETE`, and `TAG`.
    language:
      python:
        mapCase: false
type: object
required:
  - type
