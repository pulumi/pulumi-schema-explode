$token: aws:location/getTrackerAssociations:getTrackerAssociations
inputs:
  description: |
    A collection of arguments for invoking getTrackerAssociations.
  properties:
    trackerName:
      type: string
      description: |
        Name of the tracker resource associated with a geofence collection.
  type: object
  required:
    - trackerName
outputs:
  description: |
    A collection of values returned by getTrackerAssociations.
  properties:
    consumerArns:
      type: array
      items:
        type: string
      description: |
        List of geofence collection ARNs associated to the tracker resource.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    trackerName:
      type: string
  type: object
  required:
    - consumerArns
    - trackerName
    - id
