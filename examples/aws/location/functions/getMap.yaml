$token: aws:location/getMap:getMap
inputs:
  description: |
    A collection of arguments for invoking getMap.
  properties:
    mapName:
      type: string
      description: |
        Name of the map resource.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags for the map.
  type: object
  required:
    - mapName
outputs:
  description: |
    A collection of values returned by getMap.
  properties:
    configurations:
      type: array
      items:
        $ref: "#/types/aws:location/getMapConfiguration:getMapConfiguration"
      description: |
        List of configurations that specify the map tile style selected from a partner data provider.
    createTime:
      type: string
      description: |
        Timestamp for when the map resource was created in ISO 8601 format.
    description:
      type: string
      description: |
        Optional description for the map resource.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    mapArn:
      type: string
      description: |
        ARN for the map resource.
    mapName:
      type: string
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags for the map.
    updateTime:
      type: string
      description: |
        Timestamp for when the map resource was last updated in ISO 8601 format.
  type: object
  required:
    - configurations
    - createTime
    - description
    - mapArn
    - mapName
    - tags
    - updateTime
    - id
