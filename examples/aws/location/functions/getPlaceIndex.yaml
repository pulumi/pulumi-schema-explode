$token: aws:location/getPlaceIndex:getPlaceIndex
inputs:
  description: |
    A collection of arguments for invoking getPlaceIndex.
  properties:
    indexName:
      type: string
      description: |
        Name of the place index resource.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags for the place index.
  type: object
  required:
    - indexName
outputs:
  description: |
    A collection of values returned by getPlaceIndex.
  properties:
    createTime:
      type: string
      description: |
        Timestamp for when the place index resource was created in ISO 8601 format.
    dataSource:
      type: string
      description: |
        Data provider of geospatial data.
    dataSourceConfigurations:
      type: array
      items:
        $ref: "#/types/aws:location/getPlaceIndexDataSourceConfiguration:getPlaceIndexDataSourceConfiguration"
      description: |
        List of configurations that specify data storage option for requesting Places.
    description:
      type: string
      description: |
        Optional description for the place index resource.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    indexArn:
      type: string
      description: |
        ARN for the place index resource.
    indexName:
      type: string
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags for the place index.
    updateTime:
      type: string
      description: |
        Timestamp for when the place index resource was last updated in ISO 8601 format.
  type: object
  required:
    - createTime
    - dataSource
    - dataSourceConfigurations
    - description
    - indexArn
    - indexName
    - tags
    - updateTime
    - id
