$token: aws:location/placeIndex:PlaceIndex
properties:
  createTime:
    type: string
    description: |
      The timestamp for when the place index resource was created in ISO 8601 format.
  dataSource:
    type: string
    description: |
      Specifies the geospatial data provider for the new place index.
  dataSourceConfiguration:
    $ref: "#/types/aws:location/PlaceIndexDataSourceConfiguration:PlaceIndexDataSourceConfiguration"
    description: |
      Configuration block with the data storage option chosen for requesting Places. Detailed below.
  description:
    type: string
    description: |
      The optional description for the place index resource.
  indexArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) for the place index resource. Used to specify a resource across AWS.
  indexName:
    type: string
    description: |
      The name of the place index resource.
  tags:
    type: object
    additionalProperties:
      type: string
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  updateTime:
    type: string
    description: |
      The timestamp for when the place index resource was last update in ISO 8601.
required:
  - createTime
  - dataSource
  - dataSourceConfiguration
  - indexArn
  - indexName
  - tagsAll
  - updateTime
inputProperties:
  dataSource:
    type: string
    description: |
      Specifies the geospatial data provider for the new place index.
    willReplaceOnChanges: true
  dataSourceConfiguration:
    $ref: "#/types/aws:location/PlaceIndexDataSourceConfiguration:PlaceIndexDataSourceConfiguration"
    description: |
      Configuration block with the data storage option chosen for requesting Places. Detailed below.
  description:
    type: string
    description: |
      The optional description for the place index resource.
  indexName:
    type: string
    description: |
      The name of the place index resource.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
requiredInputs:
  - dataSource
  - indexName
stateInputs:
  description: |
    Input properties used for looking up and filtering PlaceIndex resources.
  properties:
    createTime:
      type: string
      description: |
        The timestamp for when the place index resource was created in ISO 8601 format.
    dataSource:
      type: string
      description: |
        Specifies the geospatial data provider for the new place index.
      willReplaceOnChanges: true
    dataSourceConfiguration:
      $ref: "#/types/aws:location/PlaceIndexDataSourceConfiguration:PlaceIndexDataSourceConfiguration"
      description: |
        Configuration block with the data storage option chosen for requesting Places. Detailed below.
    description:
      type: string
      description: |
        The optional description for the place index resource.
    indexArn:
      type: string
      description: |
        The Amazon Resource Name (ARN) for the place index resource. Used to specify a resource across AWS.
    indexName:
      type: string
      description: |
        The name of the place index resource.
      willReplaceOnChanges: true
    tags:
      type: object
      additionalProperties:
        type: string
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    updateTime:
      type: string
      description: |
        The timestamp for when the place index resource was last update in ISO 8601.
  type: object
