$token: aws:mskconnect/getCustomPlugin:getCustomPlugin
inputs:
  description: |
    A collection of arguments for invoking getCustomPlugin.
  properties:
    name:
      type: string
      description: |
        Name of the custom plugin.
  type: object
  required:
    - name
outputs:
  description: |
    A collection of values returned by getCustomPlugin.
  properties:
    arn:
      type: string
      description: |
        the ARN of the custom plugin.
    description:
      type: string
      description: |
        a summary description of the custom plugin.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    latestRevision:
      type: integer
      description: |
        an ID of the latest successfully created revision of the custom plugin.
    name:
      type: string
    state:
      type: string
      description: |
        the state of the custom plugin.
  type: object
  required:
    - arn
    - description
    - latestRevision
    - name
    - state
    - id
