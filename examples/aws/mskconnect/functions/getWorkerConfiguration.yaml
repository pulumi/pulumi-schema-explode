$token: aws:mskconnect/getWorkerConfiguration:getWorkerConfiguration
inputs:
  description: |
    A collection of arguments for invoking getWorkerConfiguration.
  properties:
    name:
      type: string
      description: |
        Name of the worker configuration.
  type: object
  required:
    - name
outputs:
  description: |
    A collection of values returned by getWorkerConfiguration.
  properties:
    arn:
      type: string
      description: |
        the ARN of the worker configuration.
    description:
      type: string
      description: |
        a summary description of the worker configuration.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    latestRevision:
      type: integer
      description: |
        an ID of the latest successfully created revision of the worker configuration.
    name:
      type: string
    propertiesFileContent:
      type: string
      description: |
        contents of connect-distributed.properties file.
  type: object
  required:
    - arn
    - description
    - latestRevision
    - name
    - propertiesFileContent
    - id
