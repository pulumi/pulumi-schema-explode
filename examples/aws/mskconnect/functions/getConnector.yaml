$token: aws:mskconnect/getConnector:getConnector
inputs:
  description: |
    A collection of arguments for invoking getConnector.
  properties:
    name:
      type: string
      description: |
        Name of the connector.
  type: object
  required:
    - name
outputs:
  description: |
    A collection of values returned by getConnector.
  properties:
    arn:
      type: string
      description: |
        ARN of the connector.
    description:
      type: string
      description: |
        Summary description of the connector.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    name:
      type: string
    version:
      type: string
      description: |
        Current version of the connector.
  type: object
  required:
    - arn
    - description
    - name
    - version
    - id
