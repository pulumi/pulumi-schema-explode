$token: aws:mskconnect/ConnectorKafkaClusterClientAuthentication:ConnectorKafkaClusterClientAuthentication
properties:
  authenticationType:
    type: string
    description: |
      The type of client authentication used to connect to the Apache Kafka cluster. Valid values: `IAM`, `NONE`. A value of `NONE` means that no client authentication is used. The default value is `NONE`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
