$token: aws:mskconnect/ConnectorCapacityProvisionedCapacity:ConnectorCapacityProvisionedCapacity
properties:
  mcuCount:
    type: integer
    description: |
      The number of microcontroller units (MCUs) allocated to each connector worker. Valid values: `1`, `2`, `4`, `8`. The default value is `1`.
    language:
      python:
        mapCase: false
  workerCount:
    type: integer
    description: |
      The number of workers that are allocated to the connector.
    language:
      python:
        mapCase: false
type: object
required:
  - workerCount
