$token: aws:mskconnect/ConnectorKafkaClusterEncryptionInTransit:ConnectorKafkaClusterEncryptionInTransit
properties:
  encryptionType:
    type: string
    description: |
      The type of encryption in transit to the Apache Kafka cluster. Valid values: `PLAINTEXT`, `TLS`. The default values is `PLAINTEXT`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
