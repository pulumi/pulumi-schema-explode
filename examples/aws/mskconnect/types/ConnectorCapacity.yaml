$token: aws:mskconnect/ConnectorCapacity:ConnectorCapacity
properties:
  autoscaling:
    $ref: "#/types/aws:mskconnect/ConnectorCapacityAutoscaling:ConnectorCapacityAutoscaling"
    description: |
      Information about the auto scaling parameters for the connector. See below.
    language:
      python:
        mapCase: false
  provisionedCapacity:
    $ref: "#/types/aws:mskconnect/ConnectorCapacityProvisionedCapacity:ConnectorCapacityProvisionedCapacity"
    description: |
      Details about a fixed capacity allocated to a connector. See below.
    language:
      python:
        mapCase: false
type: object
