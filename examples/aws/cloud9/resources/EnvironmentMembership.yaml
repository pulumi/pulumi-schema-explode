$token: aws:cloud9/environmentMembership:EnvironmentMembership
properties:
  environmentId:
    type: string
    description: |
      The ID of the environment that contains the environment member you want to add.
  permissions:
    type: string
    description: |
      The type of environment member permissions you want to associate with this environment member. Allowed values are `read-only` and `read-write` .
  userArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the environment member you want to add.
  userId:
    type: string
    description: |
      he user ID in AWS Identity and Access Management (AWS IAM) of the environment member.
required:
  - environmentId
  - permissions
  - userArn
  - userId
inputProperties:
  environmentId:
    type: string
    description: |
      The ID of the environment that contains the environment member you want to add.
    willReplaceOnChanges: true
  permissions:
    type: string
    description: |
      The type of environment member permissions you want to associate with this environment member. Allowed values are `read-only` and `read-write` .
  userArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the environment member you want to add.
    willReplaceOnChanges: true
requiredInputs:
  - environmentId
  - permissions
  - userArn
stateInputs:
  description: |
    Input properties used for looking up and filtering EnvironmentMembership resources.
  properties:
    environmentId:
      type: string
      description: |
        The ID of the environment that contains the environment member you want to add.
      willReplaceOnChanges: true
    permissions:
      type: string
      description: |
        The type of environment member permissions you want to associate with this environment member. Allowed values are `read-only` and `read-write` .
    userArn:
      type: string
      description: |
        The Amazon Resource Name (ARN) of the environment member you want to add.
      willReplaceOnChanges: true
    userId:
      type: string
      description: |
        he user ID in AWS Identity and Access Management (AWS IAM) of the environment member.
  type: object
