$token: aws:worklink/FleetIdentityProvider:FleetIdentityProvider
properties:
  samlMetadata:
    type: string
    description: |
      The SAML metadata document provided by the customer’s identity provider.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      The type of identity provider.
    language:
      python:
        mapCase: false
type: object
required:
  - samlMetadata
  - type
