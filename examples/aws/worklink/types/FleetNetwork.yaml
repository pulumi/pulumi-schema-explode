$token: aws:worklink/FleetNetwork:FleetNetwork
properties:
  securityGroupIds:
    type: array
    items:
      type: string
    description: |
      A list of security group IDs associated with access to the provided subnets.
    language:
      python:
        mapCase: false
  subnetIds:
    type: array
    items:
      type: string
    description: |
      A list of subnet IDs used for X-ENI connections from Amazon WorkLink rendering containers.
    language:
      python:
        mapCase: false
  vpcId:
    type: string
    description: |
      The VPC ID with connectivity to associated websites.
    language:
      python:
        mapCase: false
type: object
required:
  - securityGroupIds
  - subnetIds
  - vpcId
