$token: aws:worklink/fleet:Fleet
properties:
  arn:
    type: string
    description: |
      The ARN of the created WorkLink Fleet.
  auditStreamArn:
    type: string
    description: |
      The ARN of the Amazon Kinesis data stream that receives the audit events. Kinesis data stream name must begin with `"AmazonWorkLink-"`.
  companyCode:
    type: string
    description: |
      The identifier used by users to sign in to the Amazon WorkLink app.
  createdTime:
    type: string
    description: |
      The time that the fleet was created.
  deviceCaCertificate:
    type: string
    description: |
      The certificate chain, including intermediate certificates and the root certificate authority certificate used to issue device certificates.
  displayName:
    type: string
    description: |
      The name of the fleet.
  identityProvider:
    $ref: "#/types/aws:worklink/FleetIdentityProvider:FleetIdentityProvider"
    description: |
      Provide this to allow manage the identity provider configuration for the fleet. Fields documented below.
  lastUpdatedTime:
    type: string
    description: |
      The time that the fleet was last updated.
  name:
    type: string
    description: |
      A region-unique name for the AMI.
  network:
    $ref: "#/types/aws:worklink/FleetNetwork:FleetNetwork"
    description: |
      Provide this to allow manage the company network configuration for the fleet. Fields documented below.
  optimizeForEndUserLocation:
    type: boolean
    description: |
      The option to optimize for better performance by routing traffic through the closest AWS Region to users, which may be outside of your home Region. Defaults to `true`.
required:
  - arn
  - companyCode
  - createdTime
  - lastUpdatedTime
  - name
inputProperties:
  auditStreamArn:
    type: string
    description: |
      The ARN of the Amazon Kinesis data stream that receives the audit events. Kinesis data stream name must begin with `"AmazonWorkLink-"`.
  deviceCaCertificate:
    type: string
    description: |
      The certificate chain, including intermediate certificates and the root certificate authority certificate used to issue device certificates.
  displayName:
    type: string
    description: |
      The name of the fleet.
  identityProvider:
    $ref: "#/types/aws:worklink/FleetIdentityProvider:FleetIdentityProvider"
    description: |
      Provide this to allow manage the identity provider configuration for the fleet. Fields documented below.
  name:
    type: string
    description: |
      A region-unique name for the AMI.
    willReplaceOnChanges: true
  network:
    $ref: "#/types/aws:worklink/FleetNetwork:FleetNetwork"
    description: |
      Provide this to allow manage the company network configuration for the fleet. Fields documented below.
  optimizeForEndUserLocation:
    type: boolean
    description: |
      The option to optimize for better performance by routing traffic through the closest AWS Region to users, which may be outside of your home Region. Defaults to `true`.
stateInputs:
  description: |
    Input properties used for looking up and filtering Fleet resources.
  properties:
    arn:
      type: string
      description: |
        The ARN of the created WorkLink Fleet.
    auditStreamArn:
      type: string
      description: |
        The ARN of the Amazon Kinesis data stream that receives the audit events. Kinesis data stream name must begin with `"AmazonWorkLink-"`.
    companyCode:
      type: string
      description: |
        The identifier used by users to sign in to the Amazon WorkLink app.
    createdTime:
      type: string
      description: |
        The time that the fleet was created.
    deviceCaCertificate:
      type: string
      description: |
        The certificate chain, including intermediate certificates and the root certificate authority certificate used to issue device certificates.
    displayName:
      type: string
      description: |
        The name of the fleet.
    identityProvider:
      $ref: "#/types/aws:worklink/FleetIdentityProvider:FleetIdentityProvider"
      description: |
        Provide this to allow manage the identity provider configuration for the fleet. Fields documented below.
    lastUpdatedTime:
      type: string
      description: |
        The time that the fleet was last updated.
    name:
      type: string
      description: |
        A region-unique name for the AMI.
      willReplaceOnChanges: true
    network:
      $ref: "#/types/aws:worklink/FleetNetwork:FleetNetwork"
      description: |
        Provide this to allow manage the company network configuration for the fleet. Fields documented below.
    optimizeForEndUserLocation:
      type: boolean
      description: |
        The option to optimize for better performance by routing traffic through the closest AWS Region to users, which may be outside of your home Region. Defaults to `true`.
  type: object
