$token: aws:medialive/MultiplexMultiplexSettings:MultiplexMultiplexSettings
properties:
  maximumVideoBufferDelayMilliseconds:
    type: integer
    description: |
      Maximum video buffer delay.
    language:
      python:
        mapCase: false
  transportStreamBitrate:
    type: integer
    description: |
      Transport stream bit rate.
    language:
      python:
        mapCase: false
  transportStreamId:
    type: integer
    description: |
      Unique ID for each multiplex.
    language:
      python:
        mapCase: false
  transportStreamReservedBitrate:
    type: integer
    description: |
      Transport stream reserved bit rate.
    language:
      python:
        mapCase: false
type: object
required:
  - transportStreamBitrate
  - transportStreamId
language:
  nodejs:
    requiredOutputs:
      - maximumVideoBufferDelayMilliseconds
      - transportStreamBitrate
      - transportStreamId
      - transportStreamReservedBitrate
