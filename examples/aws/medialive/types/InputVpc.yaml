$token: aws:medialive/InputVpc:InputVpc
properties:
  securityGroupIds:
    type: array
    items:
      type: string
    description: |
      A list of up to 5 EC2 VPC security group IDs to attach to the Input.
    language:
      python:
        mapCase: false
  subnetIds:
    type: array
    items:
      type: string
    description: |
      A list of 2 VPC subnet IDs from the same VPC.
    language:
      python:
        mapCase: false
type: object
required:
  - subnetIds
