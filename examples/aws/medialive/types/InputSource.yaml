$token: aws:medialive/InputSource:InputSource
properties:
  passwordParam:
    type: string
    description: |
      The key used to extract the password from EC2 Parameter store.
    language:
      python:
        mapCase: false
  url:
    type: string
    description: |
      The URL where the stream is pulled from.
    language:
      python:
        mapCase: false
  username:
    type: string
    description: |
      The username for the input source.
    language:
      python:
        mapCase: false
type: object
required:
  - passwordParam
  - url
  - username
