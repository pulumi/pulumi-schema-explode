$token: aws:medialive/inputSecurityGroup:InputSecurityGroup
properties:
  arn:
    type: string
    description: |
      ARN of the InputSecurityGroup.
  inputs:
    type: array
    items:
      type: string
    description: |
      The list of inputs currently using this InputSecurityGroup.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the InputSecurityGroup. If configured with a provider [`default_tags` configuration block](https://www.terraform.io/docs/providers/aws/index.html#default_tags-configuration-block) present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
  whitelistRules:
    type: array
    items:
      $ref: "#/types/aws:medialive/InputSecurityGroupWhitelistRule:InputSecurityGroupWhitelistRule"
    description: |
      Whitelist rules. See Whitelist Rules for more details.
required:
  - arn
  - inputs
  - tagsAll
  - whitelistRules
inputProperties:
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the InputSecurityGroup. If configured with a provider [`default_tags` configuration block](https://www.terraform.io/docs/providers/aws/index.html#default_tags-configuration-block) present, tags with matching keys will overwrite those defined at the provider-level.
  whitelistRules:
    type: array
    items:
      $ref: "#/types/aws:medialive/InputSecurityGroupWhitelistRule:InputSecurityGroupWhitelistRule"
    description: |
      Whitelist rules. See Whitelist Rules for more details.
requiredInputs:
  - whitelistRules
stateInputs:
  description: |
    Input properties used for looking up and filtering InputSecurityGroup resources.
  properties:
    arn:
      type: string
      description: |
        ARN of the InputSecurityGroup.
    inputs:
      type: array
      items:
        type: string
      description: |
        The list of inputs currently using this InputSecurityGroup.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the InputSecurityGroup. If configured with a provider [`default_tags` configuration block](https://www.terraform.io/docs/providers/aws/index.html#default_tags-configuration-block) present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
    whitelistRules:
      type: array
      items:
        $ref: "#/types/aws:medialive/InputSecurityGroupWhitelistRule:InputSecurityGroupWhitelistRule"
      description: |
        Whitelist rules. See Whitelist Rules for more details.
  type: object
