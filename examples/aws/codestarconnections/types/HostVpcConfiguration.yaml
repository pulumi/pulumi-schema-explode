$token: aws:codestarconnections/HostVpcConfiguration:HostVpcConfiguration
properties:
  securityGroupIds:
    type: array
    items:
      type: string
    description: |
      he ID of the security group or security groups associated with the Amazon VPC connected to the infrastructure where your provider type is installed.
    language:
      python:
        mapCase: false
  subnetIds:
    type: array
    items:
      type: string
    description: |
      The ID of the subnet or subnets associated with the Amazon VPC connected to the infrastructure where your provider type is installed.
    language:
      python:
        mapCase: false
  tlsCertificate:
    type: string
    description: |
      The value of the Transport Layer Security (TLS) certificate associated with the infrastructure where your provider type is installed.
    language:
      python:
        mapCase: false
  vpcId:
    type: string
    description: |
      The ID of the Amazon VPC connected to the infrastructure where your provider type is installed.
    language:
      python:
        mapCase: false
type: object
required:
  - securityGroupIds
  - subnetIds
  - vpcId
