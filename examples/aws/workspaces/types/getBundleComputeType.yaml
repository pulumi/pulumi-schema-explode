$token: aws:workspaces/getBundleComputeType:getBundleComputeType
properties:
  name:
    type: string
    description: |
      Name of the bundle. You cannot combine this parameter with `bundle_id`.
    language:
      python:
        mapCase: false
type: object
required:
  - name
language:
  nodejs:
    requiredInputs: []
