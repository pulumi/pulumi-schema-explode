$token: aws:workspaces/getBundleUserStorage:getBundleUserStorage
properties:
  capacity:
    type: string
    description: |
      Size of the user storage.
    language:
      python:
        mapCase: false
type: object
required:
  - capacity
language:
  nodejs:
    requiredInputs: []
