$token: aws:workspaces/getDirectorySelfServicePermission:getDirectorySelfServicePermission
properties:
  changeComputeType:
    type: boolean
    description: |
      Whether WorkSpaces directory users can change the compute type (bundle) for their workspace.
    language:
      python:
        mapCase: false
  increaseVolumeSize:
    type: boolean
    description: |
      Whether WorkSpaces directory users can increase the volume size of the drives on their workspace.
    language:
      python:
        mapCase: false
  rebuildWorkspace:
    type: boolean
    description: |
      Whether WorkSpaces directory users can rebuild the operating system of a workspace to its original state.
    language:
      python:
        mapCase: false
  restartWorkspace:
    type: boolean
    description: |
      Whether WorkSpaces directory users can restart their workspace.
    language:
      python:
        mapCase: false
  switchRunningMode:
    type: boolean
    description: |
      Whether WorkSpaces directory users can switch the running mode of their workspace.
    language:
      python:
        mapCase: false
type: object
required:
  - changeComputeType
  - increaseVolumeSize
  - rebuildWorkspace
  - restartWorkspace
  - switchRunningMode
language:
  nodejs:
    requiredInputs: []
