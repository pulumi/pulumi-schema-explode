$token: aws:workspaces/DirectoryWorkspaceAccessProperties:DirectoryWorkspaceAccessProperties
properties:
  deviceTypeAndroid:
    type: string
    description: |
      Indicates whether users can use Android devices to access their WorkSpaces.
    language:
      python:
        mapCase: false
  deviceTypeChromeos:
    type: string
    description: |
      Indicates whether users can use Chromebooks to access their WorkSpaces.
    language:
      python:
        mapCase: false
  deviceTypeIos:
    type: string
    description: |
      Indicates whether users can use iOS devices to access their WorkSpaces.
    language:
      python:
        mapCase: false
  deviceTypeLinux:
    type: string
    description: |
      Indicates whether users can use Linux clients to access their WorkSpaces.
    language:
      python:
        mapCase: false
  deviceTypeOsx:
    type: string
    description: |
      Indicates whether users can use macOS clients to access their WorkSpaces.
    language:
      python:
        mapCase: false
  deviceTypeWeb:
    type: string
    description: |
      Indicates whether users can access their WorkSpaces through a web browser.
    language:
      python:
        mapCase: false
  deviceTypeWindows:
    type: string
    description: |
      Indicates whether users can use Windows clients to access their WorkSpaces.
    language:
      python:
        mapCase: false
  deviceTypeZeroclient:
    type: string
    description: |
      Indicates whether users can use zero client devices to access their WorkSpaces.
    language:
      python:
        mapCase: false
type: object
