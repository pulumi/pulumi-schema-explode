$token: aws:workspaces/IpGroupRule:IpGroupRule
properties:
  description:
    type: string
    description: |
      The description.
    language:
      python:
        mapCase: false
  source:
    type: string
    description: |
      The IP address range, in CIDR notation, e.g., `10.0.0.0/16`
    language:
      python:
        mapCase: false
type: object
required:
  - source
