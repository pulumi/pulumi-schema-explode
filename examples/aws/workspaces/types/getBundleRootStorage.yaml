$token: aws:workspaces/getBundleRootStorage:getBundleRootStorage
properties:
  capacity:
    type: string
    description: |
      Size of the user storage.
    language:
      python:
        mapCase: false
type: object
required:
  - capacity
language:
  nodejs:
    requiredInputs: []
