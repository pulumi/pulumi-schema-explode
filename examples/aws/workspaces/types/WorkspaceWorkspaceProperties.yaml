$token: aws:workspaces/WorkspaceWorkspaceProperties:WorkspaceWorkspaceProperties
properties:
  computeTypeName:
    type: string
    description: |
      The compute type. For more information, see [Amazon WorkSpaces Bundles](http://aws.amazon.com/workspaces/details/#Amazon_WorkSpaces_Bundles). Valid values are `VALUE`, `STANDARD`, `PERFORMANCE`, `POWER`, `GRAPHICS`, `POWERPRO`, `GRAPHICSPRO`, `GRAPHICS_G4DN`, and `GRAPHICSPRO_G4DN`.
    language:
      python:
        mapCase: false
  rootVolumeSizeGib:
    type: integer
    description: |
      The size of the root volume.
    language:
      python:
        mapCase: false
  runningMode:
    type: string
    description: |
      The running mode. For more information, see [Manage the WorkSpace Running Mode](https://docs.aws.amazon.com/workspaces/latest/adminguide/running-mode.html). Valid values are `AUTO_STOP` and `ALWAYS_ON`.
    language:
      python:
        mapCase: false
  runningModeAutoStopTimeoutInMinutes:
    type: integer
    description: |
      The time after a user logs off when WorkSpaces are automatically stopped. Configured in 60-minute intervals.
    language:
      python:
        mapCase: false
  userVolumeSizeGib:
    type: integer
    description: |
      The size of the user storage.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - runningModeAutoStopTimeoutInMinutes
