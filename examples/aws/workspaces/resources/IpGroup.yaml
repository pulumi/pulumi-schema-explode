$token: aws:workspaces/ipGroup:IpGroup
properties:
  description:
    type: string
    description: |
      The description.
  name:
    type: string
    description: |
      The name of the IP group.
  rules:
    type: array
    items:
      $ref: "#/types/aws:workspaces/IpGroupRule:IpGroupRule"
    description: |
      One or more pairs specifying the IP group rule (in CIDR format) from which web requests originate.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the WorkSpaces directory. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - name
  - tagsAll
inputProperties:
  description:
    type: string
    description: |
      The description.
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      The name of the IP group.
    willReplaceOnChanges: true
  rules:
    type: array
    items:
      $ref: "#/types/aws:workspaces/IpGroupRule:IpGroupRule"
    description: |
      One or more pairs specifying the IP group rule (in CIDR format) from which web requests originate.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the WorkSpaces directory. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
stateInputs:
  description: |
    Input properties used for looking up and filtering IpGroup resources.
  properties:
    description:
      type: string
      description: |
        The description.
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        The name of the IP group.
      willReplaceOnChanges: true
    rules:
      type: array
      items:
        $ref: "#/types/aws:workspaces/IpGroupRule:IpGroupRule"
      description: |
        One or more pairs specifying the IP group rule (in CIDR format) from which web requests originate.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the WorkSpaces directory. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
