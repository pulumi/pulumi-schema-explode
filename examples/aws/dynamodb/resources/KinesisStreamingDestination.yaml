$token: aws:dynamodb/kinesisStreamingDestination:KinesisStreamingDestination
properties:
  streamArn:
    type: string
    description: |
      The ARN for a Kinesis data stream. This must exist in the same account and region as the DynamoDB table.
  tableName:
    type: string
    description: |
      The name of the DynamoDB table. There
      can only be one Kinesis streaming destination for a given DynamoDB table.
required:
  - streamArn
  - tableName
inputProperties:
  streamArn:
    type: string
    description: |
      The ARN for a Kinesis data stream. This must exist in the same account and region as the DynamoDB table.
    willReplaceOnChanges: true
  tableName:
    type: string
    description: |
      The name of the DynamoDB table. There
      can only be one Kinesis streaming destination for a given DynamoDB table.
    willReplaceOnChanges: true
requiredInputs:
  - streamArn
  - tableName
stateInputs:
  description: |
    Input properties used for looking up and filtering KinesisStreamingDestination resources.
  properties:
    streamArn:
      type: string
      description: |
        The ARN for a Kinesis data stream. This must exist in the same account and region as the DynamoDB table.
      willReplaceOnChanges: true
    tableName:
      type: string
      description: |
        The name of the DynamoDB table. There
        can only be one Kinesis streaming destination for a given DynamoDB table.
      willReplaceOnChanges: true
  type: object
