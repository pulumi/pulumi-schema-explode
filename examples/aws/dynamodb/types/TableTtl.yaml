$token: aws:dynamodb/TableTtl:TableTtl
properties:
  attributeName:
    type: string
    description: |
      Name of the table attribute to store the TTL timestamp in.
    language:
      python:
        mapCase: false
  enabled:
    type: boolean
    description: |
      Whether TTL is enabled.
    language:
      python:
        mapCase: false
type: object
required:
  - attributeName
