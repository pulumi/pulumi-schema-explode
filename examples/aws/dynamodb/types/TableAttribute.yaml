$token: aws:dynamodb/TableAttribute:TableAttribute
properties:
  name:
    type: string
    description: |
      Name of the index
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      Attribute type. Valid values are `S` (string), `N` (number), `B` (binary).
    language:
      python:
        mapCase: false
type: object
required:
  - name
  - type
