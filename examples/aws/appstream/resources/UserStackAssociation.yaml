$token: aws:appstream/userStackAssociation:UserStackAssociation
properties:
  authenticationType:
    type: string
    description: |
      Authentication type for the user.
  sendEmailNotification:
    type: boolean
    description: |
      Whether a welcome email is sent to a user after the user is created in the user pool.
  stackName:
    type: string
    description: |
      Name of the stack that is associated with the user.
  userName:
    type: string
    description: |
      Email address of the user who is associated with the stack.
required:
  - authenticationType
  - stackName
  - userName
inputProperties:
  authenticationType:
    type: string
    description: |
      Authentication type for the user.
    willReplaceOnChanges: true
  sendEmailNotification:
    type: boolean
    description: |
      Whether a welcome email is sent to a user after the user is created in the user pool.
  stackName:
    type: string
    description: |
      Name of the stack that is associated with the user.
    willReplaceOnChanges: true
  userName:
    type: string
    description: |
      Email address of the user who is associated with the stack.
    willReplaceOnChanges: true
requiredInputs:
  - authenticationType
  - stackName
  - userName
stateInputs:
  description: |
    Input properties used for looking up and filtering UserStackAssociation resources.
  properties:
    authenticationType:
      type: string
      description: |
        Authentication type for the user.
      willReplaceOnChanges: true
    sendEmailNotification:
      type: boolean
      description: |
        Whether a welcome email is sent to a user after the user is created in the user pool.
    stackName:
      type: string
      description: |
        Name of the stack that is associated with the user.
      willReplaceOnChanges: true
    userName:
      type: string
      description: |
        Email address of the user who is associated with the stack.
      willReplaceOnChanges: true
  type: object
