$token: aws:appstream/ImageBuilderVpcConfig:ImageBuilderVpcConfig
properties:
  securityGroupIds:
    type: array
    items:
      type: string
    description: |
      Identifiers of the security groups for the image builder or image builder.
    language:
      python:
        mapCase: false
  subnetIds:
    type: array
    items:
      type: string
    description: |
      Identifiers of the subnets to which a network interface is attached from the image builder instance or image builder instance.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - securityGroupIds
      - subnetIds
