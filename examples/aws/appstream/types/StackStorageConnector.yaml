$token: aws:appstream/StackStorageConnector:StackStorageConnector
properties:
  connectorType:
    type: string
    description: |
      Type of storage connector. Valid values are: `HOMEFOLDERS`, `GOOGLE_DRIVE`, `ONE_DRIVE`.
    language:
      python:
        mapCase: false
  domains:
    type: array
    items:
      type: string
    description: |
      Names of the domains for the account.
    language:
      python:
        mapCase: false
  resourceIdentifier:
    type: string
    description: |
      ARN of the storage connector.
    language:
      python:
        mapCase: false
type: object
required:
  - connectorType
language:
  nodejs:
    requiredOutputs:
      - connectorType
      - domains
      - resourceIdentifier
