$token: aws:appstream/ImageBuilderAccessEndpoint:ImageBuilderAccessEndpoint
properties:
  endpointType:
    type: string
    description: |
      Type of interface endpoint.
    language:
      python:
        mapCase: false
  vpceId:
    type: string
    description: |
      Identifier (ID) of the VPC in which the interface endpoint is used.
    language:
      python:
        mapCase: false
type: object
required:
  - endpointType
language:
  nodejs:
    requiredOutputs:
      - endpointType
      - vpceId
