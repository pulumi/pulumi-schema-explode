$token: aws:appstream/DirectoryConfigServiceAccountCredentials:DirectoryConfigServiceAccountCredentials
properties:
  accountName:
    type: string
    description: |
      User name of the account. This account must have the following privileges: create computer objects, join computers to the domain, and change/reset the password on descendant computer objects for the organizational units specified.
    language:
      python:
        mapCase: false
  accountPassword:
    type: string
    description: |
      Password for the account.
    language:
      python:
        mapCase: false
type: object
required:
  - accountName
  - accountPassword
