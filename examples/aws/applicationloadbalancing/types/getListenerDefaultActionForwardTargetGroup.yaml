$token: aws:applicationloadbalancing/getListenerDefaultActionForwardTargetGroup:getListenerDefaultActionForwardTargetGroup
properties:
  arn:
    type: string
    description: |
      ARN of the listener. Required if `load_balancer_arn` and `port` is not set.
    language:
      python:
        mapCase: false
  weight:
    type: integer
    language:
      python:
        mapCase: false
type: object
required:
  - arn
  - weight
language:
  nodejs:
    requiredInputs: []
