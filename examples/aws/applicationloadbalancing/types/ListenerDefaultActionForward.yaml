$token: aws:applicationloadbalancing/ListenerDefaultActionForward:ListenerDefaultActionForward
properties:
  stickiness:
    $ref: "#/types/aws:applicationloadbalancing/ListenerDefaultActionForwardStickiness:ListenerDefaultActionForwardStickiness"
    description: |
      Configuration block for target group stickiness for the rule. Detailed below.
    language:
      python:
        mapCase: false
  targetGroups:
    type: array
    items:
      $ref: "#/types/aws:applicationloadbalancing/ListenerDefaultActionForwardTargetGroup:ListenerDefaultActionForwardTargetGroup"
    description: |
      Set of 1-5 target group blocks. Detailed below.
    language:
      python:
        mapCase: false
type: object
required:
  - targetGroups
