$token: aws:applicationloadbalancing/listener:Listener
properties:
  alpnPolicy:
    type: string
    description: |
      Name of the Application-Layer Protocol Negotiation (ALPN) policy. Can be set if `protocol` is `TLS`. Valid values are `HTTP1Only`, `HTTP2Only`, `HTTP2Optional`, `HTTP2Preferred`, and `None`.
  arn:
    type: string
    description: |
      ARN of the target group.
  certificateArn:
    type: string
    description: |
      ARN of the default SSL server certificate. Exactly one certificate is required if the protocol is HTTPS. For adding additional SSL certificates, see the `aws.lb.ListenerCertificate` resource.
  defaultActions:
    type: array
    items:
      $ref: "#/types/aws:applicationloadbalancing/ListenerDefaultAction:ListenerDefaultAction"
    description: |
      Configuration block for default actions. Detailed below.
  loadBalancerArn:
    type: string
    description: |
      ARN of the load balancer.
  port:
    type: integer
    description: |
      Port. Specify a value from `1` to `65535` or `#{port}`. Defaults to `#{port}`.
  protocol:
    type: string
    description: |
      Protocol. Valid values are `HTTP`, `HTTPS`, or `#{protocol}`. Defaults to `#{protocol}`.
  sslPolicy:
    type: string
    description: |
      Name of the SSL Policy for the listener. Required if `protocol` is `HTTPS` or `TLS`.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - defaultActions
  - loadBalancerArn
  - protocol
  - sslPolicy
  - tagsAll
inputProperties:
  alpnPolicy:
    type: string
    description: |
      Name of the Application-Layer Protocol Negotiation (ALPN) policy. Can be set if `protocol` is `TLS`. Valid values are `HTTP1Only`, `HTTP2Only`, `HTTP2Optional`, `HTTP2Preferred`, and `None`.
  certificateArn:
    type: string
    description: |
      ARN of the default SSL server certificate. Exactly one certificate is required if the protocol is HTTPS. For adding additional SSL certificates, see the `aws.lb.ListenerCertificate` resource.
  defaultActions:
    type: array
    items:
      $ref: "#/types/aws:applicationloadbalancing/ListenerDefaultAction:ListenerDefaultAction"
    description: |
      Configuration block for default actions. Detailed below.
  loadBalancerArn:
    type: string
    description: |
      ARN of the load balancer.
    willReplaceOnChanges: true
  port:
    type: integer
    description: |
      Port. Specify a value from `1` to `65535` or `#{port}`. Defaults to `#{port}`.
  protocol:
    type: string
    description: |
      Protocol. Valid values are `HTTP`, `HTTPS`, or `#{protocol}`. Defaults to `#{protocol}`.
  sslPolicy:
    type: string
    description: |
      Name of the SSL Policy for the listener. Required if `protocol` is `HTTPS` or `TLS`.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - defaultActions
  - loadBalancerArn
stateInputs:
  description: |
    Input properties used for looking up and filtering Listener resources.
  properties:
    alpnPolicy:
      type: string
      description: |
        Name of the Application-Layer Protocol Negotiation (ALPN) policy. Can be set if `protocol` is `TLS`. Valid values are `HTTP1Only`, `HTTP2Only`, `HTTP2Optional`, `HTTP2Preferred`, and `None`.
    arn:
      type: string
      description: |
        ARN of the target group.
    certificateArn:
      type: string
      description: |
        ARN of the default SSL server certificate. Exactly one certificate is required if the protocol is HTTPS. For adding additional SSL certificates, see the `aws.lb.ListenerCertificate` resource.
    defaultActions:
      type: array
      items:
        $ref: "#/types/aws:applicationloadbalancing/ListenerDefaultAction:ListenerDefaultAction"
      description: |
        Configuration block for default actions. Detailed below.
    loadBalancerArn:
      type: string
      description: |
        ARN of the load balancer.
      willReplaceOnChanges: true
    port:
      type: integer
      description: |
        Port. Specify a value from `1` to `65535` or `#{port}`. Defaults to `#{port}`.
    protocol:
      type: string
      description: |
        Protocol. Valid values are `HTTP`, `HTTPS`, or `#{protocol}`. Defaults to `#{protocol}`.
    sslPolicy:
      type: string
      description: |
        Name of the SSL Policy for the listener. Required if `protocol` is `HTTPS` or `TLS`.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
deprecationMessage: aws.applicationloadbalancing.Listener has been deprecated in favor of aws.alb.Listener
