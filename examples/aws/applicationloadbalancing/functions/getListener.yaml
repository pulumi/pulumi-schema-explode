$token: aws:applicationloadbalancing/getListener:getListener
inputs:
  description: |
    A collection of arguments for invoking getListener.
  properties:
    arn:
      type: string
      description: |
        ARN of the listener. Required if `load_balancer_arn` and `port` is not set.
    loadBalancerArn:
      type: string
      description: |
        ARN of the load balancer. Required if `arn` is not set.
    port:
      type: integer
      description: |
        Port of the listener. Required if `arn` is not set.
    tags:
      type: object
      additionalProperties:
        type: string
  type: object
outputs:
  description: |
    A collection of values returned by getListener.
  properties:
    alpnPolicy:
      type: string
    arn:
      type: string
    certificateArn:
      type: string
    defaultActions:
      type: array
      items:
        $ref: "#/types/aws:applicationloadbalancing/getListenerDefaultAction:getListenerDefaultAction"
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    loadBalancerArn:
      type: string
    port:
      type: integer
    protocol:
      type: string
    sslPolicy:
      type: string
    tags:
      type: object
      additionalProperties:
        type: string
  type: object
  required:
    - alpnPolicy
    - arn
    - certificateArn
    - defaultActions
    - loadBalancerArn
    - port
    - protocol
    - sslPolicy
    - tags
    - id
deprecationMessage: aws.applicationloadbalancing.getListener has been deprecated in favor of aws.alb.getListener
