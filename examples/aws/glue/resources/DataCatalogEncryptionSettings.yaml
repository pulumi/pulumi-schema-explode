$token: aws:glue/dataCatalogEncryptionSettings:DataCatalogEncryptionSettings
properties:
  catalogId:
    type: string
    description: |
      The ID of the Data Catalog to set the security configuration for. If none is provided, the AWS account ID is used by default.
  dataCatalogEncryptionSettings:
    $ref: "#/types/aws:glue/DataCatalogEncryptionSettingsDataCatalogEncryptionSettings:DataCatalogEncryptionSettingsDataCatalogEncryptionSettings"
    description: |
      The security configuration to set. see Data Catalog Encryption Settings.
    language:
      csharp:
        name: DataCatalogEncryptionSettingsConfig
required:
  - catalogId
  - dataCatalogEncryptionSettings
inputProperties:
  catalogId:
    type: string
    description: |
      The ID of the Data Catalog to set the security configuration for. If none is provided, the AWS account ID is used by default.
    willReplaceOnChanges: true
  dataCatalogEncryptionSettings:
    $ref: "#/types/aws:glue/DataCatalogEncryptionSettingsDataCatalogEncryptionSettings:DataCatalogEncryptionSettingsDataCatalogEncryptionSettings"
    description: |
      The security configuration to set. see Data Catalog Encryption Settings.
    language:
      csharp:
        name: DataCatalogEncryptionSettingsConfig
requiredInputs:
  - dataCatalogEncryptionSettings
stateInputs:
  description: |
    Input properties used for looking up and filtering DataCatalogEncryptionSettings resources.
  properties:
    catalogId:
      type: string
      description: |
        The ID of the Data Catalog to set the security configuration for. If none is provided, the AWS account ID is used by default.
      willReplaceOnChanges: true
    dataCatalogEncryptionSettings:
      $ref: "#/types/aws:glue/DataCatalogEncryptionSettingsDataCatalogEncryptionSettings:DataCatalogEncryptionSettingsDataCatalogEncryptionSettings"
      description: |
        The security configuration to set. see Data Catalog Encryption Settings.
      language:
        csharp:
          name: DataCatalogEncryptionSettingsConfig
  type: object
