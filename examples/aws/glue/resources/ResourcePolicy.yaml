$token: aws:glue/resourcePolicy:ResourcePolicy
properties:
  enableHybrid:
    type: string
  policy:
    type: string
    description: |
      The policy to be applied to the aws glue data catalog.
required:
  - policy
inputProperties:
  enableHybrid:
    type: string
  policy:
    type: string
    description: |
      The policy to be applied to the aws glue data catalog.
requiredInputs:
  - policy
stateInputs:
  description: |
    Input properties used for looking up and filtering ResourcePolicy resources.
  properties:
    enableHybrid:
      type: string
    policy:
      type: string
      description: |
        The policy to be applied to the aws glue data catalog.
  type: object
