$token: aws:glue/partition:Partition
properties:
  catalogId:
    type: string
    description: |
      ID of the Glue Catalog and database to create the table in. If omitted, this defaults to the AWS Account ID plus the database name.
  creationTime:
    type: string
    description: |
      The time at which the partition was created.
  databaseName:
    type: string
    description: |
      Name of the metadata database where the table metadata resides. For Hive compatibility, this must be all lowercase.
  lastAccessedTime:
    type: string
    description: |
      The last time at which the partition was accessed.
  lastAnalyzedTime:
    type: string
    description: |
      The last time at which column statistics were computed for this partition.
  parameters:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of initialization parameters for the SerDe, in key-value form.
  partitionValues:
    type: array
    items:
      type: string
    description: |
      The values that define the partition.
  storageDescriptor:
    $ref: "#/types/aws:glue/PartitionStorageDescriptor:PartitionStorageDescriptor"
    description: |
      A storage descriptor object containing information about the physical storage of this table. You can refer to the [Glue Developer Guide](https://docs.aws.amazon.com/glue/latest/dg/aws-glue-api-catalog-tables.html#aws-glue-api-catalog-tables-StorageDescriptor) for a full explanation of this object.
  tableName:
    type: string
required:
  - catalogId
  - creationTime
  - databaseName
  - lastAccessedTime
  - lastAnalyzedTime
  - partitionValues
  - tableName
inputProperties:
  catalogId:
    type: string
    description: |
      ID of the Glue Catalog and database to create the table in. If omitted, this defaults to the AWS Account ID plus the database name.
    willReplaceOnChanges: true
  databaseName:
    type: string
    description: |
      Name of the metadata database where the table metadata resides. For Hive compatibility, this must be all lowercase.
    willReplaceOnChanges: true
  parameters:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of initialization parameters for the SerDe, in key-value form.
  partitionValues:
    type: array
    items:
      type: string
    description: |
      The values that define the partition.
    willReplaceOnChanges: true
  storageDescriptor:
    $ref: "#/types/aws:glue/PartitionStorageDescriptor:PartitionStorageDescriptor"
    description: |
      A storage descriptor object containing information about the physical storage of this table. You can refer to the [Glue Developer Guide](https://docs.aws.amazon.com/glue/latest/dg/aws-glue-api-catalog-tables.html#aws-glue-api-catalog-tables-StorageDescriptor) for a full explanation of this object.
  tableName:
    type: string
    willReplaceOnChanges: true
requiredInputs:
  - databaseName
  - partitionValues
  - tableName
stateInputs:
  description: |
    Input properties used for looking up and filtering Partition resources.
  properties:
    catalogId:
      type: string
      description: |
        ID of the Glue Catalog and database to create the table in. If omitted, this defaults to the AWS Account ID plus the database name.
      willReplaceOnChanges: true
    creationTime:
      type: string
      description: |
        The time at which the partition was created.
    databaseName:
      type: string
      description: |
        Name of the metadata database where the table metadata resides. For Hive compatibility, this must be all lowercase.
      willReplaceOnChanges: true
    lastAccessedTime:
      type: string
      description: |
        The last time at which the partition was accessed.
    lastAnalyzedTime:
      type: string
      description: |
        The last time at which column statistics were computed for this partition.
    parameters:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of initialization parameters for the SerDe, in key-value form.
    partitionValues:
      type: array
      items:
        type: string
      description: |
        The values that define the partition.
      willReplaceOnChanges: true
    storageDescriptor:
      $ref: "#/types/aws:glue/PartitionStorageDescriptor:PartitionStorageDescriptor"
      description: |
        A storage descriptor object containing information about the physical storage of this table. You can refer to the [Glue Developer Guide](https://docs.aws.amazon.com/glue/latest/dg/aws-glue-api-catalog-tables.html#aws-glue-api-catalog-tables-StorageDescriptor) for a full explanation of this object.
    tableName:
      type: string
      willReplaceOnChanges: true
  type: object
