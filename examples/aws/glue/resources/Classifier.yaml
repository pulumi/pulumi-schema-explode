$token: aws:glue/classifier:Classifier
properties:
  csvClassifier:
    $ref: "#/types/aws:glue/ClassifierCsvClassifier:ClassifierCsvClassifier"
    description: |
      A classifier for Csv content. Defined below.
  grokClassifier:
    $ref: "#/types/aws:glue/ClassifierGrokClassifier:ClassifierGrokClassifier"
    description: |
      A classifier that uses grok patterns. Defined below.
  jsonClassifier:
    $ref: "#/types/aws:glue/ClassifierJsonClassifier:ClassifierJsonClassifier"
    description: |
      A classifier for JSON content. Defined below.
  name:
    type: string
    description: |
      The name of the classifier.
  xmlClassifier:
    $ref: "#/types/aws:glue/ClassifierXmlClassifier:ClassifierXmlClassifier"
    description: |
      A classifier for XML content. Defined below.
required:
  - name
inputProperties:
  csvClassifier:
    $ref: "#/types/aws:glue/ClassifierCsvClassifier:ClassifierCsvClassifier"
    description: |
      A classifier for Csv content. Defined below.
  grokClassifier:
    $ref: "#/types/aws:glue/ClassifierGrokClassifier:ClassifierGrokClassifier"
    description: |
      A classifier that uses grok patterns. Defined below.
  jsonClassifier:
    $ref: "#/types/aws:glue/ClassifierJsonClassifier:ClassifierJsonClassifier"
    description: |
      A classifier for JSON content. Defined below.
  name:
    type: string
    description: |
      The name of the classifier.
    willReplaceOnChanges: true
  xmlClassifier:
    $ref: "#/types/aws:glue/ClassifierXmlClassifier:ClassifierXmlClassifier"
    description: |
      A classifier for XML content. Defined below.
stateInputs:
  description: |
    Input properties used for looking up and filtering Classifier resources.
  properties:
    csvClassifier:
      $ref: "#/types/aws:glue/ClassifierCsvClassifier:ClassifierCsvClassifier"
      description: |
        A classifier for Csv content. Defined below.
    grokClassifier:
      $ref: "#/types/aws:glue/ClassifierGrokClassifier:ClassifierGrokClassifier"
      description: |
        A classifier that uses grok patterns. Defined below.
    jsonClassifier:
      $ref: "#/types/aws:glue/ClassifierJsonClassifier:ClassifierJsonClassifier"
      description: |
        A classifier for JSON content. Defined below.
    name:
      type: string
      description: |
        The name of the classifier.
      willReplaceOnChanges: true
    xmlClassifier:
      $ref: "#/types/aws:glue/ClassifierXmlClassifier:ClassifierXmlClassifier"
      description: |
        A classifier for XML content. Defined below.
  type: object
