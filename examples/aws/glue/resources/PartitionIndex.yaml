$token: aws:glue/partitionIndex:PartitionIndex
properties:
  catalogId:
    type: string
    description: |
      The catalog ID where the table resides.
  databaseName:
    type: string
    description: |
      Name of the metadata database where the table metadata resides. For Hive compatibility, this must be all lowercase.
  partitionIndex:
    $ref: "#/types/aws:glue/PartitionIndexPartitionIndex:PartitionIndexPartitionIndex"
    description: |
      Configuration block for a partition index. See `partition_index` below.
    language:
      csharp:
        name: PartitionIndexConfig
  tableName:
    type: string
    description: |
      Name of the table. For Hive compatibility, this must be entirely lowercase.
required:
  - catalogId
  - databaseName
  - partitionIndex
  - tableName
inputProperties:
  catalogId:
    type: string
    description: |
      The catalog ID where the table resides.
    willReplaceOnChanges: true
  databaseName:
    type: string
    description: |
      Name of the metadata database where the table metadata resides. For Hive compatibility, this must be all lowercase.
    willReplaceOnChanges: true
  partitionIndex:
    $ref: "#/types/aws:glue/PartitionIndexPartitionIndex:PartitionIndexPartitionIndex"
    description: |
      Configuration block for a partition index. See `partition_index` below.
    language:
      csharp:
        name: PartitionIndexConfig
    willReplaceOnChanges: true
  tableName:
    type: string
    description: |
      Name of the table. For Hive compatibility, this must be entirely lowercase.
    willReplaceOnChanges: true
requiredInputs:
  - databaseName
  - partitionIndex
  - tableName
stateInputs:
  description: |
    Input properties used for looking up and filtering PartitionIndex resources.
  properties:
    catalogId:
      type: string
      description: |
        The catalog ID where the table resides.
      willReplaceOnChanges: true
    databaseName:
      type: string
      description: |
        Name of the metadata database where the table metadata resides. For Hive compatibility, this must be all lowercase.
      willReplaceOnChanges: true
    partitionIndex:
      $ref: "#/types/aws:glue/PartitionIndexPartitionIndex:PartitionIndexPartitionIndex"
      description: |
        Configuration block for a partition index. See `partition_index` below.
      language:
        csharp:
          name: PartitionIndexConfig
      willReplaceOnChanges: true
    tableName:
      type: string
      description: |
        Name of the table. For Hive compatibility, this must be entirely lowercase.
      willReplaceOnChanges: true
  type: object
