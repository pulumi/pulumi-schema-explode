$token: aws:glue/userDefinedFunction:UserDefinedFunction
properties:
  arn:
    type: string
  catalogId:
    type: string
    description: |
      ID of the Glue Catalog to create the function in. If omitted, this defaults to the AWS Account ID.
  className:
    type: string
    description: |
      The Java class that contains the function code.
  createTime:
    type: string
  databaseName:
    type: string
    description: |
      The name of the Database to create the Function.
  name:
    type: string
    description: |
      The name of the function.
  ownerName:
    type: string
    description: |
      The owner of the function.
  ownerType:
    type: string
    description: |
      The owner type. can be one of `USER`, `ROLE`, and `GROUP`.
  resourceUris:
    type: array
    items:
      $ref: "#/types/aws:glue/UserDefinedFunctionResourceUri:UserDefinedFunctionResourceUri"
    description: |
      The configuration block for Resource URIs. See resource uris below for more details.
required:
  - arn
  - className
  - createTime
  - databaseName
  - name
  - ownerName
  - ownerType
inputProperties:
  catalogId:
    type: string
    description: |
      ID of the Glue Catalog to create the function in. If omitted, this defaults to the AWS Account ID.
    willReplaceOnChanges: true
  className:
    type: string
    description: |
      The Java class that contains the function code.
  databaseName:
    type: string
    description: |
      The name of the Database to create the Function.
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      The name of the function.
    willReplaceOnChanges: true
  ownerName:
    type: string
    description: |
      The owner of the function.
  ownerType:
    type: string
    description: |
      The owner type. can be one of `USER`, `ROLE`, and `GROUP`.
  resourceUris:
    type: array
    items:
      $ref: "#/types/aws:glue/UserDefinedFunctionResourceUri:UserDefinedFunctionResourceUri"
    description: |
      The configuration block for Resource URIs. See resource uris below for more details.
requiredInputs:
  - className
  - databaseName
  - ownerName
  - ownerType
stateInputs:
  description: |
    Input properties used for looking up and filtering UserDefinedFunction resources.
  properties:
    arn:
      type: string
    catalogId:
      type: string
      description: |
        ID of the Glue Catalog to create the function in. If omitted, this defaults to the AWS Account ID.
      willReplaceOnChanges: true
    className:
      type: string
      description: |
        The Java class that contains the function code.
    createTime:
      type: string
    databaseName:
      type: string
      description: |
        The name of the Database to create the Function.
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        The name of the function.
      willReplaceOnChanges: true
    ownerName:
      type: string
      description: |
        The owner of the function.
    ownerType:
      type: string
      description: |
        The owner type. can be one of `USER`, `ROLE`, and `GROUP`.
    resourceUris:
      type: array
      items:
        $ref: "#/types/aws:glue/UserDefinedFunctionResourceUri:UserDefinedFunctionResourceUri"
      description: |
        The configuration block for Resource URIs. See resource uris below for more details.
  type: object
