$token: aws:glue/catalogTable:CatalogTable
properties:
  arn:
    type: string
    description: |
      The ARN of the Glue Table.
  catalogId:
    type: string
    description: |
      ID of the Data Catalog in which the table resides.
  databaseName:
    type: string
    description: |
      Name of the catalog database that contains the target table.
  description:
    type: string
    description: |
      Description of the table.
  name:
    type: string
    description: |
      Name of the target table.
  owner:
    type: string
    description: |
      Owner of the table.
  parameters:
    type: object
    additionalProperties:
      type: string
    description: |
      Map of initialization parameters for the SerDe, in key-value form.
  partitionIndices:
    type: array
    items:
      $ref: "#/types/aws:glue/CatalogTablePartitionIndex:CatalogTablePartitionIndex"
    description: |
      Configuration block for a maximum of 3 partition indexes. See `partition_index` below.
  partitionKeys:
    type: array
    items:
      $ref: "#/types/aws:glue/CatalogTablePartitionKey:CatalogTablePartitionKey"
    description: |
      Configuration block of columns by which the table is partitioned. Only primitive types are supported as partition keys. See `partition_keys` below.
  retention:
    type: integer
    description: |
      Retention time for this table.
  storageDescriptor:
    $ref: "#/types/aws:glue/CatalogTableStorageDescriptor:CatalogTableStorageDescriptor"
    description: |
      Configuration block for information about the physical storage of this table. For more information, refer to the [Glue Developer Guide](https://docs.aws.amazon.com/glue/latest/dg/aws-glue-api-catalog-tables.html#aws-glue-api-catalog-tables-StorageDescriptor). See `storage_descriptor` below.
  tableType:
    type: string
    description: |
      Type of this table (EXTERNAL_TABLE, VIRTUAL_VIEW, etc.). While optional, some Athena DDL queries such as `ALTER TABLE` and `SHOW CREATE TABLE` will fail if this argument is empty.
  targetTable:
    $ref: "#/types/aws:glue/CatalogTableTargetTable:CatalogTableTargetTable"
    description: |
      Configuration block of a target table for resource linking. See `target_table` below.
  viewExpandedText:
    type: string
    description: |
      If the table is a view, the expanded text of the view; otherwise null.
  viewOriginalText:
    type: string
    description: |
      If the table is a view, the original text of the view; otherwise null.
required:
  - arn
  - catalogId
  - databaseName
  - name
  - partitionIndices
inputProperties:
  catalogId:
    type: string
    description: |
      ID of the Data Catalog in which the table resides.
    willReplaceOnChanges: true
  databaseName:
    type: string
    description: |
      Name of the catalog database that contains the target table.
    willReplaceOnChanges: true
  description:
    type: string
    description: |
      Description of the table.
  name:
    type: string
    description: |
      Name of the target table.
    willReplaceOnChanges: true
  owner:
    type: string
    description: |
      Owner of the table.
  parameters:
    type: object
    additionalProperties:
      type: string
    description: |
      Map of initialization parameters for the SerDe, in key-value form.
  partitionIndices:
    type: array
    items:
      $ref: "#/types/aws:glue/CatalogTablePartitionIndex:CatalogTablePartitionIndex"
    description: |
      Configuration block for a maximum of 3 partition indexes. See `partition_index` below.
    willReplaceOnChanges: true
  partitionKeys:
    type: array
    items:
      $ref: "#/types/aws:glue/CatalogTablePartitionKey:CatalogTablePartitionKey"
    description: |
      Configuration block of columns by which the table is partitioned. Only primitive types are supported as partition keys. See `partition_keys` below.
  retention:
    type: integer
    description: |
      Retention time for this table.
  storageDescriptor:
    $ref: "#/types/aws:glue/CatalogTableStorageDescriptor:CatalogTableStorageDescriptor"
    description: |
      Configuration block for information about the physical storage of this table. For more information, refer to the [Glue Developer Guide](https://docs.aws.amazon.com/glue/latest/dg/aws-glue-api-catalog-tables.html#aws-glue-api-catalog-tables-StorageDescriptor). See `storage_descriptor` below.
  tableType:
    type: string
    description: |
      Type of this table (EXTERNAL_TABLE, VIRTUAL_VIEW, etc.). While optional, some Athena DDL queries such as `ALTER TABLE` and `SHOW CREATE TABLE` will fail if this argument is empty.
  targetTable:
    $ref: "#/types/aws:glue/CatalogTableTargetTable:CatalogTableTargetTable"
    description: |
      Configuration block of a target table for resource linking. See `target_table` below.
    willReplaceOnChanges: true
  viewExpandedText:
    type: string
    description: |
      If the table is a view, the expanded text of the view; otherwise null.
  viewOriginalText:
    type: string
    description: |
      If the table is a view, the original text of the view; otherwise null.
requiredInputs:
  - databaseName
stateInputs:
  description: |
    Input properties used for looking up and filtering CatalogTable resources.
  properties:
    arn:
      type: string
      description: |
        The ARN of the Glue Table.
    catalogId:
      type: string
      description: |
        ID of the Data Catalog in which the table resides.
      willReplaceOnChanges: true
    databaseName:
      type: string
      description: |
        Name of the catalog database that contains the target table.
      willReplaceOnChanges: true
    description:
      type: string
      description: |
        Description of the table.
    name:
      type: string
      description: |
        Name of the target table.
      willReplaceOnChanges: true
    owner:
      type: string
      description: |
        Owner of the table.
    parameters:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of initialization parameters for the SerDe, in key-value form.
    partitionIndices:
      type: array
      items:
        $ref: "#/types/aws:glue/CatalogTablePartitionIndex:CatalogTablePartitionIndex"
      description: |
        Configuration block for a maximum of 3 partition indexes. See `partition_index` below.
      willReplaceOnChanges: true
    partitionKeys:
      type: array
      items:
        $ref: "#/types/aws:glue/CatalogTablePartitionKey:CatalogTablePartitionKey"
      description: |
        Configuration block of columns by which the table is partitioned. Only primitive types are supported as partition keys. See `partition_keys` below.
    retention:
      type: integer
      description: |
        Retention time for this table.
    storageDescriptor:
      $ref: "#/types/aws:glue/CatalogTableStorageDescriptor:CatalogTableStorageDescriptor"
      description: |
        Configuration block for information about the physical storage of this table. For more information, refer to the [Glue Developer Guide](https://docs.aws.amazon.com/glue/latest/dg/aws-glue-api-catalog-tables.html#aws-glue-api-catalog-tables-StorageDescriptor). See `storage_descriptor` below.
    tableType:
      type: string
      description: |
        Type of this table (EXTERNAL_TABLE, VIRTUAL_VIEW, etc.). While optional, some Athena DDL queries such as `ALTER TABLE` and `SHOW CREATE TABLE` will fail if this argument is empty.
    targetTable:
      $ref: "#/types/aws:glue/CatalogTableTargetTable:CatalogTableTargetTable"
      description: |
        Configuration block of a target table for resource linking. See `target_table` below.
      willReplaceOnChanges: true
    viewExpandedText:
      type: string
      description: |
        If the table is a view, the expanded text of the view; otherwise null.
    viewOriginalText:
      type: string
      description: |
        If the table is a view, the original text of the view; otherwise null.
  type: object
