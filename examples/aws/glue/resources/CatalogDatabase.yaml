$token: aws:glue/catalogDatabase:CatalogDatabase
properties:
  arn:
    type: string
    description: |
      ARN of the Glue Catalog Database.
  catalogId:
    type: string
    description: |
      ID of the Data Catalog in which the database resides.
  createTableDefaultPermissions:
    type: array
    items:
      $ref: "#/types/aws:glue/CatalogDatabaseCreateTableDefaultPermission:CatalogDatabaseCreateTableDefaultPermission"
    description: |
      Creates a set of default permissions on the table for principals. See `create_table_default_permission` below.
  description:
    type: string
    description: |
      Description of the database.
  locationUri:
    type: string
    description: |
      Location of the database (for example, an HDFS path).
  name:
    type: string
    description: |
      Name of the database. The acceptable characters are lowercase letters, numbers, and the underscore character.
  parameters:
    type: object
    additionalProperties:
      type: string
    description: |
      List of key-value pairs that define parameters and properties of the database.
  targetDatabase:
    $ref: "#/types/aws:glue/CatalogDatabaseTargetDatabase:CatalogDatabaseTargetDatabase"
    description: |
      Configuration block for a target database for resource linking. See `target_database` below.
required:
  - arn
  - catalogId
  - createTableDefaultPermissions
  - locationUri
  - name
inputProperties:
  catalogId:
    type: string
    description: |
      ID of the Data Catalog in which the database resides.
    willReplaceOnChanges: true
  createTableDefaultPermissions:
    type: array
    items:
      $ref: "#/types/aws:glue/CatalogDatabaseCreateTableDefaultPermission:CatalogDatabaseCreateTableDefaultPermission"
    description: |
      Creates a set of default permissions on the table for principals. See `create_table_default_permission` below.
  description:
    type: string
    description: |
      Description of the database.
  locationUri:
    type: string
    description: |
      Location of the database (for example, an HDFS path).
  name:
    type: string
    description: |
      Name of the database. The acceptable characters are lowercase letters, numbers, and the underscore character.
    willReplaceOnChanges: true
  parameters:
    type: object
    additionalProperties:
      type: string
    description: |
      List of key-value pairs that define parameters and properties of the database.
  targetDatabase:
    $ref: "#/types/aws:glue/CatalogDatabaseTargetDatabase:CatalogDatabaseTargetDatabase"
    description: |
      Configuration block for a target database for resource linking. See `target_database` below.
    willReplaceOnChanges: true
stateInputs:
  description: |
    Input properties used for looking up and filtering CatalogDatabase resources.
  properties:
    arn:
      type: string
      description: |
        ARN of the Glue Catalog Database.
    catalogId:
      type: string
      description: |
        ID of the Data Catalog in which the database resides.
      willReplaceOnChanges: true
    createTableDefaultPermissions:
      type: array
      items:
        $ref: "#/types/aws:glue/CatalogDatabaseCreateTableDefaultPermission:CatalogDatabaseCreateTableDefaultPermission"
      description: |
        Creates a set of default permissions on the table for principals. See `create_table_default_permission` below.
    description:
      type: string
      description: |
        Description of the database.
    locationUri:
      type: string
      description: |
        Location of the database (for example, an HDFS path).
    name:
      type: string
      description: |
        Name of the database. The acceptable characters are lowercase letters, numbers, and the underscore character.
      willReplaceOnChanges: true
    parameters:
      type: object
      additionalProperties:
        type: string
      description: |
        List of key-value pairs that define parameters and properties of the database.
    targetDatabase:
      $ref: "#/types/aws:glue/CatalogDatabaseTargetDatabase:CatalogDatabaseTargetDatabase"
      description: |
        Configuration block for a target database for resource linking. See `target_database` below.
      willReplaceOnChanges: true
  type: object
