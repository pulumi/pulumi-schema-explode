$token: aws:glue/getDataCatalogEncryptionSettings:getDataCatalogEncryptionSettings
inputs:
  description: |
    A collection of arguments for invoking getDataCatalogEncryptionSettings.
  properties:
    catalogId:
      type: string
      description: |
        ID of the Data Catalog. This is typically the AWS account ID.
  type: object
  required:
    - catalogId
outputs:
  description: |
    A collection of values returned by getDataCatalogEncryptionSettings.
  properties:
    catalogId:
      type: string
    dataCatalogEncryptionSettings:
      type: array
      items:
        $ref: "#/types/aws:glue/getDataCatalogEncryptionSettingsDataCatalogEncryptionSetting:getDataCatalogEncryptionSettingsDataCatalogEncryptionSetting"
      description: |
        The security configuration to set. see Data Catalog Encryption Settings.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
  type: object
  required:
    - catalogId
    - dataCatalogEncryptionSettings
    - id
