$token: aws:glue/CatalogTableStorageDescriptorSerDeInfo:CatalogTableStorageDescriptorSerDeInfo
properties:
  name:
    type: string
    description: |
      Name of the target table.
    language:
      python:
        mapCase: false
  parameters:
    type: object
    additionalProperties:
      type: string
    description: |
      Map of initialization parameters for the SerDe, in key-value form.
    language:
      python:
        mapCase: false
  serializationLibrary:
    type: string
    description: |
      Usually the class that implements the SerDe. An example is `org.apache.hadoop.hive.serde2.columnar.ColumnarSerDe`.
    language:
      python:
        mapCase: false
type: object
