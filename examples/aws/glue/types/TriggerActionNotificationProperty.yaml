$token: aws:glue/TriggerActionNotificationProperty:TriggerActionNotificationProperty
properties:
  notifyDelayAfter:
    type: integer
    description: |
      After a job run starts, the number of minutes to wait before sending a job run delay notification.
    language:
      python:
        mapCase: false
type: object
