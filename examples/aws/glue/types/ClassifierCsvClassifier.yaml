$token: aws:glue/ClassifierCsvClassifier:ClassifierCsvClassifier
properties:
  allowSingleColumn:
    type: boolean
    description: |
      Enables the processing of files that contain only one column.
    language:
      python:
        mapCase: false
  containsHeader:
    type: string
    description: |
      Indicates whether the CSV file contains a header. This can be one of "ABSENT", "PRESENT", or "UNKNOWN".
    language:
      python:
        mapCase: false
  delimiter:
    type: string
    description: |
      The delimiter used in the Csv to separate columns.
    language:
      python:
        mapCase: false
  disableValueTrimming:
    type: boolean
    description: |
      Specifies whether to trim column values.
    language:
      python:
        mapCase: false
  headers:
    type: array
    items:
      type: string
    description: |
      A list of strings representing column names.
    language:
      python:
        mapCase: false
  quoteSymbol:
    type: string
    description: |
      A custom symbol to denote what combines content into a single column value. It must be different from the column delimiter.
    language:
      python:
        mapCase: false
type: object
