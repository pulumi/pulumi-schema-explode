$token: aws:glue/CatalogTablePartitionIndex:CatalogTablePartitionIndex
properties:
  indexName:
    type: string
    description: |
      Name of the partition index.
    language:
      python:
        mapCase: false
  indexStatus:
    type: string
    language:
      python:
        mapCase: false
  keys:
    type: array
    items:
      type: string
    description: |
      Keys for the partition index.
    language:
      python:
        mapCase: false
type: object
required:
  - indexName
  - keys
language:
  nodejs:
    requiredOutputs:
      - indexName
      - indexStatus
      - keys
