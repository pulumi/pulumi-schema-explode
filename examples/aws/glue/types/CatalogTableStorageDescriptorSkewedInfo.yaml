$token: aws:glue/CatalogTableStorageDescriptorSkewedInfo:CatalogTableStorageDescriptorSkewedInfo
properties:
  skewedColumnNames:
    type: array
    items:
      type: string
    description: |
      List of names of columns that contain skewed values.
    language:
      python:
        mapCase: false
  skewedColumnValueLocationMaps:
    type: object
    additionalProperties:
      type: string
    description: |
      List of values that appear so frequently as to be considered skewed.
    language:
      python:
        mapCase: false
  skewedColumnValues:
    type: array
    items:
      type: string
    description: |
      Map of skewed values to the columns that contain them.
    language:
      python:
        mapCase: false
type: object
