$token: aws:glue/CatalogTableStorageDescriptorSchemaReferenceSchemaId:CatalogTableStorageDescriptorSchemaReferenceSchemaId
properties:
  registryName:
    type: string
    description: |
      Name of the schema registry that contains the schema. Must be provided when `schema_name` is specified and conflicts with `schema_arn`.
    language:
      python:
        mapCase: false
  schemaArn:
    type: string
    description: |
      ARN of the schema. One of `schema_arn` or `schema_name` has to be provided.
    language:
      python:
        mapCase: false
  schemaName:
    type: string
    description: |
      Name of the schema. One of `schema_arn` or `schema_name` has to be provided.
    language:
      python:
        mapCase: false
type: object
