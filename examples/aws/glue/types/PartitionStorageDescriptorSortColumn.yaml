$token: aws:glue/PartitionStorageDescriptorSortColumn:PartitionStorageDescriptorSortColumn
properties:
  column:
    type: string
    description: |
      The name of the column.
    language:
      python:
        mapCase: false
  sortOrder:
    type: integer
    description: |
      Indicates that the column is sorted in ascending order (== 1), or in descending order (==0).
    language:
      python:
        mapCase: false
type: object
required:
  - column
  - sortOrder
