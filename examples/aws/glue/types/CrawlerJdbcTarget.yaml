$token: aws:glue/CrawlerJdbcTarget:CrawlerJdbcTarget
properties:
  connectionName:
    type: string
    description: |
      The name of the connection to use to connect to the Delta table target.
    language:
      python:
        mapCase: false
  exclusions:
    type: array
    items:
      type: string
    description: |
      A list of glob patterns used to exclude from the crawl.
    language:
      python:
        mapCase: false
  path:
    type: string
    description: |
      The path of the Amazon DocumentDB or MongoDB target (database/collection).
    language:
      python:
        mapCase: false
type: object
required:
  - connectionName
  - path
