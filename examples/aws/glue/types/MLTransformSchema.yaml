$token: aws:glue/MLTransformSchema:MLTransformSchema
properties:
  dataType:
    type: string
    description: |
      The type of data in the column.
    language:
      python:
        mapCase: false
  name:
    type: string
    description: |
      The name you assign to this ML Transform. It must be unique in your account.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - dataType
      - name
