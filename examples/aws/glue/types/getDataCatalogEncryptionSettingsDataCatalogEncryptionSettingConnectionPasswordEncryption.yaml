$token: aws:glue/getDataCatalogEncryptionSettingsDataCatalogEncryptionSettingConnectionPasswordEncryption:getDataCatalogEncryptionSettingsDataCatalogEncryptionSettingConnectionPasswordEncryption
properties:
  awsKmsKeyId:
    type: string
    description: |
      KMS key ARN that is used to encrypt the connection password.
    language:
      python:
        mapCase: false
  returnConnectionPasswordEncrypted:
    type: boolean
    description: |
      When set to `true`, passwords remain encrypted in the responses of GetConnection and GetConnections. This encryption takes effect independently of the catalog encryption.
    language:
      python:
        mapCase: false
type: object
required:
  - awsKmsKeyId
  - returnConnectionPasswordEncrypted
language:
  nodejs:
    requiredInputs: []
