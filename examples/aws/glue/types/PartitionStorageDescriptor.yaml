$token: aws:glue/PartitionStorageDescriptor:PartitionStorageDescriptor
properties:
  bucketColumns:
    type: array
    items:
      type: string
    description: |
      A list of reducer grouping columns, clustering columns, and bucketing columns in the table.
    language:
      python:
        mapCase: false
  columns:
    type: array
    items:
      $ref: "#/types/aws:glue/PartitionStorageDescriptorColumn:PartitionStorageDescriptorColumn"
    description: |
      A list of the Columns in the table.
    language:
      python:
        mapCase: false
  compressed:
    type: boolean
    description: |
      True if the data in the table is compressed, or False if not.
    language:
      python:
        mapCase: false
  inputFormat:
    type: string
    description: |
      The input format: SequenceFileInputFormat (binary), or TextInputFormat, or a custom format.
    language:
      python:
        mapCase: false
  location:
    type: string
    description: |
      The physical location of the table. By default this takes the form of the warehouse location, followed by the database location in the warehouse, followed by the table name.
    language:
      python:
        mapCase: false
  numberOfBuckets:
    type: integer
    description: |
      Must be specified if the table contains any dimension columns.
    language:
      python:
        mapCase: false
  outputFormat:
    type: string
    description: |
      The output format: SequenceFileOutputFormat (binary), or IgnoreKeyTextOutputFormat, or a custom format.
    language:
      python:
        mapCase: false
  parameters:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of initialization parameters for the SerDe, in key-value form.
    language:
      python:
        mapCase: false
  serDeInfo:
    $ref: "#/types/aws:glue/PartitionStorageDescriptorSerDeInfo:PartitionStorageDescriptorSerDeInfo"
    description: |
      Serialization/deserialization (SerDe) information.
    language:
      python:
        mapCase: false
  skewedInfo:
    $ref: "#/types/aws:glue/PartitionStorageDescriptorSkewedInfo:PartitionStorageDescriptorSkewedInfo"
    description: |
      Information about values that appear very frequently in a column (skewed values).
    language:
      python:
        mapCase: false
  sortColumns:
    type: array
    items:
      $ref: "#/types/aws:glue/PartitionStorageDescriptorSortColumn:PartitionStorageDescriptorSortColumn"
    description: |
      A list of Order objects specifying the sort order of each bucket in the table.
    language:
      python:
        mapCase: false
  storedAsSubDirectories:
    type: boolean
    description: |
      True if the table data is stored in subdirectories, or False if not.
    language:
      python:
        mapCase: false
type: object
