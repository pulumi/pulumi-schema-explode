$token: aws:glue/CatalogTableStorageDescriptorSchemaReference:CatalogTableStorageDescriptorSchemaReference
properties:
  schemaId:
    $ref: "#/types/aws:glue/CatalogTableStorageDescriptorSchemaReferenceSchemaId:CatalogTableStorageDescriptorSchemaReferenceSchemaId"
    description: |
      Configuration block that contains schema identity fields. Either this or the `schema_version_id` has to be provided. See `schema_id` below.
    language:
      python:
        mapCase: false
  schemaVersionId:
    type: string
    description: |
      Unique ID assigned to a version of the schema. Either this or the `schema_id` has to be provided.
    language:
      python:
        mapCase: false
  schemaVersionNumber:
    type: integer
    description: |
      Version number of the schema.
    language:
      python:
        mapCase: false
type: object
required:
  - schemaVersionNumber
