$token: aws:glue/UserDefinedFunctionResourceUri:UserDefinedFunctionResourceUri
properties:
  resourceType:
    type: string
    description: |
      The type of the resource. can be one of `JAR`, `FILE`, and `ARCHIVE`.
    language:
      python:
        mapCase: false
  uri:
    type: string
    description: |
      The URI for accessing the resource.
    language:
      python:
        mapCase: false
type: object
required:
  - resourceType
  - uri
