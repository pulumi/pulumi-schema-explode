$token: aws:glue/ConnectionPhysicalConnectionRequirements:ConnectionPhysicalConnectionRequirements
properties:
  availabilityZone:
    type: string
    description: |
      The availability zone of the connection. This field is redundant and implied by `subnet_id`, but is currently an api requirement.
    language:
      python:
        mapCase: false
  securityGroupIdLists:
    type: array
    items:
      type: string
    description: |
      The security group ID list used by the connection.
    language:
      python:
        mapCase: false
  subnetId:
    type: string
    description: |
      The subnet ID used by the connection.
    language:
      python:
        mapCase: false
type: object
