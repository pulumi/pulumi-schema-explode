$token: aws:glue/CrawlerDeltaTarget:CrawlerDeltaTarget
properties:
  connectionName:
    type: string
    description: |
      The name of the connection to use to connect to the Delta table target.
    language:
      python:
        mapCase: false
  deltaTables:
    type: array
    items:
      type: string
    description: |
      A list of the Amazon S3 paths to the Delta tables.
    language:
      python:
        mapCase: false
  writeManifest:
    type: boolean
    description: |
      Specifies whether to write the manifest files to the Delta table path.
    language:
      python:
        mapCase: false
type: object
required:
  - connectionName
  - deltaTables
  - writeManifest
