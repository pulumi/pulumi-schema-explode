$token: aws:glue/CatalogTableStorageDescriptorSortColumn:CatalogTableStorageDescriptorSortColumn
properties:
  column:
    type: string
    description: |
      Name of the column.
    language:
      python:
        mapCase: false
  sortOrder:
    type: integer
    description: |
      Whether the column is sorted in ascending (`1`) or descending order (`0`).
    language:
      python:
        mapCase: false
type: object
required:
  - column
  - sortOrder
