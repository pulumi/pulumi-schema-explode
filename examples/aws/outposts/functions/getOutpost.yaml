$token: aws:outposts/getOutpost:getOutpost
inputs:
  description: |
    A collection of arguments for invoking getOutpost.
  properties:
    arn:
      type: string
      description: |
        ARN.
    id:
      type: string
      description: |
        Identifier of the Outpost.
    name:
      type: string
      description: |
        Name of the Outpost.
    ownerId:
      type: string
      description: |
        AWS Account identifier of the Outpost owner.
  type: object
outputs:
  description: |
    A collection of values returned by getOutpost.
  properties:
    arn:
      type: string
    availabilityZone:
      type: string
      description: |
        Availability Zone name.
    availabilityZoneId:
      type: string
      description: |
        Availability Zone identifier.
    description:
      type: string
      description: |
        Description.
    id:
      type: string
    name:
      type: string
    ownerId:
      type: string
    siteId:
      type: string
      description: |
        Site identifier.
  type: object
  required:
    - arn
    - availabilityZone
    - availabilityZoneId
    - description
    - id
    - name
    - ownerId
    - siteId
