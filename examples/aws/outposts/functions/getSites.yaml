$token: aws:outposts/getSites:getSites
outputs:
  description: |
    A collection of values returned by getSites.
  properties:
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    ids:
      type: array
      items:
        type: string
      description: |
        Set of Outposts Site identifiers.
  type: object
  required:
    - ids
    - id
