$token: aws:outposts/getSite:getSite
inputs:
  description: |
    A collection of arguments for invoking getSite.
  properties:
    id:
      type: string
      description: |
        Identifier of the Site.
    name:
      type: string
      description: |
        Name of the Site.
  type: object
outputs:
  description: |
    A collection of values returned by getSite.
  properties:
    accountId:
      type: string
      description: |
        AWS Account identifier.
    description:
      type: string
      description: |
        Description.
    id:
      type: string
    name:
      type: string
  type: object
  required:
    - accountId
    - description
    - id
    - name
