$token: aws:redshift/clusterIamRoles:ClusterIamRoles
properties:
  clusterIdentifier:
    type: string
    description: |
      The name of the Redshift Cluster IAM Roles.
  defaultIamRoleArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) for the IAM role that was set as default for the cluster when the cluster was created.
  iamRoleArns:
    type: array
    items:
      type: string
    description: |
      A list of IAM Role ARNs to associate with the cluster. A Maximum of 10 can be associated to the cluster at any time.
required:
  - clusterIdentifier
  - defaultIamRoleArn
  - iamRoleArns
inputProperties:
  clusterIdentifier:
    type: string
    description: |
      The name of the Redshift Cluster IAM Roles.
    willReplaceOnChanges: true
  defaultIamRoleArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) for the IAM role that was set as default for the cluster when the cluster was created.
  iamRoleArns:
    type: array
    items:
      type: string
    description: |
      A list of IAM Role ARNs to associate with the cluster. A Maximum of 10 can be associated to the cluster at any time.
requiredInputs:
  - clusterIdentifier
stateInputs:
  description: |
    Input properties used for looking up and filtering ClusterIamRoles resources.
  properties:
    clusterIdentifier:
      type: string
      description: |
        The name of the Redshift Cluster IAM Roles.
      willReplaceOnChanges: true
    defaultIamRoleArn:
      type: string
      description: |
        The Amazon Resource Name (ARN) for the IAM role that was set as default for the cluster when the cluster was created.
    iamRoleArns:
      type: array
      items:
        type: string
      description: |
        A list of IAM Role ARNs to associate with the cluster. A Maximum of 10 can be associated to the cluster at any time.
  type: object
