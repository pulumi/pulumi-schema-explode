$token: aws:redshift/parameterGroup:ParameterGroup
properties:
  arn:
    type: string
    description: |
      Amazon Resource Name (ARN) of parameter group
  description:
    type: string
    description: |
      The description of the Redshift parameter group. Defaults to "Managed by Pulumi".
    default: Managed by Pulumi
  family:
    type: string
    description: |
      The family of the Redshift parameter group.
  name:
    type: string
    description: |
      The name of the Redshift parameter.
  parameters:
    type: array
    items:
      $ref: "#/types/aws:redshift/ParameterGroupParameter:ParameterGroupParameter"
    description: |
      A list of Redshift parameters to apply.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - description
  - family
  - name
  - tagsAll
inputProperties:
  description:
    type: string
    description: |
      The description of the Redshift parameter group. Defaults to "Managed by Pulumi".
    default: Managed by Pulumi
    willReplaceOnChanges: true
  family:
    type: string
    description: |
      The family of the Redshift parameter group.
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      The name of the Redshift parameter.
    willReplaceOnChanges: true
  parameters:
    type: array
    items:
      $ref: "#/types/aws:redshift/ParameterGroupParameter:ParameterGroupParameter"
    description: |
      A list of Redshift parameters to apply.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - family
stateInputs:
  description: |
    Input properties used for looking up and filtering ParameterGroup resources.
  properties:
    arn:
      type: string
      description: |
        Amazon Resource Name (ARN) of parameter group
    description:
      type: string
      description: |
        The description of the Redshift parameter group. Defaults to "Managed by Pulumi".
      default: Managed by Pulumi
      willReplaceOnChanges: true
    family:
      type: string
      description: |
        The family of the Redshift parameter group.
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        The name of the Redshift parameter.
      willReplaceOnChanges: true
    parameters:
      type: array
      items:
        $ref: "#/types/aws:redshift/ParameterGroupParameter:ParameterGroupParameter"
      description: |
        A list of Redshift parameters to apply.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
