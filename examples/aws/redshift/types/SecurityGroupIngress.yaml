$token: aws:redshift/SecurityGroupIngress:SecurityGroupIngress
properties:
  cidr:
    type: string
    description: |
      The CIDR block to accept
    language:
      python:
        mapCase: false
  securityGroupName:
    type: string
    description: |
      The name of the security group to authorize
    language:
      python:
        mapCase: false
  securityGroupOwnerId:
    type: string
    description: |
      The owner Id of the security group provided
      by `security_group_name`.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - securityGroupName
      - securityGroupOwnerId
