$token: aws:redshift/EndpointAccessVpcEndpointNetworkInterface:EndpointAccessVpcEndpointNetworkInterface
properties:
  availabilityZone:
    type: string
    description: |
      The Availability Zone.
    language:
      python:
        mapCase: false
  networkInterfaceId:
    type: string
    description: |
      The network interface identifier.
    language:
      python:
        mapCase: false
  privateIpAddress:
    type: string
    description: |
      The IPv4 address of the network interface within the subnet.
    language:
      python:
        mapCase: false
  subnetId:
    type: string
    description: |
      The subnet identifier.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - availabilityZone
      - networkInterfaceId
      - privateIpAddress
      - subnetId
