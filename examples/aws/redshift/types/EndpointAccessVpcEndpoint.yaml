$token: aws:redshift/EndpointAccessVpcEndpoint:EndpointAccessVpcEndpoint
properties:
  networkInterfaces:
    type: array
    items:
      $ref: "#/types/aws:redshift/EndpointAccessVpcEndpointNetworkInterface:EndpointAccessVpcEndpointNetworkInterface"
    description: |
      One or more network interfaces of the endpoint. Also known as an interface endpoint. See details below.
    language:
      python:
        mapCase: false
  vpcEndpointId:
    type: string
    description: |
      The connection endpoint ID for connecting an Amazon Redshift cluster through the proxy.
    language:
      python:
        mapCase: false
  vpcId:
    type: string
    description: |
      The VPC identifier that the endpoint is associated.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - networkInterfaces
      - vpcEndpointId
      - vpcId
