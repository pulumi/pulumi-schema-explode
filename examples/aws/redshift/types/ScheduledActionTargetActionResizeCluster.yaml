$token: aws:redshift/ScheduledActionTargetActionResizeCluster:ScheduledActionTargetActionResizeCluster
properties:
  classic:
    type: boolean
    description: |
      A boolean value indicating whether the resize operation is using the classic resize process. Default: `false`.
    language:
      python:
        mapCase: false
  clusterIdentifier:
    type: string
    description: |
      The identifier of the cluster to be resumed.
    language:
      python:
        mapCase: false
  clusterType:
    type: string
    description: |
      The new cluster type for the specified cluster.
    language:
      python:
        mapCase: false
  nodeType:
    type: string
    description: |
      The new node type for the nodes you are adding.
    language:
      python:
        mapCase: false
  numberOfNodes:
    type: integer
    description: |
      The new number of nodes for the cluster.
    language:
      python:
        mapCase: false
type: object
required:
  - clusterIdentifier
