$token: aws:redshift/getClusterClusterNode:getClusterClusterNode
properties:
  nodeRole:
    type: string
    description: |
      Whether the node is a leader node or a compute node
    language:
      python:
        mapCase: false
  privateIpAddress:
    type: string
    description: |
      Private IP address of a node within a cluster
    language:
      python:
        mapCase: false
  publicIpAddress:
    type: string
    description: |
      Public IP address of a node within a cluster
    language:
      python:
        mapCase: false
type: object
required:
  - nodeRole
  - privateIpAddress
  - publicIpAddress
language:
  nodejs:
    requiredInputs: []
