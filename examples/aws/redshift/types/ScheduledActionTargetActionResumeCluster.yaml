$token: aws:redshift/ScheduledActionTargetActionResumeCluster:ScheduledActionTargetActionResumeCluster
properties:
  clusterIdentifier:
    type: string
    description: |
      The identifier of the cluster to be resumed.
    language:
      python:
        mapCase: false
type: object
required:
  - clusterIdentifier
