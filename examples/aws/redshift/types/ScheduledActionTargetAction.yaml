$token: aws:redshift/ScheduledActionTargetAction:ScheduledActionTargetAction
properties:
  pauseCluster:
    $ref: "#/types/aws:redshift/ScheduledActionTargetActionPauseCluster:ScheduledActionTargetActionPauseCluster"
    description: |
      An action that runs a `PauseCluster` API operation. Documented below.
    language:
      python:
        mapCase: false
  resizeCluster:
    $ref: "#/types/aws:redshift/ScheduledActionTargetActionResizeCluster:ScheduledActionTargetActionResizeCluster"
    description: |
      An action that runs a `ResizeCluster` API operation. Documented below.
    language:
      python:
        mapCase: false
  resumeCluster:
    $ref: "#/types/aws:redshift/ScheduledActionTargetActionResumeCluster:ScheduledActionTargetActionResumeCluster"
    description: |
      An action that runs a `ResumeCluster` API operation. Documented below.
    language:
      python:
        mapCase: false
type: object
