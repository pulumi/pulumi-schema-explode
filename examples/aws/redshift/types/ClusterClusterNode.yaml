$token: aws:redshift/ClusterClusterNode:ClusterClusterNode
properties:
  nodeRole:
    type: string
    description: |
      Whether the node is a leader node or a compute node
    language:
      python:
        mapCase: false
  privateIpAddress:
    type: string
    description: |
      The private IP address of a node within a cluster
    language:
      python:
        mapCase: false
  publicIpAddress:
    type: string
    description: |
      The public IP address of a node within a cluster
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - nodeRole
      - privateIpAddress
      - publicIpAddress
