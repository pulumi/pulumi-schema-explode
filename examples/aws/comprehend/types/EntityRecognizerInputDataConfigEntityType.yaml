$token: aws:comprehend/EntityRecognizerInputDataConfigEntityType:EntityRecognizerInputDataConfigEntityType
properties:
  type:
    type: string
    description: |
      An entity type to be matched by the Entity Recognizer.
      Cannot contain a newline (`\n`), carriage return (`\r`), or tab (`\t`).
    language:
      python:
        mapCase: false
type: object
required:
  - type
