$token: aws:comprehend/EntityRecognizerInputDataConfigDocuments:EntityRecognizerInputDataConfigDocuments
properties:
  inputFormat:
    type: string
    description: |
      Specifies how the input files should be processed.
      One of `ONE_DOC_PER_LINE` or `ONE_DOC_PER_FILE`.
    language:
      python:
        mapCase: false
  s3Uri:
    type: string
    description: |
      Location of entity list.
    language:
      python:
        mapCase: false
  testS3Uri:
    type: string
    language:
      python:
        mapCase: false
type: object
required:
  - s3Uri
