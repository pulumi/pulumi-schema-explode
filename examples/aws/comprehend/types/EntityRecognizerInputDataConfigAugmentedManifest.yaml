$token: aws:comprehend/EntityRecognizerInputDataConfigAugmentedManifest:EntityRecognizerInputDataConfigAugmentedManifest
properties:
  annotationDataS3Uri:
    type: string
    description: |
      Location of annotation files.
    language:
      python:
        mapCase: false
  attributeNames:
    type: array
    items:
      type: string
    description: |
      The JSON attribute that contains the annotations for the training documents.
    language:
      python:
        mapCase: false
  documentType:
    type: string
    description: |
      Type of augmented manifest.
      One of `PLAIN_TEXT_DOCUMENT` or `SEMI_STRUCTURED_DOCUMENT`.
    language:
      python:
        mapCase: false
  s3Uri:
    type: string
    description: |
      Location of entity list.
    language:
      python:
        mapCase: false
  sourceDocumentsS3Uri:
    type: string
    description: |
      Location of source PDF files.
    language:
      python:
        mapCase: false
  split:
    type: string
    description: |
      Purpose of data in augmented manifest.
      One of `TRAIN` or `TEST`.
    language:
      python:
        mapCase: false
type: object
required:
  - attributeNames
  - s3Uri
