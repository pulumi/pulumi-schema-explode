$token: aws:inspector/AssessmentTemplateEventSubscription:AssessmentTemplateEventSubscription
properties:
  event:
    type: string
    description: |
      The event for which you want to receive SNS notifications. Valid values are `ASSESSMENT_RUN_STARTED`, `ASSESSMENT_RUN_COMPLETED`, `ASSESSMENT_RUN_STATE_CHANGED`, and `FINDING_REPORTED`.
    language:
      python:
        mapCase: false
  topicArn:
    type: string
    description: |
      The ARN of the SNS topic to which notifications are sent.
    language:
      python:
        mapCase: false
type: object
required:
  - event
  - topicArn
