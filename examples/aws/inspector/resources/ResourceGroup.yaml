$token: aws:inspector/resourceGroup:ResourceGroup
properties:
  arn:
    type: string
    description: |
      The resource group ARN.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of tags that are used to select the EC2 instances to be included in an `Amazon Inspector assessment target` resource.
required:
  - arn
  - tags
inputProperties:
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of tags that are used to select the EC2 instances to be included in an `Amazon Inspector assessment target` resource.
    willReplaceOnChanges: true
requiredInputs:
  - tags
stateInputs:
  description: |
    Input properties used for looking up and filtering ResourceGroup resources.
  properties:
    arn:
      type: string
      description: |
        The resource group ARN.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of tags that are used to select the EC2 instances to be included in an `Amazon Inspector assessment target` resource.
      willReplaceOnChanges: true
  type: object
