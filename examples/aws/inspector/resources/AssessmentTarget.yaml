$token: aws:inspector/assessmentTarget:AssessmentTarget
properties:
  arn:
    type: string
    description: |
      The target assessment ARN.
  name:
    type: string
    description: |
      The name of the assessment target.
  resourceGroupArn:
    type: string
    description: |
      Inspector Resource Group Amazon Resource Name (ARN) stating tags for instance matching. If not specified, all EC2 instances in the current AWS account and region are included in the assessment target.
required:
  - arn
  - name
inputProperties:
  name:
    type: string
    description: |
      The name of the assessment target.
    willReplaceOnChanges: true
  resourceGroupArn:
    type: string
    description: |
      Inspector Resource Group Amazon Resource Name (ARN) stating tags for instance matching. If not specified, all EC2 instances in the current AWS account and region are included in the assessment target.
stateInputs:
  description: |
    Input properties used for looking up and filtering AssessmentTarget resources.
  properties:
    arn:
      type: string
      description: |
        The target assessment ARN.
    name:
      type: string
      description: |
        The name of the assessment target.
      willReplaceOnChanges: true
    resourceGroupArn:
      type: string
      description: |
        Inspector Resource Group Amazon Resource Name (ARN) stating tags for instance matching. If not specified, all EC2 instances in the current AWS account and region are included in the assessment target.
  type: object
