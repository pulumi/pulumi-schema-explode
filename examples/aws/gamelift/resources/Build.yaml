$token: aws:gamelift/build:Build
properties:
  arn:
    type: string
    description: |
      GameLift Build ARN.
  name:
    type: string
    description: |
      Name of the build
  operatingSystem:
    type: string
    description: |
      Operating system that the game server binaries are built to run onE.g., `WINDOWS_2012`, `AMAZON_LINUX` or `AMAZON_LINUX_2`.
  storageLocation:
    $ref: "#/types/aws:gamelift/BuildStorageLocation:BuildStorageLocation"
    description: |
      Information indicating where your game build files are stored. See below.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  version:
    type: string
    description: |
      Version that is associated with this build.
required:
  - arn
  - name
  - operatingSystem
  - storageLocation
  - tagsAll
inputProperties:
  name:
    type: string
    description: |
      Name of the build
  operatingSystem:
    type: string
    description: |
      Operating system that the game server binaries are built to run onE.g., `WINDOWS_2012`, `AMAZON_LINUX` or `AMAZON_LINUX_2`.
    willReplaceOnChanges: true
  storageLocation:
    $ref: "#/types/aws:gamelift/BuildStorageLocation:BuildStorageLocation"
    description: |
      Information indicating where your game build files are stored. See below.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  version:
    type: string
    description: |
      Version that is associated with this build.
requiredInputs:
  - operatingSystem
  - storageLocation
stateInputs:
  description: |
    Input properties used for looking up and filtering Build resources.
  properties:
    arn:
      type: string
      description: |
        GameLift Build ARN.
    name:
      type: string
      description: |
        Name of the build
    operatingSystem:
      type: string
      description: |
        Operating system that the game server binaries are built to run onE.g., `WINDOWS_2012`, `AMAZON_LINUX` or `AMAZON_LINUX_2`.
      willReplaceOnChanges: true
    storageLocation:
      $ref: "#/types/aws:gamelift/BuildStorageLocation:BuildStorageLocation"
      description: |
        Information indicating where your game build files are stored. See below.
      willReplaceOnChanges: true
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    version:
      type: string
      description: |
        Version that is associated with this build.
  type: object
