$token: aws:gamelift/script:Script
properties:
  arn:
    type: string
    description: |
      GameLift Script ARN.
  name:
    type: string
    description: |
      Name of the script
  storageLocation:
    $ref: "#/types/aws:gamelift/ScriptStorageLocation:ScriptStorageLocation"
    description: |
      Information indicating where your game script files are stored. See below.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  version:
    type: string
    description: |
      Version that is associated with this script.
  zipFile:
    type: string
    description: |
      A data object containing your Realtime scripts and dependencies as a zip  file. The zip file can have one or multiple files. Maximum size of a zip file is 5 MB.
required:
  - arn
  - name
  - storageLocation
  - tagsAll
inputProperties:
  name:
    type: string
    description: |
      Name of the script
  storageLocation:
    $ref: "#/types/aws:gamelift/ScriptStorageLocation:ScriptStorageLocation"
    description: |
      Information indicating where your game script files are stored. See below.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  version:
    type: string
    description: |
      Version that is associated with this script.
  zipFile:
    type: string
    description: |
      A data object containing your Realtime scripts and dependencies as a zip  file. The zip file can have one or multiple files. Maximum size of a zip file is 5 MB.
stateInputs:
  description: |
    Input properties used for looking up and filtering Script resources.
  properties:
    arn:
      type: string
      description: |
        GameLift Script ARN.
    name:
      type: string
      description: |
        Name of the script
    storageLocation:
      $ref: "#/types/aws:gamelift/ScriptStorageLocation:ScriptStorageLocation"
      description: |
        Information indicating where your game script files are stored. See below.
      willReplaceOnChanges: true
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    version:
      type: string
      description: |
        Version that is associated with this script.
    zipFile:
      type: string
      description: |
        A data object containing your Realtime scripts and dependencies as a zip  file. The zip file can have one or multiple files. Maximum size of a zip file is 5 MB.
  type: object
