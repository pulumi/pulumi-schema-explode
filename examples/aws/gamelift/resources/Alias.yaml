$token: aws:gamelift/alias:Alias
properties:
  arn:
    type: string
    description: |
      Alias ARN.
  description:
    type: string
    description: |
      Description of the alias.
  name:
    type: string
    description: |
      Name of the alias.
  routingStrategy:
    $ref: "#/types/aws:gamelift/AliasRoutingStrategy:AliasRoutingStrategy"
    description: |
      Specifies the fleet and/or routing type to use for the alias.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - name
  - routingStrategy
  - tagsAll
inputProperties:
  description:
    type: string
    description: |
      Description of the alias.
  name:
    type: string
    description: |
      Name of the alias.
  routingStrategy:
    $ref: "#/types/aws:gamelift/AliasRoutingStrategy:AliasRoutingStrategy"
    description: |
      Specifies the fleet and/or routing type to use for the alias.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - routingStrategy
stateInputs:
  description: |
    Input properties used for looking up and filtering Alias resources.
  properties:
    arn:
      type: string
      description: |
        Alias ARN.
    description:
      type: string
      description: |
        Description of the alias.
    name:
      type: string
      description: |
        Name of the alias.
    routingStrategy:
      $ref: "#/types/aws:gamelift/AliasRoutingStrategy:AliasRoutingStrategy"
      description: |
        Specifies the fleet and/or routing type to use for the alias.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
