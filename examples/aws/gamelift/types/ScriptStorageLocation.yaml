$token: aws:gamelift/ScriptStorageLocation:ScriptStorageLocation
properties:
  bucket:
    type: string
    description: |
      Name of your S3 bucket.
    language:
      python:
        mapCase: false
  key:
    type: string
    description: |
      Name of the zip file containing your script files.
    language:
      python:
        mapCase: false
  objectVersion:
    type: string
    description: |
      A specific version of the file. If not set, the latest version of the file is retrieved.
    language:
      python:
        mapCase: false
  roleArn:
    type: string
    description: |
      ARN of the access role that allows Amazon GameLift to access your S3 bucket.
    language:
      python:
        mapCase: false
type: object
required:
  - bucket
  - key
  - roleArn
