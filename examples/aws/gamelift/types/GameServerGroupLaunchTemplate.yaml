$token: aws:gamelift/GameServerGroupLaunchTemplate:GameServerGroupLaunchTemplate
properties:
  id:
    type: string
    description: |
      A unique identifier for an existing EC2 launch template.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      A readable identifier for an existing EC2 launch template.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  version:
    type: string
    description: |
      The version of the EC2 launch template to use. If none is set, the default is the first version created.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
language:
  nodejs:
    requiredOutputs:
      - id
      - name
