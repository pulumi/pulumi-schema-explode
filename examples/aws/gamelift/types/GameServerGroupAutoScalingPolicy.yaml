$token: aws:gamelift/GameServerGroupAutoScalingPolicy:GameServerGroupAutoScalingPolicy
properties:
  estimatedInstanceWarmup:
    type: integer
    description: |
      Length of time, in seconds, it takes for a new instance to start
      new game server processes and register with GameLift FleetIQ.
      Specifying a warm-up time can be useful, particularly with game servers that take a long time to start up,
      because it avoids prematurely starting new instances. Defaults to `60`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  targetTrackingConfiguration:
    $ref: "#/types/aws:gamelift/GameServerGroupAutoScalingPolicyTargetTrackingConfiguration:GameServerGroupAutoScalingPolicyTargetTrackingConfiguration"
    language:
      python:
        mapCase: false
type: object
required:
  - targetTrackingConfiguration
language:
  nodejs:
    requiredOutputs:
      - estimatedInstanceWarmup
      - targetTrackingConfiguration
