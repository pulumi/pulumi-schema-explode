$token: aws:gamelift/AliasRoutingStrategy:AliasRoutingStrategy
properties:
  fleetId:
    type: string
    description: |
      ID of the GameLift Fleet to point the alias to.
    language:
      python:
        mapCase: false
  message:
    type: string
    description: |
      Message text to be used with the `TERMINAL` routing strategy.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      Type of routing strategyE.g., `SIMPLE` or `TERMINAL`
    language:
      python:
        mapCase: false
type: object
required:
  - type
