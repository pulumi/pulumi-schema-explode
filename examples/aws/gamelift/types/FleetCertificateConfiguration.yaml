$token: aws:gamelift/FleetCertificateConfiguration:FleetCertificateConfiguration
properties:
  certificateType:
    type: string
    description: |
      Indicates whether a TLS/SSL certificate is generated for a fleet. Valid values are `DISABLED` and `GENERATED`. Default value is `DISABLED`.
    language:
      python:
        mapCase: false
type: object
