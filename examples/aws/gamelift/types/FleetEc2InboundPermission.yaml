$token: aws:gamelift/FleetEc2InboundPermission:FleetEc2InboundPermission
properties:
  fromPort:
    type: integer
    description: |
      Starting value for a range of allowed port numbers.
    language:
      python:
        mapCase: false
  ipRange:
    type: string
    description: |
      Range of allowed IP addresses expressed in CIDR notationE.g., `000.000.000.000/[subnet mask]` or `0.0.0.0/[subnet mask]`.
    language:
      python:
        mapCase: false
  protocol:
    type: string
    description: |
      Network communication protocol used by the fleetE.g., `TCP` or `UDP`
    language:
      python:
        mapCase: false
  toPort:
    type: integer
    description: |
      Ending value for a range of allowed port numbers. Port numbers are end-inclusive. This value must be higher than `from_port`.
    language:
      python:
        mapCase: false
type: object
required:
  - fromPort
  - ipRange
  - protocol
  - toPort
