$token: aws:imagebuilder/getImageRecipesFilter:getImageRecipesFilter
properties:
  name:
    type: string
    description: |
      Name of the filter field. Valid values can be found in the [Image Builder ListImageRecipes API Reference](https://docs.aws.amazon.com/imagebuilder/latest/APIReference/API_ListImageRecipes.html).
    language:
      python:
        mapCase: false
  values:
    type: array
    items:
      type: string
    description: |
      Set of values that are accepted for the given filter field. Results will be selected if any given value matches.
    language:
      python:
        mapCase: false
type: object
required:
  - name
  - values
