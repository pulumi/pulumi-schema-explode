$token: aws:imagebuilder/getContainerRecipeTargetRepository:getContainerRecipeTargetRepository
properties:
  repositoryName:
    type: string
    description: |
      Name of the container repository where the output container image is stored. The name is prefixed by the repository location.
    language:
      python:
        mapCase: false
  service:
    type: string
    description: |
      Service in which this image is registered.
    language:
      python:
        mapCase: false
type: object
required:
  - repositoryName
  - service
language:
  nodejs:
    requiredInputs: []
