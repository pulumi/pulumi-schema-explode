$token: aws:imagebuilder/ImageRecipeSystemsManagerAgent:ImageRecipeSystemsManagerAgent
properties:
  uninstallAfterBuild:
    type: boolean
    description: |
      Whether to remove the Systems Manager Agent after the image has been built. Defaults to `false`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - uninstallAfterBuild
