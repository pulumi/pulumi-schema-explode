$token: aws:imagebuilder/DistributionConfigurationDistribution:DistributionConfigurationDistribution
properties:
  amiDistributionConfiguration:
    $ref: "#/types/aws:imagebuilder/DistributionConfigurationDistributionAmiDistributionConfiguration:DistributionConfigurationDistributionAmiDistributionConfiguration"
    description: |
      Configuration block with Amazon Machine Image (AMI) distribution settings. Detailed below.
    language:
      python:
        mapCase: false
  containerDistributionConfiguration:
    $ref: "#/types/aws:imagebuilder/DistributionConfigurationDistributionContainerDistributionConfiguration:DistributionConfigurationDistributionContainerDistributionConfiguration"
    description: |
      Configuration block with container distribution settings. Detailed below.
    language:
      python:
        mapCase: false
  fastLaunchConfigurations:
    type: array
    items:
      $ref: "#/types/aws:imagebuilder/DistributionConfigurationDistributionFastLaunchConfiguration:DistributionConfigurationDistributionFastLaunchConfiguration"
    description: |
      Set of Windows faster-launching configurations to use for AMI distribution. Detailed below.
    language:
      python:
        mapCase: false
  launchTemplateConfigurations:
    type: array
    items:
      $ref: "#/types/aws:imagebuilder/DistributionConfigurationDistributionLaunchTemplateConfiguration:DistributionConfigurationDistributionLaunchTemplateConfiguration"
    description: |
      Set of launch template configuration settings that apply to image distribution. Detailed below.
    language:
      python:
        mapCase: false
  licenseConfigurationArns:
    type: array
    items:
      type: string
    description: |
      Set of Amazon Resource Names (ARNs) of License Manager License Configurations.
    language:
      python:
        mapCase: false
  region:
    type: string
    description: |
      AWS Region for the distribution.
    language:
      python:
        mapCase: false
type: object
required:
  - region
