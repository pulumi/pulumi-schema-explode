$token: aws:imagebuilder/InfrastructureConfigurationInstanceMetadataOptions:InfrastructureConfigurationInstanceMetadataOptions
properties:
  httpPutResponseHopLimit:
    type: integer
    description: |
      The number of hops that an instance can traverse to reach its destonation.
    language:
      python:
        mapCase: false
  httpTokens:
    type: string
    description: |
      Whether a signed token is required for instance metadata retrieval requests. Valid values: `required`, `optional`.
    language:
      python:
        mapCase: false
type: object
