$token: aws:imagebuilder/DistributionConfigurationDistributionFastLaunchConfigurationLaunchTemplate:DistributionConfigurationDistributionFastLaunchConfigurationLaunchTemplate
properties:
  launchTemplateId:
    type: string
    description: |
      The ID of the Amazon EC2 launch template to use.
    language:
      python:
        mapCase: false
  launchTemplateName:
    type: string
    description: |
      The name of the launch template to use for faster launching for a Windows AMI.
    language:
      python:
        mapCase: false
  launchTemplateVersion:
    type: string
    description: |
      The version of the launch template to use for faster launching for a Windows AMI.
    language:
      python:
        mapCase: false
type: object
