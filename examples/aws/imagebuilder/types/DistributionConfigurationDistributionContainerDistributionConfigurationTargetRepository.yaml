$token: aws:imagebuilder/DistributionConfigurationDistributionContainerDistributionConfigurationTargetRepository:DistributionConfigurationDistributionContainerDistributionConfigurationTargetRepository
properties:
  repositoryName:
    type: string
    description: |
      The name of the container repository where the output container image is stored. This name is prefixed by the repository location.
    language:
      python:
        mapCase: false
  service:
    type: string
    description: |
      The service in which this image is registered. Valid values: `ECR`.
    language:
      python:
        mapCase: false
type: object
required:
  - repositoryName
  - service
