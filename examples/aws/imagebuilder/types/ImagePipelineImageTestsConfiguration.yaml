$token: aws:imagebuilder/ImagePipelineImageTestsConfiguration:ImagePipelineImageTestsConfiguration
properties:
  imageTestsEnabled:
    type: boolean
    description: |
      Whether image tests are enabled. Defaults to `true`.
    language:
      python:
        mapCase: false
  timeoutMinutes:
    type: integer
    description: |
      Number of minutes before image tests time out. Valid values are between `60` and `1440`. Defaults to `720`.
    language:
      python:
        mapCase: false
type: object
