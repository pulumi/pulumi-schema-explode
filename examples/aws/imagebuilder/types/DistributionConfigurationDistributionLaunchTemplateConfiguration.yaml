$token: aws:imagebuilder/DistributionConfigurationDistributionLaunchTemplateConfiguration:DistributionConfigurationDistributionLaunchTemplateConfiguration
properties:
  accountId:
    type: string
    description: |
      The account ID that this configuration applies to.
    language:
      python:
        mapCase: false
  default:
    type: boolean
    description: |
      Indicates whether to set the specified Amazon EC2 launch template as the default launch template. Defaults to `true`.
    language:
      python:
        mapCase: false
  launchTemplateId:
    type: string
    description: |
      The ID of the Amazon EC2 launch template to use.
    language:
      python:
        mapCase: false
type: object
required:
  - launchTemplateId
