$token: aws:imagebuilder/getDistributionConfigurationDistributionLaunchTemplateConfiguration:getDistributionConfigurationDistributionLaunchTemplateConfiguration
properties:
  accountId:
    type: string
    description: |
      The account ID that this configuration applies to.
    language:
      python:
        mapCase: false
  default:
    type: boolean
    description: |
      Whether the specified Amazon EC2 launch template is set as the default launch template.
    language:
      python:
        mapCase: false
  launchTemplateId:
    type: string
    description: |
      ID of the Amazon EC2 launch template.
    language:
      python:
        mapCase: false
type: object
required:
  - accountId
  - default
  - launchTemplateId
language:
  nodejs:
    requiredInputs: []
