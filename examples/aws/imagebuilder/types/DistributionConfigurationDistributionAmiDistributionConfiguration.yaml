$token: aws:imagebuilder/DistributionConfigurationDistributionAmiDistributionConfiguration:DistributionConfigurationDistributionAmiDistributionConfiguration
properties:
  amiTags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of tags to apply to the distributed AMI.
    language:
      python:
        mapCase: false
  description:
    type: string
    description: |
      Description of the container distribution configuration.
    language:
      python:
        mapCase: false
  kmsKeyId:
    type: string
    description: |
      Amazon Resource Name (ARN) of the Key Management Service (KMS) Key to encrypt the distributed AMI.
    language:
      python:
        mapCase: false
  launchPermission:
    $ref: "#/types/aws:imagebuilder/DistributionConfigurationDistributionAmiDistributionConfigurationLaunchPermission:DistributionConfigurationDistributionAmiDistributionConfigurationLaunchPermission"
    description: |
      Configuration block of EC2 launch permissions to apply to the distributed AMI. Detailed below.
    language:
      python:
        mapCase: false
  name:
    type: string
    description: |
      Name to apply to the distributed AMI.
    language:
      python:
        mapCase: false
  targetAccountIds:
    type: array
    items:
      type: string
    description: |
      Set of AWS Account identifiers to distribute the AMI.
    language:
      python:
        mapCase: false
type: object
