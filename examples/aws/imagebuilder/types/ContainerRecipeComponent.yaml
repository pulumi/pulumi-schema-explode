$token: aws:imagebuilder/ContainerRecipeComponent:ContainerRecipeComponent
properties:
  componentArn:
    type: string
    description: |
      Amazon Resource Name (ARN) of the Image Builder Component to associate.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  parameters:
    type: array
    items:
      $ref: "#/types/aws:imagebuilder/ContainerRecipeComponentParameter:ContainerRecipeComponentParameter"
    description: |
      Configuration block(s) for parameters to configure the component. Detailed below.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - componentArn
