$token: aws:imagebuilder/ContainerRecipeInstanceConfiguration:ContainerRecipeInstanceConfiguration
properties:
  blockDeviceMappings:
    type: array
    items:
      $ref: "#/types/aws:imagebuilder/ContainerRecipeInstanceConfigurationBlockDeviceMapping:ContainerRecipeInstanceConfigurationBlockDeviceMapping"
    description: |
      Configuration block(s) with block device mappings for the the container recipe. Detailed below.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  image:
    type: string
    description: |
      The AMI ID to use as the base image for a container build and test instance. If not specified, Image Builder will use the appropriate ECS-optimized AMI as a base image.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
