$token: aws:imagebuilder/getContainerRecipeInstanceConfigurationBlockDeviceMappingEb:getContainerRecipeInstanceConfigurationBlockDeviceMappingEb
properties:
  deleteOnTermination:
    type: boolean
    description: |
      Whether to delete the volume on termination. Defaults to unset, which is the value inherited from the parent image.
    language:
      python:
        mapCase: false
  encrypted:
    type: boolean
    description: |
      Whether to encrypt the volume. Defaults to unset, which is the value inherited from the parent image.
    language:
      python:
        mapCase: false
  iops:
    type: integer
    description: |
      Number of Input/Output (I/O) operations per second to provision for an `io1` or `io2` volume.
    language:
      python:
        mapCase: false
  kmsKeyId:
    type: string
    description: |
      KMS key used to encrypt the container image.
    language:
      python:
        mapCase: false
  snapshotId:
    type: string
    description: |
      Identifier of the EC2 Volume Snapshot.
    language:
      python:
        mapCase: false
  throughput:
    type: integer
    description: |
      For GP3 volumes only. The throughput in MiB/s that the volume supports.
    language:
      python:
        mapCase: false
  volumeSize:
    type: integer
    description: |
      Size of the volume, in GiB.
    language:
      python:
        mapCase: false
  volumeType:
    type: string
    description: |
      Type of the volume. For example, `gp2` or `io2`.
    language:
      python:
        mapCase: false
type: object
required:
  - deleteOnTermination
  - encrypted
  - iops
  - kmsKeyId
  - snapshotId
  - throughput
  - volumeSize
  - volumeType
language:
  nodejs:
    requiredInputs: []
