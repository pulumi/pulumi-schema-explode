$token: aws:imagebuilder/getDistributionConfigurationDistributionFastLaunchConfigurationLaunchTemplate:getDistributionConfigurationDistributionFastLaunchConfigurationLaunchTemplate
properties:
  launchTemplateId:
    type: string
    description: |
      ID of the Amazon EC2 launch template.
    language:
      python:
        mapCase: false
  launchTemplateName:
    type: string
    description: |
      The name of the launch template to use for faster launching for a Windows AMI.
    language:
      python:
        mapCase: false
  launchTemplateVersion:
    type: string
    description: |
      The version of the launch template to use for faster launching for a Windows AMI.
    language:
      python:
        mapCase: false
type: object
required:
  - launchTemplateId
  - launchTemplateName
  - launchTemplateVersion
language:
  nodejs:
    requiredInputs: []
