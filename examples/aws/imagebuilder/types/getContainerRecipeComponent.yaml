$token: aws:imagebuilder/getContainerRecipeComponent:getContainerRecipeComponent
properties:
  componentArn:
    type: string
    description: |
      ARN of the Image Builder Component.
    language:
      python:
        mapCase: false
  parameters:
    type: array
    items:
      $ref: "#/types/aws:imagebuilder/getContainerRecipeComponentParameter:getContainerRecipeComponentParameter"
    description: |
      Set of parameters that are used to configure the component.
    language:
      python:
        mapCase: false
type: object
required:
  - componentArn
  - parameters
language:
  nodejs:
    requiredInputs: []
