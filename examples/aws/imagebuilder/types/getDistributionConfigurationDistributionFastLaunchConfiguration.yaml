$token: aws:imagebuilder/getDistributionConfigurationDistributionFastLaunchConfiguration:getDistributionConfigurationDistributionFastLaunchConfiguration
properties:
  accountId:
    type: string
    description: |
      The account ID that this configuration applies to.
    language:
      python:
        mapCase: false
  enabled:
    type: boolean
    description: |
      A Boolean that represents the current state of faster launching for the Windows AMI.
    language:
      python:
        mapCase: false
  launchTemplates:
    type: array
    items:
      $ref: "#/types/aws:imagebuilder/getDistributionConfigurationDistributionFastLaunchConfigurationLaunchTemplate:getDistributionConfigurationDistributionFastLaunchConfigurationLaunchTemplate"
    description: |
      Nested list of launch templates that the fast-launch enabled Windows AMI uses when it launches Windows instances to create pre-provisioned snapshots.
    language:
      python:
        mapCase: false
  maxParallelLaunches:
    type: integer
    description: |
      The maximum number of parallel instances that are launched for creating resources.
    language:
      python:
        mapCase: false
  snapshotConfigurations:
    type: array
    items:
      $ref: "#/types/aws:imagebuilder/getDistributionConfigurationDistributionFastLaunchConfigurationSnapshotConfiguration:getDistributionConfigurationDistributionFastLaunchConfigurationSnapshotConfiguration"
    description: |
      Nested list of configurations for managing the number of snapshots that are created from pre-provisioned instances for the Windows AMI when faster launching is enabled.
    language:
      python:
        mapCase: false
type: object
required:
  - accountId
  - enabled
  - launchTemplates
  - maxParallelLaunches
  - snapshotConfigurations
language:
  nodejs:
    requiredInputs: []
