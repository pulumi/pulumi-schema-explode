$token: aws:imagebuilder/ImageImageTestsConfiguration:ImageImageTestsConfiguration
properties:
  imageTestsEnabled:
    type: boolean
    description: |
      Whether image tests are enabled. Defaults to `true`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  timeoutMinutes:
    type: integer
    description: |
      Number of minutes before image tests time out. Valid values are between `60` and `1440`. Defaults to `720`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
