$token: aws:imagebuilder/DistributionConfigurationDistributionContainerDistributionConfiguration:DistributionConfigurationDistributionContainerDistributionConfiguration
properties:
  containerTags:
    type: array
    items:
      type: string
    description: |
      Set of tags that are attached to the container distribution configuration.
    language:
      python:
        mapCase: false
  description:
    type: string
    description: |
      Description of the container distribution configuration.
    language:
      python:
        mapCase: false
  targetRepository:
    $ref: "#/types/aws:imagebuilder/DistributionConfigurationDistributionContainerDistributionConfigurationTargetRepository:DistributionConfigurationDistributionContainerDistributionConfigurationTargetRepository"
    description: |
      Configuration block with the destination repository for the container distribution configuration.
    language:
      python:
        mapCase: false
type: object
required:
  - targetRepository
