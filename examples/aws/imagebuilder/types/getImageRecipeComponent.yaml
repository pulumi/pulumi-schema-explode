$token: aws:imagebuilder/getImageRecipeComponent:getImageRecipeComponent
properties:
  componentArn:
    type: string
    description: |
      ARN of the Image Builder Component.
    language:
      python:
        mapCase: false
  parameters:
    type: array
    items:
      $ref: "#/types/aws:imagebuilder/getImageRecipeComponentParameter:getImageRecipeComponentParameter"
    description: |
      Set of parameters that are used to configure the component.
    language:
      python:
        mapCase: false
type: object
required:
  - componentArn
  - parameters
language:
  nodejs:
    requiredInputs: []
