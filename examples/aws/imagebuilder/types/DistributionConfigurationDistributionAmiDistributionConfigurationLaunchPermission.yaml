$token: aws:imagebuilder/DistributionConfigurationDistributionAmiDistributionConfigurationLaunchPermission:DistributionConfigurationDistributionAmiDistributionConfigurationLaunchPermission
properties:
  organizationArns:
    type: array
    items:
      type: string
    description: |
      Set of AWS Organization ARNs to assign.
    language:
      python:
        mapCase: false
  organizationalUnitArns:
    type: array
    items:
      type: string
    description: |
      Set of AWS Organizational Unit ARNs to assign.
    language:
      python:
        mapCase: false
  userGroups:
    type: array
    items:
      type: string
    description: |
      Set of EC2 launch permission user groups to assign. Use `all` to distribute a public AMI.
    language:
      python:
        mapCase: false
  userIds:
    type: array
    items:
      type: string
    description: |
      Set of AWS Account identifiers to assign.
    language:
      python:
        mapCase: false
type: object
