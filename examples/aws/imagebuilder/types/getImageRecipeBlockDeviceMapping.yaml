$token: aws:imagebuilder/getImageRecipeBlockDeviceMapping:getImageRecipeBlockDeviceMapping
properties:
  deviceName:
    type: string
    description: |
      Name of the device. For example, `/dev/sda` or `/dev/xvdb`.
    language:
      python:
        mapCase: false
  ebs:
    type: array
    items:
      $ref: "#/types/aws:imagebuilder/getImageRecipeBlockDeviceMappingEb:getImageRecipeBlockDeviceMappingEb"
    description: |
      Single list of object with Elastic Block Storage (EBS) block device mapping settings.
    language:
      python:
        mapCase: false
  noDevice:
    type: string
    description: |
      Whether to remove a mapping from the parent image.
    language:
      python:
        mapCase: false
  virtualName:
    type: string
    description: |
      Virtual device name. For example, `ephemeral0`. Instance store volumes are numbered starting from 0.
    language:
      python:
        mapCase: false
type: object
required:
  - deviceName
  - ebs
  - noDevice
  - virtualName
language:
  nodejs:
    requiredInputs: []
