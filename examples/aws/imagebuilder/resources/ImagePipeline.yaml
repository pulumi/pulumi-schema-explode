$token: aws:imagebuilder/imagePipeline:ImagePipeline
properties:
  arn:
    type: string
    description: |
      Amazon Resource Name (ARN) of the image pipeline.
  containerRecipeArn:
    type: string
    description: |
      Amazon Resource Name (ARN) of the container recipe.
  dateCreated:
    type: string
    description: |
      Date the image pipeline was created.
  dateLastRun:
    type: string
    description: |
      Date the image pipeline was last run.
  dateNextRun:
    type: string
    description: |
      Date the image pipeline will run next.
  dateUpdated:
    type: string
    description: |
      Date the image pipeline was updated.
  description:
    type: string
    description: |
      Description of the image pipeline.
  distributionConfigurationArn:
    type: string
    description: |
      Amazon Resource Name (ARN) of the Image Builder Distribution Configuration.
  enhancedImageMetadataEnabled:
    type: boolean
    description: |
      Whether additional information about the image being created is collected. Defaults to `true`.
  imageRecipeArn:
    type: string
    description: |
      Amazon Resource Name (ARN) of the image recipe.
  imageTestsConfiguration:
    $ref: "#/types/aws:imagebuilder/ImagePipelineImageTestsConfiguration:ImagePipelineImageTestsConfiguration"
    description: |
      Configuration block with image tests configuration. Detailed below.
  infrastructureConfigurationArn:
    type: string
    description: |
      Amazon Resource Name (ARN) of the Image Builder Infrastructure Configuration.
  name:
    type: string
    description: |
      Name of the image pipeline.
  platform:
    type: string
    description: |
      Platform of the image pipeline.
  schedule:
    $ref: "#/types/aws:imagebuilder/ImagePipelineSchedule:ImagePipelineSchedule"
    description: |
      Configuration block with schedule settings. Detailed below.
  status:
    type: string
    description: |
      Status of the image pipeline. Valid values are `DISABLED` and `ENABLED`. Defaults to `ENABLED`.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags for the image pipeline. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - dateCreated
  - dateLastRun
  - dateNextRun
  - dateUpdated
  - imageTestsConfiguration
  - infrastructureConfigurationArn
  - name
  - platform
  - tagsAll
inputProperties:
  containerRecipeArn:
    type: string
    description: |
      Amazon Resource Name (ARN) of the container recipe.
    willReplaceOnChanges: true
  description:
    type: string
    description: |
      Description of the image pipeline.
  distributionConfigurationArn:
    type: string
    description: |
      Amazon Resource Name (ARN) of the Image Builder Distribution Configuration.
  enhancedImageMetadataEnabled:
    type: boolean
    description: |
      Whether additional information about the image being created is collected. Defaults to `true`.
  imageRecipeArn:
    type: string
    description: |
      Amazon Resource Name (ARN) of the image recipe.
    willReplaceOnChanges: true
  imageTestsConfiguration:
    $ref: "#/types/aws:imagebuilder/ImagePipelineImageTestsConfiguration:ImagePipelineImageTestsConfiguration"
    description: |
      Configuration block with image tests configuration. Detailed below.
  infrastructureConfigurationArn:
    type: string
    description: |
      Amazon Resource Name (ARN) of the Image Builder Infrastructure Configuration.
  name:
    type: string
    description: |
      Name of the image pipeline.
    willReplaceOnChanges: true
  schedule:
    $ref: "#/types/aws:imagebuilder/ImagePipelineSchedule:ImagePipelineSchedule"
    description: |
      Configuration block with schedule settings. Detailed below.
  status:
    type: string
    description: |
      Status of the image pipeline. Valid values are `DISABLED` and `ENABLED`. Defaults to `ENABLED`.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags for the image pipeline. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - infrastructureConfigurationArn
stateInputs:
  description: |
    Input properties used for looking up and filtering ImagePipeline resources.
  properties:
    arn:
      type: string
      description: |
        Amazon Resource Name (ARN) of the image pipeline.
    containerRecipeArn:
      type: string
      description: |
        Amazon Resource Name (ARN) of the container recipe.
      willReplaceOnChanges: true
    dateCreated:
      type: string
      description: |
        Date the image pipeline was created.
    dateLastRun:
      type: string
      description: |
        Date the image pipeline was last run.
    dateNextRun:
      type: string
      description: |
        Date the image pipeline will run next.
    dateUpdated:
      type: string
      description: |
        Date the image pipeline was updated.
    description:
      type: string
      description: |
        Description of the image pipeline.
    distributionConfigurationArn:
      type: string
      description: |
        Amazon Resource Name (ARN) of the Image Builder Distribution Configuration.
    enhancedImageMetadataEnabled:
      type: boolean
      description: |
        Whether additional information about the image being created is collected. Defaults to `true`.
    imageRecipeArn:
      type: string
      description: |
        Amazon Resource Name (ARN) of the image recipe.
      willReplaceOnChanges: true
    imageTestsConfiguration:
      $ref: "#/types/aws:imagebuilder/ImagePipelineImageTestsConfiguration:ImagePipelineImageTestsConfiguration"
      description: |
        Configuration block with image tests configuration. Detailed below.
    infrastructureConfigurationArn:
      type: string
      description: |
        Amazon Resource Name (ARN) of the Image Builder Infrastructure Configuration.
    name:
      type: string
      description: |
        Name of the image pipeline.
      willReplaceOnChanges: true
    platform:
      type: string
      description: |
        Platform of the image pipeline.
    schedule:
      $ref: "#/types/aws:imagebuilder/ImagePipelineSchedule:ImagePipelineSchedule"
      description: |
        Configuration block with schedule settings. Detailed below.
    status:
      type: string
      description: |
        Status of the image pipeline. Valid values are `DISABLED` and `ENABLED`. Defaults to `ENABLED`.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags for the image pipeline. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
