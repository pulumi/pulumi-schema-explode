$token: aws:imagebuilder/containerRecipe:ContainerRecipe
properties:
  arn:
    type: string
    description: |
      (Required) Amazon Resource Name (ARN) of the container recipe.
  components:
    type: array
    items:
      $ref: "#/types/aws:imagebuilder/ContainerRecipeComponent:ContainerRecipeComponent"
    description: |
      Ordered configuration block(s) with components for the container recipe. Detailed below.
  containerType:
    type: string
    description: |
      The type of the container to create. Valid values: `DOCKER`.
  dateCreated:
    type: string
    description: |
      Date the container recipe was created.
  description:
    type: string
    description: |
      The description of the container recipe.
  dockerfileTemplateData:
    type: string
    description: |
      The Dockerfile template used to build the image as an inline data blob.
  dockerfileTemplateUri:
    type: string
    description: |
      The Amazon S3 URI for the Dockerfile that will be used to build the container image.
  encrypted:
    type: boolean
    description: |
      Whether to encrypt the volume. Defaults to unset, which is the value inherited from the parent image.
  instanceConfiguration:
    $ref: "#/types/aws:imagebuilder/ContainerRecipeInstanceConfiguration:ContainerRecipeInstanceConfiguration"
    description: |
      Configuration block used to configure an instance for building and testing container images. Detailed below.
  kmsKeyId:
    type: string
    description: |
      Amazon Resource Name (ARN) of the Key Management Service (KMS) Key for encryption.
  name:
    type: string
    description: |
      The name of the component parameter.
  owner:
    type: string
    description: |
      Owner of the container recipe.
  parentImage:
    type: string
    description: |
      The base image for the container recipe.
  platform:
    type: string
    description: |
      Platform of the container recipe.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags for the container recipe. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  targetRepository:
    $ref: "#/types/aws:imagebuilder/ContainerRecipeTargetRepository:ContainerRecipeTargetRepository"
    description: |
      The destination repository for the container image. Detailed below.
  version:
    type: string
    description: |
      Version of the container recipe.
  workingDirectory:
    type: string
    description: |
      The working directory to be used during build and test workflows.
required:
  - arn
  - components
  - containerType
  - dateCreated
  - dockerfileTemplateData
  - encrypted
  - name
  - owner
  - parentImage
  - platform
  - tagsAll
  - targetRepository
  - version
inputProperties:
  components:
    type: array
    items:
      $ref: "#/types/aws:imagebuilder/ContainerRecipeComponent:ContainerRecipeComponent"
    description: |
      Ordered configuration block(s) with components for the container recipe. Detailed below.
    willReplaceOnChanges: true
  containerType:
    type: string
    description: |
      The type of the container to create. Valid values: `DOCKER`.
    willReplaceOnChanges: true
  description:
    type: string
    description: |
      The description of the container recipe.
    willReplaceOnChanges: true
  dockerfileTemplateData:
    type: string
    description: |
      The Dockerfile template used to build the image as an inline data blob.
    willReplaceOnChanges: true
  dockerfileTemplateUri:
    type: string
    description: |
      The Amazon S3 URI for the Dockerfile that will be used to build the container image.
    willReplaceOnChanges: true
  instanceConfiguration:
    $ref: "#/types/aws:imagebuilder/ContainerRecipeInstanceConfiguration:ContainerRecipeInstanceConfiguration"
    description: |
      Configuration block used to configure an instance for building and testing container images. Detailed below.
    willReplaceOnChanges: true
  kmsKeyId:
    type: string
    description: |
      Amazon Resource Name (ARN) of the Key Management Service (KMS) Key for encryption.
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      The name of the component parameter.
    willReplaceOnChanges: true
  parentImage:
    type: string
    description: |
      The base image for the container recipe.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags for the container recipe. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  targetRepository:
    $ref: "#/types/aws:imagebuilder/ContainerRecipeTargetRepository:ContainerRecipeTargetRepository"
    description: |
      The destination repository for the container image. Detailed below.
    willReplaceOnChanges: true
  version:
    type: string
    description: |
      Version of the container recipe.
    willReplaceOnChanges: true
  workingDirectory:
    type: string
    description: |
      The working directory to be used during build and test workflows.
    willReplaceOnChanges: true
requiredInputs:
  - components
  - containerType
  - parentImage
  - targetRepository
  - version
stateInputs:
  description: |
    Input properties used for looking up and filtering ContainerRecipe resources.
  properties:
    arn:
      type: string
      description: |
        (Required) Amazon Resource Name (ARN) of the container recipe.
    components:
      type: array
      items:
        $ref: "#/types/aws:imagebuilder/ContainerRecipeComponent:ContainerRecipeComponent"
      description: |
        Ordered configuration block(s) with components for the container recipe. Detailed below.
      willReplaceOnChanges: true
    containerType:
      type: string
      description: |
        The type of the container to create. Valid values: `DOCKER`.
      willReplaceOnChanges: true
    dateCreated:
      type: string
      description: |
        Date the container recipe was created.
    description:
      type: string
      description: |
        The description of the container recipe.
      willReplaceOnChanges: true
    dockerfileTemplateData:
      type: string
      description: |
        The Dockerfile template used to build the image as an inline data blob.
      willReplaceOnChanges: true
    dockerfileTemplateUri:
      type: string
      description: |
        The Amazon S3 URI for the Dockerfile that will be used to build the container image.
      willReplaceOnChanges: true
    encrypted:
      type: boolean
      description: |
        Whether to encrypt the volume. Defaults to unset, which is the value inherited from the parent image.
    instanceConfiguration:
      $ref: "#/types/aws:imagebuilder/ContainerRecipeInstanceConfiguration:ContainerRecipeInstanceConfiguration"
      description: |
        Configuration block used to configure an instance for building and testing container images. Detailed below.
      willReplaceOnChanges: true
    kmsKeyId:
      type: string
      description: |
        Amazon Resource Name (ARN) of the Key Management Service (KMS) Key for encryption.
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        The name of the component parameter.
      willReplaceOnChanges: true
    owner:
      type: string
      description: |
        Owner of the container recipe.
    parentImage:
      type: string
      description: |
        The base image for the container recipe.
      willReplaceOnChanges: true
    platform:
      type: string
      description: |
        Platform of the container recipe.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags for the container recipe. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    targetRepository:
      $ref: "#/types/aws:imagebuilder/ContainerRecipeTargetRepository:ContainerRecipeTargetRepository"
      description: |
        The destination repository for the container image. Detailed below.
      willReplaceOnChanges: true
    version:
      type: string
      description: |
        Version of the container recipe.
      willReplaceOnChanges: true
    workingDirectory:
      type: string
      description: |
        The working directory to be used during build and test workflows.
      willReplaceOnChanges: true
  type: object
