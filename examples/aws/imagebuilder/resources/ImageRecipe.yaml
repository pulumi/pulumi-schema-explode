$token: aws:imagebuilder/imageRecipe:ImageRecipe
properties:
  arn:
    type: string
    description: |
      (Required) Amazon Resource Name (ARN) of the image recipe.
  blockDeviceMappings:
    type: array
    items:
      $ref: "#/types/aws:imagebuilder/ImageRecipeBlockDeviceMapping:ImageRecipeBlockDeviceMapping"
    description: |
      Configuration block(s) with block device mappings for the image recipe. Detailed below.
  components:
    type: array
    items:
      $ref: "#/types/aws:imagebuilder/ImageRecipeComponent:ImageRecipeComponent"
    description: |
      Ordered configuration block(s) with components for the image recipe. Detailed below.
  dateCreated:
    type: string
    description: |
      Date the image recipe was created.
  description:
    type: string
    description: |
      Description of the image recipe.
  name:
    type: string
    description: |
      The name of the component parameter.
  owner:
    type: string
    description: |
      Owner of the image recipe.
  parentImage:
    type: string
    description: |
      Platform of the image recipe.
  platform:
    type: string
    description: |
      Platform of the image recipe.
  systemsManagerAgent:
    $ref: "#/types/aws:imagebuilder/ImageRecipeSystemsManagerAgent:ImageRecipeSystemsManagerAgent"
    description: |
      Configuration block for the Systems Manager Agent installed by default by Image Builder. Detailed below.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags for the image recipe. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  userDataBase64:
    type: string
    description: |
      Base64 encoded user data. Use this to provide commands or a command script to run when you launch your build instance.
  version:
    type: string
    description: |
      Version of the image recipe.
  workingDirectory:
    type: string
    description: |
      The working directory to be used during build and test workflows.
required:
  - arn
  - components
  - dateCreated
  - name
  - owner
  - parentImage
  - platform
  - systemsManagerAgent
  - tagsAll
  - userDataBase64
  - version
inputProperties:
  blockDeviceMappings:
    type: array
    items:
      $ref: "#/types/aws:imagebuilder/ImageRecipeBlockDeviceMapping:ImageRecipeBlockDeviceMapping"
    description: |
      Configuration block(s) with block device mappings for the image recipe. Detailed below.
    willReplaceOnChanges: true
  components:
    type: array
    items:
      $ref: "#/types/aws:imagebuilder/ImageRecipeComponent:ImageRecipeComponent"
    description: |
      Ordered configuration block(s) with components for the image recipe. Detailed below.
    willReplaceOnChanges: true
  description:
    type: string
    description: |
      Description of the image recipe.
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      The name of the component parameter.
    willReplaceOnChanges: true
  parentImage:
    type: string
    description: |
      Platform of the image recipe.
    willReplaceOnChanges: true
  systemsManagerAgent:
    $ref: "#/types/aws:imagebuilder/ImageRecipeSystemsManagerAgent:ImageRecipeSystemsManagerAgent"
    description: |
      Configuration block for the Systems Manager Agent installed by default by Image Builder. Detailed below.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags for the image recipe. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  userDataBase64:
    type: string
    description: |
      Base64 encoded user data. Use this to provide commands or a command script to run when you launch your build instance.
    willReplaceOnChanges: true
  version:
    type: string
    description: |
      Version of the image recipe.
    willReplaceOnChanges: true
  workingDirectory:
    type: string
    description: |
      The working directory to be used during build and test workflows.
    willReplaceOnChanges: true
requiredInputs:
  - components
  - parentImage
  - version
stateInputs:
  description: |
    Input properties used for looking up and filtering ImageRecipe resources.
  properties:
    arn:
      type: string
      description: |
        (Required) Amazon Resource Name (ARN) of the image recipe.
    blockDeviceMappings:
      type: array
      items:
        $ref: "#/types/aws:imagebuilder/ImageRecipeBlockDeviceMapping:ImageRecipeBlockDeviceMapping"
      description: |
        Configuration block(s) with block device mappings for the image recipe. Detailed below.
      willReplaceOnChanges: true
    components:
      type: array
      items:
        $ref: "#/types/aws:imagebuilder/ImageRecipeComponent:ImageRecipeComponent"
      description: |
        Ordered configuration block(s) with components for the image recipe. Detailed below.
      willReplaceOnChanges: true
    dateCreated:
      type: string
      description: |
        Date the image recipe was created.
    description:
      type: string
      description: |
        Description of the image recipe.
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        The name of the component parameter.
      willReplaceOnChanges: true
    owner:
      type: string
      description: |
        Owner of the image recipe.
    parentImage:
      type: string
      description: |
        Platform of the image recipe.
      willReplaceOnChanges: true
    platform:
      type: string
      description: |
        Platform of the image recipe.
    systemsManagerAgent:
      $ref: "#/types/aws:imagebuilder/ImageRecipeSystemsManagerAgent:ImageRecipeSystemsManagerAgent"
      description: |
        Configuration block for the Systems Manager Agent installed by default by Image Builder. Detailed below.
      willReplaceOnChanges: true
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags for the image recipe. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    userDataBase64:
      type: string
      description: |
        Base64 encoded user data. Use this to provide commands or a command script to run when you launch your build instance.
      willReplaceOnChanges: true
    version:
      type: string
      description: |
        Version of the image recipe.
      willReplaceOnChanges: true
    workingDirectory:
      type: string
      description: |
        The working directory to be used during build and test workflows.
      willReplaceOnChanges: true
  type: object
