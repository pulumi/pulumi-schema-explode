$token: aws:imagebuilder/infrastructureConfiguration:InfrastructureConfiguration
properties:
  arn:
    type: string
    description: |
      Amazon Resource Name (ARN) of the configuration.
  dateCreated:
    type: string
    description: |
      Date when the configuration was created.
  dateUpdated:
    type: string
    description: |
      Date when the configuration was updated.
  description:
    type: string
    description: |
      Description for the configuration.
  instanceMetadataOptions:
    $ref: "#/types/aws:imagebuilder/InfrastructureConfigurationInstanceMetadataOptions:InfrastructureConfigurationInstanceMetadataOptions"
    description: |
      Configuration block with instance metadata options for the HTTP requests that pipeline builds use to launch EC2 build and test instances. Detailed below.
  instanceProfileName:
    type: string
    description: |
      Name of IAM Instance Profile.
  instanceTypes:
    type: array
    items:
      type: string
    description: |
      Set of EC2 Instance Types.
  keyPair:
    type: string
    description: |
      Name of EC2 Key Pair.
  logging:
    $ref: "#/types/aws:imagebuilder/InfrastructureConfigurationLogging:InfrastructureConfigurationLogging"
    description: |
      Configuration block with logging settings. Detailed below.
  name:
    type: string
    description: |
      Name for the configuration.
  resourceTags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags to assign to infrastructure created by the configuration.
  securityGroupIds:
    type: array
    items:
      type: string
    description: |
      Set of EC2 Security Group identifiers.
  snsTopicArn:
    type: string
    description: |
      Amazon Resource Name (ARN) of SNS Topic.
  subnetId:
    type: string
    description: |
      EC2 Subnet identifier. Also requires `security_group_ids` argument.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags to assign to the configuration. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  terminateInstanceOnFailure:
    type: boolean
    description: |
      Enable if the instance should be terminated when the pipeline fails. Defaults to `false`.
required:
  - arn
  - dateCreated
  - dateUpdated
  - instanceProfileName
  - name
  - tagsAll
inputProperties:
  description:
    type: string
    description: |
      Description for the configuration.
  instanceMetadataOptions:
    $ref: "#/types/aws:imagebuilder/InfrastructureConfigurationInstanceMetadataOptions:InfrastructureConfigurationInstanceMetadataOptions"
    description: |
      Configuration block with instance metadata options for the HTTP requests that pipeline builds use to launch EC2 build and test instances. Detailed below.
  instanceProfileName:
    type: string
    description: |
      Name of IAM Instance Profile.
  instanceTypes:
    type: array
    items:
      type: string
    description: |
      Set of EC2 Instance Types.
  keyPair:
    type: string
    description: |
      Name of EC2 Key Pair.
  logging:
    $ref: "#/types/aws:imagebuilder/InfrastructureConfigurationLogging:InfrastructureConfigurationLogging"
    description: |
      Configuration block with logging settings. Detailed below.
  name:
    type: string
    description: |
      Name for the configuration.
    willReplaceOnChanges: true
  resourceTags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags to assign to infrastructure created by the configuration.
  securityGroupIds:
    type: array
    items:
      type: string
    description: |
      Set of EC2 Security Group identifiers.
  snsTopicArn:
    type: string
    description: |
      Amazon Resource Name (ARN) of SNS Topic.
  subnetId:
    type: string
    description: |
      EC2 Subnet identifier. Also requires `security_group_ids` argument.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags to assign to the configuration. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  terminateInstanceOnFailure:
    type: boolean
    description: |
      Enable if the instance should be terminated when the pipeline fails. Defaults to `false`.
requiredInputs:
  - instanceProfileName
stateInputs:
  description: |
    Input properties used for looking up and filtering InfrastructureConfiguration resources.
  properties:
    arn:
      type: string
      description: |
        Amazon Resource Name (ARN) of the configuration.
    dateCreated:
      type: string
      description: |
        Date when the configuration was created.
    dateUpdated:
      type: string
      description: |
        Date when the configuration was updated.
    description:
      type: string
      description: |
        Description for the configuration.
    instanceMetadataOptions:
      $ref: "#/types/aws:imagebuilder/InfrastructureConfigurationInstanceMetadataOptions:InfrastructureConfigurationInstanceMetadataOptions"
      description: |
        Configuration block with instance metadata options for the HTTP requests that pipeline builds use to launch EC2 build and test instances. Detailed below.
    instanceProfileName:
      type: string
      description: |
        Name of IAM Instance Profile.
    instanceTypes:
      type: array
      items:
        type: string
      description: |
        Set of EC2 Instance Types.
    keyPair:
      type: string
      description: |
        Name of EC2 Key Pair.
    logging:
      $ref: "#/types/aws:imagebuilder/InfrastructureConfigurationLogging:InfrastructureConfigurationLogging"
      description: |
        Configuration block with logging settings. Detailed below.
    name:
      type: string
      description: |
        Name for the configuration.
      willReplaceOnChanges: true
    resourceTags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags to assign to infrastructure created by the configuration.
    securityGroupIds:
      type: array
      items:
        type: string
      description: |
        Set of EC2 Security Group identifiers.
    snsTopicArn:
      type: string
      description: |
        Amazon Resource Name (ARN) of SNS Topic.
    subnetId:
      type: string
      description: |
        EC2 Subnet identifier. Also requires `security_group_ids` argument.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags to assign to the configuration. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    terminateInstanceOnFailure:
      type: boolean
      description: |
        Enable if the instance should be terminated when the pipeline fails. Defaults to `false`.
  type: object
