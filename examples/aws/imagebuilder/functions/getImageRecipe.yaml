$token: aws:imagebuilder/getImageRecipe:getImageRecipe
inputs:
  description: |
    A collection of arguments for invoking getImageRecipe.
  properties:
    arn:
      type: string
      description: |
        ARN of the image recipe.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags for the image recipe.
  type: object
  required:
    - arn
outputs:
  description: |
    A collection of values returned by getImageRecipe.
  properties:
    arn:
      type: string
    blockDeviceMappings:
      type: array
      items:
        $ref: "#/types/aws:imagebuilder/getImageRecipeBlockDeviceMapping:getImageRecipeBlockDeviceMapping"
      description: |
        Set of objects with block device mappings for the image recipe.
    components:
      type: array
      items:
        $ref: "#/types/aws:imagebuilder/getImageRecipeComponent:getImageRecipeComponent"
      description: |
        List of objects with components for the image recipe.
    dateCreated:
      type: string
      description: |
        Date the image recipe was created.
    description:
      type: string
      description: |
        Description of the image recipe.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    name:
      type: string
      description: |
        Name of the image recipe.
    owner:
      type: string
      description: |
        Owner of the image recipe.
    parentImage:
      type: string
      description: |
        Base image of the image recipe.
    platform:
      type: string
      description: |
        Platform of the image recipe.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags for the image recipe.
    userDataBase64:
      type: string
      description: |
        Base64 encoded contents of user data. Commands or a command script to run when build instance is launched.
    version:
      type: string
      description: |
        Version of the image recipe.
    workingDirectory:
      type: string
      description: |
        Working directory used during build and test workflows.
  type: object
  required:
    - arn
    - blockDeviceMappings
    - components
    - dateCreated
    - description
    - name
    - owner
    - parentImage
    - platform
    - userDataBase64
    - version
    - workingDirectory
    - id
