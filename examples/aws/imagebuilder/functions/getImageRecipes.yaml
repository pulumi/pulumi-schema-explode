$token: aws:imagebuilder/getImageRecipes:getImageRecipes
inputs:
  description: |
    A collection of arguments for invoking getImageRecipes.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:imagebuilder/getImageRecipesFilter:getImageRecipesFilter"
      description: |
        Configuration block(s) for filtering. Detailed below.
    owner:
      type: string
      description: |
        Owner of the image recipes. Valid values are `Self`, `Shared` and `Amazon`. Defaults to `Self`.
  type: object
outputs:
  description: |
    A collection of values returned by getImageRecipes.
  properties:
    arns:
      type: array
      items:
        type: string
      description: |
        Set of ARNs of the matched Image Builder Image Recipes.
    filters:
      type: array
      items:
        $ref: "#/types/aws:imagebuilder/getImageRecipesFilter:getImageRecipesFilter"
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    names:
      type: array
      items:
        type: string
      description: |
        Set of names of the matched Image Builder Image Recipes.
    owner:
      type: string
  type: object
  required:
    - arns
    - names
    - id
