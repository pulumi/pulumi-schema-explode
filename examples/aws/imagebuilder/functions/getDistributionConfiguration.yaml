$token: aws:imagebuilder/getDistributionConfiguration:getDistributionConfiguration
inputs:
  description: |
    A collection of arguments for invoking getDistributionConfiguration.
  properties:
    arn:
      type: string
      description: |
        ARN of the distribution configuration.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags for the distribution configuration.
  type: object
  required:
    - arn
outputs:
  description: |
    A collection of values returned by getDistributionConfiguration.
  properties:
    arn:
      type: string
    dateCreated:
      type: string
      description: |
        Date the distribution configuration was created.
    dateUpdated:
      type: string
      description: |
        Date the distribution configuration was updated.
    description:
      type: string
      description: |
        Description of the container distribution configuration.
    distributions:
      type: array
      items:
        $ref: "#/types/aws:imagebuilder/getDistributionConfigurationDistribution:getDistributionConfigurationDistribution"
      description: |
        Set of distributions.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    name:
      type: string
      description: |
        Name of the distribution configuration.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags for the distribution configuration.
  type: object
  required:
    - arn
    - dateCreated
    - dateUpdated
    - description
    - distributions
    - name
    - tags
    - id
