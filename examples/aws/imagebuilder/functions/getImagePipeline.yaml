$token: aws:imagebuilder/getImagePipeline:getImagePipeline
inputs:
  description: |
    A collection of arguments for invoking getImagePipeline.
  properties:
    arn:
      type: string
      description: |
        ARN of the image pipeline.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags for the image pipeline.
  type: object
  required:
    - arn
outputs:
  description: |
    A collection of values returned by getImagePipeline.
  properties:
    arn:
      type: string
    containerRecipeArn:
      type: string
      description: |
        ARN of the container recipe.
    dateCreated:
      type: string
      description: |
        Date the image pipeline was created.
    dateLastRun:
      type: string
      description: |
        Date the image pipeline was last run.
    dateNextRun:
      type: string
      description: |
        Date the image pipeline will run next.
    dateUpdated:
      type: string
      description: |
        Date the image pipeline was updated.
    description:
      type: string
      description: |
        Description of the image pipeline.
    distributionConfigurationArn:
      type: string
      description: |
        ARN of the Image Builder Distribution Configuration.
    enhancedImageMetadataEnabled:
      type: boolean
      description: |
        Whether additional information about the image being created is collected.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    imageRecipeArn:
      type: string
      description: |
        ARN of the image recipe.
    imageTestsConfigurations:
      type: array
      items:
        $ref: "#/types/aws:imagebuilder/getImagePipelineImageTestsConfiguration:getImagePipelineImageTestsConfiguration"
      description: |
        List of an object with image tests configuration.
    infrastructureConfigurationArn:
      type: string
      description: |
        ARN of the Image Builder Infrastructure Configuration.
    name:
      type: string
      description: |
        Name of the image pipeline.
    platform:
      type: string
      description: |
        Platform of the image pipeline.
    schedules:
      type: array
      items:
        $ref: "#/types/aws:imagebuilder/getImagePipelineSchedule:getImagePipelineSchedule"
      description: |
        List of an object with schedule settings.
    status:
      type: string
      description: |
        Status of the image pipeline.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags for the image pipeline.
  type: object
  required:
    - arn
    - containerRecipeArn
    - dateCreated
    - dateLastRun
    - dateNextRun
    - dateUpdated
    - description
    - distributionConfigurationArn
    - enhancedImageMetadataEnabled
    - imageRecipeArn
    - imageTestsConfigurations
    - infrastructureConfigurationArn
    - name
    - platform
    - schedules
    - status
    - tags
    - id
