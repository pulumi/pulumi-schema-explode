$token: aws:imagebuilder/getContainerRecipes:getContainerRecipes
inputs:
  description: |
    A collection of arguments for invoking getContainerRecipes.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:imagebuilder/getContainerRecipesFilter:getContainerRecipesFilter"
      description: |
        Configuration block(s) for filtering. Detailed below.
    owner:
      type: string
      description: |
        Owner of the container recipes. Valid values are `Self`, `Shared` and `Amazon`. Defaults to `Self`.
  type: object
outputs:
  description: |
    A collection of values returned by getContainerRecipes.
  properties:
    arns:
      type: array
      items:
        type: string
      description: |
        Set of ARNs of the matched Image Builder Container Recipes.
    filters:
      type: array
      items:
        $ref: "#/types/aws:imagebuilder/getContainerRecipesFilter:getContainerRecipesFilter"
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    names:
      type: array
      items:
        type: string
      description: |
        Set of names of the matched Image Builder Container Recipes.
    owner:
      type: string
  type: object
  required:
    - arns
    - names
    - id
