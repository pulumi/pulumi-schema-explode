$token: aws:imagebuilder/getInfrastructureConfiguration:getInfrastructureConfiguration
inputs:
  description: |
    A collection of arguments for invoking getInfrastructureConfiguration.
  properties:
    arn:
      type: string
      description: |
        ARN of the infrastructure configuration.
    resourceTags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags for the infrastructure created by the infrastructure configuration.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags for the infrastructure configuration.
  type: object
  required:
    - arn
outputs:
  description: |
    A collection of values returned by getInfrastructureConfiguration.
  properties:
    arn:
      type: string
    dateCreated:
      type: string
      description: |
        Date the infrastructure configuration was updated.
    dateUpdated:
      type: string
    description:
      type: string
      description: |
        Description of the infrastructure configuration.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    instanceMetadataOptions:
      type: array
      items:
        $ref: "#/types/aws:imagebuilder/getInfrastructureConfigurationInstanceMetadataOption:getInfrastructureConfigurationInstanceMetadataOption"
      description: |
        Nested list of instance metadata options for the HTTP requests that pipeline builds use to launch EC2 build and test instances.
    instanceProfileName:
      type: string
      description: |
        Name of the IAM Instance Profile associated with the configuration.
    instanceTypes:
      type: array
      items:
        type: string
      description: |
        Set of EC2 Instance Types associated with the configuration.
    keyPair:
      type: string
      description: |
        Name of the EC2 Key Pair associated with the configuration.
    loggings:
      type: array
      items:
        $ref: "#/types/aws:imagebuilder/getInfrastructureConfigurationLogging:getInfrastructureConfigurationLogging"
      description: |
        Nested list of logging settings.
    name:
      type: string
      description: |
        Name of the infrastructure configuration.
    resourceTags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags for the infrastructure created by the infrastructure configuration.
    securityGroupIds:
      type: array
      items:
        type: string
      description: |
        Set of EC2 Security Group identifiers associated with the configuration.
    snsTopicArn:
      type: string
      description: |
        ARN of the SNS Topic associated with the configuration.
    subnetId:
      type: string
      description: |
        Identifier of the EC2 Subnet associated with the configuration.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags for the infrastructure configuration.
    terminateInstanceOnFailure:
      type: boolean
      description: |
        Whether instances are terminated on failure.
  type: object
  required:
    - arn
    - dateCreated
    - dateUpdated
    - description
    - instanceMetadataOptions
    - instanceProfileName
    - instanceTypes
    - keyPair
    - loggings
    - name
    - resourceTags
    - securityGroupIds
    - snsTopicArn
    - subnetId
    - tags
    - terminateInstanceOnFailure
    - id
