$token: aws:imagebuilder/getInfrastructureConfigurations:getInfrastructureConfigurations
inputs:
  description: |
    A collection of arguments for invoking getInfrastructureConfigurations.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:imagebuilder/getInfrastructureConfigurationsFilter:getInfrastructureConfigurationsFilter"
      description: |
        Configuration block(s) for filtering. Detailed below.
  type: object
outputs:
  description: |
    A collection of values returned by getInfrastructureConfigurations.
  properties:
    arns:
      type: array
      items:
        type: string
      description: |
        Set of ARNs of the matched Image Builder Infrastructure Configurations.
    filters:
      type: array
      items:
        $ref: "#/types/aws:imagebuilder/getInfrastructureConfigurationsFilter:getInfrastructureConfigurationsFilter"
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    names:
      type: array
      items:
        type: string
      description: |
        Set of names of the matched Image Builder Infrastructure Configurations.
  type: object
  required:
    - arns
    - names
    - id
