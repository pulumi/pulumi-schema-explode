$token: aws:route53recoveryreadiness/readinessCheck:ReadinessCheck
properties:
  arn:
    type: string
    description: |
      ARN of the readiness_check
  readinessCheckName:
    type: string
    description: |
      Unique name describing the readiness check.
  resourceSetName:
    type: string
    description: |
      Name describing the resource set that will be monitored for readiness.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value mapping of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      Map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - readinessCheckName
  - resourceSetName
  - tagsAll
inputProperties:
  readinessCheckName:
    type: string
    description: |
      Unique name describing the readiness check.
    willReplaceOnChanges: true
  resourceSetName:
    type: string
    description: |
      Name describing the resource set that will be monitored for readiness.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value mapping of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level
requiredInputs:
  - readinessCheckName
  - resourceSetName
stateInputs:
  description: |
    Input properties used for looking up and filtering ReadinessCheck resources.
  properties:
    arn:
      type: string
      description: |
        ARN of the readiness_check
    readinessCheckName:
      type: string
      description: |
        Unique name describing the readiness check.
      willReplaceOnChanges: true
    resourceSetName:
      type: string
      description: |
        Name describing the resource set that will be monitored for readiness.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value mapping of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
