$token: aws:synthetics/CanaryArtifactConfig:CanaryArtifactConfig
properties:
  s3Encryption:
    $ref: "#/types/aws:synthetics/CanaryArtifactConfigS3Encryption:CanaryArtifactConfigS3Encryption"
    description: |
      Configuration of the encryption-at-rest settings for artifacts that the canary uploads to Amazon S3. See S3 Encryption.
    language:
      python:
        mapCase: false
type: object
