$token: aws:synthetics/CanaryArtifactConfigS3Encryption:CanaryArtifactConfigS3Encryption
properties:
  encryptionMode:
    type: string
    description: |
      The encryption method to use for artifacts created by this canary. Valid values are: `SSE_S3` and `SSE_KMS`.
    language:
      python:
        mapCase: false
  kmsKeyArn:
    type: string
    description: |
      The ARN of the customer-managed KMS key to use, if you specify `SSE_KMS` for `encryption_mode`.
    language:
      python:
        mapCase: false
type: object
