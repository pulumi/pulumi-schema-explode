$token: aws:ec2/vpnGatewayRoutePropagation:VpnGatewayRoutePropagation
properties:
  routeTableId:
    type: string
    description: |
      The id of the `aws.ec2.RouteTable` to propagate routes into.
  vpnGatewayId:
    type: string
    description: |
      The id of the `aws.ec2.VpnGateway` to propagate routes from.
required:
  - routeTableId
  - vpnGatewayId
inputProperties:
  routeTableId:
    type: string
    description: |
      The id of the `aws.ec2.RouteTable` to propagate routes into.
    willReplaceOnChanges: true
  vpnGatewayId:
    type: string
    description: |
      The id of the `aws.ec2.VpnGateway` to propagate routes from.
    willReplaceOnChanges: true
requiredInputs:
  - routeTableId
  - vpnGatewayId
stateInputs:
  description: |
    Input properties used for looking up and filtering VpnGatewayRoutePropagation resources.
  properties:
    routeTableId:
      type: string
      description: |
        The id of the `aws.ec2.RouteTable` to propagate routes into.
      willReplaceOnChanges: true
    vpnGatewayId:
      type: string
      description: |
        The id of the `aws.ec2.VpnGateway` to propagate routes from.
      willReplaceOnChanges: true
  type: object
