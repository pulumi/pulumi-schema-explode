$token: aws:ec2/vpcEndpointServiceAllowedPrinciple:VpcEndpointServiceAllowedPrinciple
properties:
  principalArn:
    type: string
    description: |
      The ARN of the principal to allow permissions.
  vpcEndpointServiceId:
    type: string
    description: |
      The ID of the VPC endpoint service to allow permission.
required:
  - principalArn
  - vpcEndpointServiceId
inputProperties:
  principalArn:
    type: string
    description: |
      The ARN of the principal to allow permissions.
    willReplaceOnChanges: true
  vpcEndpointServiceId:
    type: string
    description: |
      The ID of the VPC endpoint service to allow permission.
    willReplaceOnChanges: true
requiredInputs:
  - principalArn
  - vpcEndpointServiceId
stateInputs:
  description: |
    Input properties used for looking up and filtering VpcEndpointServiceAllowedPrinciple resources.
  properties:
    principalArn:
      type: string
      description: |
        The ARN of the principal to allow permissions.
      willReplaceOnChanges: true
    vpcEndpointServiceId:
      type: string
      description: |
        The ID of the VPC endpoint service to allow permission.
      willReplaceOnChanges: true
  type: object
