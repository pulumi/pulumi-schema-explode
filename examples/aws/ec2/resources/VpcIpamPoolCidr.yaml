$token: aws:ec2/vpcIpamPoolCidr:VpcIpamPoolCidr
properties:
  cidr:
    type: string
    description: |
      The CIDR you want to assign to the pool.
  cidrAuthorizationContext:
    $ref: "#/types/aws:ec2/VpcIpamPoolCidrCidrAuthorizationContext:VpcIpamPoolCidrCidrAuthorizationContext"
    description: |
      A signed document that proves that you are authorized to bring the specified IP address range to Amazon using BYOIP. This is not stored in the state file. See cidr_authorization_context for more information.
  ipamPoolId:
    type: string
    description: |
      The ID of the pool to which you want to assign a CIDR.
required:
  - cidr
  - ipamPoolId
inputProperties:
  cidr:
    type: string
    description: |
      The CIDR you want to assign to the pool.
    willReplaceOnChanges: true
  cidrAuthorizationContext:
    $ref: "#/types/aws:ec2/VpcIpamPoolCidrCidrAuthorizationContext:VpcIpamPoolCidrCidrAuthorizationContext"
    description: |
      A signed document that proves that you are authorized to bring the specified IP address range to Amazon using BYOIP. This is not stored in the state file. See cidr_authorization_context for more information.
    willReplaceOnChanges: true
  ipamPoolId:
    type: string
    description: |
      The ID of the pool to which you want to assign a CIDR.
    willReplaceOnChanges: true
requiredInputs:
  - ipamPoolId
stateInputs:
  description: |
    Input properties used for looking up and filtering VpcIpamPoolCidr resources.
  properties:
    cidr:
      type: string
      description: |
        The CIDR you want to assign to the pool.
      willReplaceOnChanges: true
    cidrAuthorizationContext:
      $ref: "#/types/aws:ec2/VpcIpamPoolCidrCidrAuthorizationContext:VpcIpamPoolCidrCidrAuthorizationContext"
      description: |
        A signed document that proves that you are authorized to bring the specified IP address range to Amazon using BYOIP. This is not stored in the state file. See cidr_authorization_context for more information.
      willReplaceOnChanges: true
    ipamPoolId:
      type: string
      description: |
        The ID of the pool to which you want to assign a CIDR.
      willReplaceOnChanges: true
  type: object
