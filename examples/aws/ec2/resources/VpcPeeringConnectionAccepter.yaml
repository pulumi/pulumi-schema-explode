$token: aws:ec2/vpcPeeringConnectionAccepter:VpcPeeringConnectionAccepter
properties:
  acceptStatus:
    type: string
    description: |
      The status of the VPC Peering Connection request.
  accepter:
    $ref: "#/types/aws:ec2/VpcPeeringConnectionAccepterAccepter:VpcPeeringConnectionAccepterAccepter"
    description: |
      A configuration block that describes [VPC Peering Connection]
      (https://docs.aws.amazon.com/vpc/latest/peering/what-is-vpc-peering.html) options set for the accepter VPC.
  autoAccept:
    type: boolean
    description: |
      Whether or not to accept the peering request. Defaults to `false`.
  peerOwnerId:
    type: string
    description: |
      The AWS account ID of the owner of the requester VPC.
  peerRegion:
    type: string
    description: |
      The region of the accepter VPC.
  peerVpcId:
    type: string
    description: |
      The ID of the requester VPC.
  requester:
    $ref: "#/types/aws:ec2/VpcPeeringConnectionAccepterRequester:VpcPeeringConnectionAccepterRequester"
    description: |
      A configuration block that describes [VPC Peering Connection]
      (https://docs.aws.amazon.com/vpc/latest/peering/what-is-vpc-peering.html) options set for the requester VPC.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  vpcId:
    type: string
    description: |
      The ID of the accepter VPC.
  vpcPeeringConnectionId:
    type: string
    description: |
      The VPC Peering Connection ID to manage.
required:
  - acceptStatus
  - accepter
  - peerOwnerId
  - peerRegion
  - peerVpcId
  - requester
  - tagsAll
  - vpcId
  - vpcPeeringConnectionId
inputProperties:
  accepter:
    $ref: "#/types/aws:ec2/VpcPeeringConnectionAccepterAccepter:VpcPeeringConnectionAccepterAccepter"
    description: |
      A configuration block that describes [VPC Peering Connection]
      (https://docs.aws.amazon.com/vpc/latest/peering/what-is-vpc-peering.html) options set for the accepter VPC.
  autoAccept:
    type: boolean
    description: |
      Whether or not to accept the peering request. Defaults to `false`.
  requester:
    $ref: "#/types/aws:ec2/VpcPeeringConnectionAccepterRequester:VpcPeeringConnectionAccepterRequester"
    description: |
      A configuration block that describes [VPC Peering Connection]
      (https://docs.aws.amazon.com/vpc/latest/peering/what-is-vpc-peering.html) options set for the requester VPC.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  vpcPeeringConnectionId:
    type: string
    description: |
      The VPC Peering Connection ID to manage.
    willReplaceOnChanges: true
requiredInputs:
  - vpcPeeringConnectionId
stateInputs:
  description: |
    Input properties used for looking up and filtering VpcPeeringConnectionAccepter resources.
  properties:
    acceptStatus:
      type: string
      description: |
        The status of the VPC Peering Connection request.
    accepter:
      $ref: "#/types/aws:ec2/VpcPeeringConnectionAccepterAccepter:VpcPeeringConnectionAccepterAccepter"
      description: |
        A configuration block that describes [VPC Peering Connection]
        (https://docs.aws.amazon.com/vpc/latest/peering/what-is-vpc-peering.html) options set for the accepter VPC.
    autoAccept:
      type: boolean
      description: |
        Whether or not to accept the peering request. Defaults to `false`.
    peerOwnerId:
      type: string
      description: |
        The AWS account ID of the owner of the requester VPC.
    peerRegion:
      type: string
      description: |
        The region of the accepter VPC.
    peerVpcId:
      type: string
      description: |
        The ID of the requester VPC.
    requester:
      $ref: "#/types/aws:ec2/VpcPeeringConnectionAccepterRequester:VpcPeeringConnectionAccepterRequester"
      description: |
        A configuration block that describes [VPC Peering Connection]
        (https://docs.aws.amazon.com/vpc/latest/peering/what-is-vpc-peering.html) options set for the requester VPC.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    vpcId:
      type: string
      description: |
        The ID of the accepter VPC.
    vpcPeeringConnectionId:
      type: string
      description: |
        The VPC Peering Connection ID to manage.
      willReplaceOnChanges: true
  type: object
