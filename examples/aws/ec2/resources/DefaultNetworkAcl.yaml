$token: aws:ec2/defaultNetworkAcl:DefaultNetworkAcl
properties:
  arn:
    type: string
    description: |
      ARN of the Default Network ACL
  defaultNetworkAclId:
    type: string
    description: |
      Network ACL ID to manage. This attribute is exported from `aws.ec2.Vpc`, or manually found via the AWS Console.
  egress:
    type: array
    items:
      $ref: "#/types/aws:ec2/DefaultNetworkAclEgress:DefaultNetworkAclEgress"
    description: |
      Configuration block for an egress rule. Detailed below.
  ingress:
    type: array
    items:
      $ref: "#/types/aws:ec2/DefaultNetworkAclIngress:DefaultNetworkAclIngress"
    description: |
      Configuration block for an ingress rule. Detailed below.
  ownerId:
    type: string
    description: |
      ID of the AWS account that owns the Default Network ACL
  subnetIds:
    type: array
    items:
      type: string
    description: |
      List of Subnet IDs to apply the ACL to. See the notes below on managing Subnets in the Default Network ACL
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  vpcId:
    type: string
    description: |
      ID of the associated VPC
required:
  - arn
  - defaultNetworkAclId
  - ownerId
  - tagsAll
  - vpcId
inputProperties:
  defaultNetworkAclId:
    type: string
    description: |
      Network ACL ID to manage. This attribute is exported from `aws.ec2.Vpc`, or manually found via the AWS Console.
    willReplaceOnChanges: true
  egress:
    type: array
    items:
      $ref: "#/types/aws:ec2/DefaultNetworkAclEgress:DefaultNetworkAclEgress"
    description: |
      Configuration block for an egress rule. Detailed below.
  ingress:
    type: array
    items:
      $ref: "#/types/aws:ec2/DefaultNetworkAclIngress:DefaultNetworkAclIngress"
    description: |
      Configuration block for an ingress rule. Detailed below.
  subnetIds:
    type: array
    items:
      type: string
    description: |
      List of Subnet IDs to apply the ACL to. See the notes below on managing Subnets in the Default Network ACL
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - defaultNetworkAclId
stateInputs:
  description: |
    Input properties used for looking up and filtering DefaultNetworkAcl resources.
  properties:
    arn:
      type: string
      description: |
        ARN of the Default Network ACL
    defaultNetworkAclId:
      type: string
      description: |
        Network ACL ID to manage. This attribute is exported from `aws.ec2.Vpc`, or manually found via the AWS Console.
      willReplaceOnChanges: true
    egress:
      type: array
      items:
        $ref: "#/types/aws:ec2/DefaultNetworkAclEgress:DefaultNetworkAclEgress"
      description: |
        Configuration block for an egress rule. Detailed below.
    ingress:
      type: array
      items:
        $ref: "#/types/aws:ec2/DefaultNetworkAclIngress:DefaultNetworkAclIngress"
      description: |
        Configuration block for an ingress rule. Detailed below.
    ownerId:
      type: string
      description: |
        ID of the AWS account that owns the Default Network ACL
    subnetIds:
      type: array
      items:
        type: string
      description: |
        List of Subnet IDs to apply the ACL to. See the notes below on managing Subnets in the Default Network ACL
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    vpcId:
      type: string
      description: |
        ID of the associated VPC
  type: object
