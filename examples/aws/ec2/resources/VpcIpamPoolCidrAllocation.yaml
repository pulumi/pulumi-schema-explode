$token: aws:ec2/vpcIpamPoolCidrAllocation:VpcIpamPoolCidrAllocation
properties:
  cidr:
    type: string
    description: |
      The CIDR you want to assign to the pool.
  description:
    type: string
    description: |
      The description for the allocation.
  disallowedCidrs:
    type: array
    items:
      type: string
    description: |
      Exclude a particular CIDR range from being returned by the pool.
  ipamPoolAllocationId:
    type: string
  ipamPoolId:
    type: string
    description: |
      The ID of the pool to which you want to assign a CIDR.
  netmaskLength:
    type: integer
    description: |
      The netmask length of the CIDR you would like to allocate to the IPAM pool. Valid Values: `0-32`.
  resourceId:
    type: string
    description: |
      The ID of the resource.
  resourceOwner:
    type: string
    description: |
      The owner of the resource.
  resourceType:
    type: string
    description: |
      The type of the resource.
required:
  - cidr
  - ipamPoolAllocationId
  - ipamPoolId
  - resourceId
  - resourceOwner
  - resourceType
inputProperties:
  cidr:
    type: string
    description: |
      The CIDR you want to assign to the pool.
    willReplaceOnChanges: true
  description:
    type: string
    description: |
      The description for the allocation.
    willReplaceOnChanges: true
  disallowedCidrs:
    type: array
    items:
      type: string
    description: |
      Exclude a particular CIDR range from being returned by the pool.
    willReplaceOnChanges: true
  ipamPoolId:
    type: string
    description: |
      The ID of the pool to which you want to assign a CIDR.
    willReplaceOnChanges: true
  netmaskLength:
    type: integer
    description: |
      The netmask length of the CIDR you would like to allocate to the IPAM pool. Valid Values: `0-32`.
    willReplaceOnChanges: true
requiredInputs:
  - ipamPoolId
stateInputs:
  description: |
    Input properties used for looking up and filtering VpcIpamPoolCidrAllocation resources.
  properties:
    cidr:
      type: string
      description: |
        The CIDR you want to assign to the pool.
      willReplaceOnChanges: true
    description:
      type: string
      description: |
        The description for the allocation.
      willReplaceOnChanges: true
    disallowedCidrs:
      type: array
      items:
        type: string
      description: |
        Exclude a particular CIDR range from being returned by the pool.
      willReplaceOnChanges: true
    ipamPoolAllocationId:
      type: string
    ipamPoolId:
      type: string
      description: |
        The ID of the pool to which you want to assign a CIDR.
      willReplaceOnChanges: true
    netmaskLength:
      type: integer
      description: |
        The netmask length of the CIDR you would like to allocate to the IPAM pool. Valid Values: `0-32`.
      willReplaceOnChanges: true
    resourceId:
      type: string
      description: |
        The ID of the resource.
    resourceOwner:
      type: string
      description: |
        The owner of the resource.
    resourceType:
      type: string
      description: |
        The type of the resource.
  type: object
