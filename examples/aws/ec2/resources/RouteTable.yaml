$token: aws:ec2/routeTable:RouteTable
properties:
  arn:
    type: string
    description: |
      The ARN of the route table.
  ownerId:
    type: string
    description: |
      The ID of the AWS account that owns the route table.
  propagatingVgws:
    type: array
    items:
      type: string
    description: |
      A list of virtual gateways for propagation.
  routes:
    type: array
    items:
      $ref: "#/types/aws:ec2/RouteTableRoute:RouteTableRoute"
    description: |
      A list of route objects. Their keys are documented below.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  vpcId:
    type: string
    description: |
      The VPC ID.
required:
  - arn
  - ownerId
  - propagatingVgws
  - routes
  - tagsAll
  - vpcId
inputProperties:
  propagatingVgws:
    type: array
    items:
      type: string
    description: |
      A list of virtual gateways for propagation.
  routes:
    type: array
    items:
      $ref: "#/types/aws:ec2/RouteTableRoute:RouteTableRoute"
    description: |
      A list of route objects. Their keys are documented below.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  vpcId:
    type: string
    description: |
      The VPC ID.
    willReplaceOnChanges: true
requiredInputs:
  - vpcId
stateInputs:
  description: |
    Input properties used for looking up and filtering RouteTable resources.
  properties:
    arn:
      type: string
      description: |
        The ARN of the route table.
    ownerId:
      type: string
      description: |
        The ID of the AWS account that owns the route table.
    propagatingVgws:
      type: array
      items:
        type: string
      description: |
        A list of virtual gateways for propagation.
    routes:
      type: array
      items:
        $ref: "#/types/aws:ec2/RouteTableRoute:RouteTableRoute"
      description: |
        A list of route objects. Their keys are documented below.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    vpcId:
      type: string
      description: |
        The VPC ID.
      willReplaceOnChanges: true
  type: object
