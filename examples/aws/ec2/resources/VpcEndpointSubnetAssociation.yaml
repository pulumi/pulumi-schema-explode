$token: aws:ec2/vpcEndpointSubnetAssociation:VpcEndpointSubnetAssociation
properties:
  subnetId:
    type: string
    description: |
      The ID of the subnet to be associated with the VPC endpoint.
  vpcEndpointId:
    type: string
    description: |
      The ID of the VPC endpoint with which the subnet will be associated.
required:
  - subnetId
  - vpcEndpointId
inputProperties:
  subnetId:
    type: string
    description: |
      The ID of the subnet to be associated with the VPC endpoint.
    willReplaceOnChanges: true
  vpcEndpointId:
    type: string
    description: |
      The ID of the VPC endpoint with which the subnet will be associated.
    willReplaceOnChanges: true
requiredInputs:
  - subnetId
  - vpcEndpointId
stateInputs:
  description: |
    Input properties used for looking up and filtering VpcEndpointSubnetAssociation resources.
  properties:
    subnetId:
      type: string
      description: |
        The ID of the subnet to be associated with the VPC endpoint.
      willReplaceOnChanges: true
    vpcEndpointId:
      type: string
      description: |
        The ID of the VPC endpoint with which the subnet will be associated.
      willReplaceOnChanges: true
  type: object
