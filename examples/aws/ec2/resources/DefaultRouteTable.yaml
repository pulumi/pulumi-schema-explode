$token: aws:ec2/defaultRouteTable:DefaultRouteTable
properties:
  arn:
    type: string
    description: |
      The ARN of the route table.
  defaultRouteTableId:
    type: string
    description: |
      ID of the default route table.
  ownerId:
    type: string
    description: |
      ID of the AWS account that owns the route table.
  propagatingVgws:
    type: array
    items:
      type: string
    description: |
      List of virtual gateways for propagation.
  routes:
    type: array
    items:
      $ref: "#/types/aws:ec2/DefaultRouteTableRoute:DefaultRouteTableRoute"
    description: |
      Set of objects. Detailed below
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  vpcId:
    type: string
    description: |
      ID of the VPC.
required:
  - arn
  - defaultRouteTableId
  - ownerId
  - routes
  - tagsAll
  - vpcId
inputProperties:
  defaultRouteTableId:
    type: string
    description: |
      ID of the default route table.
    willReplaceOnChanges: true
  propagatingVgws:
    type: array
    items:
      type: string
    description: |
      List of virtual gateways for propagation.
  routes:
    type: array
    items:
      $ref: "#/types/aws:ec2/DefaultRouteTableRoute:DefaultRouteTableRoute"
    description: |
      Set of objects. Detailed below
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - defaultRouteTableId
stateInputs:
  description: |
    Input properties used for looking up and filtering DefaultRouteTable resources.
  properties:
    arn:
      type: string
      description: |
        The ARN of the route table.
    defaultRouteTableId:
      type: string
      description: |
        ID of the default route table.
      willReplaceOnChanges: true
    ownerId:
      type: string
      description: |
        ID of the AWS account that owns the route table.
    propagatingVgws:
      type: array
      items:
        type: string
      description: |
        List of virtual gateways for propagation.
    routes:
      type: array
      items:
        $ref: "#/types/aws:ec2/DefaultRouteTableRoute:DefaultRouteTableRoute"
      description: |
        Set of objects. Detailed below
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    vpcId:
      type: string
      description: |
        ID of the VPC.
  type: object
