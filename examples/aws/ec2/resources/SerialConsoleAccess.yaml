$token: aws:ec2/serialConsoleAccess:SerialConsoleAccess
properties:
  enabled:
    type: boolean
    description: |
      Whether or not serial console access is enabled. Valid values are `true` or `false`. Defaults to `true`.
inputProperties:
  enabled:
    type: boolean
    description: |
      Whether or not serial console access is enabled. Valid values are `true` or `false`. Defaults to `true`.
stateInputs:
  description: |
    Input properties used for looking up and filtering SerialConsoleAccess resources.
  properties:
    enabled:
      type: boolean
      description: |
        Whether or not serial console access is enabled. Valid values are `true` or `false`. Defaults to `true`.
  type: object
