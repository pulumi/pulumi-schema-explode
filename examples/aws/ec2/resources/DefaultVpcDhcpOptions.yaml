$token: aws:ec2/defaultVpcDhcpOptions:DefaultVpcDhcpOptions
properties:
  arn:
    type: string
    description: |
      The ARN of the DHCP Options Set.
  domainName:
    type: string
  domainNameServers:
    type: string
  netbiosNameServers:
    type: string
    description: |
      List of NETBIOS name servers.
  netbiosNodeType:
    type: string
    description: |
      The NetBIOS node type (1, 2, 4, or 8). AWS recommends to specify 2 since broadcast and multicast are not supported in their network. For more information about these node types, see [RFC 2132](http://www.ietf.org/rfc/rfc2132.txt).
  ntpServers:
    type: string
  ownerId:
    type: string
    description: |
      The ID of the AWS account that owns the DHCP options set.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource.
  tagsAll:
    type: object
    additionalProperties:
      type: string
required:
  - arn
  - domainName
  - domainNameServers
  - netbiosNameServers
  - netbiosNodeType
  - ntpServers
  - ownerId
  - tagsAll
inputProperties:
  ownerId:
    type: string
    description: |
      The ID of the AWS account that owns the DHCP options set.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource.
stateInputs:
  description: |
    Input properties used for looking up and filtering DefaultVpcDhcpOptions resources.
  properties:
    arn:
      type: string
      description: |
        The ARN of the DHCP Options Set.
    domainName:
      type: string
    domainNameServers:
      type: string
    netbiosNameServers:
      type: string
      description: |
        List of NETBIOS name servers.
    netbiosNodeType:
      type: string
      description: |
        The NetBIOS node type (1, 2, 4, or 8). AWS recommends to specify 2 since broadcast and multicast are not supported in their network. For more information about these node types, see [RFC 2132](http://www.ietf.org/rfc/rfc2132.txt).
    ntpServers:
      type: string
    ownerId:
      type: string
      description: |
        The ID of the AWS account that owns the DHCP options set.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource.
    tagsAll:
      type: object
      additionalProperties:
        type: string
  type: object
