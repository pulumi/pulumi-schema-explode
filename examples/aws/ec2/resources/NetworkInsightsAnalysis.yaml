$token: aws:ec2/networkInsightsAnalysis:NetworkInsightsAnalysis
properties:
  alternatePathHints:
    type: array
    items:
      $ref: "#/types/aws:ec2/NetworkInsightsAnalysisAlternatePathHint:NetworkInsightsAnalysisAlternatePathHint"
    description: |
      Potential intermediate components of a feasible path. Described below.
  arn:
    type: string
    description: |
      ARN of the Network Insights Analysis.
  explanations:
    type: array
    items:
      $ref: "#/types/aws:ec2/NetworkInsightsAnalysisExplanation:NetworkInsightsAnalysisExplanation"
    description: |
      Explanation codes for an unreachable path. See the [AWS documentation](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_Explanation.html) for details.
  filterInArns:
    type: array
    items:
      type: string
    description: |
      A list of ARNs for resources the path must traverse.
  forwardPathComponents:
    type: array
    items:
      $ref: "#/types/aws:ec2/NetworkInsightsAnalysisForwardPathComponent:NetworkInsightsAnalysisForwardPathComponent"
    description: |
      The components in the path from source to destination. See the [AWS documentation](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_PathComponent.html) for details.
  networkInsightsPathId:
    type: string
    description: |
      ID of the Network Insights Path to run an analysis on.
  pathFound:
    type: boolean
    description: |
      Set to `true` if the destination was reachable.
  returnPathComponents:
    type: array
    items:
      $ref: "#/types/aws:ec2/NetworkInsightsAnalysisReturnPathComponent:NetworkInsightsAnalysisReturnPathComponent"
    description: |
      The components in the path from destination to source. See the [AWS documentation](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_PathComponent.html) for details.
  startDate:
    type: string
    description: |
      The date/time the analysis was started.
  status:
    type: string
    description: |
      The status of the analysis. `succeeded` means the analysis was completed, not that a path was found, for that see `path_found`.
  statusMessage:
    type: string
    description: |
      A message to provide more context when the `status` is `failed`.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      Map of tags assigned to the resource, including those inherited from the provider [`default_tags` configuration block](https://www.terraform.io/docs/providers/aws/index.html#default_tags-configuration-block).
  waitForCompletion:
    type: boolean
    description: |
      If enabled, the resource will wait for the Network Insights Analysis status to change to `succeeded` or `failed`. Setting this to `false` will skip the process. Default: `true`.
  warningMessage:
    type: string
    description: |
      The warning message.
required:
  - alternatePathHints
  - arn
  - explanations
  - forwardPathComponents
  - networkInsightsPathId
  - pathFound
  - returnPathComponents
  - startDate
  - status
  - statusMessage
  - tagsAll
  - warningMessage
inputProperties:
  filterInArns:
    type: array
    items:
      type: string
    description: |
      A list of ARNs for resources the path must traverse.
    willReplaceOnChanges: true
  networkInsightsPathId:
    type: string
    description: |
      ID of the Network Insights Path to run an analysis on.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  waitForCompletion:
    type: boolean
    description: |
      If enabled, the resource will wait for the Network Insights Analysis status to change to `succeeded` or `failed`. Setting this to `false` will skip the process. Default: `true`.
requiredInputs:
  - networkInsightsPathId
stateInputs:
  description: |
    Input properties used for looking up and filtering NetworkInsightsAnalysis resources.
  properties:
    alternatePathHints:
      type: array
      items:
        $ref: "#/types/aws:ec2/NetworkInsightsAnalysisAlternatePathHint:NetworkInsightsAnalysisAlternatePathHint"
      description: |
        Potential intermediate components of a feasible path. Described below.
    arn:
      type: string
      description: |
        ARN of the Network Insights Analysis.
    explanations:
      type: array
      items:
        $ref: "#/types/aws:ec2/NetworkInsightsAnalysisExplanation:NetworkInsightsAnalysisExplanation"
      description: |
        Explanation codes for an unreachable path. See the [AWS documentation](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_Explanation.html) for details.
    filterInArns:
      type: array
      items:
        type: string
      description: |
        A list of ARNs for resources the path must traverse.
      willReplaceOnChanges: true
    forwardPathComponents:
      type: array
      items:
        $ref: "#/types/aws:ec2/NetworkInsightsAnalysisForwardPathComponent:NetworkInsightsAnalysisForwardPathComponent"
      description: |
        The components in the path from source to destination. See the [AWS documentation](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_PathComponent.html) for details.
    networkInsightsPathId:
      type: string
      description: |
        ID of the Network Insights Path to run an analysis on.
      willReplaceOnChanges: true
    pathFound:
      type: boolean
      description: |
        Set to `true` if the destination was reachable.
    returnPathComponents:
      type: array
      items:
        $ref: "#/types/aws:ec2/NetworkInsightsAnalysisReturnPathComponent:NetworkInsightsAnalysisReturnPathComponent"
      description: |
        The components in the path from destination to source. See the [AWS documentation](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_PathComponent.html) for details.
    startDate:
      type: string
      description: |
        The date/time the analysis was started.
    status:
      type: string
      description: |
        The status of the analysis. `succeeded` means the analysis was completed, not that a path was found, for that see `path_found`.
    statusMessage:
      type: string
      description: |
        A message to provide more context when the `status` is `failed`.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags assigned to the resource, including those inherited from the provider [`default_tags` configuration block](https://www.terraform.io/docs/providers/aws/index.html#default_tags-configuration-block).
    waitForCompletion:
      type: boolean
      description: |
        If enabled, the resource will wait for the Network Insights Analysis status to change to `succeeded` or `failed`. Setting this to `false` will skip the process. Default: `true`.
    warningMessage:
      type: string
      description: |
        The warning message.
  type: object
