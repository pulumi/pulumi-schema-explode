$token: aws:ec2/localGatewayRouteTableVpcAssociation:LocalGatewayRouteTableVpcAssociation
properties:
  localGatewayId:
    type: string
  localGatewayRouteTableId:
    type: string
    description: |
      Identifier of EC2 Local Gateway Route Table.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  vpcId:
    type: string
    description: |
      Identifier of EC2 VPC.
required:
  - localGatewayId
  - localGatewayRouteTableId
  - tagsAll
  - vpcId
inputProperties:
  localGatewayRouteTableId:
    type: string
    description: |
      Identifier of EC2 Local Gateway Route Table.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value map of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  vpcId:
    type: string
    description: |
      Identifier of EC2 VPC.
    willReplaceOnChanges: true
requiredInputs:
  - localGatewayRouteTableId
  - vpcId
stateInputs:
  description: |
    Input properties used for looking up and filtering LocalGatewayRouteTableVpcAssociation resources.
  properties:
    localGatewayId:
      type: string
    localGatewayRouteTableId:
      type: string
      description: |
        Identifier of EC2 Local Gateway Route Table.
      willReplaceOnChanges: true
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    vpcId:
      type: string
      description: |
        Identifier of EC2 VPC.
      willReplaceOnChanges: true
  type: object
