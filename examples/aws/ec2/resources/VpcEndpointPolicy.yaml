$token: aws:ec2/vpcEndpointPolicy:VpcEndpointPolicy
properties:
  policy:
    type: string
  vpcEndpointId:
    type: string
    description: |
      The VPC Endpoint ID.
required:
  - policy
  - vpcEndpointId
inputProperties:
  policy:
    type: string
  vpcEndpointId:
    type: string
    description: |
      The VPC Endpoint ID.
    willReplaceOnChanges: true
requiredInputs:
  - vpcEndpointId
stateInputs:
  description: |
    Input properties used for looking up and filtering VpcEndpointPolicy resources.
  properties:
    policy:
      type: string
    vpcEndpointId:
      type: string
      description: |
        The VPC Endpoint ID.
      willReplaceOnChanges: true
  type: object
