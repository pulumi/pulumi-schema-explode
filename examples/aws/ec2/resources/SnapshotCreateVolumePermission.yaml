$token: aws:ec2/snapshotCreateVolumePermission:SnapshotCreateVolumePermission
properties:
  accountId:
    type: string
    description: |
      An AWS Account ID to add create volume permissions. The AWS Account cannot be the snapshot's owner
  snapshotId:
    type: string
    description: |
      A snapshot ID
required:
  - accountId
  - snapshotId
inputProperties:
  accountId:
    type: string
    description: |
      An AWS Account ID to add create volume permissions. The AWS Account cannot be the snapshot's owner
    willReplaceOnChanges: true
  snapshotId:
    type: string
    description: |
      A snapshot ID
    willReplaceOnChanges: true
requiredInputs:
  - accountId
  - snapshotId
stateInputs:
  description: |
    Input properties used for looking up and filtering SnapshotCreateVolumePermission resources.
  properties:
    accountId:
      type: string
      description: |
        An AWS Account ID to add create volume permissions. The AWS Account cannot be the snapshot's owner
      willReplaceOnChanges: true
    snapshotId:
      type: string
      description: |
        A snapshot ID
      willReplaceOnChanges: true
  type: object
