$token: aws:ec2/mainRouteTableAssociation:MainRouteTableAssociation
properties:
  originalRouteTableId:
    type: string
    description: |
      Used internally, see __Notes__ below
  routeTableId:
    type: string
    description: |
      The ID of the Route Table to set as the new
      main route table for the target VPC
  vpcId:
    type: string
    description: |
      The ID of the VPC whose main route table should be set
required:
  - originalRouteTableId
  - routeTableId
  - vpcId
inputProperties:
  routeTableId:
    type: string
    description: |
      The ID of the Route Table to set as the new
      main route table for the target VPC
  vpcId:
    type: string
    description: |
      The ID of the VPC whose main route table should be set
requiredInputs:
  - routeTableId
  - vpcId
stateInputs:
  description: |
    Input properties used for looking up and filtering MainRouteTableAssociation resources.
  properties:
    originalRouteTableId:
      type: string
      description: |
        Used internally, see __Notes__ below
    routeTableId:
      type: string
      description: |
        The ID of the Route Table to set as the new
        main route table for the target VPC
    vpcId:
      type: string
      description: |
        The ID of the VPC whose main route table should be set
  type: object
