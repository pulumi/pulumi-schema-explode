$token: aws:ec2/spotDatafeedSubscription:SpotDatafeedSubscription
properties:
  bucket:
    type: string
    description: |
      The Amazon S3 bucket in which to store the Spot instance data feed.
  prefix:
    type: string
    description: |
      Path of folder inside bucket to place spot pricing data.
required:
  - bucket
inputProperties:
  bucket:
    type: string
    description: |
      The Amazon S3 bucket in which to store the Spot instance data feed.
    willReplaceOnChanges: true
  prefix:
    type: string
    description: |
      Path of folder inside bucket to place spot pricing data.
    willReplaceOnChanges: true
requiredInputs:
  - bucket
stateInputs:
  description: |
    Input properties used for looking up and filtering SpotDatafeedSubscription resources.
  properties:
    bucket:
      type: string
      description: |
        The Amazon S3 bucket in which to store the Spot instance data feed.
      willReplaceOnChanges: true
    prefix:
      type: string
      description: |
        Path of folder inside bucket to place spot pricing data.
      willReplaceOnChanges: true
  type: object
