$token: aws:ec2/vpcEndpointConnectionNotification:VpcEndpointConnectionNotification
properties:
  connectionEvents:
    type: array
    items:
      type: string
    description: |
      One or more endpoint [events](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_CreateVpcEndpointConnectionNotification.html#API_CreateVpcEndpointConnectionNotification_RequestParameters) for which to receive notifications.
  connectionNotificationArn:
    type: string
    description: |
      The ARN of the SNS topic for the notifications.
  notificationType:
    type: string
    description: |
      The type of notification.
  state:
    type: string
    description: |
      The state of the notification.
  vpcEndpointId:
    type: string
    description: |
      The ID of the VPC Endpoint to receive notifications for.
  vpcEndpointServiceId:
    type: string
    description: |
      The ID of the VPC Endpoint Service to receive notifications for.
required:
  - connectionEvents
  - connectionNotificationArn
  - notificationType
  - state
inputProperties:
  connectionEvents:
    type: array
    items:
      type: string
    description: |
      One or more endpoint [events](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_CreateVpcEndpointConnectionNotification.html#API_CreateVpcEndpointConnectionNotification_RequestParameters) for which to receive notifications.
  connectionNotificationArn:
    type: string
    description: |
      The ARN of the SNS topic for the notifications.
  vpcEndpointId:
    type: string
    description: |
      The ID of the VPC Endpoint to receive notifications for.
    willReplaceOnChanges: true
  vpcEndpointServiceId:
    type: string
    description: |
      The ID of the VPC Endpoint Service to receive notifications for.
    willReplaceOnChanges: true
requiredInputs:
  - connectionEvents
  - connectionNotificationArn
stateInputs:
  description: |
    Input properties used for looking up and filtering VpcEndpointConnectionNotification resources.
  properties:
    connectionEvents:
      type: array
      items:
        type: string
      description: |
        One or more endpoint [events](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_CreateVpcEndpointConnectionNotification.html#API_CreateVpcEndpointConnectionNotification_RequestParameters) for which to receive notifications.
    connectionNotificationArn:
      type: string
      description: |
        The ARN of the SNS topic for the notifications.
    notificationType:
      type: string
      description: |
        The type of notification.
    state:
      type: string
      description: |
        The state of the notification.
    vpcEndpointId:
      type: string
      description: |
        The ID of the VPC Endpoint to receive notifications for.
      willReplaceOnChanges: true
    vpcEndpointServiceId:
      type: string
      description: |
        The ID of the VPC Endpoint Service to receive notifications for.
      willReplaceOnChanges: true
  type: object
