$token: aws:ec2/vpnConnectionRoute:VpnConnectionRoute
properties:
  destinationCidrBlock:
    type: string
    description: |
      The CIDR block associated with the local subnet of the customer network.
  vpnConnectionId:
    type: string
    description: |
      The ID of the VPN connection.
required:
  - destinationCidrBlock
  - vpnConnectionId
inputProperties:
  destinationCidrBlock:
    type: string
    description: |
      The CIDR block associated with the local subnet of the customer network.
    willReplaceOnChanges: true
  vpnConnectionId:
    type: string
    description: |
      The ID of the VPN connection.
    willReplaceOnChanges: true
requiredInputs:
  - destinationCidrBlock
  - vpnConnectionId
stateInputs:
  description: |
    Input properties used for looking up and filtering VpnConnectionRoute resources.
  properties:
    destinationCidrBlock:
      type: string
      description: |
        The CIDR block associated with the local subnet of the customer network.
      willReplaceOnChanges: true
    vpnConnectionId:
      type: string
      description: |
        The ID of the VPN connection.
      willReplaceOnChanges: true
  type: object
