$token: aws:ec2/availabilityZoneGroup:AvailabilityZoneGroup
properties:
  groupName:
    type: string
    description: |
      Name of the Availability Zone Group.
  optInStatus:
    type: string
    description: |
      Indicates whether to enable or disable Availability Zone Group. Valid values: `opted-in` or `not-opted-in`.
required:
  - groupName
  - optInStatus
inputProperties:
  groupName:
    type: string
    description: |
      Name of the Availability Zone Group.
    willReplaceOnChanges: true
  optInStatus:
    type: string
    description: |
      Indicates whether to enable or disable Availability Zone Group. Valid values: `opted-in` or `not-opted-in`.
requiredInputs:
  - groupName
  - optInStatus
stateInputs:
  description: |
    Input properties used for looking up and filtering AvailabilityZoneGroup resources.
  properties:
    groupName:
      type: string
      description: |
        Name of the Availability Zone Group.
      willReplaceOnChanges: true
    optInStatus:
      type: string
      description: |
        Indicates whether to enable or disable Availability Zone Group. Valid values: `opted-in` or `not-opted-in`.
  type: object
