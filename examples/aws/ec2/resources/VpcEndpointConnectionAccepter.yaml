$token: aws:ec2/vpcEndpointConnectionAccepter:VpcEndpointConnectionAccepter
properties:
  vpcEndpointId:
    type: string
    description: |
      AWS VPC Endpoint ID.
  vpcEndpointServiceId:
    type: string
    description: |
      AWS VPC Endpoint Service ID.
  vpcEndpointState:
    type: string
    description: |
      State of the VPC Endpoint.
required:
  - vpcEndpointId
  - vpcEndpointServiceId
  - vpcEndpointState
inputProperties:
  vpcEndpointId:
    type: string
    description: |
      AWS VPC Endpoint ID.
    willReplaceOnChanges: true
  vpcEndpointServiceId:
    type: string
    description: |
      AWS VPC Endpoint Service ID.
    willReplaceOnChanges: true
requiredInputs:
  - vpcEndpointId
  - vpcEndpointServiceId
stateInputs:
  description: |
    Input properties used for looking up and filtering VpcEndpointConnectionAccepter resources.
  properties:
    vpcEndpointId:
      type: string
      description: |
        AWS VPC Endpoint ID.
      willReplaceOnChanges: true
    vpcEndpointServiceId:
      type: string
      description: |
        AWS VPC Endpoint Service ID.
      willReplaceOnChanges: true
    vpcEndpointState:
      type: string
      description: |
        State of the VPC Endpoint.
  type: object
