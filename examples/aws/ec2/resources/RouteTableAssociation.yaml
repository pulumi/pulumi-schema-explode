$token: aws:ec2/routeTableAssociation:RouteTableAssociation
properties:
  gatewayId:
    type: string
    description: |
      The gateway ID to create an association. Conflicts with `subnet_id`.
  routeTableId:
    type: string
    description: |
      The ID of the routing table to associate with.
  subnetId:
    type: string
    description: |
      The subnet ID to create an association. Conflicts with `gateway_id`.
required:
  - routeTableId
inputProperties:
  gatewayId:
    type: string
    description: |
      The gateway ID to create an association. Conflicts with `subnet_id`.
    willReplaceOnChanges: true
  routeTableId:
    type: string
    description: |
      The ID of the routing table to associate with.
  subnetId:
    type: string
    description: |
      The subnet ID to create an association. Conflicts with `gateway_id`.
    willReplaceOnChanges: true
requiredInputs:
  - routeTableId
stateInputs:
  description: |
    Input properties used for looking up and filtering RouteTableAssociation resources.
  properties:
    gatewayId:
      type: string
      description: |
        The gateway ID to create an association. Conflicts with `subnet_id`.
      willReplaceOnChanges: true
    routeTableId:
      type: string
      description: |
        The ID of the routing table to associate with.
    subnetId:
      type: string
      description: |
        The subnet ID to create an association. Conflicts with `gateway_id`.
      willReplaceOnChanges: true
  type: object
