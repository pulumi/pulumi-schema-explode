$token: aws:ec2/vpcIpamOrganizationAdminAccount:VpcIpamOrganizationAdminAccount
properties:
  arn:
    type: string
    description: |
      The Organizations ARN for the delegate account.
  delegatedAdminAccountId:
    type: string
  email:
    type: string
    description: |
      The Organizations email for the delegate account.
  name:
    type: string
    description: |
      The Organizations name for the delegate account.
  servicePrincipal:
    type: string
    description: |
      The AWS service principal.
required:
  - arn
  - delegatedAdminAccountId
  - email
  - name
  - servicePrincipal
inputProperties:
  delegatedAdminAccountId:
    type: string
    willReplaceOnChanges: true
requiredInputs:
  - delegatedAdminAccountId
stateInputs:
  description: |
    Input properties used for looking up and filtering VpcIpamOrganizationAdminAccount resources.
  properties:
    arn:
      type: string
      description: |
        The Organizations ARN for the delegate account.
    delegatedAdminAccountId:
      type: string
      willReplaceOnChanges: true
    email:
      type: string
      description: |
        The Organizations email for the delegate account.
    name:
      type: string
      description: |
        The Organizations name for the delegate account.
    servicePrincipal:
      type: string
      description: |
        The AWS service principal.
  type: object
