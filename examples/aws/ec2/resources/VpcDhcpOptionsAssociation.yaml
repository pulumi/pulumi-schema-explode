$token: aws:ec2/vpcDhcpOptionsAssociation:VpcDhcpOptionsAssociation
properties:
  dhcpOptionsId:
    type: string
    description: |
      The ID of the DHCP Options Set to associate to the VPC.
  vpcId:
    type: string
    description: |
      The ID of the VPC to which we would like to associate a DHCP Options Set.
required:
  - dhcpOptionsId
  - vpcId
inputProperties:
  dhcpOptionsId:
    type: string
    description: |
      The ID of the DHCP Options Set to associate to the VPC.
  vpcId:
    type: string
    description: |
      The ID of the VPC to which we would like to associate a DHCP Options Set.
    willReplaceOnChanges: true
requiredInputs:
  - dhcpOptionsId
  - vpcId
stateInputs:
  description: |
    Input properties used for looking up and filtering VpcDhcpOptionsAssociation resources.
  properties:
    dhcpOptionsId:
      type: string
      description: |
        The ID of the DHCP Options Set to associate to the VPC.
    vpcId:
      type: string
      description: |
        The ID of the VPC to which we would like to associate a DHCP Options Set.
      willReplaceOnChanges: true
  type: object
