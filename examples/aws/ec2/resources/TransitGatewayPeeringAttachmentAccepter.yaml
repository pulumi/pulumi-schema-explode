$token: aws:ec2/transitGatewayPeeringAttachmentAccepter:TransitGatewayPeeringAttachmentAccepter
properties:
  peerAccountId:
    type: string
    description: |
      Identifier of the AWS account that owns the EC2 TGW peering.
  peerRegion:
    type: string
  peerTransitGatewayId:
    type: string
    description: |
      Identifier of EC2 Transit Gateway to peer with.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value tags for the EC2 Transit Gateway Peering Attachment. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  transitGatewayAttachmentId:
    type: string
    description: |
      The ID of the EC2 Transit Gateway Peering Attachment to manage.
  transitGatewayId:
    type: string
    description: |
      Identifier of EC2 Transit Gateway.
required:
  - peerAccountId
  - peerRegion
  - peerTransitGatewayId
  - tagsAll
  - transitGatewayAttachmentId
  - transitGatewayId
inputProperties:
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value tags for the EC2 Transit Gateway Peering Attachment. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  transitGatewayAttachmentId:
    type: string
    description: |
      The ID of the EC2 Transit Gateway Peering Attachment to manage.
    willReplaceOnChanges: true
requiredInputs:
  - transitGatewayAttachmentId
stateInputs:
  description: |
    Input properties used for looking up and filtering TransitGatewayPeeringAttachmentAccepter resources.
  properties:
    peerAccountId:
      type: string
      description: |
        Identifier of the AWS account that owns the EC2 TGW peering.
    peerRegion:
      type: string
    peerTransitGatewayId:
      type: string
      description: |
        Identifier of EC2 Transit Gateway to peer with.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value tags for the EC2 Transit Gateway Peering Attachment. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    transitGatewayAttachmentId:
      type: string
      description: |
        The ID of the EC2 Transit Gateway Peering Attachment to manage.
      willReplaceOnChanges: true
    transitGatewayId:
      type: string
      description: |
        Identifier of EC2 Transit Gateway.
  type: object
deprecationMessage: aws.ec2.TransitGatewayPeeringAttachmentAccepter has been deprecated in favor of aws.ec2transitgateway.PeeringAttachmentAccepter
