$token: aws:ec2/managedPrefixList:ManagedPrefixList
properties:
  addressFamily:
    type: string
    description: |
      Address family (`IPv4` or `IPv6`) of this prefix list.
  arn:
    type: string
    description: |
      ARN of the prefix list.
  entries:
    type: array
    items:
      $ref: "#/types/aws:ec2/ManagedPrefixListEntry:ManagedPrefixListEntry"
    description: |
      Configuration block for prefix list entry. Detailed below. Different entries may have overlapping CIDR blocks, but a particular CIDR should not be duplicated.
  maxEntries:
    type: integer
    description: |
      Maximum number of entries that this prefix list can contain.
  name:
    type: string
    description: |
      Name of this resource. The name must not start with `com.amazonaws`.
  ownerId:
    type: string
    description: |
      ID of the AWS account that owns this prefix list.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Map of tags to assign to this resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
  version:
    type: integer
    description: |
      Latest version of this prefix list.
required:
  - addressFamily
  - arn
  - entries
  - maxEntries
  - name
  - ownerId
  - tagsAll
  - version
inputProperties:
  addressFamily:
    type: string
    description: |
      Address family (`IPv4` or `IPv6`) of this prefix list.
    willReplaceOnChanges: true
  entries:
    type: array
    items:
      $ref: "#/types/aws:ec2/ManagedPrefixListEntry:ManagedPrefixListEntry"
    description: |
      Configuration block for prefix list entry. Detailed below. Different entries may have overlapping CIDR blocks, but a particular CIDR should not be duplicated.
  maxEntries:
    type: integer
    description: |
      Maximum number of entries that this prefix list can contain.
  name:
    type: string
    description: |
      Name of this resource. The name must not start with `com.amazonaws`.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Map of tags to assign to this resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
requiredInputs:
  - addressFamily
  - maxEntries
stateInputs:
  description: |
    Input properties used for looking up and filtering ManagedPrefixList resources.
  properties:
    addressFamily:
      type: string
      description: |
        Address family (`IPv4` or `IPv6`) of this prefix list.
      willReplaceOnChanges: true
    arn:
      type: string
      description: |
        ARN of the prefix list.
    entries:
      type: array
      items:
        $ref: "#/types/aws:ec2/ManagedPrefixListEntry:ManagedPrefixListEntry"
      description: |
        Configuration block for prefix list entry. Detailed below. Different entries may have overlapping CIDR blocks, but a particular CIDR should not be duplicated.
    maxEntries:
      type: integer
      description: |
        Maximum number of entries that this prefix list can contain.
    name:
      type: string
      description: |
        Name of this resource. The name must not start with `com.amazonaws`.
    ownerId:
      type: string
      description: |
        ID of the AWS account that owns this prefix list.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags to assign to this resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
    version:
      type: integer
      description: |
        Latest version of this prefix list.
  type: object
