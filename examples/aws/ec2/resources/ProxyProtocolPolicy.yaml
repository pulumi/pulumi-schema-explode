$token: aws:ec2/proxyProtocolPolicy:ProxyProtocolPolicy
properties:
  instancePorts:
    type: array
    items:
      type: string
    description: |
      List of instance ports to which the policy
      should be applied. This can be specified if the protocol is SSL or TCP.
  loadBalancer:
    type: string
    description: |
      The load balancer to which the policy
      should be attached.
required:
  - instancePorts
  - loadBalancer
inputProperties:
  instancePorts:
    type: array
    items:
      type: string
    description: |
      List of instance ports to which the policy
      should be applied. This can be specified if the protocol is SSL or TCP.
  loadBalancer:
    type: string
    description: |
      The load balancer to which the policy
      should be attached.
requiredInputs:
  - instancePorts
  - loadBalancer
stateInputs:
  description: |
    Input properties used for looking up and filtering ProxyProtocolPolicy resources.
  properties:
    instancePorts:
      type: array
      items:
        type: string
      description: |
        List of instance ports to which the policy
        should be applied. This can be specified if the protocol is SSL or TCP.
    loadBalancer:
      type: string
      description: |
        The load balancer to which the policy
        should be attached.
  type: object
