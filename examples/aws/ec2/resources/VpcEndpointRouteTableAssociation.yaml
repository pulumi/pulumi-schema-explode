$token: aws:ec2/vpcEndpointRouteTableAssociation:VpcEndpointRouteTableAssociation
properties:
  routeTableId:
    type: string
    description: |
      Identifier of the EC2 Route Table to be associated with the VPC Endpoint.
  vpcEndpointId:
    type: string
    description: |
      Identifier of the VPC Endpoint with which the EC2 Route Table will be associated.
required:
  - routeTableId
  - vpcEndpointId
inputProperties:
  routeTableId:
    type: string
    description: |
      Identifier of the EC2 Route Table to be associated with the VPC Endpoint.
    willReplaceOnChanges: true
  vpcEndpointId:
    type: string
    description: |
      Identifier of the VPC Endpoint with which the EC2 Route Table will be associated.
    willReplaceOnChanges: true
requiredInputs:
  - routeTableId
  - vpcEndpointId
stateInputs:
  description: |
    Input properties used for looking up and filtering VpcEndpointRouteTableAssociation resources.
  properties:
    routeTableId:
      type: string
      description: |
        Identifier of the EC2 Route Table to be associated with the VPC Endpoint.
      willReplaceOnChanges: true
    vpcEndpointId:
      type: string
      description: |
        Identifier of the VPC Endpoint with which the EC2 Route Table will be associated.
      willReplaceOnChanges: true
  type: object
