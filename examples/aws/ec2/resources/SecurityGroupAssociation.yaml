$token: aws:ec2/securityGroupAssociation:SecurityGroupAssociation
properties:
  replaceDefaultAssociation:
    type: boolean
    description: |
      Whether this association should replace the association with the VPC's default security group that is created when no security groups are specified during VPC endpoint creation. At most 1 association per-VPC endpoint should be configured with `replace_default_association = true`.
  securityGroupId:
    type: string
    description: |
      The ID of the security group to be associated with the VPC endpoint.
  vpcEndpointId:
    type: string
    description: |
      The ID of the VPC endpoint with which the security group will be associated.
required:
  - securityGroupId
  - vpcEndpointId
inputProperties:
  replaceDefaultAssociation:
    type: boolean
    description: |
      Whether this association should replace the association with the VPC's default security group that is created when no security groups are specified during VPC endpoint creation. At most 1 association per-VPC endpoint should be configured with `replace_default_association = true`.
    willReplaceOnChanges: true
  securityGroupId:
    type: string
    description: |
      The ID of the security group to be associated with the VPC endpoint.
    willReplaceOnChanges: true
  vpcEndpointId:
    type: string
    description: |
      The ID of the VPC endpoint with which the security group will be associated.
    willReplaceOnChanges: true
requiredInputs:
  - securityGroupId
  - vpcEndpointId
stateInputs:
  description: |
    Input properties used for looking up and filtering SecurityGroupAssociation resources.
  properties:
    replaceDefaultAssociation:
      type: boolean
      description: |
        Whether this association should replace the association with the VPC's default security group that is created when no security groups are specified during VPC endpoint creation. At most 1 association per-VPC endpoint should be configured with `replace_default_association = true`.
      willReplaceOnChanges: true
    securityGroupId:
      type: string
      description: |
        The ID of the security group to be associated with the VPC endpoint.
      willReplaceOnChanges: true
    vpcEndpointId:
      type: string
      description: |
        The ID of the VPC endpoint with which the security group will be associated.
      willReplaceOnChanges: true
  type: object
