$token: aws:ec2/SpotInstanceRequestMetadataOptions:SpotInstanceRequestMetadataOptions
properties:
  httpEndpoint:
    type: string
    description: |
      Whether the metadata service is available. Valid values include `enabled` or `disabled`. Defaults to `enabled`.
    language:
      python:
        mapCase: false
  httpPutResponseHopLimit:
    type: integer
    description: |
      Desired HTTP PUT response hop limit for instance metadata requests. The larger the number, the further instance metadata requests can travel. Valid values are integer from `1` to `64`. Defaults to `1`.
    language:
      python:
        mapCase: false
  httpTokens:
    type: string
    description: |
      Whether or not the metadata service requires session tokens, also referred to as _Instance Metadata Service Version 2 (IMDSv2)_. Valid values include `optional` or `required`. Defaults to `optional`.
    language:
      python:
        mapCase: false
  instanceMetadataTags:
    type: string
    description: |
      Enables or disables access to instance tags from the instance metadata service. Valid values include `enabled` or `disabled`. Defaults to `disabled`.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - httpEndpoint
      - httpPutResponseHopLimit
      - httpTokens
      - instanceMetadataTags
