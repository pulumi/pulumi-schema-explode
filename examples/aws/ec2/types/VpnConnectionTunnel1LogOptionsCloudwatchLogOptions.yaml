$token: aws:ec2/VpnConnectionTunnel1LogOptionsCloudwatchLogOptions:VpnConnectionTunnel1LogOptionsCloudwatchLogOptions
properties:
  logEnabled:
    type: boolean
    description: |
      Enable or disable VPN tunnel logging feature. The default is `false`.
    language:
      python:
        mapCase: false
  logGroupArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the CloudWatch log group to send logs to.
    language:
      python:
        mapCase: false
  logOutputFormat:
    type: string
    description: |
      Set log format. Default format is json. Possible values are: `json` and `text`. The default is `json`.
    language:
      python:
        mapCase: false
type: object
