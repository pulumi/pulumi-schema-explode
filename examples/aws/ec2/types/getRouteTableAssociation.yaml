$token: aws:ec2/getRouteTableAssociation:getRouteTableAssociation
properties:
  gatewayId:
    type: string
    description: |
      ID of an Internet Gateway or Virtual Private Gateway which is connected to the Route Table (not exported if not passed as a parameter).
    language:
      python:
        mapCase: false
  main:
    type: boolean
    description: |
      Whether the association is due to the main route table.
    language:
      python:
        mapCase: false
  routeTableAssociationId:
    type: string
    description: |
      Association ID.
    language:
      python:
        mapCase: false
  routeTableId:
    type: string
    description: |
      ID of the specific Route Table to retrieve.
    language:
      python:
        mapCase: false
  subnetId:
    type: string
    description: |
      ID of a Subnet which is connected to the Route Table (not exported if not passed as a parameter).
    language:
      python:
        mapCase: false
type: object
required:
  - gatewayId
  - main
  - routeTableAssociationId
  - routeTableId
  - subnetId
language:
  nodejs:
    requiredInputs: []
