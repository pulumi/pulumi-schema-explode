$token: aws:ec2/SpotInstanceRequestEphemeralBlockDevice:SpotInstanceRequestEphemeralBlockDevice
properties:
  deviceName:
    type: string
    description: |
      Name of the block device to mount on the instance.
    language:
      python:
        mapCase: false
  noDevice:
    type: boolean
    description: |
      Suppresses the specified device included in the AMI's block device mapping.
    language:
      python:
        mapCase: false
  virtualName:
    type: string
    description: |
      [Instance Store Device Name](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/InstanceStorage.html#InstanceStoreDeviceNames) (e.g., `ephemeral0`).
    language:
      python:
        mapCase: false
type: object
required:
  - deviceName
