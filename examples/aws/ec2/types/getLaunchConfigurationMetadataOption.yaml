$token: aws:ec2/getLaunchConfigurationMetadataOption:getLaunchConfigurationMetadataOption
properties:
  httpEndpoint:
    type: string
    description: |
      State of the metadata service: `enabled`, `disabled`.
    language:
      python:
        mapCase: false
  httpPutResponseHopLimit:
    type: integer
    description: |
      The desired HTTP PUT response hop limit for instance metadata requests.
    language:
      python:
        mapCase: false
  httpTokens:
    type: string
    description: |
      If session tokens are required: `optional`, `required`.
    language:
      python:
        mapCase: false
type: object
required:
  - httpEndpoint
  - httpPutResponseHopLimit
  - httpTokens
language:
  nodejs:
    requiredInputs: []
