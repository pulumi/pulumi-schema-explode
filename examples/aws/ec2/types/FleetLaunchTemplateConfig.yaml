$token: aws:ec2/FleetLaunchTemplateConfig:FleetLaunchTemplateConfig
properties:
  launchTemplateSpecification:
    $ref: "#/types/aws:ec2/FleetLaunchTemplateConfigLaunchTemplateSpecification:FleetLaunchTemplateConfigLaunchTemplateSpecification"
    description: |
      Nested argument containing EC2 Launch Template to use. Defined below.
    language:
      python:
        mapCase: false
  overrides:
    type: array
    items:
      $ref: "#/types/aws:ec2/FleetLaunchTemplateConfigOverride:FleetLaunchTemplateConfigOverride"
    description: |
      Nested argument(s) containing parameters to override the same parameters in the Launch Template. Defined below.
    language:
      python:
        mapCase: false
type: object
required:
  - launchTemplateSpecification
