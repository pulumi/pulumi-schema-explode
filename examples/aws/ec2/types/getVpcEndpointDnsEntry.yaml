$token: aws:ec2/getVpcEndpointDnsEntry:getVpcEndpointDnsEntry
properties:
  dnsName:
    type: string
    description: |
      DNS name.
    language:
      python:
        mapCase: false
  hostedZoneId:
    type: string
    description: |
      ID of the private hosted zone.
    language:
      python:
        mapCase: false
type: object
required:
  - dnsName
  - hostedZoneId
language:
  nodejs:
    requiredInputs: []
