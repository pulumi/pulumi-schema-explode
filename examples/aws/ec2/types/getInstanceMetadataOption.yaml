$token: aws:ec2/getInstanceMetadataOption:getInstanceMetadataOption
properties:
  httpEndpoint:
    type: string
    description: |
      State of the metadata service: `enabled`, `disabled`.
    language:
      python:
        mapCase: false
  httpPutResponseHopLimit:
    type: integer
    description: |
      Desired HTTP PUT response hop limit for instance metadata requests.
    language:
      python:
        mapCase: false
  httpTokens:
    type: string
    description: |
      If session tokens are required: `optional`, `required`.
    language:
      python:
        mapCase: false
  instanceMetadataTags:
    type: string
    description: |
      If access to instance tags is allowed from the metadata service: `enabled`, `disabled`.
    language:
      python:
        mapCase: false
type: object
required:
  - httpEndpoint
  - httpPutResponseHopLimit
  - httpTokens
  - instanceMetadataTags
language:
  nodejs:
    requiredInputs: []
