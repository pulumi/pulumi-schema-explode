$token: aws:ec2/getLaunchConfigurationEbsBlockDevice:getLaunchConfigurationEbsBlockDevice
properties:
  deleteOnTermination:
    type: boolean
    description: |
      Whether the EBS Volume will be deleted on instance termination.
    language:
      python:
        mapCase: false
  deviceName:
    type: string
    description: |
      Name of the device.
    language:
      python:
        mapCase: false
  encrypted:
    type: boolean
    description: |
      Whether the volume is Encrypted.
    language:
      python:
        mapCase: false
  iops:
    type: integer
    description: |
      Provisioned IOPs of the volume.
    language:
      python:
        mapCase: false
  noDevice:
    type: boolean
    description: |
      Whether the device in the block device mapping of the AMI is suppressed.
    language:
      python:
        mapCase: false
  snapshotId:
    type: string
    description: |
      Snapshot ID of the mount.
    language:
      python:
        mapCase: false
  throughput:
    type: integer
    description: |
      Throughput of the volume.
    language:
      python:
        mapCase: false
  volumeSize:
    type: integer
    description: |
      Size of the volume.
    language:
      python:
        mapCase: false
  volumeType:
    type: string
    description: |
      Type of the volume.
    language:
      python:
        mapCase: false
type: object
required:
  - deleteOnTermination
  - deviceName
  - encrypted
  - iops
  - noDevice
  - snapshotId
  - throughput
  - volumeSize
  - volumeType
language:
  nodejs:
    requiredInputs: []
