$token: aws:ec2/LaunchTemplateCapacityReservationSpecification:LaunchTemplateCapacityReservationSpecification
properties:
  capacityReservationPreference:
    type: string
    description: |
      Indicates the instance's Capacity Reservation preferences. Can be `open` or `none`. (Default `none`).
    language:
      python:
        mapCase: false
  capacityReservationTarget:
    $ref: "#/types/aws:ec2/LaunchTemplateCapacityReservationSpecificationCapacityReservationTarget:LaunchTemplateCapacityReservationSpecificationCapacityReservationTarget"
    description: |
      Used to target a specific Capacity Reservation:
    language:
      python:
        mapCase: false
type: object
