$token: aws:ec2/LaunchTemplatePlacement:LaunchTemplatePlacement
properties:
  affinity:
    type: string
    description: |
      The affinity setting for an instance on a Dedicated Host.
    language:
      python:
        mapCase: false
  availabilityZone:
    type: string
    description: |
      The Availability Zone for the instance.
    language:
      python:
        mapCase: false
  groupName:
    type: string
    description: |
      The name of the placement group for the instance.
    language:
      python:
        mapCase: false
  hostId:
    type: string
    description: |
      The ID of the Dedicated Host for the instance.
    language:
      python:
        mapCase: false
  hostResourceGroupArn:
    type: string
    description: |
      The ARN of the Host Resource Group in which to launch instances.
    language:
      python:
        mapCase: false
  partitionNumber:
    type: integer
    description: |
      The number of the partition the instance should launch in. Valid only if the placement group strategy is set to partition.
    language:
      python:
        mapCase: false
  spreadDomain:
    type: string
    description: |
      Reserved for future use.
    language:
      python:
        mapCase: false
  tenancy:
    type: string
    description: |
      The tenancy of the instance (if the instance is running in a VPC). Can be `default`, `dedicated`, or `host`.
    language:
      python:
        mapCase: false
type: object
