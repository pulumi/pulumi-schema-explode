$token: aws:ec2/getInstanceTypeOfferingsFilter:getInstanceTypeOfferingsFilter
properties:
  name:
    type: string
    description: |
      Name of the filter. The `location` filter depends on the top-level `location_type` argument and if not specified, defaults to the current region.
    language:
      python:
        mapCase: false
  values:
    type: array
    items:
      type: string
    description: |
      List of one or more values for the filter.
    language:
      python:
        mapCase: false
type: object
required:
  - name
  - values
