$token: aws:ec2/LaunchTemplateHibernationOptions:LaunchTemplateHibernationOptions
properties:
  configured:
    type: boolean
    description: |
      If set to `true`, the launched EC2 instance will hibernation enabled.
    language:
      python:
        mapCase: false
type: object
required:
  - configured
