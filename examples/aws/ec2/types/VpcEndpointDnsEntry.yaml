$token: aws:ec2/VpcEndpointDnsEntry:VpcEndpointDnsEntry
properties:
  dnsName:
    type: string
    description: |
      The DNS name.
    language:
      python:
        mapCase: false
  hostedZoneId:
    type: string
    description: |
      The ID of the private hosted zone.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - dnsName
      - hostedZoneId
