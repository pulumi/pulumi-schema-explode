$token: aws:ec2/getInstanceRootBlockDevice:getInstanceRootBlockDevice
properties:
  deleteOnTermination:
    type: boolean
    description: |
      If the root block device will be deleted on termination.
    language:
      python:
        mapCase: false
  deviceName:
    type: string
    description: |
      Physical name of the device.
    language:
      python:
        mapCase: false
  encrypted:
    type: boolean
    description: |
      If the EBS volume is encrypted.
    language:
      python:
        mapCase: false
  iops:
    type: integer
    description: |
      `0` If the volume is not a provisioned IOPS image, otherwise the supported IOPS count.
    language:
      python:
        mapCase: false
  kmsKeyId:
    type: string
    language:
      python:
        mapCase: false
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Map of tags assigned to the Instance.
    language:
      python:
        mapCase: false
  throughput:
    type: integer
    description: |
      Throughput of the volume, in MiB/s.
    language:
      python:
        mapCase: false
  volumeId:
    type: string
    language:
      python:
        mapCase: false
  volumeSize:
    type: integer
    description: |
      Size of the volume, in GiB.
    language:
      python:
        mapCase: false
  volumeType:
    type: string
    description: |
      Type of the volume.
    language:
      python:
        mapCase: false
type: object
required:
  - deleteOnTermination
  - deviceName
  - encrypted
  - iops
  - kmsKeyId
  - tags
  - throughput
  - volumeId
  - volumeSize
  - volumeType
language:
  nodejs:
    requiredInputs: []
