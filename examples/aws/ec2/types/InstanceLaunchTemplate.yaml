$token: aws:ec2/InstanceLaunchTemplate:InstanceLaunchTemplate
properties:
  id:
    type: string
    description: |
      ID of the launch template. Conflicts with `name`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      Name of the launch template. Conflicts with `id`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  version:
    type: string
    description: |
      Template version. Can be a specific version number, `$Latest` or `$Default`. The default value is `$Default`.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - id
      - name
