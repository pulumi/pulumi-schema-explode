$token: aws:ec2/LaunchTemplateCreditSpecification:LaunchTemplateCreditSpecification
properties:
  cpuCredits:
    type: string
    description: |
      The credit option for CPU usage. Can be `"standard"` or `"unlimited"`. T3 instances are launched as unlimited by default. T2 instances are launched as standard by default.
    language:
      python:
        mapCase: false
type: object
