$token: aws:ec2/LaunchTemplateBlockDeviceMapping:LaunchTemplateBlockDeviceMapping
properties:
  deviceName:
    type: string
    description: |
      The name of the device to mount.
    language:
      python:
        mapCase: false
  ebs:
    $ref: "#/types/aws:ec2/LaunchTemplateBlockDeviceMappingEbs:LaunchTemplateBlockDeviceMappingEbs"
    description: |
      Configure EBS volume properties.
    language:
      python:
        mapCase: false
  noDevice:
    type: string
    description: |
      Suppresses the specified device included in the AMI's block device mapping.
    language:
      python:
        mapCase: false
  virtualName:
    type: string
    description: |
      The [Instance Store Device
      Name](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/InstanceStorage.html#InstanceStoreDeviceNames)
      (e.g., `"ephemeral0"`).
    language:
      python:
        mapCase: false
type: object
