$token: aws:ec2/VpcEndpointDnsOptions:VpcEndpointDnsOptions
properties:
  dnsRecordIpType:
    type: string
    description: |
      The DNS records created for the endpoint. Valid values are `ipv4`, `dualstack`, `service-defined`, and `ipv6`.
    language:
      python:
        mapCase: false
type: object
