$token: aws:ec2/getInstanceEphemeralBlockDevice:getInstanceEphemeralBlockDevice
properties:
  deviceName:
    type: string
    description: |
      Physical name of the device.
    language:
      python:
        mapCase: false
  noDevice:
    type: boolean
    description: |
      Whether the specified device included in the device mapping was suppressed or not (Boolean).
    language:
      python:
        mapCase: false
  virtualName:
    type: string
    description: |
      Virtual device name.
    language:
      python:
        mapCase: false
type: object
required:
  - deviceName
language:
  nodejs:
    requiredInputs: []
