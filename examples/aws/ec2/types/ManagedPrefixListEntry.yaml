$token: aws:ec2/ManagedPrefixListEntry:ManagedPrefixListEntry
properties:
  cidr:
    type: string
    description: |
      CIDR block of this entry.
    language:
      python:
        mapCase: false
  description:
    type: string
    description: |
      Description of this entry. Due to API limitations, updating only the description of an existing entry requires temporarily removing and re-adding the entry.
    language:
      python:
        mapCase: false
type: object
required:
  - cidr
