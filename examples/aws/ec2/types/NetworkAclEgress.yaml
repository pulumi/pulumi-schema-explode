$token: aws:ec2/NetworkAclEgress:NetworkAclEgress
properties:
  action:
    type: string
    description: |
      The action to take.
    language:
      python:
        mapCase: false
  cidrBlock:
    type: string
    description: |
      The CIDR block to match. This must be a
      valid network mask.
    language:
      python:
        mapCase: false
  fromPort:
    type: integer
    description: |
      The from port to match.
    language:
      python:
        mapCase: false
  icmpCode:
    type: integer
    description: |
      The ICMP type code to be used. Default 0.
    language:
      python:
        mapCase: false
  icmpType:
    type: integer
    description: |
      The ICMP type to be used. Default 0.
    language:
      python:
        mapCase: false
  ipv6CidrBlock:
    type: string
    description: |
      The IPv6 CIDR block.
    language:
      python:
        mapCase: false
  protocol:
    type: string
    description: |
      The protocol to match. If using the -1 'all'
      protocol, you must specify a from and to port of 0.
    language:
      python:
        mapCase: false
  ruleNo:
    type: integer
    description: |
      The rule number. Used for ordering.
    language:
      python:
        mapCase: false
  toPort:
    type: integer
    description: |
      The to port to match.
    language:
      python:
        mapCase: false
type: object
required:
  - action
  - fromPort
  - protocol
  - ruleNo
  - toPort
