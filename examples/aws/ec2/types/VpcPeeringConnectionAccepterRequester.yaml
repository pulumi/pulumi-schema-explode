$token: aws:ec2/VpcPeeringConnectionAccepterRequester:VpcPeeringConnectionAccepterRequester
properties:
  allowClassicLinkToRemoteVpc:
    type: boolean
    description: |
      Indicates whether a local ClassicLink connection can communicate
      with the peer VPC over the VPC Peering Connection.
    deprecationMessage: With the retirement of EC2-Classic the allow_classic_link_to_remote_vpc attribute has been deprecated and will be removed in a future version.
    language:
      python:
        mapCase: false
  allowRemoteVpcDnsResolution:
    type: boolean
    description: |
      Indicates whether a local VPC can resolve public DNS hostnames to
      private IP addresses when queried from instances in a peer VPC.
    language:
      python:
        mapCase: false
  allowVpcToRemoteClassicLink:
    type: boolean
    description: |
      Indicates whether a local VPC can communicate with a ClassicLink
      connection in the peer VPC over the VPC Peering Connection.
    deprecationMessage: With the retirement of EC2-Classic the allow_vpc_to_remote_classic_link attribute has been deprecated and will be removed in a future version.
    language:
      python:
        mapCase: false
type: object
