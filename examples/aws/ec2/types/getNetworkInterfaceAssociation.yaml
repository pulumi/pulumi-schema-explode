$token: aws:ec2/getNetworkInterfaceAssociation:getNetworkInterfaceAssociation
properties:
  allocationId:
    type: string
    description: |
      Allocation ID.
    language:
      python:
        mapCase: false
  associationId:
    type: string
    description: |
      Association ID.
    language:
      python:
        mapCase: false
  carrierIp:
    type: string
    description: |
      Carrier IP address associated with the network interface. This attribute is only set when the network interface is in a subnet which is associated with a Wavelength Zone.
    language:
      python:
        mapCase: false
  customerOwnedIp:
    type: string
    description: |
      Customer-owned IP address.
    language:
      python:
        mapCase: false
  ipOwnerId:
    type: string
    description: |
      ID of the Elastic IP address owner.
    language:
      python:
        mapCase: false
  publicDnsName:
    type: string
    description: |
      Public DNS name.
    language:
      python:
        mapCase: false
  publicIp:
    type: string
    description: |
      Address of the Elastic IP address bound to the network interface.
    language:
      python:
        mapCase: false
type: object
required:
  - allocationId
  - associationId
  - carrierIp
  - customerOwnedIp
  - ipOwnerId
  - publicDnsName
  - publicIp
language:
  nodejs:
    requiredInputs: []
