$token: aws:ec2/getLaunchConfigurationRootBlockDevice:getLaunchConfigurationRootBlockDevice
properties:
  deleteOnTermination:
    type: boolean
    description: |
      Whether the EBS Volume will be deleted on instance termination.
    language:
      python:
        mapCase: false
  encrypted:
    type: boolean
    description: |
      Whether the volume is Encrypted.
    language:
      python:
        mapCase: false
  iops:
    type: integer
    description: |
      Provisioned IOPs of the volume.
    language:
      python:
        mapCase: false
  throughput:
    type: integer
    description: |
      Throughput of the volume.
    language:
      python:
        mapCase: false
  volumeSize:
    type: integer
    description: |
      Size of the volume.
    language:
      python:
        mapCase: false
  volumeType:
    type: string
    description: |
      Type of the volume.
    language:
      python:
        mapCase: false
type: object
required:
  - deleteOnTermination
  - encrypted
  - iops
  - throughput
  - volumeSize
  - volumeType
language:
  nodejs:
    requiredInputs: []
