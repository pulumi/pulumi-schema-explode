$token: aws:ec2/getInternetGatewayAttachment:getInternetGatewayAttachment
properties:
  state:
    type: string
    description: |
      Current state of the attachment between the gateway and the VPC. Present only if a VPC is attached
    language:
      python:
        mapCase: false
  vpcId:
    type: string
    description: |
      ID of an attached VPC.
    language:
      python:
        mapCase: false
type: object
required:
  - state
  - vpcId
language:
  nodejs:
    requiredInputs: []
