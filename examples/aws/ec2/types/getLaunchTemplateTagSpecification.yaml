$token: aws:ec2/getLaunchTemplateTagSpecification:getLaunchTemplateTagSpecification
properties:
  resourceType:
    type: string
    language:
      python:
        mapCase: false
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Map of tags, each pair of which must exactly match a pair on the desired Launch Template.
    language:
      python:
        mapCase: false
type: object
required:
  - resourceType
  - tags
language:
  nodejs:
    requiredInputs: []
