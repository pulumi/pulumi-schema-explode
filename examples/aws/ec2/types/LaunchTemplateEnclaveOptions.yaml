$token: aws:ec2/LaunchTemplateEnclaveOptions:LaunchTemplateEnclaveOptions
properties:
  enabled:
    type: boolean
    description: |
      If set to `true`, Nitro Enclaves will be enabled on the instance.
    language:
      python:
        mapCase: false
type: object
