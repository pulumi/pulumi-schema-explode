$token: aws:ec2/LaunchConfigurationMetadataOptions:LaunchConfigurationMetadataOptions
properties:
  httpEndpoint:
    type: string
    description: |
      The state of the metadata service: `enabled`, `disabled`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  httpPutResponseHopLimit:
    type: integer
    description: |
      The desired HTTP PUT response hop limit for instance metadata requests.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  httpTokens:
    type: string
    description: |
      If session tokens are required: `optional`, `required`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
language:
  nodejs:
    requiredOutputs:
      - httpEndpoint
      - httpPutResponseHopLimit
      - httpTokens
