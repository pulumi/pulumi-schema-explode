$token: aws:ec2/LaunchTemplateCpuOptions:LaunchTemplateCpuOptions
properties:
  coreCount:
    type: integer
    description: |
      The number of CPU cores for the instance.
    language:
      python:
        mapCase: false
  threadsPerCore:
    type: integer
    description: |
      The number of threads per CPU core. To disable Intel Hyper-Threading Technology for the instance, specify a value of 1.
      Otherwise, specify the default value of 2.
    language:
      python:
        mapCase: false
type: object
