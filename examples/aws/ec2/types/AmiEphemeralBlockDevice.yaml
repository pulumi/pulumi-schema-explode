$token: aws:ec2/AmiEphemeralBlockDevice:AmiEphemeralBlockDevice
properties:
  deviceName:
    type: string
    description: |
      Path at which the device is exposed to created instances.
    language:
      python:
        mapCase: false
  virtualName:
    type: string
    description: |
      Name for the ephemeral device, of the form "ephemeralN" where
      *N* is a volume number starting from zero.
    language:
      python:
        mapCase: false
type: object
required:
  - deviceName
  - virtualName
