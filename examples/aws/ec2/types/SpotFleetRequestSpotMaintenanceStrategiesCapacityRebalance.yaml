$token: aws:ec2/SpotFleetRequestSpotMaintenanceStrategiesCapacityRebalance:SpotFleetRequestSpotMaintenanceStrategiesCapacityRebalance
properties:
  replacementStrategy:
    type: string
    description: |
      The replacement strategy to use. Only available for spot fleets with `fleet_type` set to `maintain`. Valid values: `launch`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
