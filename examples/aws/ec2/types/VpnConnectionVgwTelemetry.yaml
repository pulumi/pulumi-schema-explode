$token: aws:ec2/VpnConnectionVgwTelemetry:VpnConnectionVgwTelemetry
properties:
  acceptedRouteCount:
    type: integer
    description: |
      The number of accepted routes.
    language:
      python:
        mapCase: false
  certificateArn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the VPN tunnel endpoint certificate.
    language:
      python:
        mapCase: false
  lastStatusChange:
    type: string
    description: |
      The date and time of the last change in status.
    language:
      python:
        mapCase: false
  outsideIpAddress:
    type: string
    description: |
      The Internet-routable IP address of the virtual private gateway's outside interface.
    language:
      python:
        mapCase: false
  status:
    type: string
    description: |
      The status of the VPN tunnel.
    language:
      python:
        mapCase: false
  statusMessage:
    type: string
    description: |
      If an error occurs, a description of the error.
    language:
      python:
        mapCase: false
type: object
language:
  nodejs:
    requiredOutputs:
      - acceptedRouteCount
      - certificateArn
      - lastStatusChange
      - outsideIpAddress
      - status
      - statusMessage
