$token: aws:ec2/getPrefixList:getPrefixList
inputs:
  description: |
    A collection of arguments for invoking getPrefixList.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getPrefixListFilter:getPrefixListFilter"
      description: |
        Configuration block(s) for filtering. Detailed below.
    name:
      type: string
      description: |
        Name of the filter field. Valid values can be found in the [EC2 DescribePrefixLists API Reference](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_DescribePrefixLists.html).
    prefixListId:
      type: string
      description: |
        ID of the prefix list to select.
  type: object
outputs:
  description: |
    A collection of values returned by getPrefixList.
  properties:
    cidrBlocks:
      type: array
      items:
        type: string
      description: |
        List of CIDR blocks for the AWS service associated with the prefix list.
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getPrefixListFilter:getPrefixListFilter"
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    name:
      type: string
      description: |
        Name of the selected prefix list.
    prefixListId:
      type: string
  type: object
  required:
    - cidrBlocks
    - name
    - id
