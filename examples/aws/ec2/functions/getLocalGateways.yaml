$token: aws:ec2/getLocalGateways:getLocalGateways
inputs:
  description: |
    A collection of arguments for invoking getLocalGateways.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLocalGatewaysFilter:getLocalGatewaysFilter"
      description: |
        Custom filter block as described below.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Mapping of tags, each pair of which must exactly match
        a pair on the desired local_gateways.
  type: object
outputs:
  description: |
    A collection of values returned by getLocalGateways.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLocalGatewaysFilter:getLocalGatewaysFilter"
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    ids:
      type: array
      items:
        type: string
      description: |
        Set of all the Local Gateway identifiers
    tags:
      type: object
      additionalProperties:
        type: string
  type: object
  required:
    - ids
    - tags
    - id
