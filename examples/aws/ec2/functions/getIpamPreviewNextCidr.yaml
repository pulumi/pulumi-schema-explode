$token: aws:ec2/getIpamPreviewNextCidr:getIpamPreviewNextCidr
inputs:
  description: |
    A collection of arguments for invoking getIpamPreviewNextCidr.
  properties:
    disallowedCidrs:
      type: array
      items:
        type: string
      description: |
        Exclude a particular CIDR range from being returned by the pool.
    ipamPoolId:
      type: string
      description: |
        ID of the pool to which you want to assign a CIDR.
    netmaskLength:
      type: integer
      description: |
        Netmask length of the CIDR you would like to preview from the IPAM pool.
  type: object
  required:
    - ipamPoolId
outputs:
  description: |
    A collection of values returned by getIpamPreviewNextCidr.
  properties:
    cidr:
      type: string
      description: |
        Previewed CIDR from the pool.
    disallowedCidrs:
      type: array
      items:
        type: string
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    ipamPoolId:
      type: string
    netmaskLength:
      type: integer
  type: object
  required:
    - cidr
    - ipamPoolId
    - id
