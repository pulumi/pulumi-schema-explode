$token: aws:ec2/getSecurityGroups:getSecurityGroups
inputs:
  description: |
    A collection of arguments for invoking getSecurityGroups.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getSecurityGroupsFilter:getSecurityGroupsFilter"
      description: |
        One or more name/value pairs to use as filters. There are several valid keys, for a full reference, check out [describe-security-groups in the AWS CLI reference][1].
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags, each pair of which must exactly match for desired security groups.
  type: object
outputs:
  description: |
    A collection of values returned by getSecurityGroups.
  properties:
    arns:
      type: array
      items:
        type: string
      description: |
        ARNs of the matched security groups.
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getSecurityGroupsFilter:getSecurityGroupsFilter"
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    ids:
      type: array
      items:
        type: string
      description: |
        IDs of the matches security groups.
    tags:
      type: object
      additionalProperties:
        type: string
    vpcIds:
      type: array
      items:
        type: string
      description: |
        VPC IDs of the matched security groups. The data source's tag or filter *will span VPCs* unless the `vpc-id` filter is also used.
  type: object
  required:
    - arns
    - ids
    - tags
    - vpcIds
    - id
