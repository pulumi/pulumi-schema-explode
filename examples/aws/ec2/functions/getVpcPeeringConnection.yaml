$token: aws:ec2/getVpcPeeringConnection:getVpcPeeringConnection
inputs:
  description: |
    A collection of arguments for invoking getVpcPeeringConnection.
  properties:
    cidrBlock:
      type: string
      description: |
        Primary CIDR block of the requester VPC of the specific VPC Peering Connection to retrieve.
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getVpcPeeringConnectionFilter:getVpcPeeringConnectionFilter"
      description: |
        Custom filter block as described below.
    id:
      type: string
      description: |
        ID of the specific VPC Peering Connection to retrieve.
    ownerId:
      type: string
      description: |
        AWS account ID of the owner of the requester VPC of the specific VPC Peering Connection to retrieve.
    peerCidrBlock:
      type: string
      description: |
        Primary CIDR block of the accepter VPC of the specific VPC Peering Connection to retrieve.
    peerOwnerId:
      type: string
      description: |
        AWS account ID of the owner of the accepter VPC of the specific VPC Peering Connection to retrieve.
    peerRegion:
      type: string
      description: |
        Region of the accepter VPC of the specific VPC Peering Connection to retrieve.
    peerVpcId:
      type: string
      description: |
        ID of the accepter VPC of the specific VPC Peering Connection to retrieve.
    region:
      type: string
      description: |
        Region of the requester VPC of the specific VPC Peering Connection to retrieve.
    status:
      type: string
      description: |
        Status of the specific VPC Peering Connection to retrieve.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags, each pair of which must exactly match
        a pair on the desired VPC Peering Connection.
    vpcId:
      type: string
      description: |
        ID of the requester VPC of the specific VPC Peering Connection to retrieve.
  type: object
outputs:
  description: |
    A collection of values returned by getVpcPeeringConnection.
  properties:
    accepter:
      type: object
      additionalProperties:
        type: boolean
      description: |
        Configuration block that describes [VPC Peering Connection]
        (https://docs.aws.amazon.com/vpc/latest/peering/what-is-vpc-peering.html) options set for the accepter VPC.
    cidrBlock:
      type: string
      description: |
        CIDR block associated to the VPC of the specific VPC Peering Connection.
    cidrBlockSets:
      type: array
      items:
        $ref: "#/types/aws:ec2/getVpcPeeringConnectionCidrBlockSet:getVpcPeeringConnectionCidrBlockSet"
      description: |
        List of objects with CIDR blocks of the requester VPC.
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getVpcPeeringConnectionFilter:getVpcPeeringConnectionFilter"
    id:
      type: string
    ownerId:
      type: string
    peerCidrBlock:
      type: string
    peerCidrBlockSets:
      type: array
      items:
        $ref: "#/types/aws:ec2/getVpcPeeringConnectionPeerCidrBlockSet:getVpcPeeringConnectionPeerCidrBlockSet"
      description: |
        List of objects with CIDR blocks of the accepter VPC.
    peerOwnerId:
      type: string
    peerRegion:
      type: string
    peerVpcId:
      type: string
    region:
      type: string
    requester:
      type: object
      additionalProperties:
        type: boolean
      description: |
        Configuration block that describes [VPC Peering Connection]
        (https://docs.aws.amazon.com/vpc/latest/peering/what-is-vpc-peering.html) options set for the requester VPC.
    status:
      type: string
    tags:
      type: object
      additionalProperties:
        type: string
    vpcId:
      type: string
  type: object
  required:
    - accepter
    - cidrBlock
    - cidrBlockSets
    - id
    - ownerId
    - peerCidrBlock
    - peerCidrBlockSets
    - peerOwnerId
    - peerRegion
    - peerVpcId
    - region
    - requester
    - status
    - tags
    - vpcId
