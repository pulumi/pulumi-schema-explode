$token: aws:ec2/getNetworkAcls:getNetworkAcls
inputs:
  description: |
    A collection of arguments for invoking getNetworkAcls.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getNetworkAclsFilter:getNetworkAclsFilter"
      description: |
        Custom filter block as described below.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags, each pair of which must exactly match
        a pair on the desired network ACLs.
    vpcId:
      type: string
      description: |
        VPC ID that you want to filter from.
  type: object
outputs:
  description: |
    A collection of values returned by getNetworkAcls.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getNetworkAclsFilter:getNetworkAclsFilter"
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    ids:
      type: array
      items:
        type: string
      description: |
        List of all the network ACL ids found.
    tags:
      type: object
      additionalProperties:
        type: string
    vpcId:
      type: string
  type: object
  required:
    - ids
    - tags
    - id
