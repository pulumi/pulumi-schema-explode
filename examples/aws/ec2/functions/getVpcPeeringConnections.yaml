$token: aws:ec2/getVpcPeeringConnections:getVpcPeeringConnections
description: |
  Use this data source to get IDs of Amazon VPC peering connections
  To get more details on each connection, use the data resource `aws.ec2.VpcPeeringConnection`
inputs:
  description: |
    A collection of arguments for invoking getVpcPeeringConnections.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getVpcPeeringConnectionsFilter:getVpcPeeringConnectionsFilter"
      description: |
        Custom filter block as described below.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Mapping of tags, each pair of which must exactly match
        a pair on the desired VPC Peering Connection.
  type: object
outputs:
  description: |
    A collection of values returned by getVpcPeeringConnections.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getVpcPeeringConnectionsFilter:getVpcPeeringConnectionsFilter"
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    ids:
      type: array
      items:
        type: string
      description: |
        IDs of the VPC Peering Connections.
    tags:
      type: object
      additionalProperties:
        type: string
  type: object
  required:
    - ids
    - tags
    - id
