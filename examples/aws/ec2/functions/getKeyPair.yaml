$token: aws:ec2/getKeyPair:getKeyPair
inputs:
  description: |
    A collection of arguments for invoking getKeyPair.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getKeyPairFilter:getKeyPairFilter"
      description: |
        Custom filter block as described below.
    includePublicKey:
      type: boolean
      description: |
        Whether to include the public key material in the response.
    keyName:
      type: string
      description: |
        Key Pair name.
    keyPairId:
      type: string
      description: |
        Key Pair ID.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Any tags assigned to the Key Pair.
  type: object
outputs:
  description: |
    A collection of values returned by getKeyPair.
  properties:
    arn:
      type: string
      description: |
        ARN of the Key Pair.
    createTime:
      type: string
      description: |
        Timestamp for when the key pair was created in ISO 8601 format.
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getKeyPairFilter:getKeyPairFilter"
    fingerprint:
      type: string
      description: |
        SHA-1 digest of the DER encoded private key.
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    includePublicKey:
      type: boolean
    keyName:
      type: string
    keyPairId:
      type: string
    keyType:
      type: string
      description: |
        Type of key pair.
    publicKey:
      type: string
      description: |
        Public key material.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Any tags assigned to the Key Pair.
  type: object
  required:
    - arn
    - createTime
    - fingerprint
    - keyType
    - publicKey
    - tags
    - id
