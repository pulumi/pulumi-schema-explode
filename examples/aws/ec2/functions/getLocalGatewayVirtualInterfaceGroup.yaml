$token: aws:ec2/getLocalGatewayVirtualInterfaceGroup:getLocalGatewayVirtualInterfaceGroup
inputs:
  description: |
    A collection of arguments for invoking getLocalGatewayVirtualInterfaceGroup.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLocalGatewayVirtualInterfaceGroupFilter:getLocalGatewayVirtualInterfaceGroupFilter"
      description: |
        One or more configuration blocks containing name-values filters. See the [EC2 API Reference](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_DescribeLocalGatewayVirtualInterfaceGroups.html) for supported filters. Detailed below.
    id:
      type: string
      description: |
        Identifier of EC2 Local Gateway Virtual Interface Group.
    localGatewayId:
      type: string
      description: |
        Identifier of EC2 Local Gateway.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags, each pair of which must exactly match a pair on the desired local gateway route table.
  type: object
outputs:
  description: |
    A collection of values returned by getLocalGatewayVirtualInterfaceGroup.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLocalGatewayVirtualInterfaceGroupFilter:getLocalGatewayVirtualInterfaceGroupFilter"
    id:
      type: string
    localGatewayId:
      type: string
    localGatewayVirtualInterfaceIds:
      type: array
      items:
        type: string
      description: |
        Set of EC2 Local Gateway Virtual Interface identifiers.
    tags:
      type: object
      additionalProperties:
        type: string
  type: object
  required:
    - id
    - localGatewayId
    - localGatewayVirtualInterfaceIds
    - tags
