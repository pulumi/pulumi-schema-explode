$token: aws:ec2/getNetworkInsightsPath:getNetworkInsightsPath
inputs:
  description: |
    A collection of arguments for invoking getNetworkInsightsPath.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getNetworkInsightsPathFilter:getNetworkInsightsPathFilter"
      description: |
        Configuration block(s) for filtering. Detailed below.
    networkInsightsPathId:
      type: string
      description: |
        ID of the Network Insights Path to select.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags assigned to the resource.
  type: object
outputs:
  description: |
    A collection of values returned by getNetworkInsightsPath.
  properties:
    arn:
      type: string
      description: |
        ARN of the selected Network Insights Path.
    destination:
      type: string
      description: |
        AWS resource that is the destination of the path.
    destinationIp:
      type: string
      description: |
        IP address of the AWS resource that is the destination of the path.
    destinationPort:
      type: integer
      description: |
        Destination port.
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getNetworkInsightsPathFilter:getNetworkInsightsPathFilter"
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    networkInsightsPathId:
      type: string
    protocol:
      type: string
      description: |
        Protocol.
    source:
      type: string
      description: |
        AWS resource that is the source of the path.
    sourceIp:
      type: string
      description: |
        IP address of the AWS resource that is the source of the path.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags assigned to the resource.
  type: object
  required:
    - arn
    - destination
    - destinationIp
    - destinationPort
    - networkInsightsPathId
    - protocol
    - source
    - sourceIp
    - tags
    - id
