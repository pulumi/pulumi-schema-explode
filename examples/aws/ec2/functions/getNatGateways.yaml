$token: aws:ec2/getNatGateways:getNatGateways
description: |
  This resource can be useful for getting back a list of NAT gateway ids to be referenced elsewhere.
inputs:
  description: |
    A collection of arguments for invoking getNatGateways.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getNatGatewaysFilter:getNatGatewaysFilter"
      description: |
        Custom filter block as described below.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags, each pair of which must exactly match
        a pair on the desired NAT Gateways.
    vpcId:
      type: string
      description: |
        VPC ID that you want to filter from.
  type: object
outputs:
  description: |
    A collection of values returned by getNatGateways.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getNatGatewaysFilter:getNatGatewaysFilter"
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    ids:
      type: array
      items:
        type: string
      description: |
        List of all the NAT gateway ids found.
    tags:
      type: object
      additionalProperties:
        type: string
    vpcId:
      type: string
  type: object
  required:
    - ids
    - tags
    - id
