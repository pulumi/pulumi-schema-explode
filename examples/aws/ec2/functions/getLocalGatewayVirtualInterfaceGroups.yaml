$token: aws:ec2/getLocalGatewayVirtualInterfaceGroups:getLocalGatewayVirtualInterfaceGroups
inputs:
  description: |
    A collection of arguments for invoking getLocalGatewayVirtualInterfaceGroups.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLocalGatewayVirtualInterfaceGroupsFilter:getLocalGatewayVirtualInterfaceGroupsFilter"
      description: |
        One or more configuration blocks containing name-values filters. See the [EC2 API Reference](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_DescribeLocalGatewayVirtualInterfaceGroups.html) for supported filters. Detailed below.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value map of resource tags, each pair of which must exactly match a pair on the desired local gateway route table.
  type: object
outputs:
  description: |
    A collection of values returned by getLocalGatewayVirtualInterfaceGroups.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLocalGatewayVirtualInterfaceGroupsFilter:getLocalGatewayVirtualInterfaceGroupsFilter"
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    ids:
      type: array
      items:
        type: string
      description: |
        Set of EC2 Local Gateway Virtual Interface Group identifiers.
    localGatewayVirtualInterfaceIds:
      type: array
      items:
        type: string
      description: |
        Set of EC2 Local Gateway Virtual Interface identifiers.
    tags:
      type: object
      additionalProperties:
        type: string
  type: object
  required:
    - ids
    - localGatewayVirtualInterfaceIds
    - tags
    - id
