$token: aws:ec2/getTransitGatewayRouteTables:getTransitGatewayRouteTables
inputs:
  description: |
    A collection of arguments for invoking getTransitGatewayRouteTables.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getTransitGatewayRouteTablesFilter:getTransitGatewayRouteTablesFilter"
      description: |
        Custom filter block as described below.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Mapping of tags, each pair of which must exactly match
        a pair on the desired transit gateway route table.
  type: object
outputs:
  description: |
    A collection of values returned by getTransitGatewayRouteTables.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getTransitGatewayRouteTablesFilter:getTransitGatewayRouteTablesFilter"
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    ids:
      type: array
      items:
        type: string
      description: |
        Set of Transit Gateway Route Table identifiers.
    tags:
      type: object
      additionalProperties:
        type: string
  type: object
  required:
    - ids
    - tags
    - id
