$token: aws:ec2/getLaunchTemplate:getLaunchTemplate
inputs:
  description: |
    A collection of arguments for invoking getLaunchTemplate.
  properties:
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLaunchTemplateFilter:getLaunchTemplateFilter"
      description: |
        Configuration block(s) for filtering. Detailed below.
    id:
      type: string
      description: |
        ID of the specific launch template to retrieve.
    name:
      type: string
      description: |
        Name of the filter field. Valid values can be found in the [EC2 DescribeLaunchTemplates API Reference](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_DescribeLaunchTemplates.html).
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags, each pair of which must exactly match a pair on the desired Launch Template.
  type: object
outputs:
  description: |
    A collection of values returned by getLaunchTemplate.
  properties:
    arn:
      type: string
    blockDeviceMappings:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLaunchTemplateBlockDeviceMapping:getLaunchTemplateBlockDeviceMapping"
    capacityReservationSpecifications:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLaunchTemplateCapacityReservationSpecification:getLaunchTemplateCapacityReservationSpecification"
    cpuOptions:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLaunchTemplateCpuOption:getLaunchTemplateCpuOption"
    creditSpecifications:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLaunchTemplateCreditSpecification:getLaunchTemplateCreditSpecification"
    defaultVersion:
      type: integer
    description:
      type: string
    disableApiStop:
      type: boolean
    disableApiTermination:
      type: boolean
    ebsOptimized:
      type: string
    elasticGpuSpecifications:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLaunchTemplateElasticGpuSpecification:getLaunchTemplateElasticGpuSpecification"
    elasticInferenceAccelerators:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLaunchTemplateElasticInferenceAccelerator:getLaunchTemplateElasticInferenceAccelerator"
    enclaveOptions:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLaunchTemplateEnclaveOption:getLaunchTemplateEnclaveOption"
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLaunchTemplateFilter:getLaunchTemplateFilter"
    hibernationOptions:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLaunchTemplateHibernationOption:getLaunchTemplateHibernationOption"
    iamInstanceProfiles:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLaunchTemplateIamInstanceProfile:getLaunchTemplateIamInstanceProfile"
    id:
      type: string
      description: |
        ID of the launch template.
    imageId:
      type: string
    instanceInitiatedShutdownBehavior:
      type: string
    instanceMarketOptions:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLaunchTemplateInstanceMarketOption:getLaunchTemplateInstanceMarketOption"
    instanceRequirements:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLaunchTemplateInstanceRequirement:getLaunchTemplateInstanceRequirement"
    instanceType:
      type: string
    kernelId:
      type: string
    keyName:
      type: string
    latestVersion:
      type: integer
    licenseSpecifications:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLaunchTemplateLicenseSpecification:getLaunchTemplateLicenseSpecification"
    maintenanceOptions:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLaunchTemplateMaintenanceOption:getLaunchTemplateMaintenanceOption"
    metadataOptions:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLaunchTemplateMetadataOption:getLaunchTemplateMetadataOption"
    monitorings:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLaunchTemplateMonitoring:getLaunchTemplateMonitoring"
    name:
      type: string
    networkInterfaces:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLaunchTemplateNetworkInterface:getLaunchTemplateNetworkInterface"
    placements:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLaunchTemplatePlacement:getLaunchTemplatePlacement"
    privateDnsNameOptions:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLaunchTemplatePrivateDnsNameOption:getLaunchTemplatePrivateDnsNameOption"
    ramDiskId:
      type: string
    securityGroupNames:
      type: array
      items:
        type: string
    tagSpecifications:
      type: array
      items:
        $ref: "#/types/aws:ec2/getLaunchTemplateTagSpecification:getLaunchTemplateTagSpecification"
    tags:
      type: object
      additionalProperties:
        type: string
    userData:
      type: string
    vpcSecurityGroupIds:
      type: array
      items:
        type: string
  type: object
  required:
    - arn
    - blockDeviceMappings
    - capacityReservationSpecifications
    - cpuOptions
    - creditSpecifications
    - defaultVersion
    - description
    - disableApiStop
    - disableApiTermination
    - ebsOptimized
    - elasticGpuSpecifications
    - elasticInferenceAccelerators
    - enclaveOptions
    - hibernationOptions
    - iamInstanceProfiles
    - id
    - imageId
    - instanceInitiatedShutdownBehavior
    - instanceMarketOptions
    - instanceRequirements
    - instanceType
    - kernelId
    - keyName
    - latestVersion
    - licenseSpecifications
    - maintenanceOptions
    - metadataOptions
    - monitorings
    - name
    - networkInterfaces
    - placements
    - privateDnsNameOptions
    - ramDiskId
    - securityGroupNames
    - tagSpecifications
    - tags
    - userData
    - vpcSecurityGroupIds
