$token: aws:ec2/getVpcDhcpOptions:getVpcDhcpOptions
inputs:
  description: |
    A collection of arguments for invoking getVpcDhcpOptions.
  properties:
    dhcpOptionsId:
      type: string
      description: |
        EC2 DHCP Options ID.
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getVpcDhcpOptionsFilter:getVpcDhcpOptionsFilter"
      description: |
        List of custom filters as described below.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags assigned to the resource.
  type: object
outputs:
  description: |
    A collection of values returned by getVpcDhcpOptions.
  properties:
    arn:
      type: string
      description: |
        ARN of the DHCP Options Set.
    dhcpOptionsId:
      type: string
      description: |
        EC2 DHCP Options ID
    domainName:
      type: string
      description: |
        Suffix domain name to used when resolving non Fully Qualified Domain NamesE.g., the `search` value in the `/etc/resolv.conf` file.
    domainNameServers:
      type: array
      items:
        type: string
      description: |
        List of name servers.
    filters:
      type: array
      items:
        $ref: "#/types/aws:ec2/getVpcDhcpOptionsFilter:getVpcDhcpOptionsFilter"
    id:
      type: string
      description: |
        The provider-assigned unique ID for this managed resource.
    netbiosNameServers:
      type: array
      items:
        type: string
      description: |
        List of NETBIOS name servers.
    netbiosNodeType:
      type: string
      description: |
        NetBIOS node type (1, 2, 4, or 8). For more information about these node types, see [RFC 2132](http://www.ietf.org/rfc/rfc2132.txt).
    ntpServers:
      type: array
      items:
        type: string
      description: |
        List of NTP servers.
    ownerId:
      type: string
      description: |
        ID of the AWS account that owns the DHCP options set.
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Map of tags assigned to the resource.
  type: object
  required:
    - arn
    - dhcpOptionsId
    - domainName
    - domainNameServers
    - netbiosNameServers
    - netbiosNodeType
    - ntpServers
    - ownerId
    - tags
    - id
