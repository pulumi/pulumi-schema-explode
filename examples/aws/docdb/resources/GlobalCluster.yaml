$token: aws:docdb/globalCluster:GlobalCluster
properties:
  arn:
    type: string
    description: |
      Global Cluster Amazon Resource Name (ARN)
  databaseName:
    type: string
    description: |
      Name for an automatically created database on cluster creation.
  deletionProtection:
    type: boolean
    description: |
      If the Global Cluster should have deletion protection enabled. The database can't be deleted when this value is set to `true`. The default is `false`.
  engine:
    type: string
  engineVersion:
    type: string
    description: |
      Engine version of the global database. Upgrading the engine version will result in all cluster members being immediately updated and will.
      * **NOTE:** Upgrading major versions is not supported.
  globalClusterIdentifier:
    type: string
    description: |
      The global cluster identifier.
  globalClusterMembers:
    type: array
    items:
      $ref: "#/types/aws:docdb/GlobalClusterGlobalClusterMember:GlobalClusterGlobalClusterMember"
    description: |
      Set of objects containing Global Cluster members.
  globalClusterResourceId:
    type: string
    description: |
      AWS Region-unique, immutable identifier for the global database cluster. This identifier is found in AWS CloudTrail log entries whenever the AWS KMS key for the DB cluster is accessed.
  sourceDbClusterIdentifier:
    type: string
  status:
    type: string
  storageEncrypted:
    type: boolean
required:
  - arn
  - engine
  - engineVersion
  - globalClusterIdentifier
  - globalClusterMembers
  - globalClusterResourceId
  - sourceDbClusterIdentifier
  - status
  - storageEncrypted
inputProperties:
  databaseName:
    type: string
    description: |
      Name for an automatically created database on cluster creation.
    willReplaceOnChanges: true
  deletionProtection:
    type: boolean
    description: |
      If the Global Cluster should have deletion protection enabled. The database can't be deleted when this value is set to `true`. The default is `false`.
  engine:
    type: string
    willReplaceOnChanges: true
  engineVersion:
    type: string
    description: |
      Engine version of the global database. Upgrading the engine version will result in all cluster members being immediately updated and will.
      * **NOTE:** Upgrading major versions is not supported.
  globalClusterIdentifier:
    type: string
    description: |
      The global cluster identifier.
    willReplaceOnChanges: true
  sourceDbClusterIdentifier:
    type: string
    willReplaceOnChanges: true
  storageEncrypted:
    type: boolean
    willReplaceOnChanges: true
requiredInputs:
  - globalClusterIdentifier
stateInputs:
  description: |
    Input properties used for looking up and filtering GlobalCluster resources.
  properties:
    arn:
      type: string
      description: |
        Global Cluster Amazon Resource Name (ARN)
    databaseName:
      type: string
      description: |
        Name for an automatically created database on cluster creation.
      willReplaceOnChanges: true
    deletionProtection:
      type: boolean
      description: |
        If the Global Cluster should have deletion protection enabled. The database can't be deleted when this value is set to `true`. The default is `false`.
    engine:
      type: string
      willReplaceOnChanges: true
    engineVersion:
      type: string
      description: |
        Engine version of the global database. Upgrading the engine version will result in all cluster members being immediately updated and will.
        * **NOTE:** Upgrading major versions is not supported.
    globalClusterIdentifier:
      type: string
      description: |
        The global cluster identifier.
      willReplaceOnChanges: true
    globalClusterMembers:
      type: array
      items:
        $ref: "#/types/aws:docdb/GlobalClusterGlobalClusterMember:GlobalClusterGlobalClusterMember"
      description: |
        Set of objects containing Global Cluster members.
    globalClusterResourceId:
      type: string
      description: |
        AWS Region-unique, immutable identifier for the global database cluster. This identifier is found in AWS CloudTrail log entries whenever the AWS KMS key for the DB cluster is accessed.
    sourceDbClusterIdentifier:
      type: string
      willReplaceOnChanges: true
    status:
      type: string
    storageEncrypted:
      type: boolean
      willReplaceOnChanges: true
  type: object
