$token: aws:docdb/ClusterParameterGroupParameter:ClusterParameterGroupParameter
properties:
  applyMethod:
    type: string
    description: |
      Valid values are `immediate` and `pending-reboot`. Defaults to `pending-reboot`.
    language:
      python:
        mapCase: false
  name:
    type: string
    description: |
      The name of the documentDB parameter.
    language:
      python:
        mapCase: false
  value:
    type: string
    description: |
      The value of the documentDB parameter.
    language:
      python:
        mapCase: false
type: object
required:
  - name
  - value
