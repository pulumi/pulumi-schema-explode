$token: aws:route53/ZoneVpc:ZoneVpc
properties:
  vpcId:
    type: string
    description: |
      ID of the VPC to associate.
    language:
      python:
        mapCase: false
  vpcRegion:
    type: string
    description: |
      Region of the VPC to associate. Defaults to AWS provider region.
    language:
      python:
        mapCase: false
type: object
required:
  - vpcId
language:
  nodejs:
    requiredOutputs:
      - vpcId
      - vpcRegion
