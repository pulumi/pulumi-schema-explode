$token: aws:route53/getTrafficPolicyDocumentRuleGeoProximityLocation:getTrafficPolicyDocumentRuleGeoProximityLocation
properties:
  bias:
    type: string
    description: |
      Specify a value for `bias` if you want to route more traffic to an endpoint from nearby endpoints (positive values) or route less traffic to an endpoint (negative values).
    language:
      python:
        mapCase: false
  endpointReference:
    type: string
    description: |
      References to an endpoint.
    language:
      python:
        mapCase: false
  evaluateTargetHealth:
    type: boolean
    description: |
      Indicates whether you want Amazon Route 53 to evaluate the health of the endpoint and route traffic only to healthy endpoints.
    language:
      python:
        mapCase: false
  healthCheck:
    type: string
    description: |
      If you want to associate a health check with the endpoint or rule.
    language:
      python:
        mapCase: false
  latitude:
    type: string
    description: |
      Represents the location south (negative) or north (positive) of the equator. Valid values are -90 degrees to 90 degrees.
    language:
      python:
        mapCase: false
  longitude:
    type: string
    description: |
      Represents the location west (negative) or east (positive) of the prime meridian. Valid values are -180 degrees to 180 degrees.
    language:
      python:
        mapCase: false
  region:
    type: string
    description: |
      Region code for the AWS Region that you created the resource in.
    language:
      python:
        mapCase: false
  ruleReference:
    type: string
    description: |
      References to a rule.
    language:
      python:
        mapCase: false
type: object
