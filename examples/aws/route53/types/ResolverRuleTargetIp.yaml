$token: aws:route53/ResolverRuleTargetIp:ResolverRuleTargetIp
properties:
  ip:
    type: string
    description: |
      One IP address that you want to forward DNS queries to. You can specify only IPv4 addresses.
    language:
      python:
        mapCase: false
  port:
    type: integer
    description: |
      The port at `ip` that you want to forward DNS queries to. Default value is `53`
    language:
      python:
        mapCase: false
type: object
required:
  - ip
