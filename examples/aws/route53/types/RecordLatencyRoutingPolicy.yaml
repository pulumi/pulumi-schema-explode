$token: aws:route53/RecordLatencyRoutingPolicy:RecordLatencyRoutingPolicy
properties:
  region:
    type: string
    description: |
      An AWS region from which to measure latency. See http://docs.aws.amazon.com/Route53/latest/DeveloperGuide/routing-policy.html#routing-policy-latency
    language:
      python:
        mapCase: false
type: object
required:
  - region
