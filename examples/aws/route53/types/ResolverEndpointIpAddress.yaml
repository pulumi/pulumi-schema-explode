$token: aws:route53/ResolverEndpointIpAddress:ResolverEndpointIpAddress
properties:
  ip:
    type: string
    description: |
      The IP address in the subnet that you want to use for DNS queries.
    language:
      python:
        mapCase: false
  ipId:
    type: string
    language:
      python:
        mapCase: false
  subnetId:
    type: string
    description: |
      The ID of the subnet that contains the IP address.
    language:
      python:
        mapCase: false
type: object
required:
  - subnetId
language:
  nodejs:
    requiredOutputs:
      - ip
      - ipId
      - subnetId
