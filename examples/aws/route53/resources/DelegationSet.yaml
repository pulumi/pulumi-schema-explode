$token: aws:route53/delegationSet:DelegationSet
properties:
  arn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the Delegation Set.
  nameServers:
    type: array
    items:
      type: string
    description: |
      A list of authoritative name servers for the hosted zone
      (effectively a list of NS records).
  referenceName:
    type: string
    description: |
      This is a reference name used in Caller Reference
      (helpful for identifying single delegation set amongst others)
required:
  - arn
  - nameServers
inputProperties:
  referenceName:
    type: string
    description: |
      This is a reference name used in Caller Reference
      (helpful for identifying single delegation set amongst others)
    willReplaceOnChanges: true
stateInputs:
  description: |
    Input properties used for looking up and filtering DelegationSet resources.
  properties:
    arn:
      type: string
      description: |
        The Amazon Resource Name (ARN) of the Delegation Set.
    nameServers:
      type: array
      items:
        type: string
      description: |
        A list of authoritative name servers for the hosted zone
        (effectively a list of NS records).
    referenceName:
      type: string
      description: |
        This is a reference name used in Caller Reference
        (helpful for identifying single delegation set amongst others)
      willReplaceOnChanges: true
  type: object
