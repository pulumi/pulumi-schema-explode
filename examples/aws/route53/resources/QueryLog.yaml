$token: aws:route53/queryLog:QueryLog
properties:
  arn:
    type: string
    description: |
      The Amazon Resource Name (ARN) of the Query Logging Config.
  cloudwatchLogGroupArn:
    type: string
    description: |
      CloudWatch log group ARN to send query logs.
  zoneId:
    type: string
    description: |
      Route53 hosted zone ID to enable query logs.
required:
  - arn
  - cloudwatchLogGroupArn
  - zoneId
inputProperties:
  cloudwatchLogGroupArn:
    type: string
    description: |
      CloudWatch log group ARN to send query logs.
    willReplaceOnChanges: true
  zoneId:
    type: string
    description: |
      Route53 hosted zone ID to enable query logs.
    willReplaceOnChanges: true
requiredInputs:
  - cloudwatchLogGroupArn
  - zoneId
stateInputs:
  description: |
    Input properties used for looking up and filtering QueryLog resources.
  properties:
    arn:
      type: string
      description: |
        The Amazon Resource Name (ARN) of the Query Logging Config.
    cloudwatchLogGroupArn:
      type: string
      description: |
        CloudWatch log group ARN to send query logs.
      willReplaceOnChanges: true
    zoneId:
      type: string
      description: |
        Route53 hosted zone ID to enable query logs.
      willReplaceOnChanges: true
  type: object
