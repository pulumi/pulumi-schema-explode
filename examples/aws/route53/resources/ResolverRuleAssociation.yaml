$token: aws:route53/resolverRuleAssociation:ResolverRuleAssociation
properties:
  name:
    type: string
    description: |
      A name for the association that you're creating between a resolver rule and a VPC.
  resolverRuleId:
    type: string
    description: |
      The ID of the resolver rule that you want to associate with the VPC.
  vpcId:
    type: string
    description: |
      The ID of the VPC that you want to associate the resolver rule with.
required:
  - name
  - resolverRuleId
  - vpcId
inputProperties:
  name:
    type: string
    description: |
      A name for the association that you're creating between a resolver rule and a VPC.
    willReplaceOnChanges: true
  resolverRuleId:
    type: string
    description: |
      The ID of the resolver rule that you want to associate with the VPC.
    willReplaceOnChanges: true
  vpcId:
    type: string
    description: |
      The ID of the VPC that you want to associate the resolver rule with.
    willReplaceOnChanges: true
requiredInputs:
  - resolverRuleId
  - vpcId
stateInputs:
  description: |
    Input properties used for looking up and filtering ResolverRuleAssociation resources.
  properties:
    name:
      type: string
      description: |
        A name for the association that you're creating between a resolver rule and a VPC.
      willReplaceOnChanges: true
    resolverRuleId:
      type: string
      description: |
        The ID of the resolver rule that you want to associate with the VPC.
      willReplaceOnChanges: true
    vpcId:
      type: string
      description: |
        The ID of the VPC that you want to associate the resolver rule with.
      willReplaceOnChanges: true
  type: object
