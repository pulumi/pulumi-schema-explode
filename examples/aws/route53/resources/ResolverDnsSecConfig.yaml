$token: aws:route53/resolverDnsSecConfig:ResolverDnsSecConfig
properties:
  arn:
    type: string
    description: |
      The ARN for a configuration for DNSSEC validation.
  ownerId:
    type: string
    description: |
      The owner account ID of the virtual private cloud (VPC) for a configuration for DNSSEC validation.
  resourceId:
    type: string
    description: |
      The ID of the virtual private cloud (VPC) that you're updating the DNSSEC validation status for.
  validationStatus:
    type: string
    description: |
      The validation status for a DNSSEC configuration. The status can be one of the following: `ENABLING`, `ENABLED`, `DISABLING` and `DISABLED`.
required:
  - arn
  - ownerId
  - resourceId
  - validationStatus
inputProperties:
  resourceId:
    type: string
    description: |
      The ID of the virtual private cloud (VPC) that you're updating the DNSSEC validation status for.
    willReplaceOnChanges: true
requiredInputs:
  - resourceId
stateInputs:
  description: |
    Input properties used for looking up and filtering ResolverDnsSecConfig resources.
  properties:
    arn:
      type: string
      description: |
        The ARN for a configuration for DNSSEC validation.
    ownerId:
      type: string
      description: |
        The owner account ID of the virtual private cloud (VPC) for a configuration for DNSSEC validation.
    resourceId:
      type: string
      description: |
        The ID of the virtual private cloud (VPC) that you're updating the DNSSEC validation status for.
      willReplaceOnChanges: true
    validationStatus:
      type: string
      description: |
        The validation status for a DNSSEC configuration. The status can be one of the following: `ENABLING`, `ENABLED`, `DISABLING` and `DISABLED`.
  type: object
