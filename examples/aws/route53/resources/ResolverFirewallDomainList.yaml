$token: aws:route53/resolverFirewallDomainList:ResolverFirewallDomainList
properties:
  arn:
    type: string
    description: |
      The ARN (Amazon Resource Name) of the domain list.
  domains:
    type: array
    items:
      type: string
    description: |
      A array of domains for the firewall domain list.
  name:
    type: string
    description: |
      A name that lets you identify the domain list, to manage and use it.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
required:
  - arn
  - name
  - tagsAll
inputProperties:
  domains:
    type: array
    items:
      type: string
    description: |
      A array of domains for the firewall domain list.
  name:
    type: string
    description: |
      A name that lets you identify the domain list, to manage and use it.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
stateInputs:
  description: |
    Input properties used for looking up and filtering ResolverFirewallDomainList resources.
  properties:
    arn:
      type: string
      description: |
        The ARN (Amazon Resource Name) of the domain list.
    domains:
      type: array
      items:
        type: string
      description: |
        A array of domains for the firewall domain list.
    name:
      type: string
      description: |
        A name that lets you identify the domain list, to manage and use it.
      willReplaceOnChanges: true
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags to assign to the resource. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type: object
