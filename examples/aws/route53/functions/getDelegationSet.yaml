$token: aws:route53/getDelegationSet:getDelegationSet
inputs:
  description: |
    A collection of arguments for invoking getDelegationSet.
  properties:
    id:
      type: string
      description: |
        Hosted Zone id of the desired delegation set.
  type: object
  required:
    - id
outputs:
  description: |
    A collection of values returned by getDelegationSet.
  properties:
    arn:
      type: string
    callerReference:
      type: string
    id:
      type: string
    nameServers:
      type: array
      items:
        type: string
  type: object
  required:
    - arn
    - callerReference
    - id
    - nameServers
