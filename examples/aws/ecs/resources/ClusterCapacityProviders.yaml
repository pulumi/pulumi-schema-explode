$token: aws:ecs/clusterCapacityProviders:ClusterCapacityProviders
properties:
  capacityProviders:
    type: array
    items:
      type: string
    description: |
      Set of names of one or more capacity providers to associate with the cluster. Valid values also include `FARGATE` and `FARGATE_SPOT`.
  clusterName:
    type: string
    description: |
      Name of the ECS cluster to manage capacity providers for.
  defaultCapacityProviderStrategies:
    type: array
    items:
      $ref: "#/types/aws:ecs/ClusterCapacityProvidersDefaultCapacityProviderStrategy:ClusterCapacityProvidersDefaultCapacityProviderStrategy"
    description: |
      Set of capacity provider strategies to use by default for the cluster. Detailed below.
required:
  - clusterName
inputProperties:
  capacityProviders:
    type: array
    items:
      type: string
    description: |
      Set of names of one or more capacity providers to associate with the cluster. Valid values also include `FARGATE` and `FARGATE_SPOT`.
  clusterName:
    type: string
    description: |
      Name of the ECS cluster to manage capacity providers for.
    willReplaceOnChanges: true
  defaultCapacityProviderStrategies:
    type: array
    items:
      $ref: "#/types/aws:ecs/ClusterCapacityProvidersDefaultCapacityProviderStrategy:ClusterCapacityProvidersDefaultCapacityProviderStrategy"
    description: |
      Set of capacity provider strategies to use by default for the cluster. Detailed below.
requiredInputs:
  - clusterName
stateInputs:
  description: |
    Input properties used for looking up and filtering ClusterCapacityProviders resources.
  properties:
    capacityProviders:
      type: array
      items:
        type: string
      description: |
        Set of names of one or more capacity providers to associate with the cluster. Valid values also include `FARGATE` and `FARGATE_SPOT`.
    clusterName:
      type: string
      description: |
        Name of the ECS cluster to manage capacity providers for.
      willReplaceOnChanges: true
    defaultCapacityProviderStrategies:
      type: array
      items:
        $ref: "#/types/aws:ecs/ClusterCapacityProvidersDefaultCapacityProviderStrategy:ClusterCapacityProvidersDefaultCapacityProviderStrategy"
      description: |
        Set of capacity provider strategies to use by default for the cluster. Detailed below.
  type: object
