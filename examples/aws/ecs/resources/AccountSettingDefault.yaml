$token: aws:ecs/accountSettingDefault:AccountSettingDefault
properties:
  name:
    type: string
    description: |
      Name of the account setting to set. Valid values are `serviceLongArnFormat`, `taskLongArnFormat`, `containerInstanceLongArnFormat`, `awsvpcTrunking` and `containerInsights`.
  principalArn:
    type: string
  value:
    type: string
    description: |
      State of the setting. Valid values are `enabled` and `disabled`.
required:
  - name
  - principalArn
  - value
inputProperties:
  name:
    type: string
    description: |
      Name of the account setting to set. Valid values are `serviceLongArnFormat`, `taskLongArnFormat`, `containerInstanceLongArnFormat`, `awsvpcTrunking` and `containerInsights`.
    willReplaceOnChanges: true
  value:
    type: string
    description: |
      State of the setting. Valid values are `enabled` and `disabled`.
requiredInputs:
  - value
stateInputs:
  description: |
    Input properties used for looking up and filtering AccountSettingDefault resources.
  properties:
    name:
      type: string
      description: |
        Name of the account setting to set. Valid values are `serviceLongArnFormat`, `taskLongArnFormat`, `containerInstanceLongArnFormat`, `awsvpcTrunking` and `containerInsights`.
      willReplaceOnChanges: true
    principalArn:
      type: string
    value:
      type: string
      description: |
        State of the setting. Valid values are `enabled` and `disabled`.
  type: object
