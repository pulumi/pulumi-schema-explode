$token: aws:ecs/TaskDefinitionVolumeEfsVolumeConfiguration:TaskDefinitionVolumeEfsVolumeConfiguration
properties:
  authorizationConfig:
    $ref: "#/types/aws:ecs/TaskDefinitionVolumeEfsVolumeConfigurationAuthorizationConfig:TaskDefinitionVolumeEfsVolumeConfigurationAuthorizationConfig"
    description: |
      Configuration block for authorization for the Amazon FSx for Windows File Server file system detailed below.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  fileSystemId:
    type: string
    description: |
      The Amazon FSx for Windows File Server file system ID to use.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  rootDirectory:
    type: string
    description: |
      The directory within the Amazon FSx for Windows File Server file system to mount as the root directory inside the host.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  transitEncryption:
    type: string
    description: |
      Whether or not to enable encryption for Amazon EFS data in transit between the Amazon ECS host and the Amazon EFS server. Transit encryption must be enabled if Amazon EFS IAM authorization is used. Valid values: `ENABLED`, `DISABLED`. If this parameter is omitted, the default value of `DISABLED` is used.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  transitEncryptionPort:
    type: integer
    description: |
      Port to use for transit encryption. If you do not specify a transit encryption port, it will use the port selection strategy that the Amazon EFS mount helper uses.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - fileSystemId
