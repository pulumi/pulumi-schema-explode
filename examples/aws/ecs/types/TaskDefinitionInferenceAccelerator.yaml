$token: aws:ecs/TaskDefinitionInferenceAccelerator:TaskDefinitionInferenceAccelerator
properties:
  deviceName:
    type: string
    description: |
      Elastic Inference accelerator device name. The deviceName must also be referenced in a container definition as a ResourceRequirement.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  deviceType:
    type: string
    description: |
      Elastic Inference accelerator type to use.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - deviceName
  - deviceType
