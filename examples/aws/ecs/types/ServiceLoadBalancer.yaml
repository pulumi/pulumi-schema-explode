$token: aws:ecs/ServiceLoadBalancer:ServiceLoadBalancer
properties:
  containerName:
    type: string
    description: |
      Name of the container to associate with the load balancer (as it appears in a container definition).
    language:
      python:
        mapCase: false
  containerPort:
    type: integer
    description: |
      Port on the container to associate with the load balancer.
    language:
      python:
        mapCase: false
  elbName:
    type: string
    description: |
      Name of the ELB (Classic) to associate with the service.
    language:
      python:
        mapCase: false
  targetGroupArn:
    type: string
    description: |
      ARN of the Load Balancer target group to associate with the service.
    language:
      python:
        mapCase: false
type: object
required:
  - containerName
  - containerPort
