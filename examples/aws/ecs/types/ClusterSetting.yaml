$token: aws:ecs/ClusterSetting:ClusterSetting
properties:
  name:
    type: string
    description: |
      Name of the setting to manage. Valid values: `containerInsights`.
    language:
      python:
        mapCase: false
  value:
    type: string
    description: |
      The value to assign to the setting. Valid values are `enabled` and `disabled`.
    language:
      python:
        mapCase: false
type: object
required:
  - name
  - value
