$token: aws:ecs/ClusterConfigurationExecuteCommandConfigurationLogConfiguration:ClusterConfigurationExecuteCommandConfigurationLogConfiguration
properties:
  cloudWatchEncryptionEnabled:
    type: boolean
    description: |
      Whether or not to enable encryption on the CloudWatch logs. If not specified, encryption will be disabled.
    language:
      python:
        mapCase: false
  cloudWatchLogGroupName:
    type: string
    description: |
      The name of the CloudWatch log group to send logs to.
    language:
      python:
        mapCase: false
  s3BucketEncryptionEnabled:
    type: boolean
    description: |
      Whether or not to enable encryption on the logs sent to S3. If not specified, encryption will be disabled.
    language:
      python:
        mapCase: false
  s3BucketName:
    type: string
    description: |
      The name of the S3 bucket to send logs to.
    language:
      python:
        mapCase: false
  s3KeyPrefix:
    type: string
    description: |
      An optional folder in the S3 bucket to place logs in.
    language:
      python:
        mapCase: false
type: object
