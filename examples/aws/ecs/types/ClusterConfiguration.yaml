$token: aws:ecs/ClusterConfiguration:ClusterConfiguration
properties:
  executeCommandConfiguration:
    $ref: "#/types/aws:ecs/ClusterConfigurationExecuteCommandConfiguration:ClusterConfigurationExecuteCommandConfiguration"
    description: |
      The details of the execute command configuration. Detailed below.
    language:
      python:
        mapCase: false
type: object
