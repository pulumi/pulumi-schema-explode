$token: aws:ecs/ServiceCapacityProviderStrategy:ServiceCapacityProviderStrategy
properties:
  base:
    type: integer
    description: |
      Number of tasks, at a minimum, to run on the specified capacity provider. Only one capacity provider in a capacity provider strategy can have a base defined.
    language:
      python:
        mapCase: false
  capacityProvider:
    type: string
    description: |
      Short name of the capacity provider.
    language:
      python:
        mapCase: false
  weight:
    type: integer
    description: |
      Relative percentage of the total number of launched tasks that should use the specified capacity provider.
    language:
      python:
        mapCase: false
type: object
required:
  - capacityProvider
