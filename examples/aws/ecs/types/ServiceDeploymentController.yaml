$token: aws:ecs/ServiceDeploymentController:ServiceDeploymentController
properties:
  type:
    type: string
    description: |
      Type of deployment controller. Valid values: `CODE_DEPLOY`, `ECS`, `EXTERNAL`. Default: `ECS`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
