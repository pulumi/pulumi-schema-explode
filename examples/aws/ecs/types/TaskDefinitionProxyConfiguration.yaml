$token: aws:ecs/TaskDefinitionProxyConfiguration:TaskDefinitionProxyConfiguration
properties:
  containerName:
    type: string
    description: |
      Name of the container that will serve as the App Mesh proxy.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  properties:
    type: object
    additionalProperties:
      type: string
    description: |
      Set of network configuration parameters to provide the Container Network Interface (CNI) plugin, specified a key-value mapping.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  type:
    type: string
    description: |
      Proxy type. The default value is `APPMESH`. The only supported value is `APPMESH`.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - containerName
