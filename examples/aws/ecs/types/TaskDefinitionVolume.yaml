$token: aws:ecs/TaskDefinitionVolume:TaskDefinitionVolume
properties:
  dockerVolumeConfiguration:
    $ref: "#/types/aws:ecs/TaskDefinitionVolumeDockerVolumeConfiguration:TaskDefinitionVolumeDockerVolumeConfiguration"
    description: |
      Configuration block to configure a docker volume. Detailed below.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  efsVolumeConfiguration:
    $ref: "#/types/aws:ecs/TaskDefinitionVolumeEfsVolumeConfiguration:TaskDefinitionVolumeEfsVolumeConfiguration"
    description: |
      Configuration block for an EFS volume. Detailed below.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  fsxWindowsFileServerVolumeConfiguration:
    $ref: "#/types/aws:ecs/TaskDefinitionVolumeFsxWindowsFileServerVolumeConfiguration:TaskDefinitionVolumeFsxWindowsFileServerVolumeConfiguration"
    description: |
      Configuration block for an FSX Windows File Server volume. Detailed below.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  hostPath:
    type: string
    description: |
      Path on the host container instance that is presented to the container. If not set, ECS will create a nonpersistent data volume that starts empty and is deleted after the task has finished.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      Name of the volume. This name is referenced in the `sourceVolume`
      parameter of container definition in the `mountPoints` section.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
type: object
required:
  - name
