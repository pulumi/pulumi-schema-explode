$token: aws:ecs/ClusterDefaultCapacityProviderStrategy:ClusterDefaultCapacityProviderStrategy
properties:
  base:
    type: integer
    description: |
      The number of tasks, at a minimum, to run on the specified capacity provider. Only one capacity provider in a capacity provider strategy can have a base defined.
    language:
      python:
        mapCase: false
  capacityProvider:
    type: string
    description: |
      The short name of the capacity provider.
    language:
      python:
        mapCase: false
  weight:
    type: integer
    description: |
      The relative percentage of the total number of launched tasks that should use the specified capacity provider.
    language:
      python:
        mapCase: false
type: object
required:
  - capacityProvider
