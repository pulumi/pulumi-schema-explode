$token: aws:ecs/ClusterConfigurationExecuteCommandConfiguration:ClusterConfigurationExecuteCommandConfiguration
properties:
  kmsKeyId:
    type: string
    description: |
      The AWS Key Management Service key ID to encrypt the data between the local client and the container.
    language:
      python:
        mapCase: false
  logConfiguration:
    $ref: "#/types/aws:ecs/ClusterConfigurationExecuteCommandConfigurationLogConfiguration:ClusterConfigurationExecuteCommandConfigurationLogConfiguration"
    description: |
      The log configuration for the results of the execute command actions Required when `logging` is `OVERRIDE`. Detailed below.
    language:
      python:
        mapCase: false
  logging:
    type: string
    description: |
      The log setting to use for redirecting logs for your execute command results. Valid values are `NONE`, `DEFAULT`, and `OVERRIDE`.
    language:
      python:
        mapCase: false
type: object
