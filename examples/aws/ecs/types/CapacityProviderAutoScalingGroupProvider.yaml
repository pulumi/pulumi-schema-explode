$token: aws:ecs/CapacityProviderAutoScalingGroupProvider:CapacityProviderAutoScalingGroupProvider
properties:
  autoScalingGroupArn:
    type: string
    description: |
      - ARN of the associated auto scaling group.
    language:
      python:
        mapCase: false
    willReplaceOnChanges: true
  managedScaling:
    $ref: "#/types/aws:ecs/CapacityProviderAutoScalingGroupProviderManagedScaling:CapacityProviderAutoScalingGroupProviderManagedScaling"
    description: |
      - Configuration block defining the parameters of the auto scaling. Detailed below.
    language:
      python:
        mapCase: false
  managedTerminationProtection:
    type: string
    description: |
      - Enables or disables container-aware termination of instances in the auto scaling group when scale-in happens. Valid values are `ENABLED` and `DISABLED`.
    language:
      python:
        mapCase: false
type: object
required:
  - autoScalingGroupArn
language:
  nodejs:
    requiredOutputs:
      - autoScalingGroupArn
      - managedScaling
      - managedTerminationProtection
