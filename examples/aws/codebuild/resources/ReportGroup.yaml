$token: aws:codebuild/reportGroup:ReportGroup
properties:
  arn:
    type: string
    description: |
      The ARN of Report Group.
  created:
    type: string
    description: |
      The date and time this Report Group was created.
  deleteReports:
    type: boolean
    description: |
      If `true`, deletes any reports that belong to a report group before deleting the report group. If `false`, you must delete any reports in the report group before deleting it. Default value is `false`.
  exportConfig:
    $ref: "#/types/aws:codebuild/ReportGroupExportConfig:ReportGroupExportConfig"
    description: |
      Information about the destination where the raw data of this Report Group is exported. see Export Config documented below.
  name:
    type: string
    description: |
      The name of a Report Group.
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value mapping of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  tagsAll:
    type: object
    additionalProperties:
      type: string
    description: |
      A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
  type:
    type: string
    description: |
      The export configuration type. Valid values are `S3` and `NO_EXPORT`.
required:
  - arn
  - created
  - exportConfig
  - name
  - tagsAll
  - type
inputProperties:
  deleteReports:
    type: boolean
    description: |
      If `true`, deletes any reports that belong to a report group before deleting the report group. If `false`, you must delete any reports in the report group before deleting it. Default value is `false`.
  exportConfig:
    $ref: "#/types/aws:codebuild/ReportGroupExportConfig:ReportGroupExportConfig"
    description: |
      Information about the destination where the raw data of this Report Group is exported. see Export Config documented below.
  name:
    type: string
    description: |
      The name of a Report Group.
    willReplaceOnChanges: true
  tags:
    type: object
    additionalProperties:
      type: string
    description: |
      Key-value mapping of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  type:
    type: string
    description: |
      The export configuration type. Valid values are `S3` and `NO_EXPORT`.
    willReplaceOnChanges: true
requiredInputs:
  - exportConfig
  - type
stateInputs:
  description: |
    Input properties used for looking up and filtering ReportGroup resources.
  properties:
    arn:
      type: string
      description: |
        The ARN of Report Group.
    created:
      type: string
      description: |
        The date and time this Report Group was created.
    deleteReports:
      type: boolean
      description: |
        If `true`, deletes any reports that belong to a report group before deleting the report group. If `false`, you must delete any reports in the report group before deleting it. Default value is `false`.
    exportConfig:
      $ref: "#/types/aws:codebuild/ReportGroupExportConfig:ReportGroupExportConfig"
      description: |
        Information about the destination where the raw data of this Report Group is exported. see Export Config documented below.
    name:
      type: string
      description: |
        The name of a Report Group.
      willReplaceOnChanges: true
    tags:
      type: object
      additionalProperties:
        type: string
      description: |
        Key-value mapping of resource tags. .If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
    tagsAll:
      type: object
      additionalProperties:
        type: string
      description: |
        A map of tags assigned to the resource, including those inherited from the provider `default_tags` configuration block.
    type:
      type: string
      description: |
        The export configuration type. Valid values are `S3` and `NO_EXPORT`.
      willReplaceOnChanges: true
  type: object
