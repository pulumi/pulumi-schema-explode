$token: aws:codebuild/ProjectSecondarySourceGitSubmodulesConfig:ProjectSecondarySourceGitSubmodulesConfig
properties:
  fetchSubmodules:
    type: boolean
    description: |
      Whether to fetch Git submodules for the AWS CodeBuild build project.
    language:
      python:
        mapCase: false
type: object
required:
  - fetchSubmodules
