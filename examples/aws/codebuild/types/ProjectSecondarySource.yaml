$token: aws:codebuild/ProjectSecondarySource:ProjectSecondarySource
properties:
  auth:
    $ref: "#/types/aws:codebuild/ProjectSecondarySourceAuth:ProjectSecondarySourceAuth"
    description: |
      Configuration block with the authorization settings for AWS CodeBuild to access the source code to be built. This information is for the AWS CodeBuild console's use only. Use the `aws.codebuild.SourceCredential` resource instead. Auth blocks are documented below.
    deprecationMessage: Use the aws_codebuild_source_credential resource instead
    language:
      python:
        mapCase: false
  buildStatusConfig:
    $ref: "#/types/aws:codebuild/ProjectSecondarySourceBuildStatusConfig:ProjectSecondarySourceBuildStatusConfig"
    description: |
      Contains information that defines how the build project reports the build status to the source provider. This option is only used when the source provider is `GITHUB`, `GITHUB_ENTERPRISE`, or `BITBUCKET`.
    language:
      python:
        mapCase: false
  buildspec:
    type: string
    description: |
      Build specification to use for this build project's related builds. This must be set when `type` is `NO_SOURCE`.
    language:
      python:
        mapCase: false
  gitCloneDepth:
    type: integer
    description: |
      Truncate git history to this many commits. Use `0` for a `Full` checkout which you need to run commands like `git branch --show-current`. See [AWS CodePipeline User Guide: Tutorial: Use full clone with a GitHub pipeline source](https://docs.aws.amazon.com/codepipeline/latest/userguide/tutorials-github-gitclone.html) for details.
    language:
      python:
        mapCase: false
  gitSubmodulesConfig:
    $ref: "#/types/aws:codebuild/ProjectSecondarySourceGitSubmodulesConfig:ProjectSecondarySourceGitSubmodulesConfig"
    description: |
      Configuration block. Detailed below.
    language:
      python:
        mapCase: false
  insecureSsl:
    type: boolean
    description: |
      Ignore SSL warnings when connecting to source control.
    language:
      python:
        mapCase: false
  location:
    type: string
    description: |
      Location of the source code from git or s3.
    language:
      python:
        mapCase: false
  reportBuildStatus:
    type: boolean
    description: |
      Whether to report the status of a build's start and finish to your source provider. This option is only valid when the `type` is `BITBUCKET` or `GITHUB`.
    language:
      python:
        mapCase: false
  sourceIdentifier:
    type: string
    description: |
      An identifier for a source in the build project.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      Type of repository that contains the source code to be built. Valid values: `CODECOMMIT`, `CODEPIPELINE`, `GITHUB`, `GITHUB_ENTERPRISE`, `BITBUCKET`, `S3`, `NO_SOURCE`.
    language:
      python:
        mapCase: false
type: object
required:
  - sourceIdentifier
  - type
