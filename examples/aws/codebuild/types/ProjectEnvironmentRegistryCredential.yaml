$token: aws:codebuild/ProjectEnvironmentRegistryCredential:ProjectEnvironmentRegistryCredential
properties:
  credential:
    type: string
    description: |
      ARN or name of credentials created using AWS Secrets Manager.
    language:
      python:
        mapCase: false
  credentialProvider:
    type: string
    description: |
      Service that created the credentials to access a private Docker registry. Valid value: `SECRETS_MANAGER` (AWS Secrets Manager).
    language:
      python:
        mapCase: false
type: object
required:
  - credential
  - credentialProvider
