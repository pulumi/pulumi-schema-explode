$token: aws:codebuild/ReportGroupExportConfig:ReportGroupExportConfig
properties:
  s3Destination:
    $ref: "#/types/aws:codebuild/ReportGroupExportConfigS3Destination:ReportGroupExportConfigS3Destination"
    description: |
      contains information about the S3 bucket where the run of a report is exported. see S3 Destination documented below.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      The export configuration type. Valid values are `S3` and `NO_EXPORT`.
    language:
      python:
        mapCase: false
type: object
required:
  - type
