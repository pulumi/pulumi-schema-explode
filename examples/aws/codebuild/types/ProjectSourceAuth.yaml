$token: aws:codebuild/ProjectSourceAuth:ProjectSourceAuth
properties:
  resource:
    type: string
    description: |
      Resource value that applies to the specified authorization type. Use the `aws.codebuild.SourceCredential` resource instead.
    deprecationMessage: Use the aws_codebuild_source_credential resource instead
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      Type of repository that contains the source code to be built. Valid values: `CODECOMMIT`, `CODEPIPELINE`, `GITHUB`, `GITHUB_ENTERPRISE`, `BITBUCKET`, `S3`, `NO_SOURCE`.
    deprecationMessage: Use the aws_codebuild_source_credential resource instead
    language:
      python:
        mapCase: false
type: object
required:
  - type
