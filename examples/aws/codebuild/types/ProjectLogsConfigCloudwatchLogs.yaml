$token: aws:codebuild/ProjectLogsConfigCloudwatchLogs:ProjectLogsConfigCloudwatchLogs
properties:
  groupName:
    type: string
    description: |
      Group name of the logs in CloudWatch Logs.
    language:
      python:
        mapCase: false
  status:
    type: string
    description: |
      Current status of logs in S3 for a build project. Valid values: `ENABLED`, `DISABLED`. Defaults to `DISABLED`.
    language:
      python:
        mapCase: false
  streamName:
    type: string
    description: |
      Stream name of the logs in CloudWatch Logs.
    language:
      python:
        mapCase: false
type: object
