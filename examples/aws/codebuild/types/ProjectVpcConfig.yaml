$token: aws:codebuild/ProjectVpcConfig:ProjectVpcConfig
properties:
  securityGroupIds:
    type: array
    items:
      type: string
    description: |
      Security group IDs to assign to running builds.
    language:
      python:
        mapCase: false
  subnets:
    type: array
    items:
      type: string
    description: |
      Subnet IDs within which to run builds.
    language:
      python:
        mapCase: false
  vpcId:
    type: string
    description: |
      ID of the VPC within which to run builds.
    language:
      python:
        mapCase: false
type: object
required:
  - securityGroupIds
  - subnets
  - vpcId
