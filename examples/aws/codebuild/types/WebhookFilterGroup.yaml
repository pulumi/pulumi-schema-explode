$token: aws:codebuild/WebhookFilterGroup:WebhookFilterGroup
properties:
  filters:
    type: array
    items:
      $ref: "#/types/aws:codebuild/WebhookFilterGroupFilter:WebhookFilterGroupFilter"
    description: |
      A webhook filter for the group. Filter blocks are documented below.
    language:
      python:
        mapCase: false
type: object
