$token: aws:codebuild/ProjectBuildBatchConfig:ProjectBuildBatchConfig
properties:
  combineArtifacts:
    type: boolean
    description: |
      Specifies if the build artifacts for the batch build should be combined into a single artifact location.
    language:
      python:
        mapCase: false
  restrictions:
    $ref: "#/types/aws:codebuild/ProjectBuildBatchConfigRestrictions:ProjectBuildBatchConfigRestrictions"
    description: |
      Specifies the restrictions for the batch build.
    language:
      python:
        mapCase: false
  serviceRole:
    type: string
    description: |
      Specifies the service role ARN for the batch build project.
    language:
      python:
        mapCase: false
  timeoutInMins:
    type: integer
    description: |
      Specifies the maximum amount of time, in minutes, that the batch build must be completed in.
    language:
      python:
        mapCase: false
type: object
required:
  - serviceRole
