$token: aws:codebuild/ProjectCache:ProjectCache
properties:
  location:
    type: string
    description: |
      Location of the source code from git or s3.
    language:
      python:
        mapCase: false
  modes:
    type: array
    items:
      type: string
    description: |
      Specifies settings that AWS CodeBuild uses to store and reuse build dependencies. Valid values:  `LOCAL_SOURCE_CACHE`, `LOCAL_DOCKER_LAYER_CACHE`, `LOCAL_CUSTOM_CACHE`.
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      Type of repository that contains the source code to be built. Valid values: `CODECOMMIT`, `CODEPIPELINE`, `GITHUB`, `GITHUB_ENTERPRISE`, `BITBUCKET`, `S3`, `NO_SOURCE`.
    language:
      python:
        mapCase: false
type: object
