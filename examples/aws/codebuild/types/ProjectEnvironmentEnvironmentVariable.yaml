$token: aws:codebuild/ProjectEnvironmentEnvironmentVariable:ProjectEnvironmentEnvironmentVariable
properties:
  name:
    type: string
    description: |
      Name of the project. If `type` is set to `S3`, this is the name of the output artifact object
    language:
      python:
        mapCase: false
  type:
    type: string
    description: |
      Type of repository that contains the source code to be built. Valid values: `CODECOMMIT`, `CODEPIPELINE`, `GITHUB`, `GITHUB_ENTERPRISE`, `BITBUCKET`, `S3`, `NO_SOURCE`.
    language:
      python:
        mapCase: false
  value:
    type: string
    description: |
      Environment variable's value.
    language:
      python:
        mapCase: false
type: object
required:
  - name
  - value
