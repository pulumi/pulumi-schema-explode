$token: aws:elasticbeanstalk/configurationTemplate:ConfigurationTemplate
properties:
  application:
    type: string
    description: |
      name of the application to associate with this configuration template
  description:
    type: string
    description: |
      Short description of the Template
  environmentId:
    type: string
    description: |
      The ID of the environment used with this configuration template
  name:
    type: string
    description: |
      A unique name for this Template.
  settings:
    type: array
    items:
      $ref: "#/types/aws:elasticbeanstalk/ConfigurationTemplateSetting:ConfigurationTemplateSetting"
    description: |
      Option settings to configure the new Environment. These
      override specific values that are set as defaults. The format is detailed
      below in Option Settings
  solutionStackName:
    type: string
    description: |
      A solution stack to base your Template
      off of. Example stacks can be found in the [Amazon API documentation](https://docs.aws.amazon.com/elasticbeanstalk/latest/dg/concepts.platforms.html)
required:
  - application
  - name
  - settings
inputProperties:
  application:
    type: string
    description: |
      name of the application to associate with this configuration template
    willReplaceOnChanges: true
  description:
    type: string
    description: |
      Short description of the Template
  environmentId:
    type: string
    description: |
      The ID of the environment used with this configuration template
    willReplaceOnChanges: true
  name:
    type: string
    description: |
      A unique name for this Template.
    willReplaceOnChanges: true
  settings:
    type: array
    items:
      $ref: "#/types/aws:elasticbeanstalk/ConfigurationTemplateSetting:ConfigurationTemplateSetting"
    description: |
      Option settings to configure the new Environment. These
      override specific values that are set as defaults. The format is detailed
      below in Option Settings
  solutionStackName:
    type: string
    description: |
      A solution stack to base your Template
      off of. Example stacks can be found in the [Amazon API documentation](https://docs.aws.amazon.com/elasticbeanstalk/latest/dg/concepts.platforms.html)
    willReplaceOnChanges: true
requiredInputs:
  - application
stateInputs:
  description: |
    Input properties used for looking up and filtering ConfigurationTemplate resources.
  properties:
    application:
      type: string
      description: |
        name of the application to associate with this configuration template
      willReplaceOnChanges: true
    description:
      type: string
      description: |
        Short description of the Template
    environmentId:
      type: string
      description: |
        The ID of the environment used with this configuration template
      willReplaceOnChanges: true
    name:
      type: string
      description: |
        A unique name for this Template.
      willReplaceOnChanges: true
    settings:
      type: array
      items:
        $ref: "#/types/aws:elasticbeanstalk/ConfigurationTemplateSetting:ConfigurationTemplateSetting"
      description: |
        Option settings to configure the new Environment. These
        override specific values that are set as defaults. The format is detailed
        below in Option Settings
    solutionStackName:
      type: string
      description: |
        A solution stack to base your Template
        off of. Example stacks can be found in the [Amazon API documentation](https://docs.aws.amazon.com/elasticbeanstalk/latest/dg/concepts.platforms.html)
      willReplaceOnChanges: true
  type: object
