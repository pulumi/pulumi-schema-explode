$token: aws:elasticbeanstalk/getApplicationAppversionLifecycle:getApplicationAppversionLifecycle
properties:
  deleteSourceFromS3:
    type: boolean
    description: |
      Specifies whether delete a version's source bundle from S3 when the application version is deleted.
    language:
      python:
        mapCase: false
  maxAgeInDays:
    type: integer
    description: |
      Number of days to retain an application version.
    language:
      python:
        mapCase: false
  maxCount:
    type: integer
    description: |
      Maximum number of application versions to retain.
    language:
      python:
        mapCase: false
  serviceRole:
    type: string
    description: |
      ARN of an IAM service role under which the application version is deleted.  Elastic Beanstalk must have permission to assume this role.
    language:
      python:
        mapCase: false
type: object
required:
  - deleteSourceFromS3
  - maxAgeInDays
  - maxCount
  - serviceRole
language:
  nodejs:
    requiredInputs: []
