$token: aws:sns/topicPolicy:TopicPolicy
properties:
  arn:
    type: string
    description: |
      The ARN of the SNS topic
  owner:
    type: string
    description: |
      The AWS Account ID of the SNS topic owner
  policy:
    type: string
    description: |
      The fully-formed AWS policy as JSON.
required:
  - arn
  - owner
  - policy
inputProperties:
  arn:
    type: string
    description: |
      The ARN of the SNS topic
    willReplaceOnChanges: true
  policy:
    type: string
    description: |
      The fully-formed AWS policy as JSON.
requiredInputs:
  - arn
  - policy
stateInputs:
  description: |
    Input properties used for looking up and filtering TopicPolicy resources.
  properties:
    arn:
      type: string
      description: |
        The ARN of the SNS topic
      willReplaceOnChanges: true
    owner:
      type: string
      description: |
        The AWS Account ID of the SNS topic owner
    policy:
      type: string
      description: |
        The fully-formed AWS policy as JSON.
  type: object
